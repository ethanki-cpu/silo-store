# CHANGELOG

## 2026-09-02 (HOTFIX-156.2 — 배경 슬라이드 패닝 속도 절반으로 완화 + 편집 UI 범위 보정)
- **사용자 재신고**: HOTFIX-156.1의 6초가 "너무 빠르다" — 속도를 절반으로 줄여달라는 요청 + 조절 기능 요청.
- **수정**: `DEFAULT_PAN_SPEED_SECONDS` 6→12초(속도 절반). 확대(220%)/이동 거리(32%)는 유지(모서리에 닿는 크기 자체는 그대로). 속도/확대/이동 거리를 슬라이드별로 직접 조절하는 기능은 이미 "배경 패닝 모션" 편집 섹션에 있었음(HOTFIX-147.27) — 다만 그 입력 필드의 `max`가 옛 기본값 기준(확대 80%/이동 30%)에 멈춰있어 새 기본값(220%/32%)보다 낮은 상한에 막혀있던 것을 확인, 확대 `max` 80→400/이동 거리 `max` 30→45로 함께 넓힘(관리자가 필요하면 기본값보다 더 크게도 설정 가능).
- **검증**: `npx tsc --noEmit`/`npm run lint` 0 errors. 로컬 dev 서버에서 `getComputedStyle(...).animationDuration`으로 실제 12s 적용 확인.

## 2026-09-02 (HOTFIX-156.1 — 배경 슬라이드 패닝 모션을 진짜 모서리까지 닿도록 재조정)
- **사용자 재신고**: EPIC-156 이후에도 배경 슬라이드 모션이 답답함 — 첨부 스크린샷(초상화에 왼쪽 상단 모서리→오른쪽 하단 모서리→위로 올라오는 경로를 그려서 첨부)처럼 "이미지 전체를 6초 안에" 보여줄 만큼 크게, 세로/가로 이미지에 맞게, 랜덤으로 모든 배경 슬라이드에 적용해달라는 요청.
- **원인**: 기존 "auto" 투어 모션(HOTFIX-147.29~152.19)이 이미 모서리를 도는 경로 자체는 갖고 있었지만, (1) 기본 속도가 20초로 느렸고, (2) 확대 65%/이동 거리 18%로는 안전 여유분 공식(`|이동%| <= 50*(zoom-1)/zoom`) 안에서도 한참 못 미쳐 실제로 모서리 근처까지 닿지 않았고, (3) 가로/세로 구분을 위해 짧은 축 이동을 35%까지 죽여놔서 대각선 정거장이 "모서리"가 아니라 애매하게 치우친 지점이 되고 있었음.
- **수정**: `DEFAULT_PAN_SPEED_SECONDS` 20→6초, `DEFAULT_PAN_ZOOM_PCT` 65→220%(3.2배 확대), `DEFAULT_PAN_DISTANCE_PCT` 18→32%(같은 안전 공식 기준 3.2배 확대에서 최대 34.4%까지 안전, 32%는 여유 있게 그 안). `buildTourVars`의 짧은 축 감쇠율도 0.35→0.65로 올려 대각선 정거장이 실제 모서리에 훨씬 가깝게 닿으면서도 가로/세로에 따른 방향 우세는 유지. 기존 "왼쪽 위→오른쪽 아래→오른쪽 위(직선으로 위)→되돌아감" 같은 경로 템플릿(TOUR_TEMPLATES) 자체는 이미 사용자가 그린 경로와 일치해 그대로 두고, 크기/속도만 실제로 체감되도록 키움. `CoverBackground` 공용 컴포넌트 하나만 고쳐 표지/이벤트 배경 전부(모든 타임라인)에 자동 적용됨.
- **검증**: `npx tsc --noEmit`/`npm run lint` 0 errors(81 warnings, 기존 기준선 유지). 로컬 dev 서버에서 2초 간격 스크린샷 비교로 이전보다 훨씬 크고 빠른 이동을 직접 확인.

## 2026-09-02 (EPIC-156 — 타임라인 밀집 시대 구간 확대 + 배경 슬라이드 랜덤 시작 + 폰트 선택 패널 확대)
- **사용자 신고 1(핵심)**: "제국~군주" 타임라인에서 르네상스/바로크/로코코처럼 짧은 시대가 여러 개 몰려있으면 라벨이 서로 겹치고, 로코코 이벤트가 2000년대를 넘는 것처럼 보임 — "촘촘한 연대는 좀 더 unit을 넓게 보였으면 좋겠다"는 요청.
  - **원인**: TimelineJS3의 TimeNav는 순수 선형 축(`TimeScale.getPosition = (t - earliest) * pixels_per_milli`)이라 실제 연대 비율 그대로 그린다 — 로마제국(622년)/비잔틴(977년) 같은 긴 시대 옆에 르네상스(250년)/바로크(150년)/로코코(65년)가 붙으면 뒤쪽 세 시대가 극단적으로 눌린다. 실측 결과 "전체를 한 화면에" 보여주는 현재 뷰포트엔 여유 공간이 사실상 없어(로코코 끝이 화면 오른쪽 경계에 거의 닿아있음), 촘촘한 구간만 넓히려면 반드시 성긴 구간에서 그만큼을 가져와야 함을 확인 — 사용자 승인("성긴 구간은 8.7% 정도 줄어도 되니 촘촘한 구간을 30% 넓혀줘")에 따라 진행.
  - **구현**: `src/components/timeline/SiloTimelineInner.tsx`에 `buildDensityWarp`(구간 경계별 밀도를 평균과 비교해 촘촘한 구간은 30% 확대, 성긴 구간은 평균 밀도까지만 축소해 되찾아오는 구간별 선형 보간 함수 생성) + `warpTimeScale`(TimeNav의 `TimeScale._positions`와 `getPosition`에 동일하게 적용해 마커와 축 눈금이 계속 일치) + `installDensityWarp`(TimeNav가 컨테이너 크기를 다시 알게 될 때마다 `_getTimeScale()`을 통째로 새로 만드는 것을 감싸, 재계산될 때마다 다시 적용되도록 설치) 신설. "group" 모드(허브 페이지의 시대 슬라이드)와 "board" 모드(개별 게시글 타임라인) 둘 다 거치는 공용 컴포넌트에 넣어 온라인 도슨트의 모든 1단계/2단계 하위 타임라인에 자동 적용됨. 이벤트 3개 미만이거나 뚜렷한 밀집/성김 차이가 없으면 조용히 원래 선형 그대로 둔다.
  - **실측 난항**: `new TL.Timeline(...)`이 반환된 직후엔 `instance._timenav`가 아직 생성자 맨 앞의 빈 자리표(`{}`)뿐이고, 실제 TimeNav로 교체되는 시점도 "change" 이벤트 직후가 아니라 그보다 더 지나야(컨테이너가 실제 레이아웃 폭을 알게 된 뒤) 안정된다는 것을 로컬 dev 서버 디버그 로그로 직접 확인 — 기존 `alignTimeNavToOverview`와 동일한 1150ms 지연 지점(TL3 첫 배치 애니메이션 종료 시점)에서 설치하도록 맞춤.
  - **검증**: 로컬 dev 서버에서 실제 마커 픽셀 폭을 직접 측정(`getBoundingClientRect`) — 르네상스 82.1→106.7px(+30%), 바로크 49.3→64.0px(+30%), 로코코 21.3→27.8px(+30%), 로마제국 204.3→188.7px/비잔틴 320.8→304.2px(합쳐서 -6.1%)로 정확히 설계대로 동작 확인. 스크린샷으로 라벨 겹침 해소 확인.
- **사용자 신고 2**: 모든 타임라인의 "배경 슬라이드"가 항상 제일 먼저 업로드한 파일부터 시작함(자동 전환 순서 자체는 이미 무작위화돼 있었지만 최초 진입 슬라이드는 인덱스 0 고정이었음) — 랜덤으로 시작하게 해달라는 요청. `SiloTimelineEmbedBlock.tsx`의 `CoverBackground`가 쓰는 `current` state의 초기값을 마운트 시점에 한 번 무작위로 고르도록 수정(표지/이벤트 오버레이 배경 전부 공용).
- **사용자 신고 3**: "홈페이지 설정"의 커스텀 폰트 선택 드롭다운이 너무 좁고 짧아 답답함(첨부 스크린샷) — `FontPicker.tsx` 패널을 `w-64`(256px)→`w-[26rem]`(416px), 폰트 목록 `max-h-48`(192px)→`max-h-[30rem]`(480px)로 확대.
- **검증**: `npx tsc --noEmit`/`npm run lint` 0 errors(81 warnings, 기존 기준선 유지).
- **다음에 확인 필요**: 관리자가 실제로 다른 온라인 도슨트 하위 타임라인(고대 문명~침략, 혁명~식민지 등)에서도 밀집 구간이 자연스럽게 넓어지는지, 배경 슬라이드가 매번 다른 이미지로 시작하는지, 폰트 선택 패널이 실사용에 편해졌는지 확인.

## 2026-09-02 (Security Advisor 후속 — posts author_id 하이재킹 취약점 + newsletter 무제한 UPDATE 정책 수정, DB 전용)
- **배경**: 앞선 Security Advisor 점검(같은 날짜, 아래 항목)에서 "RLS Policy Always True" warning 4건(`client_error_logs`/`newsletter_subscribers`×2/`posts`)을 하나씩 원인 조회한 결과, 2건이 실제 악용 가능한 취약점으로 확인됨.
- **[치명적, 즉시 수정] `posts` — author_id 자기지정으로 타인 게시글 하이재킹 가능**: `posts` UPDATE는 RLS가 무조건 허용(`qual=true`)이고 실제 보호는 `posts_protect_content_columns()` 트리거(HOTFIX-152.21 이전, EPIC-079-phase-1에서 신설) 하나에 전적으로 의존하는 구조인데, 이 트리거가 "본인 글인가"를 판단할 때 **수정 요청에 담긴 새(new) author_id**로 확인하고 있었다 — 로그인한 임의 계정이 `{author_id: 본인_id, title: "...", body: "..."}`처럼 author_id를 자기 것으로 같이 바꿔 보내면 트리거가 "본인 글"로 오판해 title/body를 포함한 전체 콘텐츠 변조를 허용해버리는 구조였다(앱 화면은 이런 요청을 안 보내지만 Supabase REST API에 로그인 토큰만 있으면 직접 가능 — 실측 재현은 하지 않고 코드/함수 정의 검토로 확인, 실 데이터 훼손 방지). **수정**: 트리거의 소유자 판정 기준을 new.author_id → **old.author_id**로 변경 + `author_id` 자체도 보호 대상 컬럼에 추가(관리자만 변경 가능, 기존 HOTFIX-099 의도와 일치). `CREATE OR REPLACE FUNCTION`으로 로직만 교체, 트리거 연결/시그니처는 그대로라 정상적인 본인 글 수정·관리자 작성자 변경 플로우엔 영향 없음.
- **[실제 방치된 구멍, 삭제] `newsletter_subscribers` — 죽은 정책인데 무제한으로 열려있었음**: `newsletter_public_resubscribe`(UPDATE, `qual=true, with_check=true`, 컬럼 제한 없음) 정책이 존재했는데, 코드 전체 검색 결과 앱 어디서도 이 테이블에 UPDATE를 호출하는 곳이 없었음(`/api/newsletter/subscribe`는 INSERT만 하고 unique 위반은 그냥 성공 취급하는 방식으로 구현돼 있어 애초에 UPDATE 경로 자체를 쓰지 않음, 라우트 코드 주석에도 명시). 즉 아무 기능도 안 쓰는데 로그인 없이 누구나 구독자 이메일/구독일시를 임의로 덮어쓸 수 있는 상태로 방치돼 있었던 것 — `DROP POLICY`로 제거(기능 영향 없음, SELECT는 이미 관리자 전용 정책이 별도로 있어 그대로 안전).
- **[안전 확인, 손 안 댐] `client_error_logs` INSERT 무제한**: 클라이언트 에러 텔레메트리(HOTFIX-152.4) 용도로 로그인 여부와 무관하게 받아야 해서 의도된 설계 — 스팸성 대량 삽입 우려는 있으나 이번 범위 밖으로 판단, 필요시 추후 rate limit 검토.
- **[의도된 설계, 손 안 댐] Storage 버킷 5개 전부 "Public Bucket Allows Listing"**: `avatars`/`attachments`/`gallery`/`post-images`/`public-assets` 전부 `public=true`로 설계된 버킷(공개 URL 서빙이 목적) — 공개 버킷은 구조적으로 listing도 함께 허용되는 Postgres/Supabase Storage의 기본 동작이라 버킷을 비공개로 바꾸지 않는 한 없앨 수 없는 항목. 전부 실제로 공개돼야 하는 콘텐츠라 문제 없음.
- **미사용 테이블 9개(RLS 정책 없음)는 사용자 지시로 방치**: "나중에 필요할지도 모르니 지금 문제 안 되면 그냥 둬" — 코드 전체에서 참조하는 곳이 없어(정책 없음 = 항상 빈 배열 반환) 현재 기능 영향 0건 확인, 그대로 둠. `drink_menu`만 5행 데이터 존재, 나머지 8개는 0행.
- **검증**: `posts_protect_content_columns` 함수 정의에 `old.author_id` 반영 확인, `newsletter_public_resubscribe` 정책이 `pg_policies`에서 사라진 것 확인.

## 2026-09-02 (Security Advisor 점검 — 뷰 3개 과다 권한 회수 + 함수 search_path 고정, DB 전용)
- **사용자 신고**: Supabase Security Advisor에 error 3건/warning 다수 — 다 고쳐야 하는지 문의.
- **실제 위험도 확인 결과(하나씩 원인 조회 후 판단)**:
  - **[실제 버그, 즉시 수정]** `public_profiles`/`poll_option_counts`/`docent_content_popularity` 3개 뷰 모두 `anon`/`authenticated`에 SELECT뿐 아니라 **INSERT/UPDATE/DELETE/TRUNCATE까지 GRANT되어 있었음**(언제 생겼는지는 불명, 아마 뷰 생성 시 기본 권한이 그대로 남은 것으로 추정) — 그중 `public_profiles`는 `SELECT id, name, avatar_url FROM members` 단순 단일 테이블 뷰라 Postgres가 자동으로 "updatable view"로 취급해, `anon`이 `/rest/v1/public_profiles`에 직접 PATCH/DELETE를 보내면 뷰 소유자(postgres) 권한으로 `members`의 RLS(본인 행만)를 우회해 **다른 회원의 이름/아바타를 수정하거나 삭제할 수 있는 실제 권한 상승 취약점**이었다. 코드 전체를 확인해 이 3개 뷰는 어디서도 write하지 않음(전부 `.select()`)을 확인 후, `anon`/`authenticated`의 write 계열 권한을 전부 REVOKE(SELECT만 남김) — Management API로 즉시 적용, 애플리케이션 동작 변화 없음(원래도 안 쓰던 권한).
  - **[의도된 설계, 그대로 둠]** 위 3개 뷰의 "SECURITY DEFINER View" 자체(뷰가 RLS를 우회하는 것)는 각각 의도된 동작 — `public_profiles`는 다른 회원 이름 노출용(`members` 직접 노출 대신 쓰는 문서화된 패턴), `poll_option_counts`/`docent_content_popularity`는 개별 투표/구매 행을 노출하지 않고 집계 숫자만 보여주기 위해 RLS를 우회해야 함. `security_invoker`로 바꾸면 기능 자체가 깨짐(본인 것만 보이게 됨) — 손대지 않음.
  - **[의도된 설계, 그대로 둠]** `is_current_user_admin()`이 `anon`/`authenticated`에 EXECUTE 권한이 열려있다는 warning — EPIC-071에서 RLS 정책 자기참조 무한 재귀를 피하려고 일부러 이렇게 설계한 함수(RLS 정책이 호출하는 role 자신이 EXECUTE 권한을 가져야 정책 평가가 됨). 여기서 EXECUTE를 뺏으면 `members` admin 우회 정책이 전부 깨져 사이트 전역 렌더링 장애(EPIC-071 사고 재현) 위험 — 손대지 않음.
  - **[실제 위험 없음, 손 안 댐]** `handle_new_user()`/`posts_protect_content_columns()`도 같은 warning(anon/authenticated가 SECURITY DEFINER 함수를 RPC로 실행 가능) — 다만 둘 다 반환 타입이 `trigger`라 Postgres가 트리거 컨텍스트 밖에서 호출 자체를 거부한다(권한 여부와 무관하게 직접 호출 불가능). 실제 악용 경로가 없어 시급하지 않고, 트리거 실행 메커니즘에 원치 않는 영향을 줄 수 있어 이번엔 EXECUTE를 건드리지 않음.
  - **[안전하게 수정]** `handle_new_user`/`slugify` 함수의 `search_path`가 고정 안 돼 있던 warning(스키마 재정의를 이용한 공격 방지 목적) — `alter function ... set search_path = public`으로 즉시 수정, 동작 변화 없음.
  - **[Free 플랜 제약, 보류]** "유출된 비밀번호 검사"(HaveIBeenPwned 연동) 활성화는 Supabase Pro 플랜부터 지원 — 현재 Free 플랜이라 API가 402로 거부함. Pro로 업그레이드하면 바로 켤 수 있음.
  - **[INFO, 기능 영향 없음, 방치]** `docent_tour_bookings`/`docent_tours`/`drink_menu`/`drink_orders`/`member_profiles`/`salon_event_rsvps`/`salon_events`/`salon_room_access`/`salon_rooms` 9개 테이블이 RLS는 켜져 있는데 정책이 하나도 없음(항상 빈 배열 반환) — 코드 전체 검색 결과 이 9개 테이블을 참조하는 곳이 하나도 없어(아직 안 만든 기능이거나 폐기된 기능의 잔재로 추정) 현재 기능 영향 0건, 이번엔 손대지 않음.
- **검증**: 3개 뷰 재조회로 `anon`/`authenticated` 권한이 SELECT만 남은 것 확인. 코드 전체에서 이 3개 뷰에 대한 write 호출이 없음을 grep으로 재확인(회귀 없음).
- **다음에 확인 필요**: (1) 9개 미사용 테이블 — 곧 쓸 예정이면 RLS 정책 추가, 폐기됐으면 테이블 자체를 삭제할지 결정, (2) Pro 플랜으로 업그레이드하면 유출 비밀번호 검사도 켤 것.

## 2026-09-02 (HOTFIX-154.2 — `public-assets` 버킷도 Cloudflare R2로 마이그레이션, DB 전용)
- **배경**: EPIC-154가 게시글/Craft 타임라인/Silo Planet은 R2로 옮겼지만 `public-assets`(로고/히어로 슬라이드쇼/좌우·상단 사이드바 아이콘/커스텀 폰트 등 "홈페이지 설정 관리"의 업로드)는 범위 밖으로 남겨뒀었다 — 사이트 모든 페이지에서 매번 로드되는 자산이라 Cached Egress 기여도가 가장 큰데도 빠져있었던 것으로 판단, 사용자 승인 하에 진행.
- **실측**: `storage.objects`로 버킷 전체 88개 파일 확인, `site_settings`(hero_slideshow/main_logo/sidebar_icons/top_bar_icons/top_sidebar 5개 키)와 `custom_fonts`를 전수 조회해 실제 참조되는 URL 56개 추출 — 그중 55개는 정상 참조, 1개(`sidebar_icons.rightIconUrl`이 가리키는 `logos/bg-a759e5a8-....png`)는 **이미 이번 작업 이전부터 깨져있던 참조**(다운로드 시도 시 400, 버킷에 파일 자체가 없음 — 마이그레이션과 무관한 기존 버그, 아래 "다음에 확인 필요" 참고). `custom_fonts`는 전부 R2 URL만 있어 손댈 것 없음.
- **마이그레이션(55개)**: 다운로드 → R2 업로드(`media/migrated-public-assets/` 프리픽스) → 응답 바이트 수 일치로 검증 → `site_settings` 5개 키의 JSON 안 URL을 전부 새 R2 URL로 치환(Management API, `update ... set setting_value = $mig$...$mig$::jsonb`로 따옴표 이스케이프 문제 회피). 재조회로 `site_settings` 전체에 남은 Supabase Storage 참조가 위 깨진 1건 외엔 0건임을 확인.
- **원본 삭제는 이번엔 보류**: EPIC-154의 Silo Planet 케이스는 관리자 브라우저 세션의 access token으로 Storage API를 통해 정식 삭제했는데, 이번 세션엔 로그인된 관리자 세션이 없어 그 방식을 못 썼다 — Management API(postgres 권한)로 `storage.objects` 행을 직접 지우는 건 실제 백엔드 바이트 삭제/사용량 정산이 Storage API를 통한 삭제와 다르게 반영될 수 있어 위험 부담이 있다고 판단해 하지 않음. `public-assets`는 현재 Storage Size(0.5GB 중극히 일부)만 차지할 뿐이라 급하지 않다고 보고, DB 참조가 전부 R2로 넘어가 Cached Egress를 실제로 줄이는 목적은 이미 달성됨.
- **orphan 32개(참조 없음, 이번엔 손 안 댐)**: `logos`/`top_bar_icons_hover`/`top_sidebar_trigger`/`wallpaper` 등에 남은, 현재 어떤 `site_settings`에서도 참조하지 않는 파일 32개는 EPIC-154의 "98개 미확인 파일"과 같은 성격 — 지우기 전 확인 필요로 남겨둠.
- **다음에 확인 필요**: (1) 관리자 세션으로 로그인한 채 다시 요청하면 이번에 마이그레이션한 55개 Supabase 원본을 Storage API로 정식 삭제 진행, (2) `sidebar_icons`의 `rightIconUrl`이 가리키던 이미지가 이미 사라져있던 건 — 실제 사이트에서 우측 사이드바 아이콘 하나가 깨진 이미지로 보일 가능성이 있음, 관리자가 "홈페이지 설정"에서 해당 아이콘 이미지를 다시 업로드해야 함(이번 작업으로 생긴 문제 아니라 원래부터 깨져있던 것), (3) orphan 32개 삭제 여부 결정.

## 2026-09-02 (HOTFIX-154.1 — 타임라인 배경 슬라이드 다중 업로드가 특정 영상 파일에서 영원히 멈추는 버그 수정)
- **사용자 신고**: Craft 에디터에서 "아르누보"/"바로크" 타임라인 이벤트의 "배경 슬라이드"에 영상을 다량(266개/53개) 업로드하는 중 각각 `[174/266]`/`[34/53]`에서 "영상 길이 확인 중..."에 멈춘 채 진행이 안 됨 — 영상이 너무 커서 그런 건지, 업로드 용량 상한을 100MB로 올려야 하는지 문의.
- **원인**: [videoCompress.ts](src/lib/videoCompress.ts)의 `readVideoDurationSeconds()`가 `<video>` 엘리먼트로 로컬에서 영상 길이를 읽는 단계(네트워크/업로드와 무관)인데, 여기에 타임아웃이 없었다 — 브라우저가 메타데이터를 못 읽는 파일(코덱/컨테이너 문제, 손상된 파일 등)을 만나면 `onloadedmetadata`도 `onerror`도 영영 안 불려 그 파일에서 배치 전체가 무한 대기에 빠진다. 이 경로엔 애초에 업로드 용량 상한이 없어(무제한 압축 시도) 100MB로 올려도 이 증상은 그대로다.
- **수정**: `readVideoDurationSeconds()`에 20초 타임아웃 추가 — 시간 초과 시 에러로 처리해 해당 파일만 실패 목록에 남기고 배치 루프가 다음 파일로 넘어가도록 함.
- **겸사겸사 확인**: 로컬 작업 브랜치(`feature/EPIC-153`)가 전날 R2 마이그레이션 커밋(EPIC-154)보다 2커밋 뒤처져 있어, `main`을 최신으로 받아 그 위(`feature/HOTFIX-154.1`)에서 작업 — 이제 이 경로의 신규 업로드는 EPIC-154에서 이미 R2로 전환된 `uploadFileToR2`를 그대로 씀(회귀 없음, `git show`로 diff 확인).
- **검증**: `npx tsc --noEmit`/`npm run lint` 0 errors(81 warnings, EPIC-155의 기존 신규 경고 1개 포함, 이번 변경으로 추가된 경고 없음).
- **다음에 확인 필요**: 관리자가 실제로 멈췄던 그 두 배치를 다시 시도해, 타임아웃에 걸리는 파일이 정확히 몇 번째/어떤 파일인지 확인하고 필요하면 그 파일만 재인코딩 후 재업로드할 것 — 20초는 임의로 정한 값이라 정상적으로 크지만 느린 파일까지 오탐으로 실패 처리할 가능성은 남아있음(그럴 경우 타임아웃 값을 늘리는 것으로 조정).

## 2026-09-01 (EPIC-155 — 에디토리얼 매거진 스타일 인터랙티브 출석체크/이벤트 캘린더 컴포넌트 신설)
- **사용자 지시**: 첨부 이미지 3장(① 7일 그리드 기본 구조, ② Cake Wines 포스터의 톤앤매너, ③ 날짜 셀↔팝업 카드를 잇는 SVG 커넥터 인터랙션 스케치)을 참고해, 기존 달력 라이브러리의 기본 스타일을 쓰지 않는 하이엔드 캘린더 컴포넌트를 새로 만들어달라는 요청.
- **구현**: `src/components/attendance/EditorialCalendar.tsx`(신규) — 7일 그리드에 두꺼운 흑백 테두리, 초대형 볼드 날짜 타이포그래피, 세로 텍스트(`writing-mode: vertical-rl`) 액센트를 적용. 출석/이벤트가 있는 날짜는 결정론적 흑백 그라디언트(날짜+제목 해시 기반, 실제 이미지 자산 없이도 항상 같은 톤 유지)로 채운 썸네일 그리드를 셀 안에 표시. 날짜 클릭 시 오른쪽 여백 레일에 팝업 카드가 뜨고, `getBoundingClientRect()`로 클릭된 셀과 팝업의 좌표를 계산해 둘을 잇는 곡선을 SVG로 그린다 — Framer Motion(신규 의존성 추가)으로 팝업은 페이드+스케일, 커넥터 선은 `pathLength` 0→1 드로잉 애니메이션. 모바일(md 미만)은 여백 레일 대신 그리드 바로 아래에 팝업이 붙고 커넥터 선은 숨김.
- **데이터 구조**: `src/components/attendance/editorialCalendarTypes.ts`(신규) — `CalendarEntry`(type: "attendance" | "event", title/description/meta) 하나의 타입으로 출석체크와 사일로 이벤트를 동일하게 표현, 한 날짜에 여러 항목(예: 출석+이벤트 동시)도 팝업에 스택으로 표시. `buildDummyEntries(year, month)`로 시연용 더미 데이터 생성.
- **시연 페이지**: `src/app/attendance/editorial-preview`(신규, 로그인 불필요) — 실제 `/attendance`(로그인 필요, 실 데이터 연동)는 건드리지 않고 별도 경로에서 더미 데이터로 바로 확인 가능하게 분리. 이 컴포넌트가 승인되면 다음 단계로 실제 출석체크 페이지에 연동.
- **실측 중 발견/수정한 버그**: 서로 다른 날짜를 연속으로 클릭하면 이전 팝업이 사라지는 애니메이션과 새 팝업이 나타나는 애니메이션이 같은 자리에서 겹쳐(크로스페이드) 두 카드 내용이 잠깐 이중노출로 보이는 문제 — 세 곳의 `AnimatePresence`에 `mode="wait"`를 적용해 이전 팝업이 완전히 사라진 뒤 다음 팝업이 나타나도록 수정.
- `tsc` 0 errors, `lint` 0 errors(81 warnings — 기존 80 기준선 + DOM 좌표 측정 후 상태 갱신이라는, 이 저장소에 이미 여러 곳 있는 것과 동일한 종류의 경고 1개 추가, 신규 규칙 위반 아님). 로컬 dev 서버에서 날짜 클릭→팝업+커넥터 등장, 한 날짜에 항목 2개 스택 표시, 연속 클릭 시 크로스페이드 버그 수정까지 실측 확인.
- **다음에 확인 필요**: 관리자가 실제 화면에서 디자인 톤을 최종 승인하면 `/attendance` 페이지에 실제 데이터(`/api/attendance`)와 연동하고 사일로 이벤트 데이터 소스(어느 테이블에서 가져올지)를 정해야 함 — 이번 세션은 컴포넌트 자체의 완결된 시연까지만 범위.

## 2026-09-01 (EPIC-154 — Supabase Storage → Cloudflare R2 마이그레이션, 앞으로의 업로드도 전부 R2로 전환)
- **배경**: Supabase 무료 플랜의 Cached Egress가 한도(5GB) 대비 155%(7.74GB)로 초과된 상태에서, 실제로 뭐가 얼마나 차지하는지 확인하고 Supabase(DB/텍스트)와 Cloudflare(미디어)로 역할을 정리해달라는 사용자 요청.
- **실측 결과**: `storage.objects`를 직접 조회해 버킷별 실제 용량 확인 — `post-images` 944MB(354개, 예상보다 압도적으로 큼), `public-assets` 130MB(88개), `attachments` 106MB(20개), `gallery` 12MB(24개).
- **Silo Planet 3D 마이그레이션**: `site_settings.about_silo_universe`에 박혀있던 레거시 Supabase Storage URL 19개(모델 9개 .glb + 배경/우주 이미지 10개, ~61MB) 전부 다운로드→R2 재업로드→URL 교체(HTTP 200/바이트수 일치 확인) 후, 원본은 관리자 세션으로 Storage API를 통해 정식 삭제(`attachments` 106MB→47MB, `gallery` 12MB→9.6MB로 감소 확인).
- **post-images 실사용 콘텐츠 마이그레이션(256개 파일)**: 354개 중 실제로 살아있는 콘텐츠에서 참조되는 파일만 골라 이전 —
  - `posts` 테이블(본문 HTML/`body_json`/대표 이미지)에서 실제 참조되는 34개 파일이 걸린 게시글 8개를 찾아 마이그레이션. Management API(SQL)로는 `posts_protect_content_columns()` 트리거(HOTFIX-152.21에서 추가된 콘텐츠 보호 장치)에 막혀, 브라우저에 이미 로그인된 관리자 세션의 access token을 그대로 재사용해 정식 REST API(PostgREST) 경로로 수정.
  - `page_builder.craft_state`에서 참조되는 222개 파일(온라인 도슨트 타임라인 배경/표지 이미지 13개 페이지 분, 오늘 만든 타임라인 배경 패닝 기능이 쓰는 바로 그 이미지들)도 동일한 방식으로 마이그레이션.
  - 두 그룹 합쳐 256개 전부 R2 재업로드 + DB 참조 교체 + 잔여 legacy URL 0건 확인.
- **앞으로 업로드되는 모든 파일이 R2로 가도록 전환**: `src/lib/storage.ts`(Supabase Storage)의 `uploadFile`/`uploadPostImage`/`uploadMultipleFiles`를 여전히 쓰던 호출부 14곳(게시글 대표 이미지 `PostForm.tsx`, 게시판 썸네일 `BoardForm.tsx`, 네이티브 타임라인 위젯 `TimelineWidgetEditor.tsx`, 갤러리 다중 업로드 `GalleryConfigModal.tsx`, Craft 블록 9종 — 히어로/배경/슬라이드쇼/이미지+텍스트/콜라주/타임라인 표지/소셜프루프/테스티모니얼)를 전부 `src/lib/r2Upload.ts`의 새 `uploadFileToR2`/`uploadMultipleFilesToR2`(기존 `storage.ts`의 `{url, path, error}` 반환 모양을 그대로 맞춘 드롭인 대체)로 교체. `BlockEditor.tsx`의 게시글 본문 이미지 업로드는 이미 R2(EPIC-082/083)였으므로 변경 없음.
- **덤으로 확인/해결한 것**: 사용자가 "홈페이지 설정에 추가한 상단 아이콘 2개가 안 보인다"고 신고 — 코드 버그가 아니라 로컬 dev 서버가 그 기능이 없는 옛 브랜치(`feature/EPIC-151`)를 보고 있던 것이었고, `main`으로 전환해 즉시 해결 확인.
- `tsc` 0 errors, `lint` 0 errors(80 warnings — 기존 `main` 기준선, 이번 변경으로 추가된 경고 없음: `editable.tsx`의 이제 안 쓰는 `uploadFolder` prop은 destructuring에서 제거해 경고 없이 정리).
- **다음에 확인 필요**: 관리자가 실제 화면에서 새 게시글 작성/게시판 썸네일/Craft 블록 이미지 업로드를 한 번씩 해보고 R2에 정상 저장되는지 육안 확인(이번 세션은 코드 경로 검증까지만 하고 실제 파일 업로드 UI 클릭 테스트는 못 함). `post-images`에 남은 나머지 98개 파일(코드 전체를 훑어도 참조를 못 찾음 — 삭제된 게시글의 잔재로 추정)은 삭제 전 재확인 필요.

## 2026-08-31 (HOTFIX-152.21 — "제국~군주"/"혁명~식민지" 페이지 콘텐츠 통째 삭제 사고 복구 + 재발 방지 안전장치 2중 추가)
- **사용자 신고**: "'온라인 도슨트'의 바로 아래 카테고리에서 타임라인을 보면... 왜 '제국~군주' 페이지에 타임라인이 없고, '혁명~식민지' 페이지에도 타임라인이 없는거지?" — 이어서 사고 경위를 설명하자 "내가 그런일 나타나게 하지 말라고 수정을 요청한건데... 이런일이 안생기려면 어떻게 하냐고 분석해. 기본 타임 라인 다시 만들어. 형제 허브 구조 참고해서."
- **실제로 벌어진 일**: `site_navigations`(메뉴 항목)는 멀쩡했지만 그게 가리키던 실제 `page_builder` 행(craft_state 포함 타임라인 설정)이 두 페이지 다 DB에서 완전히 삭제돼 있었다 — 단순 설정 문제가 아니라 데이터 삭제 사고였다.
- **근본 원인(재구성)**: "사이트 구성 관리"의 `ensureUnassignedPagesInTree`는 매번 화면을 열 때마다 "어느 메뉴에도 안 걸린 page_builder 페이지"를 자동으로 찾아 "미분류 페이지" 버킷 아래 항목으로 만들어 넣는다 — 그런데 이 판정이 href→slug 문자열 매칭에만 의존해서, href가 일시적으로 어긋난 순간(예: HOTFIX-152.16 수정 전, 관리자가 "제국~군주"의 href를 편집하던 그 짧은 창)에 실제로는 멀쩡히 연결된 진짜 페이지를 "미분류"로 오판해 버킷 아래에 유령 중복 링크를 만들어 넣을 수 있었다. **문제는 이 유령이 한 번 생기면 나중에 href가 정상으로 돌아와도 절대 스스로 정리되지 않는다는 것** — 계속 그 자리에 남아 있다가, 관리자가 "미분류 페이지" 목록에서 이 중복 항목을 보고 "정리해야겠다"며 삭제를 눌렀고, `deleteRow`가 "미분류 버킷 아래 항목 삭제 = 그 항목이 가리키는 실제 page_builder 페이지까지 삭제"로 설계돼 있어(EPIC-084, 원래는 진짜 미분류 항목을 지울 때만 쓰라고 만든 로직) — href↔slug가 지금은 실제로 일치하는지 전혀 재확인하지 않고 곧바로 실제 콘텐츠를 지워버렸다.
- **재발 방지(2중 안전장치, 코드)**:
  1. `src/app/api/admin/pages/[id]/route.ts`(DELETE) — 삭제 직전에 "지금 이 순간" 이 페이지의 slug를 실제로 가리키는 **활성** 메뉴 항목이 있는지 다시 확인해서, 있으면 409로 삭제를 거부한다(가장 중요한 최종 방어선 — 유령이 어떻게 생기든 이 지점만 지키면 실제 콘텐츠는 절대 안 지워진다).
  2. `src/components/admin/CategoryTreeManager.tsx`의 `ensureUnassignedPagesInTree` — "미분류" 판정 시 미분류 버킷 자기 자신의 자식(href=`/${slug}`가 자기 자신과 매칭돼버리는 자기참조 오염)을 제외한 "진짜" 연결 목록만으로 판정하도록 수정하고, 이미 버킷에 들어간 항목 중 지금은 실제로 다른 메뉴가 가리키는(=해소된) 유령을 화면을 열 때마다 자동으로 정리하도록 추가 — 유령이 애초에 오래 쌓이지 않게 됨.
  3. `deleteRow`/`bulkDeleteSelected` — API가 409(`stillLinked`)를 반환하면 실제 페이지는 그대로 두고 유령 링크(중복 site_navigations 행)만 지우도록 수정, 관리자에게 "이미 다른 메뉴에서 쓰이는 페이지라 링크만 지웠다"고 안내.
- **복구**: 두 페이지 다 형제 허브(고대 문명~침략/프로이트~대중문화/디지털~A.i 문화)와 동일한 구조(`SiloTimelineEmbedBlock`, `mode="group"`, 해당 `groupHref`)로 새 `page_builder` 행을 재생성 — 하위 카테고리(로마제국/비잔틴제국/르네상스/바로크/로코코, 신고전주의/리전시/빅토리안/인상파) 자체는 삭제된 적이 없어 그대로 살아있었으므로, 대시보드에 5개/4개 시대 카드가 다시 정상적으로 모여 보인다. **다만 원본에 있었을 수 있는 커버 이미지·개별 이벤트 자유편집 같은 페이지별 커스터마이징은 원본 데이터 자체가 사라져 복구 불가 — 필요하면 다시 설정해야 한다.**
- **검증**: 두 페이지 모두 로컬 dev 서버에서 breadcrumb 정상 + 실제 하위 카테고리 카드가 채워진 타임라인 렌더링 확인. 재발 방지 안전장치는 실제로 테스트 유령 항목을 만들어 (1) DELETE API가 409로 거부하는 것, (2) "사이트 구성 관리"를 여는 것만으로 그 유령이 자동 정리되는 것까지 실제 동작으로 검증(테스트 데이터는 정리). `tsc`/`lint` 0 errors(80 warnings, 기존 기준선 유지).
- **브랜치**: `feature/EPIC-153`에 이어서 커밋(EPIC-152 계열 HOTFIX 넘버링 규칙 적용).

## 2026-08-30 (HOTFIX-152.20 — "디지털~A.i 문화" 페이지에 timeline_ng 게시판 신설 + 게시글 0개일 때 TL3 원시 에러 노출 버그 근본 수정)
- **사용자 지시**: "https://dev.silostore.net/online-docent/digital-ai 이 페이지에 timeline_ng를 만들어줘. 그리고 게시판도 만들어줘."
- **조사**: 이 페이지(`online-docent-digital-ai`, 이미 `builder_type='craft'`)는 `SiloTimelineEmbedBlock`이 `mode="group"`(하위 카테고리를 모아 보여주는 집계 모드, `groupHref="/online-docent/digital-ai"`)으로 설정돼 있었는데, 이 카테고리엔 하위(리프) 카테고리가 하나도 없어 집계할 대상 자체가 없었다 — 다른 4개 허브(고대 문명~침략/제국~군주/혁명~식민지/프로이트~대중문화)와 달리 "디지털~A.i 문화"는 세분화할 하위 시대가 없는, 그 자체로 완결된 하나의 구간으로 판단해 단일 게시판을 직접 연결하는 `mode="board"`로 전환.
- **구현(DB, Management API)**: `boards` 테이블에 새 게시판("디지털~A.i 문화", `category`/`slug`="digital-ai", `group_key`="docent", `render_type`="timeline_ng", 다른 도슨트 게시판과 동일한 옵션 — 르네상스 게시판 설정을 그대로 참고) 생성 후, 해당 페이지의 `craft_state`에서 `SiloTimelineEmbedBlock`의 `mode`를 `"board"`로, `boardId`를 새 게시판 id로 바꿔 직접 연결(`groupHref`는 비움).
- **부수 발견 및 근본 수정(코드)**: 새로 만든 게시판이라 게시글이 0개인 상태로 페이지를 열어보니, TimelineJS3(TL3) 라이브러리가 "events 배열이 비어있다"는 이유로(표지 title 슬라이드가 있어도) 우리 React의 error state를 거치지 않고 **자기 DOM에 직접 영어 원시 에러**("ERROR: Timeline configuration has no events...")를 그려버렸다 — 지금까지는 이 문제를 매번 "샘플 게시글을 채워 넣는" 임시방편(HOTFIX-151.10/151.11)으로만 피해왔는데, 이번에 근본 원인을 확인해 `SiloTimelineInner.tsx`에서 API 응답의 `events` 배열이 비어있으면 TL3 생성자를 아예 호출하지 않고 "아직 게시글이 없어요" 안내 문구로 대체하도록 수정 — 앞으로 새 타임라인 게시판을 만들 때마다 이 문제가 재발하지 않는다.
- **검증**: 로컬 dev 서버에서 실제 공개 페이지가 원시 에러 대신 "아직 게시글이 없어요."를 정상적으로 보여주는 것 확인, 관리자 Craft 에디터 캔버스 미리보기 안에서도 같은 안내가 뜨는 것 확인. `tsc`/`lint` 0 errors(80 warnings, 기존 기준선 유지).
- **브랜치**: `feature/EPIC-153`에 이어서 커밋(EPIC-152 계열 HOTFIX 넘버링 규칙 적용).

## 2026-08-30 (HOTFIX-152.19 — OTE 배경 패닝 모션: 가로/세로 방향성 강화 + 이동량 확대)
- **용어 정의(사용자 지시)**: "'온라인 도슨트'의 타임라인 이벤트의 '화면 자유편집'을 앞으로 OTE라고 지칭할게" — 이후 이 문서에서도 이 축약어(Online docent Timeline event free-Edit)를 그대로 쓴다. `SiloTimelineEmbedBlock.tsx`의 `SlideOverlayConfig`(표지/이벤트 공용) 중 배경 패닝 모션 관련 필드(`panSpeedSeconds`/`panZoomPct`/`panDistancePct`/`panDirection`)가 대상.
- **사용자 신고**: "세로 미디어는 위 아래 움직임을 더 중요시 하고(대각선도 됨), 가로 미디어는 왼쪽 오른쪽 움직임을 더 중요시하길 원해. 지금 너무 조금만 움직이니까 답답해 어떤 미디어는, 모션이 없으면 뭔지 모르겠어."
- **원인**: "auto" 패닝(HOTFIX-147.29의 4정거장 투어 모션)이 가로/세로 이미지의 긴 축은 100%, 짧은 축은 60%로만 이동 거리를 눌러뒀는데, 60%는 긴 축과 차이가 크지 않아 "가로/세로에 따라 방향이 다르다"는 게 체감상 잘 안 느껴졌다. 또 기본 이동 거리 자체(12%)가 작아 전반적으로 모션이 미묘했다.
- **수정**: `DEFAULT_PAN_DISTANCE_PCT`(12%→18%)와 그에 맞춰 화면 가장자리가 비지 않도록 `DEFAULT_PAN_ZOOM_PCT`(40%→65%, 안전 여유분 공식 `|이동%| <= 50*(zoom-1)/zoom` 기준 65%에서 최대 19.7%까지 안전 — 18%는 여유 있게 이내)를 함께 상향. `buildTourVars()`의 짧은 축 비율도 60%→35%로 낮춰 긴 축 방향(가로→좌우, 세로→상하)이 훨씬 뚜렷하게 우세해지도록 했다(0%로 완전히 없애지 않은 건 대각선 정거장이 섞인 투어 경로의 자연스러운 곡선감을 유지하기 위함 — "대각선도 됨"이라는 사용자 조건과도 맞음).
- **검증**: 로컬 dev 서버에서 실제 표지 배경 요소(`.silo-cover-pan-tour`)의 CSS 변수를 직접 측정 — `--silo-pan-zoom: 1.65`, `--silo-pan-dist: 18%`, 가로(landscape) 이미지의 실제 정거장 좌표가 `p0x: -18%, p0y: -6.3%`로 x축(좌우)이 y축(상하)보다 확실히 지배적인 것 확인. `tsc`/`lint` 0 errors(80 warnings, 기존 기준선 유지).
- **영향 범위**: 이 상수/함수는 표지(cover)와 이벤트(event overlay) 둘 다 공유하므로, OTE뿐 아니라 타임라인 표지의 배경 패닝도 함께 더 크게 움직인다(의도된 일관성 — 별도 분리 요청 없었음).
- **브랜치**: `feature/EPIC-153`에 이어서 커밋(EPIC-152 계열 HOTFIX 넘버링 규칙 적용).

## 2026-08-30 (HOTFIX-152.18 — 새 페이지 생성 시 Craft 에디터를 기본값으로)
- **사용자 지시**: "페이지가 만들어질때 craft 에디터를 default 로 깔아" — HOTFIX-152.17까지는 이미 있는 리프 카테고리를 하나씩 Craft로 전환했지만, 앞으로 새로 만드는 카테고리는 여전히 `builder_type='native'`(page_modules 위젯 목록)로 시작해 같은 문제(Craft 에디터 없음)가 계속 반복될 구조였다.
- **원인/구조 조사**: 새 페이지는 `ensurePageForSlug()`(`src/lib/pageTemplates.ts`, 사이트 메뉴에서 카테고리를 만들거나 href를 채울 때마다 자동 호출)가 만든다 — 이제까지 `builder_type`을 명시하지 않아 DB 기본값 `'native'`로 생성됐다. `builder_type`을 그냥 `'craft'`로 바꾸는 것만으로는 부족했는데, 공개 렌더러(`CRAFT_RENDERERS`)/관리자 에디터(`CRAFT_EDITORS`) 둘 다 **화이트리스트** 방식이라(등록 안 된 slug는 "조용히 native로 폴백") 새 slug를 코드에 매번 등록해줘야 하는 구조였다 — 이 프로젝트의 "코드 배포 없이 DB에서 새 카테고리를 만들면 즉시 페이지가 생긴다"(EPIC-068)는 설계 철학과 정면으로 부딪히는 지점이었다.
- **구현**: `src/components/craft/generic/`(신규) — 페이지 전용 블록 없이 범용 요소(Text Directory/Newsletter/Footer + `PRIMITIVE_RESOLVER`의 30여 종, 게시판 연동·사일로 타임라인 연동 포함)만 쓰는 `CraftGenericRenderer`(공개)/`CraftGenericEditor`(관리자)를 새로 만들고, 화이트리스트에 없는 slug의 "조용히 native 폴백"을 **"CraftGeneric으로 폴백"**으로 바꿨다(`src/app/[...slug]/page.tsx`, `src/app/admin/pages/[id]/page.tsx`) — 이제 어떤 새 slug든 코드에 한 줄도 안 등록해도 즉시 Craft 에디터로 열린다. 새 페이지의 시작 화면은 빈 `RootContainer`(강제로 얹는 Hero 없이 관리자가 "+ 요소 추가"로 직접 채움). `ensurePageForSlug()`는 `builder_type: 'craft'`로 삽입하고, 더 이상 안 쓰일 native 5-위젯 기본 템플릿(hero/quote/slide/board/gallery) 자동 생성 로직은 제거(HOTFIX-152.17에서 정리한 것과 같은 "미연결 빈 위젯만 쌓인 페이지"가 다시 생기지 않도록) — 이제 안 쓰이는 `WIDGET_DEFAULT_SETTINGS`/`DEFAULT_TEMPLATE_TYPES` import도 함께 정리.
- **검증**: 관리자 화면에서 실제로 새 최상위 탭을 만들어 (1) DB에 `builder_type='craft'`로 즉시 생성되는 것, (2) 실제 공개 페이지가 CraftGenericRenderer로 오류 없이 렌더링되는 것(빈 화면, breadcrumb 정상), (3) "페이지 수정"에 곧바로 "Craft 에디터 열기" 버튼이 뜨는 것, (4) 실제로 에디터를 열어 "텍스트" 요소를 추가하고 저장 → `craft_state`에 `TextBlock` 노드가 정확히 반영되는 것까지 엔드투엔드 확인 — 검증 후 테스트 데이터 삭제. `tsc`/`lint` 0 errors(80 warnings, 기존 기준선 유지).
- **부수 사고 처리**: 검증 도중 실제 코드 문제가 아니라 이 컴퓨터의 Turbopack dev 서버가 여러 시간·수십 번의 HMR을 거치며 완전히 꼬여(`Navbar.tsx` 파싱 에러 + 이미 제거된 변수명(`headerScaleMode` 등) 참조 에러가 동시에 뜸 — 소스 자체는 `tsc` 통과로 이미 깨끗함을 확인) 페이지가 하얗게 깨지는 증상을 겪음 — `.next` 캐시 삭제 + dev 서버 재시작으로 해결(코드 변경 아님, CLAUDE.md에 이미 기록된 "설명 안 되는 컴파일 에러는 `.next` 삭제로 먼저 의심" 패턴과 동일).
- **브랜치**: `feature/EPIC-153`에 이어서 커밋(EPIC-152 계열 HOTFIX 넘버링 규칙 적용).

## 2026-08-30 (HOTFIX-152.17 — 온라인 도슨트 리프 카테고리 12개 Craft 에디터 연결)
- **사용자 신고**: "온라인 도슨트의 최하위 카테고리중에, '르네상스' '바로크' '로코코'가 craft 에디터가 연결이 안되어있어. 그리고 '혁명~식민지'의 하위 카테고리부터 craft 에디터가 안나와 전부. craft 에디터 연결하고, 그리고 스크린샷의 '위젯' 시스템도 융합할 방법을 찾아."
- **조사**: 사이트 트리를 전수 조사한 결과, HOTFIX-151.12가 "고대 문명~침략"/"제국~군주" 밑 5개 리프(이집트/바빌론/그리스/로마제국/비잔틴 제국)만 Craft로 전환했고, 같은 "제국~군주" 밑 나머지 3개(르네상스/바로크/로코코)와 "혁명~식민지"/"프로이트~대중문화" 두 허브 밑 리프 전체(신고전주의/리전시/빅토리안/인상파/아르누보/아르데코/비트 세대/반문화/대중문화, 총 9개)는 그대로 `builder_type='native'`로 남아있었다 — 사용자가 명시한 3개 외에 "프로이트~대중문화" 밑 5개도 동일한 문제라 함께 전환(언급은 없었지만 정확히 같은 증상이라 일관되게 처리).
- **구현**: HOTFIX-151.12와 동일한 패턴 — 각 리프 페이지의 기존 `page_modules`(Board/Hero/Quote/Latest Posts Slider/Gallery/Timeline 6개 위젯, 스크린샷의 "위젯" 목록) 중 실제 게시판이 연결된 Board 위젯의 `board_id`를 그대로 가져와 `SiloTimelineEmbedBlock`(`mode="board"`) Craft 노드 하나로 옮기고 `builder_type`을 `craft`로 전환(총 12개 페이지, Management API로 일괄 처리) — 시각적으로 완전히 동일하게 유지하면서 Craft의 자유 편집(배경/모션/자세히 보기 등)을 쓸 수 있게 됨. `src/app/[...slug]/page.tsx`의 `CRAFT_RENDERERS`와 `src/app/admin/pages/[id]/page.tsx`의 `CRAFT_EDITORS`에 12개 slug 모두 등록.
- **"위젯 시스템 융합" 관련**: 실제로 옮길 가치가 있는 콘텐츠는 게시판이 연결된 Board 위젯 하나뿐이었다(Hero/Quote/Slide/Gallery는 전부 `board_id` 미연결인 빈 기본 템플릿, ensurePageForSlug가 자동 생성한 것) — 그래서 "융합"은 별도 브릿지를 새로 만들 필요 없이 이미 있는 게 확인됐다: Craft 에디터의 "요소" 팔레트에 이미 "게시판 연동"/"타임라인 연동"/"사일로 타임라인 연동"/"홈페이지 슬라이드쇼 위젯" 같은, Native 위젯 시스템의 핵심 기능과 동등하거나 더 풍부한 블록들이 갖춰져 있다(EPIC-098/099, EPIC-148 등에서 누적) — 즉 Native 위젯의 실질적 기능은 이미 Craft 쪽에 흡수돼 있어, 이번처럼 페이지 하나를 통째로 Craft로 전환해도 "위젯으로 할 수 있던 걸 못 하게 되는" 손실이 없다.
- **검증**: 로컬 dev 서버에서 두 허브(제국~군주 밑 "르네상스", 혁명~식민지 밑 "빅토리안") 실제 공개 페이지가 각자의 실제 게시글로 채워진 타임라인 그대로 렌더링되는 것 확인(breadcrumb도 정상 트리 경로로 표시), 관리자 "페이지 수정"에 "Craft 에디터 열기" 버튼이 뜨고 실제로 열어 캔버스 안에 같은 콘텐츠가 보이는 것까지 확인. `tsc`/`lint` 0 errors(80 warnings, 기존 기준선 유지). 나머지 10개는 이집트 등 기존 5개와 동일한 코드 경로 재사용이라 개별 실측은 생략(같은 마이그레이션 스크립트로 일괄 처리해 구조적으로 동일).
- **브랜치**: `feature/EPIC-153`에 이어서 커밋(EPIC-152 계열 HOTFIX 넘버링 규칙 적용).

## 2026-08-30 (DB 전용 — "제국~군주" 중복 메뉴 항목 정리)
- **사용자 지시**: "그 두 개 중복 항목 정리해줘. 어찌되었든, 상단 탭의 '온라인 도슨트' -> '제국~군주' 페이지에 전에 설정한 타임라인이 나오기만 하면 돼."
- **조사**: 직전 HOTFIX-152.16에서 발견한 중복 항목(`dacf883f`, href `/online-docent-empire-monarchy`)을 다시 확인한 결과, 사용자가 만든 진짜 카테고리가 아니라 **`is_active: false`(이미 비활성)** 상태였고 부모가 "온라인 도슨트"가 아니라 **"미분류 페이지" 버킷**이었다 — `page_builder`에는 있지만 그 어떤 메뉴에도 안 걸린 페이지를 관리자가 나중에 찾아 배정할 수 있도록 시스템이 자동으로 만들어 둔 placeholder 항목(EPIC-084 `ensureUnassignedPagesInTree`)이었다. 즉 실제 방문자에게는 원래부터 노출된 적이 없는 죽은 데이터였다.
- **조치(Management API)**: 이 placeholder 항목(`site_navigations` id `dacf883f`)과 그것이 가리키던 빈 페이지(`page_builder` id `e9baf784`, 완전히 비어있는 기본 5-위젯 템플릿, `page_modules` 포함) 전부 삭제. "제국 ~ 군주"는 이제 실제 타임라인 콘텐츠를 가진 진짜 항목(`f7008a1a`, href `/online-docent/ancient-monarchy`) 하나만 남았다.
- **검증**: 실제 사이트에서 온라인 도슨트 페이지의 상단 탭 드롭다운·`EraGridBlock`·`TextDirectoryBlock` 3곳의 "제국 ~ 군주" 링크가 전부 같은 href(`/online-docent/ancient-monarchy`)로 일치하는 것 확인, 그 페이지를 직접 열어 로마제국/비잔틴제국/르네상스/바로크/로코코 5개 시대 타임라인이 정상 렌더링되는 것 확인. 코드 변경 없음(DB 전용) — `tsc`/`lint` 재실행 불필요.
- **남은 사소한 항목(이번 요청과 무관, 참고용)**: 이 페이지의 breadcrumb이 "홈›미분류 페이지›고대 ~ 왕정..."으로 표시된다 — `site_navigations` 트리 자체는 이제 정상(미분류 아님)인데 breadcrumb 로직이 다른 기준으로 판단하는 것으로 보임, 실제 콘텐츠 노출에는 영향 없어 이번엔 손대지 않음.

## 2026-08-30 (HOTFIX-152.16 — "사이트 메뉴"에서 href(슬러그)를 수정하면 기존 페이지가 고아가 되고 빈 페이지가 새로 생기던 버그 수정)
- **사용자 신고**: "'사이트 메뉴' 설정에서 '온라인 도슨트 -> 제국~군주 -> 수정'을 눌러서 slug를 /online-docent/Empire-monarchy로 바꿨더니, 상단 탭의 '온라인 도슨트 -> 제국~군주'를 눌렀더니 타임라인이 없어졌어. 사이트메뉴에서 수정을 했더니, '수정'이 아니라, 아예 새로운 페이지가 생겨버리는데 아주 불편해 죽겠다."
- **원인 조사**: DB 직접 조회로 확인한 결과, "제국~군주" 허브 페이지의 진짜 콘텐츠(타임라인, `SiloTimelineEmbedBlock` 포함 Craft `craft_state`)는 `page_builder.slug = 'online-docent-ancient-monarchy'`에 있었다. `site_navigations`의 href를 `/online-docent/Empire-monarchy`로 바꾸자 `hrefToSlug()`가 계산하는 slug가 `online-docent-empire-monarchy`로 완전히 달라졌고, `CategoryTreeManager.tsx`의 `updateRow`는 href가 바뀔 때마다 `ensurePageForSlug(새 slug, ...)`만 불러 "그 slug에 페이지가 없으면 새로 만든다"만 했지 **원래 페이지를 새 자리로 옮기지는 않았다** — 그 결과 `site_navigations`는 새 href를 가리키게 되지만 그 slug엔(공교롭게도 이미 이틀 전부터 존재하던, tier2_tab 중복 항목용 빈 페이지가 있어) 빈 기본 템플릿만 보였고, 원래 타임라인 페이지는 그 어떤 메뉴도 가리키지 않는 고아로 남았다. 즉 href 수정 = "새 slug로 갈아타기"였지 "같은 페이지를 옮기기"가 아니었던 게 근본 구조 문제.
- **즉시 복구(DB 전용)**: "제국~군주"(`site_navigations` id `f7008a1a`)의 href를 원래 값 `/online-docent/ancient-monarchy`로 되돌려 타임라인이 다시 보이도록 함(craft_state가 있는 원본 페이지와 slug 일치 확인 후 복구).
- **근본 수정**: `src/lib/pageTemplates.ts`에 `renamePageSlugIfPossible(oldSlug, newSlug)` 신규 — 새 slug 자리가 비어 있으면 옛 slug의 기존 `page_builder` 행 자체를 새 slug로 UPDATE(진짜 "이동")하고, 이미 다른 페이지가 차 있으면 안전하게 건드리지 않는다. `CategoryTreeManager.tsx`의 `updateRow`가 href 변경 시 `ensurePageForSlug`를 부르기 전에 이 함수를 먼저 `await`로 호출하도록 수정(순서가 바뀌면 경쟁 상태로 ensurePageForSlug가 먼저 "없다"고 판단해 새로 만들어버릴 수 있어 순서 보장이 중요) — 이제 href(슬러그)를 몇 번을 고쳐도 같은 페이지가 계속 따라간다.
- **검증**: 관리자 화면에서 테스트 카테고리를 만들어 실제 "수정" 폼으로 href를 두 번 변경(`/rename-test-a` → `/rename-test-b`)하며 `page_builder` 행의 `id`가 그대로 유지된 채 `slug`만 이동하고(마커로 남긴 `title`도 그대로 보존, 새 빈 페이지가 추가로 생기지 않음) 확인 — 테스트 데이터는 검증 후 정리. `tsc`/`lint` 0 errors(80 warnings, 기존 기준선 유지).
- **남은 부수 이슈(이번 범위 밖, 손대지 않음)**: "제국~군주"가 서로 다른 href 관례(`/online-docent/...` 슬래시식 vs `/online-docent-...` 대시식)를 쓰는 두 개의 독립된 `site_navigations` 행(sidebar_left용/tier2_tab용)으로 중복 존재하고 있고, 그중 tier2_tab용(`dacf883f`, href `/online-docent-empire-monarchy`)이 가리키는 페이지는 여전히 빈 기본 템플릿이다 — 오늘 사고와는 별개로 이미 있던 구조라 이번엔 손대지 않았다.
- **브랜치**: `feature/EPIC-153`에 이어서 커밋(EPIC-152 계열 HOTFIX 넘버링 규칙 적용).

## 2026-08-30 (HOTFIX-152.15 — 헤더 비율 축소를 태블릿·모바일 계층까지 확대)
- **사용자 신고**: "pc -> 탭 -> 모바일 순으로 되도록 크롬 윈도우 창을 점점 줄여보니, 탭 -> 모바일 할때 간격 유지가 안되고 있어" — HOTFIX-152.14는 PC 계층(1024px~)에만 CSS zoom 축소를 적용했었고, 태블릿(768~1023px)/모바일(~767px) 계층은 여전히 예전처럼 위치(dx)만 화면 폭 비례로 옮겨질 뿐 크기 자체는 고정이라(요소 크기는 그대로인데 배치만 이동) 그 구간에서 로고·아이콘이 창 폭에 맞춰 "정말로 작아지지" 않고 있었다.
- **구현**: HOTFIX-152.14의 PC 전용 zoom 로직을 태블릿(기준폭 820px)/모바일(기준폭 390px) 계층까지 일반화 — 세 계층 모두 "홈페이지 설정 관리"의 해당 탭 캔버스 고정폭과 동일한 기준으로, 그보다 실제 화면이 좁아지면 그 비율만큼 헤더 전체가 CSS zoom으로 축소된다(기준폭보다 넓으면 1로 고정, 더 커지지 않음). `HeaderScaleModeContext`를 문자열(`"dynamic"`/`"fixed"`) 대신 "지금 계층의 고정 기준폭" 숫자값을 직접 전달하도록 일반화해 `HeaderSlot.tsx`의 dx 오프셋도 각 계층의 고정폭 기준으로만 정규화(이중 축소 방지, HOTFIX-152.14와 동일한 원리를 세 계층에 공통 적용).
- **검증**: 로컬 dev 서버에서 실제 공개 홈페이지를 (1) 태블릿 범위(790px) → `zoom=790/820=0.9634`, (2) 모바일 범위(375px, mobile 프리셋) → `zoom=375/390=0.9615` 정확히 계산되는 것 확인. 로고·아이콘 실제 렌더 크기(`getBoundingClientRect`)도 각 zoom 비율만큼 정확히 축소되는 것 실측 확인(예: 100px 아이콘이 375px 모바일에서 96.2px = 100×0.9615). 관리자 편집 화면은 PC/태블릿/모바일 세 탭 모두 캔버스가 각자 고정폭(1440/820/390)이라 zoom이 항상 1로 유지되는 것(회귀 없음) 확인. `tsc`/`lint` 0 errors(80 warnings, 기존 기준선 유지).
- **알려진 잔여 한계(구조적)**: 태블릿(768px 하한, zoom≈0.937)과 모바일(767px 상한, zoom=1— 767>390이라 상한 1에 걸림) 사이 정확한 경계에서는 여전히 zoom이 약 6~7% 튄다 — 태블릿/모바일이 애초부터 서로 완전히 독립된 설정(다른 아이콘 구성·위치를 가질 수 있음)이라 그 경계에서 "완벽하게 이음매 없는" 연속성은 이 구조상 근본적으로 달성하기 어렵다. 사용자가 요청한 "각 구간 안에서 간격을 유지하며 줄어드는" 동작은 이번 수정으로 세 계층 전부에서 확보됐다.
- **브랜치**: `feature/EPIC-153`에 이어서 커밋(EPIC-152 계열 HOTFIX 넘버링 규칙 적용).

## 2026-08-30 (HOTFIX-152.14 — PC 헤더: 창 폭이 줄어들면 로고·아이콘 간격을 비율 유지한 채 통째로 축소)
- **사용자 지시**: "그건 알겠는데, 창의 크기가 줄어들더라도, 로고, 아이콘의 간격이 유지가 되면서 줄어들어야지" — HOTFIX-152.13은 "관리자가 설정한 위치가 세션마다 어긋나는" 문제를 고쳤지만, 그와 별개로 "실제 방문자 창 폭이 좁아질 때 로고/아이콘이 서로 겹치거나 간격이 불균일해지지 않고, 전체가 비례해서 작아지는" 진짜 반응형 동작은 아직 없었다.
- **구현 방식(2가지 옵션 중 사용자가 "헤더 전체를 통째로 축소" 방식 선택)**: 개별 아이콘/로고 크기를 하나하나 계산해서 줄이는 대신, 헤더 바(`Navbar.tsx`의 `topBarRef`) 전체에 CSS `zoom`을 적용 — 실제 창 폭이 `PC_HEADER_REFERENCE_WIDTH_PX`(1440px, "홈페이지 설정" PC 탭 캔버스와 동일 기준, HOTFIX-152.13)보다 좁아지면 `zoom = 창폭/1440`(최댓값 1, 1440px보다 넓은 화면에서는 확대하지 않음)만큼 헤더 안의 모든 것(로고 이미지 크기, 아이콘 크기, 글자 크기, 드래그로 설정한 간격까지)이 하나의 배율로 함께 줄어든다 — 요소마다 각각 코드를 추가할 필요 없이, 헤더 안에 있는 모든 것이 자동으로 비례 축소된다.
- **이중 축소 방지**: `HeaderSlot.tsx`(드래그 위치 오프셋 계산)는 원래 "지금 화면 폭 대비 드래그 당시 기준 폭" 비율로 오프셋을 스케일링했는데(HOTFIX-141.15), 이제 zoom이 이미 화면 폭 비율만큼 전체를 축소해주므로 오프셋까지 또 화면 폭 비율로 줄이면 이중으로(제곱으로) 작아진다. 새 `HeaderScaleModeContext`(React Context, `Navbar.tsx`가 PC 계층일 때 `"fixed"`, 태블릿/모바일일 때 `"dynamic"`으로 제공)로 PC 계층에서는 오프셋을 "지금 화면 폭"이 아니라 **항상 고정된 1440px 기준**으로만 정규화하도록 분기 — 화면 폭에 따른 축소는 전적으로 zoom 하나에만 맡긴다. 태블릿/모바일은 이런 전체 zoom이 없으므로 기존 방식(지금 화면 폭 기준 동적 스케일링) 그대로 유지, 변경 없음.
- **관리자 편집 화면 영향 없음**: `useReferenceWidth()`가 admin PC 탭 캔버스 안에서는 캔버스 폭(HOTFIX-152.13으로 항상 1440 고정)을 반환하므로, PC 탭 편집 중에는 `zoom`이 항상 정확히 1(1440/1440)로 고정된다 — 편집 배율이 창 크기에 따라 흔들리지 않고 항상 1:1.
- **검증**: 로컬 dev 서버에서 실제 공개 홈페이지를 1440px→1024px로 좁혀가며 (1) `topBarRef`의 `getComputedStyle().zoom`이 정확히 `1024/1440≈0.7111`로 계산되는 것, (2) 로고·카메라 아이콘 2개의 실제 렌더 크기(`getBoundingClientRect`)가 그 비율만큼 정확히 함께 줄어드는 것(스크린샷으로도 로고-아이콘 간격이 비율 그대로 유지된 채 전체가 작아지는 것 확인) 확인. 관리자 PC 탭 캔버스에서는 좁은 뷰포트(498px)에서도 `zoom=1`로 고정되고, 기존에 드래그해둔 상단 아이콘 2개의 렌더 위치가 이번 변경 전후로 전혀 변하지 않는 것(회귀 없음) 확인. `tsc`/`lint` 0 errors(80 warnings, 기존 기준선 유지).
- **스코프**: 좌/우 사이드바(LeftSidebar/RightSidebar)는 `topBarRef` 바깥의 독립 형제 요소라 이번 zoom 적용 범위 밖 — 사용자 요청이 "로고, 아이콘"(상단 헤더)에 한정돼 사이드바는 손대지 않음. 태블릿/모바일 계층도 각자 독립 설정을 쓰고 있어 대상에서 제외.
- **브랜치**: `feature/EPIC-153`에 이어서 커밋(EPIC-152 계열 HOTFIX 넘버링 규칙 적용).

## 2026-08-30 (HOTFIX-152.13 — "홈페이지 설정" PC 탭: 헤더 요소(상단 아이콘 등) 위치가 세션마다 계속 어긋나던 근본 원인 수정)
- **사용자 신고("도대체 왜 왜 왜왜 지금 몇번째 너에게 말하는데")**: "pc 버전의 '홈페이지 설정'의 아이콘들 위치가 계속 내가 정한 위치에서 바뀐다" — 이전 세션들에서도 반복 신고됐던 것으로 보이는 오래된 구조적 버그.
- **원인**: 헤더 요소 드래그 위치(`HeaderSlot.tsx`)는 "드래그 당시 캔버스 기준 폭(`refWidthPx`)" 대비 "지금 기준 폭"의 비율로 스케일링해서 렌더링한다(`effectiveDx = dxPx * (지금 폭 / refWidthPx)`) — 창 폭이 달라져도 상대적 위치가 유지되게 하려는 의도(HOTFIX-141.15). 그런데 "홈페이지 설정" PC 탭만 태블릿(820px 고정)/모바일(390px 고정)과 달리 **캔버스에 고정 폭이 없어 관리자 자신의 실제 브라우저 창 폭을 그대로 기준 폭으로 썼다** — 그래서 관리자가 창 크기를 바꾸거나(최대화/축소), 다른 모니터를 쓰거나, **이 프로젝트가 실제로 운영되는 방식대로 회사 컴퓨터/개인 컴퓨터를 오가며 작업할 때마다** 매번 다른 기준 폭이 기록/적용돼 저장된 위치가 매번 다른 비율로 재계산되며 눈에 보이는 위치가 계속 어긋났다. DB 실측으로도 같은 아이콘들의 `refWidthPx`가 세션마다 1415~1423px 사이에서 흔들려 있는 것을 확인 — 관리자가 "정한" 위치 자체가 애초에 창 폭에 종속된 불안정한 기준으로 저장되고 있었다.
- **수정**: PC 탭도 태블릿/모바일과 동일한 패턴으로 캔버스에 고정 시뮬레이션 폭(1440px, 기존에 실사용 중 저장된 `refWidthPx`(1415~1423px)와도 근접해 기존 위치의 시각적 변화를 최소화)을 부여 — 이제 관리자의 실제 브라우저 창 크기·모니터·접속 컴퓨터와 무관하게 항상 동일한 기준 폭으로 측정·저장된다. 창이 1440px보다 좁으면 캔버스를 감싸는 `overflow-auto`가 가로 스크롤을 제공(태블릿/모바일도 이미 이렇게 동작).
- **검증**: 로컬 dev 서버에서 Browser pane 폭이 498px로 좁을 때도 캔버스가 정확히 1440px로 고정 렌더링되는 것 확인, 기존에 저장된 상단 아이콘 2개의 실제 렌더 위치(`getComputedStyle().transform`)가 이전 기준(refWidthPx≈1423)과 비교해 1.2% 이내로 거의 그대로 유지되는 것 확인(위치가 눈에 띄게 안 바뀜 — 회귀 없음). `tsc`/`lint` 0 errors(80 warnings, 기존 기준선 유지).
- **참고**: 상단 아이콘뿐 아니라 로고/탭/계정 메뉴 등 PC 탭에서 드래그로 옮기는 모든 헤더 요소가 같은 `HeaderSlot.tsx` 메커니즘을 공유하므로 전부 동일하게 안정화된다.
- **브랜치**: `feature/EPIC-153`에 이어서 커밋(EPIC-152 계열 HOTFIX 넘버링 규칙 적용).

## 2026-08-30 (DB 전용 — "상단 사이드바" 모바일 탭에 "My Page/My Collection/My Story/My Timeline" 링크 누락 보완)
- **사용자 신고**: "왜 '모바일' 버전의 '상단 사이드바'에는 My page ~, 그리고 그 하위 카테고리가 안 보이는건데?"
- **원인**: 상단 사이드바의 링크 목록(`site_settings.top_sidebar.<device>.links`)은 PC/태블릿/모바일 탭마다 완전히 독립된 설정 — PC 탭에는 "My Page/My Collection(하위 8개)/My Story(하위 5개)/My Timeline(하위 6개)"가 등록돼 있었지만, 모바일 탭 링크 목록은 처음부터 별개로 "Mind Diary/Studio" 2개만 있었다(이 2개는 원래 고정 바로가기였는데 관리자가 column 2 사본으로 옮겨온 것). HOTFIX-152.6(아이콘별 메가 메뉴)/HOTFIX-152.10(모바일 이미지 뱅크)에서도 반복된 것과 같은 "디바이스 탭 = 완전히 독립된 데이터" 패턴.
- **조치**: PC 탭의 "My Page/My Collection/My Story/My Timeline" 4개 링크(하위 목록 포함)를 모바일 탭 링크 목록 맨 앞에 복사(Management API, `jsonb`로 배열 병합) — 기존 모바일의 Mind Diary/Studio는 그대로 유지.
- **검증**: 로컬 dev 서버에서 실제 공개 홈페이지를 375px 모바일 뷰포트로 열어 상단 사이드바를 실제로 열고 "My Page/My Collection▸/My Story▸/My Timeline▸/Mind Diary/Studio" 6개 항목이 전부 나타나는 것 DOM으로 확인(하위 목록 있는 3개는 HOTFIX-152.12의 화살표 표시까지 정상). 코드 변경 없음(DB 전용) — `tsc`/`lint` 재실행 불필요.
- **참고**: 태블릿 탭도 PC와 같은 4개 링크를 이미 갖고 있어(추가로 Silo Planet 1개 더) 이번 조치가 필요 없었음 — 확인만 하고 손대지 않음.

## 2026-08-30 (HOTFIX-152.12 — "상단 사이드바" 모바일: 하위 목록 있는 링크를 탭하면 즉시 이동 대신 먼저 미리보기)
- **사용자 지시("그래 그렇게해", HOTFIX-152.11 완료 보고에서 남긴 "모바일은 터치라 hover가 없어 이미지/하위 목록이 갱신될 계기가 없다"는 한계 제안에 대한 승인)**: column 2 링크에 마우스를 올려야만(`onMouseEnter`) column 0 이미지·column 3 하위 목록이 갱신되는데, 터치 기기는 hover 자체가 없어 곧장 탭 → 이동으로 끝나버려 하위 목록을 browsing할 기회가 전혀 없었다.
- **구현**: `TopSidebarPanel.tsx`의 column 2 링크에 `isMobileViewport`일 때만 적용되는 탭 로직 추가 — 하위 목록이 있는 링크(`link.children.length > 0`)를 처음 탭하면 `e.preventDefault()`로 이동을 막고 `handleLinkHover(link.id)`만 실행해(기존 hover 핸들러 재사용) column 0/3이 그 자리에서 갱신되게 하고, 이미 그 링크가 "미리보기 중"(`hoveredLinkId === link.id`)인 상태에서 다시 탭하면 그때는 막지 않고 정상 이동한다. 하위 목록이 없는 링크는 기존과 동일하게 첫 탭에 바로 이동(막을 이유가 없음). 링크 텍스트 끝에 상태 표시용 화살표(▸ 미리보기 전 / ▾ 미리보기 중)도 모바일에서만 추가.
- **검증**: 관리자 편집 화면(editable=true)은 패널 안 클릭을 전부 "Controls 선택"으로 가로채는 기존 로직(HOTFIX-140.2) 때문에 이 탭 로직 자체를 검증할 수 없다는 것을 확인 — 대신 실제 공개 홈페이지를 375px 모바일 뷰포트로 열어 검증. `site_settings.top_sidebar`의 모바일 링크 목록에 하위 목록이 있는 테스트용 링크를 Management API로 임시 추가 → 실제 클릭 이벤트로 (1) 첫 탭: `location.href` 불변(이동 안 함) + 화살표 ▸→▾ + 하위 목록(Treasures/Books) 실제로 나타남, (2) 같은 링크 재탭: `event.defaultPreventedAfterSecondTap === false`(막지 않고 정상 진행)까지 확인 — 검증 후 테스트 링크는 원래 값(Mind Diary/Studio 2개)으로 정확히 복구. `tsc`/`lint` 0 errors(80 warnings, 기존 기준선 유지).
- **브랜치**: `feature/EPIC-153`에 이어서 커밋(EPIC-152 계열 HOTFIX 넘버링 규칙 적용).

## 2026-08-30 (HOTFIX-152.11 — "상단 사이드바" 모바일: 4개 컬럼 세로 1열 쌓기 → 2열(가로 2칸씩 2행) 배치로 변경)
- **사용자 신고("정신 안차려? 일똑바로해")**: HOTFIX-152.10에서 "모바일은 컬럼을 옆으로 나열 못 해 항상 세로로 쌓는다"고 설명했던 게 실제로는 사용자가 원하던 결과가 아니었다 — 사용자가 원한 건 처음부터 "이미지 | 세션정보 / 링크목록 | 하위목록"처럼 왼쪽·오른쪽으로 2열씩 나뉜 2행 배치였다. HOTFIX-141.21(모바일에서 컬럼이 잘려 안 보이던 문제) 이후로 "일단 다 보이게"가 목표라 1열 세로쌓기로 처리했던 게, 그 후로도 계속 "버그 아님, 의도된 동작"으로만 설명되며 진짜 요청을 놓치고 있었다.
- **구현**: `TopSidebarPanel.tsx`의 컬럼 렌더링을 데스크톱/모바일이 공유하는 `columnContents`(컬럼별 내용)와, 그 바깥 배치 방식(데스크톱: 가로 1행 4칸 각각 지정 px 너비 / 모바일: `config.columnOrder`를 앞에서부터 2개씩 묶어 `flex gap-4`로 된 행을 2개 만들어 2열×2행)으로 분리. 모바일에서 각 컬럼의 너비(px)는 390px 화면에 그대로는 못 넣지만, 같은 행 두 컬럼의 px 값을 비율(%)로 환산해 `flexBasis`에 반영해 "너비 설정이 상대적으로는 계속 의미 있게" 만들었다(예: 100px/224px 두 컬럼 → 약 31%/69%). 이미지(column 0) 높이도 2열이 되며 너비가 좁아진 만큼 h-56(224px) → h-40(160px)로 축소.
- **admin 안내 문구 갱신**: "홈페이지 설정 관리"의 "컬럼 너비·순서" 섹션 모바일 탭 경고 문구를 새 동작(2열 배치 + 비율 반영)에 맞게 다시 작성.
- **검증**: 로컬 dev 서버 + 관리자 세션에서 모바일 탭 선택 → 상단 사이드바 트리거를 실제로 열어 DOM으로 직접 확인 — 1행(이미지·세션정보, 각 126px/214px≈37%/63%), 2행(링크목록·하위목록, 각 170px/170px=50%/50%, 두 컬럼 너비 설정이 둘 다 224px로 같아 정확히 반으로 나뉨)으로 렌더링되는 것 확인. PC 탭은 기존과 동일하게 4컬럼 가로 1행(160/192/126/224px)으로 회귀 없음 확인. `tsc`/`lint` 0 errors(80 warnings, 기존 기준선 유지 — 이 작업 중 잠깐 발생했던 `react/no-unescaped-entities` 에러 2건은 안내 문구의 큰따옴표를 `&quot;`로 교체해 해결).
- **브랜치**: 직전 EPIC-153(히어로 슬라이드쇼) 작업과는 별개 기능이지만 상단 사이드바는 EPIC-152 계열 기능이라 프로젝트 넘버링 규칙(HOTFIX는 독립 정수를 못 받고 관련 EPIC에 종속)에 따라 `HOTFIX-152.11`로 명명, `feature/EPIC-153`(현재 작업 브랜치)에 이어서 커밋.

## 2026-08-30 (EPIC-153 확장 — 모든 Craft 히어로 블록에 동일한 배경 슬라이드쇼 기능 적용)
- **사용자 지시**: "모든craft 의 hero 요소에 똑같은 슬라이드 기능 넣어줘" — 직전 커밋에서 살롱데상 `SalonHeroBlock`에만 넣었던 이미지·영상 슬라이드쇼를 나머지 5개 히어로 블록(홈 `EditorialHeroBlock`, 사일로 상점 `ShopHeroBlock`, 스튜디오 `StudioHeroBlock`, 온라인 도슨트 `DocentHeroBlock`, 마이 페이지 `MypageHeroBlock`)에도 동일하게 적용.
- **구현**: 슬라이드쇼 배경(`slides: {url}[]` 상태, 자동 전환 타이머, 이미지/영상 자동 판별 렌더링)과 그 Settings 패널(자동 전환 간격 입력 + 썸네일 목록 + 다중 파일 업로드)을 `src/components/craft/shared/HeroSlides.tsx`(신규)로 뽑아 6개 블록이 공유 — `HeroSlidesBackground`(배경 렌더링)와 `HeroSlidesSettings`(`related.settings`로 꽂는 컴포넌트) 2개를 export. 각 블록은 이 프로젝트의 "페이지별 전용 블록" 방침대로 여전히 별도 파일·별도 텍스트 레이아웃(태그/CTA 유무, 정렬 등)을 유지하고, 배경 부분만 공유 모듈을 가져다 쓰는 구조 — 텍스트/레이아웃까지 하나로 합치지는 않음.
- **`PageBuilderRenderer.tsx` 수정(빌드 에러 해결)**: "Native Page Builder"(`page_modules` 테이블, `craft_state`와는 완전히 별도의 저장 위치)가 `EditorialHeroBlock`을 위젯으로 노출하는 `CraftHeroFromSettings` 래퍼가 옛 `imageUrl`/`imageUrlMobile` 필드 기준으로 props를 조립하고 있어 `slides` 타입 변경 후 `tsc` 에러(TS2353/TS2339) 발생 — `settings.slides`(새 형식)를 우선 읽고, 없으면 구형 `settings.imageUrl` 문자열을 1장짜리 슬라이드로 감싸는 하위 호환 경로(`heroSlidesFromSettings`)를 추가해 해결. Management API로 `page_modules`에 `module_type = 'craft_hero'`인 실 데이터가 있는지 확인한 결과 0건이라 이 경로가 실제로 쓰일 일은 현재 없지만, 이후 누가 이 위젯을 다시 쓰기 시작해도 깨지지 않도록 남겨둠.
- **DB 마이그레이션(Management API)**: 이미 저장돼 있던 `page_builder.craft_state` 중 구형 `imageUrl` 단일 필드를 쓰던 2곳을 `slides`/`autoAdvanceSeconds` 형식으로 변환 — `silo-store` 페이지의 `ShopHeroBlock` 노드(`igQEL6oOwJ`), `online-docent` 페이지의 `DocentHeroBlock` 노드(`U2vw5uXTLX`, 라이브 커스텀 태그 "BC 2700 ~ 현재" 값 보존). `craft_state`가 이중 인코딩된 JSON 문자열이라 `#>> '{}'`로 원문 문자열을 꺼내 `replace()`로 해당 `imageUrl` 키만 치환한 뒤 `to_jsonb()`로 재인코딩 — 두 URL 모두 각 페이지 안에서 유일한 문자열이라 다른 블록을 잘못 건드릴 위험 없이 안전하게 치환됨(치환 전후 값 재조회로 확인).
- **검증**: `npx tsc --noEmit` 0 errors, `npm run lint` 0 errors(80 warnings, 기존 기준선과 동일 — 전부 이 작업과 무관한 기존 `react-hooks/set-state-in-effect` 경고). 로컬 dev 서버에서 `/silo-store`·`/online-docent` 두 페이지 모두 마이그레이션된 배경 이미지가 정상 렌더링되는 것을 DOM(`<img>` src)으로 직접 확인, 콘솔 에러 없음.
- **브랜치**: 직전 Salon Hero 작업과 이어지는 같은 EPIC-153 범위라 `feature/EPIC-153`에 계속 커밋.

## 2026-08-30 (EPIC-153 — 살롱데상 페이지 "Salon Hero" 블록에 이미지·영상 배경 슬라이드쇼 추가)
- **사용자 지시**: "Salon hero 부분은 배경 이미지를 설정할수 없어. 여기에 슬라이드를 넣을수 있도록 해줘. 슬라이드에는 이미지와 영상도 가능해야해." — `SalonHeroBlock.tsx`(살롱데상 Craft 페이지 전용 히어로)는 지금까지 `EditableResponsiveImage`(더블클릭으로 바로 바꾸는 정지 이미지 한 장, PC/모바일 각각)만 지원해 여러 장을 넣을 자리가 없었다.
- **구현**: `imageUrl`/`imageUrlMobile` 단일 필드를 `slides: {url}[]` 배열로 교체 — 각 슬라이드는 확장자로 이미지/영상을 자동 판별해 그린다(`SiloTimelineEmbedBlock.tsx`/`admin/navigation/settings`의 `ImageThumb`이 이미 쓰는 것과 동일한 정규식). 여러 장을 다루려면 목록 UI가 필요해, 더블클릭 인라인 편집 대신 다른 슬라이드쇼 블록(`SlideshowBlock.tsx` 등)과 같은 우측 Settings 패널 방식(`related.settings`)으로 새로 만들었다 — 자동 전환 간격(초) 입력 + 슬라이드 목록(썸네일·삭제) + 업로드 버튼. 파일 여러 개 한 번에 선택(HOTFIX-151.13과 동일 패턴: `multiple` + 순차 처리 + 영상 자동 압축 후 한 번에 반영, 매번 반영하면 이전 파일 추가분을 덮어쓰는 버그를 피함)도 지원한다. 이 블록은 살롱데상 페이지 하나에서만 쓰이고 그 페이지의 `craft_state`가 아직 저장 전(빈 상태)이라 기존 데이터 마이그레이션은 필요 없었다 — `.craft.props` 기본값만 새 모양으로 바꿈.
- **검증**: 로컬 dev 서버 + 관리자 세션으로 살롱데상 페이지 Craft 에디터를 열어 (1) 새 컴포넌트가 크래시 없이 기존 배경 이미지를 그대로 렌더링하는 것, (2) Salon Hero 블록 선택 시 우측 Settings 패널에 "자동 전환 간격(초)"/"배경 슬라이드 (1)"/업로드 버튼이 정상 노출되는 것, (3) 업로드 input이 `multiple`+`accept="image/*,video/*"`인 것까지 확인. `tsc`/`lint` 0 errors(80 warnings, 기존 기준선 유지). 실제 파일 업로드(다중 선택)는 브라우저 자동화로 OS 파일 선택 다이얼로그를 조작하지 못해 코드 리뷰+DOM 속성 확인까지만 검증(HOTFIX-151.13과 동일한 한계).
- **브랜치**: 새 기능(신규 EPIC)이라 프로젝트 규칙대로 `feature/EPIC-153`으로 분리(직전 세션 작업은 `feature/EPIC-152`).

## 2026-08-30 (HOTFIX-152.10 — "상단 사이드바" 모바일 탭: "컬럼 너비·순서"가 안 먹히는 이유 설명 추가 + 이미지가 아예 안 뜨던 원인(모바일 전용 이미지 뱅크가 비어있었음) 해결)
- **사용자 신고**: "모바일 버전 설정할때 '상단 사이드바'의 '컬럼 너비순서' 설정이 아직도적용 안되는데? 그리고, 이미지는 나오지도 않아." — 서로 다른 원인의 문제 2개였다.
- **원인 1(컬럼 너비 안 먹힘) — 실은 버그 아님**: `TopSidebarPanel.tsx`는 HOTFIX-141.21(모바일에서 컬럼 4개가 옆으로 잘려 하나만 보이던 문제 수정) 이후로 모바일 탭에서는 `columnWidthsPx`를 아예 무시하고 4개 컬럼을 항상 세로로 쌓아 폭 전체를 채우도록 만들어져 있다 — 의도된 동작이지만 관리자 화면은 이 사실을 전혀 알려주지 않아 "설정이 적용 안 된다"는 신고로 이어졌다.
- **원인 2(이미지가 아예 안 뜸) — 진짜 원인**: column 0(왼쪽 이미지)은 관리자가 column 2 링크에 마우스를 올릴 때만 "이미지 뱅크"(`imageBankUrls`)에서 무작위로 하나 골라 보여주는데(비어있으면 그 링크 자신의 `imageUrl`로 폴백), PC 탭에는 이미지 뱅크 4장이 등록돼 있었지만 **모바일 탭은 처음부터 이미지 뱅크가 0장**이었다(디바이스 탭마다 독립 설정 — HOTFIX-152.6/DB 재확인 때도 같은 이유로 겪은 패턴). 모바일의 링크 2개(Mind Diary/Studio)도 개별 `imageUrl`이 비어 있어, 무엇을 hover해도 보여줄 이미지 자체가 없었다.
- **수정 1(코드)**: "컬럼 너비·순서" 섹션에 `deviceTab === "mobile"`일 때만 보이는 안내 문구 추가 — "모바일에서는 이 너비 설정이 적용되지 않아요... 항상 세로로 쌓아 폭 전체를 채워요"라고 그 자리에서 바로 설명한다(순서 변경은 모바일에서도 여전히 유효하므로 그 부분은 유지된다고 명시).
- **수정 2(DB, `site_settings.top_sidebar`)**: PC 탭의 이미지 뱅크 4장을 모바일 탭에도 그대로 복사(`jsonb_set`) — 관리자가 나중에 모바일 전용으로 다른 이미지를 쓰고 싶으면 그때 바꾸면 됨, 지금은 최소한 "아무것도 안 뜨는" 빈 상태를 벗어나게 함.
- **검증**: 로컬 dev 서버 + 관리자 세션에서 모바일 탭 선택 → "컬럼 너비·순서" 섹션에 경고 문구 노출 확인, 상단 사이드바 트리거를 열어 "Mind Diary" 링크에 실제 마우스 hover → `top_sidebar_bank` 이미지가 실제로 나타나는 것 확인. `tsc`/`lint` 0 errors(80 warnings, 기존 기준선 유지).

## 2026-08-30 (HOTFIX-152.9 — "홈페이지 설정" 관리자 화면: 영상(webm/mp4)을 올린 필드의 미리보기 썸네일이 깨진 이미지로 보이던 문제 수정)
- **사용자 신고**: "사이드바 hover 이미지의 preview가 제대로 안나오고 있어" — 좌/우 사이드바 아이콘의 기본/호버 미디어는 이미지뿐 아니라 영상(webm/mp4)도 업로드할 수 있는데(실제로 지금 저장된 좌/우 hover 값 둘 다 `.webm`), 이 화면의 모든 업로드 필드가 공유하는 `ImageThumb` 컴포넌트가 항상 `<img>`만 그려 영상 URL은 깨진 이미지 아이콘으로만 보였다.
- **구현**: `ImageThumb`(로고/사이드바/상단 아이콘/상단 사이드바 트리거·이미지 뱅크·링크 이미지까지 이 화면의 모든 이미지 썸네일이 공유 — 주석에도 이미 "여기 한 곳만 고치면 전부 적용된다"고 적혀 있었음)에 `SiloTimelineEmbedBlock.tsx`의 배경 슬라이드 썸네일과 동일한 확장자 판정(`/\.(mp4|webm|mov|m4v)(\?|#|$)/i`)을 추가 — 영상이면 작은 썸네일은 `<video muted playsInline>`(첫 프레임이 정지 썸네일로 보임), 마우스를 올렸을 때 뜨는 확대 미리보기는 `<video autoPlay loop muted playsInline>`(실제 hover 시 어떻게 보이는지 재생하며 확인 가능)로 그린다.
- **검증**: 로컬 dev 서버 + 관리자 세션으로 좌측 사이드바 아이콘의 "호버 이미지/영상" 필드(실제 저장된 `.webm` URL)가 이제 `<video>` 엘리먼트로(이전엔 `<img>`) 렌더링되는 것 DOM으로 직접 확인. `tsc`/`lint` 0 errors(80 warnings, 기존 기준선 유지). 이 컴포넌트를 공유하는 다른 11곳(로고 hover/상단 아이콘 hover/상단 사이드바 트리거 등)은 같은 컴포넌트 재사용이라 별도 실측 없이 동일하게 적용됨.
- **부수 확인**: 이 신고를 조사하던 중 HOTFIX-152.8이 배포에 반영된 것도 확인(배포 후 약 45초 만에 새 빌드로 전환) — 직전 "컬럼 너비·순서가 적용 안 된다"는 신고는 그 배포 지연 중 겪은 HOTFIX-152.8 증상이었을 가능성이 높다는 진단을 뒷받침.

## 2026-08-30 (HOTFIX-152.8 — "홈페이지 설정" 관리자 화면: 좌/우/상단 사이드바 패널을 열어둔 채 Controls를 조작하면 패널이 닫히거나 화면 밖으로 사라지던 문제 수정)
- **사용자 신고**: "사이드바의 아이콘을 클릭하고, 그 아이콘의 패널을 클릭해서 설정을 바꾸려고 하는데, 그 해당 사이드바의 설정을 바꾸기위해 어딘가를 클릭하는 순간 사이드바가 닫혀, 그리고 설정 패널을 아래로 스크롤 다운하면, 사이드바가 위로 올라가 버리고 있어." — 실제로 재현해보니 서로 다른 원인의 버그 2개였다.
- **원인 1(클릭하면 닫힘)**: `LeftSidebar.tsx`/`RightSidebar.tsx`/`TopSidebarPanel.tsx`(전역 상단 사이드바 + HOTFIX-152.6의 아이콘별 메가 메뉴 둘 다 재사용)/`Navbar.tsx`의 `IconSidebarPanel`(아이콘별 슬라이드쇼 패널) — 이 4곳 전부 "패널 바깥을 클릭하면 닫는다"는 로직(HOTFIX-141)을 갖고 있는데, 그 "바깥" 판정이 관리자 자신의 Controls/Elements 패널(왼쪽 320px 영역, 캔버스와는 완전히 다른 DOM 트리)까지 "바깥"으로 취급했다 — 그래서 미리보기 패널을 열어둔 채 배경색/폰트/hover 모션 등 Controls의 입력창을 클릭할 때마다 그 즉시 패널이 닫혀, 미리보며 조정하는 게 애초에 불가능했다(주석에도 "바깥(Controls 패널 포함)을 클릭하면 닫는다"고 의도적으로 적혀 있었음 — 즉 설계 당시엔 이게 의도였는데, 실사용해보니 나쁜 UX였던 것).
- **원인 2(스크롤하면 사이드바가 위로 사라짐)**: `admin/navigation/settings/page.tsx`의 캔버스 래퍼(`overflow-auto`)가 `minHeight: 900`만 있고 상한(`maxHeight`/고정 `height`)이 없어, 실제 홈페이지 전체(헤더+본문+푸터, 수천 px)를 그대로 렌더링하면 그 높이만큼 한없이 자라버렸다 — `overflow-auto`가 있어도 "부모보다 큰 콘텐츠를 자를 상한" 자체가 없어 한 번도 작동하지 않은 것. 그 결과 Controls의 아래쪽 입력창을 보려면 캔버스 내부가 아니라 **페이지 전체**가 스크롤됐고, 캔버스 안에 열어둔 좌/우 사이드바 패널(`position:absolute`, 페이지 스크롤에 종속)도 함께 위로 밀려 올라가 화면 밖으로 사라졌다.
- **수정 1**: 4개 패널의 바깥-클릭-닫힘 로직에 `data-admin-controls-panel` 예외 추가 — Controls/Elements 패널 안을 클릭해도 미리보기 패널은 계속 열려 있는다. 진짜 캔버스 바깥(다른 요소, 빈 캔버스 영역)을 클릭하면 여전히 닫힘(회귀 없음, 실측 확인).
- **수정 2**: 캔버스 래퍼의 `minHeight: 900`을 고정 `height: 900`으로 변경 — 이제 `overflow-auto`가 실제로 이 div 내부에서만 스크롤을 만들어내 캔버스가 자체 스크롤바를 갖는다(페이지 전체 높이가 수천 px → 약 1600px로 줄어듦). Controls 패널(`absolute inset-y-0`, 이 래퍼와 같은 조상 기준)도 이제 그 조상이 900px로 고정되면서 자기 자신의 `overflow-y-auto`가 처음으로 실제로 작동해 독립적으로 스크롤된다 — 캔버스를 스크롤해도 Controls나 열린 사이드바 패널이 전혀 움직이지 않는다.
- **검증**: 로컬 dev 서버 + 관리자 세션에서 (1) 좌측 사이드바 패널을 열고 Controls의 색상 입력을 `pointerdown`으로 클릭 → 패널이 계속 `aria-hidden="false"`(열림) 유지 확인, (2) Controls 내부 스크롤 300px → 캔버스 스크롤 위치·열린 패널의 위치(`getBoundingClientRect`)가 전혀 안 바뀌는 것 확인, (3) 진짜 캔버스 바깥(빈 캔버스 영역)을 클릭하면 여전히 정상적으로 닫히는 것(회귀 없음) 확인, (4) 우측 사이드바도 동일하게 열림 유지 확인. `tsc`/`lint` 0 errors(80 warnings, 기존 기준선 유지).

## 2026-08-30 (DB 전용 — 상단 아이콘 1/2 메가 메뉴 설정이 다시 초기화돼 있던 것을 재확인·복구)
- **사용자 신고**: "네가 완료 되었다는 위의 내용 안되는데?" — HOTFIX-152.6에서 저장까지 확인했던 아이콘 1/2의 메가 메뉴 설정이 다시 원상태(아이콘 1: `sidebar.enabled=false`+`href=""`로 빈 아이콘, 아이콘 2: `panelType="slideshow"`로 되돌아감)로 바뀌어 있었다 — 실제로 확인해보니 코드 문제가 아니라 저장된 값 자체가 그렇게 되어 있었다(관리자가 직접 다시 만지다 되돌렸을 가능성이 가장 유력 — 정확한 경위는 알 수 없음).
- **조치**: 아이콘 1/2 둘 다 다시 "이 아이콘 클릭 시 상단 사이드바 열기" 켜고 "메가 메뉴"로 설정 + 이번엔 각각 실제로 구분되는 링크(아이콘1: "아이콘1 전용 메뉴" → `/shop`, 아이콘2: "아이콘2 전용 메뉴" → `/clubs`)까지 추가해 저장 — 빈 상태로만 두면 두 패널이 겉보기에 똑같아(둘 다 로그인/세션 정보만 있고 링크가 없어) "정말 독립적으로 작동하는지" 육안으로 확인하기 어려웠던 점도 함께 해결.
- **검증 중 발견한 함정(실제 버그 아님, 테스트 방법 실수)**: 처음 확인할 때 800px 폭(태블릿 구간, 768~1023px)에서 열어봤더니 링크가 안 보여 "저장이 안 됐나?" 했는데 — 메가 메뉴는 PC/태블릿/모바일이 서로 독립된 설정이고(전역 "상단 사이드바"와 동일한 구조), 이번에 링크를 추가한 건 관리자 화면 상단의 기기 탭이 기본값인 "PC"일 때뿐이라 태블릿/모바일 탭에는 빈 채로 남아있다 — 1280px(PC 폭)에서 다시 열어보니 정상적으로 보였다. **관리자가 태블릿/모바일에서도 같은 메뉴를 보이게 하려면, 관리자 화면 상단의 "🖥️ PC / ▦ 태블릿 / 📱 모바일" 탭을 각각 전환해 그 탭에서도 링크를 추가해야 한다**(전역 상단 사이드바도 처음부터 이렇게 동작해왔음 — 새로 생긴 제약이 아님).
- **검증**: `dev.silostore.net` 실사이트에서 1280px(PC) 폭으로 아이콘 1/2를 각각 클릭 → 서로 다른 메가 메뉴(로그인/세션 정보는 동일하지만 각자의 고유 링크는 다름)가 뜨는 것 실측 확인. 코드 변경 없음(DB 전용) — `tsc`/`lint` 재실행 불필요.

## 2026-08-30 (HOTFIX-152.7 — 상단 아이콘 링크를 사이트 페이지 목록에서 골라 설정할 수 있는 드롭다운 추가)
- **사용자 지시**: "상단 아이콘을 눌렀을때 어떤 페이지로 연결될지 설정하는 것도 넣어줘." — 상단 아이콘의 "링크"는 이미 있었지만(EPIC-152 초기 구현) 관리자가 정확한 경로("/shop" 등)를 직접 타이핑해야 하는 텍스트 입력창뿐이라, 어떤 경로가 존재하는지 알아야만 쓸 수 있었다.
- **구현**: `site_navigations`(사이트 구성 관리가 이미 관리하는 카테고리/게시판 전체 트리)를 조회해 부모-자식 구조를 들여쓰기(전각 공백)로 표현한 계층형 드롭다운을 상단 아이콘 Controls에 새로 추가 — 고르면 곧바로 그 아이콘의 `href`가 채워진다. 기존 텍스트 입력창은 그대로 남겨둬(대체하지 않음) 목록에 없는 경로(예: `/shop`/`/clubs`처럼 최상위 탭 전용 고정 페이지)는 여전히 직접 타이핑할 수 있다 — 드롭다운은 편의 기능으로 얹었을 뿐 기존 자유 입력 경로를 막지 않는다.
- **검증**: 로컬 dev 서버 + 관리자 세션으로 아이콘 1 선택 → 드롭다운에 153개 옵션(About Silo 하위 트리 전체가 계층 들여쓰기로 정확히 나열)이 뜨는 것 확인, "About Silo" 선택 → `icon.href`가 `/about-silo`로 반영되고 아래 텍스트 입력에도 동일하게 반영되는 것까지 엔드투엔드 확인(저장은 안 함, 테스트 값 미반영). `tsc`/`lint` 0 errors(80 warnings, 기존 기준선 유지).

## 2026-08-30 (HOTFIX-151.13 — 온라인 도슨트 타임라인 표지/이벤트 배경 슬라이드에 이미지·영상 일괄(여러 개 동시) 업로드 지원)
- **사용자 지시**: "'온라인 도슨트'의 타임라인 '이벤트' 부분에 슬라이드쇼를 위한 이미지 또는 영상을 여러개 한번에 일괄로 올릴수 있게 해줘." — 지금까지 `SiloTimelineEmbedBlock.tsx`의 배경 슬라이드 업로드(표지/이벤트별 "자유 편집" 공용 `SlideOverlayFieldsEditor`)는 파일 입력이 `multiple` 없이 한 번에 한 장씩만 받아, 슬라이드 여러 장을 채우려면 파일 선택 창을 그만큼 반복해서 열어야 했다.
- **구현**: 파일 입력에 `multiple` 추가 + 선택된 `FileList`를 순차 처리(영상 자동 압축이 ffmpeg.wasm 하나를 공유해 동시에 여러 개 돌리면 위험 — 순차가 안전)하도록 변경. **버그를 피하기 위한 설계**: 매 파일 업로드 직후 바로 `onChange`를 부르면, 이 클로저가 캡처해둔 옛 `value.slideUrls`(부모 state 갱신은 비동기라 이 함수 실행 중엔 안 바뀜)를 계속 스프레드해 이전 파일에서 추가한 URL을 다음 호출이 덮어써버린다 — 그래서 업로드된 URL들을 로컬 배열에 전부 모아뒀다가 마지막에 `onChange` 한 번만 호출한다. 진행 상태 문구도 "[2/5] 압축 중... 43%"처럼 몇 번째 파일인지 보이게 표시하고, 일부 파일이 실패해도 나머지는 계속 진행한 뒤 끝에 "N개 업로드 완료, M개 실패" 요약 알림으로 보여준다(하나 실패했다고 전체를 중단하지 않음). 표지와 이벤트별 배경 슬라이드가 이 컴포넌트 하나를 공유하므로(HOTFIX-147.13 주석 참고) 코드 한 곳만 고쳐 둘 다 자동으로 적용됨.
- **검증**: 로컬 dev 서버 + 관리자 세션으로 "이집트" 페이지 Craft 에디터에서 표지/이벤트("고왕국의 시작") 양쪽 다 실제 DOM의 파일 입력에 `multiple=true`가 걸려 있는 것과 안내 문구("+ 배경 이미지/영상 추가(여러 개 한번에 선택 가능)")가 정확히 뜨는 것 확인. `tsc`/`lint` 0 errors(80 warnings, 기존 기준선 유지). **다음에 확인 필요**: 관리자가 실제로 여러 파일을 한 번에 골라 업로드해 전부(특히 영상이 섞였을 때 순차 압축 진행 상태 문구가 파일마다 정확히 갱신되는지) 정상 반영되는지 확인 — 이번 세션은 브라우저 자동화로 OS 파일 선택 다이얼로그에서 여러 파일을 실제로 고르는 것 자체를 조작하지 못해 코드 로직 + DOM 속성 확인까지만 검증.

## 2026-08-30 (HOTFIX-152.6 — 상단 아이콘마다 독립된 "메가 메뉴"(기존 Kinfolk형 상단 사이드바와 동일한 컬럼/링크형 메뉴) 옵션 추가)
- **사용자 지시**: "새로 만들어진 아이콘과 연결된 독립된 각각의 상단 사이드바를 설정할수 있게 해줘" → 확인 결과 HOTFIX-152.1의 아이콘별 슬라이드다운 패널(이미지 슬라이드쇼)은 이미 아이콘마다 독립적으로 작동하고 있었고, 실제로 원한 건 그 대신 기존 전역 "상단 사이드바"(TopSidebarPanel, 컬럼/링크형 Kinfolk 메가 메뉴)와 동일한 형태를 아이콘마다 독립적으로 갖는 것.
- **구현**: `TopSidebarPanel.tsx`가 이미 `config` prop 하나로 동작하는 범용 컴포넌트였다는 점을 활용 — 렌더링 로직을 복제하지 않고 그대로 재사용. `topBarIconsSettings.ts`의 `TopBarIconSidebar`에 `panelType: "slideshow" | "megaMenu"`와 `megaMenu: TopSidebarValue`(전역 `site_settings.top_sidebar`와 완전히 동일한 pc/tablet/mobile 3단 구조) 필드 추가. `Navbar.tsx`는 `panelType`에 따라 기존 `IconSidebarPanel`(슬라이드쇼) 또는 `TopSidebarPanel`(이 아이콘의 `megaMenu[deviceKey]`로) 중 하나를 렌더링. 관리자 화면(`admin/navigation/settings/page.tsx`)의 기존 `TopSidebarControls`(배경/텍스트색/서체/커스텀 폰트/링크 hover 모션/로그인 버튼 스타일/컬럼 너비·순서 등을 갖춘, 전역 상단 사이드바가 이미 쓰던 완성된 에디터)를 새로 만들지 않고 그대로 재사용 — `value`/`setValue`만 해당 아이콘의 `sidebar.megaMenu`로 바꿔 넘긴다(`hideIntro` prop을 새로 추가해 "헤더 우측의 상단 사이드바 열기 버튼" 운운하는, 전역 전용 소개 문구만 아이콘 컨텍스트에서 생략). 아이콘 Controls 패널에 "패널 종류"(슬라이드쇼/메가 메뉴) 선택창을 추가 — 메가 메뉴를 고르면 기존 높이/자동전환/슬라이드 UI 대신 "캔버스에서 이 아이콘을 클릭해 패널을 연 다음, 패널 안을 클릭하면 편집 화면이 나타난다"는 안내로 바뀐다(전역 상단 사이드바와 완전히 동일한 상호작용 패턴).
- **검증**: 로컬 dev 서버 + 관리자 세션으로 아이콘 1을 "메가 메뉴"로 전환 → 캔버스에서 그 아이콘 클릭 → 패널이 열리고 실제로 로그인/로그아웃, 팔로잉/팔로워, Mind Diary/Studio/Silo Planet 등 전역 상단 사이드바와 동일한 column 1 콘텐츠가 뜨는 것 확인. 패널 안(링크)을 클릭 → Controls가 "상단 아이콘 1 — 메가 메뉴" 전용 에디터로 전환되는 것 확인(이 과정에서 좌표 기반 클릭이 이 페이지가 캔버스를 두 벌 렌더링하는 구조 때문에 엉뚱한 사본을 클릭하는 문제를 겪어, 이후 실제 DOM 요소를 직접 찾아 클릭하는 방식으로 전환해 검증). 그 에디터에서 "+ 링크 추가"로 새 링크를 만들어 — 그 링크가 아이콘 1의 패널(DOM 인덱스로 특정)에만 나타나고 전역 상단 사이드바/아이콘 2 패널에는 나타나지 않는 것까지 확인해 완전한 독립성을 검증. `tsc`/`lint` 0 errors(80 warnings, 기존 기준선 유지). 테스트 데이터는 저장하지 않고(저장하기 버튼 미클릭) 되돌림.

## 2026-08-30 (HOTFIX-152.5 — 상단/좌/우 아이콘 hover 이미지 크기 설정 신설 + 영상 반복 횟수 설정이 실제로는 작동 안 하던 버그 수정)
- **사용자 지시**: "상단 아이콘들, 좌, 우 아이콘들의 hover 했을때 이미지 크기를 조절하는 설정만들어줘, 그리고, 두번째 이미지가 영상일 때 반복 횟수(0=무한 반복) 설정이 제대로 작동 안하고 있어."
- **구현 1(hover 이미지 크기)**: `topBarIconsSettings.ts`에 `TopBarIcon.hoverSizePx`, `sidebarIconsSettings.ts`에 `leftIconHoverSizePx`/`rightIconHoverSizePx` 추가(기본값은 기존 크기와 동일 — 기존 아이콘은 화면이 그대로). `Navbar.tsx`/`LeftSidebar.tsx`/`RightSidebar.tsx`의 아이콘 래퍼를 재구성 — 바깥 박스는 기본/hover 두 크기 중 큰 쪽으로 잡고, 각 미디어는 그 안에서 자기 크기로 중앙 정렬(`absolute left-1/2 top-1/2 -translate-x/y-1/2`)해 서로 다른 크기의 기본/hover 이미지가 자연스럽게 겹치도록 했다. 관리자 화면(Controls 패널)에 "hover 이미지 크기(px)" 입력창을 기존 "크기(px)"/"아이콘 크기(px)" 바로 아래에 추가(상단 아이콘 1곳 + 좌/우 사이드바 각 1곳, 총 3곳).
- **구현 2(반복 횟수 버그 수정) — 진짜 원인**: `SidebarTriggerMedia.tsx`의 hover 크로스페이드 구조는 기본/hover 이미지 둘 다 항상 마운트해두고 CSS(`group-hover`)로 opacity만 전환하는 방식이었다 — 그런데 영상(`TransparentVideo`)은 `autoPlay` 속성 때문에 **마운트 즉시(=페이지 로드 시점)** 재생을 시작했다. 즉 반복 횟수를 예를 들어 2로 설정해도, 사용자가 실제로 마우스를 올리기 훨씬 전에 이미 2번 재생이 끝나 마지막 프레임에 멈춰 있었고, hover해도 그저 정지된 마지막 프레임만 보여 "반복 횟수 설정이 작동 안 한다"는 신고로 이어졌다. `TransparentVideo`에 `active` prop을 추가해 재생 제어를 분리 — 캔버스에 프레임을 그리는 루프는 항상 돌아가되(정지 중에도 마지막 프레임이 보이도록), 실제 재생/반복-횟수 카운트는 `active`가 true로 바뀌는 순간(=진짜 hover 시작)에만 처음부터 시작한다. `LeftSidebar`/`RightSidebar`/`Navbar`(상단 아이콘) 세 곳 모두 CSS만으로 처리하던 hover를 실제 JS state(`onMouseEnter`/`onMouseLeave`)로도 함께 추적해 이 `active` 값을 넘긴다. "always" 모드(hover 없이 항상 노출)나 기본 이미지 쪽은 기존처럼 `active` 기본값 true로 그대로 즉시 재생.
- **검증**: 로컬 dev 서버 + 관리자 세션으로 (1) 새 "hover 이미지 크기(px)" 필드가 상단 아이콘/좌우 사이드바 Controls 패널에 정확히 노출되는 것 확인. (2) 반복 횟수 버그는 실제 운영 DB에 이미 등록된 좌/우 사이드바 hover 영상(webm)으로 엔드투엔드 확인 — 페이지 로드 직후 `document.querySelectorAll('video')`로 두 영상 모두 `paused:true, currentTime:0`(수정 전이었다면 이미 재생 중이었을 것)임을 확인 후, 실제 마우스 hover(브라우저 좌표 클릭이 아니라 진짜 pointer 이동)로 좌측 아이콘 위에 올리자 그 영상만 `paused:false`로 바뀌며 처음부터 재생 시작, 우측은 그대로 정지 상태 유지되는 것까지 확인. `tsc`/`lint` 0 errors(80 warnings, 기존 기준선 유지 — 초기엔 `useState`를 조건부 early return 이후에 둬 `react-hooks/rules-of-hooks` 에러 2건이 났던 것을 발견해 return 이전으로 옮겨 바로잡음).

## 2026-08-30 (HOTFIX-152.4 — 모바일 헤더 실종 버그(HOTFIX-152.2) 재발 시 실제 원인을 잡을 수 있도록 서버/클라이언트 에러 텔레메트리 신설)
- **사용자 지시**: "모바일 navbar regression 계속 조사해줘" — HOTFIX-152.2에서 `NavbarBoundary`(Error Boundary)로 증상은 막았지만, 정확히 어떤 에러가 그 Suspense 구간에서 났는지는 이 세션 내내 특정하지 못했다(콘솔에 에러가 안 찍혔다는 사용자 신고, Vercel 프로덕션 런타임 로그에 접근할 수단이 이 환경엔 없음). 재발 시에도 같은 벽에 부딪히지 않도록, 두 경로 모두에 최소 텔레메트리를 신설.
- **구현**: 새 `client_error_logs` 테이블(Management API, `anon`/`authenticated` insert 허용 + `is_current_user_admin()` 기반 admin 전용 select) + (1) 새 `src/instrumentation.ts`의 `onRequestError`(Next.js 15+ 공식 훅) — 서버가 렌더링 중 잡은 에러를 message/digest/path/routeType/renderSource와 함께 기록(source="server-request-error"). (2) `src/components/NavbarBoundary.tsx`의 `componentDidCatch`에 `reportClientError()` 추가 — 클라이언트에서 실제로 이 Error Boundary가 발동하면 componentStack까지 함께 기록(source="client-boundary").
- **검증**: Next 16.2.11 실제 타입 정의가 문서(`node_modules/next/dist/docs`)와 달라(`RequestErrorContext`에 `renderType` 필드가 없음 — AGENTS.md가 경고한 "문서보다 버전이 새로워 다를 수 있다"의 실례) `tsc` 에러로 걸러짐, 필드 제거해 수정. 로컬 dev 서버에서 `Navbar` 최상단에 쿼리 파라미터로 게이팅한 임시 `throw`를 넣어 실제로 `NavbarBoundary`가 잡는 것 + `client_error_logs`에 componentStack까지 포함된 실제 행이 남는 것을 엔드투엔드로 확인(검증 후 임시 코드는 완전히 제거 — `git diff`로 `Navbar.tsx` 무변경 확인). **테스트 중 발견한 부수 사실**: PostgREST의 `Prefer: return=representation`(RETURNING)은 INSERT 정책과 별개로 SELECT 정책까지 통과해야 하므로, anon에게 SELECT 정책이 없는 이 테이블에 `.insert().select()`를 쓰면 RLS 위반으로 실패한다 — 실제 앱 코드(`supabase-js`의 평범한 `.insert()`, `.select()` 체이닝 없음)는 이 문제와 무관함을 curl로 별도 확인.
- **한계**: 서버측 `onRequestError`가 실제로 이 버그의 실패 지점(Suspense 구간의 스트리밍 완료 실패로 추정)에서 발동하는지는 로컬 dev 모드로는 확정하지 못함(dev 모드는 자체 에러 오버레이 경로가 있어 프로덕션과 다르게 동작할 수 있음) — 클라이언트측 경로(`NavbarBoundary`)는 실제로 발동 시 확실히 잡히는 것을 확인했다. `tsc`/`lint` 0 errors(80 warnings, 기존 기준선 유지). 코드 변경 없음 수준의 작은 추가라 로컬 미리보기로 충분히 검증, 별도 배포 전 확인 불필요.

## 2026-08-30 (HOTFIX-152.3 — 홈 화면 "상점 문" 사진이 거대한 세로 띠로 깨져 보이던 진짜 원인 발견·수정 — HOTFIX-152.2의 "버그 아님" 진단 정정)
- **정정**: 바로 아래 HOTFIX-152.2 항목에서 "세로 상점 사진 + 타일 배경"을 `objectFit:"contain"`+`wallpaperUrls` 기능 때문이라고 진단했던 것은 **틀렸다** — 홈페이지(`page_builder` slug="home")는 이미 `builder_type='craft'`라 그 진단의 근거였던 `site_settings.hero_slideshow`(및 `HeroSlideshow.tsx`)는 `src/app/page.tsx`의 분기(line 90)에서 아예 렌더링되지 않는 죽은 경로였다. 사용자가 "모바일에서는 objectFit을 cover로 바꿔주세요"라고 답해 그 설정을 DB에서 실제로 cover로 바꿨지만(`site_settings.hero_slideshow.mobile.objectFit`), 라이브 화면엔 아무 변화가 없었다 — 이 값 자체는 향후 홈페이지가 다시 `builder_type='native'`로 바뀔 경우를 대비해 그대로 둔다(무해한 설정, 되돌릴 이유 없음).
- **진짜 원인**: 실제 문제는 홈 Craft 페이지에 자유 배치(position 절대좌표)로 얹힌 `SlideshowBlock`(상점 문 사진 4장, "6odfPaThER" 노드) — `src/lib/useFreePosition.ts`의 `freePositionStyle()`이 `position:absolute`에 `top`/`height`를 전부 **%**로 넣는데, 이 박스의 containing block이 (craft.js가 모든 최상위 블록을 형제로 평탄하게 렌더링하는 구조상) 감싸고 있는 히어로 섹션이 아니라 **홈페이지 전체 콘텐츠를 세로로 쌓은 flex 컨테이너**였다(모바일 실측 시 8542px, 데스크톱 7716px). `heightPct=7.39%`가 저장된 시점엔 이 값이 작은 배지처럼 보였겠지만(그 시점의 총 페이지 높이가 지금보다 훨씬 짧았을 가능성), 이후 게시판 임베드/푸터 등 아래쪽 섹션이 늘어나며 총 페이지 높이가 커졌고, 같은 7.39%가 지금은 631px(모바일)까지 부풀어 상점 문 사진 하나가 히어로 아래 여러 섹션을 뚫고 세로로 길게 이어지는 것처럼 보였다 — "타일 패턴"으로 보인 것도 사실 그 아래 있는 골드/아이보리 벽지 섹션의 배경이 사진 좌우로 그대로 드러난 것뿐, 별도의 여백-채움 기능이 작동한 게 아니었다. 관리자 화면의 "모바일 미리보기"가 정상으로 보였던 이유도 같은 구조적 원인의 결과로 추정(미리보기가 실제 발행 페이지와 동일한 전체 높이를 갖지 않았을 가능성) — 확정 검증은 못 함.
- **수정**: 근본적인 free-position 좌표계(`useFreePosition.ts`, 8개 Craft 블록이 공유) 자체를 고치는 대신, 이번에 신고된 이 노드 하나의 `position.heightPct`만 `7.389501306271671` → `2.1`로 재보정(Management API, `page_builder.craft_state`의 해당 노드 값만 문자열 치환 — 정확히 1곳에서만 일치하는 리터럴이라 다른 노드에 영향 없음을 사전 확인). 재보정 후 모바일(375px) 631px→약 128px, 데스크톱 154.9px로 정상적인 배지 크기로 렌더링됨을 실측 확인. **주의**: 근본 원인(자유 배치 height%가 페이지 총 높이 기준이라 아래쪽 콘텐츠가 늘어날 때마다 다시 틀어질 수 있음)은 `useFreePosition.ts` 공용 유틸 자체엔 아직 남아있다 — 이 인스턴스만 수동으로 재보정했을 뿐, 같은 방식(자유 배치 + height%)을 쓰는 다른 페이지의 다른 블록도 향후 아래쪽 콘텐츠가 늘어나면 같은 증상이 재발할 수 있다(NEXT_TASK.md에 후속 과제로 기록).
- 로컬 검증 없이 `dev.silostore.net`(develop 배포)에서 직접 모바일(375×812)/데스크톱 뷰포트로 재현 → DB 값 재보정 → 재현 안 됨까지 실측 확인. 코드 변경 없음(DB 전용) — `tsc`/`lint` 재실행 불필요.

## 2026-08-30 (HOTFIX-152.2 — 실제 모바일에서 헤더(로고/상단 탭)가 완전히 사라지던 심각한 버그 수정 + Silo Planet 오브제 이미지 찌그러짐 수정)
- **사용자 신고**(실제 모바일 크롬 스크린샷 3장): "이 실제 크롬 모바일 화면은 '홈페이지 설정'의 '모바일' 프리뷰와 달라" — 실제 사이트를 실제 모바일 폭에서 열면 헤더(로고+상단 탭 전부)가 완전히 사라지는 심각한 버그였다.
- **원인**: `src/app/layout.tsx`의 `<Suspense fallback={null}><Navbar /></Suspense>`(Navbar가 `useSearchParams()`를 써서 필요, 오래전부터 있던 구조)가 Error Boundary 없이 단독으로 쓰이고 있었다. 모바일 폭에서 Navbar 렌더링이 스트리밍 도중 뭔가에 걸리면(정확한 근본 원인은 React/Next.js 16 스트리밍 내부 동작이라 완전히 특정하지 못함 — 콘솔에 에러가 안 찍힘) React가 그 Suspense 구간의 완료 신호(`$RC` 스왑 스크립트)를 영영 못 내보내는 상태에 빠져, 서버가 `<div hidden id="S:0">` 안에 이미 만들어둔 실제 헤더 HTML을 영원히 안 보이는 상태로 남긴다. `git worktree`로 오늘 세션 시작 전 커밋(99579a1)을 별도로 체크아웃해 교차 검증한 결과 — **이 버그는 오늘 세션에서 새로 생긴 게 아니라 이미 있던 문제**(로컬/`dev.silostore.net` 프로덕션 양쪽에서 동일하게 재현, 데스크톱 폭에서는 재현 안 됨). 관리자의 "홈페이지 설정 관리"/Craft 에디터 "모바일 프리뷰"는 이 root layout의 Suspense 경로를 아예 안 거치고 `&lt;Navbar editable /&gt;`를 직접 렌더링하는 별개 코드 경로라 지금까지 아무도 이 버그를 못 봤다(관리자 프리뷰가 실제 화면과 다르다고 느낀 근본 원인).
- **수정**: 새 `src/components/NavbarBoundary.tsx`(Error Boundary)로 그 Suspense를 감싼다 — 로컬에서 "Error Boundary 있음/없음"만 바꿔가며 4회 교차 검증(있음→정상, 없음→재현, 있음→정상, 없음→재현)해 이 수정이 확실히 원인을 해소함을 확인했다(React 공식 권장 패턴이기도 함 — Suspense는 항상 Error Boundary와 짝지어 쓰는 게 안전). 만약 향후 실제로 렌더 에러가 나면(다른 원인으로) 사이트 전체를 못 쓰게 만들지 않도록 최소한 홈 링크는 보여준다.
- **겸사겸사 수정(같은 신고 스크린샷 — "오브제 이미지들이 이상하게 나와")**: `src/components/about-silo/AboutSiloUniverse.tsx`의 `SpaceObjectSprite`(Silo Planet 우주 공간에 떠 있는 이미지 오브제)가 `scale`을 항상 정사각형(`[baseScale, baseScale, 1]`)으로 그려, 정사각형이 아닌 업로드 이미지(꽃/열쇠 아이콘 등)가 그 비율만큼 찌그러져 보이고 있었다 — 도입 커밋(HOTFIX-127)부터 계속 있던 버그. 텍스처 로드 후 실제 픽셀 가로/세로 비율을 읽어 긴 쪽을 기존 크기에 맞추고 짧은 쪽만 비율대로 줄이도록 수정(이미지가 더 커지지는 않음, 찌그러짐만 해소).
- **신고 스크린샷 중 나머지 하나는 버그 아님으로 확인**: 홈 화면 슬라이드쇼에 세로로 긴 상점 문 사진이 좌우 여백을 타일 패턴 배경으로 채운 채(`objectFit: "contain"` + `wallpaperUrls` 랜덤 채움, EPIC-036/039 때 의도적으로 만든 기능) 보이던 것 — 코드 버그가 아니라 관리자가 이미 설정해둔 슬라이드쇼 표시 방식(전체 보기+여백 배경)이 이 특정 세로 사진에서 좁은 모바일 화면에 두드러져 보이는 것뿐이다. 임의로 설정을 바꾸지 않고 사용자 확인 요청.
- `tsc`/`lint` 0 errors(80 warnings 스탬프 — stash 비교로 이 수정이 새 경고를 추가하지 않았음을 확인). 로컬 dev 서버(모바일 뷰포트) + 실제 `git worktree` 교차 검증으로 헤더 노출/폭/높이 정상화 확인. Silo Planet은 로컬에서 페이지 렌더링/콘솔 에러 없음까지 확인(정확한 개별 오브제 이전/이후 비주얼 diff까지는 이번 세션에서 캡처 못함 — 다음에 확인 필요).

## 2026-08-29 (HOTFIX-152.1 — 상단 아이콘마다 독립적인 슬라이드다운 사이드바 + 좌/우/상단 아이콘 hover 이미지 표시 방식·반복 횟수 설정)
- **사용자 지시**: "각각의 상단 아이콘마다 새로운 상단 사이드바가 나오도록 해줘. 그 상단 사이드바의 위아래 폭과, 슬라이드쇼도 가능하게 해줘." + "좌, 우, 그리고 각각의 상단 아이콘 마다, hover 하면 두번째 이미지가 나올지, 아니면 hover 없이 계속 두번째 이미지가 나올지, 두번째 이미지가 모션이라면 몇번 loop 할지 설정할수 있게 해줘."
- **구현 1(아이콘별 사이드바)**: `topBarIconsSettings.ts`에 `TopBarIcon.sidebar: {enabled, heightPx, slides, autoAdvanceSeconds}` 추가 — 기존 `top_sidebar`(TopSidebarPanel, 컬럼형 메가 메뉴, 전역 1개)와는 별개인 훨씬 단순한 형태로, 아이콘마다 독립적으로 여러 개 만들 수 있다. `sidebar.enabled`면 그 아이콘은 링크 대신 이 패널을 열고/닫는 토글이 된다. 새 컴포넌트 `IconSidebarPanel`(Navbar.tsx)이 TopSidebarPanel과 동일한 검증된 슬라이드다운 애니메이션/Escape·바깥 클릭 닫기 패턴을 재사용하고, 내용은 `HeroSlideshow`를 그대로 재사용(슬라이드 이미지/제목/설명, heroSlideshow.ts의 SlideItem과 동일 모양).
- **구현 2(hover 표시 방식/반복 횟수)**: `sidebarIconsSettings.ts`(좌/우 각각 `leftIconHoverMode`/`rightIconHoverMode` + `leftIconHoverLoopCount`/`rightIconHoverLoopCount`)와 `topBarIconsSettings.ts`(아이콘마다 `hoverMode`/`hoverLoopCount`)에 필드 추가. `hoverMode: "always"`면 크로스페이드 없이 두번째 이미지를 고정으로 표시. `SidebarTriggerMedia.tsx`의 영상 재생 로직을 수정 — 지금까지 `<video loop>`(네이티브, 무한 반복만 가능)였던 것을, `loopCount>0`이면 `loop` 속성을 끄고 `ended` 이벤트를 직접 세어 N번 재생 후 마지막 프레임에서 멈추도록 변경(네이티브 `loop=true`는 `ended`가 아예 발생하지 않아 횟수를 셀 방법이 없었음). `LeftSidebar.tsx`/`RightSidebar.tsx`/`Navbar.tsx`(상단 아이콘) 3곳 모두 동일한 분기 적용.
- `tsc`/`lint` 0 errors(79 warnings, 기존 기준선 유지). **세션 중 발견한 별개 문제**: 로컬 dev 서버 브라우저 검증 중 응답이 5분 이상 걸리는 이상 증상 발생 — `Get-Process node`로 확인해보니 오늘 하루 여러 번의 서버 재시작 과정에서 남은 것으로 보이는 좀비 프로세스가 CPU 시간 4시간+/비정상적인 메모리를 누적하고 있었다. 그 프로세스를 종료하고 `.next` 캐시를 지운 뒤 재시작해 해결 — 코드 문제가 아니었음(CLAUDE.md에 이미 기록된 ".next 캐시가 꼬여 가짜 에러" 사례와 유사한 계열, 이번엔 좀비 프로세스가 원인이라는 점만 다름). 로컬 dev 서버 + 관리자 세션으로 실측: 좌측 사이드바 아이콘의 새 hover 방식 드롭다운("항상"으로 전환 확인), 상단 아이콘 추가 → hover 방식/반복 횟수 필드 노출 → "상단 사이드바 열기" 체크 → 높이/자동전환/슬라이드 추가 UI 노출 → 슬라이드 1개 추가까지 확인, 실제로 그 아이콘을 클릭해 패널이 열림/닫힘(CSS 클래스 `-translate-y-full`↔`translate-y-0` 전환)까지 확인. 저장은 하지 않고 되돌림(테스트 데이터 미저장). **다음에 확인 필요**: 실제 슬라이드 이미지를 올리고 저장 후 공개 화면에서 패널이 정상 작동하는지, 영상(webm/mp4)을 hover 이미지로 올려 반복 횟수 제한이 실제로 동작하는지(이번 세션은 파일 업로드 자동화가 안 돼 코드 리뷰까지만 검증).

## 2026-08-29 (EPIC-152 — "홈페이지 설정 관리"에 상단 아이콘 추가 기능 신설, 드래그로 위치 이동 가능)
- **사용자 지시**: "'홈페이지 설정'에 상단에 아이콘을 추가하고 페이지와 링크하는 기능을 만들어줘. 그리고 그 아이콘은 드래그앤 드랍으로 위치를 설정할수 있어야해" — 조사해보니 기존 아이콘 관련 설정(main_logo/sidebar_icons/top_sidebar 트리거)은 전부 "이미지는 있지만 링크는 없음"(토글 버튼이거나 항상 `/`로 고정)이었고, 유일하게 이미지+링크 페어를 가진 `top_sidebar.links`는 메가 메뉴 안에서만 보이는 항목이라 상단에 상시 노출되는 아이콘이 아니었다 — "이미지+링크를 가진, 헤더에 항상 보이는, 자유 위치 아이콘"은 이번에 새로 만든 개념.
- **구현**: `src/lib/topBarIconsSettings.ts`(신규) — `site_settings.top_bar_icons` 키에 `{icons: [{id, imageUrl, hoverImageUrl, href, sizePx, alt}]}` 저장(accountMenuStyleSettings.ts의 extraItems와 동일하게 기기별로 나누지 않음 — 같은 아이콘/링크가 PC든 모바일이든 같아야 자연스러움). 위치는 새 시스템을 만들지 않고 기존 `header_positions`(HeaderSlot 드래그 메커니즘, `src/components/HeaderSlot.tsx`)를 그대로 재사용 — 각 아이콘이 `slotKey="top-bar-icon:<id>"`로 다른 모든 헤더 요소(로고/탭/계정 메뉴 등)와 완전히 동일한 방식으로 드래그된다(기기별 독립 위치도 자동으로 딸려옴). `Navbar.tsx`에 `topBarIconsOverride` prop + 렌더링 추가(로고와 동일한 기본/hover 이미지 크로스페이드), `/admin/navigation/settings`의 Elements 탭에 "상단 아이콘" 목록+"+ 아이콘 추가" 버튼, Controls 패널에 이미지 업로드/링크/크기/삭제 UI 추가.
- `tsc`/`lint` 0 errors(79 warnings, 기존 기준선 유지 — 처음엔 불필요한 `eslint-disable` 주석을 복붙해 80으로 늘었던 것을 제거해 바로잡음). 로컬 dev 서버 + 관리자 세션으로 실측: "+ 아이콘 추가" → 새 아이콘이 Controls 패널에 뜨는 것, 링크 입력("/shop") 반영, 드래그 핸들로 실제로 캔버스에서 아이콘이 이동하는 것(가로/세로 좌표가 실제로 바뀌고 화면에서도 위치 이동 확인), 숫자 좌표 직접 입력도 동작하는 것까지 확인 — 테스트로 만든 아이콘은 저장하지 않고 되돌림(실제 이미지 파일 업로드까지는 이 세션의 브라우저 자동화로는 검증 못 함, 파일 선택 다이얼로그 자동화 불가). **참고**: 이 세션이 계속 다뤄온 EPIC-151(타임라인) 작업과 무관한 새 기능이라 규칙(신규 기능=새 EPIC 번호)에 따라 새 브랜치 `feature/EPIC-152`로 분리.

## 2026-08-29 (HOTFIX-151.12 — 5개 리프 카테고리 페이지에 Craft 에디터 신설)
- **사용자 신고**: "위에 말한 5개의 카테고리의 '페이지 수정'에 craft 에디터가 없잖아" — 확인해보니 이 5개(이집트/바빌론/그리스/로마제국/비잔틴 제국)뿐 아니라 사이트의 모든 리프(하위) 카테고리 페이지가 애초에 `builder_type='native'`였다 — Craft 에디터는 지금까지 허브 페이지(고대 문명 ~ 침략 등)에만 만들어져 있었다.
- **구현**: 이 5개 페이지를 `builder_type='craft'`로 전환하고, 기존에 갖고 있던 게시판 임베드(`page_modules` board 위젯)를 시각적으로 동일한 `SiloTimelineEmbedBlock`(mode="board", 해당 게시판 id 그대로)으로 옮겨 craft_state를 새로 채웠다(Management API, DB 직접 작성) — 기존 `page_modules` 행은 그대로 남겨뒀다(사용 안 되지만 되돌리기 쉽게). `src/app/[...slug]/page.tsx`의 `CRAFT_RENDERERS`와 `src/app/admin/pages/[id]/page.tsx`의 `CRAFT_EDITORS`에 5개 slug 등록(둘 다 기존 `CraftDocentRenderer`/`CraftDocentEditor` 셸 재사용, 새 컴포넌트 없음). 이제 이 페이지들에서도 배경 슬라이드/자유 편집/모션/영상 자동 압축 등 지금까지 허브 페이지에서만 쓸 수 있던 기능을 그대로 쓸 수 있다.
- `tsc`/`lint` 0 errors(79 warnings, 기존 기준선 유지). 로컬 dev 서버에서 이집트 페이지 실측: 공개 화면이 기존과 동일하게(타임라인 그대로) 렌더링되는 것, 관리자 "페이지 수정"에 "Craft 에디터 열기" 버튼이 뜨는 것, 실제로 열어보니 "모드: 게시판 하나 / 게시판: 이집트"로 정확히 연결된 설정 패널이 뜨는 것까지 확인(저장은 안 함 — DB에 이미 동일한 상태가 있어 불필요). **다음에 확인 필요**: 나머지 4개(바빌론/그리스/로마제국/비잔틴 제국)도 이집트와 동일하게 작동할 것으로 보이나 개별 실측은 안 함(같은 컴포넌트 공유, 같은 방식으로 마이그레이션).

## 2026-08-29 (HOTFIX-151.11 — 타임라인 이벤트 "자유 편집" 오버레이가 "자세히 보기" 링크를 완전히 가리던 문제 수정 + 샘플 게시글 8개로 빈 타임라인 채움)
- **사용자 신고**: "'고대문명 ~ 침략' 페이지의 '이집트', '바빌론', '그리스' 이벤트에서 그 해당 페이지로 넘어가는 버튼이 없어" — 실측 확인해보니 세 이벤트 전부 "이 화면에 자유 편집 켜기"(커스텀 배경 슬라이드쇼 오버레이, `SlideOverlayFieldsEditor`/`TimelineCoverOverlay`)가 켜져 있었고, 이 오버레이가 native TL3 슬라이드(헤드라인 + "자세히 보기" 링크가 포함된) 전체를 완전히 가리고 있었다 — 오버레이 자체엔 그 링크가 없어서 결과적으로 해당 이벤트를 클릭해도 페이지로 넘어갈 방법이 없었다.
- **사용자 질문**("지금 설정은 이미 존재하는 이벤트 화면에 덮어 씌운것 처럼 보여. 오리지널 이벤트 화면을 수정하는거였는데 불가능한거야?")**: 구조상 맞는 관찰이다 — 오버레이는 native TL3 슬라이드를 "대체 편집"하는 게 아니라 그 위에 별도로 그리는 레이어다(native 콘텐츠 자체는 site_navigations의 제목/설명에서 자동 생성되며 이 오버레이 패널로 직접 편집하는 대상이 아님). 이미 3개 이벤트 모두 배경 이미지/영상을 업로드해둔 상태라(되돌리면 그 작업이 날아감) 오버레이를 끄는 대신, **오버레이 안에도 같은 "자세히 보기" 링크를 그리도록** 수정 — `/api/timeline/events`가 이제 각 이벤트에 `href`를 함께 내려주고(group/board 모드 둘 다), `SiloTimelineEmbedBlock`이 이를 불러와 `TimelineCoverOverlay`에 전달, 오버레이가 켜진 이벤트에도 설명 아래 밑줄 링크로 렌더링한다.
- **DB 전용(코드 아님)**: 위 신고를 조사하다 발견한 별개 문제 — 이집트/바빌론/로마제국/비잔틴 제국 게시판(HOTFIX-151.10에서 `timeline_ng`로 고침) 4개가 실제 게시글이 0개라 타임라인이 비어 보였다(사용자 확인 후 승인). 각 게시판에 그 시대의 실제 역사적 이정표를 다룬 샘플 게시글 2개씩(총 8개, `(샘플)` 표기 + 본문에 "[샘플 게시글 — 실제 콘텐츠로 교체 필요]" 명시, `timeline_year`를 각 시대에 맞게 설정)을 추가해 타임라인이 실제로 채워진 모습을 보여주도록 함 — 실제 역사 고증이 아니므로 나중에 진짜 콘텐츠로 교체 필요.
- `tsc`/`lint` 0 errors(79 warnings, 기존 기준선 유지). 로컬 dev 서버에서 이집트 이벤트로 이동 → 오버레이 안에 "자세히 보기" 링크가 실제로 뜨는 것 + 클릭 시 실제로 이집트 페이지로 이동하는 것까지 확인. 이집트 게시판도 샘플 게시글("고왕국의 시작 (샘플)")이 타임라인 카드로 실제 표시되는 것 확인.

## 2026-08-29 (HOTFIX-151.10 — DB 전용: 로마제국/비잔틴 제국 게시판에 타임라인 레이아웃 누락 수정)
- **사용자 신고**("내가 위에 5가지 카테고리의 페이지에 타임라인을 만들어달라고 했잖아!!! 왜 없어")**: HOTFIX-151.6 세션에서 새로 만든 이집트/바빌론 게시판과 기존 그리스 게시판은 `render_type='timeline_ng'`로 만들어졌지만, 이미 있던 로마제국/비잔틴 제국 게시판 2개는 `render_type='community'`로 남아있었다 — 그래서 그 두 페이지만 실제 타임라인 레이아웃(검색창/정렬/연도 필터/카드형 타임라인) 대신 일반 커뮤니티 목록으로 렌더링되고 있었다. 두 게시판을 `timeline_ng`로 업데이트(Management API, 코드 변경 없음).
- **실측 확인**: 로컬 dev 서버에서 이집트/로마제국/비잔틴 제국 3개 페이지 전부 스크린샷으로 직접 확인 — 이제 그리스와 동일한 타임라인 레이아웃(제목 카드/검색창/정렬 드롭다운)이 뜬다. **다만 5개 게시판 중 그리스(6개)만 실제 게시글이 있고 나머지 4개(이집트/바빌론/로마제국/비잔틴 제국)는 게시글이 0개라 "아직 게시글이 없습니다"로 비어 보인다** — 레이아웃 자체는 정상이지만 실제 콘텐츠(날짜 있는 게시글)가 아직 채워지지 않아 타임라인이 시각적으로 빈 상태다. 콘텐츠 작성은 이번 작업 범위 밖.

## 2026-08-29 (HOTFIX-151.9 — 영상 자동 압축 목표 용량을 30MB 고정값에서 타임라인별 설정으로 변경, 기본값도 8MB로 하향)
- **사용자 지시**: "아까 적용한 영상 자동 압축기능에서 '압축 목표 mb' 를 설정할수 있게 해줘. 30 mb 도 굉장히 높은 거 아니야? 웹사이트 로딩에" — 맞는 지적이라 기본값 자체를 8MB로 낮추고(짧게 반복 재생되는 배경 영상엔 충분한 화질), `SiloTimelineEmbedBlockProps.videoCompressTargetMb`로 타임라인(Craft 노드)마다 조절 가능하게 뺐다(입력 범위 2~45MB — Supabase Storage 실 상한 50MB보다 낮게 강제해 압축의 의미를 유지). `src/lib/videoCompress.ts`의 `compressVideoIfNeeded()`는 이제 "50MB 초과일 때만 압축"이 아니라 "목표 용량을 넘으면 압축"으로 트리거 기준 자체를 목표값에 맞춰 일치시켰다(예: 목표 8MB일 때 20MB 영상도 이제 압축 대상 — 이전엔 50MB 밑이라 그냥 통과됐음). 이 필드가 없던 기존 5개 카테고리 페이지의 저장된 craft_state에도 `?? 8` 폴백으로 안전하게 적용됨.
- `tsc`/`lint` 0 errors(79 warnings, 기존 기준선 유지). 로컬 dev 서버 + 관리자 세션으로 "고대 문명 ~ 침략" 설정 패널에 새 입력창이 기본값 "8"로 뜨는 것(기존 저장 데이터에 필드가 없어도 폴백 정상 작동), 값 변경(15) 시 Craft 노드 prop이 실제로 갱신되는 것까지 확인(테스트 값은 저장하지 않고 페이지 이동으로 되돌림).

## 2026-08-29 (HOTFIX-151.8 — 타임라인 슬라이드가 순서대로만 넘어가던 것을 랜덤 순환으로 변경)
- **사용자 지시**: "craft 에디터-> 타임라인 -> 이벤트 슬라이드 의 이미지가 순서대로만 넘어가는데, 랜덤으로 넘어가게 해줘" — `CoverBackground`(`SiloTimelineEmbedBlock.tsx`, 표지/이벤트 오버레이 배경 슬라이드 공용 렌더러)의 자동 전환 타이머가 항상 `(i + 1) % length`로 순증가했던 것을, 매번 무작위 인덱스를 뽑되 직전에 본 슬라이드는 제외하고 고르도록 교체(같은 슬라이드가 바로 다시 나오는 어색함 방지). 공용 컴포넌트 한 곳 수정이라 표지/모든 이벤트 오버레이 전부에 자동 적용.
- `tsc`/`lint` 0 errors(79 warnings, 기존 기준선 유지). 로컬 dev 서버에서 "고대 문명 ~ 침략" 표지(슬라이드 7개)의 실제 전환 순서를 30초간 실측 — `0 → 2 → 6 → 3 → 4 → 5`로 순차 증가가 아닌 것 확인.

## 2026-08-29 (HOTFIX-151.7 — 타임라인 영상 슬라이드 썸네일/자동 압축/패닝 모션 3종 수정)
- **사용자 신고**: "온라인 도슨트의 'craft 에디터' 에 타임라인 이벤트 슬라이드에 영상을 업로드하면 preview 썸네일이 안나오고 있어" — `SlideOverlayFieldsEditor`(`SiloTimelineEmbedBlock.tsx`, 표지/이벤트 오버레이 배경 슬라이드 편집 공용 컴포넌트, 카테고리 전체 공유)의 썸네일 그리드가 확장자와 무관하게 항상 `<img>`로 그려 `.mp4` URL은 깨진 이미지 아이콘이 됐다 — 이미 있던 `CoverBackground`의 `isVideo` 정규식 판정을 재사용해 영상이면 `<video>`(재생 없이 첫 프레임이 정지 썸네일)를 그리도록 수정.
- **사용자 지시**: "50mb 이상의 영상이 업로드 되면, 30mb 아래로 변환해서 업로드되도록 만들어줘. 모든 타임라인 게시판에 적용해" — Supabase Storage 무료 플랜 50MB 고정 상한(HOTFIX-144.5 참고)에 걸려 그보다 큰 영상은 업로드 자체가 실패했다. 서버 쪽 트랜스코딩 인프라가 없어(ffmpeg 미설치, Vercel 서버리스) 브라우저에서 ffmpeg.wasm(`@ffmpeg/ffmpeg`+`@ffmpeg/util`, 신규 의존성)으로 직접 재인코딩하는 `src/lib/videoCompress.ts`(`compressVideoIfNeeded`)를 신설 — 50MB 넘는 영상만 길이 기반 역산 비트레이트로 30MB 아래까지 압축, 그 외엔 원본 그대로 통과. 코어 wasm/js는 CDN 의존 없이 `public/vendor/ffmpeg/`에 로컬로 내려받아 둠(`public/vendor/timelinejs`와 동일한 관례, COOP/COEP 헤더가 필요 없는 싱글스레드 코어 사용). 타임라인 배경 슬라이드 업로드 지점이 `SlideOverlayFieldsEditor` 한 곳뿐이라 이 한 번의 연결로 표지/이벤트 오버레이 전부(카테고리 페이지 전체)에 자동 적용됨 — 압축 중엔 업로드 버튼 문구가 진행 상태(로딩/퍼센트/완료 용량)로 바뀌고 입력이 비활성화된다.
- **사용자 지시(둘 중 확인 후 선택 — "영상 슬라이드의 확대/이동 효과")**: `CoverBackground`가 영상 슬라이드는 항상 `!isVideo` 조건에 걸려 Ken-Burns 확대/이동(패닝) 클래스가 아예 안 붙었던 것을 제거 — 이미지와 동일하게 `<video>`에도 `onLoadedMetadata`로 가로/세로 판정 후 패닝 클래스+스타일을 적용한다.
- `tsc`/`lint` 0 errors(79 warnings, 기존 기준선 유지). 로컬 dev 서버 + 관리자 세션(공개 페이지 "고대 문명 ~ 침략" 및 그 Craft 에디터)에서 실측: (1) 표지 배경 슬라이드 7개 중 실제 `.mp4` 3개가 편집 패널 썸네일에서 `<video>` 태그로 렌더링되는 것 DOM 조회로 확인, (2) 같은 3개 `.mp4`가 공개 페이지에서 `silo-cover-pan-tour` 클래스를 실제로 갖는 것 확인(수정 전엔 빈 문자열), (3) 벤더링한 `ffmpeg-core.wasm`(32MB)이 `/vendor/ffmpeg/`에서 200으로 서빙되고 브라우저 `WebAssembly.compile()`로 정상 컴파일되는 것 확인. **다음에 확인 필요**: 실제로 50MB 넘는 영상 파일을 관리자가 직접 업로드해 브라우저 압축 전체 플로우(수 분 소요 가능)와 최종 용량이 30MB 아래인지 실측 확인 — 이번 세션은 그 정도 크기의 실제 영상 파일이 없어 wasm 로딩/컴파일까지만 검증하고 실제 인코딩 실행은 못 함. 참고로 `ffmpeg-core.wasm`(32MB)이 git에 새로 커밋됨 — 저장소 용량이 그만큼 늘어난다.

## 2026-08-28 (HOTFIX-151.6 — 좌/우 사이드바에서 병합된 탭 이름이 사라지던 문제 수정: 탭마다 독립적으로 접고 펼 수 있게 개편)
- **사용자 신고**: "사이드바에 about silo 가 없잖아 이 바보야, about silo 를 열고 접을수 있게 하고, '사일로상점' 도 두 카테고리 모두 접어놔. 오른쪽 사이드바도 마찬가지야" — HOTFIX-144.2의 `mergeSidebarTabs()`가 같은 슬롯(sidebar-left/right)을 공유하는 여러 탭의 `groups`를 하나의 배열로 flatMap해버려서, 패널 헤더엔 "주인" 탭(dropdown과 겸용이 아닌 쪽, 예: 사일로 상점/살롱데상) 이름만 남고 나머지 탭 이름("About Silo"/"온라인 도슨트")은 화면에서 완전히 사라졌었다(그룹 내용 자체는 섞여서 다 보였지만 어느 탭 소속인지 알 수 없었음).
- **구현**: `mergeSidebarTabs`를 제거하고 병합하지 않은 원본 탭 배열을 그대로 `LeftSidebar`/`RightSidebar`에 전달(`tab` → `tabs` prop). 두 컴포넌트 모두 각 탭을 자기 이름을 단 최상위 접고/펼치기 섹션으로 렌더링하도록 재구성(기존 그룹별 접고/펼치기는 그 안에 한 단계 더 중첩) — 새 최상위 토글은 요구사항대로 전부 기본 접힘. 결과: 왼쪽 사이드바를 열면 "About Silo"와 "사일로 상점 Silo Store"가 각각 독립된 ▶ 섹션으로 보이고, 오른쪽은 "온라인 도슨트 Online Docent"와 "살롱데상 Salon des Cent"가 그렇게 보인다.
- `tsc`/`lint` 0 errors(79 warnings, 기존 기준선 유지). 로컬 dev 서버에서 좌/우 사이드바 모두 열어 두 탭 이름이 각각 독립적으로 보이는 것과, "About Silo"만 펼쳤을 때 그 하위 그룹만 나타나고 "사일로 상점"은 그대로 접혀 있는 것까지 실측 확인(반대쪽 사이드바도 동일하게 확인).

## 2026-08-28 (HOTFIX-151.5 — "고대 문명 ~ 침략" 페이지에 관리자용 Craft 에디터 등록)
- **사용자 지시**: "온라인 도슨트의 '고대문명~침략' 페이지에 craft 에디터 만들어줘" — HOTFIX-151.3에서 공개 렌더러(`CRAFT_RENDERERS`, `src/app/[...slug]/page.tsx`)에는 등록해 방문자 화면엔 타임라인이 정상 노출됐지만, 관리자 편집 화면(`/admin/pages/[id]/page.tsx`)의 별도 `CRAFT_EDITORS` 목록엔 빠뜨려서 "페이지 수정"에서 "이 slug 전용 Craft 에디터가 없어요" 경고만 뜨고 편집을 못 하는 상태였다. 형제 카테고리 4개(고대~왕정/혁명~제국/프로이트~대중문화/디지털~AI)와 동일하게 `CraftDocentEditor`를 등록해 해결.
- `tsc` 0 errors. 로컬 dev 서버 + 관리자 세션으로 "Craft 에디터 열기" 버튼이 뜨고 실제로 전체화면 에디터(섹션 팔레트/캔버스)가 정상 로드되는 것까지 실측 확인(저장 없이 닫음).

## 2026-08-28 (HOTFIX-151.4 — 사이트 메뉴에 "게시판 추가" 버튼 신설 + 게시판 배정 시 빈 위젯 중복 생성 버그 수정)
- **사용자 지시**: "'미분류 게시판'을 사이트메뉴로 드래그해도 배정이 안 된다, 차라리 '추가' 버튼 옆에 '게시판 추가' 버튼을 만들어달라" — `CategoryTreeManager.tsx`의 드래그앤드롭 게시판 배정 로직(`handleBoardDragEnd`, EPIC-087-PHASE-B)은 실제로는 동작하지만, 좁은 드롭 영역을 정확히 맞히기 까다로웠다. 같은 배정 로직을 클릭 한 번으로도 쓸 수 있는 `AssignBoardButton`("게시판 추가")을 각 행의 "추가" 버튼 왼쪽에 추가 — 미배정 게시판이 있을 때만 노출되고, 클릭하면 목록에서 골라 그 행(페이지)에 바로 배정한다.
- **재발 방지 버그 수정(사용자가 직접 발견 — 바로크/르네상스/로코코에 게시판을 배정했더니 게시판이 두 개 보임)**: 게시판 배정 시 목적지 페이지에 이미 있는 "게시판이 연결되지 않았어요" 빈 위젯(`board_id=null`)을 무시하고 항상 새 위젯을 추가로 만들던 버그(`handleBoardDragEnd`) — 목적지 페이지에 빈 board 위젯이 있으면 그 자리를 채우고, 없을 때만 새로 만들도록 수정. 드래그앤드롭과 새 "게시판 추가" 버튼 둘 다 이 수정된 로직을 공유한다.
- `tsc` 0 errors, `lint` 0 errors(79 warnings, 기존 기준선 유지). 로컬 dev 서버 + 관리자 세션으로 "사이트 구성 관리"에서 "게시판 추가" 버튼이 모든 행에 노출되는 것, 클릭 시 미배정 게시판 드롭다운이 뜨는 것, "제국~군주 → 로마제국"에 게시판을 실제로 배정해 DB에 정상 반영되는 것(빈 슬롯이 이미 채워진 페이지라 새 위젯이 추가된 것도 확인 — 의도된 동작)까지 실측 확인.
- **덤으로 확인한 사실(사용자 질문 — "10개 orphan 페이지가 뭔데? 미분류 페이지에서 찾을 수 있나?")**: 이전에 삭제했다 복구한 10개 중복 페이지는 "사이트 구성 관리" 하단의 별도 "미분류 페이지 (30)" 패널(원래 138개 하드코딩 라우트 중 nav 미연결분만 추적하는 다른 목록)이 아니라, "사이트 메뉴" 트리 자체의 "미분류 페이지" 행(접혀 있음, 펼치면 보임) 아래에 전부 들어있는 것을 실측 확인.

## 2026-08-28 (HOTFIX-151.3 — 도슨트 타임라인 BC/AD 연대 파싱 버그 수정 + "고대 문명 ~ 침략" Craft 등록 + 표지/타임라인 UX 개선 묶음)
- **사용자 신고**: "'제국~군주' 페이지에 '로마제국'과 '비잔틴 제국'이 이벤트에 없어" — 실제로는 이벤트 자체가 없었던 게 아니라, `parseCategoryTitle()`(`src/lib/timelineGroup.ts`)이 "BC 146~AD 476 로마제국"/"AD 476~1453 비잔틴 제국"처럼 시작은 BC, 끝은 AD인 제목을 파싱하지 못해(기존 정규식은 시작 연도 앞에만 BC를 허용) null로 폴백 → 호출부가 순번 기반 fallback 날짜(2000년대)를 대신 써서 두 이벤트가 타임라인의 엉뚱한 위치로 밀려나 안 보이는 것처럼 보였다. 시작/끝 연도 각각에 독립적으로 BC/AD 접두사를 허용하도록 정규식을 고쳐 해결(끝 연도에 접두사가 없으면 기존처럼 시작 연도의 시대를 물려받아 기존 저장 제목과 100% 하위 호환).
- **사용자 지시(사이트 구성관리에서 새 카테고리 "고대 문명 ~ 침략"을 만들고 craft_state를 채움)**: `src/app/[...slug]/page.tsx`의 `CRAFT_RENDERERS` 등록 목록에 `"online-docent-ancient-invade"` 추가 — 등록하지 않으면 `builder_type='craft'`여도 조용히 native로 폴백해 애써 만든 타임라인이 안 보이는 기존 함정(HOTFIX-147.3 주석 참고)에 새 카테고리도 걸려 있었다.
- **사용자 신고("제목 스타일을 바꾸면 설명 스타일도 같이 바뀌는 것처럼 보인다, 분리해달라")**: `SiloTimelineEmbedBlock.tsx`의 표지/이벤트 설명 필드가 커스터마이징 안 된 상태(null)일 때 지금까지 "제목의 현재 값"에서 실시간 파생됐던 것(크기 50%, 색상/폰트/정렬 동일)을 제목과 무관한 고정 기본값(`DEFAULT_DESCRIPTION_*`)으로 교체 — 필드는 분리돼 있어도 기본값이 제목을 따라가 체감상 "같이 바뀐다"로 보이던 문제 해결.
- **사용자 신고("타임라인이 로딩되면 첫 이벤트가 너무 오른쪽에 있어, 나머지도 한눈에 보이게")**: TimelineJS3 벤더 코드가 항상 "현재 마커를 트랙 가운데 정렬"만 하는 고정 공식이라 표지 로드 시에도 결국 첫 이벤트가 가운데로 옴 — `SiloTimelineInner.tsx`에 `alignTimeNavToOverview()`를 추가해 최초 로드 1회에 한해 첫 마커의 실제 화면 위치를 직접 측정해 왼쪽 여백(40px)으로 재배치, 전체 이벤트가 한눈에 보이게 함(사용자가 마커를 직접 클릭한 뒤에는 TL3 기본 동작인 가운데 정렬을 그대로 둠).
- **사용자 지시("대시보드의 빈 영역을 클릭하면 표지로 이동하게")**: TimeNav는 마커 클릭에만 반응하고 빈 트랙 영역엔 리스너가 없던 것을 클릭 시 TL3 공식 API `goToStart()`(타입 선언 `knight-lab-timelinejs.d.ts`에 추가) 호출로 표지 복귀 + 위 개요 정렬 재적용까지 연결.
- **사용자 지시("폰트를 최근 고른 곳부터 드롭다운이 시작하도록, 맨 위엔 최근 사용 폰트 8개")**: `src/lib/recentFonts.ts`(신규, localStorage 기반) + `FontPicker.tsx`에 "최근 사용" 섹션 추가 — DB 컬럼 없이 기기별 UI 편의 기능으로 구현.
- `tsc` 0 errors, `lint` 0 errors(79 warnings, 기존 기준선 유지).
- **다음에 확인 필요**: 관리자가 실제 화면에서 로마제국/비잔틴제국 이벤트가 올바른 연대 위치에 나오는지, "고대 문명 ~ 침략" 페이지 타임라인이 정상 렌더링되는지, 빈 영역 클릭 시 표지 복귀가 자연스러운지 육안 확인.

## 2026-08-28 (HOTFIX-147.29 — 타임라인 배경 패닝을 단순 왕복에서 모서리를 도는 "랜덤 투어" 모션으로 전면 개편)
- **사용자 지시**: "그냥 나는 자동으로 슬라이드 이미지의 구석구석을 보도록 오른쪽으로 갔다가 아래로, 위로 갔다 왼쪽으로, 왼쪽 위에서 오른쪽 아래로 갔다가 직선으로 위로, 오른쪽 아래에서 왼쪽 위로 갔다가 직선으로 아래로, 왼쪽 아래에서 오른쪽 위로 갔다가 왼쪽으로, 같은 랜덤 이미지 모션이 있으면 좋겠어. 자동으로 알고리즘같이 생겨서 가로, 세로 이미지에 맞춰서 질리지 않는 모션이 되도록. 이미지의 일부분만 나오는게 아니라 전체를 다 즐길수 있도록 하는 거 말야."
- **구현**: `panDirection`이 `null`/`"auto"`일 때(기본값, 별도 설정 안 한 모든 슬라이드)의 동작을 좌우/상하 2점 왕복에서 **4개 정거장을 도는 "투어" 모션**으로 전면 교체 — `TOUR_TEMPLATES`(7종: 모서리 시계/반시계, 귀퉁이 시계/반시계, 대각선+직선 조합 3종 — 사용자가 예로 든 패턴들을 최대한 그대로 반영)에서 이미지 URL을 해시(djb2, `hashString`)해 하나를 결정론적으로 골라 쓴다 — 매 렌더마다 다시 뽑지 않아 애니메이션이 끊기지 않고, 이미지마다 다른 경로를 타 "질리지 않게" 자동으로 다양해진다. `horizontal`/`vertical`/`diagonal-down`/`diagonal-up`은 이제 "이 단순 패턴 하나만 고정하고 싶을 때"의 수동 오버라이드로 격하.
  - **가로/세로에 맞춤**: `buildTourVars()`가 가로 이미지는 x축(좌우) 이동을, 세로 이미지는 y축(상하) 이동을 100%, 반대 축은 60%만 써서 경로 모양 자체가 이미지 방향에 맞게 눌린 타원형이 된다.
  - **"전체를 다 즐기도록"**: 기존 기본값(속도 8초·확대 15%·이동 8%)으로는 이동 폭이 작아 이미지의 좁은 일부만 보였다 — 기본값을 20초·확대 40%·이동 12%로 올려 4개 귀퉁이/모서리를 실제로 도는 게 체감되게 함. 안전 여유분 공식(`|이동%| <= 50*(zoom-1)/zoom`)을 직접 유도해 재확인 — 40% 확대에서 안전 한계는 14.29%, 실제 이동 12%로 여유 있게 이내(화면 가장자리가 비어 보이지 않음). 이 기본값은 새 필드 `panSpeedSeconds`/`panZoomPct`/`panDistancePct`가 모두 여전히 조절 가능하므로(HOTFIX-147.27) 관리자가 더 크게/작게 바꿀 수 있다.
  - CSS 구현: 4단계 키프레임(`silo-cover-pan-tour`, 0%/25%/50%/75%/100%=0%로 끊김 없이 루프) + 슬라이드별 8개 CSS 변수(`--silo-pan-p0x`~`p3y`)로 정거장 좌표만 주입 — 키프레임 자체는 공용 하나.
- **적용 범위**: 이전 HOTFIX들과 동일하게 `CoverBackground`(`SiloTimelineEmbedBlock.tsx`) 한 곳만 고쳐 4개 온라인 도슨트 카테고리 페이지 전부에 자동 반영 — 관리자가 아무 설정도 안 바꿔도(기본값 `auto`) 바로 적용된다.
- **실측 검증**: 로컬 dev 서버, "고대~왕정" 실제 공개 페이지에서 아무 설정도 안 바꾼 기본 상태의 배경 이미지가 `silo-cover-pan-tour` 애니메이션(20초, 40% 확대)으로 실행 중인 것과, 4개 정거장 좌표(`--silo-pan-p0x`~`p3y`)가 실제로 서로 다른 값(이 이미지는 가로라 x=±12%, y=±7.2%로 눌린 타원형)으로 주입돼 있는 것, `transform` matrix가 실시간으로 x/y 둘 다 0이 아닌 값으로 움직이는 것까지 확인.
- `tsc` 0 errors, `lint` 0 errors(79 warnings, 기존 기준선 유지).
- **다음에 확인 필요**: 관리자가 실제 화면에서 여러 이미지에 걸쳐 정말 서로 다른 투어 경로를 타는지(이번 세션은 슬라이드가 1장뿐인 페이지로만 확인해 "다양성" 자체는 코드 로직 검증까지만 함), 전체 사이클(20초)이 지루하지 않고 자연스럽게 느껴지는지 육안 확인.

## 2026-08-28 (HOTFIX-147.28 — 타임라인 배경 패닝 모션에 대각선 방향 추가)
- **사용자 지시**: "가로 이미지는, 좌, 우도 있지만 대각선에서 대각선으로 움직이는것도 추가해줘."
- **구현**: `SlideOverlayConfig`에 `panDirection`(`"auto" | "horizontal" | "vertical" | "diagonal-down" | "diagonal-up" | null`, 표지는 `coverPanDirection`) 필드 추가 — `null`/`"auto"`면 지금까지처럼 이미지 원본 비율로 자동 판정(가로→좌우, 세로→상하), 그 외엔 관리자가 직접 고른 방향을 그대로 쓴다. `PAN_KEYFRAMES`에 대각선 2종 키프레임(`silo-cover-pan-diag-down`: 왼쪽 위→오른쪽 아래 ↘, `silo-cover-pan-diag-up`: 오른쪽 위→왼쪽 아래 ↙) 추가 — x/y축을 동시에 같은 `--silo-pan-dist`만큼만 움직여, HOTFIX-147.27에서 확인한 확대 배율의 안전 여유분을 그대로 재사용(축마다 독립적으로 여유분이 있어 대각선도 별도 계산 없이 안전). `SlideOverlayFieldsEditor`(표지·이벤트 공용 폼)의 "배경 패닝 모션" 섹션에 "이동 방향" 드롭다운(자동/좌우/상하/대각선 ↘/대각선 ↙) 추가.
- **적용 범위**: 이전 HOTFIX들과 동일하게 `CoverBackground`/`SlideOverlayFieldsEditor`(`SiloTimelineEmbedBlock.tsx`) 한 곳만 고쳐 4개 온라인 도슨트 카테고리 페이지 전부에 자동 반영.
- **실측 검증**: 로컬 dev 서버 + 관리자 세션, "고대~왕정" Craft 에디터에서 새 "이동 방향" 드롭다운의 5개 옵션 노출 확인 → "대각선 ↘"로 바꾸자 캔버스의 실제 이미지 클래스가 `silo-cover-pan-diag-down`으로, `getComputedStyle().transform`이 x/y 둘 다 동시에 0이 아닌 값(`matrix(1.15, 0, 0, 1.15, -113.62, -44.62)`)으로 바뀌는 것까지 실시간 확인(x축만/y축만이 아니라 대각선으로 실제 움직임 — 테스트 값은 저장하지 않고 에디터를 닫음).
- `tsc` 0 errors, `lint` 0 errors(79 warnings, 기존 기준선 유지).
- **다음에 확인 필요**: 관리자가 실제 화면에서 대각선 두 방향(↘/↙)이 기대한 대로 자연스럽게 보이는지, 저장 후 새로고침해도 유지되는지 확인.

## 2026-08-28 (HOTFIX-147.27 — 타임라인 배경 패닝 모션 속도/강도 조절 가능하게 + 배경 슬라이드 프리뷰 썸네일 확대)
- **사용자 신고**: "너무 느리고 움직임이 적어, 그걸 조절할 수 있게 해줘, 그리고 배경슬라이드 프리뷰가 너무 작아."
- **패닝 모션 조절**: HOTFIX-147.26까지 패닝(확대+이동)이 고정값(속도 18초→8초, 확대 15%, 이동 4%)이었다 — `SlideOverlayConfig`에 `panSpeedSeconds`/`panZoomPct`/`panDistancePct`(전부 `| null`, 미설정 시 기본값 8초/15%/8% — 기존 저장 데이터 회귀 없음) 3개 필드 추가, 표지는 기존 flat prop 패턴대로 `coverPanSpeedSeconds` 등 대응 필드 추가. `CoverBackground`가 이 값들을 CSS 커스텀 프로퍼티(`--silo-pan-duration`/`--silo-pan-zoom`/`--silo-pan-dist`)로 각 이미지에 인라인 주입하고, `PAN_KEYFRAMES`는 `var()`로 이 값을 읽어 적용 — 키프레임 자체는 하나만 두고 슬라이드마다(표지/각 이벤트) 독립적으로 조절 가능. `SlideOverlayFieldsEditor`(표지·이벤트 공용 폼)에 "배경 패닝 모션" 섹션으로 속도/확대 배율/이동 거리 3개 입력창 추가.
- **배경 슬라이드 프리뷰 확대**: 지금까지 32×32px 정사각형 아이콘 크기라 어떤 이미지인지 거의 안 보였다 — 2열 그리드에 96px 높이 썸네일(가로는 칸 전체 폭)로 키우고, 파일명/삭제 버튼을 썸네일 아래로 옮겨 한눈에 알아볼 수 있게 함.
- **적용 범위**: 이전 HOTFIX들과 동일하게 `CoverBackground`/`SlideOverlayFieldsEditor`(`SiloTimelineEmbedBlock.tsx`) 한 곳만 고쳐 4개 온라인 도슨트 카테고리 페이지 전부에 자동 반영.
- **실측 검증**: 로컬 dev 서버 + 관리자 세션, "고대~왕정" Craft 에디터에서 (1) 새 배경 패닝 섹션의 속도/확대/이동 입력창이 각각 placeholder 8/15/8로 노출되는 것, (2) 배경 슬라이드 썸네일이 `h-24 w-full`(96px)로 커진 것, (3) 속도 입력을 3으로 바꾸자 캔버스의 실제 이미지 `--silo-pan-duration`이 즉시 `3s`로, `getComputedStyle().animationDuration`도 `3s`로 바뀌는 것까지 실시간 반영 확인(테스트 값은 저장하지 않고 에디터를 닫음).
- `tsc` 0 errors, `lint` 0 errors(79 warnings, 기존 기준선 유지).
- **다음에 확인 필요**: 관리자가 실제로 원하는 속도/강도로 값을 저장해보고 새로고침 후에도 유지되는지, 실제 화면(Browser pane 열린 상태)에서 체감 속도가 적당한지 육안 확인.

## 2026-08-27 (HOTFIX-147.26 — 타임라인 배경 패닝 모션이 세로만 보이고 가로는 안 보이던 문제 — object-position 대신 확대(scale)+이동(translate)으로 교체)
- **사용자 재신고**: "보니까 슬라이드 모션이 위 아래는 되고 있는데 왼쪽 오른쪽은 안 되고 있어. 가로 이미지를 줌인해서 왼쪽에서 오른쪽으로 가는 모션으로 해줘. 이번에도 모든 '온라인 도슨트' 바로 아래 하위 카테고리 페이지의 타임라인에 모두 적용해줘."
- **원인**: HOTFIX-147.25가 `object-position`을 애니메이션하는 방식을 썼는데, 이건 `object-fit: cover`가 실제로 "잘라내는" 여유분 안에서만 움직임이 보인다 — 그런데 이 여유분이 생기는 축은 이미지 자신의 가로/세로가 아니라 **이미지 비율과 컨테이너 비율의 상대적 관계**로 정해진다. 표지 영역이 가로로 아주 넓은 배너인데, 실측한 "고대~왕정" 배경 이미지(1280×808, 표준 사진 비율)는 컨테이너 폭에 이미 정확히 맞아떨어져 가로 방향엔 잘리는 부분이 아예 없고(세로만 크게 넘쳐 잘림) — 그래서 "가로 이미지"인데도 가로 패닝이 시각적으로 전혀 안 보였던 것(세로는 이 컨테이너 형태상 거의 항상 여유분이 커서 우연히 잘 보였을 뿐).
- **수정**: `object-position` 방식을 버리고, 이미지를 `scale(1.15)`로 강제 확대해 축과 무관하게 항상 여유분을 만든 뒤 그 안에서 `translateX`/`translateY`로 이동하는 방식으로 교체(`transform: scale(1.15) translate...`) — 사용자가 요청한 "줌인해서 이동"과 정확히 일치하고, 이미지/컨테이너 비율에 더 이상 좌우되지 않아 항상 보인다. 부모(`TimelineCoverOverlay`)가 이미 `overflow-hidden`이라 확대된 여유분이 밖으로 새지 않는다. 세로(portrait) 쪽도 같은 방식으로 통일(기존에 "잘 보이던" 것도 사실 이 컨테이너 형태에 우연히 의존하고 있었을 뿐이라, 다른 화면 비율/높이 설정에서도 안정적으로 보이도록 함께 교체).
- **적용 범위**: HOTFIX-147.25와 동일하게 `CoverBackground`(`SiloTimelineEmbedBlock.tsx`) 한 곳만 고쳐 4개 온라인 도슨트 카테고리 페이지 전부에 자동 반영.
- **실측 검증**: 로컬 dev 서버, "고대~왕정" 실제 공개 페이지의 등록된 배경 이미지(1280×808)에서 `getComputedStyle().transform`이 `matrix(1.15, 0, 0, 1.15, -58.18, 0)`(scale 1.15 + 실제 음수 방향 가로 이동값)로 확인 — HOTFIX-147.25 때는 이 정확히 같은 이미지에서 가로 이동이 0이었던 것과 대조. 이번 세션은 Browser pane이 화면에 표시되지 않아(HOTFIX-151.2 때와 동일한 환경 제약) 애니메이션이 실시간으로 진행되는 것 자체(여러 프레임에 걸친 값 변화)까지는 확인 못 함 — CSS 메커니즘 자체가 결정론적으로 항상 움직임을 보장하는 방식(강제 확대 후 이동)이라 화면에서도 정상 작동할 것으로 판단.
- `tsc` 0 errors, `lint` 0 errors(79 warnings, 기존 기준선 유지).
- **다음에 확인 필요**: 관리자가 실제 화면(Browser pane 열린 상태 또는 실제 브라우저)에서 가로 이미지가 왼쪽↔오른쪽으로, 세로 이미지가 위↔아래로 실제로 부드럽게 움직이는지 육안 확인. 줌 배율(15%)이나 이동 폭(±4%)이 너무 과하거나 부족하면 `PAN_KEYFRAMES`의 `scale(1.15)`/`4%` 값 조정.

## 2026-08-27 (HOTFIX-147.25 — 온라인 도슨트 타임라인 표지/이벤트 배경 이미지에 방향별 자동 패닝(Ken Burns) 모션 추가)
- **사용자 지시**: "각 이벤트의 '배경이미지' 슬라이드마다 이미지가 가로이미지는 왼쪽에서 오른쪽으로, 세로이미지는 위에서 아래로 scope가 이동하는 모션이 있으면 좋겠어 ... 이걸 모든 '온라인 도슨트' 바로 아래 하위 카테고리 페이지의 타임라인에 모두 적용해줘."
- **구현**: 표지/이벤트 배경을 그리는 `CoverBackground`(`SiloTimelineEmbedBlock.tsx`)는 4개 온라인 도슨트 카테고리 페이지(고대~왕정/혁명~제국/프로이트~대중문화/디지털~AI문화) 전부가 공유하는 유일한 컴포넌트라, 여기 한 곳만 고치면 자동으로 전부 적용된다. 이미지가 로드될 때 `naturalWidth`/`naturalHeight`로 실제 원본 비율을 재서 가로(landscape)/세로(portrait)를 판정 — 가로 이미지는 `object-position`을 `0% 50% → 100% 50%`(왼쪽→오른쪽), 세로 이미지는 `50% 0% → 50% 100%`(위→아래)로 18초 주기 `ease-in-out infinite alternate`(왕복) 애니메이션. `object-fit: cover`가 실제로 잘라내는 여유분 안에서만 패닝이 의미가 있으므로 "전체 보기"(`backgroundFit: "contain"`, 이미 잘리는 부분이 없음)와 영상 슬라이드는 대상에서 제외.
- **실측 검증**: 로컬 dev 서버, "고대~왕정" 실제 공개 페이지에서 등록된 표지 배경(1280×808, 가로)의 실제 렌더링 `<img>`를 확인 — `className`에 `silo-cover-pan-x` 부여됨, `getComputedStyle().animationName === "silo-cover-pan-x"`(실제로 애니메이션 실행 중), `objectPosition`이 고정값 아닌 애니메이션 중간값으로 관측됨. "혁명~제국" 페이지도 같은 컴포넌트가 로드되는 것 확인(다만 이 카테고리는 아직 배경 슬라이드 자체가 설정 안 돼 있어 패닝 대상 이미지가 없음 — 코드는 공유되므로 관리자가 배경을 설정하는 즉시 동일하게 적용됨).
- `tsc` 0 errors, `lint` 0 errors(79 warnings, 기존 기준선 유지).
- **다음에 확인 필요**: 세로 이미지(portrait) 배경을 실제로 등록해 위→아래 패닝이 기대한 대로 보이는지 육안 확인(이번 세션은 등록된 배경이 전부 가로 이미지라 세로 방향은 로직상으로만 검증). 18초 주기가 너무 느리거나 빠르게 느껴지면 `PAN_KEYFRAMES`의 `18s` 값 조정.

## 2026-08-27 (HOTFIX-147.24 — 온라인 도슨트 타임라인 Craft 설정 패널의 "개별 화면(이벤트) 자유 편집"이 드롭다운으로 펼쳐져 비효율적이던 문제 — 캔버스 네비게이션에 따라 자동 전환되도록 통합)
- **사용자 신고**: "온라인 도슨트"의 "고대~왕정" 페이지를 Craft로 수정 중 — "오른쪽 패널에 '개별화면(이벤트) 자유편집'이 아래로 펼쳐지네? 이게 전혀 효율적이지 않아. 그냥 그 다음 이벤트 화면이 나타나서 그걸 클릭하면, 그 화면에 대한 설정을 하게 하면 안돼?"
- **원인/기존 UX**: HOTFIX-147.13이 "표지 자유 편집"(항상 노출)과 "개별 화면(이벤트) 자유 편집"(드롭다운으로 이벤트 하나를 찾아 골라야 그 아래로 긴 폼이 펼쳐짐) 두 섹션을 따로 뒀다 — 카테고리에 이벤트가 많을수록 원하는 이벤트를 드롭다운에서 찾아 스크롤해야 했고, 고르고 나면 패널 전체가 아래로 길게 밀렸다.
- **수정**: 캔버스(타임라인 대시보드)에서 "지금 실제로 보고 있는 화면"(표지 또는 특정 이벤트)을 렌더 컴포넌트(`SiloTimelineEmbedBlock`)가 이미 `coverState`로 추적하고 있었다 — 문제는 설정 패널(`SiloTimelineEmbedSettings`)이 같은 Craft 노드를 보는 완전히 별개의 React 컴포넌트 인스턴스라 이 값을 전혀 몰랐던 것. 노드 id별로 이 값을 공유하는 작은 외부 스토어(`useSyncExternalStore` 기반 `useActiveSlide`/`publishActiveSlide`, DB에 저장할 필요 없는 순수 UI 상태라 `setProp`/`setCustom` 대신 사용)를 추가해, 렌더 컴포넌트가 캔버스 네비게이션마다 발행하고 설정 패널이 구독한다. "표지 자유 편집"과 "개별 화면(이벤트) 자유 편집" 두 섹션을 "지금 보고 있는 화면 자유 편집" 하나로 통합 — 캔버스에서 다른 화면을 클릭하면 패널이 자동으로 그 화면의 설정(표지면 `coverXxx` 필드, 이벤트면 `eventOverlays[eventId]`)으로 전환된다. 수동 이벤트 드롭다운은 제거.
- **실측 검증**: 로컬 dev 서버 + 관리자 세션, "고대~왕정" Craft 에디터에서 타임라인 노드 선택 → 패널이 "지금 표지(첫 화면)를 보고 있어요" 표시 확인 → 캔버스에서 "next" 슬라이드 컨트롤로 "그리스" 이벤트로 이동 → 패널이 자동으로 "지금 "그리스 Greeks" 화면을 보고 있어요"로 전환되는 것 확인(수동 선택 없이) → "Return to Title"로 표지로 복귀 → 패널이 다시 표지 편집으로 전환되는 것까지 왕복 확인. 테스트 중 아무 필드도 저장하지 않고 에디터를 닫음(프로덕션 데이터 변경 없음).
- `tsc` 0 errors, `lint` 0 errors(79 warnings, 기존 기준선 유지 — 신규 경고 없음).
- **다음에 확인 필요**: 관리자가 다른 카테고리(혁명~제국 등, 이벤트 개수가 다른 페이지)에서도 캔버스 네비게이션에 따라 패널이 원하는 대로 자동 전환되는지, 실제로 이벤트별 자유 편집 필드를 입력·저장 후 새로고침해도 유지되는지 확인.

## 2026-08-27 (HOTFIX-151.2 — 커스텀 폰트 관리 미리보기가 너무 느리게 뜨던 문제 — 등록된 폰트 전부를 동시에 다운로드하고 있었음)
- **사용자 신고**: HOTFIX-151.1로 미리보기 자체는 뜨게 됐지만 "커스텀 폰트의 미리보기가 로딩이 너무 느려."
- **원인**: `@font-face` 선언 자체는 다운로드를 트리거하지 않지만, 그 `font-family`로 실제 텍스트를 그리는 순간 브라우저가 파일을 받아온다 — `/admin/fonts` 목록과 `FontPicker.tsx` 드롭다운 둘 다 등록된 **모든** 폰트에 대해 미리보기 텍스트를 마운트 즉시 렌더링하고 있었다. 로컬 dev 서버로 실측하니 등록된 폰트가 **154개**(대부분 TTF/OTF — 특히 한글 폰트는 풀 완성형 글리프 때문에 파일 용량이 큼)까지 늘어나 있어, 페이지가 뜨자마자 154개 폰트 파일을 전부 동시에 다운로드하기 시작해 대역폭을 나눠 먹으며 전체 미리보기가 느려지는 구조였다.
- **수정**: 신규 공용 컴포넌트 `LazyFontPreview`(`src/components/admin/LazyFontPreview.tsx`) — `IntersectionObserver`(`rootMargin: 200px`)로 실제로 화면에 보이는(또는 곧 보일) 항목에만 `fontFamily` 스타일을 적용해 그 시점에야 다운로드가 시작되게 하고, 보이기 전에는 기본 글꼴로 텍스트만 먼저 보여준다. `/admin/fonts` 목록과 `FontPicker.tsx` 드롭다운 둘 다 이 컴포넌트로 교체.
- **부수 발견**: 이 컴퓨터의 `node_modules`가 `package.json`과 어긋나 있었다 — EPIC-148에서 추가된 `jsbarcode`/`qrcode`가 다른 컴퓨터에서 설치·커밋만 되고 여기서는 `npm install`을 안 거쳐 dev 서버 전체가 500 에러로 안 뜨는 상태였다(내 폰트 수정과 무관, `npm install`로 해결).
- **실측 검증**: 로컬 dev 서버 + 관리자 세션, 실제 등록된 154개 폰트로 `/admin/fonts` 로드 후 `read_network_requests`로 확인 — 커스텀 폰트 파일 요청이 **0건**(수정 전이었다면 154개 동시 요청). `IntersectionObserver`가 실제로 스크롤에 반응해 켜지는 것 자체는, 이 세션의 Browser pane이 화면에 표시되지 않아(`document.visibilityState === "hidden"`) 렌더링 파이프라인이 멈춰 있어(수동으로 만든 별도 테스트 옵저버도 동일하게 콜백 무발동 확인 — 표준 브라우저의 백그라운드 탭 스로틀링, 컴포넌트 로직 문제 아님) 이번 세션에서는 시각적으로 확인 못 함.
- `tsc` 0 errors, `lint` 0 errors(79 warnings, 기존 기준선 유지 — 신규 경고 없음).
- **다음에 확인 필요**: 관리자가 실제 화면(Browser pane이 열린 상태)에서 `/admin/fonts`를 스크롤하며 화면에 들어올 때마다 폰트 미리보기가 순차적으로(전체가 한꺼번에가 아니라) 로드되는지, 전체 체감 속도가 실제로 빨라졌는지 육안 확인.

## 2026-08-27 (HOTFIX-151.1 — "커스텀 폰트 관리"에서 새로 올린 폰트가 미리보기 안 되던 문제 — 페이지가 @font-face를 아예 주입한 적이 없었음)
- **사용자 신고**: EPIC-151(일괄 업로드) 배포 직후 사용자가 실제로 폰트 ~100개를 한 번에 일괄 업로드해봤는데 — "커스텀 폰트 관리 > '등록된 폰트'에 폰트 미리보기가 전혀 안 되는데 새로 올린것들이."
- **원인**: `/admin/fonts` 페이지가 지금까지 **자기만의 별도 supabase 쿼리**(`loadFonts()`)로 목록만 채웠을 뿐, `@font-face` 규칙을 문서에 실제로 주입하는 코드가 이 페이지엔 전혀 없었다 — 그동안 미리보기가 되는 것처럼 보였던 건 순전히 **다른 컴포넌트**(전역 `Navbar` 등, `useCustomFonts()`를 부르는 곳)가 페이지 로드 시점에 이미 있던 폰트들을 먼저 주입해준 덕분이었다. 그래서 "그 시점 이후 새로 올린 폰트"는 목록엔 뜨지만 실제 `<style>` 태그에 규칙이 없어 항상 기본 글꼴로만 보였다 — 정확히 사용자가 방금 대량으로 새로 올린 폰트들이 이 케이스였다.
- **수정**: `/admin/fonts`가 자체 쿼리 대신 다른 화면들과 동일한 유일한 주입 경로인 `useCustomFonts()`(`src/lib/useCustomFonts.ts`)를 그대로 쓰도록 교체 — 업로드 직후 호출하는 `refetch()`가 이제 새 폰트까지 포함해 `<style id="custom-fonts-style">`를 다시 써준다. 관리 화면은 기존처럼 최신순으로 보여줘야 해서(훅 자체는 이름순) 화면 안에서 `createdAt` 기준으로 다시 정렬만 별도로 한다.
- **실측 검증**: 로컬 dev 서버로 사용자가 실제로 올린 105개 폰트 전부에 대해 `document.fonts`(브라우저 Font Loading API)로 로드 상태를 직접 확인 — 98개 `loaded`(정상 로드), 7개는 아직 화면에 실제로 그려지지 않아 지연 로드 중인 `unloaded`(정상적인 지연 로딩 동작, 실패 아님), `error` 상태는 0개. 리디바탕/학교안심 리코더 등 서로 확연히 다른 스타일로 렌더링되는 것도 스크린샷으로 확인.
- `tsc` 0 errors, `lint` 0 errors(80 warnings 기존 기준선에서 79로 — 자체 fetch용 `useEffect`를 제거하면서 그 자리에 걸려있던 경고 1개가 오히려 줄어듦).

## 2026-08-27 (EPIC-151 — 폰트 드롭다운 자동 닫힘 버그 수정 + 일괄 업로드 + 모든 폰트 미리보기)
- **사용자 신고**: "craft에디터의 오른쪽 설정 패널에서 폰트 드롭다운을 누르는데, 자꾸 저절로 해제가 되" + "이 silo 플랫폼에서 사용할 폰트를 그냥 일괄적으로 올리고 싶어. 그리고 모든 폰트는 프리뷰가 나오게 되어야해."
- **원인(자동 닫힘)**: `FontPicker.tsx`의 드롭다운 패널이 `onMouseLeave={() => setOpen(false)}`로만 닫혔다 — 버튼과 패널 사이 좁은 틈을 지나가거나 패널 안쪽 스크롤/모서리 근처에서 마우스가 아주 살짝만 벗어나도 곧바로 닫혀버리는 매우 예민한 방식이었다.
- **수정(자동 닫힘)**: 이 코드베이스가 이미 쓰고 있는 표준 드롭다운 패턴(`UserMenuDropdown.tsx`/`MembershipPopover.tsx`와 동일 — ref + document `mousedown` 바깥 클릭 감지 + Escape)으로 교체. 마우스가 패널 안팎을 자유롭게 오가도 저절로 닫히지 않고, 바깥을 실제로 클릭하거나 Escape를 눌러야만 닫힌다.
- **일괄 업로드**: `FontPicker.tsx`(에디터 내 인라인 업로더)와 `/admin/fonts`(전용 관리 화면) 둘 다 파일 입력에 `multiple` 추가 — 여러 폰트 파일을 한 번에 선택하면 전부 순차 업로드+등록된다. 파일마다 이름을 직접 타이핑할 수 없으므로 파일명에서 자동으로 이름을 만든다(`deriveFontNameFromFilename`, 신규 공용 헬퍼 — `src/lib/media.ts`, 예: `MyBrand-Bold.woff2` → `MyBrand Bold`). 일부 파일이 실패해도(형식 오류/이름 중복 등) 나머지는 계속 진행하고 성공/실패 개수와 실패 사유를 함께 보여준다.
- **모든 폰트 미리보기**: `/admin/fonts` 목록이 지금까지 폰트 이름 자체를 그 폰트로 렌더링할 뿐이었다(영문 이름이 대부분이라 한글 지원 여부를 못 보여주고, 몇 안 되는 글자로는 스타일 판단이 어려움) — `FontPicker.tsx`가 이미 쓰던 한글/영문/숫자 혼합 샘플 문구를 공용 상수(`FONT_PREVIEW_TEXT`, `src/lib/media.ts`)로 뽑아 두 화면이 동일한 미리보기를 쓰게 했다. 등록된 폰트 4개 전부 실제로 다른 스타일로 렌더링되는 것을 스크린샷으로 확인.
- **실측 검증**: 로컬 dev 서버 + 관리자 세션. (1) `/admin/fonts`에서 `multiple` 파일 입력 확인 + 등록된 4개 폰트가 전부 새 샘플 문구로 서로 다르게 렌더링되는 것 스크린샷 확인. (2) Craft 에디터에서 폰트 드롭다운을 연 뒤 `mouseleave`/`mouseout`을 강제로 발생시켜도 패널이 안 닫히는 것 확인 → 실제 바깥 클릭(`mousedown`)에는 정상적으로 닫히는 것 확인(비동기 리렌더 대기 후 재확인).
- `tsc` 0 errors, `lint` 0 errors(80 warnings, 기존 기준선 유지).

## 2026-08-27 (HOTFIX-147.23 — 타임라인 표지 배경 슬라이드 이미지가 항상 잘려서(object-cover) 보이던 문제 — "전체 보기" 옵션 추가)
- **사용자 재신고**: HOTFIX-147.22(제목 위치)를 고친 뒤 "아니 내말은 왜 슬라이드 이미지가 전부 안보이고 잘려서 보이냐는 말이야" — 원래 신고가 텍스트 위치가 아니라 **배경 이미지 자체가 잘려 보이는 것**이었음을 명확히 함.
- **원인**: `CoverBackground`(표지 배경 슬라이드쇼)가 지금까지 `object-cover`로 고정돼 있었다 — 이 스타일은 컨테이너를 빈틈없이 채우기 위해 이미지 비율이 컨테이너와 다르면 남는 부분을 무조건 잘라낸다. 업로드한 사진의 비율과 표지 영역(TL3 표지 슬라이드의 실제 높이)의 비율이 다르면 위/아래 또는 좌우가 크게 잘려 나가 사진의 일부만 보였다 — 텍스트 위치와는 무관한, 별개의 원인.
- **수정**: `SlideOverlayConfig`/`SiloTimelineEmbedBlockProps`에 `backgroundFit: "cover" | "contain" | null` 신설(기본값 `null` → 기존과 동일한 "꽉 채우기" 유지, 마이그레이션 불필요) — `SlideOverlayFieldsEditor`에 "배경 이미지 채우기 방식" 선택창 추가(꽉 채우기/전체 보기). "전체 보기"(contain)를 고르면 사진이 하나도 안 잘리고 전부 보이는 대신, 비율이 안 맞는 방향에 검정 배경이 채워진다(`CoverBackground`에 검정 베이스 레이어 추가 — object-contain만 쓰면 그 자리가 투명해져 바로 아래 TL3 콘텐츠가 비쳐 보였을 것).
- **실측 검증**: 로컬 dev 서버 + 관리자 세션으로 "혁명~제국" 표지에서 "전체 보기"를 선택 → 실제 `<img>`의 클래스가 `object-contain`으로 바뀌고 스크린샷에서 사진 전체(위아래 검정 여백 포함)가 잘리지 않고 다 보이는 것 확인 → "꽉 채우기"로 되돌려도 기존과 동일하게 동작하는 것 확인. 테스트 값은 저장하지 않고 되돌림.
- `tsc` 0 errors, `lint` 0 errors(80 warnings, 기존 기준선 유지).
- **다음에 확인 필요**: 관리자가 실제로 "혁명~제국"을 포함해 사진이 잘려 보였던 다른 카테고리 표지들도 "전체 보기"로 바꿔보고 저장 — 이 설정은 페이지/카테고리별로 각각 켜야 함(전역 기본값은 하위 호환을 위해 "꽉 채우기" 그대로 유지).

## 2026-08-27 (HOTFIX-147.22 — 타임라인 표지 제목/설명이 자유 배치를 안 켰을 때 화면 맨 위-왼쪽 모서리에 붙어 "짤린 것처럼" 보이던 문제 수정)
- **사용자 신고(스크린샷)**: "craft 에디터, '혁명~제국' 페이지 윗부분이 짤려." — 표지의 "Revolution ~ Empire" 제목이 캔버스 맨 위 가장자리에 딱 붙어, 위쪽 절반이 잘려 보였다.
- **원인(실측)**: `TimelineCoverOverlay`의 텍스트 박스는 "자유 배치(콜라주)"를 켜지 않으면(`position.enabled === false`, 이 페이지의 기본/현재 상태) `freePositionResponsiveAttrs()`가 `{ position: "relative" }`만 반환하고 다른 스타일은 전혀 안 준다 — 이 헬퍼는 Container/Text/Button 등 여러 블록이 공유하는 범용 함수라 그 자체를 바꾸면 다른 블록들의 "자유 배치 꺼짐 = 일반 문서 흐름" 동작까지 건드리게 된다. 이 텍스트 박스만 표지 이미지 전체를 덮는 절대 위치 오버레이의 유일한 자식이라, 별도 여백/정렬 없이 그냥 부모의 맨 위-왼쪽(0,0)에 그대로 렌더링되고 있었다 — HOTFIX-147.21에서 이 컴포넌트를 다시 손보다가(제목/설명 스타일 분리) 처음 눈에 띔.
- **수정**: `SiloTimelineEmbedBlock.tsx`의 `TimelineCoverOverlay`에 자유 배치가 꺼져 있을 때만 적용되는 래퍼(`flex h-full w-full flex-col items-center justify-center gap-2 px-8 py-10`)를 추가 — 표지 이미지 안에서 제목/설명이 가운데 정렬 + 여백을 갖고 자연스럽게 보이도록 했다. `freePositionResponsiveAttrs()`나 다른 블록들은 손대지 않았고, 자유 배치를 켠 상태(`position.enabled === true`)는 기존과 완전히 동일하게 동작(추가한 래퍼가 `position:static`이라 `position:absolute` 자식의 containing block에 영향을 주지 않음).
- **부수적으로 발견/해결**: 수정 직후 로컬 dev 서버가 이 파일에서 `Unexpected token` 파싱 에러를 계속 냈는데, `tsc --noEmit`은 0 errors였고 파일 자체도 중괄호/태그가 정확히 맞아떨어져 — 이번 세션 동안 로컬 미리보기 서버를 여러 차례 껐다 켜며 쌓인 오래된(2026-08-26) Turbopack `.next` 빌드 캐시(1.4GB)가 꼬인 것으로 확인, `.next` 삭제 후 재시작으로 해결(소스 코드 문제 아니었음).
- **실측 검증**: 로컬 dev 서버(캐시 초기화 후) + 관리자 세션으로 "혁명~제국" 페이지를 열어 제목/설명이 표지 이미지 정중앙에 여백을 두고 나타나는 것을 스크린샷으로 직접 확인. 콘솔에 새로운 종류의 에러 없음.
- `tsc` 0 errors, `lint` 0 errors(80 warnings, 기존 기준선 유지).

## 2026-08-27 (EPIC-150 — Craft 에디터 Page 설정에 Themes 프리셋 갤러리 추가, "요소별 독립 설정→새 블록 팔레트→Page 설정→Themes" 4단계 시리즈 완료)
- **사용자 지시**: "다음 phase 진행해 그냥 순서대로 알아서 전부"의 마지막 Phase — BuilderJS 레퍼런스의 "Themes" 탭(클릭 한 번으로 페이지 톤을 바꾸는 프리셋 갤러리, Blank/Minimal/Plain Text/Gallery/Sell Products/1:2:1 Column/1:3 Column/Tell a story)에 대응.
- **범위 판단**: BuilderJS의 각 테마는 배경/여백/폭뿐 아니라 페이지 전체의 "완성된 콘텐츠 레이아웃"까지 미리 짜여 있는데(이메일 템플릿), 이 사이트의 6개 페이지 패밀리는 저마다 다른 전용 섹션 블록(Docent Hero/Era Grid, Editorial Grid 등)을 쓰고 있어 "테마 하나가 임의 블록 트리를 통째로 갈아끼우는" 방식은 위험도(기존 콘텐츠 유실 가능성)와 설계 난이도가 이번 Phase 범위를 크게 넘어선다. 대신 EPIC-149에서 만든 Page 설정(배경/컨테이너 폭/블록 간격/여백)을 한 번에 묶어 적용하는 프리셋 6종으로 스코프를 좁혔다 — 안전(ROOT props만 바꿈, 기존 블록 내용 무손상)하면서도 "테마 클릭 한 번으로 톤을 바꾼다"는 핵심 가치는 그대로 제공. **폰트는 프리셋에서 의도적으로 제외** — `FontPicker`는 이 배포에 실제로 업로드된 커스텀 폰트만 고를 수 있어(사이트마다 다름), 프리셋에 특정 폰트를 박아두면 그 폰트가 없는 배포에서는 조용히 아무 효과 없이 실패하기 때문(`FontPicker.tsx` 상단 주석에 이미 기록된 동일한 함정).
- **구현**: `RootContainer.tsx`(EPIC-149에서 Page 설정을 추가한 바로 그 파일)의 `PageSettings` 패널 맨 위에 "테마" 섹션 추가 — 6개 프리셋(기본값/미니멀/갤러리(다크)/웜 빈티지/와이드 에디토리얼/컴팩트) 각각 색상 스와치 버튼, 클릭하면 배경색·컨테이너 폭·블록 간격·상하좌우 여백을 한 번에 `setProp`. 적용 후에도 바로 아래 "레이아웃"/"배경" 섹션에서 세부 값을 자유롭게 다시 조정할 수 있다(테마는 잠금이 아니라 시작점).
- **실측 검증**: 로컬 dev 서버 + 관리자 세션으로 "혁명~제국" 캔버스 빈 곳 클릭 → 새 "테마" 섹션에 6개 프리셋 스와치 노출 확인 → "웜 빈티지" 클릭 → `getComputedStyle()`로 배경색 `rgb(245,239,227)`/gap 40px/paddingTop 64px/paddingLeft 24px/maxWidth 960px가 프리셋 정의값과 정확히 일치하는 것 확인. 테스트 값은 저장하지 않고 되돌림.
- `tsc` 0 errors, `lint` 0 errors(80 warnings, 기존 기준선 유지 — 새 경고 없음).
- **네 단계 시리즈 마무리**: HOTFIX-147.21(요소별 독립 설정) → EPIC-148(새 블록 팔레트 21종) → EPIC-149(Page 전역 설정) → EPIC-150(Themes 프리셋)까지, 사용자가 BuilderJS를 레퍼런스로 요청한 Craft 에디터 개선 시리즈를 순서대로 전부 완료.
- **다음에 확인 필요**: 관리자가 실제로 여러 페이지에서 테마를 적용해보고 저장 후에도 유지되는지, 그리고 언급됐던 "홈페이지 설정관리"(`/admin/navigation/settings`)의 기존 "Themes" 탭(현재는 hover 모션 전용, 이번 작업과 무관하게 그대로 둠)과 이번 Page 설정의 "테마"가 이름이 겹쳐 헷갈리지 않는지 — 필요하면 사용자 확인 후 이름을 구분.

## 2026-08-27 (EPIC-149 — Craft 에디터에 Page(페이지 전역) 설정 신설)
- **사용자 지시**: EPIC-148(새 블록 팔레트)에 이어 "다음 phase 진행해 그냥 순서대로 알아서 전부"로 계속 자동 진행 — BuilderJS 레퍼런스의 "Page" 탭(container width/block gap/padding/기본 폰트/배경)에 대응하는 페이지 전역 설정을 신설.
- **설계**: 지금까지 이 사이트의 모든 Craft 페이지(홈/사일로상점/온라인도슨트/살롱데상/스튜디오/마이페이지 6개 패밀리 + footer 캔버스, 총 7곳)가 `RootContainer.tsx` 딱 하나를 ROOT 노드로 공유하고 있었다(각 `defaultTree.tsx`가 전부 이 한 파일을 import) — 이 컴포넌트 하나에만 실제 props와 `related.settings`를 달아도 7곳 전체에 자동 적용된다는 걸 확인하고, 별도 신규 컴포넌트 없이 여기 하나만 고쳤다. `SettingsSidebar.tsx`는 원래부터 "선택된 노드의 `related.settings`를 그대로 렌더링"하는 범용 코드라(ROOT 전용 분기 없음) 캔버스 빈 곳(=ROOT)을 클릭하면 자동으로 이 새 패널이 뜬다 — `SettingsSidebar.tsx` 자체는 손대지 않음.
- **신규 설정**: 블록 간 간격(`blockGapPx`, 자식들을 flex column + gap으로 감쌈), 콘텐츠 최대 너비(`containerWidthPx`, null이면 전체 너비), 4방향 페이지 패딩, 페이지 기본 폰트(`fontFamily` — 자식 블록이 자기 폰트를 안 정했으면 일반 CSS 상속으로 자동으로 물려받음, 자식 코드 수정 불필요), 배경(색상/이미지 업로드/채우기 방식/위치/반복/불투명도). 새 필드는 전부 기본값이 "아무 효과 없음"(null/0/빈 문자열)이라 기존에 저장된 모든 페이지(`page_builder.craft_state`의 ROOT 노드 props가 예전엔 `{}`였음)가 마이그레이션 없이도 시각적으로 그대로 유지된다.
- **BuilderJS 대비 의도적으로 뺀 것**: "Default Block Background"(이메일 빌더에서 새 블록을 삽입할 때 쓰일 기본값 개념 — 이 프로젝트의 블록들은 이미 각자 `background`/`color` 등 자기 스타일을 갖고 있어 재현할 대상이 불명확), 배경 Gradient/Blend mode(정적 페이지 배경치고 과한 옵션, 필요해지면 이후 추가).
- **실측 검증**: 로컬 dev 서버 + 관리자 세션으로 "혁명~제국" Craft 에디터의 캔버스 빈 곳을 클릭 → 새 "ROOTCONTAINER 설정" 패널(레이아웃/기본 폰트/배경 3개 섹션) 노출 확인 → 블록 간 간격 60px·상단 여백 40px·배경색 `#ffe9c7` 입력 → `getComputedStyle()`로 실제 DOM에 `gap:60px`/`padding-top:40px`/`background-color:rgb(255,233,199)`가 그대로 반영되는 것 확인. 콘솔에 새로운 종류의 에러 없음(React 19 ref 경고/HMR 웹소켓 등은 기존에도 있던 무관한 노이즈). 테스트 값은 저장하지 않고 되돌림.
- `tsc` 0 errors, `lint` 0 errors(80 warnings, 기존 기준선 유지 — 새 경고 없음).

## 2026-08-27 (EPIC-148 — Craft 에디터 "요소" 팔레트를 BuilderJS 레퍼런스 수준으로 확장)
- **사용자 지시**: HOTFIX-147.21(요소별 독립 설정)에 이어 "다음 phase 진행해 그냥 순서대로 알아서 전부"로 나머지 Phase(새 블록 팔레트/Page 전역 설정/Themes 시스템)를 순서대로 자동 진행 요청. 이번 EPIC은 그중 1번째 — BuilderJS(이메일 빌더) 레퍼런스의 Elements 팔레트를 이 Craft 에디터의 "요소" 패널에 통합.
- **범위 판단**: BuilderJS의 Elements를 전부 그대로 가져오되(사용자가 앞서 "BuilderJS 전체 팔레트 그대로"를 선택), 이 사이트 구조와 명백히 충돌/중복되는 4가지는 새로 만들지 않고 기존 걸 재사용하도록 안내만 남겼다 — **Menu Bar**(이 사이트는 전역 Navbar가 이미 있어 페이지마다 또 다른 메뉴 블록을 꽂으면 충돌만 남음), **Slider**(기존 `SlideshowBlock`과 기능 동일), **Product List**(기존 `ShopItemsGridBlock`과 기능 동일), **Grid**(기존 `ContainerBlock`을 `layout="row"`로 쓰면 이미 N단 그리드로 감쌀 수 있음), **Welcome**(TextBlock에 다른 기본 문구를 넣은 것과 다를 게 없어 별도 블록 불필요). "Image & Text"/"Text" 두 섹션은 BuilderJS가 배치만 다른 레이아웃을 5종/3종 따로 뒀지만, 블록 하나에 배치 선택지를 둬(`ImageTextBlock`/`TextCTABlock`) 코드 중복 없이 캔버스에서 바로 레이아웃을 전환할 수 있게 만들었다.
- **신규 블록 21종**(전부 `src/components/craft/primitives/`, 기존 11개 원자 블록과 동일한 `related.settings` 패턴): `ImageTextBlock`/`TextCTABlock`(위 레이아웃 통합), `DividerBlock`(구분선), `SpacerBlock`(여백), `SocialIconsBlock`(Instagram/Facebook/YouTube/X/카카오톡/블로그 인라인 SVG 아이콘), `AlertBlock`(안내/성공/경고/오류 배너), `YoutubeBlock`(URL 4가지 형식 파싱 후 반응형 embed), `RatingBlock`(별점), `StatBlock`(통계/KPI 여러 개), `ProgressBarBlock`, `BulletListBlock`, `TestimonialBlock`(후기), `FeaturesBlock`(기능 카드 N개), `PricingCardsBlock`(요금제 카드 N개), `ImageCollageBlock`(이미지 여러 장), `SocialProofBlock`(아바타 행+캡션), `DataTableBlock`(행/열 편집 가능한 표), `ChartBlock`(막대/선 그래프, 의존성 없이 inline SVG로 직접 그림), `QRCodeBlock`(`qrcode` 패키지 신규 설치), `BarcodeBlock`(`jsbarcode` 패키지 신규 설치, CODE128), `ProductSpotlightBlock`(BuilderJS "Funnel > Product" 대응 — 사일로 상점 `items` 테이블에서 물건 하나를 크게 스포트라이트).
- **공용 헬퍼 신설**: `src/components/craft/shared/FieldControls.tsx` — 새 블록 21개 각각의 설정 패널에서 반복되는 "label+input" 마크업(text/textarea/number/color/select/checkbox + N개 항목 추가·삭제 리스트)을 공용 컴포넌트로 뽑아, 기존 블록들의 시각 스타일(text-xs 라벨, rounded border-gray-300 입력창)을 그대로 유지하면서 코드 중복을 크게 줄였다.
- **신규 의존성**: `qrcode`(+`@types/qrcode`), `jsbarcode` — 둘 다 별도 서버 없이 브라우저에서 직접 QR/바코드를 그리는 정적 라이브러리(MIT, 널리 쓰임). `npm audit`로 확인한 기존 high severity 취약점(brace-expansion/js-yaml/nanoid/next/postcss/sharp)은 전부 이번 설치 이전부터 있던 무관한 전이 의존성.
- **실측 검증**: 로컬 dev 서버 + 관리자 세션으로 "혁명~제국" Craft 에디터를 열어 새 "요소" 목록에 21개 항목이 모두 노출되는 것 확인, 그중 12개(구분선/알림/통계-KPI/후기/QR코드/별점/차트/데이터테이블/유튜브/소셜아이콘/이미지&텍스트/텍스트+버튼)를 실제로 캔버스에 추가해 콘솔 에러 없이 기본값대로 렌더링되는 것 확인 — QR 코드는 실제 base64 PNG 생성, 바코드는 26개 bar를 가진 SVG 생성까지 직접 확인. 테스트 데이터는 저장하지 않고 되돌림(프로덕션 데이터 변경 없음).
- `tsc` 0 errors, `lint` 0 errors(78 warnings 기존 기준선에서 80으로 — `ProductSpotlightBlock`/`QRCodeBlock`의 데이터 로딩 `useEffect`가 이 코드베이스 전반에 이미 널리 쓰이는 `setLoading(true)` 패턴을 그대로 따른 것으로, 새로운 종류의 경고가 아니라 기존 관용구가 2곳 더 늘어난 것).
- **다음에 확인 필요**: (1) 관리자가 실제로 몇몇 블록(특히 이미지/아바타 업로드가 있는 ImageTextBlock/TestimonialBlock/ImageCollageBlock/SocialProofBlock, DB 연동인 ProductSpotlightBlock)을 실제 저장까지 해보고 새로고침 후에도 정상 유지되는지 확인 — 이번 세션은 프로덕션 데이터 훼손을 피하려 저장하지 않고 되돌렸다. (2) 남은 Phase(Page 전역 설정 탭, Themes 프리셋 시스템)는 이어서 진행 예정.

## 2026-08-27 (HOTFIX-147.21 — 타임라인 표지/이벤트 오버레이 "제목"과 "설명"이 정렬·굵기·크기·색상·폰트를 하나로 공유하던 문제 수정)
- **사용자 지시**: Craft 에디터를 BuilderJS(이메일 빌더) 레퍼런스와 비교하며, 캔버스에서 "Ancient ~ Monarchy"(제목)와 "___고대~왕정"(설명)을 위한 텍스트 설정이 서로 달라야 하는데 지금은 하나로 묶여 있다고 지적 — 나아가 이 "요소마다 완전히 독립된 설정" 원칙을 Craft 에디터 전체(및 "홈페이지 설정관리")에 적용해달라는 큰 요청. 코드베이스 전체를 감사한 결과 이 원칙을 위반하는 곳은 `SiloTimelineEmbedBlock.tsx`의 `SlideOverlayFieldsEditor`(표지 + 이벤트별 오버레이 둘 다에서 재사용) 단 한 곳뿐임을 확인 — 나머지 블록/설정 UI는 이미 필드별로 독립돼 있었다. 사용자가 1순위로 이 좁은 범위부터 처리하기로 확정(새 블록 팔레트/Page 전역 설정 탭/Themes 프리셋 시스템은 각각 별도 Phase로 보류).
- **원인**: `SlideOverlayConfig`의 `fontSizePx`/`fontWeight`/`align`/`color`/`fontFamily`가 사실상 "제목" 전용 스타일이었는데, 렌더링(`TimelineCoverOverlay`)에서 설명도 그대로 물려받아 썼다(설명 크기만 `fontSizePx * 0.5`로 축소, 굵기는 애초에 클래스 자체가 안 붙어 있었음). 설정 패널(`SlideOverlayFieldsEditor`)도 정렬/굵기/크기/색상/폰트 컨트롤 5개를 제목·설명 구분 없이 하나로만 제공했다.
- **수정**: `SlideOverlayConfig`/`SiloTimelineEmbedBlockProps`에 `description(Cover)FontSizePx`/`FontWeight`/`Align`/`Color`/`FontFamily` 5개를 각각 `| null`로 신설(기본값 `null` = "아직 커스터마이징 안 함" → 지금까지의 파생 기본값 그대로 유지, 마이그레이션 불필요). `SlideOverlayFieldsEditor`를 "제목 스타일"/"설명 스타일" 두 섹션으로 나눠 각각 독립된 정렬/굵기/크기/색상/폰트 컨트롤을 제공 — 설명 섹션은 커스터마이징 전엔 제목에서 파생된 유효값을 그대로 보여주다가(무엇이 적용 중인지 바로 알 수 있게), 값을 바꾸는 순간부터 독립적으로 저장된다.
- **실측 검증**: 로컬 dev 서버 + 관리자 세션으로 "혁명~제국" Craft 에디터에서 타임라인 블록을 선택 → 표지 자유편집을 켜고 제목(빨강/32px)과 설명(파랑/14px/오른쪽 정렬)을 서로 다르게 설정 → 캔버스의 실제 `<p>` 엘리먼트 `getComputedStyle()`로 `color`/`fontSize`/`textAlign`이 완전히 독립적으로 반영되는 것을 직접 확인(테스트 값은 저장하지 않고 되돌림, 프로덕션 데이터 훼손 없음).
- `tsc` 0 errors, `lint` 0 errors(78 warnings, 기존 기준선 유지).
- **다음에 확인 필요**: 사용자가 언급한 나머지 Phase(BuilderJS 전체 팔레트에 준하는 새 블록 타입 추가, Page 전역 설정 탭 신설, Themes 프리셋 시스템 신설)는 각각 규모가 큰 별도 설계가 필요해 이번엔 포함하지 않음 — 사용자가 다음 지시할 때 순서대로 진행.

## 2026-08-27 (HOTFIX-147.20 — "온라인 도슨트" 드롭다운 줄바꿈이 PC에만 반영되던 문제 + 홈페이지 상단 메뉴가 20초 넘게 안 뜨던 문제 수정)
- **사용자 신고 1**: "홈페이지 설정 관리"의 PC 미리보기에서는 "온라인 도슨트" 드롭다운의 "고대 ~ 왕정\nAncient ~ Monarchy" 줄바꿈이 잘 적용됐는데, 실제 웹사이트에서는 줄바꿈이 안 됐다.
- **원인(실측)**: 이 최상위 카테고리(`site_navigations`, id `710a8240`)는 `target_types`에 `dropdown`과 `sidebar_right`가 함께 태그돼 있는데, `navConfig.ts`의 `mapTargetTypes()`는 여러 타입이 동시에 있으면 `sidebar_right`를 `dropdown`보다 먼저 확인해 **groups 구조**로 트리를 조립한다(과거 HOTFIX-144.2에서 "오른쪽 사이드바 노출"이 추가되며 이 우선순위 뒤로 조용히 바뀐 것으로 보임). 그 결과 `Navbar.tsx`의 실제 렌더 코드는 하위 카테고리 텍스트 오버라이드를 **그룹 라벨 키**(`group.groupLabel`, 원본 제목 그대로)로 조회하는데, `site_settings.top_tab_style`에는 PC 설정에만 이 그룹 라벨 키로 값이 저장돼 있었고 태블릿/모바일 설정에는 (예전 items/href 구조였던 시절에 저장된) **href 키**만 남아있어 — 태블릿/모바일에서는 오버라이드가 매칭되지 않고 원본 제목(줄바꿈 없음)이 그대로 노출됐다. Claude Browser 툴로 dev.silostore.net을 직접 열어 DOM 텍스트 노드를 검사해 실제로 PC 폭(1280px)에서는 `\n`이 포함되고 태블릿 폭(839px)에서는 빠지는 것을 확인해 특정.
- **수정**: 코드 변경 없음 — Supabase Management API로 `site_settings.top_tab_style`의 `tablet`/`mobile` 양쪽 `tabs.docent.subLabelOverrides`에 PC와 동일한 그룹 라벨 키("고대 ~ 왕정 Ancient ~ Monarchy ")로 줄바꿈 포함 값을 추가(기존 PC 값·href 키는 그대로 둠, 다른 뷰포트에서 재사용될 가능성 있어 삭제하지 않음).
- **사용자 신고 2(조사 중 별도 발견)**: 홈페이지(`/`)에서만 상단 메뉴(About Silo/사일로상점/살롱데상/온라인 도슨트)가 최대 20초 넘게 아예 안 뜨는 현상을 자동화 브라우저로 반복 재현(`/about-silo`는 항상 5초 이내 정상). 네트워크 로그 확인 결과, 페이지 하나에 렌더링되는 사이트 메뉴 트리(최상단 탭 + 모든 드롭다운/메가메뉴/사이드바 하위 항목, `Navbar.tsx` 주석 기준 최대 ~96개)의 `GatedNavLink`가 전부 next/link 기본 prefetch 동작을 그대로 써서, 페이지 로드 시 `_rsc` prefetch 요청이 60~90개 가까이 동시에 쏟아지는 것을 확인 — 이미지/JS가 훨씬 많은 홈페이지에서는 이 요청 폭주가 브라우저 동시 연결을 다 잠식해, 정작 메뉴 자체를 채우는 `fetchNavTabs()`의 Supabase 조회가 완료까지 20초 넘게 밀렸다.
- **수정**: `GatedNavLink.tsx`에 `prefetch = false` 기본값 추가(개별 호출부에서 필요하면 여전히 오버라이드 가능) — 사이트 메뉴 트리 전체(Navbar/LeftSidebar/RightSidebar/UserMenuDropdown 공용 컴포넌트라 자동 전체 적용)의 불필요한 대량 prefetch를 제거.
- **실측 검증**: 로컬 dev 서버에서 수정 전(홈페이지 nav 텍스트 길이가 "글쓰기"만, 20초 이상 대기해도 그대로) → 수정 후(3초 내 nav 전체 텍스트 로드, `_rsc` prefetch 요청 0건) 직접 대조. 태블릿(839px)/모바일(375px) 폭 각각에서 "고대 ~ 왕정\nAncient ~ Monarchy" 줄바꿈이 실제로 반영되는 것도 재확인.
- `tsc` 0 errors, `lint` 0 errors(78 warnings, 기존 기준선 유지).

## 2026-08-27 (HOTFIX-147.19 — 모든 타임라인이 대시보드 전체를 한눈에 볼 수 없도록 과도하게 줌인돼있던 문제 수정 + 초기 줌 배율 설정 추가)
- **사용자 지시**: "모든 타임라인을 확인해보니, 대시보드가 그 타임라인 전체를 한눈에 볼 수 없도록 줌인되어있어. 처음 default로 줌인되어있는걸 조절할 수 있는 기능을 넣고, 모든 타임라인을 한눈에 볼 수 있도록 줌을 조절해줘."
- **원인**: TL3(TimelineJS3)의 TimeNav 확대 배율 옵션 `scale_factor`(공식 기본값 2, "타임라인이 화면 몇 개 너비만큼인가")를 지금까지 한 번도 지정한 적이 없어 TL3 자체 기본값 그대로 쓰고 있었다 — 전체 기간의 절반 정도만 화면에 보이고 나머지는 가로 스크롤해야 나오는 상태.
- **수정**: `SiloTimelineInner.tsx`가 새 `initialZoomFactor` prop을 받아 `scale_factor`로 그대로 전달, 기본값은 TL3 공식 `zoom_sequence`(피보나치 수열)의 최솟값인 `0.5`. 이 값은 `SiloTimeline.tsx` → `SiloTimelineEmbedBlock.tsx`(Craft 카테고리 페이지)와 `TimelineNgRenderer.tsx`(일반 게시판, `boards.widget_settings.timelineNgZoomFactor`)까지 각각 새 설정 UI(드롭다운, 값 미지정 시 그대로 0.5)로 노출해 관리자가 직접 조절할 수 있다.
- **더 작은 값(예: 0.05)은 시도했지만 채택 안 함**: 마커는 전부 화면에 들어오지만 TL3가 그 압축된 기간에 맞춰 축 눈금 라벨을 과도하게 넓은 범위로 그려 서로 겹쳐 못 읽게 되는 부작용을 실측으로 발견 — 공식 프리셋 중 가장 작은 0.5가 "마커 전부 보임 + 축 눈금도 읽을 수 있음"을 동시에 만족하는 값이었다.
- **적용 범위**: `SiloTimelineEmbedBlock`을 쓰는 4개 온라인 도슨트 카테고리 페이지 + `TimelineNgRenderer`를 쓰는 모든 `timeline_ng` 게시판(사일로 타임라인, 그리스/르네상스/바로크/로코코 등 리프 게시판 전부) 둘 다에 자동 적용 — 두 렌더링 경로가 결국 같은 `SiloTimelineInner.tsx`를 공유해 한 곳만 고치면 전체에 반영된다.
- **실측 검증**: "고대~왕정"(4개 카테고리 마커: 그리스/르네상스/바로크/로코코)과 "그리스" 리프 게시판(12개 게시글 마커) 둘 다에서 `getBoundingClientRect()`로 모든 마커의 좌표가 TimeNav의 실제 보이는 너비(`clientWidth`) 안에 들어오는 것(스크롤 불필요)을 직접 확인.
- `tsc` 0 errors, `lint` 0 errors(78 warnings, 기존 기준선 유지).

## 2026-08-27 (HOTFIX-147.18 — 오른쪽 패널 스크롤 회귀 수정 + 표지/이벤트 화면에 제목·설명 입력창 추가)
- **사용자 신고 1**: "지금 오른쪽 패널 스크롤이 안 되고 있어." HOTFIX-147.15에서 좌우 패널에 접기/펼치기 기능을 추가하며 Toolbox/SettingsSidebar를 wrapper div로 한 겹 더 감쌌는데, 그 wrapper에 높이를 안 줘서 `<aside className="overflow-y-auto">`가 부모의 실제(뷰포트 기준) 높이를 못 받고 콘텐츠 높이만큼 그냥 늘어나 버렸다(overflow-y-auto는 부모 높이가 실제로 한정돼 있어야 스크롤이 생김). wrapper에 `h-full flex`를 줘서 `inset-y-0`가 만든 실제 높이를 aside가 그대로 물려받아 스크롤바가 다시 생기도록 수정 — 실측: `scrollHeight/clientHeight`가 `679/679`(스크롤 불가) → `1183/551`(스크롤 가능, `scrollTop` 실제로 움직임)로 확인.
- **사용자 신고 2**: "'표지에 보여줄 텍스트를 입력하세요'라는 문구가 있는데 craft에는 텍스트를 입력하는데가 없네. 그리고 화면(고대~왕정/그리스/르네상스/바로크/로코코 5개)마다 이미 있는 텍스트들(제목/설명)을 수정할 수 있기를 원해." 지금까지 표지·이벤트 텍스트는 캔버스에서 더블클릭해야만(EditableText) 고칠 수 있었고, 설정 패널엔 입력창이 아예 없었다 — 게다가 HOTFIX-147.17 이전엔 z-index 버그로 그 텍스트 자체가 화면에 안 보여서 더블클릭할 방법도 없었다. `SlideOverlayConfig`에 `description` 필드를 추가하고(제목은 기존 `text` 필드 재사용), 설정 패널(`SlideOverlayFieldsEditor`)에 "제목"/"설명" 입력창을 추가 — 표지 섹션과 "개별 화면(이벤트) 자유 편집" 섹션 둘 다에서 5개 화면(표지 1 + 이벤트 4) 전부 동일하게 사용 가능. 캔버스에는 제목 아래 설명이 더 작은 글씨(제목 크기의 50%)로 이어서 표시된다.
- **실측 검증**: 설정 패널 "제목"/"설명" 입력창에 타이핑 → 캔버스에 즉시 반영 → `document.elementFromPoint()`로 그 텍스트가 실제로 화면 맨 위에 그려지는 것(HOTFIX-147.17의 z-index 수정 덕분에)까지 확인. "개별 화면 자유 편집" 드롭다운이 그리스/르네상스/바로크/로코코 4개 이벤트를 정확한 id와 함께 보여주고, 선택할 때마다 그 이벤트 고유의 저장값(체크박스 상태 등)을 보여주는 것(다른 이벤트 설정과 안 섞임) 확인.
- `tsc` 0 errors, `lint` 0 errors(78 warnings, 기존 기준선 유지).

## 2026-08-27 (HOTFIX-147.17 — 타임라인 표지 오버레이가 opacity:1인데도 TL3 자체 콘텐츠에 완전히 가려져 있던 진짜 원인 수정)
- **사용자 재신고(스크린샷 첨부, HOTFIX-147.16의 "저장 누락" 진단이 틀렸음을 직접 지적)**: "PC이고, 표지가 깜빡이듯 바로 사라진 뒤 흰 배경만 보인다, 1초도 안 돼서 사라진다." 실제로 사용자가 보고 있던 브라우저 preview를 스크린샷으로 직접 확인 — "고대~왕정" 페이지 표지에 업로드한 그리스 그림이 전혀 안 보이고 흰 배경 위에 TL3 기본 검은 제목 텍스트만 있었다.
- **근본 원인(실측)**: `document.elementFromPoint()`로 오버레이 좌표를 직접 찍어보니 최상단 요소가 TL3의 `<h2 class="tl-headline-title">`였다 — 그 조상 `.tl-slide-content-container`가 TL3 자체 CSS로 `position: relative; z-index: 3`을 갖고 있어(같은 스토리슬라이더 안의 `.tl-slider-touch-mask`는 z-index 25까지 감) 오버레이(z-index 지정 없음, `auto`)를 완전히 가리고 있었다. HOTFIX-147.12 이후 이 오버레이는 opacity/DOM 존재 여부만으로 "보인다"고 판단해왔을 뿐, 실제로 화면 맨 위에 그려지는지(`elementFromPoint`/스크린샷)는 이번에 처음 검증했다 — 그래서 이 버그가 지금까지 안 걸러졌다.
- **수정**: `TimelineCoverOverlay`의 루트 div에 `z-30`을 명시(TL3의 `.tl-storyslider` 내부에서 관측된 최댓값 25를 넉넉히 넘김) — `SiloTimelineEmbedBlock.tsx`.
- **HOTFIX-147.16의 "저장 안 눌러서 그렇다"는 진단은 철회한다** — 실제로는 저장도 정상 작동했고, 표지 이미지도 DB에 계속 있었다(코드가 opacity만 보고 "보인다"고 착각했을 뿐 실제로는 한 번도 화면에 그려진 적이 없었다).
- **실측 검증**: 스크린샷으로 수정 전(흰 배경+제목 텍스트만) → 수정 후(사용자가 보낸 그 그리스 그림이 실제로 화면 가득 표시, 5초 이상 유지) 직접 대조 확인.
- `tsc` 0 errors, `lint` 0 errors(78 warnings, 기존 기준선 유지).
- **참고**: 이 세션 중 "그리스" 이벤트 오버레이를 테스트로 켰다 끄며 두 차례 저장했다 — 그 시점에 사용자가 같은 페이지를 동시 편집 중이었다면 서로의 저장이 덮어썼을 수 있어(Craft.js는 낙관적 동시 편집 잠금이 없음) 사용자에게 확인을 요청함.

## 2026-08-27 (HOTFIX-147.16 — Craft 블록 높이를 드래그로 직접 조절하는 기능 추가)
- **사용자 지시**: "각 요소들의 위아래 폭을 내가 드래그 드랍으로 움직여서 조정하게 해줘" — `EditableBlockFrame`(6~7종 블록 전부가 공유하는 프레임)의 하단 가장자리에 드래그 핸들을 추가했다. 잡고 위아래로 끌면 그 블록의 높이(px)를 직접 지정하고(`frameHeightPx`, 지정한 높이를 넘는 내용은 세로 스크롤), 더블클릭하면 다시 자동 높이로 돌아간다. Craft.js의 노드 props는 그 블록의 TS 타입에 없는 키도 그냥 저장/조회할 수 있는 느슨한 저장소라, 블록 파일 7개를 각각 안 건드리고 이 프레임 하나만 고쳐 전체에 적용했다(HOTFIX-147.14/147.15의 삭제 버튼과 동일한 패턴). 자유 배치(`position.enabled`)가 이미 켜진 블록은 그쪽 리사이즈 핸들과 겹쳐 혼란스러우니 새 핸들을 숨긴다.
- `tsc` 0 errors, `lint` 0 errors(78 warnings, 기존 기준선 유지). 로컬 dev 서버에서 슬라이드쇼 블록을 실제로 100px 드래그해 519px→619px로 늘어나는 것과 더블클릭으로 다시 519px 자동 높이로 돌아가는 것 직접 확인.

## 2026-08-27 (HOTFIX-147.15 — HOTFIX-147.14로도 삭제 버튼이 안 보인 진짜 원인(패널에 가려짐) 수정 + 좌우 패널 접기/펼치기 추가)
- **사용자 재신고**: HOTFIX-147.14(선택 상태에서 opacity:1 유지) 배포 후에도 "달라진 게 없다"는 신고 — 사용자가 직접 "왼쪽 오른쪽 패널에 가려서 안 보이는거 아니야?"라고 원인을 지목.
- **실측 확인**: `document.elementFromPoint()`로 툴바 좌표를 직접 찍어보니 `opacity: 1`인데도 그 자리의 최상단 요소는 툴바가 아니라 오른쪽 SettingsSidebar의 `<h3>...설정</h3>`이었다 — 정확히 사용자 지적대로였다. 원인: `CraftPageEditor.tsx`가 Toolbox/SettingsSidebar를 캔버스와 폭을 나눠 쓰는 flex 형제가 아니라 캔버스 위에 뜨는 절대 위치 오버레이(`z-20`)로 쓰고 있다(HOTFIX-146 — 캔버스가 실제 사이트와 같은 폭을 유지해야 컨테이너 쿼리 반응형이 정확히 계산되기 때문, 의도된 구조). 블록의 툴바(`right-2`, 그동안 `z-10`)는 캔버스 오른쪽 가장자리를 기준으로 위치가 잡히는데, 캔버스가 오버레이 밑에서도 원래 폭 그대로 렌더링되다 보니 그 위치가 정확히 SettingsSidebar 오버레이 밑에 깔린 것.
- **수정 ①**: `EditableBlockFrame`의 툴바 z-index를 패널의 `z-20`보다 높은 `z-40`으로 올려, 패널이 열려 있어도 항상 그 위에 그려지고 클릭도 되게 함(`editable.tsx`). `document.elementFromPoint()`로 툴바 좌표를 다시 찍어 이제 진짜 툴바 버튼(⠿)이 최상단인 것까지 확인.
- **수정 ②(사용자 추가 지시 — "왼쪽 오른쪽 패널이 접혔다 펴질수 있게 해야지")**: `CraftPageEditor.tsx`에 `CollapsibleEdgePanel` 컴포넌트를 추가해 Toolbox/SettingsSidebar 양쪽 다 접기/펼치기 탭(◀▶)을 붙였다. 두 패널 다 이미 절대 위치 오버레이라 접어도 캔버스 폭 계산에는 전혀 영향 없음(HOTFIX-146의 "캔버스는 항상 전체 폭" 불변식 유지).
- `tsc` 0 errors, `lint` 0 errors(78 warnings, 기존 기준선 유지). 로컬 dev 서버로 컨테이너 선택 → 툴바가 SettingsSidebar보다 위에서 실제로 클릭 가능한 것, 좌우 패널 토글 버튼이 열림/닫힘 상태를 정확히 전환하는 것까지 확인.

## 2026-08-27 (HOTFIX-147.14 — Craft 에디터 블록 삭제 버튼이 선택해도 계속 안 보이던 문제 수정)
- **사용자 신고(스크린샷 첨부)**: 컨테이너 블록을 클릭해 선택하면(오른쪽 설정 패널에 "ContainerBlock 설정"이 뜸) 삭제(🗑) 버튼이 어디 있는지 안 보인다.
- **원인**: `EditableBlockFrame`(모든 Craft 블록이 공유하는 드래그/복제/삭제 툴바)이 지금까지 순수 CSS `:hover`로만 나타났다 — 블록을 클릭해서 "선택"해도(오른쪽 설정 패널은 그 노드로 바뀌지만) 마우스가 그 자리를 벗어나는 순간 툴바는 즉시 사라진다. 특히 컨테이너처럼 자유 배치된 자식만 들어있어 실제 눈에 보이는 영역이 좁거나 애매한 블록은, 정확히 그 좁은 영역에 마우스를 다시 올리기 전엔 삭제 버튼을 찾을 방법이 사실상 없었다 — 사용자가 보고한 스크린샷도 정확히 이 상태(컨테이너는 선택돼 있지만 마우스는 다른 곳에 있어 그 자식 텍스트 블록의 툴바만 보이는 상황)였다.
- **수정**: Craft.js가 이미 추적하는 "지금 선택된 노드인가"(`node.events.selected`)도 함께 봐서, 한 번 선택해두면 마우스 위치와 무관하게 그 블록의 툴바(⠿ 드래그 / ⧉ 복제 / 🗑 삭제)가 계속 보이도록 수정. 호버로 다른 블록의 툴바를 잠깐 들여다보는 기존 동작은 그대로 유지.
- **영향 범위**: `EditableBlockFrame`은 Craft.js가 관리하는 모든 블록(컨테이너/텍스트/이미지/버튼/영상/슬라이드쇼/게시판 연동/타임라인 연동 등)이 공유하는 단일 컴포넌트라, 이 한 곳만 고쳐 전부에 적용된다.
- **실측 검증(로컬 dev 서버, 관리자 세션)**: "혁명~제국" 페이지 Craft 에디터에서 컨테이너를 클릭 → 설정 패널이 "ContainerBlock 설정"으로 바뀌는 것 확인 → 그 컨테이너 자신의 툴바가 `opacity: 1`(마우스가 다른 곳에 있는 상태에서도)로 항상 보이는 것을 DOM에서 직접 확인.
- `tsc` 0 errors, `lint` 0 errors(78 warnings, 기존 기준선 유지).

## 2026-08-27 (HOTFIX-147.13 — 표지뿐 아니라 개별 이벤트(그리스/르네상스/신고전주의 등) 화면에도 자유편집 확장 + 4개 카테고리 전부 적용)
- **사용자 지시**: "고대~왕정 타임라인의 그리스/르네상스/바로크/로코코 같은 하위 화면들에도 슬라이드를 넣고 싶다, 이걸 다른 카테고리(혁명~제국/프로이트~대중문화/디지털~AI문화)에도 적용해달라" — HOTFIX-147.12로 표지 버그를 고친 뒤 이어서 요청됨.
- **설계**: 표지 전용이던 자유편집(배경 슬라이드+드래그 텍스트+정렬/굵기/크기/색상/폰트/자동전환간격/자유배치)을 "이 타임라인의 어떤 슬라이드에든" 적용 가능하게 일반화했다. 표지 플랫 prop들(`coverText` 등)은 하위 호환을 위해 그대로 두고, 새 `eventOverlays: Record<이벤트unique_id, SlideOverlayConfig>` 맵을 추가해 이벤트별로 독립적인 설정을 가질 수 있게 했다. `SiloTimelineInner.tsx`의 `change` 이벤트 핸들러가 이제 "표지인지"뿐 아니라 "표지가 아니면 정확히 어느 이벤트인지"(`eventId`)도 함께 알려주도록 확장(HOTFIX-147.12가 이미 만든 `knownEventIds` 판별을 그대로 재사용) — 표지에서 쓰던 것과 동일한 오버레이 위치 계산(`.tl-storyslider`의 top/height) 메커니즘이 이벤트 슬라이드에도 그대로 적용된다는 걸 실측으로 확인해, "TL3 이벤트 슬라이드는 자체 렌더링이라 오버레이를 얹을 훅이 없다"던 이전 가정을 뒤집었다.
- **설정 UI**: 게시판/카테고리 설정 아래 새 "개별 화면(이벤트) 자유 편집" 섹션 — 이 타임라인이 가진 실제 이벤트 목록(같은 `/api/timeline/events` 응답 재사용, 새 API 없음)을 드롭다운으로 보여주고, 하나를 고르면 표지 설정과 완전히 동일한 필드 세트가 그 이벤트 전용으로 나타난다. 표지 섹션과 이벤트 섹션이 필드를 똑같이 반복하므로 `SlideOverlayFieldsEditor`(정렬~배경슬라이드)와 `SlideOverlayPositionFields`(자유배치 좌표)로 공용 컴포넌트를 뽑아 표지/이벤트 양쪽에서 재사용한다. 위치 편집은 기존 `FreePositionSettingsSection`(다른 6개 자유배치 블록과 공유, node 최상위 position만 다루는 고정 계약이라 재사용 불가)과 별개로, 임의의 중첩 위치를 받는 새 컴포넌트로 분리.
- **캔버스 렌더링**: 지금 표지가 아니라 이벤트를 보고 있으면 `eventOverlays[그 이벤트 id]`를 찾아, 있으면(그리고 `enabled`면) 표지와 동일한 `TimelineCoverOverlay`를 그 이벤트 위에 겹친다. 설정이 없는 이벤트는 기존처럼 TL3 원본 그대로 보인다 — 카테고리/게시판 전체에 일괄 적용되는 게 아니라 이벤트 단위로 켜고 끈다.
- **영향 범위**: `SiloTimelineEmbedBlock`을 공유하는 4개 온라인 도슨트 카테고리 페이지(고대~왕정/혁명~제국/프로이트~대중문화/디지털~A.i 문화) + 개별 리프 게시판 타임라인 전부에 자동 적용 — 카테고리마다 따로 구현하지 않았다.
- **실측 검증(로컬 dev 서버, 관리자 세션 실제 Craft 에디터 조작)**: "혁명~제국" 페이지에서 이벤트 드롭다운이 실제 4개 하위 카테고리(신고전주의/리전시/빅토리안/인상파)를 정확한 unique_id와 함께 보여주는 것 확인 → "신고전주의"를 골라 자유편집 켜고 배경 이미지 업로드(진짜 Supabase Storage 업로드) → TimeNav에서 실제로 "신고전주의" 이벤트를 클릭해 이동하니 오버레이가 `opacity: 1`로 뜨고 업로드한 이미지가 렌더링됨 확인 → 설정을 안 켠 "리전시"로 이동하면 오버레이 자체가 안 뜨는 것(이벤트별 독립) 확인 → "표지로 돌아가기"를 누르면 표지 자체 설정(이번엔 꺼둠)에 따라 다시 opacity 0으로 정확히 전환되는 것까지 확인(표지/이벤트 설정 완전 독립).
- `tsc` 0 errors, `lint` 0 errors(78 warnings — 새 이벤트 목록 fetch effect 1곳이 기존 12곳과 동일한 `react-hooks/set-state-in-effect` 패턴이라 77→78, 새로운 종류의 경고 아님).

## 2026-08-27 (HOTFIX-147.12 — 타임라인 "표지 자유편집"이 슬라이드/폰트를 넣어도 절대 안 보이던 근본 원인 2건 수정)
- **사용자 신고**: "타임라인 표지(첫화면) 자유편집에 슬라이드를 넣어도 적용이 안 되고, 폰트설정도 적용이 안 된다." 관리자 세션으로 로컬 dev 서버 Craft 에디터에서 직접 재현 — "혁명~제국" 페이지에서 "표지(첫 화면) 자유 편집" 체크박스를 켜고 배경 이미지를 실제로 업로드해도(Supabase Storage에 정상 저장 확인) 캔버스에 전혀 나타나지 않음을 확인.
- **근본 원인 ①**: `TimelineCoverOverlay`는 `coverState.isTitle`이 `true`일 때만 `opacity: 1`로 보인다(`SiloTimelineEmbedBlock.tsx`). `coverState`는 TL3(TimelineJS3)의 `change` 이벤트 핸들러 안에서 `d.unique_id === "title-headline"`로 지금 표지를 보고 있는지 판별해왔는데(HOTFIX-147.8), 실측 결과 TL3는 `title` 객체에 `unique_id`를 명시적으로 안 주면 **headline 텍스트를 자체 slugify해서 자기가 직접 짓는다** — "혁명 ~ 제국 Revolution ~ Empire" 같은 실제 카테고리 제목은 `"-revolution-empire"`가 되는 식(콘솔 로그로 직접 확인). 즉 `"title-headline"`이라는 고정 문자열과는 사실상 한 번도 일치한 적이 없어 `isTitle`이 항상 `false`로 고정돼 있었다 — 이 기능은 배경/텍스트/폰트를 뭘 넣든 오버레이 자체가 절대 보이지 않는 상태였다.
- **수정 ①**: 고정 문자열 추측을 그만두고, `/api/timeline/events`가 실제로 만든 이벤트들의 `unique_id` 목록(`knownEventIds`)에 **없는** 슬라이드는 전부 표지로 판정하도록 뒤집었다(`SiloTimelineInner.tsx`) — TL3가 표지에 어떤 이름을 붙이든 항상 정확하다.
- **근본 원인 ②(부차적, 실사용 흐름에서 항상 함께 겪는 문제)**: `.tl-storyslider`가 나타나는 순간을 잡으려고 `MutationObserver`로 감시하는데(HOTFIX-147.8), 관리자가 실제로 하는 행동 순서(먼저 페이지를 보고 → 그제서야 "표지 자유 편집"을 켬)에서는 그 시점에 `.tl-storyslider`가 **이미 DOM에 있어** "새로 추가되는 순간"이 다시는 안 와 `ResizeObserver`/`emitCoverState()`가 한 번도 안 붙었다(TL3에서 아무 이벤트나 한 번 클릭했다 표지로 돌아오면 `change` 핸들러가 직접 `emitCoverState()`를 불러 그제서야 우연히 나타남 — 이 우회 경로 때문에 ①의 버그가 지금까지 안 들켰던 것으로 보임).
- **수정 ②**: effect 시작 시 `.tl-storyslider`가 이미 있는지 먼저 확인해 그 자리에서 바로 옵저버를 붙이도록 수정.
- **영향 범위**: `SiloTimelineEmbedBlock`은 4개 온라인 도슨트 카테고리 페이지(고대~왕정/혁명~제국/프로이트~대중문화/디지털~A.i 문화) + 개별 리프 게시판 타임라인이 전부 공유하는 컴포넌트라, 이 수정으로 전체가 한 번에 고쳐진다.
- **실측 검증(로컬 dev 서버, 관리자 세션 실제 Craft 에디터 조작)**: 수정 전 — 체크박스 on + 배경 업로드 후에도 오버레이 `opacity: 0` 그대로(콘솔 로그로 `change event unique_id`가 `"-revolution-empire"`임을 직접 확인). 수정 후 — 새로고침한 새 세션에서 체크박스를 켜자마자(다른 조작 없이) `opacity: 1`, 표지 안내 placeholder 텍스트(`"표지에 보여줄 텍스트를 입력하세요"`) 노출, 실제 업로드한 배경 이미지가 1249×485(스토리슬라이더 전체 크기)로 렌더링되는 것까지 확인. 텍스트 더블클릭 인라인 편집은 기존에 이미 구현돼 있었음(`EditableText`) — 별도 수정 불필요.
- `tsc` 0 errors, `lint` 0 errors(77 warnings, 기존 기준선 유지).
- **참고(코드 변경 없음, 기존 기능 확인)**: 사용자가 함께 요청한 "container block 삭제 기능"/"섹션·요소 삭제"/"container에 사진 슬라이드 넣기"는 전부 이미 구현돼 있음을 실측으로 확인 — 모든 블록(컨테이너 포함)에 드래그(⠿)/복제(⧉)/삭제(🗑) 아이콘이 이미 붙어있고, Toolbox의 "슬라이드쇼" 블록을 컨테이너 안에 드래그하면 사진 슬라이드를 바로 넣을 수 있다.

## 2026-08-27 (HOTFIX-147.11 — 갤러리 게시판 "호버 자동 슬라이드"가 사이트 전체에서 작동한 적이 없던 근본 원인 수정 + 모든 게시판 기본값 true로 전환)
- **사용자 신고**: dev.silostore.net/about-silo/daily-silo에서 "썸네일 호버 시 이미지 자동 슬라이드(끄면 좌우 화살표로 직접 넘김 — 영상은 항상 자동재생)"가 작동하지 않음 + 모든 게시판에 기본으로 켜달라 + 게시판마다 실제로 동작하는지 확인해달라는 요청.
- **실측(Supabase Management API로 DB 직접 조회) — 근본 원인**: `render_type="gallery"`인 게시판(af06e86e/silo-story/treasures/ethan-s-bluenotes/silo-daily 등 — `galleryHoverAutoSlide`가 이미 `true`였던 곳 포함)의 게시글이 예외 없이 전부 인스타그램에서 동기화된 embed 콘텐츠였다. `body_json`엔 permalink 하나만 있고 실제 캐러셀 이미지 목록은 `instagram_feeds.media_urls`에 있는데, 기존 `extractCalendarMedia()`(`src/lib/calendarMedia.ts`)는 TipTap 네이티브 `gallery`/`figureImage` 블록만 보고 `embed` 타입은 아예 걸러내므로 media가 항상 0개로 계산돼 토글이 켜져 있어도 슬라이드할 대상 자체가 없었다 — 이 기능은 사이트 전체에서 사실상 한 번도 실제로 작동한 적이 없었다.
- **수정**: `NativeInstagramEmbed.tsx`가 게시글 "본문" 임베드 하나를 native로 바꿔치기할 때 이미 쓰던 permalink→shortcode→`instagram_feeds` 매칭 로직을 `src/lib/instagramShortcode.ts`(`extractInstagramShortcode`)로 뽑아 공용화하고, `GalleryModule`이 화면에 뜬 게시글들의 인스타그램 permalink를 모아 한 번에 배치 조회하는 `fetchInstagramFeedsByShortcodes()`(`src/lib/instagramFeed.ts`)를 추가 — 카드 개수만큼 개별 요청하지 않는다. 캐러셀이 매칭되면 그 `media_urls`/`media_item_types`를 hover 슬라이드 소스로 우선 쓰고, 매칭이 안 되면(네이티브 gallery/figureImage 블록이 있는 기존/향후 콘텐츠) 기존 `extractCalendarMedia` 결과로 그대로 폴백한다. HOTFIX-147.10에서 도입된 `VIDEO_THUMBNAIL`(실제 영상 파일을 못 받아 정지 이미지로 대체된 항목)도 `InstagramFeedPost.tsx`와 동일하게 재생 버튼 아이콘을 얹어 표시.
- **기본값 false→true 전환**: `BoardForm.tsx`/`GalleryModule.tsx`/`src/app/admin/boards/[id]/page.tsx` 3곳의 기본값을 변경하고, Management API로 기존 69개 게시판 전체의 `widget_settings.galleryHoverAutoSlide`를 `true`로 일괄 백필(게시판 유형과 무관하게 전부 — 갤러리가 아닌 게시판엔 이 값이 아무 효과가 없어 무해함, 사용자가 "모든 게시판에" 기본이 되게 해달라고 명시적으로 요청).
- **실측 검증(로컬 dev 서버 + Claude Browser 툴)**: `daily-silo`(비디오가 첫 슬라이드인 캐러셀 — hover 후 `<video muted loop>`가 실제로 `paused:false`/`currentTime` 진행 중인 것까지 확인)와 `silo-s-exhibition`(10장 이미지 캐러셀 — hover 유지 중 순서대로 순환) 두 게시판에서, 진짜 synthetic pointer 기반 hover(초기엔 `dispatchEvent('mouseenter', {bubbles:false})`로 시도했으나 React의 이벤트 위임 방식과 안 맞아 안 먹혔음 — `computer` 툴의 ref 기반 hover로 교체해 검증)로 이미지가 실제 `instagram_feeds.media_urls` 순서대로 바뀌는 것을 직접 확인.
- `tsc` 0 errors, `lint` 0 errors(77 warnings 유지 — 이번 리팩터로 필요 없어진 stale `eslint-disable` 주석 1개 정리해 78→77).

## 2026-08-26 (HOTFIX-147.10 — 인스타그램 캐러셀 "영상이 사진으로 보임" 근본 원인 특정 + 정직한 표시로 수정)
- **사용자 재신고("몇 번째냐", 여러 게시물 공통 문제라고 지적)**: HOTFIX-147.4/147.8의 "children을 bare edge로 요청하면 순서가 보존된다"는 가설이 실제로는 틀렸음을 dev.silostore.net에서 진단 전용 라우트(이번 세션에서 임시로 추가 후 삭제)로 3가지 필드 조합(bare/nested children, 개별 자식 조회)을 직접 대조해 확인 — 순서는 항상 동일했다. 진짜 원인은 바로크 Act 1의 첫 번째 캐러셀 자식(영상)이 Graph API에서 `media_type: VIDEO`는 맞게 주면서 `media_url`은 어떤 필드 조합으로 조회해도 영구히 비어있고 `thumbnail_url`만 내려온다는 것(2020년 게시물 — Meta 쪽 구형 캐러셀 영상 자산에 대한 Graph API 한계로 보이며, 우리 코드로 우회해 실제 재생 파일을 구할 방법이 없음을 실측으로 확인).
- **기존 폴백의 문제**: `resolveChildMediaUrl`이 이 경우 `media_type`을 통째로 "IMAGE"로 강등해 저장 — "원래 영상이었다"는 정보 자체가 사라져 평범한 사진과 구분이 안 됐다.
- **수정**: `media_type`은 항상 사실대로(VIDEO면 VIDEO) 반환하고, 실제 재생 가능한 파일을 구했는지는 별도 `playable` 플래그로 분리(`src/lib/instagramGraph.ts`). 재생 불가능한 VIDEO는 `media_item_types`에 새 값 `"VIDEO_THUMBNAIL"`로 저장(배열 컬럼이라 CHECK 제약 없이 추가 가능)하고, `InstagramFeedPost.tsx`가 이 값을 `<video>` 대신 재생 버튼 아이콘을 얹은 `<img>`로 렌더링해 "이건 영상이었다"를 시각적으로 표시한다. R2 업로드 시 isVideo 플래그도 `playable`을 함께 확인하도록 수정(정지 이미지 바이트를 .mp4로 잘못 저장하지 않도록). `embedThumbnail.ts`의 대표 이미지 선택 로직도 `"VIDEO_THUMBNAIL"`을 안전한 후보로 포함(사용자 지시 — "그 영상이 썸네일이 되어야지": 실제 파일이 진짜 이미지라 안전하게 대표 이미지로 쓸 수 있고, 오히려 그 캐러셀의 원래 대표 영상 프레임일 가능성이 높음).
- **정직하게 밝히는 한계**: Graph API가 실제 영상 파일 자체를 주지 않는 이상 진짜 재생 가능한 영상으로 만들 방법은 없다 — 이번 수정은 "가짜로 사진처럼 보이던 것"을 "이건 영상이었다고 정확히 표시하는 것"으로 바꾸는 것이지, 재생 불가 자체를 없애는 것은 아니다.
- **전체 계정 재동기화 완료(dev.silostore.net, 관리자 세션으로 직접 실행)**: `/api/admin/instagram/resync-carousels`를 전체 CAROUSEL_ALBUM(230개)에 대해 끝까지 실행 — 그중 **52개 게시물**이 이 정확한 버그(영상인데 media_url을 못 받아 IMAGE로 잘못 저장됨)를 갖고 있었고 전부 "VIDEO_THUMBNAIL"로 정정됨(에러 0건). 사용자가 지적한 대로 바로크 Act 1 하나만의 문제가 아니었음이 실측으로 확인됨.
- `tsc`/`lint` 0 errors(77 warnings, 기존 기준선 유지).

## 2026-08-26 (HOTFIX-147.9 — 타임라인 표지 자유 편집(배경+드래그 텍스트) + Craft TextBlock 색상피커/폰트업로드)
- **사용자 지시**: "타임라인 섹션 안의... 모바일 버전에 너무 커서 이미지와 텍스트가 제대로 안보이는것을 수정... 드래그앤 드랍과 사이즈 조절을 내가 자유롭게 할수 있도록 하라구" + "craft 의 'textblock 설정'에 '색상'을 선택할수 있게 해야지" + "모든 텍스트는 폰트 업로드 할수 있게 하라고 했지" + "타임라인 섹션에 처음 로딩되면 보이는 '혁명~제국' 부분을 텍스트를 변형하고 뒷 배경으로 슬라이드영상 또는 이미지를 업로드하고 싶다". 실측(dev.silostore.net, 모바일 375px)으로 재현 확인: TL3 표지(title) 슬라이드가 `.tl-headline-title`(TL3 자체 CSS, 일반 h1/h2/h3 반응형 규칙보다 우선순위가 높아 좁은 화면에서도 줄어들지 않음)로 헤드라인을 32px 그대로 렌더링해 3줄로 줄바꿈되며 "Swipe to Navigate" 안내 툴팁·이전 화살표와 겹쳤다.
- **TL3 표지 슬라이드를 손대지 않고 그 위에 우리 오버레이를 얹는 방식 채택**: TimelineJS3(서드파티, DOM/CSS를 직접 재배치하면 라이브러리 내부와 계속 충돌)의 표지 렌더링 자체를 고치는 대신, `SiloTimelineEmbedBlock`(Craft 블록)이 표지를 보고 있을 때만 그 위에 배경 슬라이드쇼 + 자유배치 텍스트를 겹치는 오버레이를 그린다. "지금 표지를 보고 있는가"는 TL3의 공식 `change` 이벤트로 판별하는데, 표지 슬라이드의 `unique_id`가 정확히 `"title-headline"`이라는 사실은 TL3 공식 문서에 없어 로컬 스크래치 `TL.Timeline` 인스턴스를 직접 만들어 `goToId`로 왕복시키며 실측으로 확인했다(`src/components/timeline/SiloTimelineInner.tsx`). `.tl-storyslider`(실제 슬라이드 표시 영역, `.tl-timenav` 대시보드는 제외)의 top/height를 `ResizeObserver`로 계속 재서 오버레이를 정확히 그 자리에만 겹친다 — 사용자가 정확히 묘사한 구조("1번 슬라이드 부분 위에, 2번 대시보드는 그대로") 그대로 유지.
- **자유배치는 기존 인프라 재사용**: 새 드래그 시스템을 만들지 않고 HOTFIX-147.7의 `useFreePosition`/`FreePositionHandles`/`FreePositionSettingsSection`을 그대로 재사용(`position`/`mobilePosition`을 `SiloTimelineEmbedBlockProps` 최상위에 둬야 하는 계약도 동일하게 따름, PC/모바일 독립 위치 지원). 배경은 `HeroSlideshow.tsx`를 재사용하지 않고(vh 단위 높이 가정이 TL3 표지 영역의 실측 px 높이와 안 맞고, 자체 제목 오버레이가 자유배치 텍스트와 중복돼) 이미지/영상 크로스페이드만 하는 최소 컴포넌트(`CoverBackground`)를 새로 둠 — 확장자로 영상(`.mp4/.webm/.mov/.m4v`) 여부를 판별해 `<video autoPlay muted loop>`로 렌더링.
- **Craft `TextBlock` 색상피커/폰트업로드**: 색상 입력을 순수 텍스트 hex 입력 하나에서 `<input type="color">` 스와치 + hex 텍스트 입력 페어로 교체. 폰트는 EPIC-083/`FontPicker.tsx`+`useCustomFonts.ts`(이미 프로덕션에서 검증된 커스텀 폰트 업로드 인프라 — 지금까지 사이트 크롬/게시글 본문 에디터에만 연결돼 있었고 Craft 블록에는 한 번도 연결된 적 없었음)를 그대로 재사용해 `fontFamily` 필드 추가 — 새 업로드 파이프라인을 만들지 않음. 같은 색상피커/폰트피커를 `SiloTimelineEmbedBlock`의 표지 텍스트 설정에도 동일하게 적용.
- `TextBlockProps`에 필수 필드 `fontFamily`가 추가되면서 `defaultTree.tsx`(홈페이지 기본 트리)의 기존 `<TextBlock>` 7곳도 `fontFamily=""`로 맞춰 갱신.
- `tsc` 0 errors, `lint` 0 errors(77 warnings, 기존 기준선 유지). Claude Browser 툴로 dev.silostore.net을 실제 375px 모바일 뷰포트로 열어 표지 슬라이드 오버랩 버그를 직접 재현(스크린샷)하고, 로컬 스크래치 TL3 인스턴스로 `title-headline` unique_id를 검증한 뒤 구현 — 로그인 세션이 필요한 Craft 에디터 실제 조작(표지 편집 UI 클릭/드래그)까지는 이번 세션에서 로컬 개발 서버에 admin 세션을 안전하게 옮길 방법이 없어(다른 오리진 간 인증 토큰 이전은 의도적으로 차단됨) 못 함. **다음에 확인 필요**: 관리자 세션으로 실제 Craft 에디터에서 "혁명~제국" 등 그룹 타임라인의 "표지 자유 편집"을 켜고 텍스트 드래그/배경 업로드 후 모바일 화면에서 겹침 없이 잘 보이는지, 게시판(leaf) 모드 표지에도 동일하게 잘 동작하는지 육안 확인.

## 2026-08-26 (HOTFIX-147.8 — 인스타그램 캐러셀 순서 수정을 기존 저장 행에도 콕 집어 적용할 수 있는 타겟 재동기화)
- **사용자 재신고**: "아직도 첫번째 인스타그램 미디어가 영상이 아니잖아 !!!" (바로크 Act 1). HOTFIX-147.4는 앞으로의 동기화/전체 재동기화 로직만 고쳤을 뿐, 이미 `instagram_feeds`에 저장된 이 게시물의 행은 손대지 않았다 — Supabase Management API로 직접 조회해 실측 확인: `media_item_types`가 여전히 `["IMAGE","IMAGE","IMAGE","IMAGE","IMAGE","VIDEO"]`(옛 순서 그대로, VIDEO가 맨 뒤)였다.
- **원인**: `/api/admin/instagram/resync-carousels`가 `CAROUSEL_ALBUM` 행 전체를 `id` 오름차순으로 5개씩만 훑는 구조라, 약 230개 행 중 특정 게시물 하나를 고치려면 그 앞의 행을 전부 순서대로 처리해야 도달한다 — 사실상 특정 게시물만 즉시 고칠 방법이 없었다(HOTFIX-147.4가 "실측 미검증"으로 남긴 이유이기도 함, 로컬엔 `IG_ACCESS_TOKEN`이 없어 애초에 실행 자체를 못 해봄).
- **수정**: 같은 라우트에 `igMediaId` 옵션 추가 — 넘기면 그 `ig_media_id` 행 하나만 조회해 즉시 재동기화한다(기존 페이지네이션 방식은 그대로 유지, 전체 일괄 복구 용도로 계속 사용 가능). `tsc` 0 errors.
- **dev.silostore.net에서 실측 검증**: `develop`에 배포된 뒤 관리자 세션으로 `POST /api/admin/instagram/resync-carousels {igMediaId: "17873896711921843"}` 호출 → 재조회 결과 확인.

## 2026-08-26 (HOTFIX-147.7 — Craft 히어로 텍스트 자유 배치(드래그+폰트/색상) + PC/모바일 독립 위치)
- **사용자 지시**: "'혁명~제국 Revolution~Empire' 이렇게 카테고리가 나오게 하는게 아니라, 내가 원하는 크기/색상/폰트의 텍스트를 드래그&드랍으로 자유롭게 이동하는걸 원하고, 그 뒤에 배경으로 슬라이드를 정하는거야" + "모바일에서 텍스트가 제대로 안 보인다, 모바일에서도 위치를 드래그 드롭으로 따로 설정할 수 있게 해줘".
- **히어로 재구성**: 기존엔 `HeroSlideshowWidgetBlock`이 슬라이드 자체에 title/description을 갖고 있어 항상 중앙 정렬 고정 오버레이(어두운 틴트 포함)로만 나왔다 — 이제 그 슬라이드의 title/description을 비워 순수 배경 이미지 슬라이드쇼만 남기고, `ContainerBlock`(position:relative 캔버스) 안에 배경(`HeroSlideshowWidgetBlock`)과 자유 배치 텍스트(`TextBlock`, 이미 정렬/굵기/크기(px)/색상/자유 배치(콜라주) 지원)를 형제로 넣어 겹친다 — 새 블록을 만들지 않고 기존 두 원자 블록을 조합만 다르게 했다. 4개 그룹 페이지(고대~왕정/혁명~제국/프로이트~대중문화/디지털~A.i 문화)의 craft_state를 이 구조로 재작성(Management API).
- **PC/모바일 독립 자유 배치(신규 인프라, 7개 자유배치 블록 중 TextBlock만 우선 적용)**: `useFreePosition.ts`에 `freePositionResponsiveAttrs()` 추가 — 좌표를 인라인 style 대신 CSS 커스텀 프로퍼티(`--fp-x` 등)로 넘기고, globals.css의 `.craft-free-pos`/`@container (max-width:767px)` 규칙이 `--fp-*-m`(모바일 전용) 값이 있을 때만 덮어쓴다(Tailwind는 런타임에 만든 `left-[42%]` 같은 클래스명을 빌드 타임에 스캔 못 해 이 방식이 필요했음). `.craft-home`이 이미 깔아둔 `@container` 컨텍스트를 그대로 쓰므로 관리자 에디터의 "모바일 모드"(캔버스 390px로 좁힘)와 공개 페이지의 실제 좁은 화면 둘 다 별도 JS 분기 없이 동일하게 반응한다.
- **에디터에서 모드별로 다른 값을 드래그**: 새 `DeviceModeContext`(`CraftPageEditor`의 PC/모바일 토글 값을 Toolbox/캔버스/SettingsSidebar 전체에 전파 — 처음엔 캔버스에만 물려 있어 설정 패널이 항상 "pc"로만 봤던 버그를 실측으로 발견해 함께 수정)를 `FreePositionHandles`/`FreePositionSettingsSection`이 읽어, "모바일 모드"에서 드래그/입력하면 `mobilePosition`(신규, 미지정 시 PC 값을 그대로 물려받음)에 별도로 쓰고 PC의 `position`은 건드리지 않는다 — `supportsMobileOverride`를 안 켠 기존 6개 블록(컨테이너/이미지/버튼/영상/슬라이드쇼/게시판연동)은 deviceMode와 무관하게 기존 동작 그대로(회귀 없음).
- **실측 검증(로컬 dev 서버, 관리자 세션으로 실제 Craft 에디터 조작)**: 텍스트 블록 선택 → 좌표(X/Y/너비/높이%) 설정 패널 확인 → "모바일 모드" 전환 → 파란 안내문("지금 모바일 모드 — 여기서 드래그/입력하면 모바일 전용 위치가 따로 저장돼요") 노출 확인 → Y값을 38→60으로 변경 → "PC 모드"로 되돌아가 PC 쪽 Y가 38로 그대로 유지됨을 확인(테스트 변경은 저장하지 않고 폐기).
- `tsc`/`lint` 0 errors, 77 warnings(기존 기준선).

## 2026-08-26 (HOTFIX-147.6 — 온라인 도슨트 리프 페이지 13개의 미설정 위젯 잔해 정리 + 갤러리 위젯 연결, DB 전용)
- **사용자 신고**: "아르누보 페이지에 위는 타임라인, 아래는 갤러리로 하고 싶은데 둘 다 같이 연동되어서 안 된다" — 실측 결과 render_type 충돌이 아니라, 대량 생성 당시 만들어진 뒤 한 번도 손대지 않은 미설정 위젯들이 실제 방문자에게 그대로 노출되고 있었던 것: `quote` 위젯이 기본 placeholder("인용문을 입력하세요")를, `slide` 위젯과 board_id가 비어있는 여분의 `board` 위젯이 각각 "연결된 게시판이 없거나 글이 없어요"/"게시판이 연결되지 않았어요"를 그대로 화면에 뿌리고 있었다. 게다가 `gallery` 위젯은 13개 리프 페이지 중 12개에서 board_id가 아예 비어 있어(아르누보만 예외적으로 이미 연결돼 있었음) 사용자가 원한 "타임라인 다음 갤러리" 조합 자체가 성립하지 않았다.
- **확인 범위**: 그리스/르네상스/바로크/로코코/아르누보/아르데코/비트세대/반문화/대중문화/신고전주의/리전시/빅토리안/인상파 — 온라인 도슨트 산하 리프 페이지 13개 전부 동일한 패턴이었음(사용자가 "다른 페이지들도 확인해달라"고 명시적으로 요청).
- **조치(Supabase Management API, 사용자 승인 후 실행, 코드 변경 없음)**: (1) 한 번도 커스터마이징되지 않은 것으로 확인된 `quote`/`slide`/여분의 빈 `board` 위젯 37개 삭제(사전에 개수·유형을 사용자에게 보고 후 승인받음). (2) board_id가 비어있던 12개 리프 페이지의 `gallery` 위젯을 각 페이지의 실제 게시판 id로 연결. 로컬 dev 서버로 바로크 페이지 실측 확인 — 잔해 문구 전부 사라지고 타임라인 → 갤러리(실제 게시글 카드) 순서로 정상 렌더링됨을 확인.
- 코드 변경 없음(순수 데이터 정리) — `tsc`/`lint` 대상 없음.

## 2026-08-26 (HOTFIX-147.5 — 2단계 카테고리 타임라인을 게시글 집계 대신 하위 카테고리 디렉토리로 교체 + 리프 타임라인 표지 슬라이드 편집처 마련)
- **사용자 재지시**: "'혁명~제국' 타임라인에는 신고전주의/리전시/빅토리안/인상파 이 네가지의 '페이지'가 들어와야 하는거야, 게시글이 아니라." — HOTFIX-147.3이 만든 "group" 모드는 하위 게시판 전체(신고전주의+리전시+빅토리안+인상파)의 게시글을 몽땅 끌어모아 게시글 단위 슬라이드로 보여줬는데, 실제로 원한 건 "이 그룹 바로 아래 카테고리(페이지) 하나당 슬라이드 하나"였다. 추가로 "'신고전주의' 타임라인의 가장 첫번째로 보이는 대시보드 위의 슬라이드를 어디서 수정하냐, 페이지 수정에도 게시판 수정에도 없다"는 지적 — TL3가 아무 이벤트도 안 골랐을 때 보여주는 표지(title) 슬라이드가 게시판 이름 텍스트만 있고 이미지/설명을 넣을 곳이 없었음.
- **카테고리 디렉토리로 교체**: `src/lib/timelineGroup.ts`를 게시글 집계용(`resolveGroupBoardIds`)에서 카테고리 목록용(`fetchNavGroup`)으로 전면 교체 — groupHref 노드의 **직계 자식**(손자까지 안 내려감)만 슬라이드 후보가 된다. 각 카테고리의 site_navigations 제목이 이미 "1750~1850 신고전주의 NeoClassicism"처럼 연대 접두사를 달고 있어 `parseCategoryTitle()`이 그 연대를 그대로 TL3 start/end date로 파싱하고(BC 접두사도 처리 — "BC 1100~146"→연도 부호 반전), 나머지를 headline으로 쓴다. 대표 이미지는 site_navigations.thumbnail_url을 우선 쓰고, 비어있으면 그 카테고리에 매칭되는 게시판의 첫 공개 게시글 이미지로 대체(`fallbackThumbnailForHref`). "자세히 보기" 링크는 그 카테고리의 실제 nav href로 연결(실측 확인 — 4개 리프 페이지 전부 실제 `page_builder` 행이 있어 안전하게 연결됨).
- **카테고리 대표 이미지/소개 수정처**: site_navigations.thumbnail_url/description은 이미 "사이트 구성 관리" → 해당 카테고리 → 관리 → "대표 이미지"/"카테고리 소개"에서 편집 가능했다(새 UI 불필요, 기존 기능을 그대로 활용).
- **리프 타임라인 표지 슬라이드**: `/api/timeline/events`(board 모드)가 이제 `board.widget_settings.timelineHeroSlides[0]`(HOTFIX-147.3에서 만든 "첫 화면 대표사진 히어로"의 첫 슬라이드)을 TL3의 title(표지) 슬라이드 이미지/설명으로도 재사용한다 — 게시판 수정 → Timeline NG → "첫 화면 대표사진 히어로"가 이 표지 슬라이드의 실제 수정처가 된다(새 편집 UI를 또 만들지 않고 기존 히어로 편집 화면 하나로 통합).
- `tsc`/`lint` 0 errors, 77 warnings(기존 기준선). 로컬 dev 서버로 실측 확인: "혁명~제국"(4개 카테고리, 정확한 연대 1750-1850/1795-1837/1837-1901/1860-1890, 각자의 자세히 보기 링크, 3/4는 폴백 썸네일까지 확인), "고대~왕정"(4개), "프로이트~대중문화"(5개) 전부 게시글이 아닌 카테고리 단위로 정상 전환됨을 API+화면 스크린샷으로 확인.

## 2026-08-26 (HOTFIX-147.4 — 인스타그램 캐러셀 항목 순서가 실제 표시 순서와 다른 문제 수정)
- **사용자 재신고**: HOTFIX-143.5로 "캐러셀 항목 통째 누락" 복구 버튼을 만들고 실행까지 했는데도, 바로크 "빨간머리 사제 Act 1"의 캐러셀 첫 번째가 여전히 영상이 아니라 사진으로 나옴.
- **근본 원인(추가 확인)**: `information_schema` 대신 `instagram_feeds` 실데이터로 재확인 — 복구 이후에도 `media_item_types`가 `[IMAGE,IMAGE,IMAGE,IMAGE,IMAGE,VIDEO]`처럼 VIDEO 항목이 항상 배열 맨 뒤로 밀려 저장되고 있었다(HOTFIX-143.5는 "빠진 항목을 되살리는" 버그만 고쳤을 뿐, "순서 자체가 틀어지는" 별개의 버그는 남아있었음). Graph API에 `children{id,media_type,media_url,thumbnail_url}`처럼 자식 서브필드를 중첩해서 요청하면 응답 배열 순서가 실제 캐러셀 표시 순서와 달라지는(특히 VIDEO 항목이 뒤로 밀리는) 문서화되지 않은 동작이 원인으로 확인됨.
- **수정**: `children`을 서브필드 없이 **bare edge**로만 요청하도록 변경(`children{...}` → `children`) — 이 형태의 응답(`data:[{id},...]`)은 실제 표시 순서를 그대로 보존한다. 각 자식의 실제 `media_type`/`media_url`은 이미 있던 `resolveChildMediaUrl()`(HOTFIX-143.5가 "누락된 자식 복구용"으로 만든 개별 조회 로직)이 자식 id 하나하나를 조회해 채우도록 — 이제 캐러셀 자식은 항상 이 경로를 탄다. `src/app/api/instagram/fetch/route.ts`(새 게시물 동기화)와 `src/app/api/admin/instagram/resync-carousels/route.ts`(기존 게시물 복구) 둘 다 동일하게 수정.
- `tsc` 0 errors. **실측 검증 못 함**: 로컬 환경에는 `IG_ACCESS_TOKEN`이 없어(프로덕션/Vercel 전용) 이 순서 보존 가설을 실제 Graph API 응답으로 확인할 수 없었다 — 관측된 데이터 패턴(VIDEO가 항상 뒤로 밀림)과 일치하는 잘 알려진 Graph API 동작을 근거로 한 최선의 수정이며, 100% 확신할 수는 없다. **사용자가 직접 확인해야 하는 것**: 배포 후 `/admin/instagram`에서 "캐러셀 누락 항목 일괄 복구" 버튼을 다시 클릭 → 바로크 Act 1(및 다른 캐러셀들)의 순서가 실제 인스타그램 게시물과 일치하는지 확인, 여전히 틀리면 알려달라(이 경우 순서 문제의 다른 원인을 더 조사해야 함).

## 2026-08-26 (HOTFIX-147.3 — Timeline NG 리프 게시판 히어로 + 온라인 도슨트 2단계 카테고리 통합 타임라인 + TimeNav 상시 썸네일)
- **사용자 지시**: (1) Timeline NG가 적용된 온라인 도슨트 리프 페이지(신고전주의 등) 첫 화면에 대표사진 슬라이드+오버레이 텍스트(제목/요약)를 얹고, 타임라인 마커에 게시글 썸네일이 작게 보이게 해달라 — 단, 기존 게시판 이름/검색/정렬 헤더는 히어로 아래에 그대로 유지. (2) "고대~왕정"/"혁명~제국"/"프로이트~대중문화"/"디지털~A.i 문화"(온라인 도슨트 2단계 카테고리 페이지 4개)에도 타임라인을 넣되, 그 하위 3단계 카테고리 게시판 전부의 글을 한 타임라인에 모아 보여주고, 이 4개 페이지에 Craft 에디터로 자유 편집이 가능하게 해달라. (3) "호버해도 썸네일이 안 뜬다"는 재확인 요청 — 실측 결과 진짜 버그였음(아래 HOTFIX-147.2).
- **HOTFIX-147.2(먼저 발견) — TimeNav 마커에 썸네일이 전혀 안 뜨던 버그**: `public/vendor/timelinejs/js/timeline.js` 실측 확인 — TimelineJS3의 TimeNav 마커는 호버와 무관하게 `media.thumbnail`이 있으면 항상 작은 썸네일을 그린다(`tl-timemarker-media-container` 로직). `/api/timeline/events`가 지금까지 `media.url`(슬라이드용 원본 이미지)만 주고 `media.thumbnail`은 아예 안 줘서 마커에 아무것도 안 그려졌던 것 — 같은 이미지를 `media.thumbnail`로도 함께 내려주도록 수정. 로컬 dev 서버로 실측: 마커마다 작은 정사각 썸네일이 상시 노출됨을 확인.
- **리프 게시판 히어로**: 기존 `HeroSlideshow.tsx`(홈페이지 슬라이드쇼, 이미지+제목/설명 오버레이+자동 전환을 이미 전부 갖춤)를 새로 만들지 않고 그대로 재사용 — `boards.widget_settings.timelineHeroSlides`(신규, `{imageUrl,title,description}[]`)가 있으면 `BoardModule.tsx`가 기존 `<HeroModule>`(브레드크럼+게시판 이름) 위에 `<HeroSlideshow device="both">`를 추가로 그린다(사용자가 명시적으로 "히어로 아래에 그대로 유지"를 선택). `BoardForm.tsx`에 `TimelineHeroSlidesEditor`(이미지 업로드+제목+요약, 추가/삭제) 신규 — `HeroSlideshowWidgetBlock`의 슬라이드 편집 UI와 동일한 패턴.
- **온라인 도슨트 2단계 카테고리 통합 타임라인**: `src/lib/timelineGroup.ts`(신규) `resolveGroupBoardIds()`가 site_navigations 트리에서 주어진 branch(href) 아래 모든 자손 노드를 찾고, 각 href의 마지막 경로 세그먼트를 `boards.slug`로 매칭해 실제 게시판 id 목록을 뽑는다(leaf href 마지막 세그먼트=게시판 slug라는 이 세션에서 이미 여러 번 확인된 관례 재사용). `/api/timeline/events`에 `group=<href>` 쿼리 모드 추가 — 여러 게시판의 글을 `board_id IN (...)`로 한 번에 모아 시간순 병합하고, 각 글의 "자세히 보기" 링크는 (집계 대상이 아니라) 그 글이 실제로 속한 게시판 id를 그대로 쓴다. `SiloTimeline`/`SiloTimelineInner`에 `groupHref` prop 추가(`boardId`와 양자택일).
- **Craft 에디터 신규 원자 블록 — `SiloTimelineEmbedBlock`**: `src/components/craft/primitives/`(6개 Craft 페이지 패밀리가 공유하는 원자 블록 모음, `CraftPageRenderer`/`CraftPageEditor`가 항상 자동 병합)에 추가 — 게시판 하나(mode="board") 또는 카테고리 통합(mode="group", site_navigations branch 드롭다운) 중 선택해 실제 TimelineJS3(`SiloTimeline`, EPIC-147) 위젯을 Craft 캔버스 어디든 꽂을 수 있다. 기존 `TimelineEmbedBlock`(손으로 스타일링한 zig-zag `AlternatingTimelineCanvas`)과는 별개 블록 — 이름 충돌 방지.
- **4개 페이지를 Craft로 전환**: `src/app/[...slug]/page.tsx`의 `CRAFT_RENDERERS`와 `src/app/admin/pages/[id]/page.tsx`의 `CRAFT_EDITORS`에 4개 slug(`online-docent-ancient-monarchy`/`-revolution-empire`/`-freud-pop`/`-digital-ai`) 등록 — 전부 기존 `CraftDocentRenderer`/`CraftDocentEditor`를 그대로 재사용(resolver/defaultTree를 파라미터로 받는 범용 셸이라 새 컴포넌트 불필요). DB에서 이 4개 `page_builder` 행의 `builder_type`을 `craft`로 바꾸고, `craft_state`를 `HeroSlideshowWidgetBlock`+`SiloTimelineEmbedBlock`(group 모드, 각자의 href) 2블록으로 직접 채워 넣어(Management API) 전환 즉시 실제 콘텐츠가 보이게 했다 — 이전 native 위젯들(hero/quote/slide/board/gallery)은 전부 미설정 placeholder 상태였어서(실측 확인 — "게시판이 연결되지 않았어요" 등) 버릴 내용이 없었다.
- **실측 검증(로컬 dev 서버 + 프로덕션 DB, Claude Browser 툴)**: "고대~왕정" 페이지 — 히어로(제목 오버레이)+통합 타임라인(그리스/르네상스/바로크/로코코 4개 게시판 글 15건이 시간순으로 섞여 나옴, 각 글의 "자세히 보기"가 자기 게시판 id로 정확히 연결됨)+TimeNav 상시 썸네일까지 스크린샷으로 확인. "혁명~제국"(사용자가 원래 스크린샷으로 지목한 페이지) 15건, "프로이트~대중문화" 30건도 API로 확인. "디지털~A.i 문화"는 site_navigations에 하위 카테고리가 아직 전혀 없어(실측 확인) "카테고리에 연결된 게시판을 찾지 못했어요"를 정직하게 보여줌(크래시 아님) — 슬러그가 "digital"인 고아 게시판("대중 문화 pop culture", 이전 세션 NEXT_TASK.md에 이미 "맞는 게시판인지 확인 필요"로 남아있던 것과 동일 항목로 추정)이 있지만 현재 nav 트리 어디에도 연결돼 있지 않아 임의로 이어붙이지 않았다 — 사용자 확인 필요.
- `tsc`/`lint` 0 errors, 77 warnings(기존 기준선).

## 2026-08-26 (HOTFIX-147.1 — 타임라인 연대 수정이 저장되지 않는 버그 + 게시글 수정 시 필드 조용히 누락되는 위험한 폴백 제거)
- **사용자 신고**: 아르데코 "Gertrude Lawrence" 게시글을 수정 화면에서 타임라인 연대를 입력하고 "수정완료"를 눌러도 저장되지 않고 타임라인에도 반영되지 않음 — 다른 온라인 도슨트 게시글도 같은 문제인지 확인 요청.
- **근본 원인**: `posts.timeline_year`/`timeline_end_year`/`timeline_display_date` 3개 컬럼이 `authenticated` 역할에 `UPDATE` 권한 없이(INSERT/SELECT만 있고 UPDATE는 `postgres`/`service_role`에만 부여된 채) 추가돼 있었다(`information_schema.column_privileges`로 실측 확인) — 그래서 이 필드를 포함한 게시글 수정 UPDATE 문 전체가 Postgres 권한 오류(42501)로 거부됐다. 새 글 "작성"은 INSERT 권한이 멀쩡해 문제없이 저장되지만, 그 글을 나중에 "수정"하는 순간부터 실패한다.
- **더 위험했던 2차 버그**: `src/app/api/boards/[board_slug]/posts/[post_slug]/route.ts`의 수정 라우트가 UPDATE 실패 시 원인을 가리지 않고 "레거시 DB엔 신규 컬럼이 없을 수 있다(42703)"는 낡은 가정으로 무조건 title/body/is_docent_post만 남긴 축소 재시도로 넘어가 200 OK를 반환하고 있었다 — 이 때문에 타임라인 연대뿐 아니라 body_json/featured_image_url/category/tags/thumbnail_visible까지 조용히 저장에서 빠진 채 "수정 완료" 화면으로 넘어갔다. `updateError.code === "42703"`일 때만 이 재시도를 타도록 수정 — 그 외 에러(권한 문제 등)는 이제 실제 실패로 사용자에게 보인다.
- **영향 범위**: 컬럼 권한은 게시판이 아니라 테이블 전체에 걸리므로, `render_type='timeline_ng'`인 14개 게시판(온라인 도슨트 leaf 13개 + 사일로 타임라인) 전부에서 동일하게 재현되는 구조적 버그였다 — 특정 게시글/게시판만의 문제가 아니었음.
- `tsc` 0 errors. **DB 권한 보강이 아직 남아있음**: `grant update (timeline_year, timeline_end_year, timeline_display_date) on posts to authenticated;`가 근본 해결책인데 auto-mode 클래시파이어가 이 GRANT 실행을 차단해 사용자 승인 대기 중 — 승인 후 실행하면 이제 수정 라우트가 축소 재시도 대신 정상적으로 3개 필드를 저장한다.

## 2026-08-26 (HOTFIX-143.5 — 인스타그램 캐러셀 항목 통째 누락 수정 + 기존 게시물 복구 라우트)
- **사용자 신고**: 바로크 게시판 "빨간머리 사제 Act 1"/"Act 2"의 인스타그램 캐러셀에서 첫 번째 '동영상'이 안 보이고 두 번째 '사진'만 나온다는 신고 → 조사 도중 사용자가 아르데코 "Gertrude Lawrence" 게시글도 캐러셀 3개 중 마지막 1개만 나온다고 추가 신고.
- **근본 원인**: `src/app/api/instagram/fetch/route.ts`가 캐러셀(여러 장) 게시물의 각 항목을 Graph API의 `children{id,media_type,media_url,thumbnail_url}` 서브필드 응답 하나로만 받아왔는데, 이 서브필드 응답이 특정 자식(특히 VIDEO)의 `media_url`을 아예 비워서 내려주는 경우가 실제로 확인됨(Gertrude Lawrence 게시글은 원래 3개 항목 중 2개가 이 사유로 완전히 누락되어 `instagram_feeds.media_urls`에 1개만 저장돼 있었음, `media_urls.length > 1` 조건이 깨지면서 캐러셀 UI 자체가 단일 이미지 뷰로 전환됨). 기존 코드는 `media_url`이 없는 자식을 `continue`로 통째로 건너뛰어 사진/영상이 순서까지 밀리며 조용히 사라졌다.
- **수정**: `src/lib/instagramGraph.ts`(신규) — `resolveChildMediaUrl()`이 `media_url`이 빈 자식을 `GET /{child-id}?fields=media_type,media_url,thumbnail_url`로 단독 재조회하고, 그래도 실패하면 `thumbnail_url`을 정지 이미지로 대체해 최소한 아무것도 사라지지 않게 한다. `/api/instagram/fetch`가 이 헬퍼를 쓰도록 교체(앞으로 새로 동기화되는 게시물부터 적용).
- **기존에 이미 손상된 채로 저장된 게시물 복구**: `src/app/api/admin/instagram/resync-carousels/route.ts`(신규, 관리자 전용, 커서 페이지네이션) — `instagram_feeds`의 모든 `CAROUSEL_ALBUM` 행을 Graph API에서 `children`만 다시 받아와(위 복구 로직 포함) `media_urls`/`media_item_types`를 완전히 재구성해 덮어쓴다. `/admin/instagram` 페이지에 "캐러셀 누락 항목 일괄 복구" 버튼 추가(기존 "깨진 썸네일 일괄 복구"와 동일한 반복 호출 패턴).
- `tsc`/`lint` 0 errors, 77 warnings(기존 기준선). **로컬에는 `IG_ACCESS_TOKEN`/`IG_USER_ID`가 없어(프로덕션/Vercel 환경 변수 전용) 실제 Graph API 재조회는 로컬에서 검증 불가** — Supabase Management API로 `instagram_feeds` 원본 데이터를 직접 조회해 문제(누락/순서)를 확인했고, 코드 로직은 tsc/lint로만 검증됨. **사용자가 직접 해야 하는 것**: `/admin/instagram`(dev.silostore.net 또는 프로덕션, 실제 IG 환경 변수가 있는 배포)에서 "캐러셀 누락 항목 일괄 복구" 버튼을 클릭 — 이 두 게시글을 포함해 저장된 모든 캐러셀 게시물이 대상이 됨.

## 2026-08-25 (EPIC-147-후속 2차 — Timeline NG를 클래식 TimelineJS3 스타일로 전면 교체 + 연대 직접 지정 + 본문 발췌 + 슬라이드 높이 설정)
- **사용자 재지시(스크린샷 2장 + 화면 녹화 영상 첨부)**: "지금 네가 구현한 스타일은 싫어... 예전 스타일의 타임라인을 원해. 여기에 단지 하단 줌인 줌아웃 기능이 추가된걸 원해", "게시글이 쓰여진 날짜가 timeline 에 적용되었는데... 게시글이 쓰여진 날과는 독립적인, 타임라인에 등장하는 연대를 설정할수 있도록 해줘", "타임라인에는 게시글의 모든게 올라가면 안돼... 최대 100자의 게시글의 첫 도입부내용을 올리는데, instagram 게시물 보기 같은 embed 내용은 필요없어", "미디어 와 제목, 설명의 위아래 폭이 너무 좁아 설정할수 있게 해줘", "타임라인을 '위젯'처럼 페이지 수정에서도 추가제거 할수 있게 만들어줘".
- **라이브러리 전면 교체**: 이전 EPIC-147이 쓰던 `@knight-lab/timeline-ng`(Svelte 5, "Timeline NG" 신버전)의 화면 자체가 사용자가 원하는 클래식 스타일(전체 배경 이미지+오버레이 텍스트, 아이콘 칩 형태의 TimeNav, 돋보기 줌 버튼)과 근본적으로 다른 레이아웃이라 CSS로 되돌릴 수 없었다 — 그 클래식 스타일을 실제로 만드는 원조 라이브러리 `@knight-lab/timelinejs`(TimelineJS3, 순수 JS/DOM, Svelte 아님)로 완전히 교체. `svelte`/`@knight-lab/timeline-ng`/`patch-package`(업스트림 버그 패치용이었으나 이제 불필요) 전부 제거.
- **TimelineJS3 번들링 문제 2건을 실측으로 발견+해결**:
  1. package root(`@knight-lab/timelinejs`)로 `import`하면 raw source(`src/js/index.js`)가 `.less` 파일을 직접 import해 Turbopack이 "Unknown module type"으로 빌드를 실패시켰다.
  2. 미리 빌드된 `dist/js/timeline.js`(전역 `window.TL`을 설정하는 부수효과 스크립트)를 `import`로 가져와도, 번들러가 모든 모듈을 자기 함수 스코프로 감싸버려 그 `var TL`이 더 이상 진짜 전역에 닿지 않았다(로드는 되는데 `window.TL`이 계속 `undefined`).
  - **해결**: `dist` 산출물 전체(js/css/폰트/아이콘/테마/locale)를 `public/vendor/timelinejs/`에 자체 호스팅으로 복사해두고, `<SiloTimelineInner />`가 진짜 `<script src>`/`<link>` 태그를 `document.head`에 직접 주입한다 — 번들러 스코프를 완전히 우회해 라이브러리가 기대하는 대로 전역에 등록된다. `eslint.config.mjs`의 `globalIgnores`에 `public/**`이 빠져있던 것도 함께 발견해 추가(이 vendor 파일을 실수로 그대로 두면 미니파이된 서드파티 코드를 ESLint가 그대로 파싱해 수백 개의 가짜 에러를 냈다).
  - TL3 내부가 폰트/테마 CSS를 `new URL(fragment, script_path)`로 찾는데 `URL` 생성자의 `base`는 반드시 절대 URL이어야 해서(실측 — 루트-상대 경로를 넘기면 "Invalid base URL") `script_path`에 `window.location.origin`을 직접 붙였다.
- **스크롤 휠 줌**: 클래식 스타일은 그대로 두고, TimeNav(`.tl-timenav`) 위에서 마우스 휠을 굴리면 TimelineJS3가 공식 지원하는 `zoomIn()`/`zoomOut()` 메서드를 호출하도록 추가(60ms 스로틀, 트랙패드 연속 이벤트 과도 줌 방지). 슬라이드 전환 애니메이션 자체는 라이브러리 기본 동작(`duration`/`ease` 옵션, 기본 1초 easeInOutQuint) — 별도 구현 불필요.
- **게시글 작성일과 독립적인 "타임라인 연대"**: `posts.timeline_year`/`timeline_end_year`/`timeline_display_date`(신규 컬럼, 전부 nullable) 추가 — `/api/timeline/events`가 이 값이 있으면 그대로 쓰고 없으면(기존 글) `created_at`으로 폴백한다. `PostForm.tsx`에 "타임라인 연대"(시작/끝 연도 + "circa 1750" 같은 표시 텍스트 오버라이드) 입력 섹션을 새로 추가 — 게시판의 `render_type==="timeline_ng"`일 때만 보임(`useSelectedBoardTypeAndCategory`에 `renderType` 추가). 수정 API는 "값이 없음"과 "이 필드를 안 건드림"을 구분해(요청 body에 키 자체가 있는지로 판정) 다른 게시판 글 수정 시 실수로 연대 값을 지우지 않는다.
- **본문 발췌 100자 + 임베드 제거**: `htmlExcerpt.ts`의 `htmlToExcerpt()`가 태그를 벗기기 전에 임베드 블록(`<div data-type="embed">...</div>`, 예: Instagram)을 통째로 잘라내도록 수정(기존엔 태그만 벗겨 "Instagram 게시물 보기" 같은 링크 텍스트가 그대로 남았음) — 기존 호출부(TimelineRenderer 등)에도 그대로 적용되는 안전한 개선. `/api/timeline/events`가 본문 전체 HTML 대신 이 100자 발췌만 쓰고, "자세히 보기" 링크는 발췌 뒤에 별도로 붙여 계속 내비게이션 역할을 한다.
- **슬라이드 영역 높이 설정**: `boards.widget_settings.timelineNgStageHeightPx`(신규) — "게시판 관리" 화면에 Timeline NG 전용 섹션(슬라이드 영역 높이 입력)을 추가, `BoardRendererProps`/`BoardModule`/`useBoardData` 전체에 배관. TL3의 `height` 옵션으로 그대로 전달(하단 TimeNav 높이와는 무관, 미디어/제목/설명이 보이는 위쪽 영역만 커짐).
- **"위젯처럼 추가/제거" — 이미 가능했음(신규 코드 불필요)**: Page Builder의 기존 "Board" 위젯(`PageBuilderRenderer.tsx`의 `case "board"`)이 이미 `BoardModule`→`BoardRenderer`→(이번에 등록한)`TimelineNgRenderer`로 그대로 위임되는 구조라, 어떤 페이지에서든 "페이지 수정 → + 위젯 추가 → Board → timeline_ng 게시판 선택"만으로 이미 추가/제거가 된다 — 별도 위젯 타입을 새로 만들 필요가 없었다.
- **검증**: `npx tsc --noEmit`/`npm run lint` 0 errors, 77 warnings(기존 기준선과 동일, `public/vendor` 오탐 수백 건은 `eslint.config.mjs` 수정으로 해결). 로컬 dev 서버 + Claude Browser 툴로 바로크 게시판(실제 프로덕션 글 6건)을 로드해 (1) 클래식 스타일(돋보기 줌 아이콘, 칩 마커, 배경 이미지형 슬라이드) 스크린샷으로 목표 이미지와 대조 확인, (2) 실제 `WheelEvent` 디스패치로 TimeNav `.tl-timeaxis` 폭이 줌인/줌아웃 양방향으로 변하는 것 확인, (3) 발췌가 정확히 100자 근방으로 잘리고 "Instagram 게시물 보기" 텍스트가 전혀 안 남는 것 확인, (4) "자세히 보기" 클릭 시 `window` 전역 마커가 유지된 채(=새로고침 없음) 상세 페이지로 이동하는 것 확인, (5) 글쓰기 화면에 "타임라인 연대" 섹션이 실제로 보이는 것 확인.
- **다음에 확인 필요**: (1) 기존에 이미 작성된 도슨트 글들은 `timeline_year`가 비어있어 지금은 작성일(created_at)로 타임라인에 배치된다 — 실제 역사적 연대에 맞게 보이려면 관리자가 글을 열어 "타임라인 연대"를 하나씩 채워야 한다(자동 마이그레이션 불가 — 각 글의 실제 다루는 시대는 본문을 읽어야 알 수 있음). (2) 슬라이드 영역 높이를 실제 게시판 관리 화면에서 조정해보고 원하는 만큼 넓어지는지 육안 확인. (3) `public/vendor/timelinejs`는 이 라이브러리의 특정 버전 산출물을 그대로 복사한 것이라, 향후 `@knight-lab/timelinejs` 버전을 올리면 이 폴더도 수동으로 다시 복사해야 한다(자동 동기화 없음 — package.json의 devDependency로만 버전을 추적).
- **변경 파일**: `src/components/timeline/SiloTimeline.tsx`, `src/components/timeline/SiloTimelineInner.tsx`(전면 재작성), `src/types/knight-lab-timelinejs.d.ts`(신규), `public/vendor/timelinejs/**`(신규, 자체 호스팅 산출물), `eslint.config.mjs`, `src/app/api/timeline/events/route.ts`, `src/lib/htmlExcerpt.ts`, `src/components/boards/renderers/TimelineNgRenderer.tsx`, `src/components/boards/renderers/types.ts`, `src/components/modules/BoardModule.tsx`, `src/lib/useBoardData.ts`, `src/lib/useBoardOptions.ts`, `src/components/admin/BoardForm.tsx`, `src/app/admin/boards/[id]/page.tsx`, `src/app/admin/boards/new/page.tsx`, `src/components/boards/PostForm.tsx`, `src/components/boards/WriteBoardForm.tsx`, `src/app/boards/[board_slug]/[post_slug]/edit/page.tsx`, `src/app/api/boards/[board_slug]/posts/route.ts`, `src/app/api/boards/[board_slug]/posts/[post_slug]/route.ts`, `package.json`(timeline-ng/svelte/patch-package 제거, timelinejs 유지).

## 2026-08-25 (EPIC-147-후속 — 온라인 도슨트 leaf 게시판 전부를 Timeline NG로 일괄 전환, 코드 변경 없음)
- **사용자 지시**: "온라인 도슨트의 모든 하위 카테고리 페이지의 게시판들을 Timeline NG 로 바꿔줘 일일이 내가 지정하기 불편해" — EPIC-147 완료 시점에는 `group_key='docent'`로만 필터링해 르네상스/바로크 2개만 찾았는데, 실제로는 나머지 11개 leaf 게시판이 `group_key`가 비어있는 채로(카테고리 slug만 site_navigations 트리와 일치) 존재하고 있었다.
- **찾은 나머지 11개**(전체 boards 테이블을 다시 훑어 `site_navigations`의 "온라인 도슨트" 3단 트리 leaf와 이름/slug로 대조): 그리스(greeks)/로코코(rococo)/신고전주의(neoclassicism)/리젠시(regency)/빅토리아(victoria)/인상파(impressionism-club)/아르누보(art-nouveau)/아르데코(art-deco)/비트 세대(beat-generation)/카운터 컬처(counter-culture)/대중 문화(digital, "디지털~A.I 문화" 리프에 대응). Management API로 `render_type = 'timeline_ng'` 일괄 UPDATE(CHECK 제약은 EPIC-147에서 이미 확장돼 있어 추가 DDL 불필요).
- **검증**: 코드 변경 없이 순수 데이터 전환이라 별도 tsc/lint 불필요. 로컬 dev 서버 + Claude Browser 툴로 그리스/인상파 게시판을 직접 열어 SlidePlayer가 정상 마운트되는 것 확인(각각 실제 게시글 데이터로 렌더링).
- **결과**: 이제 "온라인 도슨트" 산하 leaf 게시판 13개(르네상스/바로크 포함) + "사일로 타임라인" 전부 Timeline NG로 렌더링됨 — EPIC-147이 만든 렌더러/API 코드는 전혀 안 바뀌었고 DB의 `render_type` 값만 게시판별로 켜졌다.
- **변경 파일**: 없음(DB 데이터만 변경) — CHANGELOG.md/NEXT_TASK.md만 기록.

## 2026-08-25 (EPIC-147 — 사일로 게시글을 Knight Lab "Timeline NG" 인터랙티브 슬라이드로 렌더링, 게시판 목록(List)을 대체)
- **사용자 지시**: 첨부 `Timeline Json.docx`(Knight Lab TimelineJS의 Svelte 5 재구현 "Timeline NG" 문서)를 숙지하고, 그 JSON 구조와 프론트엔드(SlidePlayer)를 그대로 써서 사일로 게시판 글을 인터랙티브 타임라인으로 렌더링 — "온라인 도슨트"의 leaf 게시판들과 "About Silo"의 "사일로 타임라인" 게시판에서 기존 목록 뷰를 대체.
- **문서 조사**: docx(pandoc 미설치라 docx를 직접 unzip해 word/document.xml + 임베드 이미지 파싱) + GitHub(`NUKnightLab/timeline-ng`, 2026-07-29 npm 0.3.0 배포)로 실제 API 확인 — `@knight-lab/timeline-ng`(Svelte 5 컴포넌트 `SlidePlayer`/`loadTimeline`)와 `@knight-lab/timeline-ng-core`(TLTimeline 스키마 타입) 둘 다 실재하는 패키지임을 확인.
- **업스트림 패키징 버그 발견+패치(가장 큰 난관)**: 이 패키지를 그대로 설치해 React에서 `mount()`(Svelte 5 공식 비-Svelte 프레임워크 연동 API)로 띄우면 두 가지가 순서대로 막혔다.
  1. **SSR 크래시**: 컴파일된 번들이 모듈 최상단에서 `DOMPurify.addHook(...)`을 즉시 호출하는데, Next.js가 "use client" 페이지도 최초 요청 때 Node.js에서 한 번 SSR한다 — `window`가 없는 그 순간 DOMPurify는 아무 메서드도 없는 빈 스텁만 반환해 즉시 TypeError. → `<SiloTimeline />`을 `next/dynamic({ssr:false})`로 감싸 클라이언트 전용으로만 로드.
  2. **`effect_orphan` 런타임 에러**: SSR 문제를 우회한 뒤에도 마운트 시 Svelte 런타임 에러(`https://svelte.dev/e/effect_orphan`)로 계속 실패. GitHub에서 `packages/player/vite.config.ts`를 직접 확인해 원인 특정 — `rollupOptions.external`에 `'svelte/internal'`(Svelte **4** 시절 경로)만 있고 Svelte 5가 실제로 쓰는 `'svelte/internal/client'`는 빠져 있어, 문자열이 정확히 일치하지 않는 그 경로가 external 처리되지 않고 **번들에 그대로 인라인**됐다 — 그 결과 우리 앱의 Svelte 런타임과 이 패키지에 통째로 박힌 별도의 Svelte 런타임, 두 개의 독립된 인스턴스가 공존하며 effect 소유권 추적이 서로 다른 스코프를 참조해 "orphan"으로 처리됐다. **수정**: 저장소를 로컬에 clone해 `external: (id) => id === 'svelte' || id.startsWith('svelte/') || ...`로 고친 뒤 `packages/player`만 재빌드(코어는 monorepo 상대경로 alias로 해결) — 결과 번들이 131KB→73KB로 줄고(`svelte/internal/client`가 이제 진짜로 external) 정상 마운트됨을 실측 확인. 이 로컬 수정을 `patch-package`(신규 devDependency, `postinstall` 스크립트로 설치마다 자동 재적용)로 고정해 `npm install`만으로 재현되게 함 — `patches/@knight-lab+timeline-ng+0.3.0.patch`.
- **구현**:
  1. **`/api/timeline/events`**(신규) — `?board=<slug|id>` 게시판의 공개 글을 DB에서 조회해 TLTimeline JSON으로 실시간 변환. 기존 `/api/boards/[board_slug]/posts`와 동일한 `fetchBoard`/`canReadBoard`(RLS와 별개의 2차 게이팅) 패턴을 재사용. `start_date`는 `created_at`을 UTC로 파싱, `text.text`는 게시글 `body`(HTML) 뒤에 `<a href="/boards/{boardId}/{postSlug}">자세히 보기</a>`를 이어붙여 그 자체로 상세 페이지 내비게이션 역할을 하게 함, `media`는 `featured_image_url`(썸네일 숨김이면 제외), `group`은 `post.category`, `unique_id`는 `post.id`.
  2. **`<SiloTimeline />`**(신규, `src/components/timeline/`) — Svelte→React 브릿지. `SiloTimeline.tsx`(공개 API, `next/dynamic({ssr:false})`로 `SiloTimelineInner.tsx`를 감싸기만 함) + `SiloTimelineInner.tsx`(실제 마운트: `loadTimeline()`로 위 API를 가져와 `mount(SlidePlayer, {target, props:{timeline}})`) 2단 구조. `loadTimeline()`이 `at://` 또는 절대 `http(s)://` URL만 받고 상대 경로는 거부하는 것도 실측으로 확인해 `window.location.origin`을 직접 붙임.
  3. **클라이언트 사이드 라우팅 가로채기(요구사항 3)** — Svelte가 그리는 DOM은 React가 알 수 없으므로, 마운트된 컨테이너에 네이티브 `click` 리스너를 달아 내부 `<a href="/...">` 클릭을 가로채(`preventDefault`+`router.push`) 새로고침 없이 이동시킨다. "미디어를 클릭해도" 이동하도록, 클릭 지점에서 가장 가까운 `.tl-slide` 안의 첫 링크로 대신 이동하는 폴백도 포함.
  4. **`BoardLayoutType`에 `"timeline_ng"` 신설**(`boardLayout.ts`) — EPIC-050의 기존 `"timeline"`(연/월/일 그룹 리스트)과는 완전히 별개 레이아웃이라 그 값을 재사용하지 않음(기존 timeline 게시판 동작 무변경). `TimelineNgRenderer.tsx`를 `BoardRendererRegistry`에 등록, 관리자 게시판 설정(`BoardForm.tsx`)의 Board Type 드롭다운에도 노출.
  5. **DB**: `boards.render_type` CHECK 제약에 `'timeline_ng'` 추가(사용자 승인 후 Management API로 실행) + 사일로 타임라인(기존 `'timeline'`)/르네상스·바로크(기존 `'gallery'`) 3개 게시판을 `'timeline_ng'`로 전환.
- **검증**: `npx tsc --noEmit`/`npm run lint` 0 errors(신규 경고 없음). 로컬 dev 서버 + Claude Browser 툴로 실제 프로덕션 게시글(바로크 6건 — 비발디/베르메르 등 실제 도슨트 글)을 로드해 (1) SlidePlayer가 정상 마운트되고 TimeNav에 실제 날짜(2020~2021)가 찍히는 것, (2) 슬라이드 본문에 원본 HTML(인스타그램 임베드 포함)과 "자세히 보기" 링크가 그대로 렌더링되는 것, (3) 그 링크를 클릭하면 `window` 전역 마커가 유지된 채(=새로고침 없음) `/boards/baroque/act-1`로 정상 이동하고 페이지 타이틀도 바뀌는 것(진짜 client-side routing)을 직접 확인. 사일로 타임라인 게시판(2건)에서도 동일하게 정상 마운트 확인.
- **다음에 확인 필요**: (1) "온라인 도슨트"의 나머지 leaf 항목들(그리스/르네상스/바로크/로코코/신고전주의/리전시/빅토리안/인상파/아르누보/아르데코/비트세대/반문화/팝컬처 — `site_navigations`에 이미 3단 트리로 존재)은 아직 대부분 `boards` 행 자체가 없어(르네상스/바로크만 존재) 이번 범위에 포함 못 함 — 새로 게시판이 만들어질 때 관리자가 Board Type을 "Timeline NG"로 지정하면 자동으로 이 렌더러를 탄다. (2) 업스트림에 `svelte/internal` 외부화 버그를 이슈로 등록할지 검토(`npx patch-package @knight-lab/timeline-ng --create-issue`로 바로 만들 수 있음). (3) 실제 관리자 화면에서 "게시판 관리 → Board Type"으로 다른 게시판도 Timeline NG로 바꿔보고 싶을 때를 위해 이 문서를 참고.
- **변경 파일**: `src/app/api/timeline/events/route.ts`(신규), `src/components/timeline/SiloTimeline.tsx`(신규), `src/components/timeline/SiloTimelineInner.tsx`(신규), `src/components/boards/renderers/TimelineNgRenderer.tsx`(신규), `src/components/boards/renderers/index.ts`, `src/lib/boardLayout.ts`, `src/components/admin/BoardForm.tsx`, `package.json`(`@knight-lab/timeline-ng`/`svelte`/`patch-package` 추가, `postinstall` 스크립트), `patches/@knight-lab+timeline-ng+0.3.0.patch`(신규).

## 2026-08-25 (EPIC-146 — "홈페이지 설정 관리"에 PC/태블릿/모바일 3단 토글 복원, 태블릿을 pc/mobile과 동등한 독립 설정으로 승격)
- **사용자 지시**: "'홈페이지 설정 관리'에 'tab' preview 토글도 추가해줘. 예전에 있었는데 어디로 사라진거야. 그리고 '모바일' preview 와 설정과 똑같은 설정가능하게 해줘".
- **조사**: `git log -S`로 추적한 결과 EPIC-136("헤더 편집 캔버스를 실제 Navbar로 전환") 커밋에서 PC/태블릿/모바일 3단 토글이 PC/모바일 2단으로 줄어들었다 — 그 이전 태블릿은 "미리보기 전용"(별도 저장값 없이 PC 값을 좁은 화면에 그대로 보여주기만 함)이었는데, 이 실전용성 낮은 기능이 실제 Navbar 전환 작업 중 정리되며 함께 빠졌다(Navbar.tsx에 "PC/태블릿/모바일 토글... 태블릿은 PC 데이터를 그대로 씀"이라는 주석만 흔적으로 남아있었음).
- **구현**: 단순 복원이 아니라 사용자 요청대로 승격 — 태블릿을 pc/mobile과 완전히 동등한 독립 설정 슬롯으로 만든다.
  - **설정 타입 6개** 확장(`{pc, mobile}` → `{pc, tablet, mobile}`): `mainLogoSettings.ts`/`sidebarIconsSettings.ts`/`topTabStyleSettings.ts`/`accountMenuStyleSettings.ts`/`headerLayoutPositions.ts`/`topSidebarSettings.ts`/`heroSlideshow.ts`. 각 파일의 `normalize*()`는 `tablet`이 없는(승격 이전) 기존 저장값을 만나면 **pc 값을 그대로 물려받아** 채운다 — 관리자가 처음 태블릿 탭을 열어도 갑자기 빈 기본값이 아니라 지금까지 보이던 대로 보이다가, 직접 편집하는 순간부터 갈라진다(이 프로젝트가 pc/mobile 분리 때부터 지켜온 "저장 전까지 기존 라이브 데이터 그대로" 원칙과 동일).
  - **`Navbar.tsx`**: `deviceOverride` prop을 `"pc"|"mobile"` → `"pc"|"tablet"|"mobile"`로 확장. 새 훅 `useDeviceTier()`(`useIsMobileViewport.ts`에 추가, 이 프로젝트가 이미 쓰는 Tailwind md(768px)/lg(1024px) 경계를 그대로 따름)로 실제 방문자 뷰포트 폭에서 3단을 판정하는 `deviceKey`를 만들고, 로고/사이드바 아이콘/상단 탭 스타일/계정 메뉴 스타일/헤더 위치/상단 사이드바 6곳의 `isMobileViewport ? .mobile : .pc` 삼항연산자를 전부 `[deviceKey]` 제네릭 인덱싱으로 교체. **기존 `isMobileViewport`(767px 이하) 판정 자체는 그대로 남겨뒀다** — 상단 사이드바 컬럼 스택 등 "모바일 전용 레이아웃 트릭"에 이미 쓰이고 있어, 이 부분까지 태블릿 인식으로 바꾸면 태블릿 폭에서 불필요하게 발동할 위험이 있는 별개의 질문이기 때문.
  - **`src/app/admin/navigation/settings/page.tsx`("홈페이지 설정 관리")**: `deviceTab` state에 `"tablet"` 추가 + PC/▦태블릿/모바일 3단 토글 버튼 + 태블릿 전용 캔버스 프레임(820px, 공개 사이트의 태블릿 판정 구간 768~1023px 한가운데 값). 나머지 코드는 이미 `value[deviceTab]` 제네릭 패턴이라 대부분 자동으로 동작했지만, `duplicateNavItem()`(탭 복제 시 스타일도 복사)이 `["pc", "mobile"] as const`로 하드코딩돼 있던 것을 발견해 `["pc", "tablet", "mobile"]`로 수정(안 고쳤으면 탭을 복제할 때마다 태블릿 스타일만 조용히 누락될 뻔함).
  - **`src/app/page.tsx`/`HeroSlideshow.tsx`**: 홈페이지 히어로 슬라이드쇼의 PC/모바일 2-way `hidden md:block`/`md:hidden` 분기를 3-way(`hidden lg:block` / `hidden md:block lg:hidden` / `md:hidden`)로 확장 — 이미지 이중 다운로드 방지용 `<picture><source media>` 반전 트릭(EPIC-094)도 태블릿 구간을 포함하도록 media query 갱신(태블릿의 "반대쪽"은 `(max-width: 767px), (min-width: 1024px)`처럼 두 구간의 합집합, 콤마로 이은 media query가 OR로 동작하는 CSS 표준 동작을 이용).
- **의도적으로 건드리지 않은 것**: (1) `headerLayoutSettings.ts`(EPIC-134 GrapesJS 통합 헤더 레이아웃, `unified_header_layout`)는 EPIC-135 이후 "홈페이지 설정 관리" UI에 노출 창구 자체가 없는 사실상 죽은 경로라 태블릿을 추가하지 않음. (2) `CraftPageEditor.tsx`(Craft.js 본문 에디터)의 PC/모바일 토글은 이번 요청 범위(헤더/로고/슬라이드쇼 설정)와 다른 화면이라 그대로 둠. (3) `HeroSlideshowWidgetBlock.tsx`(Craft 블록의 "PC/모바일 설정 동기화" 드롭다운)도 별개 보조 기능이라 태블릿 옵션을 추가하지 않음 — 필요해지면 후속으로 추가 가능.
- **검증**: `npx tsc --noEmit`/`npm run lint` 0 errors(신규 경고 없음, 기존 77개 warning과 동일 — 새 `useDeviceTier` 훅도 기존 `useIsMobileViewport`와 동일한 setState-in-effect 패턴이라 신규 경고 유발 안 함). 로컬 dev 서버 + Claude Browser 툴로: (1) 태블릿 토글 클릭 시 캔버스가 820px로 정확히 리사이즈되는 것 확인, (2) 태블릿 탭에서 로고 왼쪽 텍스트를 "TABLET-TEST"로 바꾼 뒤 PC/모바일 탭으로 전환해 각각 원래 값("I'm your")을 그대로 유지하는 것(=세 상태가 완전히 독립)을 직접 확인(프로덕션 DB에는 저장하지 않고 되돌림), (3) 실제 820px 폭에서 공개 홈페이지를 로드해 콘솔에 새 에러 없이 정상 렌더링되는 것 확인(이 사이트의 홈페이지는 현재 `builder_type='craft'`라 히어로 슬라이드쇼 3-way 분기 자체는 이 사이트에서 당장 노출되지 않음 — `native` 모드 페이지에서 실제로 쓰이게 될 코드 경로).
- **변경 파일**: `src/lib/mainLogoSettings.ts`, `src/lib/sidebarIconsSettings.ts`, `src/lib/topTabStyleSettings.ts`, `src/lib/accountMenuStyleSettings.ts`, `src/lib/headerLayoutPositions.ts`, `src/lib/topSidebarSettings.ts`, `src/lib/heroSlideshow.ts`, `src/lib/useIsMobileViewport.ts`, `src/components/Navbar.tsx`, `src/components/HeroSlideshow.tsx`, `src/app/page.tsx`, `src/app/admin/navigation/settings/page.tsx`.

## 2026-08-25 (HOTFIX-141.22 — "홈페이지 설정 관리"/Craft 에디터 preview가 실제 홈페이지와 다르던 문제의 진짜 근본 원인 2건 수정, 작업 브랜치 `feature/EPIC-146`)
- **사용자 반복 신고("수십 번째")**: "'홈페이지 설정'의 PC preview가 실제 홈페이지 출력과 다르다", "Craft 에디터 preview와 실제 홈페이지 출력이 다르다", "'홈페이지 설정'의 모바일 preview와 실제 모바일 출력도 다르다" — HOTFIX-141.1~.21에 걸쳐 여러 번 패치했지만 재발.
- **원인 1(PC 헤더 '스튜디오'/'관리자'가 화면 밖으로 사라짐)**: 실측(로컬 dev 서버 + Claude Browser 툴로 실제 저장된 프로덕션 `header_positions` 값을 그대로 로드해 확인) 결과 `account:admin`(dxPx=-747)/`tab:space_inquiry`="스튜디오"(dxPx=-735) 두 슬롯이 HOTFIX-141.15(드래그 오프셋을 기준 폭 대비 비율로 스케일링하는 기능) 도입 **이전**에 드래그된 값이라 `refWidthPx`가 아예 없었다 — 141.15 코드는 `refWidthPx`가 없으면 "배율 1로 그대로 적용"했는데, 이게 관리자 캔버스 폭과 실제 방문자 창 폭이 조금만 달라도 −747px 같은 큰 오프셋을 그대로 옮겨 화면 밖(x좌표 음수)으로 밀어내는 원인이었다. (HOTFIX-141.15 커밋 당시 NEXT_TASK.md에도 "과거에 저장된 오프셋은 재드래그해야 완전히 반영된다"는 한계가 이미 기록돼 있었으나, 두 슬롯이 그 이후로 한 번도 재드래그되지 않아 계속 재발했다.)
- **수정 1**: `refWidthPx`를 모르는 오프셋은 더 이상 "배율 1로 믿고 적용"하지 않고 **아예 무시(dx=0, 원래 flex 자리로)** 하도록 `HeaderSlot.tsx`/`LeftSidebar.tsx`/`RightSidebar.tsx` 3곳을 동일하게 수정 — 기준 폭을 모르는 값을 추측해서 쓰는 것보다, 항상 화면 안에 보이는 원래 자리로 안전하게 되돌리는 게 낫다는 판단(재드래그하면 그 즉시 `refWidthPx`가 함께 저장돼 정상적으로 스케일링된다). DB 값은 건드리지 않음 — 코드가 이 값을 무시하도록 바꾼 것만으로 즉시 해결됨(자가 치유).
- **원인 2(Craft 에디터 PC 모드 preview가 실제 홈페이지와 다른 레이아웃으로 보임)**: `CraftPageEditor.tsx`(홈페이지 포함 6개 페이지가 공유하는 Craft.js 에디터 셸, `CraftHomeEditor.tsx`가 이걸 그대로 위임)의 Toolbox(`w-60`)/SettingsSidebar(`w-72`)가 캔버스와 **flex로 폭을 나눠 쓰고** 있어, PC 모드 캔버스(`w-full`)가 실제 방문자 화면보다 항상 528px 좁게 렌더링됐다 — 블록 내부 반응형이 뷰포트가 아니라 이 캔버스의 실제 렌더링 폭 기준 컨테이너 쿼리(`@[768px]:` 등, EPIC-100/EPIC-101)라서, 이 528px 차이 때문에 실제 사이트에서는 데스크톱 레이아웃일 폭에서도 에디터 안에서는 모바일 레이아웃으로 잘못 계산될 수 있었다. `/admin/navigation/settings`(헤더 캔버스)의 좌측 패널을 오버레이로 바꿔 동일한 문제를 해결했던 HOTFIX-141.9와 정확히 같은 종류의, 다른 파일에 남아있던 버그.
- **수정 2**: HOTFIX-141.9와 동일한 패턴 재사용 — Toolbox/SettingsSidebar를 flex 형제 대신 캔버스 위에 뜨는 절대 위치 오버레이로 바꿔, 캔버스(및 컨테이너 쿼리)가 항상 이 에디터 셸의 전체 폭을 그대로 쓰게 함(실측: 브라우저 폭 859px 기준 수정 전 캔버스 316px → 수정 후 829px, 같은 폭에서 라이브 페이지의 844px과 거의 일치).
- **모바일 preview는 확인 결과 이미 정상**: 실측(관리자 모바일 탭 캔버스 vs 실제 사이트를 동일하게 390px 폭으로 맞춰 비교)한 `account:admin`/`write-button` 등 헤더 슬롯 좌표가 소수점 이하까지 일치 — `refWidthPx=390`이 이미 올바르게 저장돼 있어 별도 버그를 재현하지 못했다. Craft 에디터 모바일 모드도 원래부터 `w-[390px]` 고정폭이라 이번 flex-squeeze 버그의 영향을 받지 않는다. 사용자가 실제로 다른 기기 폭에서 여전히 어긋남을 본다면 그 폭/기기를 알려주면 재조사 가능.
- **검증**: `npx tsc --noEmit`/`npm run lint` 0 errors(신규 경고 없음). 로컬 dev 서버 + Claude Browser 툴로 (1) 실제 저장된 프로덕션 `header_positions`를 그대로 로드해 수정 전 '관리자'/'스튜디오'가 뷰포트 밖(x<0)으로 나가는 것 재현 → 수정 후 두 요소가 관리자 캔버스와 라이브 사이트 양쪽에서 화면 안에 보이는 것 확인, (2) `/admin/pages/[home-id]`에서 "Craft 에디터 열기" → PC 모드 캔버스 폭이 828~829px(라이브 844px와 거의 일치)로 늘어난 것 확인, 모바일 모드는 여전히 정확히 390px로 중앙 정렬되는 것 확인, (3) 콘솔에 새 에러 없음(기존 React19 `element.ref` 경고만 존재, 무관).
- **변경 파일**: `src/components/HeaderSlot.tsx`, `src/components/LeftSidebar.tsx`, `src/components/RightSidebar.tsx`, `src/components/craft/shared/CraftPageEditor.tsx`.

## 2026-08-25 (HOTFIX-144.8 — "거대한 하늘색 덩어리"의 진짜 근본 원인: 아웃라인이 원점 기준 균일 확대라 가늘고 넓게 뻗은 형태에서 파탄 + 스케일 정규화도 장식 메쉬 제외하도록 확장)
- **사용자 재신고**: "여전히 여러가지 오브제들이 클릭하면 거대한 하늘색 덩어리가 보여, 몇몇 오브제는 (스케일) 이 안맞아서 최대로 크게 해도 아주 작아" — HOTFIX-144.7(받침대 메쉬 제외)로는 안 끝났다는 신고, "오브젝트 3"(My Page 행성)로 재현.
- **실측**: 라이브 config에 걸린 .glb 오브젝트 9개 전부를 scratchpad 스크립트로 직접 다운로드+파싱(GLB JSON chunk의 accessor min/max + 삼각형 수)해 비교. "오브젝트 3"은 실제로는 Sketchfab CC0 식물 스캔("Marsh Labrador Tea")이고, 라벨 없는 노드 "Cube_2"(삼각형 12개, 사실상 빈 자리표시자)가 섞여 있었다 — HOTFIX-144.7의 "얇고 넓은 받침대" 판정에는 안 걸리는 전혀 다른 종류의 찌꺼기.
- **진짜 근본 원인 (별개 버그)**: `ModelSelectionOutline`이 복제본 전체를 그 오브젝트의 피벗(원점) 기준으로 균일하게 살짝 확대(`scale={1+thickness}`)해 셸을 만들었다 — 원점에서 가까운 덩어리진 오브젝트(로봇/카메라 등)에서는 안전하지만, 원점에서 멀리 뻗은 부분(키 큰 식물의 가지 끝 등)은 그 거리에 비례해 훨씬 크게 밀려난다. 가늘고 넓게 뻗은 형태(식물의 가지들)에서 가지 끝마다 거대하게 부풀어 올라 "하늘색 폭발"처럼 보였다 — 이게 실제로 사용자가 본 "거대한 덩어리"의 주범이었다(받침대 메쉬 문제와는 완전히 별개).
- **수정 1 (아웃라인 기법 교체)**: 정점을 자기 법선(normal) 방향으로 "고정된 작은 양"만큼만 밀어내는 정점 셰이더(`THREE.ShaderMaterial`, 표준 toon 아웃라인 기법)로 교체 — 정점의 원점 거리와 무관하게 항상 일정하게 얇은 셸이 된다. 두께는 각 메쉬 자신의 로컬 바운딩 박스 기준으로 정해 가는 가지는 얇게, 굵은 몸통은 그에 맞게 나온다.
- **수정 2 (찌꺼기 메쉬 판정 확장 + 스케일 정규화에도 동일 적용)**: `isDecorativeMesh` 판정에 "삼각형이 30개 미만이면서 전체 모델의 2% 미만"인 사실상 빈 자리표시자 규칙을 추가(기존 "얇고 넓은 받침대" 규칙과 OR). 이 판정을 `ModelSelectionOutline`뿐 아니라 스케일 정규화(바운딩 박스 계산, `UniverseObjectModel`/`SpaceObjectModel`)에도 동일하게 적용하는 공용 함수(`significantBoundingBox`)로 통합 — 찌꺼기 메쉬가 실제 형상보다 큰 축을 가진 경우 "스케일을 최대로 올려도 여전히 작게" 보이던 불일치를 근본적으로 막는다.
- **검증**: `npx tsc --noEmit`/`npm run lint` 0 errors(신규 경고 없음). 로컬 dev 서버 + Claude Browser 툴로 문제가 재현됐던 "오브젝트 3"(식물)를 다시 선택 — 수정 전엔 화면을 뒤덮는 하늘색 폭발이 보였고, 수정 후엔 사라지고 식물 형태에 맞는 얇은 아웃라인만 남는 것을 직접 확인. "laputa garden robot"도 여전히 정상.
- **변경 파일**: `src/components/about-silo/AboutSiloUniverse.tsx`.

## 2026-08-25 (HOTFIX-144.7 — 오브젝트 선택 시 뜨던 "거대한 하늘색 덩어리" 제거 + Universe Settings 우주 오브젝트/별똥별 업로드도 R2로 이관)
- **사용자 신고 1**: "오브제를 클릭하면 하늘색으로 뭔가가 너무 거대하게 오브제 모양으로 보여 그거 없애줘. 예를들어 'laputa garden robot' 오브제를 확인해봐".
- **원인 1**: `ModelSelectionOutline`이 선택된 오브젝트의 모든 메쉬를 예외 없이 불투명한 하늘색 BackSide 셸로 덮어씌운다 — 다운로드한 .glb 에셋에 흔히 딸려오는 장식용 받침대/바닥판 메쉬는 평소엔 자기 텍스처로 배경에 자연스럽게 묻혀 안 보이지만, 이 셸 처리로 그대로 드러나며 "오브젝트 모양의 거대한 하늘색 판"처럼 보였다. 실제로 `laputa garden robot`의 .glb를 직접 파싱해 확인(scratchpad 스크립트로 GLB JSON chunk의 accessor min/max 분석) — 원형 받침 메쉬가 한 축으로는 두께 0.34, 나머지 두 축은 5.03×5.03(모델 전체 발자국의 30%)인 얇고 넓은 판이었다.
- **수정 1**: `ModelSelectionOutline`이 아웃라인 대상 메쉬를 하나씩 훑을 때, 로컬 바운딩 박스의 가장 얇은 축이 가장 큰 축의 15% 미만이면서 나머지 두 축의 넓이가 모델 전체 발자국의 20%를 넘는 메쉬는 "받침대"로 보고 `visible = false`로 숨긴다 — 실제 오브젝트 형상(로봇 본체 등)은 이 정도로 납작하지 않아 걸리지 않는다.
- **사용자 신고 2**: "universe setting 에서 아직도 업로드가 용량 제한이 있어, 특히 '먼우주 배경' 으로 glb 파일을 올리려 했더니 실패했어".
- **원인 2**: HOTFIX-144.5가 Supabase Storage(50MB 상한) → R2(100MB 상한) 이관을 `PlanetSettingsPanel.tsx`/`ObjectInspectorPanel.tsx` 두 곳만 하고 `UniverseSettingsPanel.tsx`(우주 공간 오브젝트 + 별똥별 업로드, "먼 우주 배경"도 이 경로)를 빠뜨렸다 — 여전히 `src/lib/storage.ts`의 Supabase Storage를 쓰고 있었다.
- **수정 2**: `UniverseSettingsPanel.tsx`의 `handleSpaceUpload`/`handleShootingStarUpload`도 동일하게 `uploadRawFileToR2`(R2 direct-upload, 100MB 상한)로 이관.
- **검증**: `npx tsc --noEmit`/`npm run lint` 0 errors(신규 경고 없음). 로컬 dev 서버 + Claude Browser 툴로 실제 `laputa garden robot`을 선택해 더 이상 거대한 하늘색 판이 뜨지 않는 것(수정 전/후 스크린샷 비교) 직접 확인.
- **변경 파일**: `src/components/about-silo/AboutSiloUniverse.tsx`(`ModelSelectionOutline`), `src/components/about-silo/UniverseSettingsPanel.tsx`.

## 2026-08-25 (HOTFIX-144.6 — Silo Planet 오브젝트를 드래그로 회전할 수 있게)
- **사용자 지시**: "오브제들을 회전할수 있게 해줘. 드래그 드롭으로".
- **배경**: 기존엔 선택된 오브젝트에 이동(TransformControls "translate") 기즈모만 붙어 있었다 — 회전은 아예 저장할 필드도, 조작할 UI도 없었다. 행성 표면 오브젝트는 매 렌더 `setFromUnitVectors((0,1,0), 표면법선)`으로 orientation이 강제로 재계산되는 구조(중력 정렬)라, 단순히 회전 기즈모만 추가하면 그 정렬 로직이 매 'change' 이벤트마다 회전 조작을 즉시 덮어써 아무 효과가 없었을 것.
- **구현**: `UniverseObject`에 `yaw`(라디안, 기본 0) 필드 신설 — 오브젝트가 서 있는 자리의 로컬 "위" 축(표면 오브젝트는 법선, 우주 오브젝트는 identity) 기준 추가 회전. `ObjectInspectorPanel`에 "↔ 이동 / ⟳ 회전" 토글 추가(새 오브젝트 선택 시 항상 "이동"으로 리셋). `AboutSiloUniverse.tsx`의 `TransformControls`가 이 모드에 따라 `mode`를 바꾸고, 회전 모드에서는 `space="local"` + `showX/showZ=false`로 로컬 Y축 하나로만 제한(표면에서 기울어지거나 파묻히는 것 방지) — 이동 모드의 표면 재정렬 `onChange`는 회전 모드일 때 완전히 꺼둔다(안 그러면 위 배경 문단의 문제 그대로 재발). 드래그가 끝나면(`onMouseUp`) 기준 orientation(법선 정렬 또는 identity)의 역행렬을 현재 quaternion에 곱해 순수 Y축 회전만 뽑아내(로컬+단일축 제한 덕분에 축이 안 섞여 손실 없이 `Euler.y`로 복원 가능) `yaw`로 저장. 렌더링 쪽(`UniverseObjectModel`은 quaternion 곱, 우주 `.glb`(`SpaceObjectModel`)는 `rotation` prop)이 이 값을 반영.
- **스코프 제한**: 우주 공간의 이미지 빌보드(`kind: "sprite"`, 별/은하수 등)는 Three.js Sprite가 항상 카메라를 향해 스스로 방향을 재계산해(billboarding) 부모 그룹을 돌려도 화면상 아무 효과가 없다(EPIC-144 주석에 이미 기록된 기존 한계) — 그런 오브젝트를 선택하면 회전 토글 자체를 숨긴다. 행성 표면 장식 오브젝트와 우주 공간 `.glb` 모델(`kind: "model"`)은 전부 회전 가능.
- **검증**: `npx tsc --noEmit`/`npm run lint` 0 errors(신규 경고 없음). 로컬 dev 서버 + Claude Browser 툴로 실제 저장된 프로덕션 오브젝트("laputa garden robot")를 선택해 "⟳ 회전" 토글 → 안내 라벨이 "고리를 드래그해서 회전"으로 바뀌는 것 확인 → 실제 드래그로 회전 → 다른 오브젝트 선택 후 재선택해도 회전된 orientation이 그대로 유지되는 것(= 상태에 실제로 저장됨, 드래그 중 시각 효과가 아님) 확인. "이동" 모드로 되돌린 뒤에도 위치 이동 기즈모가 정상적으로 재부착되는 것 확인.
- **변경 파일**: `src/lib/aboutSiloUniverseConfig.ts`, `src/components/about-silo/AboutSiloUniverse.tsx`, `src/components/about-silo/ObjectInspectorPanel.tsx`, `src/components/about-silo/PlanetSettingsPanel.tsx`(신규 오브젝트 생성 시 `yaw: 0` 기본값), `src/components/about-silo/UniverseSettingsPanel.tsx`(신규 우주 오브젝트 동일).

## 2026-08-24 (HOTFIX-144.5 — Silo Planet 오브젝트/텍스처 업로드 상한을 Supabase Storage 50MB → R2 100MB로)
- **사용자 지시**: "silo planet 에 오브제 업로드 제한을 100mb 로 올려줘. 필요하면 R2 로 저장공간을 옮겨줘".
- **원인**: `PlanetSettingsPanel.tsx`(행성 텍스처/위성 디자인/캐릭터 .glb/장식 오브젝트)와 `ObjectInspectorPanel.tsx`(오브젝트 썸네일)가 전부 `src/lib/storage.ts`의 Supabase Storage 업로드를 쓰고 있었는데, Supabase 무료 플랜은 파일당 50MB가 고정 상한(유료 전환 전까지 API로도 못 올림 — 기존 HOTFIX 주석에 이미 기록돼 있던 제약).
- **수정**: 새로 만들지 않고 EPIC-082/083에서 이미 만들어둔 Cloudflare R2 direct-upload 파이프라인(presigned PUT, `src/app/api/media/presigned/route.ts`)으로 이관 — 이 라우트는 이미 `MAX_FILE_SIZE_BYTES = 100MB`로 설정돼 있어 별도 상한 조정 없이 이관만으로 100MB 상한이 적용된다. `src/lib/r2Upload.ts`에 media_library insert 없이 URL만 돌려주는 공용 헬퍼 `uploadRawFileToR2`를 추가(기존 `uploadFontToR2`와 로직 공유, `presignedPutToR2`로 추출). presigned route의 `isAllowedUpload`에 `.glb`/`.gltf` 확장자 허용 추가(브라우저가 이 확장자의 `File.type`을 못 채우는 경우가 흔해 폰트와 동일한 패턴으로 처리) — 안 하면 캐릭터 모델/장식 오브젝트 업로드가 "지원하지 않는 파일 형식"으로 막힘.
- **번호 정정**: 최초 커밋에는 `HOTFIX-141.5`(작업 브랜치 번호를 그대로 씀) → `develop`과 무관한 EPIC이라 `HOTFIX-144.1`로 정정 → `git fetch` 결과 `develop`이 다른 기기에서 이미 `HOTFIX-144.1`~`.4`(아래 항목들)를 쓴 뒤였음이 드러나 최종적으로 `HOTFIX-144.5`로 재정정.
- **검증**: `npx tsc --noEmit`/`npm run lint` 0 errors(신규 경고 없음). 로컬 dev 서버에서 `/silo-planet` 렌더링 확인(콘솔에 기존에 없던 에러 없음). 실제 100MB 근접 파일 업로드는 로그인 세션이 필요해 이번 세션에서 직접 재현하지 않음 — 다음에 관리자 계정으로 실제 대용량 .glb 업로드 확인 필요.
- **변경 파일**: `src/lib/r2Upload.ts`, `src/app/api/media/presigned/route.ts`, `src/components/about-silo/PlanetSettingsPanel.tsx`, `src/components/about-silo/ObjectInspectorPanel.tsx`.

## 2026-08-24 (HOTFIX-144.4 — 메인 로고에 마우스를 올렸을 때 바뀌는 hover 이미지 업로드 추가)
- **요청**: "'홈페이지 설정' 에서 '메인로고' 에 hover 하면 나타날 이미지도 업로드 할수 있게 해줘."
- **구현**: LeftSidebar/RightSidebar 여닫이 아이콘의 기본/호버 미디어 크로스페이드(EPIC-078, `SidebarTriggerMedia.tsx`)와 동일한 패턴을 로고에도 적용. `mainLogoSettings.ts`에 `hoverImageUrl` 필드 추가(비어있으면 기존과 완전히 동일하게 단일 이미지 고정, 하위 호환). `Navbar.tsx`에 `renderMainLogoImage()` 헬퍼를 새로 만들어 로고 렌더링 2곳(그룹화 배치/개별 배치)의 중복 마크업을 하나로 합치면서, `hoverImageUrl`이 있을 때만 두 이미지를 겹쳐 opacity로 크로스페이드하도록 분기(로고를 감싸는 `<Link>`에 `group` 클래스 추가). 관리자 화면(`/admin/navigation/settings`)의 "로고" Controls에 "마우스를 올렸을 때 나타날 이미지" 업로드 필드(+미리보기+제거 버튼)를 기존 로고 이미지 업로드 바로 아래에 추가 — `main_logo_hover` 폴더에 저장, `handleLogoFile`과 동일한 패턴.
- **검증**: `npx tsc --noEmit`/`npx eslint` 0 errors(기존 경고만 그대로). 로컬 dev 서버에서 `?__adminPreview=1` 쿼리(관리자 미리보기 iframe이 실시간 편집 값을 postMessage로 주입하는 기존 채널, HOTFIX 이전부터 있던 기능)로 `hoverImageUrl`이 채워진 로고 설정을 주입해 DOM을 직접 확인 — 두 겹 `<img>`가 `opacity-100 group-hover:opacity-0` / `opacity-0 group-hover:opacity-100`으로 정확히 렌더링됨, 기존 데이터(hoverImageUrl 없음)로는 이전과 동일한 단일 `<img>` 그대로 유지됨(회귀 없음) 확인. 실제 마우스 hover로 opacity가 실시간 전환되는 것은 이 저장소가 이미 프로덕션에서 검증된(EPIC-078) 동일한 Tailwind `group`/`group-hover` 메커니즘이라 별도 스크린샷 없이도 신뢰 가능하다고 판단.
- **변경 파일**: `src/lib/mainLogoSettings.ts`, `src/components/Navbar.tsx`, `src/app/admin/navigation/settings/page.tsx`.

## 2026-08-24 (HOTFIX-144.3 — 상단 탭 1차/2차 드롭다운의 상하 위치 설정 추가 + Controls 패널의 hover 모션 select 전부에 실제 CSS로 즉시 확인 가능한 미리보기 추가)
- **요청 1**: "'홈페이지 설정' 에 상단탭 의 control 설정에서, 1차 & 2차 드롭다운 카테고리의 상/하 위치를 설정할수 있게 해줘 모든 요소들에게." — 기존에 좌우 위치(`dropdownOffsetXPx`/`subDropdownOffsetXPx`, HOTFIX-141.16)는 있었지만 상하(Y) 이동은 없었다. `topTabStyleSettings.ts`에 `dropdownOffsetYPx`/`subDropdownOffsetYPx`를 X와 동일한 패턴으로 추가하고, `Navbar.tsx`가 드롭다운 위치 style을 만드는 3곳(1차 컨테이너 + 2차 flyout 2곳 — group 분기/item.children 분기)이 전부 `dropdownOffsetTransform(x, y)` 헬퍼로 X/Y를 함께 `translate()`에 반영하도록 통일. 탭마다 독립 설정이라 "모든 요소들에게" 요청대로 하위 탭 전부에 개별 적용 가능(PC/모바일도 기존처럼 독립).
- **요청 2**: "'홈페이지 설정' 의 상단탭 의 control 설정에서 hover 모션 의 preview 를 볼수 있게 해줘, 어떤 모션이 어떻게 나올지 모르니까 불편해." — hover 모션 select가 관리자 화면 안에 총 5곳(상단 탭 드롭다운 항목/계정 메뉴/상단 사이드바 항목/상단 사이드바 링크/로그인 버튼) 있었는데 전부 라벨 텍스트만 나열해 실제로 어떻게 보이는지 저장 후 실제 사이트에서 직접 hover해봐야 알 수 있었다. `HoverMotionSelect`(select + 미리보기 스와치) 컴포넌트를 새로 만들어 5곳 전부 교체 — 미리보기는 Navbar.tsx가 실제 탭에 쓰는 것과 완전히 동일한 `tabHoverMotionCss()` 함수로 CSS를 만들어 그 자리에서 바로 마우스를 올려 확인할 수 있다(실제 화면과 어긋날 여지 없음). 겸사겸사 "호버 모션 테마"(전체 일괄 적용) 버튼 목록에도 같은 미리보기를 붙여 12종 중 뭘 고를지 미리 보고 선택할 수 있게 했다.
- **검증**: `npx tsc --noEmit`/`npx eslint` 0 errors(기존 경고만 그대로). 관리자 로그인 세션이 필요한 화면이라 브라우저로 직접 재현/확인은 못 함 — `tabHoverMotionCss`/`dropdownOffsetTransform` 둘 다 이미 실사용 중인 함수를 그대로 재사용하는 방식이라 회귀 위험은 낮다고 판단.
- **변경 파일**: `src/lib/topTabStyleSettings.ts`, `src/components/Navbar.tsx`, `src/app/admin/navigation/settings/page.tsx`.

## 2026-08-24 (HOTFIX-144.2 — "About Silo"를 왼쪽 사이드바에, "온라인 도슨트"를 오른쪽 사이드바에 노출 체크했는데 실제 화면엔 반영 안 되던 문제)
- **신고**: "'About Silo' 카테고리를 '왼쪽사이드바' 에 노출시키고, '온라인 도슨트' 카테고리를 '오른쪽 사이드바' 에 노출시키는 체크를 눌렀는데, 실제 왼쪽 오른쪽 사이드바에 적용이 안되고 있어."
- **원인**: EPIC-138이 최상위 카테고리마다 노출 위치(`site_navigations.target_types`)를 복수 선택 가능하게 만들었지만, `Navbar.tsx`는 여전히 `navTabs.find(t => t.type === "sidebar-left"/"sidebar-right")`로 배열에서 딱 하나만 골라 썼다. DB 확인 결과 "About Silo"(sort_order 0)가 이미 `sidebar_left`였던 "사일로 상점"(sort_order 1)보다 먼저 오고, "온라인 도슨트"(sort_order 2)도 이미 `sidebar_right`였던 "살롱데상"(sort_order 3)보다 먼저 와서, `find()`가 새로 체크한 카테고리를 조용히 집어삼키고 기존 카테고리 쪽은 완전히 무시했다(체크박스 저장 자체는 정상 동작 — 시각적 반영만 실패). 두 카테고리가 정확히 같은 슬롯을 두고 "승자 독식"으로 경합하는 구조라 어느 쪽을 체크해도 결과가 하나로만 좁혀졌다.
- **수정**: `navConfig.ts`에 `mergeSidebarTabs()`를 추가 — 같은 슬롯(sidebar-left/right)을 공유하는 탭이 여럿이면 `find()` 대신 `filter()`로 전부 모아 `groups`를 합친다. 패널 헤더(라벨/링크)는 `target_types`에 `dropdown`이 없는(=이 사이드바 슬롯 전용으로 만들어진) 탭을 "주인"으로 우선한다 — "사일로 상점"/"살롱데상"처럼 다른 곳에 동시 노출되지 않는 카테고리를 헤더로 유지하고, "About Silo"/"온라인 도슨트"처럼 상단 드롭다운과 겸용인 카테고리는 "손님"으로 그룹만 추가하는 방식. `Navbar.tsx`의 `leftSidebarTab`/`rightSidebarTab` 계산이 이 함수를 쓰도록 교체.
- **검증**: `npx tsc --noEmit`/`npx eslint` 0 errors(기존 경고만 그대로). 로컬 dev 서버 + Claude Browser 툴로 `/shop` 페이지에서 실제 왼쪽 사이드바를 열어 "사일로 상점" 기존 그룹(사일로 보물들/사일로의 뮤즈 등) + "About Silo" 그룹(Silo Timeline/Daily Silo 등) 12개가 한 패널에 전부 뜨는 것, 오른쪽 사이드바에서도 "살롱데상" 기존 그룹(커뮤니티/멤버십 등) + "온라인 도슨트" 그룹(고대~왕정 등 4개 시대구간) 11개가 전부 뜨는 것을 프로그래밍적으로(DOM 조회) 확인. 패널 헤더 라벨도 여전히 "사일로 상점"/"살롱데상"으로 유지됨을 확인(체감상 회귀 없음).
- **변경 파일**: `src/lib/navConfig.ts`, `src/components/Navbar.tsx`.

## 2026-08-24 (HOTFIX-144.1 — 게시판 설정의 "슬러그" 필드가 비어있는 기존 게시판마다 저장할 때 필수 입력 경고가 계속 뜨던 문제)
- **신고**: "게시판의 슬러그 부분이 게시판 설정을 수정할때마다 입력하라는 알림이 뜨는데, 그렇지 않도록 설정해줘."
- **원인**: `BoardForm.tsx`의 "슬러그" 입력란은 실제로는 URL 라우팅에 쓰이는 `boards.slug`(NOT NULL, 이 폼에서 편집 대상 아님)가 아니라 `boards.category`(nullable, `INDIVIDUAL_BOARD_DEFINITIONS` 조회 키 — 미지정 시 `BOARD_DEFINITIONS.topic`으로 자동 폴백, `src/lib/boardLayout.ts`)에 연결돼 있는데 `required` 속성이 붙어 있었다. `category`가 원래 null이어도 정상 동작하는 선택 필드인데도, DB에서 이미 11개 게시판(사일로 타임라인 포함)이 `category = null` 상태라 그 게시판 설정을 열 때마다 필드가 비어있는 채로 렌더링되고, 어떤 값을 바꿔 저장하든 브라우저 기본 "이 입력란을 작성하세요" 검증이 매번 제출을 막았다.
- **수정**: `values.category` input의 `required` 속성 제거. `category`가 비어있어도 그대로 저장 가능(기존 폴백 동작 그대로 유지, DB 컬럼도 nullable이라 스키마와 일치).
- **검증**: `npx tsc --noEmit` 0 errors, `npx eslint src/components/admin/BoardForm.tsx` 0 errors(기존 경고 2개는 이번 변경과 무관, 그대로). 관리자 로그인 세션이 필요한 화면이라 브라우저로 직접 재현/확인은 못 함 — DB에서 `category IS NULL`인 게시판 11개를 조회해 증상 조건(필드가 비어있는 채로 로드됨)을 직접 확인.
- **변경 파일**: `src/components/admin/BoardForm.tsx`.

## 2026-08-24 (EPIC-144 — Silo Planet 우주 오브젝트 모션 10종 추가 + 진짜같은 별똥별(orbit+tail) + 먼 우주 배경 배치 + 줌 감도/행성 크기 설정 + 설정창 바깥 클릭 시 닫기)
- **배경**: 사용자가 한 번에 여러 요청 — (1) "모션효과가 위아래 부유 말고 작동이 안돼, 10가지 모션이 더 있으면좋겠어", (2) "진짜같은 별똥별 효과(내가 설정한 오브제가 orbit 하고 tail 이 있음 반짝이는 tail)", (3) "먼곳에 은하수, 블랙홀, nebula... 왜 전부터 요청했는데 안만들어? ... glb 파일 넣을수 있게 우주의 먼곳에 보일수 있게 하고 크기 설정할수 있게해", (4) "줌인 줌아웃이 너무 조금씩 되니까 답답해 그거도 설정할수 있게", (5) "각 행성의 크기도 설정할수 있게해줘", (6) "어떤 설정창이든 그 설정창 외부를 누르면 설정창이 해제될수 있게".
- **(1) 모션 버그 원인 + 10종 추가**: "제자리 회전"(spin)이 이미지 빌보드(별/은하수 등 SpaceObjectSprite)에서 시각적으로 아예 안 보이던 진짜 원인은 Three.js의 Sprite가 항상 카메라를 향해 스스로 방향을 재계산해서(billboarding) 부모 group의 rotation을 완전히 무시하기 때문이었다 — `useObjectMotion`(AboutSiloUniverse.tsx)의 회전 계열 모션이 이제 스프라이트에는 group 회전 대신 `SpriteMaterial.rotation`(billboarding과 무관하게 실제로 보임)을 대신 돌리도록 수정. 기존 4개(없음/반짝임/부유/회전)에 sway/pendulum/tumble/drift/orbitSelf/figure8/flicker/shimmer/fadeInOut/nod 10개 추가(`ObjectMotion` 타입, `ObjectInspectorPanel.tsx`의 `MOTION_OPTIONS`) — 전부 기존 규칙 그대로 position/rotation/opacity만 쓰고 scale은 안 건드린다(카메라 줌 거리가 바운딩 반지름에 비례해 계산되므로).
- **(2) 별똥별 orbit+tail**: 궤적을 직선에서 2차 베지어 곡선(완만한 호)으로 바꿔 "orbit"하듯 곡선을 그리며 지나가게 했고, 그 궤적을 따라 잔상 점 여러 개(꼬리 길이 설정 가능, 기본 10개)를 흩뿌려 반짝이는 꼬리를 만들었다. `shootingStars.objectUrl`/`objectKind`를 설정하면 기본 막대 대신 업로드한 .glb 모델이나 이미지가 별똥별 머리가 된다(비우면 기존 막대 그대로 하위 호환).
- **(3) 먼 우주 배경(은하수/블랙홀/네뷸러)**: `SpaceObject.placement`("between" 기본 / "distant" 신규) — 이미 있던 GLB/이미지 업로드 + 개수·크기 설정 기능은 그대로 두고, "먼 우주 배경"을 고르면 훨씬 먼 반지름(40~85, 기존 7~16)에 배치돼 깊은 우주 배경처럼 보인다. 크기(스케일) 슬라이더 최대값도 2→12로 넓혀 멀리서도 화면을 채울 만큼 키울 수 있게 했다 — "왜 안 만드냐"는 지적에 대한 답: 이 업로드+배치 인프라 자체는 EPIC-143 이전부터 있었고 이번엔 "먼 배경"이라는 배치 모드가 빠져 있던 것을 채운 것.
- **(4) 줌 감도**: `CameraControls`의 `dollySpeed`(예전엔 0.55 고정)를 `UniverseConfig.zoomSpeed`로 노출, `UniverseSettingsPanel`에 슬라이더 추가.
- **(5) 행성 크기**: `PlanetConfig.sizeScale`(기본 1) — 행성 반지름뿐 아니라 표면 오브젝트 배치 반지름/캐릭터 위치/이름 라벨 위치/위성(게시글 마커) 궤도 반지름/카메라 포커스 거리까지 전부 이 배율에 비례해 함께 커지거나 작아지도록 `AboutSiloUniverse.tsx`의 관련 계산을 전부 모듈 상수에서 로컬 계산으로 바꿨다(안 그러면 행성만 커지고 오브젝트/위성이 파묻히거나 등등 뜸). `PlanetSettingsPanel`에 슬라이더 추가.
- **(6) 설정창 바깥 클릭 시 닫기**: `useDraggablePanel` 훅에 `onClickOutside`/`enabled` 옵션 + `panelRef`를 추가해 세 설정 패널(UniverseSettingsPanel/PlanetSettingsPanel/ObjectInspectorPanel)에 공통 적용. **중요한 예외 처리**: 3D 뷰(react-three-fiber `<canvas>` 하나)에 대한 클릭은 "바깥 클릭"에서 제외했다 — 행성/오브젝트 선택, TransformControls 드래그 기즈모, 카메라 궤도 회전이 전부 그 캔버스 위에서 일어나는데, 이걸 예외 처리하지 않으면 오브젝트를 클릭해서 고르거나 드래그하려는 순간마다 패널이 곧바로 닫혀버려 사실상 못 쓰게 된다.
- **검증**: `npx tsc --noEmit`/`npm run lint` 0 errors(기존 77개 경고 그대로, 신규 경고 없음). 로컬 dev 서버 + Claude Browser 툴로 실제 저장된 프로덕션 설정(우주 오브젝트 7개 등 실데이터)을 로드해 크래시 없이 렌더링 확인, 14개 모션 전부 순회하며 콘솔 에러 없음 확인, 바깥 클릭 시 패널이 실제로 닫히고 캔버스 클릭 시에는 안 닫히는 것 프로그래밍적으로(synthetic MouseEvent) 검증.
- **참고**: `AboutMeSatellites`(User Page 하단의 "About Me" 링크 고리)는 행성 크기 배율과 별개로 그대로 뒀다 — 범위 밖 요청이라 별도 확인 없이 건드리지 않음.
- **변경 파일**: `src/lib/aboutSiloUniverseConfig.ts`, `src/components/about-silo/AboutSiloUniverse.tsx`, `src/components/about-silo/UniverseSettingsPanel.tsx`, `src/components/about-silo/PlanetSettingsPanel.tsx`, `src/components/about-silo/ObjectInspectorPanel.tsx`, `src/components/about-silo/useDraggablePanel.ts`.

## 2026-08-24 (HOTFIX-143.4 — 백필 후에도 남아있던 깨진 썸네일 4개(About Silo) + 사이트 전체 76개: 캐러셀 첫 항목이 영상이면 .mp4가 대표 이미지로 저장되던 버그)
- **신고**: "복구했는데 about silo 페이지에 4개가 아직도 안 됨, 직접 대표 이미지 재설정해도 안 됨" → 이어서 "silo daily에도 상당수 안 됨".
- **원인**: HOTFIX-143.3에서 `instagram_feeds`에 `thumbnail_url`이 없는 캐러셀 게시물의 경우 `media_urls[0]`을 무조건 썸네일로 썼는데, **캐러셀의 첫 항목이 영상인 경우**(Graph API 원본 순서 그대로 저장) `media_urls[0]`이 `.mp4` 파일이라 `<img src>`로는 렌더링될 수 없어 깨져 보였다 — DB 값 자체는 R2 URL이라 "정상"처럼 보이지만 실제로는 이미지가 아닌 영상 파일을 가리킴. About Silo 4개 + `silo-daily`(17개) 포함 전체 사이트 약 76개 게시글이 이 패턴이었다. 사용자가 직접 "대표 이미지" 재지정을 시도해도 똑같은(고쳐지지 않은) 로직을 타므로 계속 같은 결과였음.
- **수정 1**: `embedThumbnail.ts` — `media_item_types`와 `media_urls`를 짝지어 **실제 타입이 IMAGE인 첫 항목**만 대표 이미지 후보로 쓴다. 캐러셀 전체가 영상뿐이면(이미지가 하나도 없으면) 억지로 아무거나 쓰지 않고 `null`(대표 이미지 없음 — 깨진 표시보다 낫다는 기존 원칙 유지).
- **수정 2**: `backfill-thumbnails/route.ts`의 "깨짐" 판정 패턴에 `.mp4`를 추가(기존엔 `cdninstagram.com`/`fbcdn.net` 직링크만 찾았음 — 이미 R2로 재호스팅됐지만 잘못된 파일(영상)을 가리키는 케이스는 이 패턴에 안 걸려 재백필 대상에서 빠졌었다).
- **검증**: `npx tsc --noEmit`/`npm run lint` 0 errors. DB로 실제 깨진 4개(About Silo)의 `instagram_feeds` 원본 데이터를 대조해 캐러셀 순서상 영상이 먼저 오는 게 원인임을 직접 확인.
- **사용자 액션 필요**: HOTFIX-143.3 때와 동일하게 `/admin/instagram`에서 "깨진 썸네일 일괄 복구" 버튼을 다시 클릭해야 함(DB 트리거가 실제 로그인 세션만 허용해 스크립트로 대신 못 함) — 이번엔 `.mp4` 패턴도 걸리므로 About Silo 4개 + Silo Daily 17개 등 전체 ~76개가 이번엔 잡힐 것으로 예상.
- **변경 파일**: `src/lib/embedThumbnail.ts`, `src/app/api/admin/instagram/backfill-thumbnails/route.ts`.

## 2026-08-24 (HOTFIX-143.3 — Instagram이 og:image 자체를 더 이상 안 줘서 폐기 직전이던 스크래핑 폴백을 instagram_feeds 캐시로 대체 + 자사 계정 히스토리 전체 동기화)
- **배경**: HOTFIX-143.2로 "로딩 멈춤"은 해결됐지만, 여전히 실제 미디어 대신 "Instagram에서 게시물 보기 ↗" 폴백 링크만 뜬다는 후속 확인("still not working") — `/api/instagram-post`가 이제 401 없이 정상 호출은 되지만 404("미디어를 찾지 못했어요")를 돌려주고 있었다.
- **원인 재확인**: Node로 직접 `fetch()`해본 결과, Instagram이 페이지 렌더링 방식을 Comet 프레임워크로 완전히 바꿔 **서버가 받는 최초 HTML에 `og:image`/`og:video` 메타 태그 자체가 아예 없다**(UA를 바꿔도, `/embed/captioned/` 경로를 써도 동일 — 실측 확인). `src/lib/instagramScraper.ts`(EPIC-133)와 `src/lib/embedThumbnail.ts`(썸네일)가 둘 다 이 메타 태그에 의존하고 있어 사실상 항상 실패하는 상태였다 — 코드 버그가 아니라 Instagram 쪽 플랫폼 변경.
- **핵심 재발견**: 문제의 테스트 게시물이 실제로 `_silo_store` 소유 계정의 게시물임을 Graph API로 직접 확인(`_silo_store` 미디어 목록 15번째 페이지에서 발견) — 즉 "임의의 다른 계정 게시물이라 공식 API로 못 가져온다"는 제약이 아니라, **EPIC-143 Graph API 동기화가 아직 최근 8개만 돌았을 뿐**이었다.
- **수정 1 — 전체 히스토리 동기화**: `/admin/instagram`의 "동기화" 버튼(브라우저 세션+반복 클릭 필요)을 기다리는 대신, 이미 가진 `IG_ACCESS_TOKEN`/R2 자격증명/`SUPABASE_MANAGEMENT_API_TOKEN`으로 서버사이드 스크립트를 직접 실행해 `_silo_store` 계정의 미디어 전체를 한 번에 동기화 — **381개 신규 동기화, 기존 8개 포함 총 389개**가 `instagram_feeds`/R2에 저장됨(에러 1건). 이제 사일로 스토어 자사 게시물을 참조하는 본문 임베드는 거의 전부 `instagram_feeds` 캐시 적중 → 완전히 네이티브 렌더링(Instagram 요청 0회).
- **수정 2 — `embedThumbnail.ts`**: Instagram 분기가 이제 og:image 스크래핑을 시도하기 **전에** `instagram_feeds`를 shortcode로 먼저 조회(`NativeInstagramEmbed.tsx`와 동일한 매칭 로직) — 찾으면 `thumbnail_url`(주로 VIDEO) 또는 없으면 `media_urls[0]`(IMAGE/CAROUSEL)를 즉시 반환. og:image 스크래핑은 완전히 제거하지 않고 최후 폴백으로 남겨둠(Instagram이 향후 다시 바꿀 가능성 대비, 지금은 거의 항상 실패해도 무해).
- **아직 안 한 것 — 기존 깨진 썸네일 247개 게시글의 일괄 백필**: `/api/admin/instagram/backfill-thumbnails`(HOTFIX-143 이전 후속에서 이미 구현됨)로 나머지 247개를 고치려 시도했으나, Management API(슈퍼유저 연결, `auth.uid()` 컨텍스트 없음)로는 `posts_protect_content_columns()`라는 DB 트리거가 "작성자 또는 관리자만 이 콘텐츠를 수정할 수 있다"며 전부 거부함(247건 전부 동일 에러) — **의도된 보호 장치라 우회하지 않음**(서비스 롤 키를 새로 도입하는 것도 이 프로젝트의 기존 설계 원칙과 어긋나 하지 않음). 실제 로그인된 관리자 세션(브라우저)을 통해서만 통과되므로, **사용자가 `/admin/instagram`에서 "깨진 썸네일 일괄 복구" 버튼을 직접 눌러야** 한다 — 이번 수정(embedThumbnail.ts + instagram_feeds 389개)으로 이제 그 버튼이 실제로 성공할 것으로 예상됨(전엔 스크래핑 자체가 죽어있어 버튼을 눌러도 대부분 실패했을 것).
- **검증**: `npx tsc --noEmit`/`npm run lint` 0 errors. 브라우저로 실제 게시글(`/boards/ethan-s-bluenotes/2e3d9b55`)에서 R2 호스팅 이미지(`pub-*.r2.dev/instagram/...`)가 실제로 뜨는 것 직접 확인(폴백 링크 아님, 로딩 멈춤도 아님).
- **변경 파일**: `src/lib/embedThumbnail.ts`. (DB 변경: `instagram_feeds` +381 rows, 코드 변경 아님이라 별도 마이그레이션 파일 없음.)

## 2026-08-24 (HOTFIX-143.2 — 인스타그램 임베드가 로딩에서 영원히 멈추던 진짜 원인: createRoot()의 Context 단절)
- **배경**: HOTFIX-143.1로 배포한 뒤 사용자가 실제 브라우저에서 재확인 — "로딩에서 멈춘다"고 재신고. Claude Browser 툴로 라이브 디버깅 재개, 로컬 dev 서버에서 `AuthProvider`/`InstagramMediaSlider`에 임시 디버그 로그를 심어 추적.
- **진짜 원인(HOTFIX-143.1의 진단은 틀렸음— 정정)**: `nativeInstagramEmbed.ts`가 `react-dom/client`의 `createRoot()`로 만드는 트리는 앱의 메인 React 트리와 **완전히 분리된 별도 트리**다 — React Context는 트리 경계를 넘어 전달되지 않으므로, 그 안에서 `useAuth()`를 부르는 `NativeInstagramEmbed`/`InstagramMediaSlider`는 실제 `<AuthProvider>`를 조상으로 만난 적이 없고 `createContext()`에 지정된 **기본값**(`session: null`, `loading: true`)만 영원히 본다. 이게 두 가지 다른 증상의 공통 원인이었다: HOTFIX-143.1 이전엔 `loading`을 안 기다려서 매번 세션 없이 요청 → 401(진짜 로그인 여부와 무관하게 항상), HOTFIX-143.1 이후엔 `loading`이 기본값 그대로 `true`라 요청 자체가 영원히 안 나감(로딩 멈춤). 디버그 로그로 실측 확인: 앱 최상단 `AuthProvider`는 `loading: false`로 정상 전환됐는데, 같은 순간 `InstagramMediaSlider`가 받는 `authLoading`은 계속 `true`로 고정.
- **수정**: `nativeInstagramEmbed.ts`의 `createRoot(mount).render(...)`가 `<NativeInstagramEmbed>`를 자체 `<AuthProvider>`로 한 번 더 감싸도록 변경 — 이 분리된 트리 전용으로 세션을 다시 확인한다(같은 supabase 세션을 다시 읽으므로 사용자에게 재로그인 요구 없음; 게시글당 임베드가 여러 개면 그만큼 중복 확인이 일어나지만 보통 많지 않아 감수 가능한 트레이드오프). HOTFIX-143.1의 `authLoading` 가드와 `AuthProvider`의 `getSession()` 타임아웃 폴백(5초)은 그대로 유지 — 이번 수정으로 `authLoading`이 실제로 의미 있는 값이 되면서 그 가드가 비로소 제대로 작동하게 됐다.
- **로컬 검증**: `npm run dev` + Claude Browser 툴로 동일 게시글(`/boards/ethan-s-bluenotes/2e3d9b55`)을 재현 — 수정 전엔 6초+ 대기해도 로딩 스켈레톤에 멈춰 있었고, 수정 후엔 즉시 "Instagram에서 게시물 보기 ↗" 폴백 링크로 확정 상태 전환됨(로그인/스크래핑 성공 여부와 무관하게 "영원히 로딩"은 해소됨).
- **검증**: `npx tsc --noEmit`/`npm run lint` 0 errors(신규 경고 없음).
- **변경 파일**: `src/lib/nativeInstagramEmbed.ts`, `src/components/content/InstagramMediaSlider.tsx`(주석 정리), `src/lib/AuthProvider.tsx`(디버그 로그 제거, 타임아웃 폴백 유지).

## 2026-08-24 (HOTFIX-143.1 — dev.silostore.net 실사용 디버깅 중 발견한 인증 레이스 컨디션)
- **배경**: 사용자가 "dev.silostore.net에서 인스타그램 임베드 치환이 여전히 안 된다"고 신고 — 직접 브라우저(Claude Browser 툴)로 재현 조사. 두 가지가 확인됨: (1) `develop`/`dev.silostore.net`이 아직 이전 임시 디버그 커밋(`0a2387d`, `[nativeIgDebug]` 콘솔 로그가 찍히는 구버전)을 서빙 중이었다 — `git push origin 0580ad0:develop`은 정상적으로 반영됐음을 `git merge-base`로 재확인했으므로 Vercel 빌드/배포가 아직 못 따라온 것으로 판단, 별도 코드 수정 불필요(배포가 따라잡을 때까지 대기). (2) 그 과정에서 진짜 코드 버그를 하나 더 발견 — `InstagramMediaSlider.tsx`(EPIC-133 폴백)가 `/api/instagram-post`를 호출할 때 `useAuth()`의 `session`이 아직 초기값(`null`, 비동기 세션 확인 전)인 상태에서 요청을 보내 로그인한 사용자에게도 401("로그인이 필요해요")이 발생하는 레이스 컨디션을 네트워크 로그로 실측 확인.
- **수정**: `InstagramMediaSlider.tsx`가 `useAuth()`에서 `loading`(세션 확인 자체가 끝났는지, EPIC-079에서 이미 확립된 패턴)도 함께 받아, 세션 확인이 끝나기 전에는 요청을 보내지 않도록(`if (authLoading) return`) 가드 추가 — effect 의존성 배열에도 `authLoading` 반영.
- **별도로 확인(수정 아님, 기존에 알려진 제약)**: 인증 문제를 우회해 수동으로 직접 호출해보니 `scrapeInstagramPost()`(EPIC-133, 비공식 스크래핑)가 실제 게시물에 대해 404("미디어를 찾지 못했어요")를 반환 — Instagram이 스크래핑을 차단했을 가능성이 높고, 이건 EPIC-133 자체가 애초에 "best-effort, 성공 여부 미확인"으로 설계된 제약이지 이번에 새로 생긴 회귀가 아니다. `instagram_feeds`(R2 캐시, `/admin/instagram`으로 동기화)에 아직 없는 게시물은 이 폴백에 의존하는데, 폴백 자체가 이 환경에서 안정적으로 동작하지 않을 수 있음 — 근본적으로는 R2 캐시 커버리지를 늘리는 것만이 신뢰할 수 있는 해결책(단, Graph API는 `_silo_store` 자사 계정 게시물만 조회 가능하다는 한계는 여전함).
- **검증**: `npx tsc --noEmit`/`npm run lint` 0 errors(신규 경고 없음, 기존에도 있던 setState-in-effect 패턴 경고만 유지).
- **변경 파일**: `src/components/content/InstagramMediaSlider.tsx`.

## 2026-08-24 (EPIC-143-후속-2 — 인스타그램 임베드 재도색 버그 + 사이트 전체 게시글 썸네일 근본 수정 + 기존 글 백필)
- **신고 1**: "about silo 페이지인데 썸네일이 안나오고, 게시글을 확인해도 인스타그램 embed의 영상/사진/carousel 안나오고 있어" → 실사용 테스트로 재현: `processNativeInstagramEmbeds()`(EPIC-143-후속)가 최초 1회는 정상적으로 blockquote를 네이티브 UI로 치환하지만, 잠시 뒤 원래 blockquote로 되돌아가고 마운트한 div가 통째로 사라지는 현상을 콘솔 로그로 확인. `dangerouslySetInnerHTML`의 `__html` 문자열 값 자체는 안 바뀌어 React 리렌더 경로로는 감지 안 되는 재도색이라(정확한 트리거는 특정 못함 — RSC 스트리밍/라우터 캐시 관련으로 추정) 근본 차단 대신 `src/lib/nativeInstagramEmbed.ts`를 1회성 스캔에서 **MutationObserver 상시 감시**로 바꿔, 컨테이너가 몇 번을 다시 그려지든 처리 안 된 blockquote가 나타날 때마다 즉시 재처리하도록 했다. `UniversalBlockRenderer.tsx`는 `containerRef`를 새로 붙이고 감시 해제 함수를 effect cleanup에서 호출하도록 갱신.
- **신고 2**: "게시판이 존재하는 모든 페이지의 글들도 다 확인해. 지금 'silo daily' 페이지 확인해보니 여기 thumbnail 다 작동안돼" → DB 확인 결과 전체 285개 게시글 중 248개의 `posts.featured_image_url`이 `scontent-*.cdninstagram.com` 직링크였다(About Silo에 국한된 문제가 아니라 게시판 전체 공통). 원인: Instagram og:image URL은 서명(`oe=` 만료 파라미터)이 걸려 있고 다른 도메인 `<img src>`에서 직접 불러오면 핫링크 보호에도 걸려, 저장 시점엔 보이다가 시간이 지나면 전부 깨진다.
- **근본 수정(앞으로 저장되는 글)**: `src/lib/r2Server.ts`(신규) — "외부 URL 다운로드 → R2 업로드" 공용 헬퍼(`downloadBuffer`/`uploadBufferToR2`/`rehostUrlToR2`)를 뽑아 `embedThumbnail.ts`의 Instagram og:image 처리와 `/api/instagram/fetch/route.ts`(EPIC-143 동기화 파이프라인) 둘 다 이걸 재사용하도록 정리 — 기존엔 후자에만 로컬로 있던 로직. `resolveEmbedThumbnailUrl(provider: "instagram", ...)`이 이제 og:image를 찾자마자 R2에 우리 소유 사본으로 재호스팅하고 그 R2 URL을 돌려준다(R2 업로드 실패 시엔 최후 수단으로 원본 URL 그대로 — 당장은 보이다 나중에 깨지더라도 완전히 없는 것보단 낫다는 기존 동작 유지).
- **백필(이미 깨진 기존 248개 글)**: 저장 로직만 고쳐선 과거 글엔 소급 안 되므로 `POST /api/admin/instagram/backfill-thumbnails`(신규, 관리자 전용) 추가 — `featured_image_url`이 `cdninstagram.com`/`fbcdn.net` 패턴인 게시글을 커서 페이지네이션(10개씩, 게시글당 og:image 스크래핑+R2 업로드로 네트워크 왕복 2회라 타임아웃 방지)으로 찾아 `resolveFallbackEmbedThumbnail(body_json)`으로 다시 계산해 덮어쓴다. `/admin/instagram` 페이지에 "깨진 썸네일 일괄 복구" 버튼(`nextCursor`가 없어질 때까지 자동 반복 호출, 기존 "최근 게시물 동기화" 버튼과 동일한 패턴)으로 노출.
- **RLS 확인**: `posts` 테이블은 `featured_image_url`을 포함한 대부분 컬럼에 `authenticated`용 UPDATE 컬럼 GRANT가 이미 있고, 매칭되는 UPDATE 정책(`posts update like fields`, USING/CHECK 둘 다 `true`)이 행 단위 제한 없이 컬럼 GRANT에만 의존하는 구조라 — 백필 라우트는 admin 전용 별도 RLS 우회 없이 호출자의 `scopedClient`로 그대로 동작한다(관리자 권한 체크는 라우트 자체에서 `is_admin`으로 별도 수행).
- **검증**: `npx tsc --noEmit`/`npm run lint` 0 errors(둘 다 변경 파일 기준 신규 경고 없음).
- **변경 파일**: `src/lib/nativeInstagramEmbed.ts`, `src/components/content/UniversalBlockRenderer.tsx`, `src/lib/r2Server.ts`(신규), `src/lib/embedThumbnail.ts`, `src/app/api/instagram/fetch/route.ts`, `src/app/api/admin/instagram/backfill-thumbnails/route.ts`(신규), `src/app/admin/instagram/page.tsx`.

## 2026-08-23 (EPIC-143-후속 — 게시글 본문 내 인스타그램 임베드를 사일로 네이티브 UI로 실시간 치환)
- **정정 요청**: 방금 만든 EPIC-143(신규 피드 그리드)이 요구사항을 오해했다는 지적 — "인스타그램 피드를 새 게시글로 만드는 게 아니라, 기존/향후 게시글 **본문**에 이미 삽입된 인스타그램 임베드(iframe/blockquote)를 사일로 네이티브 UI로 바꿔치기하라"는 게 핵심이었다.
- **기존 구조 확인**: 게시글 본문의 Instagram 임베드는 `<blockquote class="instagram-media" data-instgrm-permalink="...">`(EPIC-079, Instagram 공식 embed.js 위젯 마크업)로 저장돼 있고, 지금까지 `UniversalBlockRenderer.tsx`(게시글 읽기 전용 렌더러)가 `processInstagramEmbeds()`(`src/lib/instagramEmbed.ts`)로 Instagram의 공식 embed.js를 로드해 이 blockquote를 iframe으로 부풀려왔다.
- **수정**: `processNativeInstagramEmbeds()`(`src/lib/nativeInstagramEmbed.ts`, 신규)가 그 자리를 대신한다 — 같은 blockquote를 찾아 `data-instgrm-permalink`를 읽고, DOM에서 들어낸 뒤 `react-dom/client`의 `createRoot`로 그 자리에 `NativeInstagramEmbed` React 컴포넌트를 새로 마운트한다(이 저장소 다른 임베드 처리기는 순수 문자열 innerHTML 치환이지만, embla 훅이 필요해 이번만 실제 React 루트를 얹는 표준 패턴을 썼다). `UniversalBlockRenderer.tsx`는 이 한 줄만 바뀌었으므로, **과거에 이미 작성된 게시글도 저장된 마크업은 그대로 두고 렌더링 시점에만 교체되어 즉시 소급 적용**된다(요구사항 3 그대로).
- **`NativeInstagramEmbed.tsx`(신규)의 판단 로직과, 사용자에게 짚어야 할 기술적 한계**: Instagram Graph API는 "우리 소유 계정(_silo_store)의 미디어 목록"만 조회 가능하고, **임의의 permalink URL로 다른 계정의 게시물을 즉석 조회하는 공식 API는 존재하지 않는다** — "본문에서 감지된 URL을 즉시 Graph API로 가져오라"는 원 요청을 문자 그대로는 구현할 수 없는 이유. 그래서: (1) permalink에서 shortcode를 추출해 `instagram_feeds`(R2 캐시, EPIC-143이 미리 동기화해둔 사일로 스토어 자사 게시물)에서 일치하는 행을 찾으면 → 완전히 네이티브로(R2 URL, Instagram 요청 0회) 렌더링. (2) 못 찾으면(아직 동기화 전인 자사 게시물이거나, 애초에 타 계정 게시물) → 이미 이 저장소에 있었지만 한 번도 실제로 연결된 적 없던 EPIC-133의 `InstagramMediaSlider`(공개 og:meta 가벼운 스크래핑+실시간 프록시, 영구 저장 없음)로 폴백 — **새로운 비공식 스크래핑을 추가한 게 아니라 기존에 미완성으로 남아있던 걸 여기서 처음 실제로 배선한 것**.
- **`InstagramFeedPost.tsx`에 `variant` prop 추가**: 그리드 카드(`"grid"`, 기존 정사각형 크롭)와 본문 임베드(`"embed"`, 신규 — 크롭 없이 `object-contain`, 420px 고정 높이로 레터박스) 두 컨텍스트를 하나의 컴포넌트로 공유 — 로직 중복 없음.
- **검증**: `npx tsc --noEmit`/`npm run lint` 0 errors. 실제 REST API 호출로 shortcode 기반 permalink 매칭(`ilike`)이 방금 동기화한 실데이터(`https://www.instagram.com/p/DcS2mrpN8y6/`)에 대해 정확히 매치됨을 확인.
- **의도적으로 손대지 않은 곳**: `BlockEditor.tsx`(글쓰기 화면의 라이브 미리보기)는 여전히 기존 `processInstagramEmbeds()`(공식 embed.js)를 쓴다 — 이번 요청은 "게시글 렌더러"(독자가 보는 화면)에 관한 것이라 편집 중 미리보기까지 바꾸는 건 별도 확인 없이 범위를 넓히지 않았다(에디터 쪽은 더 복잡하고 회귀 위험이 커 신중하게 접근). 필요하면 다음에 별도로 통일 가능.
- **변경 파일**: `src/components/content/UniversalBlockRenderer.tsx`, `src/components/content/InstagramFeedPost.tsx`, `src/components/content/NativeInstagramEmbed.tsx`(신규), `src/lib/nativeInstagramEmbed.ts`(신규).

## 2026-08-23 (EPIC-143 — Instagram Graph API 기반 네이티브 피드 렌더링)
- **배경**: EPIC-142에서 "인스타그램 비공식 스크래핑 + User-Agent 위장으로 rate limit 우회 + R2 영구 재호스팅" 파이프라인을 요청받았으나, 명시적 탐지 회피(UA 위장)와 저작권 재배포 우려로 그 형태의 구현을 거부하고 대신 **공식 Instagram Graph API** 경로를 제안했다. 사용자가 Meta Business 설정에서 사일로 스토어 소유 계정(`_silo_store`, IG User ID 확인 완료) System User 장기 액세스 토큰을 직접 발급받아 진행을 확정 — EPIC-143으로 착수.
- **DB**: `instagram_feeds` 테이블 신설 — `ig_media_id`(unique), `media_type`(IMAGE/VIDEO/CAROUSEL_ALBUM, CHECK 제약), `caption`, `permalink`, `posted_at`, `media_urls`/`media_item_types`(text[], 캐러셀은 순서대로 여러 개), `thumbnail_url`. RLS: public read(마케팅성 공개 콘텐츠) + `is_current_user_admin()`(EPIC-071 패턴 재사용) 기반 admin-only write.
- **동기화 API**: `POST /api/instagram/fetch`(관리자 전용) — Graph API `/{ig-user-id}/media`(fields에 `children{media_type,media_url}` 포함해 캐러셀 하위 미디어까지 조회) 호출 → 원본 CDN URL을 클라이언트에 절대 노출하지 않고 서버가 버퍼로 직접 다운로드 → `@aws-sdk/client-s3`로 R2에 업로드 → 결과 R2 URL을 `instagram_feeds`에 upsert. 서버리스 타임아웃을 피하려 한 번에 한 페이지(12개)만 처리하고 `nextCursor`를 반환 — 이미 동기화된 `ig_media_id`는 건너뛴다. **실사용 테스트로 발견한 엣지케이스**: 일부 VIDEO/릴스 게시물은 `media_url` 필드 자체가 안 내려오고 `thumbnail_url`만 있다 — 그 경우도 조용히 실패시키지 않고 썸네일만이라도 저장.
- **관리자 UI**: `/admin/instagram`(신규, `AdminLayout` 메뉴에 "Instagram 동기화" 추가) — "최근 게시물 동기화" 버튼이 `nextCursor`가 없어질 때까지 `/api/instagram/fetch`를 자동 반복 호출해 진행 상황(새로 저장/건너뜀 개수, 오류 목록)을 보여준다.
- **네이티브 프론트엔드**: `InstagramFeedPost.tsx`(embla-carousel-react 신규 도입) — IMAGE는 `<img>`, VIDEO는 `autoPlay muted loop playsInline`, CAROUSEL_ALBUM은 화살표+점 인디케이터가 있는 자체 슬라이더로 렌더링. Instagram 로고/좋아요/댓글 등 원본 UI는 애초에 응답 데이터에 없어 렌더링 대상 자체가 아니다(iframe/embed.js 전혀 미사용). `InstagramNativeFeed.tsx`(서버 컴포넌트, `instagram_feeds`를 직접 읽어 Instagram API 호출 없이 즉시 렌더링) + 빈 상태(동기화 전) fallback UI.
- **검증**: `npx tsc --noEmit`/`npm run lint` 0 errors. 격리 스크립트로 (1) Graph API 호출→R2 업로드→공개 URL 접근 파이프라인 개별 검증, (2) DB INSERT/제약조건 검증(테스트 행은 삭제) 이후, **실제로 8개 게시물(IMAGE 1/VIDEO 5/CAROUSEL_ALBUM 2, 캐러셀 최대 10장, media_url 없는 릴스의 썸네일 폴백까지 포함)을 라이브로 동기화**해 DB에 실데이터가 정상 저장됨을 확인.
- **아직 안 한 것**: `InstagramNativeFeed`를 특정 페이지(홈페이지 등)에 아직 배치하지 않음 — 어디에 넣을지는 사용자 결정 대기, 컴포넌트 자체는 어디든 바로 끼워 넣을 수 있게 준비 완료.
- **변경 파일**: `src/app/api/instagram/fetch/route.ts`(신규), `src/app/admin/instagram/page.tsx`(신규), `src/app/admin/layout.tsx`, `src/lib/instagramFeed.ts`(신규), `src/components/content/InstagramFeedPost.tsx`(신규), `src/components/content/InstagramNativeFeed.tsx`(신규), `package.json`(embla-carousel-react 추가). + `instagram_feeds` 테이블/RLS, `.env.local`(IG_ACCESS_TOKEN/IG_USER_ID/IG_APP_ID/IG_APP_SECRET — 커밋 안 됨).

## 2026-08-22 (HOTFIX-141.21 — "상단 사이드바" 모바일 프리뷰에 세션정보 컬럼만 보이던 문제)
- **신고**: "'홈페이지 설정' -> '모바일' 버전에서 상단 사이드바에 4가지 컬럼들중 '세션정보' 컬럼만 나오고 있어. 모든 컬럼이 나오게 해줘".
- **원인**: `TopSidebarPanel.tsx`의 column 0(이미지 뱅크)이 `hidden md:block`으로 **실제 브라우저 창 폭**만 보고 판단하고 있었다 — "홈페이지 설정 관리"의 모바일 프리뷰는 390px짜리 프레임 `<div>`로 좁아 보이게 흉내만 낼 뿐, 그 프레임을 담은 실제 브라우저 창 자체는 그보다 넓을 수 있어 `md:`(768px 이상) 판정이 시뮬레이션과 무관하게 어긋났다(HOTFIX-141.9~141.11에서 반복됐던 "실제 폭 vs 시뮬레이션 폭 불일치" 계열의 같은 근본 원인). column 2/3(관리자 링크 목록/hover 하위 목록)은 숨겨진 건 아니었지만 가로 스크롤(HOTFIX-141)로만 닿을 수 있어 세션정보 컬럼 하나만 있는 것처럼 보였다.
- **수정**: `Navbar.tsx`가 이미 계산해둔 `isMobileViewport`(디바이스 탭 오버라이드 반영, 실제 뷰포트가 아니라 관리자가 고른 시뮬레이션 기준)를 `TopSidebarPanel`에 새 prop으로 넘긴다. 모바일이면: (1) column 0도 더 이상 숨기지 않고, (2) 4개 컬럼을 가로 나열+스크롤 대신 세로로 쌓아(`flex-col`) 스크롤 없이 전부 한 번에 보이게 했다. PC는 기존 가로 4열 레이아웃 그대로.
- **검증**: `npx tsc --noEmit`/`npm run lint` 0 errors.
- **참고**: column 3(하위 목록)은 여전히 마우스 hover로 채워지는 구조라, 관리자 화면(마우스 사용)에서는 정상 동작하지만 실제 터치 전용 모바일 기기에서는 hover 자체가 없어 확인이 어려울 수 있음 — 별도 개선 여지로 남겨둠(이번 신고 범위 밖).
- **변경 파일**: `src/components/Navbar.tsx`, `src/components/TopSidebarPanel.tsx`.

## 2026-08-22 (HOTFIX-141.20 — 헤더 요소 위치를 좌표 직접 입력으로도 옮길 수 있게 — 화면 밖 요소 복구)
- **신고**: "모바일 버전에서 '상단 사이드바' 아이콘이 안보여" → (드래그로 옮기라고 안내) → "안보이는데 어떻게 드래그를 하냐고 이바보야". DB로 직접 확인해보니 실제로 `top-sidebar-trigger`(모바일)의 dxPx가 -1.8로 사실상 원래(안 옮겨진) 자리였고, 같은 줄의 다른 8개 요소(로고/탭들/관리자 등)는 이미 다 손으로 옮겨져 있었다 — 390px 폭 한 줄에 로고+탭+계정메뉴 5개+글쓰기+이 아이콘까지 다 들어가야 해서, 아직 안 옮긴 이 요소만 화면 밖으로 밀려 안 보이는 상태였다. 그런데 드래그는 "보이는 걸 옮기는" 수단이라 0픽셀만 보이는(또는 완전히 안 보이는) 요소는 애초에 캔버스에서 잡을 수가 없다 — "드래그해서 옮기라"는 이전 안내가 순환논리였다.
- **수정**: Controls 패널 "위치" 섹션에 가로(px)/세로(px) **숫자 직접 입력 필드**를 추가했다 — 요소가 안 보여도, Elements 패널에서 선택만 하면(캔버스 클릭 불필요) 좌표를 타이핑해서 옮길 수 있다. 0으로 두면 원래(자연스러운 flex) 자리로, 값을 조금씩 줄여가며 화면 안으로 들어오는지 확인하는 식으로 쓸 수 있다. 드래그 시스템(HOTFIX-141.15의 폭 비례 스케일링 refWidthPx)과 동일한 데이터를 쓰므로 서로 완전히 호환된다 — 숫자로 옮긴 뒤 필요하면 드래그로 더 미세 조정도 가능.
- **검증**: `npx tsc --noEmit`/`npm run lint` 0 errors.
- **변경 파일**: `src/app/admin/navigation/settings/page.tsx`.

## 2026-08-22 (HOTFIX-141.19 — 1차/2차 드롭다운 좌우 위치가 "오른쪽 기준" 정렬일 때 적용 안 되던 CSS 버그)
- **신고**: "'살롱데상' 상단 탭 메뉴의 '1차 좌우 위치', '2차 좌우 위치'가 설정이 적용이 안돼".
- **확인**: DB(`top_tab_style.mobile.tabs.salon`)를 직접 조회해 `dropdownOffsetXPx: 50`이 정확히 저장돼 있음을 확인 — 저장은 정상, 렌더링만 반영 안 됨.
- **원인**: HOTFIX-141.16에서 이 값을 `marginLeft`로 적용했는데, 살롱데상의 모바일 설정은 `dropdownAlign: "right"`(오른쪽 기준 정렬)다 — 오른쪽 기준(`right-0`)으로 고정된 auto-width(shrink-to-fit) 부모 박스에 자식의 `marginLeft`를 더하면, 그 여백만큼 박스 자체가 왼쪽으로 넓어지면서 자식의 화면상 위치가 원래 자리로 정확히 되돌아가 버려(margin이 shrink-to-fit 너비 계산에 흡수됨) 시각적으로 아무 효과가 없었다 — "왼쪽 기준" 정렬에서는 우연히 정상 동작했기 때문에 처음 테스트(온라인 도슨트 등, 왼쪽 기준 기본값)에서는 문제가 드러나지 않았다.
- **수정**: `marginLeft` 대신 `transform: translateX(px)`로 적용 방식을 바꿨다 — transform은 레이아웃 계산(shrink-to-fit) 이후 페인트 단계에서만 적용되므로 앵커 방향(왼쪽/오른쪽 기준)과 무관하게 항상 지정한 px만큼 정확히 이동한다.
- **검증**: `npx tsc --noEmit`/`npm run lint` 0 errors.
- **변경 파일**: `src/components/Navbar.tsx`.

## 2026-08-22 (HOTFIX-141.18 — Elements/Controls 패널이 캔버스 안 사이드바 패널에 가려 통째로 안 보이던 문제)
- **신고**: "element와 control 패널을 어떻게 선택하지? 안보이는데" — 첨부 스크린샷엔 Elements/Controls 패널도, 그걸 다시 켜는 "▶ 패널 보기" 토글도 전혀 안 보였다(1차 답변에서 "◀ 버튼을 찾아보라"고 한 건 틀린 진단이었음 — 스크린샷 재확인 요청으로 바로잡음).
- **진짜 원인**: 캔버스가 실제 `Navbar`를 그대로 렌더링하는 구조라, 사일로상점/살롱데상 좌우 사이드바 패널이나 "상단 사이드바" 메가메뉴를 캔버스 안에서 열어두면 그 패널들(`LeftSidebar.tsx`/`RightSidebar.tsx`/`TopSidebarPanel.tsx` — 열렸을 때 전부 `z-50`, `editable`이면 `absolute`라 이 관리자 패널과 같은 containing block까지 올라옴)이 Elements/Controls 패널(`z-40`)과 "▶ 패널 보기" 토글(기존 `z-50`, 동률에선 나중에 그려지는 캔버스 쪽이 이김)을 통째로 덮어버렸다.
- **수정**: Elements/Controls 패널과 그 토글 버튼만 `z-[100]`으로 올려 캔버스 안에서 나올 수 있는 어떤 z-index(최대 `z-50`)보다 항상 위에 그려지게 함 — 사이트 콘텐츠를 시뮬레이션하는 캔버스 요소가 관리자 자신의 UI를 가리는 일이 구조적으로 불가능해진다.
- **검증**: `npx tsc --noEmit`/`npm run lint` 0 errors.
- **변경 파일**: `src/app/admin/navigation/settings/page.tsx`.

## 2026-08-22 (HOTFIX-141.17 — 로고 좌/우 텍스트를 로고와 하나로 묶어 함께 고정하는 "그룹화" 기능)
- **요청**: "그냥 차라리, 로고 좌 텍스트 + 메인 로고 + 우 텍스트를 하나로 그룹화 하는 기능을 줘 고정되게" — 셋을 독립적으로 드래그/잠금/폭-비례-스케일링(HOTFIX-141.10~141.15)까지 다 손봐도 "따로 움직이는 3개"라는 구조 자체가 계속 어긋날 여지를 남긴다는 지적.
- **구현**: `MainLogoConfig`에 `groupSideTexts`(boolean)/`groupGapPx`(px) 신설. 켜면 `Navbar.tsx`가 셋을 하나의 `HeaderSlot`(slotKey `logo-group`)으로 묶어 `flex` + 고정 간격(`groupGapPx`, 기본 16px)으로 렌더링 — 위치(드래그/잠금/폭 비례 스케일링)가 이제 이 그룹 하나에만 존재해 서로 어긋날 수가 없다. 끄면(기본값) 기존처럼 셋이 완전히 독립된 3개 요소로 남아 100% 하위 호환.
- **텍스트 내용/서체는 그대로 개별 편집**: 그룹화는 위치만 합칠 뿐, "로고"/"로고 왼쪽 텍스트"/"로고 오른쪽 텍스트" 각각의 텍스트 내용·서체·크기·색상 편집은 그대로 따로 가능 — 다만 그룹 안에서는 캔버스에 개별 HeaderSlot이 없어 왼쪽 Elements 패널에서 선택해야 한다(Controls에 안내 문구 추가). "로고" Controls에 그룹화 켜기/끄기 체크박스 + 간격(px) 입력을 두었고, Elements 패널에 그룹이 켜져 있을 때만 "로고 그룹(위치)" 버튼이 나타난다.
- **검증**: `npx tsc --noEmit`/`npm run lint` 0 errors.
- **변경 파일**: `src/lib/mainLogoSettings.ts`, `src/components/Navbar.tsx`, `src/app/admin/navigation/settings/page.tsx`.

## 2026-08-22 (HOTFIX-141.16 — 1차/2차 드롭다운에 좌우 위치(px) 지정 추가)
- **신고**: "아니 1차/2차 드롭다운의 방향말고, 위치를 정하게 해달라고 좌우" — HOTFIX-141.15에서 넣은 dropdownAlign/subDropdownAlign(왼쪽·오른쪽 기준 중 택1)은 "방향"만 바꿀 뿐 세밀한 "위치" 조정이 아니었다.
- **수정**: `TopTabStyleEntry`에 `dropdownOffsetXPx`(1차)/`subDropdownOffsetXPx`(2차) 신설 — 기존 정렬 기준선(dropdownAlign/subDropdownAlign이 정한 왼쪽·오른쪽 기준)에서 px 단위로 추가로 밀 수 있다(양수=오른쪽, 음수=왼쪽), `marginLeft`로 적용. Controls 패널에 "1차 좌우 위치(px)"/"2차 좌우 위치(px)" 입력 추가 — 폭(width) 필드 바로 아래, 방향(align) 필드 아래에 배치. `deviceTab`별로 저장되므로 PC/모바일 각각 독립.
- **검증**: `npx tsc --noEmit`/`npm run lint` 0 errors.
- **변경 파일**: `src/lib/topTabStyleSettings.ts`, `src/components/Navbar.tsx`, `src/app/admin/navigation/settings/page.tsx`.

## 2026-08-22 (HOTFIX-141.15 — 헤더 드래그 오프셋이 창 폭에 비례해 스케일링되도록 수정 + 2차 드롭다운 방향 개별 설정)
- **신고**: "pc 버전의 좌우 폭을 줄이니까, '로고 좌 우 텍스트'가 고정이 안되고... 겹쳐지잖아... '스튜디오', '관리자', '상단 사이드바 아이콘' 버튼도 좌우 폭과 함께 움직이고 있어 고정이 안되고" + "1차와 2차 드롭다운의 좌 우 위치를 설정할수 있게 해줘. pc 와 모바일 둘다".
- **원인**: `HeaderSlot.tsx`(로고/탭/계정메뉴/상단 사이드바 아이콘 등 대부분의 헤더 요소)와 `LeftSidebar.tsx`/`RightSidebar.tsx`(좌우 여닫이 아이콘, 별도 드래그 로직)가 지금까지 `transform: translate(dxPx, dyPx)`의 dxPx를 **고정 px 값**으로 저장했다 — 드래그한 순간의 브라우저 창 폭에서만 정확한 값이라, 창 폭이 바뀌면(로고의 `flex-1` 박스 크기 등 반응형 레이아웃 자체가 다시 계산되므로) 같은 px만큼 밀어도 실제 목표 위치와 어긋나 겹쳐 보였다. "잠금"(HOTFIX-141.13)을 걸어도 이 근본 원인은 그대로라 폭을 줄이면 여전히 깨졌다.
- **수정**: `HeaderSlotOffset`에 `refWidthPx`(드래그 당시 기준 폭) 신설. 드래그를 시작할 때 지금 폭([data-admin-canvas] 프레임 폭, 캔버스 밖이면 `window.innerWidth`)을 함께 저장하고, 렌더링 시 `현재 폭 / refWidthPx` 비율만큼 dxPx를 스케일링해 적용 — 창 폭이 달라져도 상대적 위치가 유지된다(세로 dyPx는 폭과 무관하므로 그대로). 기준 폭 측정/추적 로직은 `src/lib/useReferenceWidth.ts`로 공용화해 `HeaderSlot.tsx`와 `LeftSidebar.tsx`/`RightSidebar.tsx` 셋 다 재사용(사용자가 신고한 4개 요소는 전부 HeaderSlot 경유지만, 좌우 사이드바 아이콘도 동일한 결함 패턴이라 함께 고쳤다).
- **2차 드롭다운 방향**: `TopTabStyleEntry`에 `subDropdownAlign` 신설(HOTFIX-141.14의 `subDropdownWidthPx`와 동일 패턴, 비면 1차 `dropdownAlign` 상속) — Controls 패널에 "1차 펼치는 방향"/"2차 펼치는 방향" 두 선택으로 분리. `deviceTab`별로 이미 저장되므로 PC/모바일 각각 독립.
- **검증**: `npx tsc --noEmit`/`npm run lint` 0 errors. 실제 클릭 재현은 이 세션 환경 제약(Supabase REST 403)으로 여전히 못 함.
- **변경 파일**: `src/lib/headerLayoutPositions.ts`, `src/lib/useReferenceWidth.ts`(신규), `src/lib/topTabStyleSettings.ts`, `src/components/HeaderSlot.tsx`, `src/components/LeftSidebar.tsx`, `src/components/RightSidebar.tsx`, `src/components/Navbar.tsx`, `src/app/admin/navigation/settings/page.tsx`.

## 2026-08-22 (HOTFIX-141.14 — 헤더 요소 어디든 클릭-드래그 가능 + 드롭다운 2차 플라이아웃 방향/폭 개별 설정 + "상단 사이드바" 드래그 미구현 사실 확인)
- **요청 다발**: 실사용 스크린샷 4장과 함께 여러 건 동시 신고 — (1) "요소의 어디를 클릭하든 드래그" pc/모바일 둘 다, (2) "About Silo→사일로의 취향" 2차 플라이아웃이 화면 아래로 잘림(위로 열리게), (3) "온라인 도슨트" 2차(하위의 하위) 플라이아웃 폭을 1차와 따로 지정, (4) "상단 사이드바"(TopSidebarPanel, 실제 사이트에 뜨는 메가메뉴) 안 요소들이 여전히 드래그 안 됨, (5) 모바일에서 로고 오른쪽 텍스트("Silo")/상단 사이드바 아이콘이 화면 밖이라 안 보이고 선택도 안 됨, (6) "살롱데상→커뮤니티" 2차 플라이아웃이 오른쪽으로 잘림, (7) "로고 왼쪽 텍스트"를 잠금 해제했는데도 안 끌림.
- **(1) 해결** — `HeaderSlot.tsx`: 지금까지 20×20px짜리 ✥ 핸들 버튼만 드래그를 시작할 수 있었다 — 이미 선택된 상태라면 요소 wrapper 전체(`<Tag>`)에 pointerdown/move/up 핸들러를 걸어 어디를 눌러도 드래그가 시작되게 함(첫 클릭=선택, 선택 후 클릭=드래그는 그대로 유지, 잠긴 요소는 여전히 제외).
- **(2) 해결** — `Navbar.tsx`: 그룹/항목 목록에서 마지막 그룹·마지막 항목의 2차 플라이아웃만 `top-0` 대신 `bottom-0`으로 열리게(인덱스로 판별) — 목록 끝쪽 항목의 플라이아웃이 뷰포트 바닥에 잘리는 문제를 항상 위로 열어 해결. 나머지는 기존과 동일하게 아래로 열림.
- **(3) 해결** — `TopTabStyleEntry`에 `subDropdownWidthPx` 신설(비면 `dropdownWidthPx` 상속) — Controls 패널에 "1차 폭"/"2차 폭" 두 입력으로 분리.
- **(6) 즉시 대응 가능** — 코드 변경 없이도 해결 가능: HOTFIX-141.12로 살롱데상도 이제 다른 탭과 동일하게 "펼치는 방향"(dropdownAlign) 설정이 가능해졌다 — Controls에서 "오른쪽 기준"으로 바꾸면 왼쪽으로 펼쳐져 잘리지 않는다(온라인 도슨트에 이미 쓰던 것과 동일한 설정).
- **(4) 조사 결과, 코드가 아니라 사실관계 정정** — Explore 서브에이전트로 확인: 실제 사이트/캔버스에 뜨는 시각적 메가메뉴(`TopSidebarPanel.tsx`)에는 드래그 코드가 전혀 없다(dnd-kit도 HTML5 DnD도 grip UI도 0건). 과거 HOTFIX-141.7/141.10 커밋 메시지가 "상단 사이드바에 드래그 순서변경 추가"라고 적었지만, 실제로 건드린 건 `/admin/navigation/settings` **Controls 패널의 별도 텍스트 목록**(`TopSidebarControls`, "⠿" 그립 아이콘 + 네이티브 HTML5 draggable, `page.tsx` 2028행~)뿐이었다 — 그 목록에서는 순서 변경이 정상 동작할 것으로 보이나, 사용자가 기대한 "캔버스에 실제로 뜨는 메가메뉴를 직접 드래그"는 애초에 구현된 적이 없었다. 이번 세션에서 새로 만들지는 못했다(호버로만 열리는 패널을 편집 모드에서 강제로 열어둔 채 드래그해야 해서 별도 설계가 필요) — **다음 작업으로 명시적으로 남김**(아래 NEXT_TASK 참고).
- **(5)(7) 진단** — 데이터 확인 결과 실제 원인: `header_positions`에 과거 실험적으로 드래그된 극단적 오프셋들(`account:admin` -991px, `tab:space_inquiry` -910px 등)이 쌓여있었고, 세션 도중 사용자가 같은 화면을 다른 탭에서 동시에 열어두고 라이브로 계속 드래그하며 저장하고 있어(마커 값으로 재현 확인) DB를 직접 고쳐도 몇 초 만에 되돌아갔다 — 즉 "화면 밖이라 안 보인다"/"잠금 해제해도 안 끌린다"는 대부분 코드 버그가 아니라 (a) 과거 극단 오프셋이 다른 요소를 시각적으로 가리거나 클릭을 가로채고 있었고 (b) 모바일은 애초에 여러 헤더 요소를 각자 수동으로 재배치해야 390px 폭에 다 들어가는 설계라 아직 위치를 안 잡은 요소가 화면 밖에 걸쳐 있었던 것. (1)의 "어디든 드래그" 개선으로 재배치가 훨씬 쉬워졌고, Elements 패널(왼쪽 목록)에서는 화면 밖 요소도 항상 선택 가능하다는 점을 사용자에게 안내함 — 근본적인 "화면 밖 요소를 시각적으로 표시" 기능은 별도 후속 작업으로 남김.
- **검증**: `npx tsc --noEmit`/`npm run lint` 0 errors. 실제 클릭 재현은 이 세션의 브라우저 프리뷰가 Supabase REST 호출을 못 하는 문제로 여전히 불가 — 사용자가 직접 재확인 필요.
- **변경 파일**: `src/components/HeaderSlot.tsx`, `src/components/Navbar.tsx`, `src/lib/topTabStyleSettings.ts`, `src/app/admin/navigation/settings/page.tsx`.

## 2026-08-22 (HOTFIX-141.13 — 로고 양옆 텍스트 간격 불일치 원인 규명 + 헤더 요소 "위치 고정" 기능 신설)
- **신고**: "I'm your [로고] Silo 이 텍스트 + 로고 + 텍스트 간격은 일정했으면 좋겠어 pc 버전은".
- **원인**: `site_settings.header_positions`(PC)를 직접 조회해보니 세 요소의 드래그 오프셋(dxPx)이 `logo: +95.8px`, `logo-left-text("I'm your"): +390.4px`, `logo-right-text("Silo"): -186.4px`로 서로 크게 달랐다 — `HeaderSlot.tsx`는 순수 `transform: translate(dx, dy)`만 얹는 구조라 오프셋이 0이면 원래 flex 레이아웃(`gap-4` + 로고의 `justify-center`)이 자동으로 좌우 대칭 간격을 만들어주는데, 과거 드래그로 세 값이 서로 어긋나면서 시각적으로 간격이 들쭉날쭉해졌다.
- **DB로 한 번 고쳤는데 되돌아간 문제**: `dxPx`를 0으로 되돌리는 UPDATE를 실행했더니 몇 초 안에 원래 값(95.8/390.4/-186.4)으로 정확히 되돌아갔다 — 마커 값(1234)으로 재현 테스트해 확인. 사용자에게 확인한 결과 다른 탭에서 "홈페이지 설정 관리" 화면을 이미 열어둔 상태였고, 그 세션이 메모리에 든 예전 위치를 계속 다시 저장하고 있었던 것 — DB만 고쳐서는 열린 세션이 새로고침되기 전까지 근본적으로 못 이긴다.
- **수정(사용자 지시 — "드래그로 움직이고, 수정이 끝나면 고정되도록, pc와 mobile 둘 다")**: 일회성 되돌리기 대신, 모든 헤더 요소(HeaderSlot 기반 전체 + 좌/우 사이드바 아이콘)에 재사용 가능한 "위치 고정" 기능을 신설했다. `HeaderSlotOffset`에 `locked?: boolean` 추가 — 잠그면 캔버스의 ✥ 드래그 핸들 자체가 렌더링되지 않아(`HeaderSlot.tsx`) 실수로 다시 끌리는 게 구조적으로 불가능해진다. 캔버스 위 작은 🔓/🔒 토글 버튼과, Controls 패널 "위치" 섹션의 체크박스 둘 다에서 잠금/해제 가능. `LeftSidebar.tsx`/`RightSidebar.tsx`(독자적인 드래그 로직을 쓰는 사이드바 아이콘)도 `offset?.locked`를 `startDrag` 가드에 추가해 동일하게 동작. 로고/로고 왼쪽 텍스트/로고 오른쪽 텍스트(PC) 세 슬롯의 `dxPx`를 0으로 되돌리고 `locked: true`로 설정 — 이제 간격이 대칭이고, 다시 실수로 끌려도 핸들이 없어 벌어지지 않는다.
- **검증**: `npx tsc --noEmit`/`npm run lint` 0 errors. Management API로 DB 값이 `{dxPx:0, locked:true}`로 정확히 반영됨을 재조회로 확인. 실제 캔버스 클릭 재현은 이전 HOTFIX들과 동일한 이유(이 세션의 브라우저 프리뷰가 Supabase REST 호출을 못 함)로 못 함 — 다음 세션에서 로그인 후 확인 필요.
- **변경 파일**: `src/lib/headerLayoutPositions.ts`, `src/components/HeaderSlot.tsx`, `src/components/LeftSidebar.tsx`, `src/components/RightSidebar.tsx`, `src/app/admin/navigation/settings/page.tsx`. (+ `site_settings.header_positions` 데이터 직접 수정)

## 2026-08-22 (HOTFIX-141.12 — 상단 탭 최상위/하위 카테고리 텍스트 개별 커스터마이징을 모든 탭으로 확장 + 로고 좌/우 텍스트 독립 스타일)
- **요청**: "상단 탭의 최상위 카테고리와 그다음 하위 카테고리의 텍스트를 각각 수정하게 해달라(줄바꿈 등으로 폭이 맞춰지게), 지금은 about silo/온라인 도슨트 탭만 세부 설정이 가능한데 모든 요소가 그렇게 돼야 한다" + "모바일의 I'm your,/Silo 텍스트도 PC처럼 세부 설정 가능하게 연결해달라".
- **실제 원인(사일로상점/살롱데상)**: 라이브 DB(`site_navigations`)를 직접 조회해보니 About Silo/온라인 도슨트/스튜디오는 이미 `dropdown` 타입이라 `/admin/navigation/settings`의 탭 스타일 편집(표시 텍스트/서체/크기/색상 등)이 원래부터 동작했다 — 진짜로 안 됐던 건 `사일로 상점`(`sidebar_left`)/`살롱데상`(`sidebar_right`) 두 개뿐이었다. `Navbar.tsx`는 타입과 무관하게 tier1/tier2 노출 위치가 있으면 `tab:` 스타일 메커니즘을 이미 지원하는데, `/admin/navigation/settings`의 Elements 목록(`topNavRows`)만 `sidebar-left`/`sidebar-right` 타입을 통째로 걸러내고 있어 이 두 탭은 애초에 선택조차 할 수 없었다.
- **수정**: (1) `topNavRows` 필터를 제거해 사일로상점/살롱데상도 다른 탭과 동일하게 선택·편집 가능하게 함. (2) 표시 텍스트(탭 자신)를 `<input>`→`<textarea>`로 바꿔 줄바꿈 입력 가능하게 하고, `Navbar.tsx` 렌더링에 `white-space: pre-line` 적용 — 버튼 폭이 텍스트(여러 줄)에 자연히 맞춰진다. (3) 하위 카테고리(그룹 헤더/항목) 하나하나의 표시 텍스트를 원본과 별개로 오버라이드하는 `subLabelOverrides`(키: 그룹은 groupLabel, 항목은 href)를 `TopTabStyleEntry`에 신설, Controls 패널에 하위 카테고리별 textarea 목록 추가 — 메가/일반 드롭다운 어느 모드든 동일하게 적용.
- **로고 좌/우 텍스트("I'm your,"/"Silo" 등)**: 지금까지 `leftText`/`rightText`는 내용만 편집 가능했고 서체/굵기/크기/색상은 전부 "로고" 슬롯 값을 그대로 상속해 독립적으로 커스터마이징할 수 없었다(PC/모바일 공통 한계 — "PC는 되는데 모바일만 안 된다"가 아니라 애초에 둘 다 안 됐다). `MainLogoConfig`에 `leftTextFontFamily`/`leftTextCustomFonts`/`leftTextBold`/`leftTextFontSizePx`/`leftTextColor`(우측도 동일 세트)를 신설 — 비어있으면(기본값) 기존처럼 로고 스타일을 그대로 상속해 기존 데이터와 100% 호환. `deviceTab`이 이미 pc/mobile을 나누므로 이 필드들도 자동으로 기기별 독립 저장된다.
- **검증**: `npx tsc --noEmit`/`npm run lint` 0 errors. Management API로 라이브 `site_navigations.target_types` 직접 조회해 원인 확정(사일로상점=`sidebar_left`, 살롱데상=`sidebar_right`, 나머지는 이미 `dropdown`/`tier1_tab`). 이 세션의 브라우저 프리뷰는 Supabase REST 호출이 막혀 있어(이 세션 환경 자체의 네트워크 제약 — dev.silostore.net에서도 동일 증상 재현, 이번 변경과 무관) 실제 클릭 재현은 못 함 — 다음 세션에서 로그인 후 재확인 필요.
- **변경 파일**: `src/components/Navbar.tsx`, `src/app/admin/navigation/settings/page.tsx`, `src/lib/topTabStyleSettings.ts`, `src/lib/mainLogoSettings.ts`.

## 2026-08-22 (HOTFIX-141.11 — live preview가 실제 사이트와 계속 다르던 두 번째 근본 원인: 로고/사이드바 아이콘/상단 탭/계정 영역 스타일이 애초에 캔버스에 실시간 반영된 적이 없었다)
- **배경**: `dropdownAlign`("온라인 도슨트" 드롭다운을 오른쪽 기준으로 바꾸는 설정, HOTFIX-141.10에서 막 추가)을 실제로 캔버스에서 테스트하니 — Controls의 select 값은 "right"로 정확히 바뀌었는데, 드롭다운 요소의 실제 className은 여전히 `left-0`이었다(캔버스가 갱신되지 않음). 저장 후 새로고침 없이는 Controls에서 바꾼 게 전혀 안 보인다는 뜻이었다.
- **진짜 원인**: `Navbar.tsx`를 코드로 직접 확인해보니 `positionsOverride`(header_positions)와 `topSidebarOverride`(top_sidebar) 딱 두 설정만 캔버스에 "지금 편집 중인 값"을 즉시 넘기는 override prop이 있었고, **로고(main_logo)/사이드바 아이콘(sidebar_icons)/상단 탭 스타일(top_tab_style)/계정 영역 스타일(account_menu_style)은 그런 prop 자체가 애초에 없었다** — 대신 EPIC-136 이전(캔버스가 아직 iframe이던 시절)에 쓰던 `postMessage` 기반 실시간 갱신 코드만 죽은 채로 남아있었다(`?__adminPreview=1` 쿼리 파라미터로만 켜지는데, 지금 캔버스는 iframe이 아니라서 이 코드 자체가 완전히 no-op). 즉 이 네 설정에 대한 모든 Controls 편집(서체/크기/색상/hover 모션/드롭다운 폭·정렬·업로드 폰트 등, HOTFIX-141.7~141.10에서 새로 만든 필드 전부 포함)이 **"저장하기"를 누르고 새로고침해야만** 캔버스에 반영되고 있었다 — "live preview가 실제 사이트와 다르다"는 이번 세션 내내 반복된 신고의 상당 부분이, 사실은 버그가 아니라 "아직 저장 안 한 편집 내용이 애초에 미리보기에 반영되는 경로가 없었다"는 훨씬 근본적인 설계 공백이었다.
- **수정**: `positionsOverride`/`topSidebarOverride`와 동일한 패턴으로 `mainLogoOverride`/`sidebarIconsOverride`/`topTabStyleOverride`/`accountMenuStyleOverride` 4개를 신설 — 각 설정의 자체 `useEffect` 조회를 override가 있으면 건너뛰고, override 값을 우선 사용하도록 `resolved*Value` 파생값으로 교체(내부에서 `hiddenKinds`/`writeButtonHidden`/`extraItems`/`extraWriteButtonIds`처럼 최상위 필드를 직접 참조하던 4곳도 함께 교체). `page.tsx`의 캔버스 `<Navbar>` 호출에 이미 최상위 state로 들고 있던 `mainLogoValue`/`sidebarIconsValue`/`topTabStyleValue`/`accountMenuStyleValue`를 그대로 넘긴다 — 이제 Controls에서 뭘 바꾸든 저장 없이 캔버스에 즉시 반영된다.
- **검증**: `npx tsc --noEmit`/`npm run lint`(0 errors)/`npm run build` 전부 통과. 부수적으로 `page.tsx`에서 더 이상 안 쓰이던 `HEADER_MENU_ITEM_KEYS` import와 `sidebarIcons`(미사용 지역 변수) 정리. dev.silostore.net에서 실제 배포 확인은 배포 후 필요(HOTFIX-141.10과 같은 배포 묶음으로 확인 예정).
- **변경 파일**: `src/components/Navbar.tsx`, `src/app/admin/navigation/settings/page.tsx`.

## 2026-08-22 (HOTFIX-141.10 — GIF hover 애니메이션 정지 + 상단 탭 hover 텍스트 실종 + 드롭다운 폭/폰트/색상 설정 + 링크 드래그 순서변경 — 후속: 모바일 드롭다운 잘림/로고 텍스트 실종/상단 사이드바 하위 목록 드래그)
- **배경**: 사용자가 3가지를 추가 요청 — (1) 모바일 preview에서 About Silo/사일로 상점/살롱데상/온라인 도슨트 드롭다운이 잘 안 보임, (2) 모바일 preview에서 메인 로고 좌우 "I'm your"/"SILO" 텍스트가 아예 안 나옴 — 차라리 모바일에서는 독립적으로 드래그해 옮길 수 있게, (3) 상단 사이드바의 각 컬럼 안 요소들도 드래그&드롭으로 움직일 수 있게.
- **(1) 모바일 드롭다운 잘림 — 실측으로 원인 확정**: 관리자 캔버스(390px 모바일 프레임)에서 각 탭의 드롭다운(hidden 상태를 임시로 강제 표시해 실제 레이아웃 위치를 측정)을 조사한 결과, 맨 오른쪽 탭 "온라인 도슨트"의 드롭다운이 화면 오른쪽으로 **102px** 넘쳤고, "사일로 상점"도 6px 넘쳤다 — 전부 `absolute left-0`(트리거 왼쪽 끝 기준, 오른쪽으로 펼침)라 화면 오른쪽에 가까운 탭일수록 넘칠 수밖에 없는 구조였다. `TopTabStyleEntry`에 `dropdownAlign`("left"|"right") 신설 — "right"를 고르면 트리거 오른쪽 끝 기준으로 왼쪽으로 펼쳐(`right-0`/`right-full`) 화면 오른쪽 가장자리 탭에서도 안 잘리게 했다(1차 드롭다운과 2차 하위 카테고리 플라이아웃 둘 다 적용). HOTFIX-141.7의 `dropdownWidthPx`(폭 축소)와 함께 쓰면 완전히 해결된다.
- **(2) 로고 좌우 텍스트 실종 — 진짜 원인은 flex-1 찌그러짐**: `mainLogo.leftText`/`rightText`가 로고 그래픽과 `flex-1 min-w-0` 형제로 같은 `HeaderSlot` 안에 있었다 — 로고 그래픽(`shrink-0`, 안 줄어듦)이 좁은 모바일(390px) 폭에서 남는 공간을 다 차지하면 두 텍스트 컨테이너가 폭 0으로 완전히 찌그러진다(렌더링은 되지만 보이는 픽셀이 없음 — "안 나온다"의 실체). 근본 수정: 좌/우 텍스트를 로고에서 완전히 분리해 각자 독립된 `HeaderSlot`("logo-left-text"/"logo-right-text")으로 만들었다 — 더 이상 flex-1 형제에 눌리지 않고, 다른 모든 헤더 요소와 동일하게 자유 드래그 가능(PC/모바일 각각 독립 위치, 정확히 사용자가 요청한 방식). 로고 자체(crest 그래픽)는 여전히 `flex-1`이라 tier1 탭/계정 영역을 오른쪽으로 미는 기존 역할은 그대로 유지.
- **(3) 상단 사이드바 하위 목록(column 3) 드래그 순서변경**: HOTFIX-141.7이 column 2(관리자 링크 목록)에는 이미 드래그 재정렬(⠿ 그립, 네이티브 HTML5 DnD)을 추가했는데, 그 아래 하위 목록(각 링크에 마우스 올리면 나타나는 column 3)에는 추가/삭제만 있고 순서를 바꿀 방법이 아예 없었다 — 동일한 그립+드래그 패턴을 하위 목록에도 적용.
- **검증**: `npx tsc --noEmit`/`npm run lint`(0 errors, 기존 73개 warning)/`npm run build` 전부 통과. 모바일 드롭다운 오버플로우 수치(102px/6px)는 관리자 캔버스에서 실측(임시로 `display:block` 강제 후 `getBoundingClientRect`)해 확정한 값이고, 로고 텍스트 폭 0 현상도 동일하게 실측 확인 — 수정 코드 자체의 클릭/드래그 재현은 배포 후 필요.
- **변경 파일**: `src/components/Navbar.tsx`, `src/lib/topTabStyleSettings.ts`, `src/app/admin/navigation/settings/page.tsx`.

## 2026-08-22 (HOTFIX-141.9 — live preview가 실제 사이트와 계속 다르던 진짜 이유: 캔버스가 항상 320px 좁게 렌더링되고 있었다)
- **배경**: 사용자가 "스튜디오"/"관리자"를 화면 왼쪽 상단(로고 배너 코너)에 의도적으로 드래그해 배치했는데, 캔버스 안에서 완전히 사라졌다며 "메인로고 섹션이랑 중첩이 안되서 그런거면 가능하게 만들어 제발 근본적인 문제를 해결해"로 강하게 요청. 실측해보니 두 요소 모두 DOM에 정상 존재(`display:block`, `opacity:1`, `z-index:30`, transform도 저장된 값과 정확히 일치)했고, **실제 공개 홈페이지에서는 정확히 원하는 위치(좌상단, 스튜디오 위·관리자 아래)에 잘 보였다** — 즉 데이터도, 렌더링 로직도 멀쩡했다. 문제는 오직 관리자 캔버스 안에서만 발생했다.
- **진짜 근본 원인**: `/admin/navigation/settings`의 레이아웃이 `<div className="flex">`로 왼쪽 Elements/Controls 패널(`w-80` = 320px, flex 형제)과 캔버스(`flex-1`, 남은 공간)가 폭을 나눠 쓰고 있었다 — 즉 캔버스는 항상 **실제 사이트보다 정확히 320px 좁게** 렌더링되고 있었다. `header_positions`의 `dxPx`/`dyPx`는 절대 픽셀값이라, 실제 사이트(전체 폭) 기준으로 계산된 드래그 오프셋을 이 좁아진 캔버스에 그대로 적용하면 왼쪽으로 갈수록 오차가 커진다 — 사용자가 "왼쪽 상단"을 노리고 드래그한 값(-992px, -910px)이 실제 사이트에서는 딱 맞는 위치였지만, 320px 좁은 캔버스에서는 그만큼 더 왼쪽으로 밀려나 **캔버스의 실제 콘텐츠 영역이 아니라 왼쪽 Elements/Controls 패널(사이트의 일부가 아니라 순수 에디터 UI, 불투명한 흰 배경) 밑에 깔려버렸다** — 값 자체는 정확한데 렌더링되는 "무대"의 크기가 실제 사이트와 달라서 생긴 문제였다.
- **왜 이전 5차례(141.5~141.8) 수정이 매번 재발했는지**: 전부 `header_positions`의 값(오프셋/raised 플래그)만 고쳤다 — 그 값이 계산되는 **기준 폭 자체가 실제 사이트와 다르다**는, 데이터가 아니라 레이아웃 구조의 문제였다. 어떤 값으로 리셋하든, 사용자가 다시 왼쪽으로 드래그하는 순간(정당한 사용, 실제 사이트 기준으로는 올바른 조작) 캔버스의 320px 좁은 폭 때문에 다시 패널 밑에 깔릴 수 있는 구조였던 것 — 사용자가 지적한 "근본적인 문제를 회피하고 겉핥기식으로 고쳤다"는 지적이 정확했다.
- **수정**: `/admin/navigation/settings/page.tsx`의 레이아웃을 `flex`(폭 분할) 구조에서 `relative` + `absolute` 오버레이 구조로 전면 교체 — 캔버스 컨테이너는 더 이상 `flex-1`이 아니라 항상 **이 섹션의 전체 폭**을 그대로 쓰고(패널이 있든 없든 캔버스 폭이 변하지 않음, 즉 항상 실제 사이트와 동일한 폭), Elements/Controls 패널은 `position: absolute; inset-y-0 left-0` 오버레이로 캔버스 위에 떠서 왼쪽 320px를 시각적으로만 가린다. 패널 상단에 "◀"(숨기기) 버튼과, 숨겼을 때 다시 부르는 "▶ 패널 보기" 버튼을 추가해 — 필요하면 패널을 완전히 치우고 **실제 사이트와 픽셀 단위로 동일한 폭**의 캔버스를 그대로 확인할 수 있게 했다(왼쪽 끝 근처에 배치한 요소를 점검할 때 특히 유용). 남은 잔차: 이 섹션 자체가 `<main className="... max-w-[1600px] mx-auto ...">` 안에 있어 아주 넓은 화면(1600px+)에서는 여전히 미세한 차이가 남을 수 있음 — 하지만 320px라는 지배적인 오차 원인은 완전히 제거됐다.
- **검증**: `npx tsc --noEmit`/`npm run lint`(0 errors, 기존 73개 warning)/`npm run build` 전부 통과. 실 사용(로그인 세션으로 패널 숨기기 버튼 클릭 후 좌상단 요소 위치 재확인)은 배포 후 필요.
- **변경 파일**: `src/app/admin/navigation/settings/page.tsx`.

## 2026-08-22 (HOTFIX-141.8 — "관리자"/"스튜디오" 클릭 불가 재발의 진짜 근본 원인 — HeaderSlot의 영구 z-index 격상 버그)
- **배경**: HOTFIX-141.5/141.6/141.7에서 세 번 연속 "관리자/스튜디오 클릭 안 됨"을 DB의 극단적 드래그 오프셋 탓으로 보고 매번 값만 리셋했는데, 사용자가 다시 "!!!!"로 신고 — 이번엔 DB를 먼저 확인하니 `account:admin`/`tab:space_inquiry` 둘 다 이미 `{dx:0,dy:0}`였다(직전 수정이 멀쩡히 남아있었음, `updated_at`도 그대로). 즉 오프셋 값 자체는 문제가 아니었다 — **DB 값과 무관하게 재현되는 진짜 코드 버그**가 있었다는 뜻.
- **진짜 원인**: `HeaderSlot.tsx`의 `wrapperStyle`이 `position:relative;z-index:30`을 부여하는 조건이 `moved || (value.raised && editable)`이었다 — `value.raised`는 드래그가 끝날 때마다 `onOffsetChange(..., { raised: true })`로 **영구 저장**되고 그 뒤로 절대 `false`로 되돌아가지 않는다. 즉 "한 번이라도 드래그된 적 있는 슬롯"은 dx/dy를 0,0으로 되돌려도 z-index 격상만 계속 남는다. 이게 왜 하필 "관리자"/"스튜디오"에서만 반복 재현됐냐면 — **로고(logo) 슬롯이 `flex-1`이라 헤더 폭 거의 전체를 차지하는 박스**인데, 로고도 과거에 한 번 드래그된 적이 있어 `raised:true`가 영구히 남아있었다. 로고의 z-30 박스가 눈에는 안 보여도(실제 로고 이미지 크기와 무관하게 flex-1로 할당된 전체 박스가 클릭을 가로챔) 헤더 폭 전체에 깔려있어, 그 밑에 있는(격상 안 된, 또는 stacking context가 더 낮은) 다른 요소를 계속 가렸다 — DB에서 dx/dy만 아무리 0으로 되돌려도 `raised:true`가 남아있는 한 로고의 z-30 박스는 그대로였으므로 매번 재발한 것.
- **실제 재현·확진**: `elementFromPoint`로 캔버스 안 헤더 슬롯 9개를 전수 검사한 결과 "스튜디오"(`tab:space_inquiry`, tier1 — `z-10` 스태킹 컨텍스트 안이라 로고의 z-30에 항상 짐)는 `logo`에, "관리자"(`account:admin`)는 (트리거 아이콘 크기가 여전히 100px로 남아있던) `top-sidebar-trigger`에 가려져 있었다 — 전부 오프셋이 0에 가깝거나 0인데도 발생, 오프셋 문제가 아니라는 확증.
- **수정(코드)**: `HeaderSlot.tsx`의 격상 조건을 `moved || (value.raised && editable)`에서 `moved || (selected && editable)`로 변경 — 이제 "실제로 옮겨진 상태"이거나 "지금 선택된 상태"일 때만 z-index가 올라간다. 과거에 드래그됐던 이력(raised 플래그) 자체는 더 이상 격상 사유가 아니다 — 이 클래스의 버그가 다른 어떤 슬롯 조합에서도 재발할 수 없게 근본적으로 막는다.
- **수정(데이터)**: `header_positions`의 0,0-그리고-raised:true였던 잔여 슬롯(pc: logo/tab:mypage/account:name/account:tier/account:logout/account:mypage/write-button/account:admin/tab:space_inquiry/tab:salon/top-sidebar-trigger, mobile 상당수)을 값 유지가 아니라 **키 자체를 삭제**(진짜 기본값 `{dx:0,dy:0,raised:false}`로 완전히 복귀 — HOTFIX-141.5/141.6이 "raised는 유지"로 했던 판단은 이번 사건으로 틀렸음이 확인돼 뒤집음). `site_settings.top_sidebar`의 `triggerIconSizePx`도 두 기기 다 100/80(예전 테스트 잔재로 계속 방치했던 값)에서 진짜 기본값 20으로 복귀 — 100px짜리 트리거 아이콘이 "관리자"를 물리적으로도 덮고 있었다.
- **GIF 애니메이션 재확인**: DB에서 `top_sidebar.pc.triggerIconHoverUrl`이 `.../gif_rose.webp`로 저장돼 있는 걸 발견 — 파일명은 "gif_rose"인데 확장자가 webp라는 게, HOTFIX-141.7 배포가 완료되기 *전에* 업로드돼 구버전 압축 로직(GIF 첫 프레임만 캡처)을 탔다는 직접 증거. 코드 수정 자체는 이미 배포됐으니, 같은 GIF 파일을 다시 업로드하면 이번엔 애니메이션이 유지될 것 — 기존에 이미 압축돼 저장된 webp는 소급 복구가 안 되므로 재업로드가 필요하다는 걸 확인.
- **검증**: `npx tsc --noEmit`/`npm run lint` 0 errors. dev.silostore.net에서 1280px/1440px 두 뷰포트로 캔버스 안 9개 헤더 슬롯 전수 재검사 — 전부 자기 자신이 클릭 타깃(`elementFromPoint`)임을 확인. **다음에 확인 필요**: `npm run build` 및 실제 마우스 클릭(로그인 세션 필요)으로 최종 재확인, 모바일 쪽 잔여 오프셋(sidebar 제외)도 필요시 동일하게 정리.
- **변경 파일**: `src/components/HeaderSlot.tsx`. DB(Management API): `site_settings.header_positions`(잔여 0,0-raised 슬롯 전체 키 삭제), `site_settings.top_sidebar`(triggerIconSizePx 100/80 → 20).

## 2026-08-22 (HOTFIX-141.7 — GIF hover 애니메이션 정지 + 상단 탭 hover 텍스트 실종 + 드롭다운 폭/폰트/색상 설정 + 링크 드래그 순서변경)
- **배경**: 사용자가 5가지를 한 번에 지적 — (1) 상단 사이드바 hover 이미지가 gif인데 애니메이션이 안 됨(PC/모바일 둘 다), (2) 상단 사이드바 요소들도 드래그&드랍으로 움직일 수 있게 해달라, (3) 여전히 live preview와 실제 사이트가 다르고 "관리자"/"스튜디오" 위치가 이상하다, (4) 모바일에서 상단 탭 드롭다운·하위 카테고리가 좁아서 안 보임 — 폭 조절 + 폰트업로드/크기/색상 설정 + 상단 탭 hover 시 글씨가 아예 안 보이는 버그, (5) 홈페이지 "페이지설정" → Craft 에디터의 live preview가 실제 출력과 매우 다르고 매번 다르게 나옴.
- **(1) GIF 애니메이션 정지 — 내가 만든 회귀**: HOTFIX-141.4에서 대용량 파일 업로드 타임아웃을 막으려고 트리거 아이콘 업로드에 `compressImage()`를 추가했는데, 이 함수는 `createImageBitmap`으로 캔버스에 그려 재인코딩한다 — 브라우저의 `createImageBitmap`은 애니메이션 GIF의 **첫 프레임만** 캡처하므로 결과물이 항상 정지 이미지가 됐다. `compressImage()`에 `file.type === "image/gif"`면 원본을 그대로 통과시키는 분기를 추가 — 이 함수를 쓰는 모든 현재/향후 업로드 경로에 한 번에 적용된다.
- **(3) "관리자"/"스튜디오" 위치 — 이번엔 진짜 라이브 드래그 활동으로 확인**: DB를 다시 조회하니 `updated_at`이 방금(HOTFIX-141.6 이후) 갱신돼 있었고 오프셋 값도 소수점까지 정밀한(`-840.29px`) 실제 드래그 결과물이었다 — 즉 다른 기기/세션에서 실제로 드래그를 시도하다 화면 밖으로 나가버린 것으로 보인다(디버그 데이터 잔재가 아니라 실사용). 다시 `{dx:0,dy:0,raised:true}`로 원위치. **드래그 자체는 이제 정상 동작**(실제 좌표가 저장된다는 게 그 증거) — 다만 드래그 결과가 마음에 안 들면 각 요소 Controls의 "원래 위치로 되돌리기"를 쓰면 된다는 걸 사용자에게 안내할 필요가 있음(HOTFIX-141.6까지 겪은 문제는 "드래그가 안 됨"이 아니라 "어쩌다 화면 밖으로 나간 뒤 클릭조차 안 됨"이었고, 이건 이제 해소).
- **(4-a) 상단 탭 hover 시 글씨 실종 — 진짜 CSS 버그 발견**: `topTabStyleCss` 생성 로직에 `.${cls}:hover { color: #fff !important; }`가 있었다 — 예전에 탭 hover 시 뜨던 진한 초록 배경(green-800)과 대비시키려던 규칙인데, HOTFIX-141이 그 배경 자체를 `.${cls}:hover { background-color: transparent !important; }`로 지워버리면서(호버 모션이 실제로 보이게 하려던 목적) 대비 대상이 사라졌다 — 결과는 흰 배경 위에 강제로 흰 글씨, 완전히 안 보임. 커스텀 서체/크기/굵기/색상 중 하나라도 설정된 탭에서만 발생(그 조건에서만 이 규칙 블록이 만들어짐). 강제 흰색 규칙을 제거 — hover 시엔 이제 hoverMotion(모션 CSS)만 적용된다.
- **(4-b) 드롭다운/하위 카테고리 폭·폰트·크기·색상 설정 신설**: `TopTabStyleEntry`에 `dropdownWidthPx`/`dropdownFontFamily`/`dropdownCustomFonts`/`dropdownFontSizePx`/`dropdownColor` 추가 — 1차 드롭다운(기존 고정 `w-64`)과 2차 하위 카테고리 플라이아웃(기존 고정 `w-56`, `absolute left-full`로 1차 드롭다운 오른쪽에 이어 붙는 구조라 모바일 390px 프레임에서 폭 합이 화면을 넘기기 쉬웠음) 둘 다 이 값을 공유한다. 폰트는 mainLogo customFonts와 동일한 업로드+@font-face 패턴(탭 자체 서체와는 별도 접두사로 독립). 관리자 페이지의 탭 Controls에 "드롭다운 / 하위 카테고리" 섹션 신설(메가 드롭다운이 아닐 때만 표시 — 메가 드롭다운은 다른 레이아웃이라 이 값을 안 씀).
- **(2) 상단 사이드바 링크 드래그 순서 변경**: `TopSidebarControls`의 "링크 목록"에 있던 ↑/↓ 버튼(그대로 유지, 접근성 대안)에 더해 실제 드래그로 순서를 바꿀 수 있게 했다 — 각 링크 행에 "⠿" 그립 아이콘(하단 메뉴 Craft 블록과 동일한 관례)을 추가하고, 그 아이콘만 `draggable`(입력창의 텍스트 선택/드래그를 방해하지 않기 위해 행 전체가 아니라 그립만), 네이티브 HTML5 Drag and Drop(별도 라이브러리 없이 `draggable`/`onDragStart`/`onDragOver`/`onDrop`)으로 구현. 컬럼 순서/너비는 기존처럼 숫자 입력+↑/↓ 버튼 유지(HOTFIX-141 때 "실사이트 전역에 뜨는 메가메뉴에 검증 안 된 픽셀 드래그를 얹는 위험" 판단은 그대로 — 이번엔 세로 리스트 reorder만 범위로 좁혔다).
- **(5) Footer Craft 에디터 live preview 불일치 — 이번 커밋에는 미포함**: 원인 조사를 시작하지 못했다 — Craft.js의 상태 직렬화/하이드레이션 문제는 위 항목들과 성격이 달라(과거 EPIC-137.6에서 `addNodeTree`의 노드 id 재사용 문제를 리버스엔지니어링으로 확인한 전례가 있음) 서둘러 얹기보다 별도로 제대로 조사할 필요가 있다고 판단해 이번 범위에서 뺐다.
- **검증**: `npx tsc --noEmit`/`npm run lint`(0 errors, 기존 73개 warning)/`npm run build` 전부 통과. dev.silostore.net에서 관리자/스튜디오 오프셋 재초기화는 DB로 직접 확인. GIF 압축 스킵/hover 텍스트 색상 버그/드롭다운 폭·폰트/링크 드래그는 코드 리뷰로 검증(로그인 세션이 다시 필요한 실제 클릭·드래그 재현은 배포 후 사용자 확인 필요).
- **변경 파일**: `src/lib/adminImageUpload.ts`, `src/components/Navbar.tsx`, `src/lib/topTabStyleSettings.ts`, `src/app/admin/navigation/settings/page.tsx`. DB(Management API): `site_settings.header_positions`(account:admin/tab:space_inquiry 재초기화).

## 2026-08-22 (HOTFIX-141.6 — "관리자"/"스튜디오" 여전히 클릭 안 되던 문제 — 이번엔 뷰포트 너비 전체에서 재검증)
- **배경**: HOTFIX-141.5로 고쳤다고 보고했는데 사용자가 "'관리자' 가 클릭이 안된다고 !!! '스튜디오' 도 클릭도 안되고 드롭다운도 안나와"로 재신고. 원인은 검증 방법 자체의 허점이었다 — HOTFIX-141.5는 이 Browser pane의 기본 뷰포트(784px)에서만 `elementFromPoint`로 확인했는데, 이 문제의 진짜 성격이 **뷰포트 너비에 따라 달라지는 겹침**이라는 걸 놓쳤다. `header_positions`의 `dxPx`/`dyPx`는 절대 픽셀값이라 반응형 레이아웃에서 뷰포트가 넓어지면 요소들의 "원래" 위치가 옆으로 밀리는데, 드래그된(raised) 요소는 그 절대 오프셋만큼 고정으로 이동해 있어 너비에 따라 전혀 다른 요소와 겹칠 수 있다 — 실제로 뷰포트를 1440px로 넓히자 이번엔 "스튜디오"가 (지난번 고친 account:admin이 아니라) **로고**(`dx:176.8px`, 아직 안 건드림)에 완전히 가려져 있었다(`elementFromPoint` 3개 지점 전부 `topSlot:"logo"`).
- **근본 판단 변경**: 지금까지 "명백히 극단적인 값"(수백 px)만 골라 지웠는데, 이번 건으로 "적당히 커 보이는 값"(176px, 47px, -111px 등)도 뷰포트 너비에 따라 언제든 다른 요소를 가릴 수 있다는 게 실증됐다 — 즉 라이브 사이트 전역에 노출되는 이 헤더 슬롯 시스템에서, 관리자가 실제로 의도해서 넣었다고 믿을 근거가 없는 이런 절대 픽셀 드래그 값은 전부 같은 위험군으로 취급해야 한다. `sidebar:left`/`sidebar:right`(dy 380~400px대, 헤더 아래로 완전히 밀어내는 용도가 명백)와 `top-sidebar-trigger`(dy 30px대, 작고 위치 조정 성격이 뚜렷)만 남기고, 나머지 탭/계정 영역 슬롯의 잔여 오프셋(pc: `logo`/`tab:mypage`/`account:name`/`account:tier`/`account:logout`/`account:mypage`, mobile: `logo`/`tab:salon`/`tab:docent`/`tab:mypage`/`tab:silostore`/`account:logout`/`tab:877a1576-...`)까지 전부 `{dxPx:0,dyPx:0,raised:true}`로 초기화했다(HOTFIX-141.5와 동일하게 raised는 유지).
- **검증 방식 자체를 고침**: 이번엔 뷰포트 하나가 아니라 1280/1440/1920px 세 너비에서 캔버스 안의 **모든** `data-header-slot` 요소를 순회하며 각자의 중심점에서 `elementFromPoint`가 자기 자신을 가리키는지 전수 확인(이전처럼 신고받은 두 요소만 확인하는 방식은 다음 번에도 "다른 너비의 다른 요소"를 놓칠 수 있어 폐기) — 세 너비 전부 9개 슬롯 전원 통과. "스튜디오" 클릭 시 실제로 "상단 탭 / 메뉴 항목" Controls 패널(복제/삭제/노출 위치/hover 모션 등)이 뜨는 것도 재확인.
- **여전히 남는 것**: hover로 뜨는 드롭다운 flyout 자체(`group-hover/tab:block` CSS)는 이 자동화 도구의 합성 마우스 이동으로 실제 hover 상태를 재현하지 못해(이 프로젝트에서 여러 번 문서화된 환경 한계) 육안으로는 최종 확인 못 했다 — 다만 클릭 가능해졌다는 것 자체가 "마우스가 그 자리에 실제로 닿을 수 있다"는 뜻이라, hover 대상이 화면 밖/다른 요소에 가려 있었다는 원래 원인은 확실히 해소됐다.
- **변경 파일**: 없음(DB만).

## 2026-08-22 (HOTFIX-141.5 — "관리자" 클릭 불가 + "스튜디오" 드롭다운 안 뜨는 버그 수정, HOTFIX-141.4가 만든 회귀)
- **배경**: 사용자가 "홈페이지 설정에 '관리자'가 클릭이 안되는데? '스튜디오'도 hover 해도 드롭다운이 안나와"로 신고. 둘 다 바로 직전 HOTFIX-141.4에서 내가 만든 회귀였다.
- **"관리자" 원인**: HOTFIX-141.4에서 `header_positions.pc/mobile.slots["account:admin"]`의 극단적 오프셋(-694px 등)을 지우면서 그 항목의 `raised:true`까지 통째로 없앴다 — `HeaderSlot.tsx`는 `raised`가 있어야만 `position:relative;z-index:30`을 얻는데, 이게 없어지자 "관리자"는 평범한 static 요소가 됐다. 반면 로고(`logo` 슬롯)는 여전히 `dx:176.8px`만큼 오른쪽으로 드래그된 채 `raised:true`(z-30)를 유지하고 있어, 로고의 트랜스폼된 박스가 "관리자" 자리 위로 겹쳐 클릭을 가로챘다(`document.elementFromPoint`로 실측 확인 — "관리자" 텍스트 위치에서 실제로 이벤트를 받는 건 `data-header-slot="logo"` DIV였다).
- **"스튜디오" 원인**: "스튜디오"의 내부 키가 `space_inquiry`(EPIC-019 이전 "공간 문의")라는 걸 다시 확인 — 바로 이전 세션에서 발견해놓고 "이번 신고 범위 밖"이라 일부러 안 건드렸던 `tab:space_inquiry`의 `dx:-615.8px`가 바로 이 탭이었다. 화면 x좌표가 -154px로 실측돼(뷰포트 완전히 밖) 마우스를 올릴 대상 자체가 없었다 — hover 드롭다운 자체(`group-hover/tab:block` CSS)는 멀쩡했다.
- **수정**: 이번엔 오프셋 항목을 통째로 지우지 않고 `{dxPx:0, dyPx:0, raised:true}`로 값만 초기화 — 위치는 원래대로 돌아가되 z-index 우위는 그대로 유지해 다른 raised 요소에 다시 가려지지 않는다. `account:admin`/`write-button`(pc+mobile) + `tab:space_inquiry`(pc) 대상. DB만 수정, 코드 변경 없음.
- **검증**: `dev.silostore.net`에서 실제 로그인 세션으로 재확인 — `document.elementFromPoint`가 "관리자"/"스튜디오" 둘 다 자기 자신의 `<a>` 요소를 정확히 가리킴(클릭 가능 확정), "스튜디오"가 화면 안(x:462)으로 돌아옴, 드롭다운 flyout의 `group-hover/tab:block` 클래스가 손상 없이 그대로 있음을 DOM으로 확인(실제 마우스 hover 렌더링까지는 이 자동화 도구의 합성 hover 한계로 육안 재현은 못 함 — 이 프로젝트에서 이미 여러 번 확인된 환경 제약).
- **교훈**: `header_positions` 슬롯을 "치우는" 리팩터는 앞으로 `raised:true`를 유지한 채 `dxPx/dyPx`만 0으로 되돌리는 방식을 기본으로 한다 — 통째로 삭제하면 이번처럼 다른 raised 요소에 가려지는 회귀가 재발할 수 있다.
- **변경 파일**: 없음(DB만).

## 2026-08-22 (HOTFIX-141.4 — 글쓰기/드래그 오프셋 버그 수정 + 실제 드래그 스냅 + 로그인/로그아웃을 상단 사이드바로 이동)
- **배경**: 사용자가 4가지를 한 번에 지적 — (1) 홈페이지 설정 관리의 live preview와 실제 웹사이트 출력이 다르다, (2) 드래그 시 'center' 맞추기·다른 요소 align이 "가능하게" 해달라, (3) "글쓰기"와 "마이페이지 (드롭다운)" 요소가 사라졌다(마이페이지는 본인이 노출 옵션을 다 해제해서일 수 있지만 "설정"에는 보여야 한다), (4) 모바일 live preview에 "관리자"/"글쓰기"/"마이페이지 (드롭다운)" 3개가 안 보인다. 여기에 더해 로그인/로그아웃 버튼을 계정 영역에서 없애고 상단 사이드바로 옮기면서 폰트 업로드/크기/색/hover 모션까지 직접 설정하고 싶다는 신규 요청.
- **글쓰기 버튼이 사라진 진짜 원인**: `site_navigations`에서 "마이 페이지 My Page" 행의 `target_types`가 `[]`(완전히 빈 배열)로 저장돼 있었다(Management API로 직접 확인) — 즉 1단/2단 탭 어디에도 노출 안 되게 설정된 상태. 그런데 "글쓰기" 버튼은 `Navbar.tsx`의 `renderTab()` 안에서 `tab.key === "mypage"`인 탭이 실제로 렌더링될 때만 그 옆에 끼워 넣는 구조였다(EPIC-084-REVISED 때부터) — "마이 페이지" 탭 자체가 하나도 렌더링되지 않게 되자 부수 효과로 글쓰기 버튼까지 통째로 사라졌다. **두 UI가 이름만 같을 뿐 실제로는 완전히 다른 요소**(하나는 site_navigations 탭, 하나는 로그인 세션에 묶인 계정 영역 버튼인 "마이페이지 (드롭다운)")라는 걸 사용자가 헷갈릴 만한 설계였다. `writeButtonEl`을 `renderTab()` 밖으로 완전히 분리해 특정 탭의 존재 여부와 무관하게 항상 한 번(tier2Tabs 뒤) 렌더링하도록 고쳤다 — 이미 자체 드래그 위치(`write-button` slotKey)가 있어 탭에 안 붙어도 관리자가 원하는 자리로 옮길 수 있다.
- **"마이페이지" 계정 영역 항목**: 실제로 확인해보니 Elements 탭 → "마이페이지" 클릭 → Controls의 "숨긴 항목 → 마이페이지 복원" 버튼으로 이미 접근 가능했다(hiddenKinds에 들어있어도 Elements 리스트 자체는 하드코딩이라 사라지지 않음) — 이 부분은 버그가 아니라 기존에 이미 있던 정상 동작이었음을 확인.
- **live preview가 실제 사이트와 다르고, 모바일에서 "관리자"/"글쓰기"가 안 보이던 원인**: `site_settings.header_positions`를 직접 조회해보니 `account:admin`이 PC에서 `dx:-694px`, 모바일에서 `dx:-275.8px`, `write-button`이 PC에서 `dx:-624px`, 모바일에서 `dx:-207px`나 드래그돼 있었다 — 과거 세션들의 드래그 검증/테스트 흔적이 원상복구 안 된 채 남아있던 것으로 보인다. `HeaderSlot`의 `transform: translate(dx,dy)`는 editable 여부와 무관하게 항상 적용되므로 실제 사이트에도 동일하게 반영되지만, 모바일 캔버스(390px, `overflow-hidden`)에서는 이 정도 오프셋이면 완전히 프레임 밖으로 밀려나 안 보이고, PC 캔버스는 훨씬 넓어 덜 두드러지되 실제 전체 너비 사이트와는 다르게 보였다 — "live preview가 실제와 다르다"는 증상의 상당 부분이 바로 이 데이터였다. Management API로 이 4개 오프셋만 정확히 제거(다른 슬롯은 사용자가 의도적으로 조정했을 수 있어 손대지 않음 — `tab:space_inquiry`의 `dx:-615.8px`도 비슷하게 의심스럽지만 이번 신고 범위 밖이라 보류, NEXT_TASK.md에 기록).
- **드래그 가이드라인 실제 스냅**: HOTFIX-141.1은 캔버스 중앙/다른 요소와 몇 픽셀 이내로 가까워지면 안내선만 그리고 실제 위치는 보정하지 않았다(강제로 튕기면 먹통처럼 느껴질까 봐 의도적으로 뺐던 것) — 이번엔 사용자가 명시적으로 "가능하게 해달라"고 요청해 `HeaderSlot.tsx`의 `moveDrag`에 실제 스냅을 추가했다. 반경(6px) 안에 들어오면 그 축(세로/가로)에 정확히 달라붙고, 안내선도 그 자리에 뜬다 — 여러 후보가 동시에 걸리면 가장 가까운 것 하나에만 붙는다(여러 축이 동시에 당기면 어디 붙었는지 헷갈리므로).
- **로그인/로그아웃을 상단 사이드바로 이동**: `topSidebarSettings.ts`에 `LoginButtonStyle`(fontFamily/customFonts/fontSizePx/color/hoverMotion) 신설, `TopSidebarPanel.tsx` column 1 맨 위에 항상 표시(로그인 상태면 로그아웃 버튼 — `Navbar.tsx`의 `handleLogout`과 동일하게 `supabase.auth.signOut()` 후 `/`로 이동, 아니면 `/login` 링크)되도록 추가 — 이 버튼만의 독립된 폰트 파일 업로드(mainLogo의 customFonts와 동일한 @font-face 주입 패턴)/글자 크기/색상/hover 모션(6종 프리셋)을 관리자 페이지에 신설. 계정 영역의 기존 "account:logout"은 삭제하지 않고 `accountMenuStyleValue.hiddenKinds`에 `"logout"`을 추가해 기본 숨김 처리(다른 계정 영역 항목들과 동일한 숨김/복원 모델 재사용 — 원하면 언제든 "복원" 버튼으로 되돌릴 수 있음), Elements 리스트 라벨과 Controls 안내 문구에 이동 사실을 명시.
- **검증**: `npx tsc --noEmit`/`npm run lint`(0 errors, 기존 73개 warning 그대로)/`npm run build` 전부 통과. 이번엔 `dev.silostore.net`에 실제 로그인된 관리자 세션이 Browser pane에 남아있어 Elements/Controls 클릭 다수를 직접 확인(글쓰기/마이페이지 Elements 항목 클릭 → Controls 정상 표시, hiddenKinds 상태의 "숨긴 항목" 복원 버튼 목록 확인) — 단 이번 세션에서 새로 만든 코드(글쓰기 분리 렌더링, 드래그 스냅, 로그인 버튼 이동)는 로컬 dev 서버(별도 오리진이라 세션 공유 불가, 세션 토큰을 다른 오리진에 주입하는 것은 안전 정책상 시도하지 않음)에서는 클릭 검증을 못 했다 — `develop`/`main` 배포 후 `dev.silostore.net`에서 재확인 필요.
- **변경 파일**: `src/components/Navbar.tsx`, `src/components/HeaderSlot.tsx`, `src/components/TopSidebarPanel.tsx`, `src/lib/topSidebarSettings.ts`, `src/app/admin/navigation/settings/page.tsx`. DB(Management API): `site_settings.header_positions`(오프셋 4건 제거), `site_settings.account_menu_style`(hiddenKinds에 logout 추가).

## 2026-08-21 (HOTFIX-141.3 — Supabase Storage 업로드가 한글/공백 포함 파일명에서 "Invalid key"로 실패하던 버그 수정)
- **배경**: HOTFIX-141.2로 추가한 alert 덕분에 실제 에러가 확인됨 — `dev.silostore.net`에서 사용자가 상단 사이드바 트리거 "기본 이미지"를 업로드하자 `Invalid key: top_sidebar_trigger/1787339581367-ChatGPT Image 2026년 8월 22일 오전 03_59_23.webp`. 같은 업로드에서 "호버 이미지"는 정상 적용됐는데(영문 파일명), "기본 이미지"만 실패한 이유가 바로 파일명 차이였다.
- **근본 원인**: `src/lib/adminImageUpload.ts`의 `uploadImage()`가 저장 경로를 `${folder}/${Date.now()}-${file.name}`으로, 업로드한 파일의 원본 이름을 그대로 이어붙여 만든다 — Supabase Storage는 S3 호환 오브젝트 키라 한글이나 일부 비-ASCII 공백문자(예: ChatGPT 내보내기 파일명이 자주 쓰는 좁은 공백)가 섞인 키를 "Invalid key"로 거부한다. `uploadImage()`는 로고/슬라이드/사이드바 아이콘/링크 이미지/커스텀 폰트 등 관리자 페이지 전체 업로드가 공유하는 함수라, 한글·공백이 든 파일명을 올리는 모든 업로드 경로가 동일하게 실패할 수 있는 구조적 버그였다(HOTFIX-141.2가 이 실패를 alert로 보여주게 만들지 않았다면 원인 파악 자체가 훨씬 오래 걸렸을 것).
- **수정**: `uploadImage()` 안에 `sanitizeFileName()`을 추가해 확장자는 보존하고 나머지는 영숫자/`-`/`_`만 남기도록 정규화한 뒤 저장 경로를 만든다 — 원본 파일명은 UI 어디에도 노출되지 않는 내부 저장 키일 뿐이라 사람이 매번 파일명을 영문으로 바꿔 재업로드할 필요가 없어졌다. 한 곳만 고쳐서 이 함수를 쓰는 모든 업로드 경로에 동시에 적용된다.
- **검증**: `npx tsc --noEmit`/`npm run lint` 0 errors(경고 73개 그대로). 사용자가 실제 `dev.silostore.net`에서 재현한 alert 메시지를 근거로 원인을 확정했고, 정규화 로직 자체는 코드 리뷰로 검증(한글/공백/특수문자 제거 + 확장자 보존 케이스 확인) — **다음에 확인 필요(배포 반영 후)**: 원래 실패했던 "ChatGPT Image..." 파일을 다시 업로드해 실제로 성공하는지, 파일명이 온통 비-ASCII라 정규화 후 base가 빈 문자열이 되는 극단적 케이스(현재 `"file"`로 폴백 처리됨)도 실사용에서 문제없는지.
- **변경 파일**: `src/lib/adminImageUpload.ts`.

## 2026-08-21 (HOTFIX-141.2 — 상단 사이드바 여닫이 트리거 아이콘 업로드가 preview에 반영 안 되던 버그 수정)
- **배경**: 사용자가 "상단 사이드바의 '여닫이 트리거 아이콘' 이 둘다 업로드를 새로 해도 preview 왼쪽 설정창의 preview 도 오른쪽의 live preview 에도 적용이 안돼고 있어"로 신고.
- **원인 진단**: 코드 리뷰만으로는 재현이 안 돼(로그인 세션이 없는 세션이라 직접 클릭 검증 불가) DB를 직접 조회해 근거를 확보했다 — `storage.objects`를 조회해보니 사용자가 방금 올린 1.6MB PNG는 실제로 스토리지에 저장까지 됐는데(서버는 성공), `site_settings.top_sidebar`의 `triggerIconDefaultUrl`/`triggerIconHoverUrl`은 훨씬 이전(184KB 테스트 이미지)에서 전혀 갱신되지 않은 채였다 — 즉 **업로드는 성공했는데 화면에 반영하는 `patch()`가 한 번도 안 불렸다**는 뜻. 사용자에게 "선택 직후부터 안 보였는지"/"콘솔에 에러가 떴는지" 확인 요청 → "그 순간부터 안 보임" + "에러 없음" 답변으로, 큰 파일 업로드가 클라이언트 쪽에서만 조용히 실패(타임아웃 등, 서버는 이미 받은 뒤)하는 전형적 패턴임을 확정. `handleTriggerIconFile`(그리고 같은 컴포넌트의 `handleLinkImage`/`handleAddBankImage`)이 `uploadImage()`의 `error`를 한 번도 확인하지 않고 `if (url)`만 봐서 실패 시 아무 피드백 없이 조용히 끝나던 게 근본 원인 — HOTFIX-134.2/137.3이 슬라이드쇼·여백 배경 업로드에 적용했던 "실패 시 alert" 원칙이 HOTFIX-141에서 새로 추가된 이 3개 핸들러에는 애초에 적용된 적이 없었다.
- **수정**: `handleTriggerIconFile`은 업로드 전 `compressImage(file, 85)`로 재인코딩(트리거 아이콘은 헤더에 작게 뜨는 여닫이 버튼이라 원본 해상도가 필요 없음 — 여백 배경 이미지 업로드와 동일한 패턴, 큰 파일의 타임아웃 위험 자체를 줄인다), 3개 핸들러(`handleTriggerIconFile`/`handleLinkImage`/`handleAddBankImage`) 전부 `try/catch` + 실패 시 `alert`로 사유를 보여주도록 통일.
- **검증**: `npx tsc --noEmit`/`npm run lint` 0 errors(경고 73개 그대로, 무관한 파일). **이 세션도 로그인 세션이 없어 실제 업로드 재현 클릭 검증은 못 했다** — 대신 (1) Management API로 스토리지·DB 상태를 직접 대조해 원인을 실증적으로 확정, (2) 사용자의 실시간 답변(콘솔 무에러 + 즉시 미반영)으로 "조용한 업로드 실패" 가설을 좁혔다. **다음에 확인 필요(로그인 세션 필수)**: 실제로 다시 업로드해 (a) 압축 후 정상적으로 preview에 반영되는지, (b) 만약 여전히 실패한다면 이제는 alert로 뜨는 실제 에러 메시지가 무엇인지(타임아웃이 아닌 다른 원인일 가능성 포함) 확인.
- **변경 파일**: `src/app/admin/navigation/settings/page.tsx`.

## 2026-08-21 (HOTFIX-141.1 — 상단 사이드바 아이콘 크기/컬럼1→2 이동, '노출위치'에 상단 사이드바 추가, hover 모션 5종 추가, 모바일 프레임 클리핑 버그, 드래그 정렬 안내선, site_navigations target_type NOT NULL 버그, About Silo/Silo Planet 페이지 분리)
- **배경**: EPIC-141 직후 사용자가 이어서 다수의 버그/기능을 지적 — 상단 사이드바 아이콘 크기, column 1(Mind Diary 등) → column 2 이동, hover 모션 라이브 프리뷰/모션 5종 추가, '노출위치'에 '상단 사이드바' 추가 + '드롭다운' 의미 질문, 모바일에서 좌우 사이드바/상단 사이드바 컬럼이 잘려 안 보임, 캔버스 드래그 정렬 안내선, 사이트 메뉴 "미분류 페이지" 추가 시 DB 오류, About Silo의 3D 우주를 새 Silo Planet 페이지로 분리, 상단 사이드바도 바깥 클릭 시 닫히게.
- **DB 버그 수정(즉시 적용) — 사이트 메뉴에 새 카테고리 추가 시 `"null value in column "target_type"... violates not-null constraint"`**: `site_navigations.target_type`(EPIC-138 이전 구버전 단일값 컬럼)이 `target_types`(배열)로 완전히 대체된 뒤에도 NOT NULL 제약이 남아있어, 이제 아무도 안 쓰는 이 컬럼에 값을 안 채우는 모든 INSERT가 실패하고 있었다 — `alter table site_navigations alter column target_type drop not null` 한 줄로 해결(CHECK 제약은 NULL을 자동 통과시켜 그대로 둬도 안전).
- **'드롭다운' 노출 위치가 뭔지 답변**: 하위 항목이 있는 카테고리를 상단탭에 올렸을 때 hover/클릭 시 드롭다운 메뉴로 펼칠지 여부 — 체크 안 하면 하위 항목이 있어도 평범한 링크로만 보인다(코드로 정확한 의미 확인 후 답변).
- **모바일 프리뷰에서 좌우 사이드바/상단 사이드바 컬럼이 잘려 보이던 버그**: 진짜 원인은 관리자 페이지의 390px 모바일 프레임(`admin/navigation/settings/page.tsx`)에 `overflow-hidden`이 없었던 것 — 닫힌 사이드바 패널(자기 너비만큼 왼쪽으로 translate)이나 데스크톱 폭 그대로인 상단 사이드바(`max-w-5xl`)가 프레임 밖으로 그대로 삐져나와 보였다. 프레임에 `overflow-hidden` 추가(사이드바 완전히 안 보이게 해결) + `TopSidebarPanel.tsx`의 컬럼 줄에 `overflow-x-auto` 추가(좁은 화면에서 옆으로 밀어 나머지 컬럼에 닿을 수 있게).
- **상단 사이드바 트리거 아이콘 크기 조절**: `TopSidebarConfig.triggerIconSizePx` 신설(PC/모바일 독립 설정이라 모바일 크기도 자동으로 별도 관리됨), Controls에 숫자 입력 추가.
- **column 1(Mind Diary/Studio/Silo Planet) → column 2 이동**: 이 3개는 `TopSidebarPanel.tsx`에 하드코딩된 값이라 실제 드래그로 옮길 데이터가 아니다 — `hiddenFixedLinkHrefs` 필드 + "column 2로 이동" 버튼으로 column 1에서 숨기고 column 2(admin이 편집하는 links 배열)에 동일한 항목을 추가하는 방식으로 구현(결과적으로 "옮긴"것과 동일 효과).
- **hover 모션 5종 추가(총 11종 + 없음)**: 기울임+자간(빈티지 인쇄체)/이중 밑줄(전시 라벨풍)/모서리 브래킷(전시 액자풍)/은은한 페이드 이동/샤인 스침 효과 — `tabHoverMotion.ts`에 CSS 프리셋 추가, 상단탭/상단 사이드바/계정 영역/좌우 사이드바 전부 자동으로 선택 가능해짐(공용 `TAB_HOVER_MOTIONS` 배열을 쓰므로).
- **'노출위치'에 '상단 사이드바' 추가**: `DbTargetType`에 `top_sidebar` 신설(두 Controls 화면 — `admin/navigation/settings`와 `사이트 구성 관리`의 `CategoryTreeManager.tsx` — 양쪽 다 반영), `navConfig.ts`의 `buildNavTree`가 `userMenuItems`와 동일한 패턴으로 `topSidebarItems`를 만들어 `NavData`에 추가, `Navbar.tsx`가 이 태그된 카테고리들을 admin이 수동으로 만든 top-sidebar 링크 뒤에 렌더링 시점에만 이어붙인다(저장 시 site_settings.top_sidebar를 오염시키지 않음).
- **드래그 정렬 안내선(align guideline)**: `HeaderSlot.tsx`의 드래그 중 드래그되는 요소의 중심이 (1) 캔버스 가로 중앙, (2) 다른 요소들의 중심과 4px 이내로 가까워지면 분홍 안내선을 그린다(자동 스냅은 하지 않고 순수 시각 피드백만 — 강제로 위치를 튕기면 드래그가 먹통처럼 느껴질 위험 회피).
- **상단 사이드바도 좌우 사이드바처럼 바깥 클릭 시 닫힘**: `TopSidebarPanel.tsx`에 `pointerdown` 기반 바깥 클릭 감지 추가(트리거 버튼은 `data-top-sidebar-trigger` 마커로 식별) — 이전엔 ✕ 버튼/Escape 말고는 닫는 방법이 실제 사이트에도 전혀 없었다(L/R 사이드바와 달리 backdrop조차 없었음).
- **About Silo/Silo Planet 페이지 분리**: EPIC-113이 `/about-silo`에 정적 라우트 오버라이드를 얹어 3D 우주로 완전히 대체했던 것을 되돌린다 — `src/app/about-silo/page.tsx` 삭제로 `/about-silo`가 다시 catch-all(`[...slug]/page.tsx`)의 `page_builder` 렌더링을 받는다. DB 확인 결과 hero/quote/board/gallery 모듈이 있는 기존 `page_builder` 행(`published`)이 그대로 살아있어 데이터 마이그레이션 없이 "예전처럼 갤러리 게시판과 페이지 수정이 뜨는 페이지"가 즉시 복원됐다. 3D 우주는 새 `src/app/silo-planet/page.tsx`로 옮기고 `AboutSiloUniverse.tsx`의 화면 표시 이름도 "About Silo" → "Silo Planet"으로 정정, `TopSidebarPanel.tsx`의 고정 링크 href도 `/silo-planet`로 갱신.
- **검증**: `npx tsc --noEmit`/`npm run lint`(0 errors, 신규 warning 없음)/`npm run build`(About Silo 라우트 삭제로 생긴 stale Next.js 타입 캐시는 `.next` 삭제 후 재빌드로 해결, 이후 클린) 전부 통과. **이번엔 관리자 로그인 세션이 브라우저에 이미 남아있어(사용자가 실제로 이 세션 사이 시간에 로그인해 직접 테스트한 것으로 보임) Browser pane으로 관리자 Controls까지 직접 확인할 수 있었다**: `/about-silo`가 실제 게시글이 있는 보드+갤러리로 정상 복원됨을 페이지 텍스트로 확인, `/silo-planet`이 "SILO PLANET" 표시명과 함께 3D 우주를 정상 렌더링함을 확인, 상단 사이드바 Controls에서 컬럼 너비/순서·트리거 아이콘 크기·column1→2 이동 버튼·11종 hover 모션 전부 정상 렌더링 확인, 계정 영역 항목의 복제/삭제/숨긴 항목 복원 UI 정상 확인. **뜻밖의 발견**: DB에 이미 `hiddenKinds`/`hoverMotion`/`top_sidebar_trigger` 업로드 이미지 등 사용자가 실제로 저장한 값이 들어있어, EPIC-141의 새 필드들이 저장→새로고침 왕복까지 실제로 정상 동작하고 있음을 코드 변경 없이 그대로 확인했다. 반복적으로 뜨는 403(P2, 세션 내내 모든 페이지에서 동일하게 재현되는 원인 불명 노이즈, 이번 변경과 무관)은 기존과 동일하게 보류.
- **변경 파일**: `src/components/HeaderSlot.tsx`, `src/components/Navbar.tsx`, `src/components/TopSidebarPanel.tsx`, `src/components/about-silo/AboutSiloUniverse.tsx`, `src/components/admin/CategoryTreeManager.tsx`, `src/lib/navConfig.ts`, `src/lib/tabHoverMotion.ts`, `src/lib/topSidebarSettings.ts`, `src/app/admin/navigation/settings/page.tsx`, `src/app/about-silo/page.tsx`(삭제), `src/app/silo-planet/page.tsx`(신규). **DB**: `site_navigations.target_type` NOT NULL 제거.

## 2026-08-21 (EPIC-141 — 좌우 사이드바 자유 설정+드래그 이동, 상단 사이드바 아이콘/폰트/컬럼 편집, hover 모션 마스킹 버그 3건 수정, 전역 실행취소, 계정 영역 복제/삭제)
- **배경**: 사용자가 한 번에 다수의 버그/기능을 지적 — (1) 좌/우 사이드바를 열어도 안의 요소를 상단 사이드바처럼 자유롭게 설정할 수 없음 + 바깥 클릭 시 안 닫힘, (2) 상단 사이드바 트리거 아이콘 이미지 설정이 없음, (3) 상단 사이드바 "링크 hover 모션"을 바꿔도 적용이 안 됨 + 서체 파일 업로드가 없음, (4) 상단 사이드바 컬럼 너비/순서를 드래그로 조절하고 싶음, (5) "홈페이지 설정 관리"에서 글쓰기/관리자/등급/이름/마이페이지 같은 "사용자 메뉴" 요소들의 복제/삭제가 안 됨, (6) 실행취소(Ctrl+Z)가 작동 안 함, (7) 상단탭 hover 모션도 (3)과 같은 증상, (8) 좌우 사이드바 아이콘도 드래그로 위치 조정하고 싶음.
- **좌우 사이드바 바깥 클릭 시 닫힘(HOTFIX-141a)**: `Navbar.tsx`의 어두운 backdrop이 편집 캔버스에서는 의도적으로 꺼져 있어(전체 화면을 덮으면 왼쪽 Controls 패널까지 가려버리므로) 실제 사이트에서만 바깥 클릭이 먹혔다 — `LeftSidebar.tsx`/`RightSidebar.tsx`에 `editable`일 때만 동작하는 `pointerdown` 문서 리스너를 추가, 패널/트리거 버튼 내부는 `ref.contains()`로 제외하고 그 바깥(Controls 패널 클릭 포함)이면 닫는다.
- **hover 모션이 "적용 안 됨" 버그의 진짜 원인과 수정(HOTFIX-141b)**: 상단탭·상단 사이드바 링크·계정 영역 항목이 전부 `hover:underline` 또는 `hover:bg-green-800`(그리고 드롭다운 탭의 `group-hover/tab:bg-green-800`) 같은 하드코딩된 Tailwind hover 클래스를 갖고 있어, 관리자가 6종 모션 중 뭘 골라도 이 고정된 시각 효과가 항상 그대로 나타나 "안 바뀌는 것처럼" 보였다(모션 CSS 자체는 정상 주입되고 있었음 — 마스킹 문제였다). `TopSidebarPanel.tsx`의 컬럼2/3 링크와 `Navbar.tsx`의 계정 영역 5항목에서 하드코딩 `hover:underline`을 제거해 `hoverMotion`(기본값 underline-glow)이 전담하게 했고, 탭(`topTabStyleCss`)에는 `.${cls}:hover { background-color: transparent !important; border-color: transparent !important; }` 규칙을 추가해 배경 전환이 모션을 가리지 못하게 했다.
- **상단 사이드바 트리거 아이콘 이미지 + 서체 파일 업로드(HOTFIX-141c)**: `TopSidebarConfig`에 `triggerIconDefaultUrl`/`triggerIconHoverUrl`(sidebarIconsSettings.ts와 동일한 기본/hover 크로스페이드 패턴, `Navbar.tsx`의 "☰" 버튼에 적용) + `customFonts`(mainLogo/topTabStyle과 동일한 폰트 파일 업로드+활성 토글, `TopSidebarPanel.tsx`가 @font-face 주입) 필드 신설, `TopSidebarControls`에 두 섹션 추가.
- **좌우 사이드바 "자유 설정" + 아이콘 드래그 이동(HOTFIX-141d)**: `SidebarIconsConfig`에 `left/rightPanelBackgroundColor`·`left/rightPanelTextColor`·`left/rightPanelFontFamily`·`left/rightPanelHoverMotion`(TopSidebarConfig와 동일한 4개 필드, 좌/우 독립) 신설 — 패널 "안의 콘텐츠"(카테고리 그룹/링크) 자체는 여전히 site_navigations가 담당한다(HOTFIX-140.2에서 이미 내린 "이원화하지 않는다" 결정 유지, 이번엔 스타일 레벨 파리티만 추가). 좌/우 아이콘 위치는 HeaderSlot과 동일한 `headerPositions.slots` 오프셋 시스템을 그대로 재사용(`slotKey: "sidebar:left"/"sidebar:right"`)하되, 트리거 버튼 자신이 이미 absolute/fixed로 스스로 배치돼 있어 HeaderSlot으로 감싸면 containing block이 깨지는 문제 때문에 감싸지 않고 같은 pointer-capture 드래그 로직만 이식해 버튼 자신을 직접 드래그하게 했다.
- **계정 영역(관리자/등급/마이페이지/이름/로그아웃) + 글쓰기 버튼 복제/삭제(HOTFIX-141e)**: 이 5+1개 항목은 로그인 세션에 묶인 조건부 렌더링이라 site_navigations처럼 순수 데이터로 다룰 수 없다 — "삭제"는 데이터를 지우는 대신 그 kind를 숨기고(`AccountMenuStyleValue.hiddenKinds`/`writeButtonHidden`), "복제"는 같은 kind를 한 번 더 그린다(`extraItems`/`extraWriteButtonIds`). `Navbar.tsx`의 계정 영역 렌더링을 5개 손코딩 블록에서 `renderMenuItem(kind)` 재사용 루프로 리팩터링해 원본과 사본이 완전히 같은 콘텐츠 로직을 공유하게 했다. Elements 탭의 항목 목록(`selectSlot("account:admin")` 등)은 숨김 여부와 무관하게 항상 클릭 가능해, 전부 숨긴 뒤에도 Controls에서 되돌릴 수 있다.
- **전역 실행취소/다시실행(HOTFIX-141f)**: 기존 Ctrl+Z는 하단 메뉴(Footer) Craft.js 캔버스 전용이라(HOTFIX-137.7) 탭 위치를 드래그하거나 색상/폰트/사이드바 스타일을 바꾸는 등 이 페이지의 나머지 편집에는 아무 반응이 없었다 — 이게 "실행취소가 안 된다"는 신고의 실제 원인. `mainLogo`/`sidebarIcons`/`topTabStyle`/`accountMenuStyle`/`heroSlideshow`/`headerPositions`/`topSidebar` 7개 state를 하나의 스냅샷으로 묶는 자체 undo/redo 스택을 추가해 같은 Ctrl/⌘+Z·⌘+Shift+Z와 툴바 버튼에 Craft의 undo/redo와 나란히 연결했다(서로 다른 데이터라 동시 호출해도 무해).
- **상단 사이드바 컬럼 너비/순서(HOTFIX-141g)**: `TopSidebarConfig`에 `columnWidthsPx`(4개 컬럼 픽셀 너비, 기존 하드코딩 w-40/w-48/w-56/w-56과 동일한 기본값)와 `columnOrder`(4개 컬럼의 화면상 좌우 순서) 신설, `TopSidebarPanel.tsx`가 컬럼들을 배열로 만들어 `columnOrder` 순서대로 렌더링. **의도적 구현 방식 변경**: 사용자가 "드래그 드랍"을 요청했지만, 이 컴포넌트는 사이트 전역에 노출되는 실제 메가메뉴이고 이 세션은 관리자 로그인이 없어 픽셀 드래그 인터랙션을 직접 검증할 방법이 없어 — 링크 목록이 이미 쓰는 것과 같은 검증된 패턴(정확한 숫자 입력 + ←/→ 순서 버튼)으로 구현했다. 실제 프리핸드 드래그가 필요하면 사람이 로그인해서 확인 후 후속 작업으로 요청 바람.
- **검증**: `npx tsc --noEmit`/`npm run lint`(0 errors, 기존과 동일한 warning 수, 새 warning 없음)/`npm run build` 전부 통과. Browser pane으로 로그아웃 상태 공개 홈페이지에서 상단 사이드바 열기(트리거 아이콘 기본 "☰" 정상, 컬럼 순서 기본값 유지 확인)/좌측 사이드바 열기(패널 스타일 기본값 유지, 콘솔 무에러) 직접 클릭 확인. **관리자 로그인 자격 증명이 이 세션에 없어(그리고 세션 도중 실수로 Google 로그인 페이지가 뜬 것을 즉시 감지해 아무 것도 입력하지 않고 이탈함) 새로 만든 관리자 Controls UI(복제/삭제 버튼, 컬럼 너비 입력, 폰트 업로드, 실행취소 버튼)와 드래그 인터랙션 자체는 이 세션에서 직접 클릭 검증하지 못했다** — 로그인 가능한 브라우저에서 사람이 직접 확인 필요.
- **변경 파일**: `src/components/LeftSidebar.tsx`, `src/components/RightSidebar.tsx`, `src/components/Navbar.tsx`, `src/components/TopSidebarPanel.tsx`, `src/lib/sidebarIconsSettings.ts`, `src/lib/topSidebarSettings.ts`, `src/lib/accountMenuStyleSettings.ts`, `src/app/admin/navigation/settings/page.tsx`.

## 2026-08-21 (HOTFIX-140.4 — About Silo 3D 우주: 클릭 선택 시 하늘색 구체 제거 + 얇은 표면 하이라이트, 오브젝트별 모션 설정, 별똥별 옵션화)
- **배경**: 사용자가 `/about-silo` 스크린샷(하늘색 글로우 구체 + "여기를 클릭해서 이동" 툴팁)을 첨부하며 3가지 요청 — (1) 오브제/행성을 클릭했을 때 보이는 하늘색 구체를 없애고, 대신 "클릭됐다"는 걸 표면의 얇은 하이라이트로만 표시, (2) 오브젝트마다 반짝임 등 모션 효과를 관리자가 설정할 수 있게, (3) "우주"라는 컨셉에 맞게 별똥별·성운(nebula)·초신성(supernova)·별자리 같은 랜덤 연출 옵션 추가 + 각 요소를 설정 가능하게.
- **하늘색 구체 → 얇은 표면 하이라이트로 교체**: 기존 `SpaceObjectGlow`(선택 시 맥동하는 반투명 구체, HOTFIX-134.2/134.3에서 다뤘던 것)를 완전히 삭제하고, 오브젝트 종류별로 실제 표면에 붙는 아웃라인으로 교체했다. 단일 메시(구체)인 행성(`PlanetMaterial`)은 drei의 `<Outlines>`를 그대로 사용(이 컴포넌트는 `group.parent.geometry`를 직접 읽어 단일 메시에서만 동작 — 유일하게 그대로 쓸 수 있는 곳). 여러 메시로 구성된 GLB 오브젝트(장식/우주 오브젝트)는 drei `Outlines`가 동작하지 않아, 신규 `ModelSelectionOutline` 컴포넌트를 직접 구현 — 원본을 통째로 복제해 모든 재질을 `THREE.BackSide`+반투명+파스텔 스카이블루 톤 `MeshBasicMaterial`로 바꾼 뒤 살짝 확대(`1+thickness`)해 원본 뒤에 렌더링하는 고전 "toon outline shell" 기법. 스프라이트(별/성운 이미지 빌보드)는 같은 텍스처를 재사용한 두 번째 스프라이트를 살짝 크게(`1.18x`)+파스텔 톤으로 `renderOrder={-1}`(뒤쪽)에 렌더링해 실루엣 테두리 효과를 냈다. 아웃라인 불투명도는 `UniverseSettingsPanel`에서 조절 가능(기존 "오브젝트 선택 레이어 불투명도" 슬라이더를 "선택 시 표면 하이라이트 불투명도"로 이름만 정정, `selectionGlowOpacity` 필드는 하위호환을 위해 이름 유지).
- **오브젝트별 모션 효과 설정**: `aboutSiloUniverseConfig.ts`에 `ObjectMotion = "none" | "twinkle" | "bob" | "spin"` 타입과 `UniverseObject.motion` 필드를 신설(스케일 애니메이션은 의도적으로 제외 — HOTFIX-132.1의 카메라 줌 거리 계산이 바운딩 반지름 기반이라 스케일이 흔들리면 그 계산과 어긋날 위험이 있어서 뺐다). 신규 `useObjectMotion` 훅이 `useFrame` 안에서 twinkle(재질 투명도 진동)/bob(위아래 이동)/spin(제자리 회전)을 적용, 카메라 거리 계산에 쓰이는 바운딩 반지름·기존 위치/쿼터니언에는 손대지 않도록 별도의 안쪽 `<group>`으로만 감쌌다. 별/성운 스프라이트는 기존에도 하드코딩된 반짝임이 있었는데(사용자가 "반짝이는 효과가 없는거 같다"고 느낀 것과 별개로 실제로는 있었음) 이제 `obj.motion`으로 덮어쓸 수 있게 하고, 미설정 시 기존 동작(twinkle)을 기본값으로 유지해 하위호환을 지켰다. `ObjectInspectorPanel.tsx`에 "모션 효과" 셀렉트 신설(없음/반짝임/위아래 부유/제자리 회전).
- **별똥별 옵션화**: 기존 `ShootingStars`가 개수 4개 고정 하드코딩이었던 것을 발견해, `UniverseConfig.shootingStars = {enabled, count, color, speedMultiplier}`로 설정 가능하게 확장(기존 하드코딩 값을 그대로 기본값으로 사용해 무변화 마이그레이션). `UniverseSettingsPanel.tsx`에 활성화 체크박스 + 개수(0~20) + 속도 배율(0.3~3배) + 색상 선택 UI 추가.
- **의도적으로 범위 밖으로 미룸 — 성운(nebula)/초신성(supernova)/별자리**: 코드베이스 전체를 검색해 이 세 가지가 현재 어디에도 구현돼 있지 않은 완전 신규 VFX 시스템임을 확인했다. 기존 별똥별처럼 "이미 있는데 설정 옵션만 없는" 수준이 아니라 셰이더/파티클/텍스처 애셋부터 새로 설계해야 하는 규모이고, 이 세션은 관리자 로그인 세션이 없어 새로 만든 설정 UI를 눈으로 검증할 방법도 없어 무리하게 욱여넣지 않고 이번 범위에서 뺐다 — 사용자가 원하면 별도 EPIC으로 진행.
- **검증**: `npx tsc --noEmit`/`npm run lint`(0 errors, 기존 72개 warning 그대로) / `npm run build` 전부 통과. Browser pane으로 `/about-silo` 실제 로드 확인 — three.js/@react-three(fiber·drei)/postprocessing/n8ao/maath 청크와 오브젝트 텍스처·모델(blob: URL 약 60개) 전부 200 OK, 오브제 클릭 시 카메라 포커스 이동 정상, 스크린샷상 하늘색 구체 미노출 확인. 콘솔에 뜬 WebSocket HMR 연결 실패/`/login` `ERR_CONNECTION_REFUSED` 뭉치는 파일을 연달아 저장하는 동안 Turbopack이 재컴파일하던 순간의 일시적 현상(타임스탬프상 그 직후 `/`·`/about-silo` 재요청이 전부 200 OK로 정상 복구)으로 판단, 콘솔에 뜬 약 20건의 403은 네트워크 로그 어디에도 대응하는 URL이 없어(캡처되지 않는 소스맵/devtools 성격으로 추정) P2로 분류해 그대로 둔다(에러 등급 정책상 P2는 기능 개발을 막지 않음). **다음에 확인 필요(관리자 로그인 세션 필수)**: `ObjectInspectorPanel`의 모션 셀렉트/`UniverseSettingsPanel`의 별똥별 설정 UI 실제 클릭 왕복, 저장 후 새로고침 시 값 유지.
- **변경 파일**: `src/components/about-silo/AboutSiloUniverse.tsx`, `src/components/about-silo/ObjectInspectorPanel.tsx`, `src/components/about-silo/UniverseSettingsPanel.tsx`, `src/lib/aboutSiloUniverseConfig.ts`.

## 2026-08-21 (HOTFIX-140.3 — 여백 배경 이미지 업로드 RLS 실패 수정 + 관리자 이미지 미리보기 hover 확대)
- **배경**: 사용자가 스크린샷을 첨부 — "홈페이지 설정관리 → 슬라이드쇼 → 여백 배경 이미지"를 추가하려 하면 "여백 배경 이미지 업로드에 실패했어요. new row violates row-level security policy" 에러가 뜨고, 추가된 이미지의 미리보기도 안 보인다고 신고. 같은 메시지에서 기존 미리보기들도 너무 작아 안 보이니 hover하면 크게 보이게 해달라고 요청(슬라이드쇼 이미지 포함).
- **RLS 버그 원인 및 수정(DB 직접 수정, 코드 변경 없음, 즉시 적용됨)**: Management API로 `pg_policies`를 조회해 `storage.objects`에 `avatars`/`post-images`/`gallery`/`attachments` 버킷용 정책만 있고 `public-assets`(메인 로고·슬라이드쇼·사이드바 아이콘·상단 사이드바·여백 배경·커스텀 폰트 등 "홈페이지 설정 관리"의 거의 모든 업로드가 쓰는 버킷) 정책이 아예 없었음을 확인 — 오래전부터 있었던 진짜 버그로 보이며, 이전 업로드들이 어떻게 성공했었는지는 불명(다른 경로로 시딩됐을 가능성). `public-assets` 버킷에 admin 전용 insert/update/delete 정책 3개 + 공개 select 정책 1개를 추가해 즉시 해결(배포 불필요, DB 반영과 동시에 효과).
- **관리자 이미지 미리보기 hover 확대**: `admin/navigation/settings/page.tsx`의 공용 `ImageThumb` 컴포넌트(로고/슬라이드/여백 배경/사이드바 아이콘 기본·hover/상단 사이드바 이미지 뱅크·링크 이미지 등 7곳에서 재사용)에 hover 상태를 추가 — hover 중이면 `position:fixed`(패널의 `overflow-y-auto`에 잘리지 않도록 `absolute` 대신 의도적으로 선택) 오버레이로 원본을 화면 중앙에 최대 80vw/80vh로 크게 보여준다.
- **검증**: `npx tsc --noEmit`/`npm run lint`/`npm run build` 전부 0 errors. RLS 정책은 Management API 재조회로 정책 4개가 실제로 생성됐음을 확인(`policyname`/`cmd` 검증). **관리자 로그인 세션이 없어 실제 업로드 성공 여부와 hover 확대 UI는 이 세션에서 직접 클릭 검증하지 못했다** — 로그인 가능한 브라우저에서 확인 필요.
- **변경 파일**: `src/app/admin/navigation/settings/page.tsx` (+ DB: `storage.objects` RLS 정책 4개 추가, 코드 아님).

## 2026-08-21 (HOTFIX-140.2 — "홈페이지 설정 관리" 캔버스에서 상단/좌우 사이드바를 실제로 열고 설정 가능하게 + 상단 사이드바 콘텐츠·스타일 확장)
- **배경**: 사용자가 스크린샷 2장(등급/팔로우/최근활동 등이 보이는 실제 열린 패널 캡처 + column 0/1/2/3 손그림 스펙)을 첨부하며 "홈페이지 설정 관리"에서 실제 라이브 프리뷰의 상단 사이드바 여닫이 아이콘을 클릭하면 아이콘 설정이 왼쪽에 열리는 동시에 실제 패널도 열려야 하고, 배경색/텍스트색/폰트/hover 모션/이미지 뱅크 랜덤 노출/요소 추가·삭제·복제까지 자유롭게 설정 가능해야 한다고 요구. 같은 요청에 좌우 사이드바(LeftSidebar/RightSidebar)도 실제 아이콘이 캔버스에 보이고 실제 사이트처럼 hover/클릭이 동작하며 설정 가능해야 한다고 확장.
- **버그 발견 및 수정 — 편집 모드에서 트리거 클릭이 아예 안 열리던 문제**: 직전 세션(EPIC-139/HOTFIX-137.1)이 "편집 모드에서 로그아웃 버튼을 클릭해도 실제 로그아웃되면 안 된다"는 이유로 `HeaderSlot`의 모든 하위 요소 클릭을 캡처 단계에서 무조건 차단하도록 고쳤는데, 그 부작용으로 "상단 사이드바 열기" 버튼처럼 페이지 이동이나 세션 변경 없이 로컬 state만 토글하는 안전한 트리거까지 똑같이 막혀버렸다(관리자 화면 주석엔 "캔버스에서 직접 클릭해 열고 닫아 미리볼 수 있다"고 적혀 있었지만 실제로는 동작하지 않는 상태였음). `HeaderSlot`에 `interactive` prop을 추가 — true면 선택 처리는 그대로 하되 `preventDefault`/`stopPropagation`을 걸지 않아 children 자신의 토글 동작도 함께 실행된다. `top-sidebar-trigger`에 적용.
- **좌우 사이드바를 실제 캔버스 컴포넌트로 전환**: EPIC-136 시점엔 "화면 가장자리 fixed 트리거라 캔버스 대상이 아님"이라며 편집 모드에서 `LeftSidebar`/`RightSidebar` 자체를 렌더링하지 않고, `admin/navigation/settings/page.tsx`에 별도의 정적 "대표 미리보기" 썸네일(`SidebarIconPreview`)을 대신 보여주고 있었다(EPIC-136 CHANGELOG의 "사이드바 아이콘을 이 캔버스에 포함시켜 달라는 후속 요청 가능성"이 실제로 들어온 것). `LeftSidebar.tsx`/`RightSidebar.tsx`에 `editable`/`selected` prop을 추가해 TopSidebarPanel과 동일한 패턴(`position:fixed` ↔ `:absolute` 전환, 편집 모드에선 `<header>`의 `relative` 래퍼 기준으로 자리잡음)을 적용하고, `Navbar.tsx`의 `!editable &&` 렌더 가드를 제거해 실제 컴포넌트가 항상 그려지도록 바꿨다. 트리거 클릭(`onIconClick`)이 실제로 패널을 열면서 동시에 왼쪽 Controls를 `sidebar:left`/`sidebar:right`로 전환하도록 배선. 이제 쓸모없어진 admin 페이지의 가짜 미리보기 블록(`SidebarIconPreview`/`SidebarTriggerMediaPreview`)은 삭제.
- **패널 내부 클릭이 관리자 화면을 벗어나지 않도록**: `TopSidebarPanel`/`LeftSidebar`/`RightSidebar` 패널 내부의 실제 네비게이션 링크를 편집 모드에서 클릭하면 캡처 단계에서 가로채 실제 이동을 막는다 — 단, 무조건 `stopPropagation`하면 같은 패널 안의 "닫기(✕)" 버튼 자신의 클릭까지 캡처 단계에서 막혀버리는 버그가 나서(구현 중 직접 재현해 발견), `data-panel-close` 마커로 닫기 버튼만 예외로 통과시키도록 고쳤다.
- **상단 사이드바 column 1 콘텐츠 확장**(`TopSidebarPanel.tsx`): 팔로워만 있던 것에 팔로잉 수를 추가, "최근 활동" 이 클릭 불가 링크 하나뿐이던 것을 실제 `points_ledger` 최근 3건 + "전체보기" 링크로 교체(MembershipPopover.tsx의 활동 조회 패턴 축소 재사용), 새로 고정 바로가기 Mind Diary(`/salon/mind-diary`)/Studio(`/studio`)/Silo Planet(`/about-silo`) 3개를 Message 아래에 추가.
- **상단 사이드바 스타일·이미지 뱅크 신설**(`topSidebarSettings.ts` 확장 + `TopSidebarControls`): `TopSidebarConfig`에 `backgroundColor`/`textColor`/`fontFamily`/`hoverMotion`(`tabHoverMotion.ts`의 기존 6종 프리셋 재사용)/`imageBankUrls`(다중 이미지 풀) 필드 신설. column 2 링크에 마우스를 올리면 이 풀에서 무작위로 하나 골라 column 0에 보여준다(풀이 비어있으면 기존처럼 그 링크 자신의 imageUrl로 폴백 — 하위호환). 관리자 Controls에 배경색/텍스트색/서체 입력, hover 모션 셀렉트, 이미지 뱅크 업로드/삭제 그리드를 추가했고, 링크 목록에 "복제" 버튼도 추가(Craft.js 노드가 아니라 평범한 배열이라 EPIC-139가 우회했던 id 재사용 위험이 없어 안전하게 구현 가능했음).
- **검증**: `npx tsc --noEmit`/`npm run lint`(React Compiler의 `react-hooks/purity`가 컴포넌트 스코프의 `Math.random()`을 정적으로 걸러낸 것 1건을 `eslint-disable-next-line`으로 해결 — 실제로는 렌더 중이 아니라 이벤트 핸들러에서만 호출되는 안전한 케이스)/`npm run build` 전부 0 errors. **이번엔 Browser pane이 정상 동작**(직전 세션에서 확인했던 "site_navigations client-side fetch가 실행 안 됨" sandbox 제약이 이번엔 재현되지 않음, 원인 불명이나 일시적이었던 것으로 보임) — 실제 공개 홈페이지(로그인 없는 상태)에서 상단 사이드바 열기(Mind Diary/Studio/Silo Planet 링크 정상 확인)/좌측 사이드바 열기(사일로 상점 트리 정상)까지 클릭으로 직접 확인, 콘솔에 새 에러 없음(기존 무관한 React 19 ref 경고만). **다만 관리자 로그인 자격 증명이 이 세션에 없어 `/admin/navigation/settings` 캔버스 자체(편집 모드에서의 클릭-열기/Controls 연동/스타일 반영)는 실제로 열어보지 못했다** — 로직은 HeaderSlot과 동일한 검증된 패턴을 그대로 재사용했지만, 실제 로그인 가능한 브라우저에서 사람이 직접 확인 필요.
- **의도적으로 범위 밖으로 미룸**: 사용자가 요청한 "컬럼을 드래그 드랍으로 옮기거나, 이미지 위치, 컬럼 안의 요소를 자유롭게 움직이는" 완전 자유 배치(페이지 빌더 수준의 드래그앤드롭)는 이번 범위에서 빠졌다 — 지금은 순서 변경(↑/↓ 버튼)까지만 가능. 좌우 사이드바 콘텐츠(그룹/링크)는 여전히 site_navigations(EPIC-138 "사이트 구성 관리 > 사이트 메뉴")가 담당 — 상단 사이드바 column 2처럼 별도 site_settings 기반 목록으로 바꾸지 않았다(현재 방식이 이미 카테고리 트리와 일관돼 있어 굳이 이원화하지 않음).
- **변경 파일**: `src/components/HeaderSlot.tsx`, `src/components/Navbar.tsx`, `src/components/LeftSidebar.tsx`, `src/components/RightSidebar.tsx`, `src/components/TopSidebarPanel.tsx`, `src/lib/topSidebarSettings.ts`, `src/app/admin/navigation/settings/page.tsx`.

## 2026-08-21 (HOTFIX-140.1 — 상단 사이드바 트리거 아이콘이 실제 홈페이지에 안 보이던 버그 수정)
- **배경**: EPIC-140에서 신규 "상단 사이드바"(Kinfolk형 메가 메뉴)를 만들면서, 아직 링크가 하나도 없는 빈 패널을 열게 하는 걸 막으려고 `TopSidebarConfig`에 `enabled`(기본값 `false`) 플래그를 넣고 트리거 아이콘을 `editable || resolvedTopSidebar?.enabled`일 때만 보이게 했다 — 그런데 이 기본값이 `false`라 관리자가 "상단 사이드바 사용" 체크박스를 직접 켜고 저장하기 전까지는 트리거 자체가 실제 사이트에 전혀 안 보였고, 아무 안내도 없어 사용자가 "상단 사이드바 아이콘이 실제 홈페이지에는 안보여"로 신고했다.
- **근본 원인 재진단**: `Navbar.tsx`의 다른 모든 헤더 요소(로고/사이드바 아이콘/탭/계정 영역 5항목)는 예외 없이 "항상 보이고, 내용만 관리자가 채운다"는 원칙으로 설계돼 있는데 `top_sidebar`만 "숨겨진 마스터 스위치"라는 다른 패턴을 썼다 — 이게 설계 일관성 위반이자 버그의 직접 원인. `curl`로 라이브 `site_settings.top_sidebar`를 직접 조회해 `{"pc":{"links":[],"enabled":false},"mobile":{"links":[],"enabled":false}}`임을 확인, 단순히 DB 값을 `true`로 뒤집는 대신 **`enabled` 개념 자체를 제거**해 같은 종류의 버그가 다른 관리자가 체크박스를 깜빡했을 때 재발하지 않도록 했다.
- **수정**: `src/lib/topSidebarSettings.ts`(`TopSidebarConfig`/`normalizeConfig`에서 `enabled` 필드 제거), `src/components/Navbar.tsx`(트리거 표시 조건을 `mounted && !loading && (editable || resolvedTopSidebar?.enabled) && (...)`에서 `mounted && !loading && (...)`로 변경 — 다른 계정 영역 아이콘들과 동일하게 무조건 표시), `src/app/admin/navigation/settings/page.tsx`(`TopSidebarControls`에서 "상단 사이드바 사용" 체크박스 제거, 안내 문구를 "다른 헤더 요소처럼 항상 표시된다"로 정정).
- **검증**: `npx tsc --noEmit`/`npm run lint`(0 errors, 기존 71개 warning 그대로)/`npm run build` 전부 통과. 이번엔 저장된 로그인 세션 없이도 로그아웃 상태 홈페이지로 충분히 확인 가능해 Browser pane으로 실제 클릭까지 검증: 새 dev 서버로 `/` 접속 → `read_page`로 "상단 사이드바 열기" 버튼이 헤더에 나타남을 확인 → 클릭 → 스크린샷으로 패널이 화면 위에서 아래로 슬라이드해 열리고 ✕ 버튼과(비로그인 상태이므로) "로그인" 링크가 정상 표시됨을 확인(등록된 링크가 아직 없어 컬럼2/3은 비어 있음 — 정상) → ✕ 클릭 → 스크린샷으로 패널이 닫히고 트리거 아이콘만 남음을 확인. 콘솔 에러는 이 변경과 무관한 기존 React 19 `element.ref` deprecation 경고 4건뿐(P2, 프레임워크 레벨, 손대지 않음).
- **변경 파일**: `src/lib/topSidebarSettings.ts`, `src/components/Navbar.tsx`, `src/app/admin/navigation/settings/page.tsx`.

## 2026-08-21 (EPIC-140 — 상단 탭/사용자 메뉴 추가·복제·삭제 + 드롭다운 z-index 버그 수정 + 신규 "상단 사이드바"(Kinfolk형 메가 메뉴))
- **배경**: 사용자가 "홈페이지 설정 관리" 캔버스 스크린샷과 Kinfolk 레퍼런스 스크린샷을 첨부하며 3가지를 지적/요청 — (1) 상단 탭·사용자 메뉴에 새 요소를 추가/복제/삭제할 수 없다, (2) 탭에 hover했을 때 뜨는 드롭다운이 슬라이드쇼에 가려서 안 보인다, (3) Kinfolk처럼 헤더 우측 아이콘을 누르면 위에서 아래로 슬라이드하는 3-컬럼 메가 메뉴("상단 사이드바")를 새로 만들어 달라(컬럼1: 실제 로그인 이름/등급/팔로워/활동/메시지, 컬럼2: 관리자가 정하는 링크 목록, 컬럼3: 컬럼2 항목에 hover 시 나타나는 하위 목록, 전부 hover 시에만 좌측에 그 링크의 이미지가 뜸, "컬럼1/2/3"이라는 라벨 텍스트 자체는 표시 안 함).
- **z-index 버그 원인**: EPIC-136이 헤더 wrapper(`topBarRef`)의 `position:fixed`를 편집 모드에서 걷어낼 때(캔버스 밖 뷰포트로 튀어오르는 걸 막으려고) `z-40`까지 함께 지워버렸다 — `position:relative`만 남고 z-index가 없으면 새 stacking context가 안 생겨, 내부의 `z-40` 드롭다운 flyout이 DOM 순서상 뒤에 오는 슬라이드쇼(역시 `position:relative`, z-index 없음)에 그대로 덮였다(둘 다 "auto" 레벨이라 나중에 그려지는 쪽이 위로 올라옴). `editable ? "relative" : "fixed inset-x-0 top-0 z-40"`을 `editable ? "relative z-40" : "fixed inset-x-0 top-0 z-40"`으로 수정 — 두 분기 모두 항상 z-40을 유지해 원래(비-editable) 사이트와 동일한 stacking을 보장한다.
- **탭/메뉴 항목 추가·복제·삭제**: EPIC-138부터 "사용자 메뉴"(마이페이지 드롭다운, `UserMenuDropdown.tsx`)는 별도 데이터가 아니라 `site_navigations` 최상위 행의 `target_types`에 `"user_menu"` 플래그 하나가 붙는 것뿐이다 — 즉 "상단 탭 추가"와 "사용자 메뉴 항목 추가"는 같은 테이블에 대한 같은 CRUD고, 노출 위치(어느 탭 줄/사이드바/사용자 메뉴에 보일지)는 이미 있는 "노출 위치" 체크박스로 정하면 된다. `CategoryTreeManager.tsx`(사이트 구성 관리)의 `addChild`/`deleteRow`와 동일한 컬럼·기본값·`ensurePageForSlug` 자동 페이지 생성 흐름을 그대로 재사용해 `addNavItem`/`duplicateNavItem`(스타일까지 함께 복제)/`deleteNavItem`을 신설 — Elements 탭에 "+ 새 탭/메뉴 항목 추가" 버튼, 탭 Controls 상단에 복제/삭제 액션 바(하단 메뉴 Craft 블록의 기존 액션 바와 동일한 UI 패턴)를 추가. 계정 영역(관리자/등급/마이페이지/이름/로그아웃) 5개 고정 아이콘은 로그인 세션에 묶인 기능이라 그대로 추가/삭제 불가로 남기고, 그 Controls 패널 안내 문구를 "새 메뉴 항목은 여기가 아니라 Elements 탭에서 추가 후 노출 위치를 사용자 메뉴로 체크하라"고 명확히 정정.
- **신규 "상단 사이드바"(Kinfolk형 메가 메뉴)**: `src/lib/topSidebarSettings.ts`(신규, `{pc,mobile}` 패턴 — `links[].{label,href,imageUrl,children[]}`)와 `src/components/TopSidebarPanel.tsx`(신규)를 추가. 컬럼1(이름/등급/팔로워/최근 활동/메시지)은 관리자가 편집하는 콘텐츠가 아니라 `MembershipPopover.tsx`와 동일한 방식으로 실제 세션 데이터를 직접 조회 — 로그인 안 돼 있으면 "로그인" 링크만 보임, "메시지"는 이 코드베이스에 DM 시스템이 없어 `MembershipPopover`와 동일하게 "준비 중"으로 정직하게 표시. 컬럼2는 `Navbar.tsx`의 새 `HeaderSlot`("top-sidebar-trigger", 계정 영역 맨 끝 ☰ 아이콘)을 눌러 열리는, 관리자가 자유롭게 추가/삭제/순서 변경하는 링크 목록, 컬럼3은 그중 마우스를 올린 링크의 `children`(hover cascade), 좌측 이미지 영역은 hover 중인 링크의 `imageUrl`만 보여주고 그 외엔 비어 있다 — "컬럼1/2/3" 텍스트 라벨은 어디에도 렌더링하지 않는다. `TopSidebarPanel` 자체도 `LeftSidebar.tsx`와 동일한 `fixed inset-x-0 top-0 ... -translate-y-full ↔ translate-y-0` 슬라이드 패턴을 쓰되, EPIC-136 헤더 fixed 버그와 같은 이유로 `editable`일 때는 `absolute`로 전환하고 `topBarRef`(편집 모드에서 이미 `position:relative`) 안에 렌더링해 관리자 캔버스 안에 그대로 자리잡게 한다. `Navbar.tsx`에 `topSidebarOverride` prop을 추가(`positionsOverride`와 동일한 override-우선 패턴), 관리자 페이지에 `TopSidebarControls`(활성화 토글 + 링크 목록 추가/삭제/순서 변경 + 항목마다 라벨/링크/hover 이미지 업로드+썸네일 + 하위 링크 추가/삭제) 신설, `site_settings.top_sidebar` 저장을 "저장하기" 버튼에 합류.
- **검증**: `npx tsc --noEmit`/`npm run lint`/`npm run build` 전부 0 errors(신규 warning 없음). Browser pane 실측 — 이번 세션은 관리자 로그인 세션이 없는 새 브라우저 프로필이라(이전 세션들과 달리 저장된 Supabase 세션이 전혀 없었음, 로그인 자격 증명도 없어 로그인 자체를 진행하지 못함) 캔버스 내 클릭/드래그 상호작용은 직접 확인하지 못했다 — 대신 (1) z-index 버그는 실제 DOM을 `getComputedStyle`/스택 조상 체인으로 직접 추적해 근본 원인을 코드로 확정한 뒤 수정, (2) 탭 CRUD는 이미 프로덕션에서 검증된 `CategoryTreeManager.tsx`의 동일 컬럼/흐름을 그대로 재사용, (3) 로그아웃 상태의 실제 공개 홈페이지가 회귀 없이 정상 렌더링됨(로고·탭·슬라이드쇼·계정 영역 "로그인" 링크까지)과 `top_sidebar` 비활성 상태에서 트리거 아이콘이 의도대로 안 보이는 것을 DOM으로 확인. **다음에 확인 필요(사람이 직접, 로그인 세션으로)**: 드롭다운이 이제 실제로 슬라이드쇼 위에 보이는지 눈으로 재확인, Elements 탭 "+ 새 탭/메뉴 항목 추가"→Controls 복제/삭제 버튼 실제 클릭 왕복, 상단 사이드바 트리거 아이콘 클릭 시 패널이 실제로 슬라이드 다운되는지 + hover 시 이미지·컬럼3가 정상 전환되는지, 관리자 캔버스 안에서 패널이 `absolute` 전환으로 인해 잘리거나 어긋나 보이지 않는지.
- **변경 파일**: `src/components/Navbar.tsx`, `src/app/admin/navigation/settings/page.tsx`, `src/lib/topSidebarSettings.ts`(신규), `src/components/TopSidebarPanel.tsx`(신규).

## 2026-08-20 (EPIC-139 — 헤더 편집 캔버스 버그 3건 수정 + 드롭다운/메가 드롭다운/실행취소 기능 추가)
- **배경**: 사용자가 "홈페이지 설정 관리" 캔버스를 실사용하며 3가지 버그와 2가지 기능 요청을 한 번에 제기 — (1) 상단탭의 "글쓰기" 버튼이 드래그&드롭이 안 됨, (2) "로그아웃" 버튼을 조정하려고 클릭하면 진짜 로그아웃이 실행돼버림, (3) 요소를 클릭해도 이동만 되고 수정/삭제가 없음, (4) 각 요소마다 "드롭다운"이 되게 하는 선택 기능 + 한 번에 여러 카테고리를 보이는 "메가 드롭다운" 옵션 요청, (5) BuilderJS 레퍼런스 스크린샷 9장을 첨부하며 "그대로 구현해줘", (6) 슬라이드쇼의 "여백 배경 이미지" 업로드 시 프리뷰가 안 뜸 + 개수 상한을 10 → 100으로. 세션 종료 전 사용자가 "이후의 모든 결정사항은 네가 권장하는 최선의 선택으로 자동으로 해"라고 전권을 위임해 범위 판단(무엇을 이번에 하고 무엇을 후속으로 미룰지)까지 포함해 자체적으로 진행했다.
- **HOTFIX-137.1 — 로그아웃 오작동 수정**: `HeaderSlot.tsx`가 예전엔 `onClick`(버블 단계)에서 `stopPropagation`만 걸었는데, 그건 "위로 전파"만 막지 클릭이 먼저 도달하는 children 자신의 핸들러(로그아웃 실행)나 기본 동작(`<a>` 네비게이션)은 전혀 막지 못했다 — `onClickCapture`(캡처 단계, children보다 먼저 실행)로 옮기고 `preventDefault`+`stopPropagation`을 걸어 편집 모드에서는 어떤 children도 실제 동작을 실행하지 않고 선택만 되도록 근본적으로 고쳤다. 로그아웃뿐 아니라 다른 링크형 요소(탭 등)도 편집 중 실수로 실제 이동해버리는 잠재 버그까지 함께 해소.
- **HOTFIX-137.2 — "글쓰기" 버튼 드래그 불가 수정**: 다른 탭/계정 메뉴 항목과 달리 `Navbar.tsx`의 `writeButtonEl`만 `HeaderSlot`으로 감싸지 않은 평범한 `<Link>`였다 — 전용 `slotKey="write-button"`으로 감싸 다른 요소와 동일하게 클릭 선택 + 드래그 이동이 가능해졌고, `admin/navigation/settings/page.tsx`의 Elements 목록/Controls 패널에도 추가했다.
- **HOTFIX-137.3 — 슬라이드쇼 이미지 업로드 실패가 조용히 무시되던 버그 수정**: "여백 배경 이미지"(그리고 슬라이드 이미지도 함께)를 업로드했을 때 `uploadImage()`가 반환하는 `error`를 지금까지 아무도 읽지 않고 버려서, 업로드가 실패(예: Supabase 무료 플랜 파일 용량 상한)하면 아무 피드백 없이 "프리뷰가 안 뜬다"로만 보였다 — HOTFIX-134.2가 다른 업로드 경로에 적용했던 것과 동일하게 실패 사유를 `alert`로 보여주도록 수정, `compressImage`/`uploadImage` 호출을 `try/catch`로 감싸 예외로 "업로드 중..." 상태에 멈춰버리는 것도 방지.
- **HOTFIX-137.4 — 여백 배경 이미지 개수 상한 10 → 100**: `MAX_WALLPAPERS` 상수 변경.
- **HOTFIX-137.5 — 탭마다 "드롭다운" 선택 기능 + "메가 드롭다운" 옵션 추가**: `navConfig.ts`의 `NavTab`에 실제 `site_navigations` 행 id와 `target_types` 배열을 함께 실어 보내도록 확장해, `admin/navigation/settings/page.tsx`의 탭 Controls 패널에서 EPIC-138이 "사이트 구성 관리 > 사이트 메뉴"에 만든 것과 동일한 노출 위치 체크박스(1단/2단 상단탭·드롭다운·좌우 사이드바·사용자 메뉴)를 바로 편집할 수 있게 했다(다른 화면으로 안내만 하던 것 대신 직접 `site_navigations` update). 신규 "메가 드롭다운" 옵션은 `TopTabStyleEntry.megaDropdown`(순수 표시 스타일이라 site_navigations가 아니라 탭별 스타일 설정에 저장)으로 추가 — 켜면 `Navbar.tsx`의 드롭다운 플라이아웃이 기존처럼 항목에 마우스를 올려야 열리는 중첩 리스트 대신, 모든 그룹/1차 항목을 컬럼으로 나란히 펼쳐 하위 항목까지 한 번에 보여주는 그리드형 메가 메뉴로 렌더링된다.
- **HOTFIX-137.6 — 하단 메뉴(Footer) 블록에 삭제/순서 이동 추가**: "클릭하면 이동만 되고 수정/삭제가 없다"는 지적 중 "수정"은 이미 `Settings` 패널이 담당하고 있었지만 "삭제"가 정말로 없었다 — `FooterCraftControls`에 선택된 블록 헤더 옆으로 위로/아래로(Craft.js `actions.move`, 같은 부모 안에서만) + 삭제(`actions.delete`, 확인 다이얼로그 포함) 버튼을 추가했다. **"복제"는 이번 범위에서 뺐다**: Craft.js의 `addNodeTree`가 전달받은 트리의 노드 id를 그대로 재사용해 `state.nodes`를 덮어쓴다는 것을 번들 소스를 직접 리버스엔지니어링해 확인(문서화되지 않은 내부 동작) — 안전하게 복제하려면 트리의 모든 노드 id를 직접 재생성해 `parent`/`nodes`/`linkedNodes` 참조를 다시 매핑해야 하는데, 이 세션은 Browser pane으로 실제 검증이 안 되는 상태(아래 검증 한계 참고)라 모든 페이지에 노출되는 하단 메뉴 데이터를 망가뜨릴 위험을 감수하지 않고 후속 과제로 미뤘다(NEXT_TASK.md 기록).
- **HOTFIX-137.7 — 실행취소/다시실행(Undo/Redo) 추가**: Craft.js에 이미 내장돼 있던 `actions.history.undo/redo`(+`query.history.canUndo/canRedo`)를 처음으로 연결 — 상단 툴바에 버튼 추가 + `CraftBridge`에서 Ctrl/⌘+Z, Ctrl/⌘+Shift+Z 전역 단축키 배선(입력 필드/textarea/contentEditable 안에서는 가로채지 않고 네이티브 undo에 양보). 하단 메뉴(Footer) Craft 캔버스 범위에만 적용되고, site_settings 기반 다른 섹션(로고/탭 스타일/슬라이드쇼 등)에는 적용되지 않는다(별도 history 시스템이 없음 — 범위 밖).
- **"9개 스크린샷 그대로 구현해줘"에 대한 범위 판단**: 첨부된 BuilderJS 레퍼런스 스크린샷은 인라인 서식 툴바(B/I/U/S/링크/폰트크기/하이라이트), 정직한 드롭존 하이라이트, 선택-인식 툴바(복제·삭제·이동), 인라인 이미지 크롭 도구, 실행취소 히스토리, 그룹/배너가 있는 위젯 팔레트 재설계, `BaseWidget` 서브클래스 확장 시스템, 폼/가격표/체크아웃 프리미티브 카탈로그까지 사실상 하나의 완성된 별도 제품 수준의 기능 목록이었다 — 이번 세션은 그중 사용자가 본문에서 명시적으로 요청한 두 가지(요소 삭제, 드롭다운/메가 드롭다운)와 저위험·고가치인 실행취소만 구현했고, 나머지(리치 텍스트 툴바, 이미지 크롭, 위젯 팔레트 재설계, 커스텀 위젯 확장 문서화, 폼/가격표 프리미티브)는 각각 별도의 설계·구현이 필요한 규모라 이번 범위에서 뺐다 — NEXT_TASK.md에 후속 항목으로 기록.
- **검증**: `npx tsc --noEmit` 0 errors, `npm run lint` 0 errors(경고는 기존 70개 그대로), `npm run build` 0 errors. **Browser pane 검증 한계(EPIC-138과 동일)**: 이 세션의 dev 서버 브라우저에서 `site_navigations` client-side fetch가 실행되지 않는 sandbox 환경 제약이 여전해 관리자 캔버스 화면을 실제로 열어보지 못했다 — 코드 리뷰(diff 전체 재검토, Craft.js 번들 소스 직접 확인으로 `addNodeTree`/`move`/`delete`/`history` API 동작 검증)로 대체했다. **실제 로그인 가능한 브라우저에서 직접 확인 필요**: 글쓰기 버튼 드래그, 로그아웃 버튼이 더 이상 실제로 로그아웃시키지 않는지, 탭 노출 위치 체크박스, 메가 드롭다운 렌더링, 하단 메뉴 삭제/이동 버튼, Undo/Redo 단축키.
- **변경 파일**: `src/components/HeaderSlot.tsx`, `src/components/Navbar.tsx`, `src/lib/navConfig.ts`, `src/lib/topTabStyleSettings.ts`, `src/app/admin/navigation/settings/page.tsx`.

## 2026-08-20 (EPIC-138 — 사이트 메뉴 노출 위치를 복수 선택 가능하게 확장: 1단/2단 상단탭·사용자 메뉴 추가)
- **배경**: 사용자가 "사이트 구성관리 > 사이트메뉴"에서 최상위 카테고리 오른편에 있는 노출 위치 선택([상단탭, 드롭다운, 왼쪽 사이드바, 오른쪽 사이드바])에 "1단 상단탭"/"2단 상단탭"/"사용자 메뉴"를 추가하고, 카테고리마다 복수 선택이 가능하도록 요청. 조사 결과 기존 "상단탭"은 항상 flat link였고, 1단/2단 배치는 `/admin/navigation/settings`(상단 탭 디자인)에 있는 완전히 별개의 tier 설정(site_settings.top_tab_style, PC/모바일 각각)이 담당하고 있었다 — 이중 설정 구조였음. "사용자 메뉴"(계정 영역)는 카테고리 트리와 무관한 5개 고정 항목(관리자/회원등급/마이페이지/회원이름/로그아웃)뿐이었다. AskUserQuestion으로 3가지 확정: (1) 복수 선택 = 같은 카테고리가 동시에 여러 위치에 실제로 노출, (2) 기존 "상단탭" 제거하고 1단/2단으로 완전히 대체(이중 설정 제거), (3) "사용자 메뉴"는 "마이페이지" 클릭 시 여는 신규 드롭다운 안에 노출.
- **스키마**: `site_navigations.target_type`(단일 enum: tab/dropdown/sidebar_left/sidebar_right) → `target_types text[]`(배열: tier1_tab/tier2_tab/dropdown/sidebar_left/sidebar_right/user_menu) 신설. 기존 데이터는 `target_type` + `site_settings.top_tab_style.pc.tabs[key].tier`를 근거로 백필(dropdown/sidebar_left/sidebar_right는 그대로 유지, tab은 폐기하고 tier 값에 따라 tier1_tab 또는 tier2_tab 추가 — 라이브 데이터 기준 "스튜디오"/"마이 페이지"만 tier1, 나머지는 tier2). 기존 `target_type` 컬럼은 되돌리기 쉽도록 삭제하지 않고 남겨뒀다(더 이상 코드에서 읽지 않음). Management API로 직접 실행, 결과를 anon REST API로 재조회해 검증 완료.
- **CategoryTreeManager.tsx(사이트 메뉴 관리)**: `TargetTypeLiteral`을 6개 값으로 재정의, `CategoryNavRow.target_types: TargetTypeLiteral[]`로 변경. 각 행의 단일 `<select>`를 신규 `TargetTypePopoverSelect`(컴팩트 버튼+체크박스 팝오버, 트리 행처럼 좁은 공간용)/`TargetTypeCheckboxList`(체크박스 목록, 모달처럼 넓은 공간용) 두 컴포넌트로 교체 — 둘 다 복수 선택을 지원하고 "관리" 모달의 "노출 위치" 필드도 동일하게 바꿨다. "+최상위 탭 추가" 툴바 셀렉트도 동일 패턴으로 교체.
- **navConfig.ts(렌더링 로직)**: `mapTargetTypes()`가 target_types 배열에서 탭의 "모양"(link/dropdown/sidebar-left/sidebar-right, 우선순위 sidebar_left > sidebar_right > dropdown > link)만 결정하고, `tiersOf()`가 별도로 tier1/tier2 소속을 뽑아 `NavTab.tiers: (1|2)[]`에 담는다 — 모양과 배치(tier)가 완전히 독립적인 두 축이 됐다. `buildNavTree()`가 반환 타입을 `NavTab[]`에서 `{ tabs: NavTab[], userMenuItems: NavItem[] }`(`NavData`)로 확장 — `user_menu`로 태그된 최상위 카테고리를 별도 평면 링크 목록으로 뽑아낸다. `fetchNavTabs()`도 이 새 반환 타입을 그대로 따른다.
- **Navbar.tsx**: `tabTier()`(topTabEntries[key].tier 조회, 예전 이중 설정의 실제 소비처)를 제거하고 `tabTiers()`(`tab.tiers ?? [2]`)로 교체 — tier1Tabs/tier2Tabs 필터링이 이제 카테고리 자체의 target_types에서 직접 온다(같은 카테고리가 tier1_tab과 tier2_tab을 둘 다 선택하면 두 줄 모두에 나타난다). "마이페이지" 항목을 `/mypage` 직행 Link에서, 클릭하면 신규 `UserMenuDropdown`(MembershipPopover.tsx와 동일한 click-outside+Escape 패턴)을 여는 버튼으로 변경 — 드롭다운 맨 위에 "마이페이지 바로가기" 링크, 그 아래 `user_menu` 카테고리 링크 목록(GatedNavLink로 등급 게이트 동일 적용). 회원등급/이름 팝오버와 같은 자리에 뜨므로 하나를 열면 다른 하나를 닫도록 상호 배제 처리. `renderMenuItem()`(통합 헤더 레이아웃 경로, 현재 라이브 데이터에서는 비활성)도 동일하게 갱신.
- **admin/navigation/settings/page.tsx**: "상단 탭" Controls 패널에 있던 "배치"(1단/2단) `<select>`(예전 이중 설정의 UI 쪽)를 제거하고, "사이트 구성 관리 > 사이트 메뉴"로 안내하는 문구로 교체.
- **검증**: `npx tsc --noEmit`/`npm run lint` 0 errors(새 경고 없음, 기존 70개 warning은 전부 무관한 파일). DB 마이그레이션은 anon REST API로 백필 결과 직접 재조회해 확인(스튜디오/마이 페이지 = tier1_tab, 나머지 = tier2_tab, dropdown/sidebar_left/sidebar_right 그대로 유지). **Browser pane 검증 한계**: 이 세션의 dev 서버 브라우저 환경에서 `Navbar.tsx`의 `fetchNavTabs()`가 client-side에서 `site_navigations`를 조회하는 fetch 자체가 실행되지 않아(다른 `site_settings`/`items` 쿼리는 정상 실행됨, `performance.getEntriesByType('resource')`로 직접 확인) 상단 탭이 빈 채로 렌더링됨을 발견 — **`git stash`로 이번 변경을 완전히 되돌린 원본(origin/main) 코드에서도 동일한 증상이 재현됨을 A/B 테스트로 확인**, 즉 이번 변경과 무관한 이 sandbox 환경 고유의 제약(EPIC-133이 문서화한 "이 서버 sandbox의 아웃바운드 네트워크 제약"과 같은 계열로 추정)이다. `/admin/site-structure`(관리자 로그인 필요)도 이 세션에는 로그인 자격 증명이 없어 브라우저로 직접 열어보지 못했다. 코드 로직은 `tsc`/`lint`/DB 재조회로 교차검증했지만, **실제 배포 환경 또는 로그인 가능한 브라우저에서 사이트 메뉴 관리 화면의 체크박스 UI와 공개 홈페이지의 1단/2단 탭·사용자 메뉴 드롭다운 렌더링을 사람이 직접 확인 필요**.
- **변경 파일**: `src/components/admin/CategoryTreeManager.tsx`, `src/lib/navConfig.ts`, `src/components/Navbar.tsx`, `src/app/admin/navigation/settings/page.tsx`, 신규 `src/components/UserMenuDropdown.tsx`, `docs/database-schema.sql`, `docs/navigation-blueprint.md`.

## 2026-08-20 (EPIC-137 — "홈페이지 설정 관리"를 화면 전환 없는 단일 실시간 캔버스 + Elements/Controls/Page/Themes 4탭 패널로 재구성)
- **배경**: EPIC-136이 헤더를 진짜 Navbar 기반 자유 드래그 캔버스로 바꿨지만, 관리자 페이지 자체는 여전히 "홈페이지 헤더 디자인/슬라이드쇼/사이드바 아이콘/하단 메뉴 관리" 4개 섹션을 상단 버튼으로 전환하는 구조였다. 사용자가 이를 다시 "또 4개의 화면으로 나눴다"고 강하게 지적("한개의 홈페이지를 실시간으로 설정하는거야. 한개의 preview를 가지고, 각 블록마다 Elements, Control 탭이 다르게 나오는거라구")하며 BuilderJS 레퍼런스 스크린샷을 다시 제시, Elements/Controls/Page/Themes 4탭 구조를 명시적으로 요구했다. 슬라이드쇼/사이드바 아이콘 이미지 필드에 실제 미리보기가 없는 것도 별도로 지적.
- **단일 캔버스로 통합**: 섹션 전환 버튼(`activeSection`)을 완전히 제거 — 이제 캔버스 하나가 실제 페이지 순서 그대로 `<Navbar editable>`(로고/탭/계정메뉴, EPIC-136에서 이미 완성) → 슬라이드쇼(신규 `ClickSelectSlot`으로 감싼 실제 `HeroSlideshow`, 클릭하면 선택) → 사이드바 아이콘 대표 미리보기 칩 2개(실제 좌/우 아이콘은 화면 가장자리에 `position:fixed`로 뜨는 트리거라 스크롤 흐름 안에 그대로 넣을 수 없어, 실제 업로드된 이미지를 쓰는 대표 미리보기로 대신하고 그 사실을 라벨에 명시) → 하단 메뉴(Footer)까지 이어 붙인다. 하단 메뉴는 더 이상 별도 전체화면 에디터(`CraftFooterEditor`)로 열리지 않고, 같은 캔버스 안에 `<Frame data={footerCraftState}>`로 인라인 렌더링된다.
- **왼쪽 패널을 4탭으로 재구성**: **Elements** — 페이지 전체 요소(로고/각 탭/계정메뉴 5항목/슬라이드쇼/좌우 사이드바 아이콘) 클릭 이동 목록 + 기존 `Toolbox.tsx`와 동일한 `connectors.create()` 드래그 소스 패턴을 재사용해 하단 메뉴에 새 블록을 추가하는 섹션. **Controls** — 선택된 요소의 실제 설정(기본값, 선택 없으면 안내 문구) — 로고 이미지/슬라이드 이미지/배경 이미지/사이드바 아이콘 이미지 전부에 실제 `<img>` 썸네일 미리보기를 새로 추가해 "preview 어디있어?" 지적에 대응, EPIC-137 과정에서 빠졌던 탭 "배치"(1단/2단) 드롭다운도 복원. **Page** — 지금 무엇을 편집 중인지 안내 + 탭 구조 변경은 사이트 구성 관리로 안내하는 링크. **Themes** — 호버 모션 6종을 상단 탭 전체 + 계정 메뉴 전체에 한 번에 일괄 적용하는 프리셋 목록(개별 조정은 각 요소 Controls에서 그대로 가능).
- **선택 상태 통합(Craft.js와 커스텀 슬롯 시스템을 하나의 selection으로)**: 신규 `CraftBridge` 컴포넌트가 `useEditor()`로 하단 메뉴(Craft) 블록이 새로 선택될 때마다 상위 `selection` state를 `{kind:"craft"}`로 갱신하고, `query`를 ref로 끌어올려 Editor 컨텍스트 밖(최상단 "저장하기" 버튼)에서도 `query.serialize()`를 호출할 수 있게 한다 — Craft.js는 React context 기반이라 DOM 위치와 무관하게 같은 `<Editor>` 트리 안이면 어디서든 상태를 공유할 수 있다는 점을 활용. 헤더/슬라이드쇼/사이드바 슬롯 클릭은 `{kind:"slot", key}`로 별도 관리되며, 둘 다 같은 Controls 탭에서 종류에 따라 다른 패널을 보여준다.
- **저장**: 기존 6개 `site_settings` 키에 더해 `craftBridgeRef.current.query.serialize()`로 얻은 하단 메뉴 `craft_state`까지 "저장하기" 버튼 하나로 `Promise.all`에 함께 묶어 저장한다.
- **검증**: `npx tsc --noEmit`/`npm run lint`(HTML `<a>` 대신 `next/link` 사용하도록 1건 수정)/`npm run build` 전부 0 errors. Browser pane 실측 — 캔버스가 로고→슬라이드쇼(VOGUE 실제 슬라이드 이미지)→사이드바 아이콘 미리보기→하단 메뉴까지 화면 전환 없이 이어져 있음을 스크롤로 확인, 사이드바 아이콘 칩 클릭 시 Controls 탭에 실제 업로드된 기본/호버 이미지 썸네일이 뜸을 확인, Elements 탭에서 "스튜디오" 클릭 시 Controls 탭으로 자동 전환되며 캔버스의 실제 "스튜디오" 탭에 선택 테두리가 뜨고 배치(1단)/색상 필드가 채워짐을 확인, 하단 메뉴 영역을 클릭하면 Craft의 실제 `ContainerBlock 설정`(방향/정렬/여백)이 같은 Controls 탭에 뜸을 확인(Craft·커스텀 슬롯 두 시스템의 selection 통합 검증), Themes 탭 프리셋 목록 렌더링 확인. "저장하기" 클릭 → "저장됐어요" 확인 → 실제 홈페이지(`/`)로 이동해 새로고침까지 왕복해 헤더/슬라이드쇼/하단 메뉴(사업자 정보·링크 행 포함) 전부 회귀 없이 정상 렌더링됨을 확인.
- **다음에 확인 필요**: 사이드바 아이콘의 "대표 미리보기 칩"이 실제 위치(화면 가장자리 fixed)와 다르게 보이는 것에 대해 사용자가 추가로 만족하지 못할 가능성(다른 방식 필요 시 재검토), 하단 메뉴 블록을 Elements 탭에서 드래그로 캔버스에 직접 놓는 조작감(클릭 추가는 검증됐지만 실제 드래그는 이 세션의 반복된 합성 입력 한계로 미검증), Page 탭에 더 실질적인 페이지 단위 설정이 필요한지.
- **변경 파일**: `src/app/admin/navigation/settings/page.tsx`(전면 재작성).

## 2026-08-20 (EPIC-136 — 헤더 편집 캔버스를 "스타일 전용 복제품"에서 실제 `<Navbar>` 그 자체로 전환 + 진짜 자유 드래그)
- **배경**: EPIC-135가 만든 통합 캔버스를 스크린샷과 함께 재검토한 사용자가 세 가지를 정확히 지적 — (1) "실제 홈페이지 상단의 출력되는 모습이랑 '홈페이지 설정' 페이지의 모습이 다르잖아?"(캔버스가 `src/components/craft/chrome/views.tsx`라는 별도 클론이라 실제 로그인 이름 대신 placeholder 텍스트를 보여주는 등 구조적으로 드리프트가 날 수밖에 없었음, 게다가 `site_navigations` 기반 탭만 그리고 `navConfig.ts`의 별도 하드코딩 탭인 "스튜디오"/"마이 페이지"+전역 "글쓰기" 버튼 행 자체를 트리에서 아예 빠뜨리고 있었음), (2) "블록을 눌렀을때 왜 왼쪽에 아무런 설정할수 있는게 없어?"(빈 공간이나 RootContainer를 클릭하면 일반 "DIV 설정" 폴백만 뜨는 사각지대가 있었음), (3) "드래그앤 드롭으로 버튼이든, 이미지, 영상, 무슨 요소든지 자유롭게... 화면 안에서 마음대로 움직일수 있게 해달라"(EPIC-135는 숫자 슬라이더(`rowHeightPx`/`tier1OffsetPx`/`tier2OffsetPx`)만 제공했지 진짜 드래그가 없었음). 사용자에게 두 갈래 선택지(① Navbar.tsx를 실제 캔버스로 완전 재구축 vs ② 현재 구조 유지+부분 개선)를 제시해 "완전 재구축"으로 명시적 확인 받음.
- **핵심 아키텍처 전환**: `src/components/craft/chrome/`(블록 5종+뷰 클론+Craft 리졸버+ChromeTierZone, EPIC-132/135가 쌓아온 전체) + `src/components/admin/craft/ChromeCraftEditor.tsx`를 통째로 삭제. 대신 `Navbar.tsx` 자체에 `editable`/`selectedSlotKey`/`onSelectSlot`/`positionsOverride`/`onOffsetChange`/`deviceOverride` optional prop을 추가해, 관리자 화면이 이 컴포넌트를 그 프롭들만 얹어 그대로 렌더링한다 — 별도 클론을 만들지 않으므로 드리프트가 구조적으로 불가능해졌고, 로그인 세션/드롭다운 메가메뉴/`글쓰기` 버튼처럼 Navbar.tsx가 이미 가진 모든 실제 동작이 관리자 캔버스 안에서도 그대로 동작한다(실제 로그인 이름 "Ethan ki"가 그대로 보임, "스튜디오/마이 페이지/글쓰기" 행도 저절로 포함됨). 각 요소(로고 전체/탭 하나하나/계정 메뉴 5항목)는 신규 `HeaderSlot`(`src/components/HeaderSlot.tsx`)으로 감싸 클릭→선택 표시(`SelectionOverlay`, 옛 `ChromeSelectionOverlay`를 Craft 의존성 없는 공용 위치로 이동)+왼쪽 설정 패널 연동을 부여했다.
- **자유 드래그 메커니즘**: 홈페이지 콜라주(EPIC-108)의 `useFreePosition.ts`(%좌표 + `position:absolute`, 공유된 하나의 `position:relative` 캔버스 전제)를 그대로 재사용하지 않았다 — Navbar.tsx의 로고 줄/탭 줄은 각자 독립된 `position:relative`를 이미 갖고 있어(드롭다운 메가메뉴가 그 위에 의존) 공유 캔버스로 합치려면 그 relative들을 걷어내야 해 회귀 위험이 컸다. 대신 이 저장소가 이미 검증한 더 안전한 패턴(EPIC-117 tier1Tabs의 "translate-y-1/2로 로고 줄에 걸치기" 트릭)을 일반화 — 각 `HeaderSlot`은 원래 flex 자리를 그대로 차지한 채 `transform: translate(dxPx, dyPx)`만 얹어 화면 어디로든 옮겨 보이게 한다(신규 `src/lib/headerLayoutPositions.ts`, `HeaderSlotOffset`). 드래그 핸들은 `FreePositionHandles.tsx`의 포인터 로직을 Craft(`useNode`) 의존 없이 plain props(`editable`/`selected`/`onChange`)로 포크. `position:absolute`가 아니라 `transform`만 쓰기 때문에 어떤 조상이 relative인지와 무관하게 항상 동작하고 문서 흐름 자체를 바꾸지 않아, 메가메뉴/사이드바 등 기존 구조를 전혀 건드리지 않고 얹을 수 있었다.
- **`position:fixed` 함정 발견·수정**: 실측 중 편집 캔버스 안에 `<Navbar editable>`을 그대로 넣었더니 아무것도 안 보이는 버그를 재현 — `topBarRef`의 `position:fixed`(EPIC-104, 스크롤 숨김용)가 뷰포트 기준으로 붙어버려 캔버스 박스 밖으로 튀어올라 실제 사이트 헤더 뒤에 완전히 가려지고 있었다(DOM에는 정상적으로 존재, 화면에만 안 보임). `editable`일 때는 이 wrapper를 `position: relative`로, 스크롤-숨김(`useHideOnScroll`) 결과도 무시하도록, 그에 맞춰 `topBarHeight` spacer div/사이드바 백드롭/`LeftSidebar`/`RightSidebar`도 편집 모드에서는 렌더링하지 않도록 분기 처리해 해결.
- **관리자 화면 재작성**: `src/app/admin/navigation/settings/page.tsx`를 다시 작성 — 섹션을 "홈페이지 헤더 디자인"(신규 캔버스, 왼쪽 320px 설정 패널+오른쪽 실제 `<Navbar editable>`), "슬라이드쇼"(단순 폼으로 복원 — 홈페이지 본문 요소라 헤더 캔버스와 무관), "사이드바 아이콘"(단순 폼으로 복원 — 화면 가장자리 fixed 트리거라 헤더 캔버스 자유 드래그 대상이 아님), "하단 메뉴 관리"(기존 Craft 풀스크린 에디터, 무변경) 4개로 재편. 왼쪽 패널은 `selectedSlotKey`의 접두사(`logo`/`tab:`/`account:`)로 분기해 그 요소 전용 필드(로고 텍스트/이미지/커스텀 폰트, 탭 라벨/색상/hover 모션, 계정 메뉴 공통 스타일)+공통 "위치" 섹션(드래그 안내+"원래 위치로 되돌리기")을 보여준다. 저장 시 `main_logo`/`sidebar_icons`/`top_tab_style`/`account_menu_style`/`hero_slideshow`/신규 `header_positions`(`{pc,mobile}`, 슬롯 키별 오프셋) 6개 `site_settings` 키를 함께 쓴다.
- **검증**: `npx tsc --noEmit`/`npm run lint`/`npm run build` 전부 0 errors(신규 warning 없음). Browser pane 실측 — 실제 홈페이지(`/`)에서 로그아웃 버튼 클릭 없이도 로그인 세션/드롭다운 메가메뉴/멤버십 팝오버가 그대로 정상 동작함을 먼저 확인(회귀 없음), 관리자 캔버스에서 로고/탭/계정 메뉴 항목 각각 클릭→선택 표시+왼쪽 패널 필드 전환 확인. **드래그는 이 환경의 구조적 한계로 `computer` 도구의 합성 드래그로는 반응하지 않아**(이 세션에서 이미 여러 번 확인된, 신뢰되지 않은 합성 포인터 입력을 브라우저가 무시하는 문제) 실제 `PointerEvent`를 직접 디스패치하는 스크립트로 우회 검증: 탭을 60px/100px 이동 → `transform` 즉시 반영(드롭다운 메가메뉴도 함께 이동) → 저장 → 페이지 전체 새로고침 후에도 관리자 캔버스에 유지 → 실제 공개 홈페이지(1280px 데스크톱 폭)에서도 동일한 `transform`이 적용됨을 DOM에서 직접 확인(PC/모바일 값이 독립적으로 저장되는 것도 함께 확인 — 375px대 좁은 폭에서는 의도대로 모바일 슬롯이 비어 있어 오프셋이 적용되지 않음을 확인, 처음엔 버그로 오인했으나 실제로는 설계대로 동작한 것이었음). 검증 후 테스트로 저장한 오프셋은 Management API로 원상복구. 슬라이드쇼/사이드바 아이콘/하단 메뉴 관리 3개 섹션도 각각 실제 데이터(슬라이드 12개, 사이드바 아이콘 폼, 하단 메뉴 실제 콘텐츠)가 정상 로드됨을 확인.
- **다음에 확인 필요**: 사람이 실제 마우스로 드래그 핸들을 직접 잡고 옮기는 손맛(합성 포인터 검증으로 로직은 확인했지만 실제 트랙패드/마우스 제스처는 미검증), 계정 메뉴 5항목의 스타일이 지금은 여전히 하나의 그룹 설정(개별 스타일 아님, 위치만 개별)이라 "항목마다 스타일도 따로"까지 원하는지 확인 필요, 사이드바 아이콘 자체를 이 자유 드래그 캔버스에 포함시켜 달라는 후속 요청이 있을 수 있음(이번엔 화면 가장자리 fixed 트리거라는 이유로 의도적으로 범위 밖).
- **변경 파일**: `src/components/Navbar.tsx`, `src/components/HeaderSlot.tsx`(신규), `src/components/SelectionOverlay.tsx`(이동, 옛 `src/components/craft/chrome/ChromeSelectionOverlay.tsx`), `src/lib/headerLayoutPositions.ts`(신규), `src/app/admin/navigation/settings/page.tsx`, `src/components/craft/chrome/**`(전체 삭제), `src/components/admin/craft/ChromeCraftEditor.tsx`(삭제).

## 2026-08-20 (EPIC-135 — "홈페이지 설정 관리" 4개 분리 화면 + EPIC-134 GrapesJS를 전부 폐기하고 실제 페이지 순서 그대로 쌓은 craft.js 통합 캔버스 하나로 전환)
- **배경**: 사용자가 GrapesJS 기반 헤더 에디터(EPIC-134) 스크린샷을 보고 "이걸로 어떻게 상단 탭과 사용자 메뉴를 디자인하라는 거지? 정말 내가 원하는게 뭔지 모르는구나"라고 재차 지적하며, PC/Tab/Mobile 토글 + 왼쪽 설정창(Elements/Controls/Page/Themes) + 오른쪽 실제 페이지 블록을 클릭·드래그로 편집하는 BuilderJS류 화면을 레퍼런스로 요구 — "지금처럼 메인 로고 설정 따로, 사용자 메뉴 설정 따로가 아니라, 한 화면에서 모든 설정을 할 수 있는 걸 원해". 이어서 "페이지가 출력되는 위치 그대로 나오는 상태에서 페이지 위의 요소들을 클릭 → 왼쪽 설정창, 드래그 드롭으로 블록 위치 조정 할수 있어야해"라고 설정 패널 위치(왼쪽)를 명시적으로 정정.
- **설계**: 이미 프로덕션에 쓰이는 성숙한 Craft.js 인프라(EPIC-132가 만든 로고/사이드바아이콘/상단탭/계정메뉴 4개 블록)를 재사용해, `main_logo`→`account_menu_style`→`top_tab_style`(1단+2단)→`hero_slideshow`→`sidebar_icons`(좌/우) 순서로 하나의 `Element is={RootContainer} canvas` Craft 트리에 조립. 저장 방식은 그대로 `site_settings`(각 블록의 `related.settings`가 Craft `setProp`과 페이지의 기존 shim setState를 동시에 호출) — `page_builder.craft_state` 같은 새 저장 경로를 만들지 않아 하단 메뉴(별도 Craft Editor 인스턴스, `query.serialize()`가 전체 트리만 직렬화하는 라이브러리 제약으로 같은 캔버스에 못 넣음)만 예외적으로 옆 섹션 전환 버튼으로 분리.
- **신규**: `ChromeSlideshowBlock.tsx` — 실제 공개 홈페이지가 쓰는 `HeroSlideshow.tsx`(`device="both"`)를 그대로 재사용해 캔버스에서도 진짜와 동일하게 보이는 슬라이드쇼 블록, 설정 패널에 슬라이드/여백/objectFit/여백 배경 이미지 CRUD 전부 이식. `ChromeCraftEditor.tsx`를 캔버스(오른쪽)+`SettingsSidebar`(왼쪽) 레이아웃으로 재구성하고 `ChromeDeviceToggle`(PC/태블릿/모바일 3단 폭 토글) 신설 — 실제 저장 데이터는 이 저장소 전체가 `{pc,mobile}` 2단으로만 설계돼 있어 태블릿 폭은 PC 값의 미리보기 전용(별도 태블릿 데이터를 새로 만들지 않음). `ChromeLogoBlock`의 설정 패널에 그동안 빠져있던 커스텀 폰트 파일 관리(업로드/사용 토글/삭제)를 새로 추가 — 옛 폼에는 있었는데 EPIC-132가 Craft로 옮기며 빠뜨렸던 기능 회귀를 이번에 메움.
- **재작성**: `src/app/admin/navigation/settings/page.tsx`를 대규모로 재작성 — `activeSection`을 5종에서 `"unified" | "footer"` 2종으로 단순화, HOTFIX-134가 만들고 EPIC-134가 지웠던 상단 탭 좌우 순서 드래그(`updateTabStyle`/`tabOrderValue`/`handleTabDrop`/fractional-order, `TopTabStyleEntry.order`)를 복원, 옛 로고/슬라이드쇼/사이드바아이콘 폼 필드 JSX와 GrapesJS(`HeaderGrapesEditor`) 관련 코드를 전부 제거. 저장 버튼(`handleSaveAll`)이 5개 `site_settings` 키를 함께 쓰면서 `unified_header_layout`도 매번 빈 값(`{pc:{items:[]},mobile:{items:[]}}`)으로 함께 써서, `Navbar.tsx`의 "값이 있으면 그 값 우선" 폴백 규칙(EPIC-134가 만듦) 때문에 저장한 내용이 실제 사이트에 반영 안 되는 사고를 방지. `HeaderGrapesEditor.tsx`(EPIC-134) 파일 자체를 삭제(다른 참조 없음 확인).
- **실측 중 재발견한 Craft.js 버그**: `<Frame>` 트리 안에 두 개의 `ChromeTierZone(...)` 호출을 `<>...</>`(React.Fragment)로 감싸 넣자 "Invariant failed: The component type specified for this node (undefined) does not exist in the resolver"로 즉시 크래시 — Craft의 정적 트리 파서가 Fragment까지 하나의 노드로 해석하려 시도하는데, `React.Fragment`는 `.name`/`.displayName`이 없는 Symbol이라 오류 메시지에 `(undefined)`로 찍힌다(`node_modules/@craftjs/core`의 `resolveComponent` 내부 `invariant` 호출부를 직접 역추적해 확정). `ChromeTierZone` 자체는 HOTFIX-134가 이미 검증해 둔 대로 JSX 태그가 아니라 일반 함수처럼 직접 호출(`{ChromeTierZone({...})}`)했지만, 그 두 호출을 감싸던 Fragment가 새로운 원인이었다 — Fragment를 없애고 `{조건 && ChromeTierZone(...)}` 형태로 각각 독립시켜 해결.
- **검증**: `npx tsc --noEmit`/`npm run lint`/`npm run build` 전부 0 errors(신규 warning 없음). Browser pane 실측(관리자 로그인 세션) — 통합 캔버스에서 로고/계정메뉴(클릭→왼쪽 패널에 사용자 메뉴 설정 표시→"굵게" 체크박스 토글이 캔버스에 즉시 반영)/상단 탭 1단·2단 드롭존(스튜디오·마이 페이지가 1단에, About Silo 등 나머지가 2단에 정상 배치)/슬라이드쇼(클릭→왼쪽에 슬라이드별 파일/제목/설명 편집 폼, 실제 슬라이드 이미지가 캔버스에 그대로 보임)/좌측 사이드바 아이콘까지 순서·클릭 선택·왼쪽 설정 패널 반영을 전부 확인. "저장하기" 클릭 → "저장됐어요" 확인 → 실제 홈페이지(`/`)로 이동해 새로고침까지 왕복해, 로고·계정 메뉴(관리자/Lautrec/마이페이지/Ethan ki/로그아웃)·상단 탭(About Silo/사일로 상점/온라인 도슨트/살롱데상)·좌우 사이드바가 회귀 없이 정상 렌더링됨을 확인.
- **다음에 확인 필요**: 실제 마우스로 탭 칩을 드래그해 1단/2단 사이·같은 단 안에서 순서를 바꾸는 손맛(브라우저 자동화가 신뢰되지 않은 drop 이벤트의 `dataTransfer.getData()`를 빈 값으로 반환하는 환경 제약 때문에 자동 검증 불가 — HOTFIX-134에서 이미 같은 한계를 문서화했고 이번엔 로직 자체를 바꾸지 않았음), 로고 커스텀 폰트 관리 UI(추가/토글/삭제) 실사용, 태블릿 폭 미리보기가 실제 태블릿에서도 자연스러운지.
- **변경 파일**: `src/app/admin/navigation/settings/page.tsx`, `src/components/admin/craft/ChromeCraftEditor.tsx`, `src/components/craft/chrome/resolver.ts`, `src/components/craft/chrome/blocks/ChromeLogoBlock.tsx`, `src/components/craft/chrome/blocks/ChromeSlideshowBlock.tsx`(신규), `src/components/admin/grapes/HeaderGrapesEditor.tsx`(삭제).

## 2026-08-20 (HOTFIX-134.6 — GrapesJS 헤더 에디터 스타일시트 누락으로 패널 전체가 "처참하게" 보이던 버그 수정)
- **배경**: 사용자가 "헤더(상단 탭 + 사용자 메뉴)" 관리자 화면 스크린샷을 첨부하며 "이 페이지 빌더는 정말 처참한 수준이다, 이게 최선이냐"고 강하게 지적 — 레이어 패널의 눈 모양 아이콘이 원본 SVG 크기 그대로 거대하게 나오고, 스타일 패널 드롭다운이 브라우저 기본 화살표로 나오는 등 전체가 스타일이 하나도 안 입혀진 모습이었다.
- **원인**: `HeaderGrapesEditor.tsx`(EPIC-134)가 `import("grapesjs")`로 JS 모듈만 동적 로드하고, GrapesJS 자체 스타일시트(`grapesjs/dist/css/grapes.min.css`)는 아예 import한 적이 없었다 — GrapesJS는 이 CSS 없이는 패널 배경/테두리/아이콘 크기/버튼 모양이 전혀 적용되지 않고 브라우저 기본 스타일 그대로 렌더링된다. "GrapesJS 자체가 조악하다"가 아니라 "필수 스타일시트 import 한 줄이 빠진" 단순 누락 버그였다.
- **수정**: 파일 상단에 `import "grapesjs/dist/css/grapes.min.css";` 추가.
- **검증**: `npx tsc --noEmit`/`npm run lint`/`npm run build` 전부 0 errors. Browser pane 실측 — 수정 전/후 스크린샷 비교(레이어 패널 다크 테마 적용, 아이콘이 78개 전부 13×13px로 정상 축소, 스타일 패널 드롭다운이 `appearance: none`으로 커스텀 스타일 적용, 캔버스에 실제 탭/계정 메뉴 항목 칩이 정상 배치돼 보임), 콘솔에 신규 에러 없음.
- **변경 파일**: `src/components/admin/grapes/HeaderGrapesEditor.tsx`.

## 2026-08-19 (HOTFIX-134.5 — 프리셋 배경 점 그리드의 진짜 원인 제거: 화면에 고정된 2D 별 레이어를 완전히 삭제)
- **배경**: 다른 컴퓨터에서 origin/main으로 동기화 후, 사용자가 스크린샷과 함께 "점 그리드가 화면을 패닝할 때마다 계속 따라다닌다"고 신고 — HOTFIX-134.4가 "실제 재현·수정"했다고 기록한 것과 같은 증상이 여전히 남아있었다.
- **진짜 원인**: HOTFIX-134.4는 별 점 SVG가 15~40배로 확대 렌더링되던 **크기** 버그는 실제로 고쳤지만, 더 근본적인 구조 문제를 놓쳤다 — `PresetBackground.tsx`의 별 점은 `<Canvas>` 밖의 순수 2D DOM(`position: fixed`)에 CSS `background-repeat`로 그려지고 있었다. 즉 3D 카메라와 완전히 무관한 레이어라, 카메라를 아무리 돌리거나 패닝해도 이 점들은 화면에 그대로 붙박여 있을 수밖에 없었다 — "그리드가 화면을 따라다닌다"는 신고는 정확히 이 현상이었다(크기를 고쳐도 애초에 화면 고정 레이어인 이상 절대 해결될 수 없는 종류의 버그). 게다가 `AboutSiloUniverse.tsx`의 `UniverseParticles`(drei `<Stars>`/`<Sparkles>`/`<ShootingStars>`, `<Canvas>` 안에서 `backgroundMode`와 무관하게 이미 항상 렌더링되는 진짜 3D 별)가 이미 같은 역할을 정확하게 하고 있어서, 이 2D 레이어는 애초에 불필요한 중복이었다.
- **수정**: `PresetBackground.tsx`에서 별 점을 그리던 `starDotDataUrl`/`starLayerStyle` 및 3개 레이어 `<div>`를 전부 삭제 — 배경 그라디언트 색상 + 흐릿한 원거리 행성 2개만 남긴다. 실제 별은 이미 있던 `UniverseParticles`(3D, 카메라 이동에 정상적으로 시차가 생김)가 전담.
- **환경 노트**: 이 세션에서 다른 컴퓨터의 커밋을 pull한 뒤 `npm run build`가 `Module not found: Can't resolve 'grapesjs'`로 실패했다 — `package.json`/`package-lock.json`은 최신이었지만 `node_modules`가 그대로라 그랬던 것, `npm install` 실행으로 해결(정상적인 멀티 디바이스 동기화 단계, 코드 버그 아님).
- **검증**: `npx tsc --noEmit`/`npm run lint`/`npm run build` 전부 0 errors. Browser pane 실측 — 수정 전 스크린샷과 동일한 격자 무늬가 재현된 것을 먼저 확인 후 수정, 수정 후 스크린샷에서 격자 완전히 사라짐 확인 + 카메라를 실제로 드래그해 패닝한 전/후 스크린샷을 비교해 별과 행성이 함께 자연스럽게 시차 이동함(화면에 고정되지 않음)을 확인.
- **변경 파일**: `src/components/about-silo/PresetBackground.tsx`.

## 2026-08-19 (HOTFIX-134.4 — 프리셋 배경 별 점 그리드 버그 실제 재현·수정)
- **배경**: 사용자가 "그리드 버그가 있는건 확실하니까, 원인을 찾아서 해결해"라고 재차 지시 — HOTFIX-134.2/134.3의 "우주 공간 오브젝트 스프라이트 밀도" 진단만으로는 부족하다고 판단해 `PresetBackground.tsx`의 별 점 렌더링 자체를 다시 처음부터 조사.
- **실제 재현 성공**: 별 점을 CSS `radial-gradient` 트릭 대신 SVG 이미지로 바꿔보는 과정에서 그 자체로 "그리드 버그"를 실제로 재현했다 — SVG를 `width="8" height="8"`(작은 고유 크기)로 만들고 `background-size: 120px 120px`(훨씬 큰 타일 간격)로 반복시키면, 브라우저가 SVG 전체를 8px→120px로 **15~40배 확대**해서 채운다. 그 결과 원래 "옅은 별 점 하나"였어야 할 이미지가 화면을 뒤덮는 커다란 원형 얼룩의 규칙적인 그리드로 렌더링됨을 Browser pane 스크린샷으로 직접 확인 — 사용자가 신고한 스크린샷과 정확히 같은 종류(간격이 일정한 원형 얼룩 그리드)의 결과.
- **수정**: `starDotDataUrl(size, offset)` — 별 점 SVG의 `width`/`height`(고유 크기)를 매번 실제 타일 간격(`size`, 120/220/340px)과 정확히 일치시켜 확대율이 항상 1:1이 되도록 크기별로 별도 data URI를 생성. 점의 상대 위치(`offset`)는 그 SVG 안의 `cx`/`cy`로 직접 지정 — `background-position` 없이도 각 타일 안에서 정확한 자리에 그려진다. Browser pane 스크린샷으로 수정 후 다시 확인 — 옅고 작은 별 점들이 정상적으로 흩뿌려짐, 큰 원형 얼룩 그리드 사라짐.
- **검증**: `npx tsc --noEmit`/`npm run lint`/`npm run build` 전부 0 errors, 신규 warning 없음. Browser pane으로 수정 전(재현) → 수정 후(정상) 스크린샷 비교 완료, 콘솔에 신규 에러 없음(기존 무해한 노이즈만).
- **변경 파일**: `src/components/about-silo/PresetBackground.tsx`.

## 2026-08-19 (HOTFIX-134.3 — 우주 공간 오브젝트 사본 간 크기/반짝임 위상 다양화)
- **배경**: HOTFIX-134.2에서 "프리셋 배경 그리드처럼 보이는 문제"의 원인을 우주 공간 오브젝트 스프라이트 92개로 진단해 보고한 뒤, 사용자가 "그대로 두기(다른 방법으로 해결)"를 선택 — 디자인/개수/불투명도는 유지하되 그리드처럼 보이는 겉모습만 다른 방법으로 고쳐달라는 지시.
- **진짜 원인 특정**: `SpaceObjectSprite`/`SpaceObjectModel`가 "개수"로 만든 사본(예: "손그림 별 클래식" 28개)마다 전부 `obj.id` **하나만** 시드로 썼다 — 그 결과 같은 디자인의 모든 사본이 완전히 같은 크기로 렌더링되고, 반짝임(sin 파동)도 완전히 같은 위상으로 동시에 맥동했다. 크기·리듬이 완전히 동일한 사물 수십 개가 무작위 위치에 흩뿌려지면(실제 3D 좌표 자체는 진짜 랜덤이었음, `spaceObjectDefaultPosition` 확인) 사람 눈에는 무작위 산개가 아니라 인위적인 반복 패턴("그리드")으로 읽힌다 — 스크린샷의 "이상한 점 그리드"는 이 시각적 착시였다.
- **수정**: `SpaceObjectsLayer`가 각 사본에 `index`를 새로 전달, `SpaceObjectSprite`/`SpaceObjectModel` 둘 다 시드 계산에 `obj.id`뿐 아니라 `index`도 섞어(`${obj.id}#${index}` 해시) 사본마다 크기(0.7~1.3배 무작위)와 회전/반짝임 위상이 자연스럽게 달라지게 했다. 디자인(이미지 URL)·개수·전역 위치 분포·선택 글로우 불투명도 등 관리자가 실제로 설정한 값은 전혀 건드리지 않음 — 순수하게 "같은 디자인의 여러 사본을 시각적으로 덜 인위적으로 보이게" 렌더링만 조정.
- **검증**: `npx tsc --noEmit`/`npm run lint`/`npm run build` 전부 0 errors, 신규 warning 없음. Browser pane으로 `/about-silo` 재렌더링 확인 — 이전 스크린샷들과 비교해 오브제 크기가 눈에 띄게 다양해짐을 확인, 콘솔에 신규 에러 없음(기존 무해한 노이즈만).
- **변경 파일**: `src/components/about-silo/AboutSiloUniverse.tsx`.

## 2026-08-19 (HOTFIX-134.2 — 오브젝트 선택 글로우 정적화+opacity 설정, 업로드 실패 무음 처리 수정)
- **배경**: 사용자가 About Silo 우주 관련 3건을 신고 — (1) Universe Settings에서 배경 프리셋 "수채화 블루"/"딥 블루" 선택 시 화면에 이상한 점 그리드가 생긴다, (2) 오브제 선택 시 나타나는 하늘색 구체가 커졌다 작아졌다(맥동) 하는데 opacity를 설정 가능하게 하고 맥동 없는 정적 파스텔 레이어로 바꿔달라, (3) SILO/My Page 행성에 오브제(.glb) 추가가 안 된다(60MB 넘는 파일이라 그런 것 같다).
- **(2) 선택 글로우 정적화 + opacity 설정**: `UniverseObjectModel`/`SpaceObjectGlow`가 `useFrame`으로 매 프레임 scale/opacity를 사인파로 맥동시키던 로직을 완전히 제거 — 정적인 파스텔 하늘색(`#bae6fd`, 기존 두 곳이 쓰던 서로 다른 색 `#7dd3fc`/`#a8d8ff`를 하나로 통일) 레이어로 교체. 새 설정 `UniverseConfig.selectionGlowOpacity`(기본 0.5)를 `aboutSiloUniverseConfig.ts`에 추가하고 `UniverseSettingsPanel.tsx`에 슬라이더 신설, `AboutSiloUniverse.tsx`에 신규 `SelectionGlowOpacityContext`(React Context)로 Scene 루트에서 각 글로우 컴포넌트까지 중간 컴포넌트 props를 거치지 않고 전역 전달.
- **(3) 오브제 업로드 실패가 조용히 무시되던 버그 수정 + 근본 원인 진단**: `PlanetSettingsPanel.tsx`(`handleUpload`, 4곳: 텍스처/위성 디자인/캐릭터/오브젝트)·`UniverseSettingsPanel.tsx`(`handleSpaceUpload`)·`ObjectInspectorPanel.tsx`(썸네일 업로드) 전부 업로드 실패 시 `uploadingField`만 원상복구되고 아무 알림이 없어 "그냥 작동을 안 한다"로 보였다 — 이제 실패 사유를 `alert()`로 보여준다. **근본 원인 확인**: Supabase Management API로 이 프로젝트의 스토리지 설정을 조회한 결과 `fileSizeLimit: 52428800`(정확히 50MB)이 프로젝트 전역에 걸려 있음을 확인, 300MB로 올리려 시도했으나 `"File size limit more than 52,428,800 bytes. Please upgrade the project to a paid plan to unlock higher file size limits."` 응답 — **이 프로젝트가 Supabase 무료(Free) 플랜이라 50MB가 API로도 못 올리는 하드 캡**임을 확인했다. 60MB+ 파일은 (a) Supabase 프로젝트를 유료 플랜으로 업그레이드하거나 (b) .glb 파일 자체를 50MB 미만으로 압축(Draco 압축, 텍스처 해상도 축소 등)하기 전까지는 근본적으로 업로드할 수 없다 — 코드로 우회 불가능한 제약이라 사용자에게 이 사실과 옵션을 명시적으로 전달.
- **(1) 그리드 버그는 코드 버그가 아니라 실제 데이터로 확인됨**: 로컬 dev/배포된 dev.silostore.net 둘 다 프리셋 배경 자체(PresetBackground.tsx의 CSS 별 점 패턴, 1.4px 크기 정상 렌더링 확인)에서는 재현되지 않았다 — Universe Settings의 "우주 공간 오브젝트" 목록을 열어보니 손그림 스타일 별 스프라이트 7종이 총 92개(19+8+28+8+13+8+8) 씬에 흩뿌려져 있고, 그중 "손그림 별 클래식"(28개) 이미지를 직접 열어보니 사용자가 신고한 스크린샷의 주황/노랑 스크리블 모양과 정확히 일치함을 확인 — 밝은 "크림 백지" 배경에서는 묻혀 안 보이다가 어두운 두 프리셋(딥 블루/수채화 블루) 배경에서 대비 때문에 두드러져 보였을 가능성이 높다. 코드 버그가 아니라 실제 저장된 관리자 설정(스프라이트 92개)이라 사용자 확인 없이 삭제/축소하지 않고 원인만 확인 후 보고 — 사용자의 다음 지시 대기.
- **검증**: `npx tsc --noEmit`/`npm run lint`/`npm run build` 전부 0 errors, 신규 warning 없음. Browser pane으로 Universe Settings 패널에 새 불투명도 슬라이더가 정상 렌더링됨을 확인, 콘솔에 신규 에러 없음(기존 사이트 전반의 무해한 노이즈만).
- **변경 파일**: `src/lib/aboutSiloUniverseConfig.ts`, `src/components/about-silo/AboutSiloUniverse.tsx`, `src/components/about-silo/UniverseSettingsPanel.tsx`, `src/components/about-silo/PlanetSettingsPanel.tsx`, `src/components/about-silo/ObjectInspectorPanel.tsx`.

## 2026-08-19 (HOTFIX-134.1 — 통합 헤더 레이아웃 PC/모바일 독립 설정)
- **배경**: 사용자가 EPIC-134(GrapesJS 통합 헤더 캔버스) 직후 "pc/모바일 독립으로 설정할 수 있게 해야지"라고 신고 — 그 EPIC이 "의도적으로 범위 밖"으로 명시해뒀던 항목(PC/모바일 공용 단일 목록)을 다른 모든 설정(`main_logo`/`sidebar_icons`/`top_tab_style`/`account_menu_style`)과 동일하게 `{pc, mobile}` 독립값으로 확장.
- **`headerLayoutSettings.ts`**: `HeaderLayoutValue`를 `HeaderLayoutConfig`(단일 목록)에서 `{ pc: HeaderLayoutConfig; mobile: HeaderLayoutConfig }`로 변경. `normalizeHeaderLayout()`이 신버전(`{pc,mobile}`)과 구버전(EPIC-134 최초 출시분, `{items:[...]}` 단일 목록) 둘 다 처리 — 구버전 값은 pc/mobile 양쪽에 그대로 복제해 back-compat(이미 저장해둔 레이아웃이 사라지지 않음). `defaultHeaderLayoutValue()` 신규.
- **`page.tsx`(관리자 설정)**: `headerLayoutValue` state를 다른 섹션들과 동일한 `chromeDeviceTab` shim 패턴으로 전환, `HeaderGrapesEditor`에 `key={chromeDeviceTab}` 추가(GrapesJS 캔버스는 마운트 시의 initialItems만 시드로 쓰고 이후 prop 변화에 반응하지 않으므로, 기기 탭 전환 시 강제 리마운트가 필요 — 다른 Craft 에디터들과 동일한 이유). "헤더" 섹션을 PC/모바일 토글이 보이는 섹션 목록에 추가.
- **`Navbar.tsx`**: `headerLayoutValue`(fetch 결과, `{pc,mobile}`)에서 `isMobileViewport`에 맞는 쪽만 고르는 `headerLayout` 파생값 추가(다른 3개 설정과 동일한 패턴) — 이후 렌더링 로직(`unifiedHeaderItems` 등)은 이 값만 참조.
- **검증**: `npx tsc --noEmit`/`npm run lint`/`npm run build` 전부 0 errors(HOTFIX-116 교훈대로 build까지 확인). **실사용 검증**: 관리자 세션으로 PC 탭에서 저장 → 모바일 탭으로 전환(캔버스 리마운트 확인) → 저장 → DB에서 `setting_value.pc`/`setting_value.mobile`이 각각 독립된 12개 항목 배열로 저장됨을 직접 조회 확인(PC 저장이 모바일 저장에 덮어써지지 않음, 반대도 마찬가지). 데스크톱 뷰포트/375px 모바일 뷰포트 둘 다 새로고침해 각자 올바른 쪽(`pc`/`mobile`)을 읽어 렌더링함을 DOM으로 확인. 검증 후 테스트 데이터는 Management API로 삭제해 원상복구.
- **다음에 확인 필요**: 실제로 PC와 모바일에 서로 다른 순서/스타일을 넣어보는 것(이번 세션은 구조적 독립성만 검증 — 두 값을 실제로 다르게 만들어 다른 화면에서 다르게 보이는지까지는 미확인, 메커니즘상 문제없을 것으로 판단되나 사람이 직접 확인 권장).
- **변경 파일**: `src/lib/headerLayoutSettings.ts`, `src/app/admin/navigation/settings/page.tsx`, `src/components/Navbar.tsx`.

## 2026-08-19 (EPIC-134 — GrapesJS 기반 통합 헤더 캔버스 에디터, 상단 탭+사용자 메뉴 폐기·통합)
- **배경**: 사용자가 "현재 6개 탭 방식 Form 기반 설정창은 WYSIWYG 빌더 요건을 전혀 충족하지 못한다"며 GrapesJS(또는 동급 캔버스 기반 Drag & Drop 에디터) 도입을 명시적으로 지시 — 기존 "상단 탭 디자인"/"사용자 메뉴 디자인"(Craft.js 기반, EPIC-132/HOTFIX-132.1/132.2) 두 탭을 완전히 폐기하고 하나의 캔버스에서 탭과 계정 메뉴 항목을 자유롭게 섞어 배치(예: "로그아웃 옆에 스튜디오 탭을 끌어다 놓을 수 있어야 함")할 것을 요구.
- **`grapesjs@0.23.5` 신규 설치**: 이 저장소 최초의 "vanilla-JS 라이브러리를 ref 컨테이너에 imperative하게 마운트/파괴"하는 패턴(`next/dynamic` 대신 `useEffect` 내부 동적 `import("grapesjs")` + cleanup에서 `editor.destroy()`) — 기존 Craft.js(선언적 React 컴포넌트)와는 근본적으로 다른 통합 방식이라 이 리포에 전례가 없었다.
- **`src/lib/headerLayoutSettings.ts`(신규)**: `HeaderLayoutItem`(`{id, type: "tab"|"menu", refId, style}`) 순서 목록 — GrapesJS가 만드는 임의 HTML을 그대로 저장/렌더링하지 않고, "탭이든 계정 메뉴든 실제 자산(NavTab.key / 고정 5개 메뉴 키)을 참조만 하는 구조화된 순서 목록"으로 제한해 설계했다(중요한 안전장치 — 아래 참고).
- **`src/components/admin/grapes/HeaderGrapesEditor.tsx`(신규)**: GrapesJS 캔버스를 "임의 HTML을 그리는 범용 페이지 빌더"가 아니라 커스텀 컴포넌트 타입 2종(`header-item` 칩 / `header-row` 컨테이너)만 존재할 수 있는 제한된 구조적 에디터로 설계 — 블록 매니저(좌, 실제 `site_navigations` 탭 7개 + 고정 계정 메뉴 5개)/스타일 매니저(우, 색상·크기·굵기·여백)/레이어 매니저까지 3단 레이아웃. 저장 시 `header-row`의 자식 컴포넌트 트리를 그대로 `HeaderLayoutItem[]`로 직렬화(`component.getStyle()`/`getAttributes()`) — `site_settings.unified_header_layout`에 upsert.
- **`Navbar.tsx`(실제 사이트 헤더) 연동 — 핵심 안전장치**: EPIC-132 계열이 지금까지 한 번도 건드리지 않았던 유일한 이유(사이트 전체 블라스트 반경)를 감안해, `unified_header_layout`이 **없으면 100% 기존 3분할 레이아웃(tier1/계정영역/tier2) 그대로**(완전한 opt-in) — 값이 있을 때만 `tier2Tabs`의 `<nav>` 한 줄에 탭+계정 메뉴를 뒤섞은 순서로 렌더링한다. 이때도 GrapesJS가 만든 raw HTML을 직접 주입하지 않고, 기존 `renderTab()`(드롭다운/메가메뉴/hover 모션/권한 게이팅)과 신규 `renderMenuItem()`(로그인 상태 게이팅 5개 분기, 기존 JSX를 함수로 추출한 것뿐 — 로직 변경 없음)을 그대로 재사용해 실제 인터랙션이 전혀 깨지지 않는다. 항목별 스타일 오버라이드는 `<span style>` 인라인 wrapper(CSS 상속)로 적용.
- **의도적으로 범위 밖으로 미룸**: (1) 로고/슬라이드쇼/하단 메뉴를 GrapesJS 블록으로 흡수하는 것(요구사항 3의 나머지 절반) — 이번엔 "상단 탭+사용자 메뉴 통합"(요구사항 2의 핵심)만 범위로 확정, 나머지 4개 탭은 기존 폼 그대로 유지. (2) PC/모바일 독립 레이아웃 — 다른 모든 설정과 달리 이 통합 헤더는 PC/모바일 공용 단일 목록(GrapesJS Device Manager까지 이번 스코프에 넣으면 위험도가 커짐). (3) 관리자 캔버스 옆 실시간 iframe 미리보기 — GrapesJS 칩은 실제 사이트 스타일을 그대로 재현하지 않는 자리표시자라 유의미하지 않다고 판단.
- **검증**: `npx tsc --noEmit`/`npm run lint`/`npm run build` 전부 0 errors(HOTFIX-116 교훈에 따라 build까지 실행). **Browser pane 실제 로그인 세션(관리자)으로 전체 파이프라인 실사용 검증 완료**: (1) `/admin/navigation/settings` "헤더" 섹션 진입 → 블록 매니저에 실제 탭 7개+메뉴 5개 렌더링, 레이어 매니저에 기본 시드 12개 항목 확인, (2) "저장하기" 클릭 → `site_settings.unified_header_layout` 실제 DB 반영 확인, (3) 홈페이지 새로고침 → `<nav>` 한 줄에 탭+계정 메뉴 12개 항목이 실제로 뒤섞여 렌더링됨을 DOM으로 직접 확인("About Silo | 사일로 상점 | ... | 관리자 | Lautrec | ... | 로그아웃" 한 줄), 콘솔 신규 에러 없음. **검증 후 테스트로 저장한 `unified_header_layout` 값은 Management API로 삭제해 원상복구**(EPIC-088 관례 따름) — 이 코드가 배포돼도 실제 관리자가 새 에디터에서 직접 저장하기 전까지는 기존 3분할 레이아웃이 그대로 유지된다.
- **다음에 확인 필요**: 실제 마우스로 블록을 캔버스에 드래그해 순서를 섞는 조작감(이번 세션은 초기 렌더/저장/조회 왕복만 확인, 드래그 자체는 GrapesJS 라이브러리 표준 기능이라 별도 커스텀 로직 없음 — 그래도 실사용 확인 권장), 통합 레이아웃 활성 상태에서 멤버십 팝오버 위치(계정 영역이 이제 비어있어 팝오버가 화면 우상단에 앵커되는데 트리거 버튼과 시각적으로 멀어질 수 있음 — 기능은 정상, 위치만 개선 여지), 로고/슬라이드쇼/하단 메뉴까지 GrapesJS로 통합할지 여부.
- **변경/신규 파일**: `package.json`/`package-lock.json`(grapesjs 추가), `src/lib/headerLayoutSettings.ts`(신규), `src/components/admin/grapes/HeaderGrapesEditor.tsx`(신규), `src/app/admin/navigation/settings/page.tsx`(topTabs/accountMenu 섹션 제거+header 섹션 추가, 관련 죽은 코드/import 정리), `src/components/Navbar.tsx`(unified_header_layout 조회+선택적 렌더링 분기 추가).

## 2026-08-19 (HOTFIX-132.2 — 오브젝트 클릭 줌인 거리 슬라이더가 전혀 작동 안 하던 버그 수정)
- **배경**: 사용자가 "행성의 '오브제' 클릭하면 줌인되는 정도를 조절하는게 전혀 작동이 안되는데?"라고 신고 — HOTFIX-132.1이 추가한 `ObjectInspectorPanel`의 "클릭 시 줌인 거리" 슬라이더(`focusDistanceMultiplier`, 1~8배)가 실제로는 아무 효과가 없었다. 그 항목 자체가 "3D 줌인 거리 수정은... 이 환경의 스크린샷 기능이 계속 실패해 실제 줌 결과를 시각적으로 확인하지는 못했다"고 명시했던 대로, 실제로 코드에 두 가지 결함이 있었다.
- **원인 (1) — 전역 `minDistance` 하한이 슬라이더를 대부분 무력화**: `<CameraControls minDistance={0.6}>`는 카메라-컨트롤 라이브러리가 매 프레임 강제하는 하한이라, `boundingRadius * multiplier`가 0.6보다 작으면(장식 오브젝트 대부분이 여기 해당 — 정규화된 목표 크기가 `행성 반지름 * 0.45 * scale(기본 0.6)`이라 boundingRadius가 보통 0.1~0.3 수준, 즉 슬라이더 기본값 2.4를 포함한 하위~중간 구간 전체가 dead zone) 배율을 아무리 바꿔도 실제 거리는 항상 0.6으로 눌렸다 — 옛 코드의 `Math.max(..., controls.minDistance, 0.6)`은 이 라이브러리 강제를 그대로 따라간 것이라 이 클램프를 지웠어도 라이브러리가 다음 프레임에 다시 0.6으로 되돌렸을 것. 그 하한의 원래 목적(행성 표면 클리핑 방지)은 행성처럼 큰 오브젝트 기준이라 훨씬 작은 개별 오브젝트를 들여다볼 때는 적용될 이유가 없음을 확인, 오브젝트 포커스 중에만 `controls.minDistance`를 그 오브젝트가 실제로 필요로 하는 거리로 임시 하향(`BASE_MIN_DISTANCE=0.6`보다 커지지는 않게 클램프)하고, 포커스를 벗어나면(행성/게시물 선택, 패널 닫기, 리셋 — `restoreBaseMinDistance()`) 원래 하한으로 되돌려 일반 자유 탐색에서는 여전히 행성 클리핑을 막도록 했다.
- **원인 (2) — 슬라이더를 움직여도 카메라가 다시 움직이지 않음**: `ObjectInspectorPanel`의 슬라이더 `onChange`는 `focusDistanceMultiplier` 값을 client state(`panelConfig`)에만 반영할 뿐 카메라를 재호출하지 않아, 이미 선택돼 패널이 열려 있는 오브젝트의 배율을 바꿔도 다시 클릭하기 전까지는(그리고 재클릭해도 원인 (1) 때문에 여전히 안 보일 수 있었음) 화면에 아무 변화가 없었다 — `handleSelectedObjectChange`가 `focusDistanceMultiplier` patch를 받으면 즉시(애니메이션 없이, 드래그 스크러빙과 자연스럽게 맞도록) `focusCameraOnObject`를 다시 호출하도록 배선.
- **검증**: `npx tsc --noEmit`/`npm run lint` 0 errors(신규 warning 없음, 기존 69개 warning 그대로). **이번에도 스크린샷 검증은 못함** — Browser pane이 "표시되지 않음" 상태(`screenshot failed: the Browser pane is not displayed`)로 이 페이지의 반복된 환경 한계가 재현됨, `get_page_text`로 `/about-silo` 페이지 자체는 정상 로드됨과 콘솔에 새로운 런타임 에러가 없음만 확인. 수정 근거는 코드/수치 분석(정규화된 오브젝트 크기 범위와 `minDistance` 상수를 직접 대조해 dead zone을 계산적으로 확인)과 `@react-three/drei` `CameraControls` 소스 코드 확인(매 프레임 `minDistance` 제약이 실제로 강제됨, `<primitive>` prop 값이 안 바뀌면 R3F가 우리의 imperative 오버라이드를 되돌리지 않음을 확인)으로 대체.
- **다음에 확인 필요**: 사람이 직접 `/about-silo`에서 여러 크기의 오브젝트를 클릭 후 슬라이더를 낮음(1배)→높음(8배)으로 움직여가며 실제로 카메라 거리가 즉각 반응하는지, "자동으로 되돌리기" 버튼도 즉시 반영되는지, 슬라이더 조작 후 오브젝트를 닫고 행성/게시물을 클릭했을 때 정상적인(표면을 뚫고 들어가지 않는) 거리로 돌아오는지 확인 필요.
- **변경 파일**: `src/components/about-silo/AboutSiloUniverse.tsx`.

## 2026-08-19 (EPIC-133 — 인스타그램 비공식 서버 사이드 스크래핑 + 리버스 프록시 + 미니멀 슬라이더)
- **배경**: 사용자가 "복잡한 Meta Graph API 토큰 발급 없이" 인스타그램 게시물(이미지/영상/캐러셀)을 사일로 플랫폼에 직접 띄우고 싶다고 요청 — 클라이언트 사이드 호출 없이 철저히 서버 사이드(Route Handler)에서 스크래핑+프록시하도록 명시. 이 요청은 기존 `src/lib/embedThumbnail.ts`(instagram og:image 스크래핑, EPIC-079-HOTFIX-3)와 `src/lib/instagramEmbed.ts`(공식 blockquote+embed.js 위젯, `X-Frame-Options: DENY`로 자주 실패)가 이미 이 저장소에서 여러 차례 부딪힌 "Instagram 임베드가 신뢰할 수 없다"는 문제(NEXT_TASK.md EPIC-084-REVISED 후속, HOTFIX-134의 CDN 서명 만료 진단)의 연장선 — 기존엔 썸네일 1장만 스크래핑했다면, 이번엔 실제 미디어(캐러셀 포함) 전체를 우리 서버가 대신 가져와 보여주는 방식으로 확장.
- **`src/lib/instagramScraper.ts`(신규, 서버 전용)**: 게시물 permalink HTML을 스푸핑된 Chrome UA로 fetch해 (1) `edge_sidecar_to_children` 인라인 JSON(있으면 이미지/영상이 섞인 캐러셀도 순서·타입까지 정확히 추출)을 우선 시도하고, 실패하면 (2) `og:video`/`og:image` 메타 태그로 단일 이미지/영상 폴백. `embedThumbnail.ts`와 동일하게 best-effort — 실패하면 null(호출부가 자연히 "미디어 없음" 처리).
- **`src/app/api/proxy-ig/route.ts`(신규)**: `<img>/<video>`가 인스타그램 CDN 원본 URL을 직접 참조하면 애드블락/CORS로 막히거나(HOTFIX-134에서 확인된 서명 만료 403도 같은 계열 문제) — 우리 서버가 스푸핑 UA로 대신 다운로드해 스트림으로 릴레이한다. `<img src>`로 직접 불러오는 엔드포인트라 Authorization 헤더를 못 보내므로, SSRF 방어는 `src/app/api/embed-thumbnail/route.ts`의 `ALLOWED_HOSTS`와 동일한 패턴(인스타그램 CDN 호스트 화이트리스트: `cdninstagram.com`/`fbcdn.net`/`instagram.com`/`fbsbx.com`, https만 허용)으로만 건다.
- **`src/app/api/instagram-post/route.ts`(신규)**: permalink → 스크래핑 결과(미디어 배열 JSON)를 반환하는 라우트. `embed-thumbnail`과 동일하게 로그인 요구(`getRequestMember`, 서버가 임의 URL을 대신 요청하는 구조라 익명 오픈 스크래퍼로 남용되지 않도록) + `instagram.com/(p|reel|tv)/` 패턴 검증.
- **`src/components/content/InstagramMediaSlider.tsx`(신규)**: permalink 하나로 미디어 배열을 불러와 좋아요/댓글/로고 없이 미디어만 보여주는 미니멀 슬라이더. 시각적으로는 기존 `.gallery-*`(EPIC-079-PHASE-5, `globals.css`) 클래스를 재사용해 플랫폼 톤을 맞췄고, 화살표/점 인터랙션은 `galleryCarousel.ts`와 동일한 scroll-snap+`scrollIntoView` 방식을 React state로 재구현. 스크래핑 실패 시 기존 `instagramEmbed.ts`와 동일하게 "Instagram에서 게시물 보기 ↗" 링크로 폴백.
- **CDN URL 만료를 구조적으로 회피**: HOTFIX-134에서 "예전에 스크래핑한 인스타그램 이미지의 서명付 CDN 링크를 재호스팅 없이 그대로 저장해 뒀다가 시간이 지나 403"이 난 것을 진단한 바 있음 — 이번 구현은 그 실수를 반복하지 않도록 스크래핑 결과를 DB 등에 전혀 캐싱/영구 저장하지 않고, `/api/proxy-ig`가 매 요청마다 실시간으로 새로 가져오는 구조로 설계했다.
- **검증**: `npx tsc --noEmit`/`npm run lint` 0 errors(신규 파일 자체엔 lint 에러 없음, `InstagramMediaSlider.tsx`의 effect-내 `setState` 경고 1건은 이 코드베이스에 이미 여러 곳 존재하는 동일 계열 warning으로 차단 대상 아님). 신규 라우트 3개는 로컬 dev 서버에 curl로 직접 검증: `/api/instagram-post`는 미로그인 401, `/api/proxy-ig`는 화이트리스트 밖 호스트·URL 누락 둘 다 400으로 정상 응답. **실제 스크래핑 자체는 이번 세션에서 성공 확인 못 함**: 이 환경(서버 sandbox)의 아웃바운드 IP로 `instagram.com`의 실제 게시물 permalink를 여러 개(임의 shortcode·실존 공개 게시물·프로필 페이지, Chrome UA·`facebookexternalhit` 크롤러 UA 둘 다) 직접 curl로 가져와보니 매번 메타 태그/인라인 JSON이 전혀 없는 로그인 월 격의 빈 앱 셸(`<title>Instagram</title>`만 있고 `og:image`/`edge_sidecar` 전무)만 돌아왔다 — 클라우드/데이터센터 아웃바운드 IP를 차단하는 것으로 보이는, 이 리포에 이미 기록된 것과 같은 계열의 Instagram 쪽 제약(embedThumbnail.ts/instagramEmbed.ts 주석 참고)으로 추정. 코드 자체는 스펙대로(캐러셀 우선 파싱 → og 태그 폴백) 정확히 구현했지만, **Vercel 배포 환경에서도 같은 방식으로 막힐 가능성이 높다는 걸 미리 알아둬야 한다** — best-effort 설계(실패 시 조용히 링크 폴백)로 대비는 해뒀지만 근본적으로 우리 쪽 코드로 우회할 수 없는 제약일 수 있다.
- **아직 배선 안 함(의도적으로 범위 밖)**: 기존 에디터(`BlockEditor.tsx`)/`UniversalBlockRenderer.tsx`의 인스타그램 embed 파이프라인(blockquote+embed.js)은 건드리지 않았다 — 이미 저장된 게시글의 렌더링 방식을 바꾸는 문제라, 이번 신규 컴포넌트를 실제로 어디에 노출할지(에디터에 새 임베드 타입으로 추가할지, 기존 instagram provider를 아예 이 방식으로 교체할지 등)는 사용자 확인 후 별도 작업으로 진행하는 게 안전하다고 판단.
- **다음에 확인 필요**: (1) 실제 로그인 세션 + 진짜 공개 인스타그램 게시물 URL로 `/api/instagram-post`를 직접 호출해 미디어가 정말 추출되는지(Vercel 배포 IP 기준으로) 확인, (2) 안 되면 대안(예: 공식 oEmbed 앱 토큰 발급, 또는 사용자가 이미지를 직접 업로드하는 수동 경로 병행) 검토, (3) `InstagramMediaSlider`를 실제 게시글 작성 흐름에 어떻게 노출할지(에디터 새 노드 vs 기존 provider 교체) 결정.
- **변경/신규 파일**: `src/lib/instagramScraper.ts`(신규), `src/app/api/proxy-ig/route.ts`(신규), `src/app/api/instagram-post/route.ts`(신규), `src/components/content/InstagramMediaSlider.tsx`(신규).

## 2026-08-19 (HOTFIX-132.1 — 넘버링 규칙 변경 + Craft 캔버스 선택 시 테두리/이름표 표시 + 오브젝트 줌인 거리를 크기 비례로 수정)
- **배경**: 사용자가 (Gemini를 통해 작성한) 지시로 두 가지를 명확히 요구 — (1) 넘버링 규칙을 "HOTFIX는 독립 정수를 쓸 수 없고 EPIC에 종속된 하위 버전(`HOTFIX-<EPIC>.<n>`)으로만 기록"하도록 즉시 변경. 이번 항목부터 새 규칙 적용(과거 HOTFIX-131/133/134는 소급 변경 안 함, CLAUDE.md에 명문화). (2) BuilderJS 스크린샷과 관리자 캔버스 스크린샷 4장을 나란히 비교하며 "그동안 만든 게 맘에 안 든다"고 지적 — 실제로 비교해보니 클릭→설정 패널 실시간 연동 자체는 이미 동작하지만(EPIC-132/HOTFIX-133/134), **선택된 블록에 아무 시각적 표시(테두리)가 없어** BuilderJS와 달리 "지금 뭘 선택했는지" 안 보이는 게 가장 눈에 띄는 차이였다. 함께 "오브제를 클릭하면 카메라가 너무 가깝게 밀착돼 형체를 알아볼 수 없다"는 3D 씬 버그도 신고.
- **선택 테두리/이름표**: 신규 `ChromeSelectionOverlay.tsx` — 선택되면 파란 테두리(ring)+블록 이름 라벨 pill, 호버만 해도 옅은 테두리를 보여준다(`pointer-events-none`이라 실제 클릭/드래그는 그대로 콘텐츠에 전달됨). `useNode(node => ({ selected: node.events.selected, hovered: node.events.hovered }))`로 4개 블록(로고/사이드바 아이콘/상단 탭/계정 메뉴) 전부에 적용.
- **오브젝트 클릭 시 줌인 거리 — 실제 원인은 고정 거리 벡터**: `focusCameraOnObject`가 오브젝트 크기와 무관하게 `(0.35, 0.3, 1.0)`(≈1.09 유닛) 고정 거리로 다가가고 있었다 — 큰 오브젝트일수록 카메라가 표면 안쪽에 파묻히듯 보인 원인. `group`의 실제 렌더링된 월드 바운딩 박스(`Box3.setFromObject`, `UniverseObjectModel`이 이미 쓰던 것과 동일한 기법)를 매번 다시 재서 그 크기에 비례하는 거리로 물러나도록 수정(기본 배율 2.4배, `cameraControlsRef.minDistance`보다 항상 크게 클램프해 클리핑 방지). `UniverseObject`에 `focusDistanceMultiplier`(널이면 자동) 필드 신설 + `ObjectInspectorPanel`에 "클릭 시 줌인 거리" 슬라이더 추가 — 관리자가 오브젝트별로 배율을 직접 조정할 수 있다.
- **검증**: `npx tsc --noEmit`/`npm run lint`/`npm run build` 전부 0 errors. Browser pane 실측 중 이 세션의 브라우저 탭 하나가 완전히 입력에 반응하지 않게 되는 문제(단순 텍스트 입력조차 등록 안 됨 — 코드 문제가 아니라 탭/CDP 연결 자체의 문제로 확인, 새 탭을 열어 재현 확인 후 정상 동작 확인)를 겪었다 — 새 탭에서 로고 블록 클릭 시 실제로 파란 테두리+"메인 로고" 라벨이 DOM에 렌더링됨을 확인. 3D 줌인 거리 수정은 코드 리뷰(동일 파일의 기존 검증된 Box3 패턴 재사용) + 무에러 로드까지 확인했지만, 이 환경의 스크린샷 기능이 계속 실패해("Browser pane이 표시되지 않음") 실제 줌 결과를 시각적으로 확인하지는 못했다.
- **다음에 확인 필요**: 3D 씬에서 실제로 여러 크기의 오브젝트를 클릭해가며 줌 거리가 항상 여유 있게 느껴지는지(사람이 직접 확인 권장), 선택 테두리가 다른 화면 크기/PC-모바일 토글에서도 정확한 위치에 그려지는지.
- **변경/신규 파일**: `CLAUDE.md`, `src/components/craft/chrome/ChromeSelectionOverlay.tsx`(신규), `src/components/craft/chrome/blocks/ChromeLogoBlock.tsx`, `ChromeSidebarIconBlock.tsx`, `ChromeTopTabBlock.tsx`, `ChromeAccountMenuBlock.tsx`, `src/components/about-silo/AboutSiloUniverse.tsx`, `src/lib/aboutSiloUniverseConfig.ts`, `src/components/about-silo/ObjectInspectorPanel.tsx`, `src/components/about-silo/PlanetSettingsPanel.tsx`, `src/components/about-silo/UniverseSettingsPanel.tsx`.

## 2026-08-19 (HOTFIX-134 — 상단 탭 같은 단 안에서 좌우 순서 드래그 + 로고/탭 프리뷰 커스텀 폰트 누락 수정 + 인스타그램 CDN 원인 확인)
- **배경**: 사용자가 HOTFIX-133 직후 스크린샷 2장을 첨부해 지적 — (1) 실제 홈페이지 스크린샷에 스튜디오/마이 페이지/글쓰기를 동그라미+화살표로 표시하며 "드래그 & 드랍으로 위치를 내가 정할수 없잖아"(HOTFIX-133의 1단/2단 드래그는 "어느 줄인지"만 바꿀 뿐 같은 줄 안에서 좌우 순서는 못 바꿨다는 지적), (2) 관리자 캔버스 스크린샷에서 "메인 로고 프리뷰가 실제랑 다른데?", (3) "instagram을 썸네일로 한 모든 게시물이 이미지가 안 나오고 있어, 이게 그냥 인스타그램의 cdn 문제인지 알려줘".
- **(1) 같은 단(tier) 안에서도 실제 드래그로 좌우 순서 변경**: `TopTabStyleEntry`에 `order`(fractional index, 기본 null=원래 site_navigations 순서) 신설. `ChromeTierZone.tsx`가 각 탭 칩을 그 자체로도 드롭 타겟으로 감싸(HOTFIX-133은 존 전체에만 드롭 가능했음) 특정 칩 위에 드롭하면 "그 칩 앞에 끼워넣기"로 해석 — 앞뒤 탭 order의 중간값을 매겨(끼워넣기마다 전체 재번호 불필요) `handleTabDrop()`이 계산한다. `Navbar.tsx`의 `tier1Tabs`/`tier2Tabs`도 이 order로 정렬하도록 변경(값 없는 기존 탭은 원래 순서 그대로라 회귀 없음). **범위 밖**: "글쓰기" 버튼은 `site_navigations`의 실제 탭이 아니라 "mypage" 탭 옆에 하드코딩으로 끼워 넣는 항목이라(Navbar.tsx) 이번 드래그 시스템 대상이 아니다 — 사용자가 스크린샷에서 함께 표시했지만 별도 작업 필요.
- **(2) 로고/상단 탭 프리뷰가 실제와 다르던 원인**: EPIC-132에서 만든 `ChromeLogoView`/`ChromeTopTabView`가 Navbar.tsx의 커스텀 폰트 처리(업로드한 폰트 파일 배열 + `@font-face` 동적 주입 + 폴백 체인)를 통째로 빠뜨리고 "서체 직접 입력" 필드만 보고 있었다 — 실제 사이트는 업로드된 커스텀 폰트로 렌더링되는데 프리뷰는 기본 서체로 폴백돼 확연히 달라 보였다. Navbar.tsx와 동일한 폴백 체인(활성 커스텀 폰트들 → 직접입력 서체 → sans-serif/inherit)을 그대로 재현하고 같은 `@font-face` 주입 패턴을 추가.
- **(3) 인스타그램 CDN 문제 맞음, 확인 완료**: DB에 저장된 게시글 사진 URL이 전부 `cdninstagram.com` 서명付 링크(`oe=` 파라미터가 만료 시각)였는데, 그중 하나를 직접 디코딩해보니 2026-08-16 15:27 UTC에 이미 만료돼 있었고, 서버에서 직접 `curl`로 요청한 결과 실제로 `403 Forbidden`을 반환함을 확인했다 — **이 저장소 코드의 버그가 아니라, 예전에 스크래핑(EPIC-085)한 인스타그램 이미지를 자체 스토리지로 재호스팅하지 않고 원본 서명 링크를 그대로 저장해 뒀던 게 시간이 지나며 만료된 것**. 다른 부분(EPIC-084)에서는 이미 "외부 이미지를 서버가 대신 내려받아 재호스팅"하는 패턴을 쓰고 있는데, 스크래핑 시스템은 그 처리가 안 돼 있어 언젠가는 반드시 이렇게 된다 — 이번 세션에서는 이 진단만 전달하고 코드 수정은 하지 않았다(사이트 전체 스크래핑 콘텐츠에 영향을 주는 별도 범위의 작업이라 임의로 착수하지 않음, 사용자 확인 필요).
- **검증**: `npx tsc --noEmit`/`npm run lint`/`npm run build` 전부 0 errors. Browser pane 실측 — 로고 프리뷰 vs 실제 Navbar의 `getComputedStyle().fontFamily`를 직접 비교해 완전히 동일한 커스텀 폰트를 쓰는 것을 확인, 인스타그램 CDN URL을 서버에서 직접 curl로 403 확인. **같은 단 안 드래그 재정렬 자체는 검증 못함**: 합성 `DragEvent`/`left_click_drag` 둘 다 시도했으나, 실제 React `onDrop` 핸들러 안에서 `dataTransfer.getData()`가 신뢰되지 않은(비-사용자 제스처) 이벤트에 대해 빈 문자열을 반환하는 크롬의 보안 정책 때문에 자동화로는 이 특정 상호작용을 재현할 수 없음을 직접 확인(스크립트가 직접 소유한 DataTransfer 객체는 정상적으로 값을 유지하지만, 이벤트 핸들러 내부에서 읽으면 비워짐) — 로직 자체는 코드 리뷰로 재확인(설정 계산/정렬/폴백 전부 올바름), 실제 마우스 드래그는 사람이 직접 확인 필요.
- **변경/신규 파일**: `src/lib/topTabStyleSettings.ts`, `src/components/craft/chrome/ChromeTierZone.tsx`, `src/components/craft/chrome/views.tsx`, `src/components/Navbar.tsx`, `src/app/admin/navigation/settings/page.tsx`.

## 2026-08-19 (HOTFIX-133 — 3D 카메라 버그 3건 수정 + 우주 오브젝트 무작위 개수 설정 + 상단 탭 드래그앤드롭 배치)
- **배경**: 사용자가 EPIC-132/HOTFIX-131 이후 실사용 피드백으로 5가지를 요청 — (1) "화살표 위로 하면 화면이 아래로 가네? 고쳐", (2) "빈우주 더블클릭 줌인/아웃 작동안돼", (3) "오브제를 클릭하면 오브제를 중심으로 화면이 되어야지", (4) "홈페이지 설정관리는... 드래그 앤 드랍으로 요소들 위치를 정할수 있는걸 원했어. 예를 들어 상단 탭의 1단과 2단을... 드래그앤드랍으로" (BuilderJS 스크린샷 첨부), (5) "'별/은하수 이미지 추가'로 추가한 이미지들이 무작위로 우주 공간에 몇개 랜덤 공간에 떠다니는지 설정할수 있게 해줘". 레퍼런스로 로컬 동영상(little prince.mp4)도 첨부했으나, 이 세션의 파일 도구는 동영상을 읽을 수 없고(바이너리 파일 오류) 이 머신에 ffmpeg도 없어 프레임 추출도 못해 — 영상 자체는 확인하지 못한 채 텍스트 설명과 이 저장소에 이미 있던 "어린왕자 감성"(EPIC-113~115) 톤을 기준으로 작업했다는 한계를 사용자에게 그대로 알림.
- **(1) 화살표 위/아래 반전**: `truck(0, step)`의 부호가 카메라를 로컬 -Y로 옮기는 값이라 "화면(콘텐츠)이 위로 올라와야 한다"는 체감 방향과 반대였다 — ArrowUp/ArrowDown의 step 부호를 맞바꿔 수정.
- **(2) 더블클릭 줌 — 진짜 원인은 별도로 있었음**: 더블클릭 핸들러 자체(`onPointerMissed`)는 정상 동작했다(디버그 로그로 실측 확인) — 실제 원인은 배경을 채운 Stars(4500개)/Sparkles(140개) 점들이 기본적으로 raycast 대상이라, "빈 공간처럼 보이는" 곳을 클릭해도 근처 별 점 하나가 자주 걸려 `hits.length > 0`이 되면서 "미스(miss)" 판정 자체가 거의 안 나던 것. 두 drei 컴포넌트에 ref로 실제 `THREE.Points` 인스턴스를 받아 `raycast`를 no-op으로 덮어써 완전히 장식 전용으로 만들었다. 이 과정에서 다른 세션의 공유 dev 서버가 파일 변경을 못 받아들이는(HMR 실패로 보이는) 문제를 만나, 이번 세션 전용의 독립 프로덕션 서버(포트 3001)를 새로 띄워 검증했다.
- **(3) 오브젝트 클릭 시 카메라 포커스**: `handleFocusPlanet`/`handleSelectPost`와 동일한 `setLookAt` 패턴으로 `focusCameraOnObject(key)` 신설 — `objectRefs` 맵에서 실제 `THREE.Group`을 찾아 `getWorldPosition()`으로 정확한 월드 좌표(행성 자전 그룹에 중첩돼 있어도)를 구해 그 앞으로 카메라를 부드럽게 이동시킨다. `handleSelectObject`/`handleSelectSpaceObject` 양쪽에 배선.
- **(4) 홈페이지 설정 관리 — 진짜 드래그앤드롭**: 이전 EPIC-132는 "클릭하면 설정 패널이 뜬다"까지만 구현했고 "드래그로 위치를 정한다"는 빠져 있었다는 지적을 받아, 상단 탭의 1단/2단 배치를 실제 HTML5 드래그앤드롭으로 구현 — 캔버스 위에 "1단"/"2단" 두 드롭존(`ChromeTierZone`, 신규)을 두고 탭 칩(`ChromeTopTabBlock`)을 그 사이로 직접 끌어다 놓으면 tier가 즉시 바뀐다. 구현 중 두 가지 실제 버그를 만나 고쳤다: (a) `ChromeTierZone`을 `<ChromeTierZone>` JSX 태그로 트리에 그대로 끼워 넣었더니 Craft.js의 정적 트리 파서가 리졸버에 없는 컴포넌트로 해석하려다 "Invariant failed"로 전체 페이지가 죽었다 — 일반 함수처럼 직접 호출(`ChromeTierZone({...})`)해 이미 펼쳐진 `<div>` 결과만 트리에 넣도록 수정. (b) 드롭 자체는 되는데 화면에서 탭이 실제로 다른 존으로 옮겨가지 않았다 — 원인은 Craft.js `<Frame>`이 최초 마운트 시의 children만 읽고 이후 리렌더에서 다시 읽지 않기 때문(값은 바뀌지만 DOM 위치는 그대로 남음) — tier가 바뀔 때만 `ChromeCraftEditor`를 강제로 리마운트시키는 버전 카운터(`topTabsTreeVersion`)를 도입해 해결. 합성 `DragEvent`(dragstart→dragover→drop, 실제 `DataTransfer` 사용)로 실제 탭 하나("스튜디오")를 1단→2단으로 옮기는 왕복을 브라우저에서 직접 검증.
- **(5) 우주 오브젝트 무작위 개수/재배치**: `SpaceObject`에 `count`(기본 1, 별/은하수는 업로드 시 기본 8)와 `scatterSeed`(기본 0) 필드 신설 — count가 2 이상이면 같은 이미지가 그만큼 서로 다른 무작위 위치(id+index+scatterSeed를 섞은 해시)에 흩뿌려지고, `obj.position`(단일 드래그 위치)은 이 경우 무시된다(여러 사본을 개별적으로 드래그해 옮기는 개념 자체가 없는 순수 배경 장식이라는 전제). Universe Settings 패널의 우주 오브젝트 목록에 "개수" 입력 + "🎲 다시 배치"(scatterSeed 증가) 버튼 추가.
- **검증**: `npx tsc --noEmit`/`npm run lint`/`npm run build` 전부 0 errors. 세션 전용 독립 프로덕션 서버(포트 3001)에서 Browser pane 실측 — 더블클릭 줌인 시도→`controls.dolly()` 정상 resolve+거리 실제로 절반으로 감소 확인, 로고 블록 실시간 setProp 왕복(이전 세션 확인 사항 재확인), 상단 탭 드래그앤드롭이 실제로 탭을 1단→2단 드롭존으로 옮기는 것을 합성 DragEvent로 확인. 화살표 키 반전 수정과 오브젝트 클릭 시 카메라 포커스는 코드 리뷰 수준으로 확인(기존에 이미 검증된 동일 패턴 재사용) — 실제 마우스/키보드 손맛은 사람이 직접 확인 필요. **Invariant failed 콘솔 에러 관련**: 상단 탭 섹션뿐 아니라 기존에 이미 배포돼 있던 하단 메뉴(Footer) Craft 에디터를 열 때도 동일하게 발생함을 대조 확인 — 이번 세션 변경과 무관한 기존 Craft.js/React 19 호환성 노이즈로 판단, 기능 자체는 정상 동작.
- **다음에 확인 필요**: little prince.mp4 레퍼런스 영상을 실제로 확인해(화면 공유나 프레임 캡처 등 다른 방법으로) 우주 오브젝트 밀도/느낌이 그 톤에 맞는지, 화살표 키 4방향 전부 실제 손맛, 오브젝트 클릭 시 카메라 포커스가 다양한 오브젝트 크기/위치에서 자연스러운지, 상단 탭 드래그앤드롭을 실제 마우스로(합성 이벤트가 아니라) 시도했을 때 드롭존 시각적 피드백(hover 시 테두리 강조 등)이 충분한지 — 지금은 기능만 있고 드래그 중 시각적 힌트가 약함.
- **변경/신규 파일**: `src/components/about-silo/AboutSiloUniverse.tsx`, `src/lib/aboutSiloUniverseConfig.ts`, `src/components/about-silo/UniverseSettingsPanel.tsx`, `src/components/craft/chrome/ChromeTierZone.tsx`(신규), `src/components/craft/chrome/blocks/ChromeTopTabBlock.tsx`, `src/app/admin/navigation/settings/page.tsx`.

## 2026-08-18 (EPIC-132 — "홈페이지 설정 관리" 4개 섹션을 craft.js 클릭-선택 WYSIWYG로 전환)
- **배경**: 사용자가 "홈페이지 설정관리에 대해 네가 아직도 내가 원하는걸 정확히 모르고 있는거 같다"며, "WYSWYG 실시간 프리뷰가 창에 띄워져 있고, 그 창 안의 요소들을 클릭하면 설정창이 띄워지도록... craft.js의 페이지 빌더처럼... 요소들을 클릭 드래그로 옮기게 해달라... 최고급 퀄리티의 '유료' 웹사이트 수준에서 기대할 수 있는 페이지 빌더를 만들어"라고 명확히 요구. 이 저장소 히스토리(EPIC-126/129/130)는 정확히 이 요청을 세 번 검토했지만 "전역 크롬(로고/사이드바/상단 탭)은 모든 페이지에 렌더링돼 블라스트 반경이 크다"는 이유로 매번 범위 밖으로 미뤄왔다 — 이번이 그 별도 EPIC.
- **핵심 설계 결정**: 계획 단계에서는 Navbar.tsx의 렌더 코드를 "추출"해 재사용할 계획이었으나, 실제로 Navbar.tsx를 읽어보니 CSS-only hover 메가메뉴/5갈래 로그인 세션 분기/ResizeObserver 스페이서/전역 `<style>` 주입이 깊게 얽혀 있어(모든 페이지에서 렌더링) 그대로 손대면 사이트 전체 내비게이션이 깨질 위험이 컸다 — **Navbar.tsx는 한 글자도 수정하지 않고**, 완전히 독립된 프레젠테이션 뷰(`src/components/craft/chrome/views.tsx`: `ChromeLogoView`/`ChromeSidebarIconView`/`ChromeTopTabView`/`ChromeAccountMenuView`)를 새로 만들어 같은 설정 lib(`mainLogoSettings.ts` 등)과 같은 hover 모션 CSS 생성 함수(`tabHoverMotionCss`)로 스타일링 전용 복제품을 렌더링하고, 그 위에만 Craft.js를 얹었다. 저장 방식도 그대로 `site_settings` 유지(새 `page_builder`/`craft_state` 행 없음) — 각 블록의 `related.settings` 패널이 Craft의 `setProp`(캔버스 즉시 반영용)과 기존 shim setter(`setMainLogo`/`setSidebarIcons`/`setTopTabStyle`/`setAccountMenuStyle`, 실제 "저장하기" 버튼용)를 **동시에** 호출하는 이중 배선으로, 캔버스에서 타이핑하는 즉시 눈에 보이면서도 저장 버튼은 여전히 정확한 값을 저장한다.
- **신규 컴포넌트**: `src/components/admin/craft/ChromeCraftEditor.tsx`(전용 경량 셸 — 기존 `CraftPageEditor.tsx`는 Toolbox+`page_builder.craft_state` 저장 흐름을 전제해 재사용 안 함, `<Editor>`+`<Frame>`+재사용 `SettingsSidebar`만 조합), `src/components/craft/chrome/blocks/`(`ChromeLogoBlock`/`ChromeSidebarIconBlock`/`ChromeTopTabBlock`/`ChromeAccountMenuBlock`, 각각 `related.settings` 인라인 패널 포함), `src/components/craft/chrome/resolver.ts`. 사이드바 아이콘은 좌/우 고정 2개 인스턴스(추가/삭제 불가), 상단 탭은 `site_navigations` 최상위 행 수만큼 트리 빌드 시점에 자동 생성(이 캔버스에는 탭 추가/삭제/순서 변경 UI가 없음 — 계속 `/admin/site-structure` 담당), 계정 메뉴는 5개 항목 전체에 적용되는 단일 설정(기존 데이터 모델과 동일).
- **레이아웃 버그(실측 중 발견해 고침)**: 새 캔버스를 기존 중앙 컬럼에 처음 넣었을 때 클릭이 전혀 반응하지 않는 문제가 있었다 — 원인은 버그가 아니라 레이아웃이었다: 우측에 이미 640px 폭의 PC+모바일 dual iframe 미리보기가 항상 떠 있어서, 1280px 뷰포트 기준 중앙 컬럼이 300px 미만으로 찌그러져 있었고(SettingsSidebar 고정폭 288px에 캔버스가 거의 0px만 남음) 로고 텍스트가 실제로 폭 0으로 렌더링되고 있었다 — 클릭할 대상 자체가 없었다. 마이그레이션된 4개 섹션(로고/사이드바아이콘/상단탭/계정메뉴)에서는 우측 iframe 프리뷰를 숨겨(새 캔버스가 이미 실시간 미리보기+편집을 겸하므로) 중앙 컬럼에 폭을 온전히 내주는 방식으로 해결(슬라이드쇼 섹션은 계속 노출).
- **`uploadImage`/`compressImage` 추출**: `page.tsx` 안에 있던 두 유틸을 `src/lib/adminImageUpload.ts`로 추출(동작 변화 없음) — 새 블록의 설정 패널도 재사용.
- **검증**: `npx tsc --noEmit`/`npm run lint`/`npm run build` 전부 0 errors. Browser pane 실측(다른 세션이 이미 띄워둔 dev 서버에 이 세션의 브라우저 탭을 붙여 확인) — 4개 섹션 전부에서: 캔버스 안 실제 요소(로고 텍스트/사이드바 아이콘 이미지/상단 탭 7개/계정 메뉴 5개 항목)를 클릭하면 우측에 해당 블록 전용 설정 패널이 뜨고, DB에서 로드된 실제 값(예: 로고 왼쪽 텍스트 "I'm your"/오른쪽 텍스트 "SILO", 상단 탭 hover 모션 "금빛 그라디언트 밑줄")이 정확히 채워져 있음을 확인. 로고의 "오른쪽 텍스트" 필드를 캔버스에서 직접 타이핑해 캔버스 텍스트와 옛 폼 필드 둘 다 동시에 갱신되는 것(setProp+shim 이중 배선이 실제로 동작)을 DOM으로 직접 확인 후 원래 값으로 되돌림(저장 버튼은 누르지 않아 DB에는 영향 없음). 콘솔에 새 에러 없음(기존부터 있던 craft.js/React19 `element.ref` 경고만 — 이 세션 변경과 무관, 기존 footer 에디터에서도 동일하게 발생함을 대조 확인).
- **명시적으로 범위 밖으로 미룸**: (1) 기존 폼 필드 UI(B6 컷오버) — 아직 제거하지 않고 새 캔버스 위/아래에 나란히 유지, 파일 업로드 왕복·PC/모바일 토글 전환·실제 저장 버튼 클릭까지의 전체 왕복 확인 후 다음 세션에서 제거 예정. (2) 슬라이드쇼 섹션 — 이미 홈페이지 Craft 캔버스에 `HeroSlideshowWidgetBlock`으로 임베드 가능한 상태라 별도 후속으로 분리.
- **다음에 확인 필요**: 사람이 직접 실제 파일(이미지/영상) 업로드 왕복, PC↔모바일 토글 전환 시 캔버스가 올바르게 리마운트되는지, "저장하기" 버튼을 눌러 `site_settings`에 실제로 반영되는지, B6 컷오버(옛 폼 제거) 진행 여부.
- **변경/신규 파일**: `src/app/admin/navigation/settings/page.tsx`, `src/components/craft/chrome/views.tsx`(신규), `src/components/craft/chrome/resolver.ts`(신규), `src/components/craft/chrome/blocks/ChromeLogoBlock.tsx`(신규), `ChromeSidebarIconBlock.tsx`(신규), `ChromeTopTabBlock.tsx`(신규), `ChromeAccountMenuBlock.tsx`(신규), `src/components/admin/craft/ChromeCraftEditor.tsx`(신규), `src/lib/adminImageUpload.ts`(신규).

## 2026-08-18 (HOTFIX-131 — My Page 행성 캐릭터 크기 버그 + 설정 패널 드래그 이동 + 오브제 기즈모 안내 + SILO 오브제 크기 + 키보드/터치 카메라 조작)
- **배경**: 사용자가 "my page 행성에 캐릭터를 추가했더니 이미지처럼 나왔어 고쳐"(캐릭터 GLB가 거대하고 찌그러져 보임), "행성이나 위성을 클릭하면 나오는 창을 드래그 드랍해서 이동할수 있게", "오브제가 선택된 상황에서 xyz plane이 나오는데 '여기를 클릭해서 이동'이라는 안내 메시지", "silo 행성에 오브제를 추가해봤더니 너무 작아서 안 보여, 디폴트 크기를 충분히 크게", "화면을 좌,우,상,하 키보드로 움직일 수 있게", "모바일에서는 손가락 두개로 화면을 움직이거나, 빈 우주를 더블클릭하면 줌인/줌아웃" 6가지를 한 번에 요청.
- **캐릭터 GLB 크기/변형 버그**: `CustomCharacterModel`(`AboutSiloUniverse.tsx`)이 업로드된 `.glb`를 원본 모델링 단위 그대로(정규화 없이) 렌더링하던 게 원인 — 장식 오브젝트용 `UniverseObjectModel`은 이미 하던 `Box3` 기반 바운딩박스 정규화를 캐릭터에도 동일 패턴으로 추가(단, 사람 실루엣이라 최대 치수가 아니라 키(y축)를 기준으로 정규화 — 옆으로 퍼진 채 찌그러져 보이던 원인도 함께 해소).
- **설정 패널 드래그 이동**: 신규 공용 훅 `src/components/about-silo/useDraggablePanel.ts` — 헤더 바를 `onPointerDown`으로 눌러 드래그한 만큼 오프셋을 누적, `PlanetSettingsPanel`/`UniverseSettingsPanel`/`ObjectInspectorPanel` 3개 패널 전부(예전엔 순수 CSS `fixed`라 이동 불가) 적용.
- **오브제 이동 기즈모 안내**: 오브젝트 선택 시 `TransformControls` 기즈모 옆에 drei `Html`로 "여기를 클릭해서 이동" 라벨을 띄우는 `SelectionHint` 컴포넌트 신설 — 대상이 행성의 자전 그룹 등에 중첩돼 있어도 매 프레임 실제 월드 좌표를 따라간다.
- **SILO 오브제 기본 크기**: `UniverseObjectModel`의 정규화 목표 크기(`targetSize`)가 행성 반지름과 무관한 고정값(0.5)이라 SILO(반지름 ~2.0)처럼 큰 행성 위에서 상대적으로 지나치게 작았던 것 — 반지름에 비례(`radius * 0.45`)하도록 변경해 어느 행성에 놓든 상대적으로 같은 크기로 보이게 함. 기본 관리자 scale도 0.3 → 0.6으로 상향(`aboutSiloUniverseConfig.ts`/`PlanetSettingsPanel.tsx`)해 추가 즉시 눈에 보이도록.
- **키보드 화살표 카메라 패닝**: 화살표 키로 drei `CameraControls.truck()` 호출 — 입력 필드 포커스 중이거나 기즈모 드래그로 카메라 컨트롤이 비활성화된 동안은 건너뛴다.
- **모바일 두 손가락 팬 + 더블탭 줌**: `camera-controls`의 두 손가락 기본 동작(`TOUCH_DOLLY_TRUCK`, 핀치줌+팬이 한 묶음)을 순수 팬(`TOUCH_TRUCK`)으로 재설정 — 줌은 별도로 전담. `Canvas`의 `onPointerMissed`(3D 오브젝트에 안 맞은 클릭에만 호출돼 행성/오브젝트 클릭과 자동으로 안 겹침)에 더블클릭 핸들러를 달아, 이미 홈 구도 거리의 60% 이내로 다가가 있으면 홈 구도로 되돌리고(줌아웃, 행성 한눈에 보기) 아니면 현재 거리의 절반만큼 더 다가간다(줌인).
- **검증**: `npx tsc --noEmit`/`npm run lint` 전부 0 errors(대상 파일 한정 lint 확인, 새로 만든 `useDraggablePanel.ts`에서 React Compiler 계열 lint 에러 2건 — 렌더 중 ref 변경, 자기참조 클로저 — 발견해 즉시 재작성으로 해결). 다른 세션이 이미 같은 저장소 폴더에서 dev 서버를 띄워둔 상태라 이번 세션은 그 서버에 붙어 `/about-silo` 페이지가 콘솔 에러 없이 렌더링되는 것까지는 확인했지만, Browser pane이 "표시되지 않음" 상태라 스크린샷은 찍지 못했다.
- **다음에 확인 필요**: 사람이 직접 브라우저에서 (1) 실제 캐릭터 `.glb` 업로드 후 비율이 정상인지, (2) 설정 패널 드래그가 자연스러운지, (3) SILO 행성에 오브제 추가 시 즉시 잘 보이는지, (4) 화살표 키/모바일 두 손가락 팬/더블탭 줌 손맛 — 이번 세션은 코드 레벨 검증(타입체크/린트/무에러 페이지 로드)까지만 완료.
- **변경/신규 파일**: `src/components/about-silo/AboutSiloUniverse.tsx`, `src/components/about-silo/PlanetSettingsPanel.tsx`, `src/components/about-silo/UniverseSettingsPanel.tsx`, `src/components/about-silo/ObjectInspectorPanel.tsx`, `src/components/about-silo/useDraggablePanel.ts`(신규), `src/lib/aboutSiloUniverseConfig.ts`.

## 2026-08-18 (EPIC-130 — 사용자 메뉴(계정 영역) 디자인+hover 모션 설정 탭 + 모션 프리뷰 샘플)
- **배경**: 사용자가 "'홈페이지 설정관리'에 맨 위의 '관리자, (회원 등급), 마이페이지, (사용자이름), 로그아웃' 이런 메뉴의 디자인을 설정하는 또다른 탭을 만들어줘, 그리고 그 디자인의 모션에 대해서도 옵션을 줘. 상단 메뉴탭과 또 맨 위의 '사용자 메뉴'의 모션에 대한 옵션을 프리뷰 할 수 있는 샘플을 보여줘"라고 요청.
- **사용자 메뉴 디자인**: Navbar 우측 상단 계정 영역(관리자 링크/멤버십 등급 버튼/마이페이지 링크/회원 이름 버튼/로그아웃·로그인 버튼 — 로그인 여부에 따라 갈리는 5개 항목 전부)에 공통으로 적용되는 서체/크기/굵기/색상 + hover 모션(기존 6종 프리셋 재사용) 설정을 신설. 신규 공용 lib `src/lib/accountMenuStyleSettings.ts`(다른 3개 설정과 동일하게 `{pc, mobile}` 독립 설정 지원, 관리자 페이지+Navbar.tsx 공유) + Navbar.tsx에 `silo-account-menu-item` 공유 클래스와 CSS 주입 로직(`topTabStyleCss`와 동일한 패턴) 추가. "홈페이지 설정 관리"에 6번째 좌측 섹션("사용자 메뉴 디자인")으로 노출, 기존 PC/모바일 토글 대상에 포함.
- **모션 프리뷰 샘플**: 축소된 iframe 미리보기 안에서 정확히 마우스를 올려 hover 모션을 비교하기 번거롭다는 지적 — 신규 `src/components/admin/MotionPreviewSamples.tsx`: 6가지 프리셋을 나란히 놓고 바로 마우스를 올려볼 수 있는 스와치, `tabHoverMotion.ts`의 실제 CSS 생성 함수를 그대로 재사용해 실제 사이트와 100% 동일한 모션을 보여준다. "상단 탭 디자인"(탭별 hover 모션 선택 옆) + "사용자 메뉴 디자인" 두 곳 모두에 배치.
- **검증**: `npx tsc --noEmit`/`npm run lint`/`npm run build` 전부 0 errors. 이번엔 세션 후반부에 Browser pane이 "not displayed"(`document.visibilityState`가 계속 "hidden") 상태가 되어 실제 화면 스크린샷 확인은 하지 못했다 — 대신 DOM 직접 조회로 `.silo-account-menu-item` 대상 CSS(`tabHoverMotionCss`가 생성한 골든 그라디언트 밑줄 규칙 등)가 `<header>`의 `<style>` 태그에 정확히 주입되는 것과, 계정 영역 컨테이너(`<div class="flex items-center gap-3 shrink-0 relative">`)가 예상된 위치에 존재하는 것까지는 확인했다 — 로그인 세션/데이터 로딩까지 포함한 완전한 실사용 확인은 다음 세션에서 필요.
- **다음에 확인 필요**: 실제 로그인 상태에서 계정 영역 5개 항목에 hover했을 때 모션이 자연스럽게 보이는지, 모션 프리뷰 샘플의 실제 시각적 품질, PC/모바일 값이 계정 메뉴에서도 독립적으로 동작하는지.
- **변경/신규 파일**: `src/app/admin/navigation/settings/page.tsx`, `src/components/Navbar.tsx`, `src/lib/accountMenuStyleSettings.ts`(신규), `src/components/admin/MotionPreviewSamples.tsx`(신규).

## 2026-08-18 (EPIC-129 — 홈페이지 설정 관리: 메인 로고/사이드바 아이콘/상단 탭 디자인 PC·모바일 독립 설정)
- **배경**: 사용자가 "'홈페이지 설정 관리'에서 'pc 설정'과 '모바일 설정'이 따로 구분이 되게 해야지"라고 요구사항으로 명시.
- **데이터 모델**: `hero_slideshow`가 이미 쓰고 있던 `{ pc, mobile }` 패턴(EPIC-092)을 메인 로고/사이드바 아이콘/상단 탭 디자인 3개 설정에도 동일하게 적용. 기존엔 이 3개 타입이 관리자 페이지와 `Navbar.tsx` 두 파일에 각자 중복 선언돼 있었는데(이 저장소의 기존 관례), pc/mobile 정규화 로직까지 중복시키면 두 파일이 어긋날 위험이 커서 이번엔 `heroSlideshow.ts`와 동일하게 신규 공용 파일 3개(`src/lib/mainLogoSettings.ts`/`sidebarIconsSettings.ts`/`topTabStyleSettings.ts`)로 뽑아 관리자 페이지와 Navbar.tsx가 타입+정규화(`normalizeMainLogo` 등)를 공유하게 했다. `site_settings`의 키(`main_logo`/`sidebar_icons`/`top_tab_style`) 자체는 그대로 — JSON 값의 모양만 `{pc, mobile}`로 바뀌었고, 옛 flat 데이터는 로드 시 pc/mobile 양쪽에 동일하게 채워 넣어 하위 호환한다.
- **관리자 페이지**: 좌측 섹션 목록 위에 "🖥️ PC 설정 / 📱 모바일 설정" 토글 신설(메인 로고/사이드바 아이콘/상단 탭 디자인 3개 섹션이 공유 — 슬라이드쇼는 이미 자체 PC/모바일 탭이 있어 대상 아님, 하단 메뉴는 Craft 전체화면 에디터라 별개). 기존 JSX(수십 개 입력 필드)를 건드리지 않기 위해 "읽기/쓰기 shim" 패턴을 썼다 — `mainLogo`/`sidebarIcons`/`topTabStyle` 변수는 여전히 "지금 고른 기기의 단일 설정"을 가리키고, `setMainLogo(next)` 같은 기존 setter 호출도 그대로 동작하되 내부적으로만 `mainLogoValue[chromeDeviceTab]`에 쓰도록 리다이렉트했다 — 실제 JSX 필드 바인딩은 단 한 줄도 안 바꿔도 됐다. 저장 버튼과 실시간 프리뷰(`overrides`)는 활성 기기 값이 아니라 `{pc,mobile}` 전체(`mainLogoValue` 등)를 내보낸다.
- **Navbar.tsx**: 신규 `useIsMobileViewport()` 훅(`matchMedia("(max-width: 767px)")`, 기존 hero_slideshow의 md 브레이크포인트 관례와 동일)으로 실제 뷰포트를 판정해 `mainLogoValue.pc`/`.mobile` 중 하나를 골라 기존 이름(`mainLogo` 등)으로 노출 — 이 파일도 마찬가지로 다운스트림 렌더 코드(`mainLogo?.text` 등 10여 곳)를 전혀 안 건드렸다(전부 optional-chaining + `||` 기본값 패턴이라 "값이 없는 null" 대신 "필드가 빈 문자열인 항상-존재하는 객체"로 바뀌어도 동일하게 동작함을 먼저 확인).
- **검증**: `npx tsc --noEmit`/`npm run lint`/`npm run build` 전부 0 errors. 로컬 dev + Browser pane 실측: 홈페이지를 PC/모바일 뷰포트 둘 다에서 스크린샷으로 확인(Navbar 정상 렌더, 새 에러 없음 — 사이트 전체에 영향 주는 변경이라 특히 신경 써서 확인). 관리자 페이지에서 모바일 탭으로 전환해 로고 텍스트를 다르게 입력한 뒤 PC 탭으로 되돌아가면 원래 값이 그대로 유지되는 것을 DOM에서 직접 확인(PC="Silo Store", 모바일="MOBILE ONLY LOGO" 로 서로 독립).
- **다음에 확인 필요**: 실제로 모바일 전용 값을 저장한 뒤 진짜 모바일 기기(또는 좁은 브라우저 창)로 사이트를 열어 반영되는지, 상단 탭 디자인의 tier1/tier2 오프셋처럼 레이아웃에 민감한 필드가 모바일에서 이상하게 겹치지 않는지.
- **변경/신규 파일**: `src/app/admin/navigation/settings/page.tsx`, `src/components/Navbar.tsx`, `src/lib/mainLogoSettings.ts`(신규), `src/lib/sidebarIconsSettings.ts`(신규), `src/lib/topTabStyleSettings.ts`(신규), `src/lib/useIsMobileViewport.ts`(신규).

## 2026-08-18 (HOTFIX-128 — SILO 고정 카테고리 마커 삭제 + 오브제 상단 클러스터 배치 + 게시판 연동 정보 카드)
- **배경**: 사용자가 "SILO 행성에 있는 고정된 오브제들(사일로상점/살롱데상/스튜디오/온라인도슨트)을 다 삭제시켜줘", "행성에 있는 오브제들을 가까운곳에(행성 맨위에) 모아달라니까?"(이전 요청이 반영 안 됐다는 재지적), "오브제를 클릭했을 때 팝업에 연결된 게시판의 썸네일과 설명이 올라오게 해줘 + 관리자 창에는 어떤 게시판 링크로 연결되는지/썸네일/설명을 수정할 수 있게" 세 가지를 요청.
- **고정 카테고리 마커 완전 삭제**: `CORE_CATEGORIES`(보물상자/조각상/휴대폰/카메라 4종 하드코딩 모양) + `CoreCategoryShape`/`CoreCategoryNode`/`CoreCategories` 컴포넌트 전부와, 그 모양을 고르던 `UniverseSettingsPanel`의 "기본 카테고리 마커 모양" 섹션(chestVariant/cameraVariant, `UniverseConfig`에서도 필드 자체 제거) 전부 삭제. 카테고리 링크는 이제 순수하게 관리자가 자유 오브젝트를 올리고 "연결된 게시판"을 고르는 것으로 대체된다(아래 항목).
- **오브제 상단 클러스터 배치**: `localSurfacePlacementsFor`를 피보나치 구면 고른 분포에서, 행성 로컬 "위" 방향(북극)을 중심으로 골든 앵글 나선으로 촘촘히 모으는 방식으로 교체 — `obj.position`이 없는(한 번도 드래그 안 한) 오브젝트만 적용되고, 이미 옮긴 오브젝트는 그대로 유지. SILO/유저 행성 둘 다 동일하게 적용(공용 함수).
- **게시판 연동 정보 카드**: `UniverseObject`에 `boardSlug` 필드 신설. `ObjectInspectorPanel`에 "연결된 게시판" select(공개 게시판 목록, `boards` 테이블 직접 조회) 추가 — 고르면 `ObjectInfoCard`가 그 게시판의 실제 `name`/`description`/`thumbnail_url`(`boards.slug`로 조회)을 자동으로 보여준다. 기존 썸네일/요약/링크 필드는 "오버라이드" 의미로 재정의(값을 직접 입력해두면 게시판 값보다 우선) — 게시판 원본 레코드를 건드리지 않고 오브제 단위로 다르게 보여줄 수 있다. 링크도 boardSlug가 있으면 `/boards/{slug}`로 자동 연결(수동 링크가 있으면 그게 우선).
- **검증**: `npx tsc --noEmit`/`npm run lint`/`npm run build` 전부 0 errors(사용하지 않게 된 `Text`/`useRouter` import도 함께 정리). 로컬 dev + Browser pane 실측: About Silo 페이지에서 카테고리 마커(사일로 상점/살롱데상/스튜디오/온라인 도슨트 라벨)가 더 이상 보이지 않는 것 확인, SILO 행성 상단에 오브제가 모여 배치되는 것 스크린샷으로 확인, 콘솔에 새 에러 없음.
- **변경 파일**: `src/components/about-silo/AboutSiloUniverse.tsx`, `src/components/about-silo/ObjectInspectorPanel.tsx`, `src/components/about-silo/PlanetSettingsPanel.tsx`, `src/components/about-silo/UniverseSettingsPanel.tsx`, `src/lib/aboutSiloUniverseConfig.ts`.

## 2026-08-18 (HOTFIX-127 — 우주 공간 오브젝트(별/은하수/소행성) 업로드 + 하단 메뉴 관리 병합 + 홈페이지 설정 관리 프리뷰 실시간화)
- **배경**: 사용자가 세 가지를 요청 — (1) "universe setting에서 오브제를 업로드할 수 있는 게 없네, 예를 들어 별, 은하수, 별똥별, asteroid 등등 우주에 있는 것들 말이야. 추가해줘", (2) "'홈페이지 설정관리'에 '하단메뉴관리'를 병합해줘", (3) "'홈페이지 설정관리'에 프리뷰가 안 나오는데? PC와 모바일 버전 실시간으로 보이게 해줘".
- **(1) 우주 공간 오브젝트**: 기존 오브젝트 업로드는 전부 "행성 표면에 중력으로 붙는" `UniverseObject`뿐이었다 — 별/은하수/별똥별/소행성처럼 어느 행성에도 속하지 않고 두 행성 사이 우주 공간에 자유롭게 떠 있는 오브젝트 개념이 아예 없었다. 신규 `SpaceObject`(`UniverseObject`를 확장해 `kind: "model"|"sprite"` 추가) + `UniverseConfig.spaceObjects[]`를 추가하고, `UniverseSettingsPanel`(전역 설정, 특정 행성에 속하지 않으므로)에 "🪨 소행성(.glb) 추가"(3D 모델, useGLTF)와 "✨ 별/은하수 이미지 추가"(카메라를 항상 향하는 스프라이트 빌보드, useTexture) 두 업로드 버튼을 신설. 기본 위치는 id를 해시한 시드로 두 행성 중간 지점 주변에 결정론적으로 흩뿌리고(새로고침해도 자리가 안 바뀜), 3D 뷰에서 클릭 선택 + 파란 화살표로 자유 드래그(행성 표면 오브젝트와 달리 "중력"/표면 스냅 없음) + 선택 시 glow + 스프라이트는 밝기가 사인파로 반짝이는 트윙클 적용. `ObjectInspectorPanel`/`ObjectInfoCard`는 구조가 같아 그대로 재사용(썸네일/요약/링크 편집·표시도 동일하게 동작).
- **(2) 하단 메뉴 관리 병합**: `/admin/footer`가 쓰던 것과 동일한 Craft.js 풀스크린 에디터(`CraftFooterEditor`, `page_builder(slug="footer")` 행 로드/생성 로직 포함)를 "홈페이지 설정 관리"의 5번째 좌측 섹션("하단 메뉴 관리")으로 그대로 흡수 — 클릭하면 기존과 동일한 Craft 에디터가 전체화면으로 뜨고, 닫으면 "메인 로고" 섹션으로 돌아온다(기존엔 `/admin/site-structure`로 이동했음). 관리자 상단 탭 목록에서 "하단 메뉴 관리" 항목은 제거(라우트 자체는 직접 URL 접근용으로 남겨둠).
- **(3) 프리뷰 실시간화**: 두 가지 문제였다 — (a) **안 보이던 버그**: 직전 EPIC-126에서 프리뷰 영역에 `2xl:block`(1536px 이상에서만 노출)을 걸어놨는데, 일반적인 관리자 브라우저 창 폭(1280~1440px대)에서는 그 기준을 못 넘어 항상 숨겨져 있었다 — `xl:block`(1280px)로 낮추고 상자 폭도 640px로 재조정. (b) **저장해야만 갱신되던 것**: 기존엔 실제 배포 페이지를 그대로 iframe으로 띄우되(정확성 우선) DB에 저장된 값만 반영해 "저장하기"를 눌러야 보였다 — `postMessage` 기반 실시간 오버라이드를 추가해, 미리보기 iframe이 `?__adminPreview=1`로 열렸을 때만 `Navbar.tsx`가 부모 창이 보내는 아직 저장 전인 현재 편집 값(메인 로고/사이드바 아이콘/상단 탭 디자인)을 즉시 반영하도록 했다(그 쿼리 파라미터가 없는 일반 방문자에게는 완전히 no-op). **알려진 한계**: "슬라이드쇼" 섹션은 이 실시간 미리보기에 반영되지 않을 수 있다 — 홈페이지가 이미 Craft.js 에디터로 완전히 렌더링되고 있어(`CraftHomeRenderer`) `site_settings.hero_slideshow`를 이 방식으로는 실시간으로 보여줄 대상 자체가 없을 수 있기 때문(기존부터 있던 구조적 불일치, 이번에 새로 만든 문제 아님) — 미리보기 안내 문구에 이 사실과 "페이지 수정"으로 직접 편집하라는 안내를 추가.
- **검증**: `npx tsc --noEmit`/`npm run lint`/`npm run build` 전부 0 errors. 로컬 dev + Browser pane 실측: (1) `/admin/navigation/settings`에서 1450px 폭으로 프리뷰가 실제로 노출되는 것 확인, 로고 텍스트 입력값을 바꾸자 저장 없이도 PC/모바일 두 iframe의 실제 DOM 텍스트가 즉시 바뀌는 것을 `iframe.contentDocument`로 직접 확인. (2) "하단 메뉴 관리" 섹션 클릭 → Craft 풀스크린 에디터가 뜨고 "닫기"를 누르면 "메인 로고" 섹션으로 돌아오는 것 확인. (3) About Silo의 "🌌 Universe Settings" 패널에 "우주 공간 오브젝트(0개)" 섹션과 두 업로드 버튼이 정상 렌더링되는 것 확인(실제 파일 업로드 왕복은 이번 세션에서 실행 안 함 — 기존에 검증된 행성 오브젝트 업로드와 동일한 패턴 재사용).
- **변경/신규 파일**: `src/lib/aboutSiloUniverseConfig.ts`(`SpaceObject` 타입/필드), `src/components/about-silo/AboutSiloUniverse.tsx`(`SpaceObjectModel`/`SpaceObjectSprite`/`SpaceObjectsLayer` 등 신규 + 선택/드래그 배선), `src/components/about-silo/UniverseSettingsPanel.tsx`(업로드 UI), `src/app/admin/navigation/settings/page.tsx`(5번째 섹션 + 프리뷰 breakpoint/overrides), `src/app/admin/layout.tsx`(하단 메뉴 관리 탭 제거), `src/components/Navbar.tsx`(postMessage 리스너), `src/components/admin/LivePreviewFrame.tsx`(overrides prop).

## 2026-08-18 (EPIC-126 — "홈페이지 설정 관리"를 섹션 클릭형 3단 레이아웃으로 재편 + 상단 탭 hover 모션 6종 + 드래그 슬라이더)
- **배경**: 사용자가 "관리자의 '홈페이지 설정관리'를 다시 재편할 필요가 있어. craft.js를 활용해서 페이지 빌더를 다시 만들어... 각 섹션별 설정들을, 섹션을 클릭하면 수정할 수 있는 사이드바가 왼쪽에 뜨게 하고, 오른쪽에 '데스크탑'과 '모바일' 프리뷰가 더 크게 뜨게 해. 그리고 자유롭게 드래그 앤 드랍 형식으로... 상단 탭 디자인의 '탑 섹션 높이', '1단 겹침정도', '2단 위치조정'을 숫자값 대신 드래그앤드롭으로... 각 탭 위에 커서가 hover 되었을 때의 모션들을 6가지로 설정할 수 있게, 고급스러운 느낌이 나도록 네가 임의로 판단해서"라고 지시.
- **범위 조사(코드 작성 전 조사 에이전트로 기존 Craft.js 인프라 전수 확인)**: 이 저장소는 이미 EPIC-098~111에 걸쳐 매우 성숙한 Craft.js 페이지 빌더(원자 블록 10종, 자유 배치 드래그/리사이즈 `FreePositionHandles`, 모션 프리셋 3종, `CraftPageEditor`/`CraftPageRenderer`/`CraftWidgetShell` 공용 셸, Footer의 "합성 slug" 패턴)를 갖추고 있음을 확인. 다만 **"홈페이지 설정 관리"(`/admin/navigation/settings`)가 다루는 4개 섹션(메인 로고/슬라이드쇼/사이드바 아이콘/상단 탭 디자인)은 전부 `site_settings`를 직접 읽는 `Navbar.tsx`/`HeroSlideshow.tsx`(비-Craft) 코드이고, Craft로 옮겨간 적이 없다** — 그리고 이 4개는 로그인 여부와 무관하게 **모든 페이지**에서 렌더링되는 전역 크롬(chrome)이라, 홈페이지 콘텐츠(이미 Craft로 완전히 전환됨, `CraftHomeRenderer`)와는 블라스트 반경이 질적으로 다르다.
- **명시적으로 축소한 범위(위험도 판단)**: 전역 크롬 렌더링 자체를 Craft.js로 완전히 이전(Navbar.tsx를 걷어내고 Craft 블록으로 대체)하는 것과, 로고/사이드바/탭 바 안에 버튼·이미지·영상 등을 자유롭게 드래그해 겹쳐 놓는 진짜 Craft 자유 배치 기능은 **이번 범위에서 제외**했다 — 모든 페이지의 헤더/사이드바를 새 렌더링 경로로 바꾸는 것은 실패 시 사이트 전체가 깨지는 성격의 변경이라, 기존에 검증된 `site_settings` 기반 렌더링 경로(`Navbar.tsx`)는 그대로 두고 **관리자 편집 경험만** WYSIWYG에 가깝게 업그레이드하는 쪽을 택했다. 사용자가 여전히 전역 크롬 자체를 Craft로 완전히 이전하길 원하면 별도 EPIC으로 진행 필요 — 세션 중 사용자에게 이 판단 근거를 함께 전달함.
- **레이아웃 재편**: 4개 섹션을 전부 세로로 쌓아 보여주던 단일 폼을, 좌측 섹션 목록(클릭형 네비게이션) → 클릭한 섹션 하나만 보여주는 중앙 편집 폼 → 우측 실시간 PC/모바일 프리뷰의 3단 레이아웃으로 재편(`AdminNavigationSettingsPage`의 `activeSection` state + `SECTION_NAV`). 프리뷰 상자도 기존 340px/220px(세로 스택)에서 440px/240px(가로 배치)로 확대(`LivePreviewFrame.tsx`의 `DEVICE_PRESETS`), 컨테이너 폭도 `max-w-6xl`→`max-w-[1600px]`로 확장.
- **드래그 슬라이더로 숫자 입력 대체**: 신규 `DragValueSlider`(`src/components/admin/DragValueSlider.tsx`) — 트랙을 포인터로 누르고 끌면 min~max 범위 안에서 실시간으로 값이 바뀌는 1D 드래그 슬라이더(정밀 조정용 ±버튼 병행). "상단 탭 섹션 높이"/"1단 겹침 정도"/"2단 위치 조정" 3개 필드의 `<input type="number">`를 이걸로 교체.
- **상단 탭 hover 모션 6종**: 신규 `src/lib/tabHoverMotion.ts` — 밑줄 슬라이드/자간 확장/은은한 부상+그림자/배경 와이프/확대+글로우/금빛 그라디언트 밑줄(추천, 기본값) 6종 + 없음, 전부 순수 CSS(가상 요소+transition)로 구현해 성능 부담 없음. 탭마다 개별 선택 가능(`TopTabStyleEntry.hoverMotion`, "상단 탭 디자인" 섹션의 탭별 편집 블록에 select 추가), 아무 것도 안 고르면 사이트 전체 탭에 기본값(금빛 그라디언트 밑줄)이 이미 적용된다 — 기존엔 커스텀 필드가 하나라도 설정된 탭에만 스타일 클래스가 붙었는데, hover 모션은 그 여부와 무관하게 모든 탭에 적용돼야 해서 `Navbar.tsx`의 `topTabStyleCss` 생성 로직을 "커스터마이즈된 탭만" 순회에서 "전체 navTabs" 순회로 변경.
- **검증**: `npx tsc --noEmit`/`npm run lint`/`npm run build` 전부 0 errors(홈페이지 포함 전 페이지 정상 빌드). 로컬 dev + Browser pane 실측: (1) 홈페이지에서 탭 클래스+주입된 hover 모션 CSS가 정확히 생성되는 것을 DOM에서 직접 확인. (2) `/admin/navigation/settings`에서 좌측 4개 섹션 클릭 전환, "상단 탭 디자인" 섹션의 3개 드래그 슬라이더와 탭별 hover 모션 select(7개 탭 전부) 렌더링 확인. (3) 드래그 슬라이더 로직은 합성 PointerEvent를 트랙의 90% 지점에 디스패치해 값이 정확히 `-100 + 0.9*300 = 170px`로 계산되는 것을 확인(자동화 도구의 `left_click_drag`가 실제 PointerEvent를 발생시키지 않아 이 방식으로 검증 — 실제 사람 손 드래그는 다음에 확인 필요).
- **다음에 확인 필요**: 실제 마우스로 드래그 슬라이더를 조작하는 손맛, 상단 탭 hover 모션이 기존 배경색 hover 효과(green-800)와 겹쳤을 때 실제로 눈에 띄는지(밑줄류 프리셋은 배경 박스 아래 가장자리에 나타나 상대적으로 은은함 — 의도된 절제인지 더 강조가 필요한지 사용자 확인), 새 프리뷰 크기가 실제 관리자 화면(1536px 이상 2xl 뷰포트에서만 노출)에서 잘리지 않는지.
- **변경/신규 파일**: `src/app/admin/navigation/settings/page.tsx`, `src/components/Navbar.tsx`, `src/components/admin/LivePreviewFrame.tsx`, `src/components/admin/DragValueSlider.tsx`(신규), `src/lib/tabHoverMotion.ts`(신규).

## 2026-08-18 (HOTFIX-125 — 게시글 위성 hover/클릭 무반응 + 행성 색·텍스처 블렌드 안 되던 버그 수정)
- **배경**: 사용자가 EPIC-124 직후 실사용하며 두 가지를 신고 — (1) 행성을 도는 특정 게시판 게시물(위성)에 커서를 올려도 반응이 없고 클릭도 안 된다, 커서를 올리면 반짝이게 하고 클릭하면 그 게시물의 썸네일+제목이 보이게 해달라. (2) 행성 설정의 "색상 ↔ 텍스처 블렌드" 슬라이더가 전혀 작동하지 않는다.
- **(1) 위성 hover/클릭 무반응 근본 원인**: HOTFIX-123부터 게시글 썸네일이 hover 없이도 CSS로 자동 페이드 인/아웃하도록 바뀌면서, 화면에서 가장 크고 눈에 띄는 요소가 이 사진이 됐다 — 그런데 실제 클릭 가능한 3D 마커(작은 팔면체 별)는 이 사진과 다른 화면 좌표(y로 0.24 유닛 오프셋)에 있고, 사진 쪽 Html 오버레이는 `pointerEvents:"none"`이라 클릭이 그대로 캔버스를 통과해 아무 것도 없는 지점을 때렸다 — 사용자는 자연히 눈에 보이는 사진을 hover/클릭하는데 정작 반응하는 건 그 옆의 보이지 않는 작은 별뿐이었던 것. 사진 자체(`OrbitSatelliteMarker`)를 `pointerEvents:"auto"` + 실제 클릭/hover 핸들러를 가진 진짜 인터랙션 대상으로 바꾸고, hover 시 CSS `box-shadow`로 즉시 밝게 빛나는 테두리(글로우)+확대 효과를 준다. 클릭 시 뜨는 `SelectedPostPanel`에도 그동안 빠져있던 썸네일 이미지를 제목 옆에 추가.
- **(2) 색상 ↔ 텍스처 블렌드 무반응 근본 원인**: 기존엔 베이스 색 구 위에 텍스처를 입힌 반투명 구를 살짝 더 큰 반지름으로 겹쳐(WebGL 알파 블렌딩에 의존) "블렌드"를 흉내 냈는데, 이 방식은 조명 각도/뷰 방향에 따라 결과가 달라지고 슬라이더를 중간값(50%)으로 둬도 텍스처 레이어가 시각적으로 거의 다 덮어버려 색이 거의 안 보였다(육안 확인: 50%에서도 100% 텍스처와 구분이 안 감, 0%에서만 정상적으로 색이 보임). 3D 알파 블렌딩에 맡기는 대신 2D Canvas에서 픽셀 단위로 직접 합성(수채화 베이스를 그린 뒤 업로드 텍스처를 `globalAlpha=textureOpacity`로 그 위에 덧그려 "이미 섞인 이미지 하나"를 만듦)하도록 `PlanetMaterial`을 재작성 — 이제 조명/각도/렌더 순서와 무관하게 항상 슬라이더 비율 그대로 섞인다(신규 `usePlanetBlendedTexture`/`paintWatercolor`, 기존 `CustomPlanetTextureLayer`/`useWatercolorTexture` 제거).
- **개발 중 겪은 lint 이슈**: 새 `usePlanetBlendedTexture`를 처음엔 `useRef` 싱글턴 캐시(3D 코드에 흔한 패턴)로 짰다가 이 저장소의 React Compiler 린트(`react-hooks/refs`)가 "렌더 중 ref.current 접근" 자체를 금지하는 것과 충돌 — 기존 `useWatercolorTexture`와 동일하게 "입력이 바뀔 때마다 `useMemo` 안에서 캔버스를 통째로 새로 그려 새 텍스처를 반환"하는 순수한 형태로 재작성해 해결.
- **검증**: `npx tsc --noEmit`/`npm run lint`/`npm run build` 전부 0 errors. 로컬 dev + Browser pane 실측: 색↔텍스처 슬라이더를 60%/40%로 두자 초록+갈색이 실제로 얼룩덜룩 섞인 것을 스크린샷으로 확인(이전엔 40%에서도 순수 텍스처처럼 보였음). 위성 클릭은 궤도 회전 때문에 좌표 기반 자동클릭이 계속 빗나가, DOM에 직접 `click` 이벤트를 디스패치해 검증 — `SelectedPostPanel`이 썸네일+제목+요약+링크와 함께 정상적으로 뜨는 것 확인.
- **변경 파일**: `src/components/about-silo/AboutSiloUniverse.tsx`.

## 2026-08-18 (EPIC-124 — 행성별 독립 설정 창(SILO/유저) + 오브제 정보 카드 + 게시글 위성 자동 페이드/디자인 업로드 + SILO 오브제 정리)
- **배경**: 사용자가 HOTFIX-123 직후 한 번에 여러 항목을 지시 — (1) SILO 행성의 "venus de milo" 외 모든 오브제 삭제 + 남은 오브제를 한곳에 모으기, (2) 오브제가 행성과 함께 자전하도록, (3) 오브제가 행성 "표면" 위에 제대로 놓이도록, (4) 오브제를 클릭하면 뜨는 썸네일/요약/링크를 관리자가 설정 가능하게, (5) SILO를 도는 "게시글 위성"이 지정 게시판 글 썸네일을 hover 없이도 주기적으로 떴다 사라지게 + 위성 디자인 업로드, (6) 유저 행성도 SILO와 동일한 구조(오브제/위성 디자인)로 설정 가능하되 SILO 설정과는 "다른 창"으로, (7) 행성을 클릭하면 그 행성 전용 설정 창이 뜨게, (8) 배경/디자인 등 전체 공통 설정은 별도로 관리.
- **데이터 모델 재구성(항목 2, 3, 6, 7의 전제)**: `UniverseConfig`가 지금까지는 `planetName`/`planetColor`/`characterModelUrl`/`objects`/`boardSlug` 등을 최상위에 SILO 전용으로 평평하게 갖고 있었고 유저 행성은 색상 하나 빼면 설정 자체가 불가능했다 — `planets: { silo: PlanetConfig; user: PlanetConfig }`로 재구성해 두 행성이 완전히 동일한 필드 묶음(이름/색/텍스처/텍스처 블렌드/캐릭터 .glb+애니메이션/장식 오브젝트/궤도 위성 게시판 소스/위성 디자인)을 갖게 했다. `normalizeUniverseConfig()`가 옛 평평한 저장값을 `planets.silo`로 자동 마이그레이션해 하위 호환(`src/lib/aboutSiloUniverseConfig.ts`).
- **오브제가 행성과 함께 자전(항목 2) + 표면 클리핑 완전 해소(항목 3)**: 장식 오브젝트 레이어(`UniverseObjectsLayer`)를 각 행성의 자전 그룹(`CentralPlanet`/`UserPlanet`의 `meshRef`/`spinRef`) **안으로 로컬 좌표 중첩**시켰다 — 이제 오브젝트 위치는 행성의 월드 중심이 아니라 "그 행성 로컬 원점(0,0,0)" 기준이라 부모 그룹이 자전하면 표면에 붙은 채 함께 돈다. 동시에 표면 투영 수식이 단순해져(더 이상 SILO_CENTER를 빼고 더할 필요 없음) 좌표 계산 버그 여지가 줄었다. 유저 행성도 SILO와 동일한 `PlanetMaterial`(텍스처 블렌드 지원)로 교체해 완전히 같은 표면 렌더링 파이프라인을 공유한다.
- **오브제 클릭 시 썸네일/요약/링크 정보 카드(항목 4)**: `UniverseObject`에 `thumbnailUrl`/`summary`/`link` 3개 필드 신설, `ObjectInspectorPanel`(편집용, 우측 상단)에 이미지 업로드+요약 textarea+링크 입력 추가. 신규 `ObjectInfoCard`(공개용, 하단 중앙 — `SelectedPostPanel`과 같은 자리/스타일)가 오브젝트 선택 시 그 내용을 보여준다(세 필드 모두 비어있으면 아무것도 안 뜸).
- **게시글 위성 자동 페이드 + 디자인 업로드(항목 5)**: 사진 미리보기를 hover 전용에서 CSS `@keyframes` 기반 자동 페이드 인/아웃(마커마다 `post.id` 해시로 다른 위상)으로 전환 — hover/선택 시에는 애니메이션을 일시정지하고 불투명도 1로 고정. `PlanetConfig.satelliteDesignUrl`(이미지 업로드)이 있으면 기본 별(팔면체) 대신 그 이미지를 카메라를 향하는 스프라이트로 렌더링(`SatelliteDesignSprite`), 없으면 기존 별 모양 폴백.
- **유저 행성도 SILO와 동일 구조, 단 별도 창(항목 6, 7)**: `UserPlanet`도 `CentralPlanet`과 동일하게 오브젝트 레이어+게시글 위성(`PlanetSatellites`, 자체 board_slug/디자인)을 갖는다. 신규 `PlanetSettingsPanel.tsx`(행성 하나를 담당 — 이름/색/텍스처/캐릭터/오브젝트 목록/위성 소스+디자인)를 SILO/유저 둘 다 재사용하되, 어느 행성을 클릭했느냐에 따라 한 번에 하나만 뜬다(`openPlanetSettings` 상태) — 서로 다른 인스턴스라 항상 "다른 창"처럼 동작한다. 행성(SILO/유저) 클릭 시 카메라 포커싱과 동시에 이 설정 창이 열리도록 `useDragAwareClick` 콜백에 `onOpenSettings` 추가.
- **전체 공통 설정 분리(항목 8)**: `UniverseSettingsPanel`을 배경(유튜브/프리셋)/공전 속도/기본 카테고리 마커 모양(보물상자·카메라 종류)/연결선 색/자동저장·저장 버튼만 남기도록 트리밍 — 행성별 필드는 전부 위 `PlanetSettingsPanel`로 이동.
- **SILO 오브제 정리(항목 1, 라이브 데이터 변경)**: 라이브 DB(`site_settings.about_silo_universe`)를 Supabase Management API로 직접 조회해 SILO에 저장돼 있던 오브젝트 2개("venus de milo"/"vintage camera")를 확인, "vintage camera"를 삭제하고 "venus de milo"만 남긴 채(오브젝트가 하나뿐이라 자연히 "한곳"에 배치됨) 새 `planets` 스키마로 마이그레이션된 값을 직접 UPDATE — 실행 후 재조회로 `planets.silo.objects`에 venus de milo 하나만 남은 것 확인.
- **검증**: `npx tsc --noEmit`/`npm run lint`/`npm run build` 전부 0 errors(무관한 기존 경고만 그대로). 로컬 dev에서 페이지 텍스트/네트워크 요청(모두 200 OK, 마이그레이션된 게시판 slug로 위성 데이터 정상 로드)/콘솔(새 에러 없음)로 확인, 트리밍된 전역 패널이 실제로 배경/공전속도/마커모양/연결선/저장만 담고 있는 것도 DOM 텍스트로 확인. **다음에 확인 필요**: 이번 세션은 Browser pane이 화면에 표시되지 않아(호스트 측 UI 상태) 스크린샷/픽셀 좌표 클릭 검증을 못 했다 — 행성을 실제로 클릭했을 때 전용 설정 창이 뜨는지, 위성 썸네일이 실제로 페이드 인/아웃하는 게 눈으로 자연스러운지, 유저 행성 오브젝트 업로드/위성 디자인 업로드 왕복, 오브제 정보 카드가 실제로 뜨는지 사람이 직접 확인 필요.
- **변경/신규 파일**: `src/lib/aboutSiloUniverseConfig.ts`(대폭 재작성), `src/components/about-silo/AboutSiloUniverse.tsx`(대폭 재작성), `src/components/about-silo/UniverseSettingsPanel.tsx`(트리밍), `src/components/about-silo/PlanetSettingsPanel.tsx`(신규), `src/components/about-silo/ObjectInspectorPanel.tsx`(필드 추가).

## 2026-08-15 (HOTFIX-123 — 행성 드래그 스냅백/오브제 glow/저장 안 됨/표면 중력·클리핑/썸네일 반짝임 5건 수정)
- **배경**: 사용자가 About Silo 3D 우주(World Builder)를 실제로 써보고 5가지를 한 번에 신고/지시 — (1) 행성을 클릭+드래그 후 놓으면 놓인 곳이 아니라 자꾸 이전 위치로 돌아간다, (2) 오브제 클릭 시 선택됐다는 걸 알 수 있게 glow 효과, (3) 오브제 설정을 수정해도 저장이 안 된다, (4) 행성에 "중력" 개념을 줘서 근처 오브제가 표면에 붙고 드래그로 표면 위에서 움직이게 해달라 + SILO 행성에서 오브제가 행성 안에 있거나 표면에 절반만 나오는 버그 수정, (5) 게시글 마커(썸네일 오브제)가 반짝반짝 빛나게 + 클릭하면 미리보기가 hover 없이 계속 떠 있게.
- **(1) 행성 드래그 스냅백**: 실제로는 행성 자체가 이동하는 기능이 없었다 — 행성 위에서 드래그하면 `CameraControls`가 카메라를 궤도 회전시키는데, 마우스를 그 행성 위에서 떼면 R3F의 `onClick`이 이동 거리와 무관하게 그대로 발생해 `onFocus()`가 매번 같은 고정 각도로 카메라를 스냅시키고 있었다 — "드래그해 옮긴 방향"이 아니라 "클릭한 지점" 기준의 고정 프레이밍으로 계속 되돌아가는 것처럼 보인 것. `useDragAwareClick` 신설(포인터다운→업 사이 화면 이동 거리가 5px 미만일 때만 진짜 클릭으로 간주) — `CentralPlanet`/`UserPlanet` 양쪽에 적용, 드래그(카메라 회전)와 클릭(포커스 이동)을 분리.
- **(2) 오브제 선택 glow**: 업로드된 임의 `.glb`의 내부 머티리얼을 직접 건드리는 대신(구조가 제각각이라 위험), 선택 시에만 나타나는 가산 블렌딩(`AdditiveBlending`) 오라 구체를 사인파로 맥동시키는 방식으로 추가(`UniverseObjectModel`). `EffectComposer`에 `Bloom`(`@react-three/postprocessing`) 신설 — 밝은 요소만 번지도록 `luminanceThreshold=0.55`로 임계값을 높게 잡아 씬 전체가 뿌예지지 않게 했다.
- **(3) 저장 안 됨**: 두 가지 원인 — (a) `handleSave`가 `error`를 그냥 버려서 RLS 거부/네트워크 실패가 완전히 조용히 사라졌다(성공 토스트도 안 뜨고 아무 피드백 없음) → `console.error` + 빨간 에러 토스트로 노출. (b) `ObjectInspectorPanel`(오브제 우측 인스펙터)에는 애초에 저장 버튼이 없어서, 관리자가 오브제만 편집하고 창을 닫으면 좌측 "🛠 Universe Settings" 패널의 "지금 저장" 버튼을 따로 찾아 눌러야 하는지 몰랐을 가능성이 큼(둘 다 같은 `handleSave`/`site_settings.about_silo_universe`를 공유하므로 저장 로직 자체는 정상 동작) → `ObjectInspectorPanel`에 "이 변경사항 저장" 버튼 신설(부모의 `handleSave`를 그대로 노출).
- **(4) 행성 중력(표면 부착) + SILO 표면 클리핑 버그**: 근본 원인은 `UniverseObjectsLayer`가 드래그로 옮긴 오브제의 위치(`obj.position`)는 그대로 쓰면서, 방향(quaternion, "표면 바깥쪽"을 가리키는 값)은 원래 인덱스의 기본 배치 값을 그대로 재사용하던 것 — 위치와 방향이 서로 어긋나 오브제가 행성 안에 파묻히거나 절반만 표면 밖으로 나온 것처럼 보였다. 매 렌더 `obj.position`을 `SILO_CENTER` 기준 구면(`SURFACE_PLACEMENT_RADIUS`)에 다시 투영하고, 방향도 그 지점의 실제 법선으로 다시 계산하도록 수정 — 이제 오브제는 항상 표면에 딱 붙는다("중력"). `TransformControls`의 `onChange`(드래그 중 매 프레임 호출)에도 동일한 투영을 적용해, 마우스를 떼기 전부터 표면을 따라 미끄러지듯 이동하는 느낌을 준다(기존엔 마우스를 뗀 순간에만 위치가 반영됐음).
- **(5) 썸네일 반짝임 + 클릭 시 계속 보이기**: `OrbitStarMarker`에 개별 위상(`post.id` 해시 기반 seed)으로 스케일/밝기를 사인파로 맥동시키는 트윙클 애니메이션 추가(Bloom과 합쳐져 실제로 빛나 보임). 사진 미리보기 표시 조건을 `hovered` 단독에서 `hovered || selected`로 바꿔, 클릭해 선택한 마커는 마우스를 치워도 미리보기가 계속 떠 있게 했다(선택 해제는 기존 `SelectedPostPanel`의 닫기 버튼 그대로 사용).
- **범위 관련 메모(6번째 요청 — "행성마다 오브제를 여러 개 추가할 수 있게")**: 장식 오브제 시스템(`config.objects[]`, `UniverseSettingsPanel`의 "+ .glb 오브젝트 추가")은 원래부터 개수 제한 없이 임의 개수를 지원한다 — 이번 세션에서 발견된 유일한 실질적 장애물은 (4)의 표면 클리핑 버그(오브제가 여러 개일 때 특히 두드러짐)였고, 그건 위에서 수정했다. 사일로 상점/온라인 도슨트/살롱데상/스튜디오 4대 카테고리 마커(`CORE_CATEGORIES`)는 여전히 코드에 하드코딩된 별도 시스템이라 5번째 카테고리를 추가하려면 이 상수 배열에 항목을 늘리는 별도 코드 변경이 필요하다 — 사용자가 예시로 든 "SILO 최상위 카테고리 5개"가 이 고정 4종 마커 확장을 뜻하는 것이라면 별도로 알려달라고 요청.
- **검증**: `npx tsc --noEmit`/`npm run lint` 0 errors(무관한 기존 경고 2건만 그대로). 로컬 dev + Browser pane 실측: SILO 행성 위에서 카메라를 드래그로 회전시킨 뒤 놓아도 즉시/지연 스냅백 없이 그 자리에 유지되는 것을 두 장의 스크린샷(직후/1.5초 후)으로 비교 확인, 진짜 클릭(카테고리 마커)은 여전히 정상적으로 포커스 이동하는 것도 확인, 콘솔에 새 에러 없음(관련 없는 React 19 `element.ref` 라이브러리 경고만 기존대로 존재).
- **변경 파일**: `src/components/about-silo/AboutSiloUniverse.tsx`, `src/components/about-silo/ObjectInspectorPanel.tsx`.

## 2026-08-15 (HOTFIX-122 — 게시글 마커를 sphere에서 hover-썸네일 별 모양으로 + 클릭 시 깜빡임 버그 수정)
- **배경**: 사용자가 EPIC-121에서 도입한 "sphere 썸네일"을 써보고 별로라며, 대신 SILO 행성을 도는 작은 별 모양 마커로 바꾸고 마우스를 올렸을 때만 동그란 사진 미리보기가 뜨게 해달라고 요청. 추가로 "썸네일을 클릭하면 깜빡이는(flicker) 현상"을 신고.
- **깜빡임 근본 원인**: `surfaceOpacityFor()`(카메라~SILO 거리 기반 opacity 페이드)가 EPIC-121에서 "내핵 오브젝트"(카테고리)에서는 제거됐지만 게시글 마커(`SurfaceImage`/`AboutSiloMarker`)에는 그대로 남아있었다. 이 함수는 "가까울수록 낮은 opacity"로 계산되는데(기존엔 "카메라가 다가가 안쪽 내핵을 보여주기 위해 표면 마커를 지운다"는 의도였던 로직의 잔재), 마커를 클릭하면 카메라가 그 마커 쪽으로 다가가도록 날아가고, 그러면 거리가 줄면서 이 마커 자신의 opacity가 계속 낮아지려 하다가 lerp로 요동치며 깜빡이는 것처럼 보였다.
- **수정**: 씬 전체에서 거리 기반 opacity 페이드 메커니즘(`DistanceContext`/`CameraDistanceTracker`/`surfaceOpacityFor`)을 완전히 제거했다 — 이제 "내핵을 숨겼다 드러내는" 연출 자체가 없으므로(EPIC-121부터 모든 카테고리/오브젝트가 항상 보임) 이 메커니즘은 더 이상 필요 없는 죽은 개념이었다.
- **썸네일 → hover 미리보기가 있는 별(HOTFIX-122 신규 `OrbitStarMarker`)**: `SurfaceImage`(텍스처 입힌 sphere)와 `AboutSiloMarker`(단순 점)를 이 컴포넌트 하나로 통합 — 평소엔 작은 팔면체(별/다이아몬드 모양) 마커만 떠 있고, 마우스를 올리면(`onPointerOver`) 그 옆에 실제 사진을 원형으로 담은 일반 HTML `<img>`(drei `<Html>`)가 나타난다. Three.js 텍스처 로딩(`useTexture`+`Suspense`+에러 바운더리)이 필요 없어져 마커 쪽 코드가 크게 단순해졌다(브라우저가 이미지 로딩/에러를 알아서 처리).
- **부가**: 이제 궤도 마커에 "sphere로 보여주기" on/off 토글 자체가 의미 없어져 설정 패널에서 제거(기존 `showThumbnails` 필드는 과거 저장값과의 호환을 위해 타입에만 `@deprecated`로 남김, 아무 코드도 안 읽음).
- **검증**: `npx tsc --noEmit`/`npm run lint`/`npm run build` 전부 0 errors. 로컬 dev + Browser pane 실측: 별 모양 마커가 SILO 행성 주위에 떠 있는 것 확인, 마커에 마우스를 올리자 실제 사진이 담긴 원형 미리보기가 뜨는 것을 스크린샷으로 확인, 마커를 클릭하자 카메라가 다가가며 "자세히 보기" 요약 패널이 정상적으로 뜨는 것까지 확인(콘솔에 새 에러 없음).
- **변경 파일**: `src/components/about-silo/AboutSiloUniverse.tsx`, `src/components/about-silo/UniverseSettingsPanel.tsx`, `src/lib/aboutSiloUniverseConfig.ts`.

## 2026-08-15 (EPIC-121 — 행성 줄무늬 제거/자유 회전/오브젝트 표면 배치+드래그/UI 통합 등 사용자 지시 다수 반영)
- **배경**: 사용자가 실제 화면 스크린샷 2장과 함께 다수 항목을 한 번에 지시 — (1) 행성 표면 줄무늬 제거, (2) 카메라 자유 회전(행성 아래에서 위로 못 돌리는 문제), (3) 우측 Leva 패널(배경/행성/캐릭터/내핵 오브젝트/연결선)을 좌측 Universe Settings로 통합 + 우측엔 선택된 오브젝트 전용 설정창, (4) "내핵 오브제"를 행성 안에 숨기지 말고 표면 위에 항상 노출, (5) venus de milo가 여전히 안 보이는 문제, (6) 오브젝트 크기/위치를 드래그앤드롭으로 조정, (7) 게시글 썸네일을 sphere로, (8) 행성 이름/색상↔텍스처 블렌드(투명도), (9) 각 설정 저장, 캐릭터도 .glb 업로드 가능하게, (10) 행성마다 공전 + 행성간 거리 설정.
- **줄무늬 제거 + 색↔텍스처 블렌드(항목 1, 8)**: 원인은 `meshToonMaterial`의 3단 `gradientMap`(셀 셰이딩 경계선)이 실사 텍스처 위에서 봉제선처럼 도드라져 보이던 것 — `PlanetMaterial`을 toon 셰이딩 없는 2겹 구조(베이스 색 구 + 텍스처 구를 살짝 큰 반지름으로 겹쳐 `opacity`로 블렌드)로 전면 교체. `planetTextureOpacity`(0~1) 슬라이더 신설. 유저 행성도 동일 이유로 toon 재질을 제거(`meshStandardMaterial`).
- **카메라 자유 회전(항목 2)**: `CameraControls`에 `minPolarAngle=0`/`maxPolarAngle=Math.PI`/무제한 `azimuthAngle`을 명시(기존엔 값을 아예 안 줘서 라이브러리 기본값에 암묵적으로 의존).
- **UI 통합(항목 3)**: `leva` 의존성을 이 컴포넌트에서 완전히 제거 — 예전에 Leva가 담당하던 배경 모드/프리셋/행성 색/썸네일 표시/공전 속도/캐릭터 폴백 모델/보물상자·카메라 종류/연결선 색을 전부 `UniverseSettingsPanel`(좌측 하단) 안으로 이동. 신규 `ObjectInspectorPanel`(우측 상단)이 3D 뷰에서 클릭 선택한 오브젝트 하나의 이름/크기/위치/삭제를 담당.
- **내핵 오브젝트를 표면으로(항목 4)**: `CoreCategoryNode`가 예전엔 반지름 0.9(행성 반지름 2.1보다 훨씬 안쪽)에서 카메라 거리 기반 LOD로 숨겨져 있다가 깊이 줌인해야만 드러났다 — 장식 오브젝트와 동일한 표면 반지름(`SURFACE_PLACEMENT_RADIUS`, 행성 반지름의 97%)에 항상 보이도록 변경, LOD 페이드 로직 전체 제거.
- **venus de milo가 안 보이던 근본 원인 추가 발견(항목 5)**: `fibonacciSphere()`의 위도 공식(`y = 1-(i/(count-1))*2`)이 `i=0`/`i=count-1`일 때 정확히 남·북극(`y=±1`)에 점을 찍는 결함이 있었다 — 게시글처럼 개수가 많을 땐 다수 중 2개일 뿐이라 안 보였지만, 오브젝트 2개/카테고리 4개처럼 개수가 작을 땐 절반이 극점에 겹쳐 찍히는 심한 편중 배치가 됐다(HOTFIX-120의 크기 정규화만으론 못 잡던 별도 원인). 표준 오프셋 변형(`y = 1-(2i+1)/count`)으로 교체해 극점을 피하고 고르게 분산되도록 수정 — 모든 `fibonacciSphere` 사용처(오브젝트/카테고리/About Me 위성/게시글 마커)가 함께 이득을 본다.
- **드래그앤드롭 위치 조정(항목 6)**: `@react-three/drei`의 `TransformControls`를 붙여 3D 뷰에서 오브젝트를 클릭 선택하면 이동 기즈모가 뜬다 — 드래그해 옮기면 그 좌표가 `UniverseObject.position`(명시적 오버라이드, 없으면 자동 표면 배치로 폴백)에 저장된다. 드래그 중엔 `CameraControls.enabled=false`로 카메라 회전과 충돌하지 않게 한다. 크기는 인스펙터 패널의 슬라이더로 조정.
- **게시글 썸네일을 sphere로(항목 7)**: `SurfaceImage`의 `planeGeometry`(카메라를 항상 바라보는 평면)를 `sphereGeometry`(사진을 텍스처로 입힌 작은 구슬)로 교체.
- **저장/캐릭터 업로드(항목 9)**: 새 필드(`planetName`/`planetTextureOpacity`/오브젝트 `position`) 전부 기존 `site_settings.about_silo_universe` 저장/자동저장 경로에 포함(스키마 자연 확장). 캐릭터 `.glb` 업로드는 EPIC-119부터 이미 지원 중이던 기능(재확인).
- **범위 밖으로 명시적으로 미룸(항목 10 일부)**: "각 행성마다 공전 + 행성간 거리 설정"은 SILO/유저 행성을 실제로 서로 다른 지점으로 움직이는 것을 뜻하는데, 현재 아키텍처는 카테고리/오브젝트/궤도 마커/연결선 전부 `SILO_CENTER`를 **고정 월드 좌표**로 계산한다 — 행성을 실제로 움직이려면 이 모든 자식을 하나의 움직이는 그룹 아래로 재배치하고 카메라 추적·거리 기반 페이드·연결선 끝점 계산을 전부 그 움직이는 좌표계에 맞게 다시 짜야 하는 큰 리팩터라, 이번 범위에서 서둘러 처리하면 카메라 추적이 깨지는 등 부작용 위험이 크다고 판단해 별도 EPIC으로 분리 제안 — 사용자에게 명시적으로 알림.
- **검증**: `npx tsc --noEmit`/`npm run lint`/`npm run build` 전부 0 errors. 로컬 dev + Browser pane 실측: 스크린샷으로 (a) 줄무늬 없이 매끈한 행성 확인, (b) SILO/My Page/4개 카테고리 라벨이 전부 처음부터 화면에 보이는 것 확인, (c) venus de milo/vintage camera/카테고리 마커가 실제로 행성 주변에 별개의 형태로 렌더링되는 것 확인, (d) 오브젝트 클릭 → 우측 인스펙터 패널 + 3D 뷰에 TransformControls 화살표 기즈모가 실제로 뜨는 것까지 확인, (e) 좌측 패널에 배경/행성/궤도/캐릭터/오브젝트/카테고리마커/연결선/저장이 전부 한 곳에 모인 것 확인(Leva 패널 잔재 없음).
- **변경/신규 파일**: `src/components/about-silo/AboutSiloUniverse.tsx`(대폭 재작성), `src/components/about-silo/UniverseSettingsPanel.tsx`(대폭 재작성), `src/components/about-silo/ObjectInspectorPanel.tsx`(신규), `src/lib/aboutSiloUniverseConfig.ts`(필드 추가), `src/lib/fibonacciSphere.ts`(극점 편중 버그 수정).

## 2026-08-15 (HOTFIX-120 — 업로드한 장식 오브젝트(.glb)가 화면에 반영 안 되던 버그 수정)
- **배경**: 사용자가 EPIC-119 직후 "venus de milo"라는 .glb 오브젝트를 업로드했는데 SILO 행성 표면에 나타나지 않는다고 신고.
- **근본 원인 확인(라이브 DB 직접 조회로 실측)**: 저장된 두 오브젝트("venus de milo"/"vintage camera")의 실제 `.glb` 파일을 Management API+fetch로 직접 내려받아 확인한 결과, **파일 자체는 완전히 정상**(glTF 2.0 매직 바이트/길이 일치, Supabase Storage에서 정상 서빙, CORS 문제 없음) — 문제는 로딩이 아니라 **렌더링 크기/방향**이었다. `venus de milo`는 원본 모델링 단위가 아주 작아(최대 변 0.28) 기존 고정 배율(관리자 scale 기본 0.3)을 곱하면 실제 크기가 0.083 유닛(행성 반지름 2.1의 4%도 안 됨)이 되고, 반대로 `vintage camera`는 원본이 커서(최대 변 2.80) 곱하면 0.84 유닛 — **행성보다 더 큰 오브젝트**가 된다. 게다가 배치 지점의 구면 법선으로 방향을 맞추는 로직 자체가 없어(EPIC-119 최초 구현엔 없었음) 두 모델 다 임의의 방향으로 행성 표면에 절반쯤 파묻히거나 뒤집힌 채 놓여 사실상 안 보였다.
- **수정**: `UniverseObjectModel`이 로드된 모델의 실제 바운딩 박스를 재서 목표 크기(0.5 유닛)로 먼저 정규화하고, 관리자가 지정한 scale은 그 위에 곱하는 배율로만 쓴다(원본 파일의 단위가 cm든 m든 상관없이 항상 일관된 크기로 시작). 바닥(bounding box 최저점)을 y=0에 맞춰 표면에 "박히지" 않고 서게 하고, 배치 지점의 구면 법선 방향으로 회전시켜(quaternion) 행성 어디에 놓여도 "위"가 바깥을 향하게 했다.
- **로드 실패 가시화(부가 개선)**: `.glb` 로드가 실패해도 기존엔 조용히 아무것도 안 보여 관리자가 원인을 알 방법이 없었다 — `AssetLoadErrorBoundary`에 `onError` 콜백을 추가해 실패한 오브젝트 id를 `UniverseSettingsPanel`로 올려보내고, 해당 오브젝트 행에 "⚠ 로드 실패" 문구를 표시한다.
- **검증**: `npx tsc --noEmit`/`npm run lint`/`npm run build` 0 errors. 라이브 DB에 실제 저장돼 있던 사용자의 두 오브젝트로 직접 재현+검증(임시 디버그 로그로 정규화 배율이 venus 1.80배/camera 0.18배로 정반대 방향으로 보정되는 것 확인, 검증 후 로그 제거) — 두 파일 모두 CORS/파싱 에러 없이 로드되는 것도 재확인. 별도로 공개 CORS 테스트 GLB(Khronos Duck.glb)를 실제 업로드 경로(파일 input에 합성 File 객체 주입)로 끝까지 흘려보내 업로드→저장→목록 반영까지 왕복 확인.
- **변경 파일**: `src/components/about-silo/AboutSiloUniverse.tsx`, `src/components/about-silo/UniverseSettingsPanel.tsx`.

## 2026-08-15 (EPIC-119 — About Silo 3D 우주를 [3D World Builder]로 전면 개편)
- **배경**: 사용자가 "3D 렌더링 퀄리티가 참담하다, 임의로 3D Mesh(찰흙 덩어리)를 억지로 코딩으로 만들지 말고 관리자가 외부 3D 모델(.glb)을 직접 업로드하고 씬을 게임처럼 커스텀할 수 있는 [3D World Builder] 아키텍처로 전면 개편하라"고 8개 항목(카메라 포커싱/물리 인터랙션, 파티클 배경, 커스텀 데이터/유튜브 프리셋, 3D 에셋 업로더/환경 빌더, 자동 저장)을 지시. 요청 문서상 번호는 "[EPIC-117]"이었으나 이 저장소는 이미 EPIC-118까지 진행돼 있어 다음 실제 번호인 **EPIC-119**로 기록한다.
- **아키텍처 전환(핵심)**: `AboutSiloUniverse.tsx`의 절차적 실루엣/오브젝트를 완전히 걷어내는 대신, **업로드된 에셋이 있으면 그것을, 없으면 기존 절차적 결과물을 폴백으로** 쓰는 구조로 바꿨다 — 지금 이 세션엔 관리자가 실제로 올릴 .glb/텍스처 파일이 없어(사용자 지시문도 "임시 GLTF"라 전제했던 EPIC-115와 동일한 제약) 완전 삭제하면 빈 화면만 남기 때문. `CharacterRenderer`(캐릭터), `PlanetMaterial`(행성 텍스처), `UniverseObjectsLayer`(장식 오브젝트) 세 곳 모두 이 폴백 패턴을 따른다.
- **[3D 에셋 업로더 & 환경 빌더] 신규 `UniverseSettingsPanel.tsx`**: 화면 좌측 하단 "🛠 Universe Settings" 접이식 패널 — (1) 캐릭터 `.glb` 업로드(`CustomCharacterModel`이 `useGLTF`+`useAnimations`로 로드, 클립 목록을 패널로 올려보내 관리자가 액션 버튼으로 재생 클립 선택), (2) 행성 지형 텍스처 이미지 업로드(`PlanetMaterial`이 `useTexture`로 교체), (3) 장식 오브젝트(나무/꽃 등) `.glb` 여러 개 추가/삭제/스케일 조절 — `fibonacciSphere`로 SILO 행성 표면에 자동 배치, (4) 유튜브 프리셋 URL 배열을 텍스트 입력으로 직접 추가/수정/삭제(`YoutubeBackground.tsx`가 `urls` prop을 받도록 확장, 하드코딩 8개는 기본값으로만 남음), (5) 궤도 썸네일 소스를 "전체 통합 피드" 대신 특정 게시판 하나로 좁히는 드롭다운(`/api/boards`에서 실제 목록 조회, 선택 시 `/api/boards/[slug]/posts`로 전환). 업로드는 기존 `uploadFile()`(attachments/gallery 버킷) 재사용.
- **카메라 포커싱(Item 5)**: SILO/유저 행성 자체를 클릭하면 `CameraControls.setLookAt`으로 그 행성 중심을 향해 부드럽게 줌인(fit-to-object 근사) — 기존엔 궤도 마커만 클릭 가능했다.
- **실뜨개 물리 인터랙션(Item 5)**: 연결선(`ConnectingThread`)을 드래그하면(`onPointerDown/Move`) 곡선의 제어점이 마우스를 따라 늘어나고, 놓으면 lerp 기반 감쇠로 원래 흔들림으로 서서히 복귀한다 — **주의**: 곡선 하나만을 위해 `@dimforge/rapier3d-compat`(이미 package.json에 있음, EPIC-113~115가 설치)까지 붙이는 건 과하다고 판단해 실제 물리 엔진 대신 목표점을 향한 lerp 수렴으로 "탄력 있게 늘어나고 흔들리는" 체감을 근사했다 — 진짜 물리 시뮬레이션은 아님. Leva "연결선" 섹션에 색상 피커 추가.
- **파티치 기반 감성 배경(Item 6)**: `<Stars>`(4500개)+`<Sparkles>`(140개, drei) 대량 배치 + 절차적 별똥별 `ShootingStars`(에셋 없이 스트릭 메시 4개를 재사용하는 풀, 랜덤 간격으로 발사·페이드아웃).
- **자동 저장(Item 7)**: `site_settings.about_silo_universe`(새 site_settings 키, `docs/database-schema.sql`이 이미 기술하는 기존 key-value 테이블 재사용 — 지시문의 "site_configurations 테이블 신설"은 스키마 변경 없이 기존 패턴으로 대체) — 카메라 구도를 제외한 전 설정(업로드 URL/별 개수 등은 상수, 실제 값은 색상·토글·URL 배열·모델 경로 등)을 JSON으로 직렬화. 5분 간격 자동 저장(체크박스로 on/off) + 수동 "지금 저장" 버튼 + 우측 상단 "✓ 저장됨" 토스트(2.2초). Leva 값은 `useControls`를 `[값, set]` 튜플 형태로 바꿔, DB 로드 시 `set()`으로 비동기 초깃값을 반영(Leva는 마운트 후 defaultValue를 다시 안 읽어 이 방법이 필요).
- **검증**: `npx tsc --noEmit`/`npm run lint`/`npm run build` 전부 0 errors(빌드까지 반드시 확인 — 바로 이전 HOTFIX-116 사고 재발 방지). 로컬 dev + Browser pane 실측: `/about-silo` 캔버스 렌더링(행성 2개+연결선+파티클 배경) 확인, Universe Settings 패널이 열리고 유튜브 URL 8개/게시판 드롭다운(실제 게시판 목록 로드 확인)/파일 업로드 버튼/오브젝트 목록 UI 전부 정상 렌더링, "지금 저장" 클릭 시 에러 없이 "저장됨" 토스트 노출 확인. **실제 에셋으로 검증 못한 것(이 세션에 업로드할 실제 .glb/텍스처 파일이 없음)**: 캐릭터 모델 업로드 후 실제 클립 재생, 장식 오브젝트가 행성 표면에 실제로 마운트되는 모습, 행성 텍스처 교체 후 시각 결과, 실뜨개 드래그의 실제 손맛(헤드리스 브라우저 pointer 이벤트 한계로 자동화 검증도 어려움) — 관리자가 실제 파일로 직접 업로드해 확인 필요.
- **변경/신규 파일**: `src/components/about-silo/AboutSiloUniverse.tsx`(대폭 개편), `src/components/about-silo/UniverseSettingsPanel.tsx`(신규), `src/components/about-silo/YoutubeBackground.tsx`(urls prop 추가), `src/lib/aboutSiloUniverseConfig.ts`(신규).

## 2026-08-15 (EPIC-118 — 1단 탭 계정 영역 충돌 수정 + 실제 홈페이지 기반 PC/모바일 미리보기로 교체)
- **배경**: 사용자가 스크린샷으로 두 가지를 지적 — (1) 로그인 상태에서 1단 탭(온라인 도슨트/스튜디오/마이 페이지)이 우측 계정 영역(관리자/등급/이름/로그아웃 버튼, 사이드바 열쇠 아이콘)과 겹쳐버림 + "상단 탭 섹션 높이"/"1단 겹침 정도"를 바꿔도 미리보기에 아무 변화가 없음(그리고 1단/2단 둘 다 위치를 조절할 수 있어야 함), (2) 우측 "실시간 미리보기"가 모바일 하나뿐이고 그나마도 제대로 안 보임(PC 미리보기도 필요).
- **1단 탭 위치 충돌 수정(`Navbar.tsx`)**: 이전엔 1단 탭 줄을 `position: absolute; right: 4` 로 로고 줄의 오른쪽 끝에 고정했는데, 그 자리가 계정 영역(로그인 시에만 나타나는 관리자/등급/이름/로그아웃)과 정확히 겹치는 자리였다 — 로그아웃 상태로만 검증하고 로그인 상태를 확인하지 않아 놓친 버그. `position: absolute` 대신 계정 영역 **바로 앞에 오는 일반 flex 형제**로 바꿔, 가로 위치는 flexbox가 자동으로 계정 영역과 절대 안 겹치게 잡아주도록 했다(로고 줄의 `gap-4`가 자연스러운 간격도 만들어줌) — 세로 겹침 효과(`translateY`, 로고 줄 경계선에 걸치는 것)는 그대로 유지.
- **"1단, 2단 위치를 둘 다 정할 수 있게"(사용자 지시)**: `TopTabStyleValue.tier2OffsetPx` 신설 — 2단(기존 탭 줄) 자체도 위/아래로 미세 이동 가능해졌다(`<nav>`에 `translateY` 적용). `/admin/navigation/settings`에 "2단 위치 조정" 입력 필드 추가. "상단 탭 섹션 높이"는 `min-height`라 탭 버튼 기본 높이(~36px)보다 작은 값은 원래도 시각적 효과가 없는 게 정상 동작(더 작게 줄이려면 탭 자체의 padding을 줄여야 하는데, 그러면 다른 화면 폭에서 줄바꿈이 깨질 위험이 있어 이번 범위 밖) — 라벨에 이 제약을 명시해 "설정해도 변화 없다" 혼동을 없앴다.
- **미리보기 전면 교체(`MobilePreviewFrame.tsx` 삭제 → `LivePreviewFrame.tsx` 신규)**: 기존 미리보기는 실제 `Navbar`를 쓰지 않고 로고+슬라이드만 손으로 흉내 낸 가짜였다(주석에 이미 명시돼 있었음) — 그래서 상단 탭 관련 설정처럼 이 가짜 미리보기가 애초에 그릴 줄 모르는 요소는 구조적으로 "변화 없음"으로 보일 수밖에 없었다. 실제 배포되는 홈페이지("/")를 `<iframe>`으로 그대로 띄우고 `transform: scale()`로 PC(1280px 기준)/모바일(390px 기준) 두 크기를 나란히 보여주는 방식으로 교체 — 100% 실제 렌더링이라 어떤 설정이든 정확히 반영된다. 트레이드오프: iframe은 DB에 저장된 값을 읽는 실제 페이지라 타이핑 중인 값은 못 보여준다 — 대신 각 섹션 `handleSave` 성공 시 `previewRefreshKey`를 올려 iframe을 자동으로 다시 불러오게 해(+수동 "↻ 새로고침" 버튼도 추가) "저장할 때마다 바로 갱신"되는 실시간성은 유지했다.
- **검증**: `npx tsc --noEmit`/`npm run lint`/`npm run build` 전부 0 errors. 로컬 dev + Browser pane 실측: 관리자 로그인 상태의 실제 홈페이지에서 1단 탭(온라인 도슨트 끝 x=1028.9)과 계정 영역(관리자~로그아웃 시작 x=1044.9)이 더 이상 겹치지 않는 것을 좌표로 확인, `/admin/navigation/settings`의 PC/모바일 미리보기 iframe 둘 다 실제 홈페이지 DOM(`<nav>` 14499자, 실제 사이트와 동일)을 그대로 담고 있는 것 확인, "2단 위치 조정"을 10으로 저장 → 두 미리보기 iframe이 자동으로 새로고침되며 `<nav>`에 정확히 `transform: translateY(-10px)`가 반영되는 것까지 확인(테스트 후 0으로 원복해 재저장).
- **변경/신규/삭제 파일**: `src/components/Navbar.tsx`, `src/app/admin/navigation/settings/page.tsx`, `src/components/admin/LivePreviewFrame.tsx`(신규), `src/components/admin/MobilePreviewFrame.tsx`(삭제).

## 2026-08-15 (EPIC-117 — 상단 탭 1단/2단 배치, 1단은 로고 섹션과 겹치게)
- **배경**: 사용자가 손그림 스크린샷으로 요청 — "온라인 도슨트/스튜디오/마이 페이지"는 로고 줄(1단)과 겹치는 자리에, "About Silo/사일로 상점/살롱데상"은 기존 탭 줄(2단)에 두는 2단 구조.
- **`Navbar.tsx`**: `TopTabStyleEntry.tier?: 1 | 2`(탭별 배치 지정, 없으면 2)와 `TopTabStyleValue.tier1OffsetPx`(1단 줄의 위/아래 미세 조정 px) 신설. 기존에 `<nav>` 안 인라인 `.map()` 콜백이던 탭 1개 렌더링 로직(단순 링크/드롭다운/메가메뉴/2차 플라이아웃 전부 포함)을 `renderTab()` 함수로 뽑아 1단/2단 두 자리에서 그대로 재사용 — 로직 중복 없이 동일한 드롭다운/hover 동작을 두 자리 모두에서 유지한다. 1단 탭 줄은 로고 줄 컨테이너(`relative` 추가) 안에 `position: absolute; bottom: 0; translate-y-1/2`로 배치해 **자기 높이의 절반이 항상 로고 줄 하단 경계선에 걸치도록**(로고 줄에 절반, 2단 탭 줄에 절반) 만들고, `tier1OffsetPx`로 추가 조정한다. 1단 오버레이 바깥 wrapper는 `pointer-events-none`(로고/계정 영역 클릭을 가리지 않게), 안쪽 탭 그룹만 `pointer-events-auto`.
- **`/admin/navigation/settings`**: "상단 탭 디자인" 섹션에 "1단 겹침 정도(px)" 전역 입력 + 탭마다 "표시 위치"(1단/2단) select 추가.
- **검증**: `npx tsc --noEmit`/`npm run lint`/`npm run build` 전부 0 errors. 로컬 dev + Browser pane 실측: 온라인 도슨트/스튜디오/마이 페이지를 1단으로 저장 → 홈페이지에서 실제로 로고 줄 경계선에 걸쳐 뜨는 것을 스크린샷으로 확인(사용자 레퍼런스 이미지와 배치 일치), 2단 `<nav>`에는 About Silo/사일로 상점/살롱데상만 남는 것 확인, "글쓰기" 버튼도 마이 페이지가 옮겨간 1단 쪽에 함께 따라가는 것 확인. **검증 중 겪은 삽질**: 코드 저장 직후 홈페이지 nav가 완전히 빈 채로 뜨는 현상을 봐서 처음엔 내 코드가 깨진 줄 알고 `git stash`까지 해서 원인 조사했으나, 재현이 안 되고(서버 재시작 후 정상) dev 서버/Turbopack HMR 쪽 일시적 현상으로 결론 — 실제 코드 결함 아님(재현 스크립트로 반복 확인).
- **변경 파일**: `src/components/Navbar.tsx`, `src/app/admin/navigation/settings/page.tsx`.

## 2026-08-15 (HOTFIX-116 — `next build` 타입 에러로 EPIC-113부터 dev.silostore.net 배포가 전부 실패하던 문제 수정)
- **배경**: 사용자가 "https://dev.silostore.net/about-silo 에서 (3D 우주가) 안 보인다"고 신고. 실제로 열어보니 `x-matched-path: /[...slug]`(구 catch-all 위젯 페이지)가 뜨고 있었다 — EPIC-113(신규 정적 라우트 `src/app/about-silo/page.tsx`)이 반영 안 된 상태.
- **근본 원인**: `npm run build`(`next build`)가 `src/components/craft/shared/SettingsSidebar.tsx:35`에서 `Type '{}' is not assignable to type 'never'`로 실패하고 있었다 — `<selected.Settings />`의 `Settings` 타입이 이 저장소에 등록된 모든 Craft 블록의 `related.settings` 컴포넌트 타입을 전부 합쳐 추론되는데, 블록 종류가 계속 늘면서(EPIC-102~115) 그 교집합이 `never`로 붕괴한 것. **`npx tsc --noEmit`은 이 에러를 재현하지 않아**(별도 타입체크 경로/파일 분석 순서 차이로 추정) EPIC-113/114/115 세 번의 CHANGELOG 항목 모두 "무관한 기존 에러, 영향 없음"으로 기록된 채 실제 `next build` 성공 여부는 한 번도 재확인되지 않았다 — Vercel은 `next build`를 쓰므로, **EPIC-113이 머지된 시점부터 develop/main에 대한 모든 배포가 조용히 실패**하고 있었고(빌드 실패 시 Vercel은 마지막 성공 배포를 계속 서빙), dev.silostore.net은 그 이전 커밋에 멈춰 있었던 것으로 추정된다.
- **수정**: `node.related?.settings`를 `ComponentType<Record<string, never>> | undefined`로 명시 캐스팅해 "props 없는 컴포넌트"로 타입을 고정 — 여러 블록의 서로 다른 `Settings` 시그니처를 전부 합치는 추론 자체를 피한다.
- **검증**: `npm run build` 로컬에서 **처음으로 실제 성공**(수정 전 재현 확인 후 수정 → 재빌드 성공), 빌드 산출물에 `/about-silo`가 정적 라우트(`○ /about-silo`)로 정확히 잡히는 것 확인(수정 전엔 `/[...slug]`에 흡수됨). `npx tsc --noEmit` 0 errors.
- **다음 확인 필요(레포 밖, agent가 직접 볼 수 없음)**: 이 커밋이 `main`/`develop`에 fast-forward된 뒤 Vercel이 실제로 새 배포를 성공시키는지, 성공 후 `https://dev.silostore.net/about-silo`가 3D 우주로 바뀌는지 — 사용자가 직접 확인 필요.
- **변경 파일**: `src/components/craft/shared/SettingsSidebar.tsx`.

## 2026-08-14 (EPIC-115 — 어린 왕자 감성 렌더링 오버홀 + Leva 설정 패널)
- **배경**: EPIC-114 결과물이 "기계적이고 찰흙 같다"는 재피드백 — 스케치/원화 레퍼런스를 다시 기준으로 (1) 행성 질감을 절차적 수채화 텍스처로, (2) 지저분한 사각형 썸네일을 기본 숨김 처리, (3) 줌아웃 한계를 5000까지 해제, (4) 궤도 위성/캐릭터 모션/내핵 오브젝트 디테일을 스케치대로 복원, (5) 관리자가 씬 전체를 실시간 제어하는 Leva 설정 패널을 추가.
- **수채화 텍스처(`useWatercolorTexture`, 신규)**: 외부 텍스처 에셋이 없어 캔버스에 반투명 블롭(붓자국)을 90겹 + 픽셀 노이즈(종이 질감)를 절차적으로 그려 `CanvasTexture`로 굽고 `MeshToonMaterial.map`으로 적용 — Leva 컬러 피커로 베이스 컬러를 바꾸면 다시 굽는다. 툰 그라디언트 밴드도 한 번 더 완화(110/190/245 → 120/195/245).
  - **실측 중 잡은 lint 에러**: 처음엔 선형합동 PRNG(`let seed` 클로저 재대입)로 블롭 좌표를 뽑았는데, `useMemo` 콜백(렌더 단계) 안에서 클로저 변수를 재대입하는 패턴을 React Compiler 규칙(`react-hooks/immutability`)이 에러로 막았다 — 인덱스만의 순수 함수인 해시 기반 의사난수(`Math.sin(i*a+b)*c` 소수부)로 교체해 상태 없이 해결.
- **오브젝트 클린업**: 궤도 사진(`SurfaceImage`, 풀 텍스처 사각형)을 Leva `showThumbnails` 토글로 감싸 **기본값 false**로 숨기고, 대신 텍스처를 아예 로드하지 않는 가벼운 `AboutSiloMarker`(작은 구, Suspense/CORS 리스크 자체가 없음)를 기본 표시 — 클릭하면 기존과 동일하게 요약 패널이 뜬다.
- **줌아웃 해제**: `CameraControls.maxDistance` 120 → **5000**(지시문 "Infinity 혹은 2000 이상").
- **씬 디테일 복원(스케치 기준)**:
  - **궤도 위성 복구**: SILO 궤도(`AboutSiloMarker`/`SurfaceImage`, 기존 게시글 데이터)에 더해, 유저 행성 궤도에 "About Me" 상징 위성 5종(찜한 아이템/나의 컬렉션/타임라인/뱃지/방문자 — `/mypage` 하위 탭 라벨과 맞춤, 로그인 없이도 보여줄 수 있는 장식용 마커)을 `AboutMeSatellites`로 신설.
  - **캐릭터 모션**: `SittingFigure`의 머리/다리를 개별 그룹으로 분리해 `useFrame`에서 sin 곡선 기반 절차적 모션(고개 두리번거림 + 다리 까딱임, 개체마다 seed로 위상을 어긋나게 해 "여러 모션이 랜덤하게" 보이는 효과)을 걸었다. **`AnimationMixer`를 쓰지 않은 이유**: 지시문도 "임시 GLTF 또는 뼈대 모델"이라 전제했듯, 실제 리깅된 GLTF/애니메이션 클립 파일이 이 저장소에 없어 `AnimationMixer`가 재생할 클립 자체가 없다 — 클립 없이 같은 결과(시간에 따른 트랜스폼 보간)를 코드로 직접 구현.
  - **내핵 오브젝트**: 살롱데상 오브제를 "다이얼 전화기 + 널부러진 폴라로이드 사진들"로 통합(EPIC-114 때는 폴라로이드가 카메라 쪽에 있었음 — 지시문 3-3 반영해 이동), 4개 라우팅(`/silo-store`/`/online-docent`/`/salon-des-cent`/`/studio`)은 EPIC-114와 동일하게 유지(이미 정확히 일치 확인).
- **Leva 설정 패널(신규 `leva` 설치)**: Canvas 바깥 최상위 컴포넌트에서 `useControls`로 선언 — 패널은 import 시점에 자동으로 뜬다(`<Leva/>` 명시 마운트 불필요). **배경**(유튜브/프리셋 토글 + 프리셋 3종), **행성**(베이스 컬러 피커, 썸네일 노출 토글, 공전/자전 속도 슬라이더 — SILO 자전/유저 행성 자전/궤도 그룹 공전/About Me 위성 공전에 공용 적용), **캐릭터**(Type A/B/C — 실제 GLTF가 없어 비율·색상 변형으로 대체), **내핵 오브젝트**(보물상자 3종/카메라 3종 색상·부속 변형 드롭다운).
- **프리셋 배경(`PresetBackground.tsx`, 신규)**: 크림 백지/딥 블루/수채화 블루 3종 — 마찬가지로 이미지 에셋이 없어 CSS 그라디언트 + 반복 `radial-gradient`(별 3겹) + 흐릿한 원(멀리 있는 행성)으로 절차적 스타필드를 그린다. `YoutubeBackground.tsx`와 동일한 레이어 규칙(`fixed`, 뷰포트 전체, `pointer-events-none`, 최하단 z-index) 공유, 배경 모드 토글로 서로 교체.
- **검증**: `npx tsc --noEmit` 0 errors(무관한 기존 `SettingsSidebar.tsx` 에러 1건은 EPIC-113부터 존재 확인됨). `npm run lint` 0 errors(위 PRNG 에러 수정 후 통과, 나머지는 기존 패턴과 동일한 class의 경고만). 로컬 dev + Browser pane 실측: Leva 패널의 모든 컨트롤 그룹(배경/행성/캐릭터/내핵 오브젝트) 렌더링 확인, 수채화 텍스처가 행성 표면에 실제로 얼룩덜룩하게 적용된 것을 스크린샷으로 확인, `showThumbnails` 체크박스를 두 번 토글해도(켬→끔) 크래시 없이 안정적으로 동작(Suspense/ErrorBoundary가 여전히 안전망), About Me 위성 5개(다이아몬드형 마커)가 유저 행성 주위에 실제로 도는 것 확인. **자동화로 확인 못한 것**: 이번에도 헤드리스 브라우저의 synthetic WheelEvent 한계로 실제 마우스 휠 dolly 체감은 미확인. Leva 패널의 프리셋 배경 전환/컬러 피커/슬라이더/드롭다운 각각을 실제로 값 변경까지 눌러보는 것은 자동화 클릭 도구가 이 페이지(연속 렌더 루프가 있는 WebGL 캔버스)에서 반복적으로 30초 타임아웃이 나 완전히 검증하지 못했다(코드 리뷰 + `showThumbnails` 토글 성공 사례로 나머지 컨트롤도 동일한 `useControls` 배선이라 정상 동작할 것으로 판단) — 실제 사람 손 클릭 테스트 권장.
- **신규/변경 파일**: `src/components/about-silo/AboutSiloUniverse.tsx`(전면 재작성), `src/components/about-silo/PresetBackground.tsx`(신규), `package.json`/`package-lock.json`(`leva` 추가).

## 2026-08-14 (EPIC-114 — 3D 우주 감성 렌더링 강화: 유튜브 배경 + 씬 재배치 + 내핵 오브젝트)
- **배경**: EPIC-113 프로토타입이 "기계적이고 차갑다"는 피드백 — 스케치(두 행성 동시 등장 + 실뜨개 연결선 + 앉은 실루엣 + 내핵 4개 오브젝트)와 어린 왕자 원화 참고 이미지를 기준으로 씬 구조를 재설계하고, 몰입형 유튜브 배경 + 후처리(Noise/Vignette)로 "종이에 그린 듯한" 톤을 더했다.
- **몰입형 유튜브 배경(`src/components/about-silo/YoutubeBackground.tsx`, 신규)**: 지정된 8개 URL(`src/lib/blockEditorCore.ts`의 기존 `extractYoutubeId`로 ID 추출·재사용, 별도 파서 안 만듦) 중 하나를 마운트 시 무작위로 골라 자동재생(`autoplay=1`)·음소거(`mute=1`)·반복(`loop=1&playlist=<id>`, 유튜브 iframe이 단일 영상을 반복하는 표준 파라미터 조합)·컨트롤 숨김으로 재생. `pointer-events-none`+`fixed`(뷰포트 전체, `z-index:-2`)로 3D 상호작용을 가리지 않는다. iframe에는 CSS `object-fit`이 적용되지 않아(iframe은 자기 콘텐츠를 스스로 레이아웃) 지시문의 "object-cover"는 유튜브 배경 영상의 표준 대체 기법(16:9로 항상 뷰포트보다 크게 그린 뒤 `min-width/min-height`를 `vw/vh`로 오버스캔) — 결과는 object-cover와 시각적으로 동일. 무작위 선택은 hydration mismatch를 피하려 `useEffect` 안에서만 수행(마운트 전엔 아무것도 렌더링 안 함).
- **씬 구조 전면 재배치(`AboutSiloUniverse.tsx`)**:
  - **행성 동시 등장**: 원점 기준 궤도이던 SILO/유저 행성을 좌(`SILO_CENTER=(-3.1,0,0)`, 반지름 2.1)/우(`USER_CENTER=(3.3,-0.4,-0.6)`, 반지름 1.05, "My Page" 라벨)로 고정 배치하고 카메라 홈 위치/화각을 두 행성이 동시에 프레임에 들어오도록 재조정 — 유저 행성은 완전 정지 대신 아주 느린 제자리 부양(sin bobbing)만 남김.
  - **연결선(실뜨개 끈)**: `THREE.CatmullRomCurve3`(4개 제어점, 중간 2개를 위/아래로 어긋나게 배치해 직선이 아닌 완만한 S자)+`TubeGeometry`로 두 행성 표면 사이를 잇는 얇은 크림색 관 — 전체를 sin으로 아주 미세하게 흔들어 "우주를 유영하는" 느낌을 더함.
  - **캐릭터 실루엣**: 원기둥(몸)+구(머리)+목도리(평면)로 구성한 임시 `SittingFigure` 컴포넌트를 SILO/유저 행성 각 표면(북쪽 가장자리)에 하나씩 배치.
  - **거리 기준 재정의**: 두 행성이 원점이 아니게 되면서, "행성 내부로 들어갔는지" 판정 기준을 세계 원점이 아니라 **SILO 행성 중심까지의 거리**로 변경(연결선/유저 행성은 이 판정과 무관).
- **줌 한계 해제 + 내핵 오브젝트**:
  - `CameraControls`의 `maxDistance`를 9.5 → **120**으로 확대(지시문 "100 이상").
  - 4개 카테고리 상징 형상(`CoreCategoryShape`, 박스/실린더/토러스 등 기본 지오메트리 조합) 신설 — 사일로상점(보물상자: 몸체+살짝 열린 뚜껑+걸쇠 고리), 온라인 도슨트(밀로의 비너스: 팔 없는 원통형 몸+머리, 대리석 톤), 살롱데상(로터리 다이얼 전화기: 박스 몸체+다이얼 판+토러스 링+수화기), 스튜디오(빈티지 필름 카메라: 몸체+렌즈 원통+흩어진 폴라로이드 사각형 3장). 라벨은 드레이(drei)의 `<Text>`(troika-three-text, drei의 기존 의존성이라 신규 설치 불필요)로 3D 공간에 직접 그림.
  - **실측 중 발견·수정한 구조적 버그**: (1) 내핵 오브젝트가 SILO 중심 반경 0.9에 배치돼 있는데, 행성 셸(반지름 2.1)이 완전 불투명 솔리드 구라 카메라가 셸 안쪽(반지름 2.1 이내)으로 들어가지 않으면 원천적으로 안 보임 — 실제로는 Three.js 기본 프런트페이스 컬링 덕분에 카메라가 셸 반지름 안쪽에 들어가는 순간 바깥을 향한 셸 폴리곤이 자동으로 안 보이게 컬링되어 "내부 진입 시 표면이 사라지고 내핵이 드러난다"는 요구사항이 별도 처리 없이 자연스럽게 만족됨을 확인(설계 자체는 옳았음). (2) 그런데 `CameraControls`가 마운트 시 Canvas가 준 초기 camera position을 자기 나름대로 재해석해(내부적으로 target을 원점으로 가정) 의도한 "홈 구도"와 다르게 초기화되는 것을 실측으로 확인 — 마운트 직후 `setLookAt(...,false)`로 한 번 스냅해 보정. (3) 그 보정 effect가 실제로는 `cameraControlsRef.current`가 아직 `null`인 시점에 실행돼(R3F가 부모 DOM 트리와 별도 리컨실러로 Canvas 내부를 커밋하기 때문에 타이밍이 어긋남) 아무 효과가 없던 것을 `console.log` 삽입으로 직접 확인 — `requestAnimationFrame` 재시도 루프로 ref가 실제로 준비된 프레임에만 스냅하도록 수정. 이 수정 후 카메라를 SILO 중심 안쪽(distance≈0.4)으로 임시 이동시켜 4개 내핵 오브젝트 중 실제로 2개(보물상자/다이얼 전화기)가 셸 컬링을 뚫고 정상적으로 드러나며 opacity가 페이드인하는 것을 스크린샷으로 확인 완료(나머지 2개는 같은 `CoreCategoryShape` 스위치 분기라 코드 리뷰로 동등하게 확인).
- **감성 렌더링**: `@react-three/postprocessing`+`postprocessing` 신규 설치, `<EffectComposer><Noise opacity={0.06}/><Vignette .../></EffectComposer>`로 디지털 렌더링 느낌을 완화. 툰 그라디언트 밴드 값을 낮춰(70/170/255 → 110/190/245) 대비를 부드럽게, 조명 강도를 한 단계 낮춤(directional 1.05→0.8), 행성/내핵 색상을 파스텔 어스톤으로 조정. Canvas를 `alpha:true`(투명 배경)로 바꿔 유튜브 배경이 3D 장면 사이로 비치도록 함.
- **검증**: `npx tsc --noEmit` 0 errors(무관한 기존 `SettingsSidebar.tsx` 에러 1건은 EPIC-113 때부터 존재 확인됨, 이번에도 무영향). `npm run lint` 0 errors(경고만, 기존 패턴과 동일한 class). 로컬 dev + Browser pane 실측: 홈 구도(두 행성+연결선+궤도 사진+유튜브 배경 동시 렌더링)/클릭 fly-to+요약 패널/"처음으로" 리셋 왕복/CORS 차단 이미지 자리표시자 대체(EPIC-113과 동일 매커니즘, 이번에도 재확인)/내핵 오브젝트 2종 실제 노출까지 스크린샷으로 확인, 콘솔에는 예상된 CORS 에러(ErrorBoundary가 처리) 외 신규 에러 없음(troika 폰트 관련 에러도 없음). **자동화로 여전히 확인 못한 것**: 실제 마우스 휠로 SILO 행성 내부까지 자연스럽게 줌인되는 체감(EPIC-113과 동일한 이유로 헤드리스 브라우저의 synthetic WheelEvent를 `camera-controls`가 인식하지 않음 — 이번엔 대신 `setLookAt` 직접 호출로 내핵 도달 자체는 검증했지만, 휠 dolly 조작감 자체는 사람 손 테스트 필요), maxDistance=120까지 실제로 줌아웃했을 때의 시각적 스케일감, 유튜브 배경 8종 전체가 고르게 로드되는지(1종만 실측).
- **신규/변경 파일**: `src/components/about-silo/YoutubeBackground.tsx`(신규), `src/components/about-silo/AboutSiloUniverse.tsx`(전면 재작성), `package.json`/`package-lock.json`(`@react-three/postprocessing`/`postprocessing` 추가).

## 2026-08-14 (EPIC-113 — /about-silo를 3D 우주(Universe) 프로토타입으로 개편)
- **배경**: `/about-silo`(기존엔 Page Builder 위젯으로 구성된 일반 정보 페이지)를 "어린 왕자" 감성의 3D 행성 공간으로 전면 개편 — 중심 행성 표면에 사일로의 대표 이미지들이 궤도를 돌고, 클릭하면 그 이미지로 날아가 텍스트 요약을 보여주고, 휠로 행성 내부로 들어가면 실제 플랫폼 카테고리(사일로상점/살롱데상/온라인 도슨트) 노드가 드러나는 인터랙티브 프로토타입.
- **기술 스택**: `three`/`@react-three/fiber`/`@react-three/drei`/`@types/three` 신규 설치. 지시문이 예시로 든 `framer-motion-3d`는 설치하지 않음 — `framer-motion-3d@12.x`가 `@react-three/fiber@8.2.2`를 peer로 요구해(최근 몇 년간 fiber v9로 업데이트되지 않음) `@react-three/drei@10`(fiber v9 필수)과 설치 자체가 `npm ERESOLVE`로 충돌, 지시문이 명시한 대안인 `@react-three/drei`의 `<CameraControls>`(`camera-controls` 라이브러리 래퍼)만으로 fly-to 애니메이션을 구현.
- **`src/lib/fibonacciSphere.ts`(신규)**: 피보나치 구면 알고리즘(황금각 기반 위도/경도 분산)으로 N개의 점을 구 표면에 겹침 없이 고르게 배치하는 순수 함수 — 대표 이미지 궤도 배치와 내핵 카테고리 노드 배치 양쪽에 재사용.
- **`src/components/about-silo/AboutSiloUniverse.tsx`(신규)**: `/api/boards/feed`(기존 라우트, 신규 API 없음)에서 추천→인기→최신 순으로 사진 있는 게시글을 최대 18개 수집해 구 표면에 billboard(항상 카메라를 향하는 평면)로 배치. `MeshToonMaterial`(3단계 명암 DataTexture를 코드로 직접 생성, 별도 텍스처 에셋 없음) + 따뜻한 톤의 Directional/Ambient 조명 2쌍. 이미지 클릭 시 `CameraControls.setLookAt(...,true)`로 표면 법선 방향에서 부드럽게 fly-to하고, 선택된 게시글의 본문(기존 게시글 상세 API + `htmlToExcerpt`)을 하단 패널에 요약 표시. 카메라 거리(원점 기준)를 매 프레임 ref로 추적해 React state 리렌더 없이 각 메시가 스스로 opacity를 보간(lerp)하는 방식으로 거리 기반 LOD 구현 — 멀리서는 궤도 사진이 보이고, 행성 내부(거리<1.55)로 들어가면 사진은 사라지고 3개 핵심 카테고리 노드(`/silo-store`/`/salon-des-cent`/`/online-docent`로 이동)가 페이드인. 유저 위성(작은 초록 구, 궤도 공전)에 향후 마이페이지 데이터(찜한 아이템 등)가 자라날 빈 Object3D("bone") 자리를 미리 마련. 성능 방어: `Canvas dpr={[1,1.5]}` 상한, `<BakeShadows />`, 그림자 미사용(무거운 실시간 그림자 대신 동화적 플랫 톤 유지).
- **CORS 방어(실측 중 발견)**: DB에서 가져온 이미지 중 일부(Instagram CDN 등 외부 호스팅 embed 썸네일)는 텍스처 로드 시 CORS로 차단돼 `useTexture`가 던지는 에러가 처리 안 되면 Canvas 전체가 크래시함을 실측으로 확인 — 이미지 하나마다 `Suspense`+`ErrorBoundary`(`ImageLoadErrorBoundary`, 신규)로 감싸 실패한 이미지 하나만 조용히 자리표시자(작은 구)로 대체하고 나머지 우주는 영향받지 않도록 방어.
- **`src/app/about-silo/page.tsx`(신규 정적 라우트)**: 기존엔 `/about-silo`가 catch-all(`src/app/[...slug]/page.tsx`)이 담당하는 slug였다 — 이 정적 라우트 파일을 두면 Next.js가 catch-all보다 먼저 매칭해(홈페이지/스튜디오와 동일한 EPIC-099 원칙) Page Builder 위젯 시스템을 완전히 벗어난다.
- **검증**: `npx tsc --noEmit` 0 errors(무관한 기존 `SettingsSidebar.tsx` 에러 1건은 이 EPIC 이전부터 존재 확인, `git stash`로 재확인). `npm run lint` 0 errors(경고만, 기존 패턴과 동일한 class). 로컬 dev + Browser pane 실측: 툰 셰이딩 행성/궤도 이미지/따뜻한 조명 렌더링 확인, 이미지 클릭 → fly-to → 하단 요약 패널(실제 게시글 제목/본문 발췌/"자세히 보기" 링크) → "처음으로" 리셋까지 왕복 확인, CORS 차단 이미지 1건이 크래시 없이 자리표시자로 대체되는 것 확인. **자동화로 확인 못한 것**: 실제 마우스 휠 dolly(행성 내부 진입 → 카테고리 노드 페이드인) — `camera-controls` 라이브러리가 헤드리스 브라우저의 synthetic `WheelEvent`(`dispatchEvent`)에 반응하지 않아(신뢰된 사용자 입력만 인식하는 것으로 추정) 자동화로 재현 불가, 코드 로직(거리 임계값 기반 opacity 보간)은 리뷰로 확인함 — 실제 사람 손으로 마우스 휠 스크롤 테스트 필요. 프로토타입 단계라 대표 이미지 10~20개 중 사진이 있는 게시글이 실제로 몇 개인지(현재 DB 데이터 상태)는 확인 안 함.
- **신규/변경 파일**: `src/lib/fibonacciSphere.ts`(신규), `src/components/about-silo/AboutSiloUniverse.tsx`(신규), `src/app/about-silo/page.tsx`(신규), `package.json`/`package-lock.json`(three 등 4개 패키지 추가).

## 2026-08-14 (EPIC-112 — [Stage 2] 사이트 전체 에디터 툴바를 SunEditor Classic 배열 문법으로 재배치)
- **배경**: 지시문은 "Tiptap(JSON 블록 구조) 기반 저장 + SunEditor `buttonList`와 100% 동일한 툴바"를 요청했지만, 코드 확인 결과 데이터 파이프라인(`posts.body_json`을 정본으로 저장, `generateHTML`+`sanitizeHtml`로 구형 HTML 폴백 렌더링)은 이미 EPIC-053.1부터 구축돼 있었다. 진짜 작업 대상은 툴바뿐이었는데, 지시문이 예시로 준 6버튼 배열(`undo/redo | bold/italic/underline | list/link/image`)을 문자 그대로 적용하면 EPIC-079~096에 걸쳐 쌓인 기존 고급 기능(표/갤러리/임베드/색상/폰트/출처 등)이 화면에서 사라지는 큰 폭의 기능 축소가 되므로, 진행 전 사용자에게 범위를 확인 — **"6버튼 배열은 SunEditor의 그룹핑+구분선(`|`) 문법을 보여주는 구조적 예시일 뿐, 기존 기능은 절대 삭제하지 말고 전부 그 문법대로 논리적으로 재배치하라"**는 답변을 받아 진행.
- **`src/components/editor/BlockEditor.tsx`의 `Toolbar` 컴포넌트 전면 리팩토링**(기능 로직은 전혀 안 건드림, 렌더링 구조만 재배치):
  - 이모지/텍스트 글리프(`B`/`I`/`U`/`🔗`/`🖼`/`▶` 등)를 전부 `lucide-react` 아이콘(신규 설치)으로 교체.
  - SunEditor `buttonList` 문법(`[...]`로 묶고 `"|"`로 그룹 구분)을 그대로 참고해 논리적 그룹 11개로 재배치: `[undo,redo]` | `[bold,italic,underline,strike]` | `[h1,h2,h3]` | `[bulletList,orderedList,taskList,blockquote,horizontalRule]` | `[alignLeft,center,right]` | `[textColor칩+커스텀+초기화, fontFamily]` | `[link]` | `[image,gallery,embed,linkCard,table,sourceAttribution]` | (spacer) | `[preview,autosave]`. 재사용 가능한 `ToolbarGroup`/`ToolbarDivider` 헬퍼 신설.
  - **신규 기능**: undo/redo 버튼이 이전엔 아예 없었다(브라우저 기본 Ctrl+Z에만 의존) — StarterKit의 History 확장을 툴바 버튼으로 노출(`editor.chain().undo()/redo()`, `editor.can().undo()/redo()`로 비활성 상태 표시). 이 외 삭제/축소된 기능은 없음.
  - `npm install lucide-react` 신규 의존성 추가.
- **검증**: `npx tsc --noEmit`/`npm run lint` 0 errors(BlockEditor.tsx 관련 경고 없음). 클린 dev 서버 기동 확인(빌드/런타임 에러 없음). **다음에 확인 필요(관리자/회원 로그인 세션)**: `/write`·`/boards/[slug]/write`·게시글 수정 화면에서 실제로 새 아이콘 툴바가 그룹/구분선대로 렌더링되는지, undo/redo 버튼 클릭 동작, 기존처럼 표/갤러리/임베드/색상/폰트/출처 삽입이 전부 정상 동작하는지 육안 클릭 검증(이 세션은 로그인 세션이 없어 `/write` 접근 시 홈으로 리다이렉트됨을 확인만 함).
- **변경 파일**: `src/components/editor/BlockEditor.tsx`, `package.json`/`package-lock.json`(lucide-react 추가).

## 2026-08-14 (EPIC-111 — 자유 배치 콘텐츠 사라짐/비율 미적용 근본 원인 수정 + 픽셀 직접 입력)
- **배경**: 사용자가 (1) 슬라이드쇼 위에 슬라이드쇼를 자유 배치로 겹쳤는데 z-index를 높여도 아무것도 안 보이고 이미지를 업로드해도 안 보인다는 것, (2) 자유 배치 리사이즈에서 "높이(%)"가 이상하게 동작한다는 것, (3) `ImageBlock`의 "비율 선택" 드롭다운을 바꿔도 프리뷰가 전혀 안 바뀐다는 것을 스크린샷 2장으로 재현 — (1)(2)(3) 모두 같은 근본 원인 하나로 귀결됨을 코드 추적으로 확인.
- **근본 원인 — `EditableBlockFrame`(`src/components/craft/home/editable.tsx`)의 높이 체인 단절**: 편집 모드(Craft 에디터 안, 공개 페이지는 이 컴포넌트를 아예 안 거쳐 무관)에서 이 컴포넌트가 렌더링하는 두 wrapper `<div>`에 `h-full`이 없었다. 자유 배치로 절대 위치+%높이를 가진 블록(예: 슬라이드쇼 위 슬라이드쇼)의 실제 콘텐츠는 `EditableBlockFrame` → `RevealWrapper` → 실제 콘텐츠 순으로 내려가는데, 그 사이의 `EditableBlockFrame` wrapper에서 `height:100%` 체인이 끊겨 안쪽 콘텐츠가 사실상 0 높이로 무너져 있었다(바깥 wrapper 자체의 % 크기는 정상이라 선택 핸들/테두리는 정상 위치에 보이는데 내용물만 안 보이는 증상과 정확히 일치). 리사이즈 핸들의 `containerRect()`가 이 깨진 크기를 기준으로 %→px 환산을 하고 있어 "높이(%) 이상함" 증상도 같은 원인. `h-full`은 조상에 실제 높이가 없을 때는 `auto`와 동일하게 동작하므로 자유 배치가 아닌 일반 블록에는 영향이 없다.
- **`ImageBlock`의 별도 버그(비율 선택 무반응)**: `body` div가 `position.enabled` 여부와 무관하게 항상 `h-full`을 갖고 있었는데, `height:100%`가 명시돼 있으면 CSS `aspect-ratio`는 (그 100%가 조상 미확정으로 사실상 auto로 계산되더라도) 무시된다 — 자유 배치가 꺼져 있을 때만 `h-full`을 빼도록 수정. 추가로 `EditableResponsiveImage`/`EditableImage`의 실제 `<img>`가 항상 `object-cover`로 하드코딩돼 있어 `ImageBlock`의 "채움 방식"(cover/contain) 설정이 애초에 실제 `<img>`에 전달되지 못하고 있었다(엉뚱하게 div에 object-fit 클래스를 얹고 있었는데, object-fit은 div에 아무 효과가 없음) — `fit` prop을 신설해 실제 `<img>`까지 전달.
- **"가로세로 비율 직접 입력" → "픽셀 직접 입력"(사용자 지시)**: 자유 텍스트("4/3", "16:9" 등)를 없애고 너비(px)/높이(px) 숫자 입력 2개로 교체 — 내부 저장 형식(`aspectRatio: "너비/높이"` 문자열)은 그대로라 기존 프리셋 드롭다운과 호환된다. `splitAspectRatio()`(`src/lib/useFreePosition.ts`)로 기존 저장값을 두 입력창에 되비쳐 보여준다.
- **검증**: `npx tsc --noEmit`/`npm run lint` 0 errors. 클린 dev 서버+실제 관리자 세션에서: (1) 슬라이드쇼 위에 자유 배치로 겹친 다른 슬라이드쇼의 실제 이미지가 이제 화면에 보이는 것을 스크린샷으로 확인(수정 전엔 빈 배경만 보임), (2) 비율 선택 드롭다운을 16:9→9:16으로 바꾸자 실제 렌더링 박스의 가로세로 비율이 `0.563`(=9/16)로 정확히 바뀌는 것을 `getBoundingClientRect()`로 확인, (3) 픽셀 직접 입력(너비=9, 높이=4)이 `aspect-ratio: 9 / 4`로 정확히 반영되는 것 확인, (4) 채움 방식을 cover→contain으로 바꾸자 실제 `<img>`의 class가 `object-cover`→`object-contain`으로 바뀌는 것 확인. 테스트 중 변경한 값은 저장하지 않아(에디터의 "저장" 버튼 미클릭) DB에는 영향 없음.
- **변경 파일**: `src/components/craft/home/editable.tsx`, `src/components/craft/primitives/ImageBlock.tsx`, `src/lib/useFreePosition.ts`.

## 2026-08-14 (EPIC-110 — 자유 배치 리사이즈 UX 개선 + 슬라이드쇼/로고/상단 탭 섹션 높이 설정)
- **배경**: 사용자가 스크린샷 2장으로 (1) 자유 배치 이미지를 클릭해도 선택 표시(모서리 점)가 안 뜨고 리사이즈가 불편하다는 점, (2) 설정 패널에서 바꾼 값이 캔버스 프리뷰에 반영 안 되는 것 같다는 점을 지적, (3) "홈페이지 설정 관리"의 슬라이드쇼/메인 로고/상단 탭 섹션 높이를 조절할 수 있게 해달라고 요청.
- **`FreePositionHandles` 전면 개편**: 기존엔 이동(✥)/리사이즈(우하단 점 1개) 버튼 2개가 자유 배치만 켜지면 항상 떠 있었다 — (1) `useNode((node) => node.events.selected)`로 **실제로 클릭해 선택한 블록일 때만** 핸들이 보이도록 변경(여러 자유배치 블록이 화면에 있어도 지금 만지는 것만 표시), (2) 모서리 점을 4개(nw/ne/sw/se)로 늘려 어느 방향에서든 반대쪽 변을 고정한 채 리사이즈 가능, (3) `pointer-events-none` 파란 테두리 박스를 추가해 "선택됨" 상태가 시각적으로 분명하게(더블클릭 편집 등 기존 인라인 동작은 계속 안 가림).
- **프리뷰 미반영 이슈 조사**: 실제 로컬 dev의 홈페이지 Craft 에디터에서 자유배치 이미지 블록을 선택해 오버레이 캡션 텍스트 입력/좌표 값을 직접 확인한 결과, 설정 패널 값과 캔버스 DOM 스타일이 정확히 일치하고 실시간으로 반영됨을 확인 — 코드 결함은 재현되지 않았다(이전 세션의 스테일 HMR/캐시로 추정). 다만 `FreePositionSettingsSection`의 X/Y/너비/높이/z-index 숫자 입력이 `Number(e.target.value) || 0`이라 입력칸을 지우는 순간(빈 문자열) 값이 0으로 튀는 부수적 버그를 발견해 `valueAsNumber` 기반으로 수정(빈 값일 때는 아무 것도 반영하지 않음).
- **섹션 높이 설정 3종 신설**:
  - `src/lib/heroSlideshow.ts`의 `HeroSlideshowConfig`에 `heightVh: number | null` 추가(null이면 기존 기본값 모바일 60vh/PC 70vh 그대로) — `HeroSlideshow.tsx`가 inline style로 override, `src/app/page.tsx`/`HeroSlideshowWidgetBlock.tsx` 양쪽 호출부에 전달, "홈페이지 설정 관리"의 슬라이드쇼 섹션과 Craft `HeroSlideshowWidgetBlock` 설정 패널(수동 모드) 양쪽에 입력 필드 추가.
  - `MainLogoValue.rowHeightPx`/`TopTabStyleValue.rowHeightPx`(둘 다 `number | null`) 신설 — `Navbar.tsx`의 로고 줄(`p-4` div)/상단 탭 줄(`nav`)에 `minHeight` 인라인 스타일로 적용(null이면 기존처럼 내용물 기준 자동). "홈페이지 설정 관리"의 "메인 로고"/"상단 탭 디자인" 섹션에 각각 입력 필드 추가.
- **검증**: `npx tsc --noEmit`/`npm run lint` 0 errors(기존 경고만 남음). 클린 dev 서버+실제 관리자 세션(로그인 유지된 세션)에서: (1) 자유배치 이미지 클릭 시에만 핸들 4개+테두리가 뜨고 선택 해제 시 사라지는 것, (2) 4개 모서리 각각 드래그 시 반대쪽 변이 고정된 채 정확한 %로 리사이즈되는 것(픽셀 좌표 계산으로 검증), (3) "로고 섹션 높이"/"상단 탭 섹션 높이"를 각각 140px/80px로 저장 후 공개 홈페이지 DOM에서 `min-height` 스타일과 실제 렌더 높이가 반영되는 것을 확인 — 확인 후 두 값 모두 자동(빈 값)으로 원상복구해 저장. 슬라이드쇼 `heightVh`는 코드 리뷰로 배선 확인(관리자 세션에서 저장 왕복은 로고/탭과 동일 패턴이라 생략).
- **변경 파일**: `src/lib/{useFreePosition.ts는 미변경, heroSlideshow.ts}`, `src/components/craft/shared/{FreePositionHandles,FreePositionSettingsSection}.tsx`, `src/components/HeroSlideshow.tsx`, `src/components/craft/primitives/HeroSlideshowWidgetBlock.tsx`, `src/app/page.tsx`, `src/components/Navbar.tsx`, `src/app/admin/navigation/settings/page.tsx`.

## 2026-08-14 (EPIC-109 — 자유 배치 이미지 리사이즈에 비율 유지/직접입력/프리셋 추가)
- **배경**: EPIC-108에서 이미지 위에 이미지를 자유롭게 겹쳐 놓는 기능은 됐는데, 겹쳐진 이미지의 크기 조절 시 가로세로 비율을 유지할 방법이 없었다("비율유지/유지안함 기능", "가로세로 비율을 직접 입력", "드랍다운에서 비율을 여러가지 샘플에서 선택").
- **`src/lib/useFreePosition.ts`**: `parseAspectRatio("4/3" | "16:9" | "1.5")` 파서와 `ASPECT_RATIO_PRESETS`(자유/1:1/4:3/3:4/3:2/2:3/16:9/9:16/21:9/5:4) 목록 추가.
- **`FreePositionHandles`**: `lockedAspectRatio?: number` prop 신설 — 켜져 있으면 리사이즈 핸들 드래그 시 가로 이동량으로 너비(%)를 정하고, 세로는 **컨테이너의 실제 픽셀 너비/높이로 환산**해 그 비율에 맞는 높이(%)를 자동 계산한다(컨테이너가 정사각형이 아니면 같은 %라도 실제 픽셀 비율이 달라지므로, %만으로는 비율을 못 맞춘다 — 픽셀 환산이 핵심). 부수적으로 `setPointerCapture` 실패(드문 케이스)를 `try/catch`로 방어해 드래그 시작 자체가 막히지 않게 함.
- **`ImageBlock`**: `lockAspectRatio: boolean` prop 신설(기존 `aspectRatio` 텍스트 필드를 그대로 재사용해 잠글 목표 비율로 삼음). 설정 패널에 (1) 비율 프리셋 드롭다운, (2) 비율 직접 입력 텍스트 필드(둘 다 같은 `aspectRatio` 값을 공유), (3) "자유 배치로 크기 조절할 때 비율 유지" 체크박스를 추가.
- **검증**: `npx tsc --noEmit`/`npm run lint`/`npm run build` 전부 통과(0 errors). 클린 서버+새 탭에서 1:1 비율 잠금 상태로 리사이즈 핸들을 드래그해, 너비(%)·높이(%)가 서로 다른데도 실제 픽셀 환산 시 정확히 1:1(예: 274.9px × 274.8px)로 유지되는 것을 확인, 콘솔 에러 없음(이전 `setPointerCapture` NotFoundError는 합성 이벤트로 반복 테스트할 때만 발생하던 테스트 산출물이었음을 클린 세션으로 재확인 + 그래도 try/catch로 방어).
- **변경 파일**: `src/lib/useFreePosition.ts`, `src/components/craft/shared/FreePositionHandles.tsx`, `src/components/craft/primitives/ImageBlock.tsx`.

## 2026-08-13 (EPIC-108 — 자유 배치(콜라주) + 홈페이지 슬라이드쇼 위젯 관리자 연동)
- **배경**: 사용자가 Kinfolk 실제 화면(1번 블록)을 다시 보여주며 "화면을 꽉 채우는 슬라이드쇼만 있는 게 아니라, 이미지 안에 이미지를 넣는 콜라주처럼 컨테이너/텍스트/이미지/버튼/영상/슬라이드쇼/게시판을 자유롭게 겹쳐 드래그로 놓을 수 있게" 요청, 그리고 `HeroSlideshowWidgetBlock`(EPIC-107)에 이미 존재하는 관리자 "홈페이지 설정 관리" 슬라이드쇼와 연동하는 옵션이 없다고 지적.
- **자유 배치(콜라주) — 신규 공용 인프라**:
  - `src/lib/useFreePosition.ts` — `FreePosition = {enabled, xPct, yPct, widthPct, heightPct, zIndex}`(부모 기준 %, 컨테이너 쿼리 반응형과 호환). `enabled=false`면 항상 `position:relative`만(핸들 앵커용), `true`면 `position:absolute`+%좌표로 부모 위에 뜬다.
  - `src/components/craft/shared/FreePositionHandles.tsx` — 블록 전체를 덮는 오버레이가 아니라 모서리의 작은 이동(✥)/리사이즈 핸들 2개만 렌더링해, 더블클릭 텍스트 편집·이미지 클릭 업로드 등 기존 인라인 편집을 가리지 않는다. `pointerdown`+`setPointerCapture`로 드래그, 부모(`offsetParent`) 크기 기준 %로 환산해 `onChange`.
  - `src/components/craft/shared/FreePositionSettingsSection.tsx` — 토글+정확한 수치 입력(X/Y/너비/높이/z-index) 설정 패널 섹션.
  - **적용 대상(사용자가 나열한 7종 그대로)**: `ContainerBlock`(다른 컨테이너 위에도 자유배치 가능 — 컨테이너 안 컨테이너 중첩도 됨), `TextBlock`, `ImageBlock`, `ButtonBlock`, `VideoBlock`, `SlideshowBlock`, `BoardEmbedBlock`. `ContainerBlock`/`RootContainer`는 항상 `position:relative`가 기본이라 그 안의 자유배치 자식들이 정확히 그 컨테이너 기준으로 겹친다(다른 곳으로 새지 않음).
  - **검증**: 로컬 dev에서 임시 진단 라우트로 배경 이미지 위에 오버레이 이미지(20%,20%,30%×30%)와 텍스트를 자유배치로 겹쳐 실제 DOM 좌표가 배경 이미지 영역 안에 정확히 들어오는 것을 확인(image over image 성공), 편집 모드에서 이동/리사이즈 핸들이 렌더링되고 실제 pointer 드래그 시퀀스로 좌표(%)가 정확히 갱신되는 것까지 확인(이동 20%→37.96%, 리사이즈 30%→47.96%).
- **`HeroSlideshowWidgetBlock` 관리자 연동(신규)**: `syncWithSiteSettings`(불리언) + `syncDevice`(pc/mobile) 프롭 추가. 켜면 이 블록 자신의 슬라이드/여백/objectFit을 전부 무시하고 `site_settings.hero_slideshow`(관리자 "홈페이지 설정 관리"가 쓰는 바로 그 설정)를 실시간으로 읽어와 그대로 렌더링 — 즉 관리자 화면에서 슬라이드를 바꾸면 이 위젯이 박힌 모든 Craft 페이지가 자동으로 함께 바뀐다. 켜져 있을 때는 설정 패널에서 로컬 슬라이드 편집 UI를 숨기고 "홈페이지 설정 관리에서 수정하세요" 링크만 보여준다. **검증**: 임시 진단 라우트에서 `syncWithSiteSettings` 켠 채로 렌더링해, 실제 라이브 `public-assets/slides/*` 버킷의 관리자 업로드 이미지("Vogue Vintage Jewelry" 등)가 그대로 불러와지는 것을 확인.
- **전체 검증**: `npx tsc --noEmit`/`npm run lint`/`npm run build` 전부 통과(0 errors, 새 경고 없음).
- **변경/신규 파일**: `src/lib/useFreePosition.ts`(신규), `src/components/craft/shared/{FreePositionHandles,FreePositionSettingsSection}.tsx`(신규), `src/components/craft/primitives/{ContainerBlock,TextBlock,ImageBlock,ButtonBlock,VideoBlock,SlideshowBlock,BoardEmbedBlock,HeroSlideshowWidgetBlock}.tsx`, `src/components/craft/home/RootContainer.tsx`.

## 2026-08-13 (EPIC-107 — 홈페이지 슬라이드쇼를 Craft 위젯으로 전환, EPIC-101~107 로드맵 완결)
- **배경**: 사용자가 "홈페이지 설정 관리"에 있는 기존 히어로 슬라이드쇼(`HeroSlideshow.tsx`, 크로스페이드+objectFit=contain 시 랜덤 여백 배경+4방향 여백px+자동전환 — "그거 만드느라 시간 오래 쏟았는데")를 새로 만들지 않고 그대로 Craft 캔버스 어디든 넣을 수 있는 위젯으로 만들어달라고 요청. EPIC-099 Phase 3가 이미 4개 공용 Craft 블록을 Native 위젯으로 노출한 선례는 있지만, 거꾸로 "기존 Native 기능을 Craft 블록으로 감싸는" 것은 이번이 처음.
- **`HeroSlideshowWidgetBlock`(신규 원자 블록)**: `HeroSlideshow.tsx`를 직접 import해 그대로 렌더링(`device="both"` — Native Page Builder의 기존 "hero" 위젯과 동일한 단순화, PC/모바일 분리가 필요하면 기존 사이트 설정 기능을 계속 쓰면 됨). 설정 패널에 슬라이드 목록(이미지 업로드+제목+설명, 추가/삭제), 자동 전환 간격, 이미지 채움 방식(cover/contain), 4방향 여백(px), (`contain` 선택 시만) 여백 배경 이미지 목록(최대 10개, 추가/삭제) — 기존 관리자 폼이 갖고 있던 옵션을 거의 그대로 Craft 설정 패널로 옮겼다(여백 배경 압축 품질만 범위 밖 — 업로드 시점 최적화라 핵심 기능이 아님).
- **검증**: `npx tsc --noEmit`/`npm run lint`/`npm run build` 전부 통과(0 errors). 로컬 dev에서 임시 진단 라우트(`Editor`+`Frame`으로 이 블록 하나만 직접 렌더링, 확인 후 삭제)로 실제 `HeroSlideshow` 컴포넌트와 완전히 동일한 DOM 구조(`h-[60vh] sm:h-[70vh]` 등 원본 클래스 그대로)가 나오는 것을 확인 — 새로 만든 게 아니라 기존 컴포넌트를 그대로 감싼 것임을 증명. 콘솔에 새 에러 없음.
- **EPIC-101~107 로드맵 완결**: 이미지 버그 수정 → 프리폼 에디터 코어 → Kinfolk 16블록 → 헤더 스크롤 모션 → Footer Craft 전환 → 게시판 카테고리 필터 → 슬라이드쇼 위젯화까지, 최초 요청서의 전 항목이 이번 세션에 순차 완료됐다.
- **변경/신규 파일**: `src/components/craft/primitives/HeroSlideshowWidgetBlock.tsx`(신규)+`index.ts`.

## 2026-08-13 (EPIC-106 — 게시판 연동 블록 카테고리 필터)
- **배경**: EPIC-102/103의 `BoardEmbedBlock`/`TimelineEmbedBlock`은 게시판 하나를 통째로 가져올 뿐, 그 안에서 카테고리(주제/연대 등 `posts.category` — 게시판 자체의 라우팅 슬러그인 `board.category`와는 다른, 게시글 하나하나의 자유 텍스트 분류)로 좁혀볼 수 없었다. `GET /api/boards/[board_slug]/posts`에 이미 `tag`/`year` 필터가 있던 것과 같은 자리에 `category` 필터를 추가.
- **`GET .../posts` 라우트**: `categoryFilter`(쿼리 파라미터 `category`) 추가 — `tagFilter`/`yearFilter`와 동일한 위치·패턴으로 적용. 응답에 `availableCategories`(해당 게시판에 실제 존재하는 카테고리 값 목록, 페이지네이션 전 전체 기준이라 count와 무관하게 정확) 필드 신설.
- **`src/lib/useBoardPosts.ts`**: 4번째 인자로 `category: string | null`(기본 `null`, 하위 호환 — 기존 3개 호출부 `DbFeedModules.tsx`는 무변경) 추가, 캐시 키에도 포함. 반환값에 `availableCategories` 추가.
- **`BoardEmbedBlock`/`TimelineEmbedBlock`**: `category?: string` prop 추가(기본 `""` = 전체), 설정 패널에 게시판 선택 직후 그 게시판의 실제 카테고리 목록으로 채워지는 드롭다운 신설(게시판을 바꾸면 카테고리 선택은 자동 초기화). 설정 패널은 `useBoardPosts(boardId, 1)`을 별도로 호출해 카테고리 목록만 가볍게 얻는다(count와 무관하게 정확하므로 1이면 충분).
- **검증**: `npx tsc --noEmit`/`npm run lint`/`npm run build` 전부 통과(0 errors). 로컬 dev에서 실제 게시판에 존재하지 않는 카테고리로 필터링하면 0건, 필터 없이는 정상 건수가 나오는 것을 확인해 파라미터가 서버까지 실제로 전달·적용됨을 검증(라이브 DB에는 아직 `posts.category`를 실제로 채운 글이 하나도 없어 "존재하는 카테고리로 필터링하면 그 카테고리 글만 나온다"는 양성 케이스까지는 실데이터로 확인 못함 — 이미 검증된 tag/year 필터와 완전히 동일한 코드 패턴이라 논리적으로는 동일하게 동작).
- **변경 파일**: `src/app/api/boards/[board_slug]/posts/route.ts`, `src/lib/useBoardPosts.ts`, `src/components/craft/primitives/{BoardEmbedBlock,TimelineEmbedBlock}.tsx`.

## 2026-08-13 (EPIC-105 — 하단 Footer를 Craft.js 비주얼 에디터로 전환)
- **배경**: 기존 "하단 메뉴(Footer) 관리"(`/admin/footer`)는 `site_settings.footer_config`(회사정보+링크 3종+SNS)를 편집하는 평범한 폼이었다. 사용자가 이걸 다른 Craft 페이지들처럼 자유 레이아웃 에디터로 바꾸길 원해(EPIC-102 계획 단계에서 확인) 전환.
- **신규 Craft 페밀리 `src/components/craft/footer/`**: `FooterCompanyInfoBlock`(회사명/대표자/사업자등록번호/주소/이메일/전화, 전부 인라인 편집)과 `FooterLinksRowBlock`(items+openInNewTab — 법적/FAQ/부가링크 행과 SNS 링크 행 둘 다 이 하나로 커버) 신설. 원자 블록(Container 등)은 EPIC-102의 PRIMITIVE_RESOLVER 자동 병합 덕분에 별도 등록 불필요.
- **빈 필드 처리**: `FooterCompanyInfoBlock`은 편집 모드가 아니고 값이 비어 있으면 그 필드를 아예 렌더링하지 않는다(기존 `Footer.tsx`의 `company.address && <p>...</p>` 조건부 렌더링과 동일한 동작) — `EditableText` 기본 동작(빈 값이면 방문자에게도 "텍스트를 입력하세요" placeholder를 그대로 보여줌)을 그대로 뒀다면 매 페이지 하단에 깨진 것처럼 보이는 문구가 노출됐을 것을 로컬 확인 중 발견해 수정.
- **`GlobalFooter.tsx`(신규, Server Component)**: 기존 클라이언트 컴포넌트 `Footer.tsx`(site_settings 직접 조회)를 대체 — 홈페이지 `page.tsx`와 동일한 패턴으로 `fetchPublishedPageBySlug("footer")`를 서버에서 조회해 `CraftPageRenderer`에 넘긴다. `layout.tsx`에서 `<Footer/>` → `<div className="mt-auto"><GlobalFooter/></div>`로 교체(기존 "짧은 페이지에서 화면 바닥에 붙는" 동작 유지 — `CraftPageRenderer`는 다른 5개 Craft 페밀리도 공유하는 셸이라 그쪽에 `mt-auto`를 넣을 수 없어 여기서 감쌈). 다 쓰지 않게 된 `src/components/Footer.tsx`는 삭제(참조하는 곳이 새 `/admin/footer/page.tsx` 하나뿐이라 그 파일도 함께 새로 씀).
- **`/admin/footer/page.tsx` 전면 재작성**: footer는 URL을 가진 실제 "페이지"가 아니라 `page_builder.slug='footer'` 행 하나만 쓰는 전역 조각이라, 진입 시 그 행이 없으면 자동으로 만든(insert) 뒤 바로 `CraftFooterEditor`(다른 페밀리와 동일한 얇은 `CraftPageEditor` 래퍼)를 전체화면으로 띄운다. "닫기"는 `/admin/site-structure`로 이동(별도 admin 인덱스 페이지가 없어 기존 admin 허브로).
- **라이브 데이터 마이그레이션**: 기존 `site_settings.footer_config`를 Management API로 직접 조회(`company.name="사일로상점 / 살롱데상"`, `representative="채춘자 / 김수미"`, `email="ethanki@silostore.net"`, 나머지 필드/링크는 전부 비어 있었음)해 `footerDefaultTree`의 초깃값으로 그대로 옮겨 마이그레이션 손실 없음. `page_builder`에 `slug='footer'` 행이 없어 Management API로 직접 생성(`builder_type='craft'`, `status='published'`, `craft_state=null`→기본 트리로 폴백).
- **검증**: `npx tsc --noEmit`/`npm run lint`/`npm run build` 전부 통과(0 errors). `.next` 캐시 삭제 + 완전 재시작한 클린 dev 서버로 홈페이지/`/shop` 양쪽에서 새 Footer가 회사정보+링크 3개(About Silo/이용약관/개인정보처리방침)까지 정확히 렌더링되는 것을 확인, 서버 로그(신뢰 가능한 소스)에 에러 없음 확인.
- **변경/신규 파일**: `src/components/craft/footer/{blocks/FooterCompanyInfoBlock.tsx,blocks/FooterLinksRowBlock.tsx,resolver.ts,defaultTree.tsx}`(신규), `src/components/GlobalFooter.tsx`(신규), `src/components/admin/craft/CraftFooterEditor.tsx`(신규), `src/app/admin/footer/page.tsx`(재작성), `src/app/layout.tsx`, `src/components/Footer.tsx`(삭제).

## 2026-08-13 (EPIC-104 — 헤더 스크롤 모션)
- **배경**: Kinfolk.com처럼 스크롤을 내리면 헤더가 숨고 올리면 다시 나타나는 모션 요청. 기존 `Navbar.tsx`는 `<header>`가 일반 문서 흐름(static)이라 스크롤에 반응하지 않았고, 이 레포에 스크롤 방향 감지 전례도 없었다.
- **`src/lib/useHideOnScroll.ts` 신설**: `scrollY` 방향을 비교해 아래로 스크롤하면 `hidden=true`, 위로 스크롤하면 `hidden=false`, `threshold`(기본 80px) 아래에서는 항상 보이게 하는 훅.
- **`Navbar.tsx` 구조 변경**: 로고 줄+탭 줄만 `position: fixed` + `transform`(translateY)으로 감싸 스크롤에 반응하게 하고, `LeftSidebar`/`RightSidebar`(이미 자체적으로 `position: fixed`)는 그 wrapper **밖**(형제)에 그대로 둔다 — `transform`이 걸린 조상은 자손 `fixed` 요소의 containing block을 바꿔버려 화면 좌표가 어긋나므로, 사이드바를 wrapper 안에 넣지 않는 게 중요했다. `fixed`로 뜬 만큼 문서 흐름에서 빈 자리를 `ResizeObserver`로 실측한 높이의 spacer `<div>`로 대신 채워 본문이 위로 붙지 않게 함(로고 높이/탭 줄바꿈 수에 따라 헤더 높이가 가변적이라 하드코딩 불가). 사이드바 열림 backdrop의 `z-index`를 30→45로 올려(헤더가 이제 `z-40`으로 떠 있어) 사이드바 오픈 시 헤더도 함께 어둡게 덮이도록 함(전에는 static이라 항상 backdrop보다 아래였음).
- **버그 발견+수정(개발 중 실제 재현)**: `ResizeObserver`가 스페이서 높이를 갱신 → 문서 전체 높이 변화 → 스크롤바 유무/폭 변화 → 헤더 자체 줄바꿈이 미세하게 바뀌어 다시 `ResizeObserver` 발화 → ... 되먹임 루프가 생겨, 리렌더가 쉼 없이 도는 동안 `navTabs`/`mainLogo` 등 다른 데이터 fetch effect의 cleanup(`cancelled=true`)이 응답 오기 전에 계속 실행되면서 헤더 메뉴/로그인 영역 전체가 영원히 "로딩 중" 상태로 멈추는 것을 로컬에서 직접 재현(클린 서버 재시작 + 새 탭에서도 동일 재현, `git stash`로 구버전 코드는 정상 동작함을 대조 확인해 원인을 확정). `requestAnimationFrame`으로 측정을 한 프레임 늦추고, 반올림한 값이 실제로 바뀔 때만 `setState`하도록 고쳐 루프 자체가 발생하지 않게 함.
- **검증**: `npx tsc --noEmit`/`npm run lint`/`npm run build` 전부 통과(0 errors). 로컬 dev에서 순수 JS로 `scrollTo(0→300→600→300)` 시퀀스를 재현해 각 단계에서 `VISIBLE→HIDDEN→HIDDEN(계속 아래로)→VISIBLE(위로 돌아옴)`이 정확히 논리대로 동작함을 클래스 문자열로 확인, 좌측 사이드바를 열어 backdrop(z-45)/사이드바 패널(z-50)/헤더(z-40) stacking이 의도대로(사이드바가 backdrop 위, backdrop이 헤더 위) 되는 것도 확인. 콘솔에 새 에러 없음(React19 ref 경고는 기존과 동일하게 무관하게 존재).
- **변경/신규 파일**: `src/lib/useHideOnScroll.ts`(신규), `src/components/Navbar.tsx`.

## 2026-08-13 (EPIC-103 — Kinfolk 16블록 홈 레이아웃)
- **배경**: 사용자가 스크린샷 14장으로 요청한 Kinfolk.com 홈페이지 16개 블록 구조를, EPIC-102가 만든 원자 블록을 조합해 실제로 재현. 저작권 문제를 피하기 위해 EPIC-098과 동일한 원칙(구조·레이아웃 리듬만 재현, 텍스트/브랜딩은 플레이스홀더)을 유지.
- **원자 블록 확장 3건**:
  - `SlideshowBlock`에 `sticky` prop 추가(1번 블록용) — `position: sticky`로 고정해 다음 형제 블록(2번, 불투명 배경)이 스크롤에 따라 자연스럽게 그 위를 덮어 올라오는 "장막이 걷히는" 효과를 별도 JS 스크롤 리스너 없이 순수 CSS로 구현.
  - `BoardEmbedBlock`에 `cardStyle` 2종 추가 — `dragRow`(화면 어디든 클릭+드래그로 좌우 넘기는 게시글 목록, 3rd/6th/9th 블록용)와 `spotlight`(게시글 1개를 여백 넉넉하게 강조, 4th 블록용). 드래그 로직은 `SlideshowBlock`(mode="drag")과 완전히 같은 코드였던 것을 `src/lib/usePointerDragScroll.ts`로 뽑아 공유(중복 제거).
  - `ImageBlock`에 `overlayCategory`/`overlayTitle`/`overlaySummary` 선택 필드 추가(5th/8th/10th/13th 블록의 "이미지 위 캡션") — 전부 비어 있으면 기존과 동일하게 렌더링(하위 호환).
- **신규 원자 블록**: `ShopItemsGridBlock`(12th 블록, "썸네일/카테고리/제목/가격/쇼핑 안내") — 게시판 `posts`에는 가격 필드가 없어(EPIC-102에서 이미 확인) `items`(사일로 상점, 공개 read RLS) 테이블을 `/shop/page.tsx`와 동일한 select로 클라이언트에서 직접 조회. 새 API 라우트 불필요.
- **`src/components/craft/home/defaultTree.tsx` 전면 재구성**: `homeDefaultTree`를 16블록 + 기존 뉴스레터/푸터로 구성. 1번(좌우 클릭 슬라이드쇼, sticky)/2번(자동전환 + Latest Stories 링크)/3·6·9번(드래그 스크롤 게시판 목록, 배경색만 다르게)/4번(스포트라이트)/5·8·10번(꽉 채운 클릭 이미지+카테고리 캡션)/7번(뉴스레터)/11번(큰 텍스트+오른쪽 이미지, ContainerBlock 조합)/12번(사일로 상점 그리드)/13번(이미지 2개+캡션)/14번(3개 글 썸네일)/15번(Stories You May Have Missed)/16번(검은 배경 텍스트+썸네일 3개). 게시판을 참조하는 블록(3/6/9/14/15/16)은 `boardId`가 빈 채로 시작해 관리자가 우측 설정 패널에서 실제 게시판을 골라야 데이터가 채워진다.
- **검증**: `npx tsc --noEmit`/`npm run lint`/`npm run build` 전부 통과(0 errors, 새 경고 없음). 홈페이지는 이미 `page_builder.craft_state`가 저장돼 있어(EPIC-098부터) 이 기본 트리 변경이 즉시 라이브에 반영되지 않는다 — 로컬에서 임시 진단 라우트(`craftState={null}`로 `CraftHomeRenderer` 직접 렌더링, 확인 후 삭제)로 16블록 전체가 실제 데이터(사일로 상점 아이템 "아르누보 유리 램프 180,000원" 등 실제 DB 값)까지 포함해 정상 렌더링되는 것을 `get_page_text`로 확인, 콘솔 신규 에러 없음("Accessing element.ref was removed in React 19" 경고는 기존 홈페이지에도 동일하게 있는 Craft.js 0.2.x/React19 사전 존재 이슈로 무관함을 대조 확인).
- **다음 결정 필요**: 이 16블록 레이아웃을 실제 라이브 홈페이지로 전환하려면 `page_builder.craft_state`를 비워(null) 이 기본 트리로 폴백시키거나, 관리자가 Craft 에디터에서 직접 다시 구성해야 한다 — 사용자 확인 후 진행.
- **변경/신규 파일**: `src/lib/usePointerDragScroll.ts`(신규), `src/components/craft/primitives/{SlideshowBlock,BoardEmbedBlock,ImageBlock}.tsx`, `src/components/craft/primitives/ShopItemsGridBlock.tsx`(신규)+`index.ts`, `src/components/craft/home/defaultTree.tsx`.

## 2026-08-13 (EPIC-102 — Craft.js 프리폼 에디터 코어)
- **배경**: Kinfolk 홈페이지 16블록을 그대로 재현해달라는 요청과 함께, "Craft.js 에디터 안에서 블록 내부에 버튼/이미지/텍스트/게시판/영상/슬라이드쇼/타임라인을 자유롭게 드래그앤드롭하고, 개별 오브젝트의 세부 설정·모션을 지정하고 싶다"는 요구 — 지금은 "정해진 6~7종 섹션 블록을 ROOT 끝에 추가"만 가능해 이 요구를 채울 수 없음을 확인. 범위가 5~6개 EPIC 분량이라 사용자와 상의해 이번 실행은 EPIC-101(버그 수정)+EPIC-102(에디터 코어)로 확정, 나머지(Kinfolk 16블록 조합/헤더 모션/하단메뉴 Craft 전환/게시판 연동 다듬기/슬라이드쇼 위젯화)는 이 기반 위에서 다음 세션에 순차 진행.
- **신규 원자 블록 8종**(`src/components/craft/primitives/`): `ContainerBlock`(canvas — 다른 블록을 담는 nestable 그릇, `<Element canvas is={ContainerBlock}>`로 isCanvas 지정, RootContainer와 동일한 관례), `TextBlock`(폰트 크기/굵기/정렬/색상), `ImageBlock`(objectFit/aspectRatio/href, `EditableResponsiveImage` 재사용), `ButtonBlock`(라벨/href/스타일 variant), `VideoBlock`(YouTube/Vimeo URL 파싱 → iframe embed, mp4는 `<video>`, autoplay는 muted 강제), `SlideshowBlock`(mode: auto/arrows/drag — auto는 기존 `HeroSlideshow.tsx`와 같은 setInterval 크로스페이드 패턴, arrows는 이미지 좌우 절반 클릭으로 이전/다음, drag는 pointer down/move/up 기반 `scrollLeft` 조작 — 이 레포에 전례 없어 새로 구현), `BoardEmbedBlock`(list/thumbnail/gallery 카드 스타일, 기존 `src/lib/useBoardPosts.ts` 그대로 재사용 — 새 fetch 로직 안 만듦), `TimelineEmbedBlock`(게시글을 `TimelineItemSettings`로 매핑해 기존 `AlternatingTimelineCanvas` 재사용). `BoardEmbedBlock`/`TimelineEmbedBlock`은 `posts` 테이블에 가격 컬럼이 없음을 `docs/database-schema.sql`로 재확인해 가격 없이 썸네일/카테고리/제목/요약까지만 표시(가격이 필요한 12번째 Kinfolk 블록은 다음 세션에 `items`/사일로 상점 데이터로 별도 블록 예정).
- **설정 패널 인프라(이 코드베이스에 Craft.js `related` 패턴 최초 도입)**: `SettingsSidebar.tsx` — `useEditor(state => state.events.selected)`로 현재 선택 노드를 읽어 그 노드의 `craft.related.settings` 컴포넌트를 렌더링. `MotionSettingsSection.tsx`(모든 원자 블록이 공통으로 붙이는 모션 섹션: 없음/페이드/슬라이드업/슬라이드레프트 + 지연/길이)와 `useScrollReveal.ts`(`IntersectionObserver` 기반, `prefers-reduced-motion` 존중, 한 번 노출되면 되돌리지 않음) + `RevealWrapper.tsx`(블록 콘텐츠를 감싸 모션 적용)로 구성.
- **진짜 드래그 툴박스**: `Toolbox.tsx` — Craft.js 공식 `connectors.create(ref, buildElement)` 패턴으로 각 항목을 캔버스 어디로든 드래그해 놓을 수 있게 함(클릭 시 ROOT 끝에 추가하는 폴백도 유지, 접근성). "섹션"(페이지별 기존 named 블록)과 "요소"(신규 원자 블록) 두 그룹으로 표시.
- **`CraftPageEditor.tsx` 레이아웃 개편**: 기존 "+ 섹션 추가" 드롭다운을 제거하고 좌측 Toolbox / 중앙 캔버스 / 우측 SettingsSidebar 3분할로 교체(`EditorBody`로 분리, `useEditor()`가 `<Editor>` 컨텍스트 안에서만 호출 가능해서). `<Editor resolver={{...PRIMITIVE_RESOLVER, ...resolver}}>`로 원자 블록 리졸버를 모든 페밀리에 자동 병합 — 5개 페밀리(shop/docent/salon/studio/mypage)는 파일을 전혀 안 건드려도 새 기능을 자동으로 받는다. `CraftPageRenderer.tsx`(공개 렌더러)도 동일하게 병합해 저장된 원자 블록이 공개 페이지에서도 정상 해석되도록 함.
- **홈페이지 전용 독자 구현 마이그레이션**: EPIC-098부터 홈페이지만 `CraftHomeEditor.tsx`/`CraftHomeRenderer.tsx`에 별도 구현을 갖고 있었는데(다른 5개 페밀리가 쓰는 공용 셸과 별개), 이번에 공용 셸(`CraftPageEditor`/`CraftPageRenderer`)로 마이그레이션 — 동작은 동일(craft_state 있으면 그 트리, 없으면 6블록 기본 스켈레톤)하고 진입점만 통일된다. 기존 인라인 기본 트리를 다른 페밀리와 같은 패턴(`src/components/craft/home/defaultTree.tsx`, `homeDefaultTree`/`homeBlockOptions`)으로 분리. EPIC-101이 고친 `@container`도 공용 셸에 이미 있어 계속 유지.
- **`EditableText`(`craft/home/editable.tsx`)에 `style` prop 추가**: `TextBlock`이 `fontSizePx`/`color`를 인라인 스타일로 적용해야 해서 최소 확장(하위 호환, 다른 6개 기존 블록은 영향 없음).
- **검증**: `npx tsc --noEmit`/`npm run lint`/`npm run build` 전부 통과(0 errors, 경고는 기존 패턴과 동일한 `set-state-in-effect` 1건 추가뿐, 새 유형 없음). 로컬 dev에서 공개 홈페이지 재확인 — `CraftHomeRenderer`→`CraftPageRenderer` 마이그레이션 후에도 그리드 2/3/4열 그대로 유지(무회귀), 콘솔 에러 없음. **관리자 로그인 세션이 없어 Toolbox 드래그/설정 패널 입력/모션 저장 왕복은 코드 리뷰로만 검증** — 다음 세션에서 실제 클릭 확인 필요.
- **변경/신규 파일**: `src/components/craft/primitives/*`(신규 9개), `src/components/craft/shared/{Toolbox,SettingsSidebar,MotionSettingsSection,RevealWrapper,types}.tsx`(신규), `src/lib/useScrollReveal.ts`(신규), `src/components/craft/shared/{CraftPageEditor,CraftPageRenderer}.tsx`, `src/components/craft/home/{CraftHomeRenderer.tsx,defaultTree.tsx(신규),editable.tsx}`, `src/components/admin/craft/CraftHomeEditor.tsx`.

## 2026-08-13 (EPIC-101 — 홈페이지 Craft.js 이미지 크기 버그 긴급 수정)
- **신고**: 홈페이지 이미지 크기가 이상하게 너무 크게 보인다는 신고.
- **원인**: 홈페이지는 `page_builder(slug="home").builder_type = 'craft'`(Management API로 확인)라 `CraftHomeRenderer.tsx`가 렌더링을 담당하는데, EPIC-100에서 3개 공용 셸(`CraftPageRenderer`/`CraftPageEditor`/`CraftWidgetShell`)의 `.craft-home` 래퍼에는 `@container` 클래스를 추가했지만, 홈페이지 전용인 이 렌더러는 별도 구현이라 그 수정에서 빠졌다. 그 결과 `EditorialGridBlock`/`LatestIssueBlock`/`TextDirectoryBlock`/`MinimalFooterBlock`의 `@[768px]:` 컨테이너 쿼리가 데스크톱에서도 전혀 매치되지 않아 모바일 1열 레이아웃으로 고정됐고, 그리드 안 이미지가 카드 폭이 아닌 풀 페이지 폭(`w-full`)으로 렌더링됐다.
- **수정**: `src/components/craft/home/CraftHomeRenderer.tsx`의 wrapper `<div>`에 `@container` 클래스 추가(다른 3곳과 동일하게).
- **검증**: 로컬 dev에서 뷰포트 1280px로 확인 — 수정 전 3개 그리드 전부 1열 고정이던 것이 수정 후 각각 2열/3열/4열로 정상 전환됨을 `getComputedStyle().gridTemplateColumns`로 확인.
- **변경 파일**: `src/components/craft/home/CraftHomeRenderer.tsx`.

## 2026-08-13 (EPIC-100 — Craft.js 에디토리얼 레이아웃 모바일 반응형 교정 + LCP 최적화 + 관리자 PC/모바일 반응형 빌더 모드)
- **요청**: EPIC-098~099에서 구축한 Craft.js 에디토리얼 블록(21종 페이지 전용 + 6종 공용)이 모바일에서 찌그러지지 않게 하고, 화보 이미지 로딩 속도(LCP)를 최적화하고, 관리자가 PC/모바일 뷰를 실시간으로 크로스체크할 수 있는 반응형 빌더 모드를 구축.
- **1. 모바일 스태킹 — 뷰포트 미디어쿼리 대신 컨테이너 쿼리 채택(중요한 기술적 결정)**: 지시문은 `md:`/`lg:` 뷰포트 유틸리티 사용을 명시했지만, 그대로 적용하면 항목 3(관리자 토글)이 무의미해진다는 걸 설계 중 발견했다 — `md:`는 **실제 브라우저 창 너비**를 기준으로 평가되므로, 관리자 에디터에서 미리보기 캔버스 `<div>`의 너비만 390px로 좁혀도(브라우저 창 자체는 그대로 데스크톱 크기) `md:grid-cols-3`는 계속 3열로 남아 캔버스 안에서 짓눌린 모습만 보여줄 뿐 실제로 재배치되지 않는다. 대신 Tailwind v4가 별도 설정 없이 지원하는 **컨테이너 쿼리**(`@container` + `@[768px]:` 등 임의값 변형)로 전환 — 공개 페이지에서는 블록의 실제 렌더링 너비가 사실상 뷰포트 너비와 같아 `md:`와 동일하게 동작하고, 관리자 에디터에서는 캔버스 컨테이너 너비 변경이 즉시 모든 자식 블록에 반영된다. 3개 공용 셸(`CraftPageRenderer`/`CraftPageEditor`/`CraftWidgetShell`)의 `.craft-home` 최상위 래퍼에 `@container` 클래스를 걸고, 21개 페이지 전용 블록 + 6개 공용 블록(EditorialGrid/LatestIssue/MinimalFooter/TextDirectory 등) 전체의 `md:` 접두사를 `@[768px]:`로 일괄 치환(기존 브레이크포인트 숫자 그대로 유지, 컨테이너 쿼리 엔진으로만 전환). 대부분의 그리드/스플릿 블록은 이미 EPIC-099 제작 당시 모바일 우선(`grid-cols-1`/`grid-cols-2` 베이스 + `md:grid-cols-3/4`, `grid-cols-1 md:grid-cols-2` 스플릿)으로 만들어져 있어 실제 구조 변경은 거의 없었다.
- **2. LCP/Lazy Loading**: 공용 `EditableImage`/`EditableResponsiveImage`(`editable.tsx`)에 `priority?: boolean` prop 추가 — `false`(기본값)면 `loading="lazy" decoding="async"`, `true`면 `loading="eager" fetchPriority="high" decoding="async"`를 `<img>`에 적용(편집 모드/공개 렌더링 양쪽 `<img>` 태그 전부). 6개 히어로 블록(EditorialHero/ShopHero/DocentHero/SalonHero/StudioHero/MypageHero)의 `EditableResponsiveImage`에만 `priority`를 넘겨 최상단 화보만 우선 로드, 그 외 21+6개 블록의 이미지(Grid/Spotlight 등, 전부 EditableImage/EditableResponsiveImage 경유라 별도 처리 불필요)는 기본값 그대로 lazy.
- **3. 관리자 PC/모바일 반응형 빌더 모드**: `CraftPageEditor.tsx` 툴바에 `DeviceModeToggle`(🖥️ PC 모드 / 📱 모바일 모드) 신설 — `deviceMode` state로 미리보기 캔버스 `<div>`의 너비를 `w-full`(PC) ↔ `w-[390px]`(모바일, 기기 프레임처럼 테두리+그림자+가운데 정렬) 전환. 컨테이너 쿼리 전환(1번) 덕분에 이 너비 변경만으로 모든 하위 블록의 그리드/스플릿이 실시간으로 재계산된다.
- **검증**: `npx tsc --noEmit`/`npm run lint` 0 errors(새 경고 없음, 기존 57개 무관). 로컬 dev에서 실측 — 공개 페이지(`/silo-store`)에서 히어로 이미지만 `fetchpriority="high"`/`loading="eager"`, 나머지 이미지는 `loading="lazy"` DOM 속성 확인, 전체 너비에서 그리드가 기존과 동일하게 3/4열로 렌더링됨을 `getComputedStyle().gridTemplateColumns`로 확인(무회귀). 관리자 에디터(`/admin/pages/[silo-store-id]`)에서 "📱 모바일 모드" 클릭 → 캔버스 390px로 즉시 좁혀지고 3열 그리드(3→2열)/2열 스플릿(2→1열)/4열 디렉토리(4→2열) 전부 실시간으로 재배치되는 것을 `getComputedStyle()`로 확인, 콘솔 에러 없음.
- **변경 파일**: `src/components/craft/home/editable.tsx`(priority prop), `src/components/craft/shared/CraftPageRenderer.tsx`/`CraftWidgetShell.tsx`(`@container` 추가), `src/components/craft/shared/CraftPageEditor.tsx`(DeviceModeToggle + 캔버스 너비 전환), 21개 페이지 전용 블록 + 6개 공용 블록 전체(`md:`→`@[768px]:` 치환, 히어로 6종은 `priority` prop 추가).

## 2026-08-13 (EPIC-099, 3/3 Phase 3 완료 — Craft 공용 블록 4종을 Native Page Builder 위젯으로 노출, 항목 3 전체 완료)
- **배경**: Phase 3 착수 전 범위가 여러 방향으로 해석될 수 있어(1. 공용 블록 4종만 위젯화, 2. 21종 Craft 블록 전부 위젯화, 3. 이번엔 보류) 사용자에게 확인 — "공용 블록 4종만 위젯화(권장)"를 선택받아 그 범위로 진행. 지금까지의 Craft 블록은 "페이지 전체를 Craft가 그리는" 용도였는데, Phase 3는 반대로 **Native Page Builder(위젯 조합형 페이지) 안에 Craft 블록 하나를 섞어 넣는** 기능.
- **`PageModuleType` 4종 추가**(`src/lib/widgetSchema.ts`): `craft_hero`/`craft_directory`/`craft_newsletter`/`craft_footer` — EPIC-098부터 5개 허브 페이지가 재사용해온 검증된 컴포넌트(`EditorialHeroBlock`/`TextDirectoryBlock`/`NewsletterBlock`/`MinimalFooterBlock`)를 그대로 렌더링. 페이지 전용 블록(ShopHeroBlock 등 21종)은 이번 범위 밖. 위젯 팔레트에 새 그룹 "Craft 블록"으로 노출, `WIDGET_FIELDS`/`WIDGET_DEFAULT_SETTINGS`는 각 블록의 실제 prop과 1:1 대응(예: craft_directory의 `items` 목록 필드는 기존 cta/faq 위젯과 동일한 `list` 필드 패턴 재사용).
- **`CraftWidgetShell` 신설**(`src/components/craft/shared/CraftWidgetShell.tsx`): Craft 블록은 `useNode()`로 자기 Craft 노드 컨텍스트를 읽으므로 `<Editor><Frame>` 밖에서 직접 렌더링하면 크래시한다 — 페이지 전체용 `CraftPageRenderer`와 별개로, 단일 블록 하나만을 위한 최소 Editor+Frame 컨텍스트를 제공하는 셸을 새로 만들었다(`enabled={false}` 고정 — 더블클릭 인라인 편집은 Craft 페이지 자체를 열 때만 쓰고, 이 위젯은 기존 위젯들과 동일하게 WidgetInspectorForm 필드 폼으로만 편집).
- **`PageBuilderRenderer.tsx` 배선**: `CraftHeroFromSettings`/`CraftDirectoryFromSettings`/`CraftNewsletterFromSettings`/`CraftFooterFromSettings` 4개 함수 추가, 각각 `CraftWidgetShell` 안에서 해당 Craft 블록 컴포넌트를 렌더링하고 `renderModule`의 switch에 4개 case 추가.
- **검증**: `npx tsc --noEmit`/`npm run lint` 0 errors(새 경고 없음). Native 페이지("About Silo", 실 콘텐츠 보유)에서 실제로 "+ 위젯 추가" → "Craft 블록" 그룹에 4종 노출 확인 → "Craft: Editorial Hero" 추가 → 관리자 미리보기에 실제로 렌더링(기본값 텍스트까지 정확히 표시) → "설정" Inspector 폼 5개 필드 정상 렌더링까지 확인, 콘솔 에러 없음. 테스트로 추가한 위젯 행은 Management API로 정리 완료(브라우저 자동화 환경의 `window.confirm()`이 항상 취소로 처리되어 UI 삭제 버튼으로는 못 지움 — DB에서 직접 삭제, "About Silo" 페이지는 위젯 5개로 원상복구 확인).
- **EPIC-099(항목 3) 전체 완료**: Phase 1(사이트 구성 관리 토글) + Phase 2(5개 허브 페이지 전용 Craft 블록) + Phase 3(공용 Craft 블록 4종 Native 위젯화) 전부 완료.
- **변경 파일**: `src/lib/widgetSchema.ts`, `src/components/craft/shared/CraftWidgetShell.tsx`(신설), `src/components/PageBuilderRenderer.tsx`.

## 2026-08-13 (EPIC-099, 3/3 Phase 2 완료 — 살롱데상/스튜디오/마이 페이지 전용 Craft 블록, 5개 허브 페이지 전부 완료)
- **배경**: 사용자 지시로 "다음 세션도 알아서 진행"(자율 진행) + "SQL도 Management API로 직접 실행"으로 범위/권한이 확장됨 — 남은 3개 허브 페이지(살롱데상/스튜디오/마이 페이지)를 한 세션에서 이어서 완료.
- **살롱데상 전용 블록 3종**(`src/components/craft/salon/blocks/`): `SalonHeroBlock`, `GroupGridBlock`(nav 구조 key:"salon"의 7개 그룹 중 6개를 6칸 그리드로), `FeaturedClubBlock`("이달의 클럽" 스포트라이트).
- **스튜디오 전용 블록 3종**(`src/components/craft/studio/blocks/`): `StudioHeroBlock`, `ServiceGridBlock`(nav 구조 key:"space_inquiry"의 4개 서비스), `FeaturedServiceBlock`("이달의 서비스" 스포트라이트).
- **마이 페이지 전용 블록 3종**(`src/components/craft/mypage/blocks/`): `MypageHeroBlock`, `MypageGridBlock`(nav 구조 key:"mypage"의 3개 최상위 그룹 — My Collections/My Silo Timeline/My Story), `FeaturedHighlightBlock`("나의 하이라이트" 스포트라이트). 모두 개인화된 실데이터가 아니라 다른 허브 페이지와 동일하게 정적 디렉토리형 콘텐츠(로그인 여부와 무관한 안내 페이지 성격 유지).
- **라우팅 버그 발견 및 수정**: `/silo-store`/`/online-docent`/`/salon-des-cent`는 정적 `page.tsx` 라우트 파일이 없어 catch-all(`src/app/[...slug]/page.tsx`)의 `CRAFT_RENDERERS` 등록만으로 충분했지만, **`/studio`와 `/mypage`는 홈페이지처럼 전용 정적 라우트 파일(`src/app/studio/page.tsx`, `src/app/mypage/page.tsx`)이 이미 존재**해 Next.js가 catch-all보다 그 파일을 먼저 매칭한다 — DB 플래그를 `craft`로 바꾼 뒤 실제 브라우저로 확인하다가 두 페이지가 여전히 기존 Native 렌더링(캘린더 위젯 등)을 보여주는 것을 발견해 원인을 확인했다. `CRAFT_RENDERERS`에만 등록해선 절대 호출되지 않으므로 제거하고(죽은 코드 방지), 두 정적 라우트 파일 각각에 홈페이지(`src/app/page.tsx`)와 동일한 `builder_type`/`craft_state` 분기를 직접 추가해 해결 — 이 패턴은 앞으로 정적 라우트가 있는 페이지를 Craft로 옮길 때마다 재사용해야 한다(catch-all 등록만으로는 부족).
- **DB 플래그 전환**: `docs/sql/EPIC-099-remaining-hubs-craft.sql`(`page_builder` slug in ('salon-des-cent','studio','mypage') 행을 `builder_type='craft'`로 전환) — Management API로 agent가 직접 실행 완료(사용자 지시로 이번엔 자동 분류기 차단 없이 정상 동작). 재조회로 5개 허브 페이지(`silo-store`/`online-docent`/`salon-des-cent`/`studio`/`mypage`) 전부 `builder_type='craft'` 확인.
- **검증**: `npx tsc --noEmit`/`npm run lint` 0 errors(새 경고 없음, 기존 경고 57개는 무관). 다른 세션이 이미 띄워둔 로컬 dev 서버(같은 파일시스템이라 hot reload 공유)에 직접 URL로 접속해 5개 페이지 전부 실측 — `/salon-des-cent`/`/online-docent` 공개 렌더링 6섹션 확인, `/studio`/`/mypage`는 라우팅 버그 수정 전/후 차이까지 실제로 확인(수정 전엔 기존 Native 화면, 수정 후 Craft 화면으로 전환됨), `/studio`의 관리자 Craft 에디터("Craft 에디터 — 스튜디오")를 실제로 열어 6블록+Toolbox(⠿/⧉/🗑)+"+ 섹션 추가" 정상 렌더링 및 콘솔 에러 없음 확인.
- **EPIC-099(항목 3) 전체 완료**: Phase 1(사이트 구성 관리 토글) + Phase 2(5개 허브 페이지 전용 Craft 블록) 전부 완료. Phase 3(Craft를 네이티브 위젯 타입으로 노출)은 아직 시작 전.
- **변경 파일**: `src/components/craft/salon/**`(신설), `src/components/craft/studio/**`(신설), `src/components/craft/mypage/**`(신설), `src/components/admin/craft/CraftSalonEditor.tsx`/`CraftStudioEditor.tsx`/`CraftMypageEditor.tsx`(신설), `src/app/[...slug]/page.tsx`(studio/mypage 등록 제거+주석 보강), `src/app/studio/page.tsx`/`src/app/mypage/page.tsx`(craft 분기 추가), `docs/sql/EPIC-099-remaining-hubs-craft.sql`(신설, 실행 완료).

## 2026-08-13 (EPIC-099, 3/3 Phase 2 — 온라인 도슨트(/online-docent) 전용 Craft 블록)
- **배경**: Phase 2 두 번째 대상 페이지. `/silo-store`에서 확립한 패턴(공용 셸 재사용 + 페이지 전용 블록 3종 + 범용 블록 재사용)을 그대로 따른다.
- **온라인 도슨트 전용 블록 3종**(`src/components/craft/docent/blocks/`): `DocentHeroBlock`(ShopHeroBlock과 동일 레이아웃, 태그를 시대 구간 라벨로), `EraGridBlock`(TreasureGridBlock과 동일 형태의 아이콘+라벨 그리드, 항목은 실제 nav 구조(`navConfig.ts` `FALLBACK_NAV_TABS` key:"docent")의 4개 시대 구간 — 고대~왕정/혁명~제국/프로이트~인공지능/디지털 문화), `FeaturedEraBlock`(SpotlightItemBlock과 동일한 어두운 카드형, "이달의 시대" 스포트라이트). 범용 블록(Text Directory/Newsletter/Footer)은 홈페이지 것 재사용, Text Directory 기본값만 도슨트 하위 카테고리로 채움.
- **라우팅**: `src/app/[...slug]/page.tsx`의 `CRAFT_RENDERERS`와 `src/app/admin/pages/[id]/page.tsx`의 `CRAFT_EDITORS`에 각각 `"online-docent"` 한 줄만 추가(공용 셸이 이미 일반화돼 있어 이 두 줄 외 다른 변경 불필요).
- **`docs/sql/EPIC-099-online-docent-craft.sql`**: `page_builder` slug='online-docent' 행을 `builder_type='craft'`로 전환. 이번 세션은 자동 분류기가 Supabase Management API `curl` 호출(읽기 포함)을 전부 차단해 agent가 직접 실행하지 못했고, 사용자가 Supabase SQL Editor에서 직접 실행 — Management API로 재조회해 라이브 반영 확인 완료(`/online-docent`가 이제 Craft 렌더링으로 노출).
- **검증**: `npx tsc --noEmit`/`npm run lint` 0 errors(새 경고 없음, 기존 경고 57개는 이 변경과 무관). **다음에 확인 필요**: 실제 브라우저로 공개 페이지 6섹션 렌더링과 관리자 에디터("Craft 에디터 — 온라인 도슨트") 열기, 이번 세션은 로컬 dev 포트가 다른 세션 점유 중이라 실측 못함.
- **변경 파일**: `src/components/craft/docent/**`(신설: `resolver.ts`/`defaultTree.tsx`/`CraftDocentRenderer.tsx`/`blocks/DocentHeroBlock.tsx`/`blocks/EraGridBlock.tsx`/`blocks/FeaturedEraBlock.tsx`), `src/components/admin/craft/CraftDocentEditor.tsx`(신설), `src/app/[...slug]/page.tsx`, `src/app/admin/pages/[id]/page.tsx`, `docs/sql/EPIC-099-online-docent-craft.sql`(신설, 실행 완료).

## 2026-08-13 (EPIC-099, 3/3 Phase 2 — 사일로 상점(/silo-store) 전용 Craft 블록)
- **배경**: 사용자 확인으로 범위가 확대됨 — 대상 5개 허브 페이지(사일로 상점/온라인 도슨트/살롱데상/스튜디오/마이 페이지)에 "페이지별 전용 블록"을 새로 만들기로 결정. 첫 페이지로 `/silo-store` 진행.
- **Craft 셸 일반화**: 지금까지 홈페이지 전용이던 `CraftHomeRenderer`/`CraftHomeEditor`(EPIC-098)는 이미 검증·병합된 코드라 손대지 않고 그대로 둔 채, `src/components/craft/shared/CraftPageRenderer.tsx`/`CraftPageEditor.tsx`를 새로 만들어 resolver/defaultTree/블록 목록을 파라미터로 받게 했다 — 앞으로 페이지를 추가할 때마다 이 공용 셸에 얇은 wrapper 하나씩만 만들면 된다.
- **사일로 상점 전용 블록 3종**(`src/components/craft/shop/blocks/`): `ShopHeroBlock`(좌측 정렬 태그+타이틀+CTA, 홈 히어로의 중앙 오버레이형과 구조 자체가 다름), `TreasureGridBlock`(사진 대신 이모지+라벨 6칸 촘촘한 그리드, 홈의 3열 사진 그리드와 다른 형태), `SpotlightItemBlock`("이 주의 물건" 어두운 카드형 단일 아이템 스포트라이트). 범용 블록(Text Directory/Newsletter/Footer)은 홈페이지 것을 그대로 재사용(중복 생성 금지 원칙 — 레이아웃이 페이지마다 다를 이유가 없는 블록까지 복제하지 않음), 다만 Text Directory의 기본 카테고리 목록은 사일로 상점 맥락(사일로 유산/할머니/할아버지/보물 목록)으로 채움.
- **라우팅**: 사일로 상점은 catch-all(`src/app/[...slug]/page.tsx`, 클라이언트 컴포넌트)이 담당 — 홈페이지(`src/app/page.tsx`, 별도 Server Component)와 다른 파일이라 분기를 따로 추가. slug→Craft 렌더러 매핑(`CRAFT_RENDERERS`)을 하나 두어 앞으로 페이지를 추가할 때 한 줄만 더하면 되게 했다. 관리자 에디터(`src/app/admin/pages/[id]/page.tsx`)도 slug→에디터 매핑(`CRAFT_EDITORS`)으로 같은 패턴 — 등록 안 된 slug가 실수로 craft가 되면 조용히 깨지는 대신 안내 문구를 보여준다.
- **`docs/sql/EPIC-099-silo-store-craft.sql`**: `page_builder` slug='silo-store' 행을 `builder_type='craft'`로 전환. Management API로 즉시 실행 완료(라이브 반영됨).
- **검증**: `npx tsc --noEmit`/`npm run lint` 0 errors(새 경고 없음). 로컬 dev 관리자 세션에서 실측 — 공개 페이지 6개 섹션 렌더링, 관리자 에디터 열기(제목 "Craft 에디터 — 사일로 상점" 확인), Treasure Grid 블록 복제→삭제(EPIC-099 Phase 1에서 고친 노드 id 충돌 버그의 재발 여부 확인 목적)까지 크래시 없이 정상 동작 확인. 기존 Native 페이지(About Silo) 회귀 없음도 재확인.
- **변경 파일**: `src/components/craft/shared/CraftPageRenderer.tsx`(신설), `src/components/craft/shared/CraftPageEditor.tsx`(신설), `src/components/craft/shop/**`(신설), `src/components/admin/craft/CraftShopEditor.tsx`(신설), `src/app/[...slug]/page.tsx`, `src/app/admin/pages/[id]/page.tsx`, `docs/sql/EPIC-099-silo-store-craft.sql`(신설).

## 2026-08-13 (EPIC-099, 3/3 Phase 1 — 사이트 구성 관리 트리에 빌더 엔진 토글)
- **배경**: EPIC-098에서 도입한 `page_builder.builder_type`은 지금까지 `/admin/pages/[id]`(개별 페이지 편집 화면)에서만 볼 수 있었다 — 사용자 지시로 "사이트 구성 관리 > 사이트 메뉴" 트리의 "관리" 모달에서 카테고리(페이지)마다 바로 native/craft를 토글할 수 있게 한다.
- **`CategoryTreeManager.tsx`**: 기존 "최소 접근 가능 티어"(min_rank_to_read) 편집과 완전히 동일한 draft/저장 패턴(별도 state, 저장 버튼 누르기 전엔 다른 값에 영향 없음)으로 `builder_type` select + 저장 버튼을 그 바로 아래에 추가. `LinkedPageInfo` 타입/조회 쿼리에 `builder_type` 컬럼 포함.
- **참고**: 이 토글은 데이터(`page_builder.builder_type`)만 바꾼다 — 실제로 Craft 렌더링이 적용되려면 그 페이지의 Server Component 라우트에도 홈페이지(`src/app/page.tsx`)와 같은 분기 로직이 있어야 한다(지금은 홈페이지만). 다른 허브 페이지 라우트에 분기를 추가하는 건 Phase 2(페이지별 전용 블록 제작)에서 함께 진행.
- **검증**: `npx tsc --noEmit`/`npm run lint` 0 errors(새 경고 없음). 로컬 dev 관리자 세션에서 "관리" 모달에 select가 실제 DB 값(native)을 정확히 불러오는 것 확인.
- **변경 파일**: `src/components/admin/CategoryTreeManager.tsx`.

## 2026-08-13 (EPIC-099, 2/3 — Craft 에디터 Toolbox UI: 복제/삭제/드래그 재정렬/섹션 추가)
- **배경**: EPIC-098에서 "정해진 6종 안에서 복제/삭제/순서변경"으로 스코프를 좁혀뒀던 항목(제안서 순서 2번째) — 자유 컴포넌트 팔레트가 아니라 정해진 6개 블록만 다루는 범위 그대로.
- **`EditableBlockFrame`(editable.tsx) 확장**: 편집 모드일 때 각 블록 우측 상단에 드래그 핸들(⠿)/복제(⧉)/삭제(🗑) 버튼을 추가. `useNode()`가 이 컴포넌트를 감싸는 블록의 노드 컨텍스트를 그대로 물려받는 Craft.js 특성 덕분에 6개 블록 파일은 전혀 안 건드리고 이 파일 하나만 고쳐 전 블록에 배선됐다.
- **`CraftHomeEditor.tsx`에 "+ 섹션 추가"**: 툴바에 드롭다운으로 6종 블록 목록을 보여주고, 고르면 캔버스 끝에 그 블록의 기본값으로 새 인스턴스를 추가.
- **버그 발견 및 수정(로컬 실측 중 재현)**: 처음 구현은 `query.node(id).toNodeTree()`로 복제 트리를 만들었는데, 이 API가 원본과 **같은 id**를 그대로 들고 있는 트리를 반환한다는 걸 몰랐다 — `addNodeTree`에 그대로 넘기면 내부 노드 맵에 같은 id가 두 개 생기고, 겉보기엔 복제가 성공한 것처럼 보이지만 **아무 블록이나 삭제하는 순간 전체 렌더 트리가 깨지며 페이지가 크래시**했다("Cannot read properties of undefined (reading 'children')", craft.js에 `ERROR_DUPLICATE_NODEID`라는 전용 에러가 있는 이유). `query.parseReactElement()`(+ 섹션 추가가 이미 쓰던, 항상 새 id를 발급하는 API)로 현재 라이브 props를 복사한 새 element를 만들어 우회 — 로컬에서 복제→삭제 반복해도 크래시 없음을 재확인했다.
- **검증**: `npx tsc --noEmit`/`npm run lint` 0 errors(새 경고 없음). 로컬 dev 관리자 세션에서 복제→삭제(크래시 재현 후 수정 확인)→"+ 섹션 추가"→정리, 전부 실측.
- **변경 파일**: `src/components/craft/home/editable.tsx`, `src/components/admin/craft/CraftHomeEditor.tsx`.

## 2026-08-13 (EPIC-099, 1/3 — 뉴스레터 실제 구독 저장(최소 범위))
- **배경**: EPIC-098에서 장식용으로만 남겨뒀던 `NewsletterBlock` 이메일 폼을, 제안서(EPIC-099) 순서대로 가장 먼저 실제로 동작하게 만든다. 발송 채널(Resend 등) 연동은 이번 스코프 밖 — 이메일 저장까지만.
- **`newsletter_subscribers` 테이블 신설**(`docs/sql/EPIC-099-newsletter-subscribers.sql`, Management API로 즉시 실행 완료): 로그인 여부와 무관하게 누구나 구독할 수 있어야 해서 `members`에 옵트인 플래그를 추가하는 대신 독립 테이블로 분리. RLS: 공개 insert 허용, select는 관리자 전용(`is_admin` exists 서브쿼리, `members` 자기 참조 아니라 재귀 문제 없음).
- **`POST /api/newsletter/subscribe`**: 서버에서 이메일 형식 재검증 후 저장. **트러블슈팅**: 처음엔 `upsert(onConflict: "email")`로 구현했는데 로컬 실측에서 anon 요청이 42501(RLS 위반)로 막혔다 — Postgres의 `INSERT ... ON CONFLICT DO UPDATE`는 충돌 행을 확인하려고 내부적으로 SELECT RLS까지 통과해야 하는데, SELECT가 관리자 전용이라 막힌 것(SQL로 직접 재현해 원인 확정). 관리자만 이메일 목록을 볼 수 있어야 한다는 요구사항은 그대로 유지하면서, 라우트는 평범한 `insert`만 하고 unique 위반(23505, 이미 구독 중)을 에러가 아니라 성공으로 처리하도록 우회 — SELECT 정책을 건드리지 않는 훨씬 단순한 해법.
- **`NewsletterBlock.tsx`**: Craft 편집 모드에서는 입력을 막아 실수로 제출되지 않게 하고(장식용 프리뷰 그대로), 공개 렌더링에서만 실제 controlled form으로 동작 — 제출 성공 시 "구독해주셔서 감사해요" 메시지로 폼을 대체, 실패 시 인라인 에러 문구.
- **검증**: `npx tsc --noEmit`/`npm run lint` 0 errors(새 경고 없음). 로컬 dev에서 실제 폼 제출→저장→같은 이메일 재제출(멱등)→잘못된 형식 이메일(400) 전부 실측 확인, 테스트 데이터는 정리 완료.
- **변경 파일**: `docs/sql/EPIC-099-newsletter-subscribers.sql`(신설), `src/app/api/newsletter/subscribe/route.ts`(신설), `src/components/craft/home/blocks/NewsletterBlock.tsx`.

## 2026-08-13 (EPIC-098 후속 — Craft 히어로/Latest Issue 블록 PC·모바일 이미지 분리)
- **사용자 지시**: 기존 히어로 슬라이드쇼(EPIC-092/094)처럼 Craft 블록도 PC/모바일용 이미지를 따로 둘 수 있게 해달라.
- **`EditableResponsiveImage` 신설**(`editable.tsx`): 편집 모드에선 이미지 위 호버 오버레이에 "PC 이미지 변경"/"모바일 이미지 변경(또는 추가)" 버튼 2개를 따로 노출, 공개 렌더링에선 `<picture><source media="(max-width:767px)">` 로 모바일 URL이 있으면 그 파일을, 없으면 데스크톱 파일 하나로 자연히 폴백 — 기존 `HeroSlideshow.tsx`의 "반대쪽 기기엔 투명 픽셀" 트릭과 달리 desktop/mobile URL을 한 컴포넌트가 동시에 알고 있어 더 단순한 구현으로 동일한 효과(기기별 다른 파일 다운로드)를 낸다. `EditorialHeroBlock`/`LatestIssueBlock`에 `imageUrlMobile?` prop 추가해 배선(둘 다 큰 단일 이미지를 쓰는 블록만 — Editorial Grid 카드 썸네일은 작아서 이번 스코프에서 제외).
- **검증**: `npx tsc --noEmit`/`npm run lint` 0 errors(새 경고 없음). 로컬 dev 관리자 세션에서 Craft 에디터를 열어 히어로 블록에 "PC 이미지 변경"/"모바일 이미지 추가" 버튼이 정상 노출되는 것 확인.
- **변경 파일**: `src/components/craft/home/editable.tsx`, `src/components/craft/home/blocks/EditorialHeroBlock.tsx`, `src/components/craft/home/blocks/LatestIssueBlock.tsx`.

## 2026-08-13 (EPIC-098 — 홈페이지 전용 Craft.js 에디토리얼 빌더, Two-Track 아키텍처)
- **요청**: 홈페이지(`/`)에 Craft.js 기반 빌더를 도입하되, 초기 렌더링 화면이 킨포크류 매거진 사이트의 레이아웃 흐름(풀블리드 히어로 → 비대칭 스플릿 → 3열 에디토리얼 그리드 → 텍스트 디렉토리 → 뉴스레터 → 미니멀 푸터)과 동일하게 채워진 상태로 시작하고, 관리자는 텍스트/이미지를 더블클릭으로만 바꾼다.
- **범위 조율(사용자 확인)**: (1) 기존 Native Page Builder(EPIC-060/065)는 폐기하지 않고 다른 모든 페이지에서 그대로 유지 — `page_builder.builder_type`('native'|'craft')으로 페이지 단위 분기하는 Two-Track 구조, 이번 EPIC은 홈페이지 하나만 'craft'로 전환(허브 페이지 확장은 후속). (2) 저작권 문제를 피하기 위해 킨포크 실제 사진/문구는 가져오지 않고 레이아웃 구조·비율·타이포그래피 리듬만 재현 — 이미지는 Unsplash 스톡, 텍스트는 사일로 플레이스홀더 카피. 컴포넌트 이름도 브랜드명 대신 중립적인 `Editorial*`을 사용.
- **DB**: `docs/sql/EPIC-098-page-builder-craft.sql` — `page_builder`에 `builder_type`(기본값 `'native'`, 기존 모든 페이지 회귀 없음)/`craft_state`(jsonb, Craft.js `query.serialize()` 결과 문자열) 컬럼 추가 + `slug='home'` 행이 지금까지 아예 없었던 것을 확인해(홈페이지에 Page Builder 모듈을 붙인 적이 실제로 없었음) 이 시점에 `builder_type='craft'`로 최초 생성. Management API로 즉시 실행 완료(라이브 반영됨).
- **패키지**: `@craftjs/core` 신규 설치.
- **신규 컴포넌트**(`src/components/craft/home/`): `EditorialHeroBlock`/`LatestIssueBlock`/`EditorialGridBlock`/`TextDirectoryBlock`/`NewsletterBlock`/`MinimalFooterBlock` 6종(각각 Craft.js User Component, `craft.props`로 기본값 보유) + 공용 `editable.tsx`(`EditableText`/`EditableImage`/`EditableBlockFrame` — 더블클릭 시 `contentEditable`/이미지 업로드로 전환, `useNode().setProp`으로 커밋) + `RootContainer`(Frame의 canvas 루트) + `resolver.ts`(직렬화 노드→컴포넌트 이름 매핑) + `font.ts`(`next/font/google` EB Garamond, `.craft-home .font-serif`로 scope — 전역 body 폰트는 그대로 둠).
- **Default State 주입(요구사항 4)**: Craft.js 표준 패턴 그대로 — `craft_state`가 없으면 `<Frame>`에 6개 블록이 이미 채워진 `<Element is={RootContainer} canvas>` JSX를 children으로 직접 전달(별도 래퍼 컴포넌트로 감싸면 "resolver에 없는 컴포넌트" invariant가 남을 확인해 인라인으로 수정), 있으면 `<Frame data={craft_state}>`로 로드. `CraftHomeRenderer.tsx`(공개, 읽기 전용)와 `src/components/admin/craft/CraftHomeEditor.tsx`(관리자, 전체화면 오버레이 — 다른 위젯들과 동일한 UX 패턴)가 이 트리를 공유.
- **라우팅**: `src/app/page.tsx` — `homePage.page.builder_type === "craft"`면 기존 히어로 슬라이드쇼(`site_settings`)/Native `PageBuilderRenderer`를 건너뛰고 `CraftHomeRenderer` 하나만 렌더링. `src/app/admin/pages/[id]/page.tsx` — 같은 조건일 때 위젯 목록/팔레트/미리보기 패널 대신 "Craft 에디터 열기" 진입점 하나로 교체(다른 모든 페이지는 `builder_type` 기본값 `'native'`라 이 분기가 생기기 전과 완전히 동일).
- **검증**: `npx tsc --noEmit`/`npm run lint` 0 errors(새 경고 없음). 로컬 dev + 관리자 로그인 세션으로 실측: `/` 접속 시 6개 섹션이 기본 스켈레톤으로 즉시 렌더링, `/admin/pages/[home-id]`에서 Craft 에디터 열기 → 히어로 캡션 더블클릭 수정 → 저장 → `/`에 실제로 반영까지 전부 확인. 기존 Native 페이지(About Silo)는 회귀 없음(동일 화면) 확인.
- **변경 파일**: `docs/sql/EPIC-098-page-builder-craft.sql`(신설), `src/lib/pageBuilder.ts`, `src/app/page.tsx`, `src/app/admin/pages/[id]/page.tsx`, `src/app/globals.css`, `src/components/craft/home/**`(신설), `src/components/admin/craft/CraftHomeEditor.tsx`(신설).

## 2026-08-13 (EPIC-097 후속 — 타임라인 항목을 기존 게시글에서 가져오기)
- **사용자 요청**: 타임라인 항목을 매번 손으로 입력하지 말고, 이미 존재하는 게시글을 연결해서 그 게시글의 썸네일/제목/내용 요약이 타임라인에 자동으로 나오게 해달라.
- **`TimelinePostPicker` 신설**: 게시판 선택 + 제목 검색(기존 `GET /api/boards/[board_slug]/posts?q=` 그대로 재사용) + 목록(썸네일 미리보기 포함)으로 게시글 하나를 고르는 모달. `TimelineItemAccordion`에 "📎 게시글에서 가져오기" 버튼을 추가해 선택한 게시글의 `featured_image_url`(우선)/`photo_url`(폴백, `thumbnail_visible`가 false면 둘 다 무시) → Card Image, `title` → Card Title, `stripHtml(body).slice(0,120)` → Card Description(Tiptap 문단 하나짜리 문서로 감싸 저장), 게시글 URL(`/boards/[board_slug]/[post_slug]`) → Card Link로 한 번에 채운다. 실시간 동기화가 아니라 "가져오기"(스냅샷 복사) — 가져온 뒤에는 다른 필드처럼 자유롭게 고칠 수 있다. 출처 게시글 id는 `TimelineItemSettings.linkedPostId`에 참고용으로만 남기고(재동기화 로직 없음), 아코디언 헤더에 🔗 배지로 표시.
- **버그 수정(TimelineDescriptionEditor)**: 콘텐츠 동기화 `useEffect`가 `[editor]`에만 의존해서, 최초 마운트 이후 부모가 `content`를 프로그래밍적으로 바꿔도(이번 "가져오기" 기능처럼) 에디터 화면에 반영되지 않는 버그가 있었다 — deps에 `content`를 추가(무한 루프는 기존의 JSON 문자열 동일성 비교로 방지).
- **검증**: `npx tsc --noEmit` 0 errors, `npm run lint` 0 errors(새 경고 2건 — `TimelinePostPicker`의 board/posts 조회 `useEffect`가 이 저장소 전역에 이미 있는 "effect 안에서 setState" 패턴과 동일, 정책상 비차단). **다음 세션에서 확인 필요(관리자 로그인 세션)**: 실제로 게시글을 검색·선택해 항목에 채워지는지, 저장 후 공개 페이지에 썸네일/요약/링크가 정확히 반영되는지.
- **변경 파일**: `src/components/admin/TimelinePostPicker.tsx`(신설), `src/components/admin/TimelineWidgetEditor.tsx`, `src/components/admin/TimelineDescriptionEditor.tsx`, `src/lib/widgetSchema.ts`.

## 2026-08-13 (EPIC-097 — 타임라인 위젯: 수동 항목 입력 + 프로 레벨 2단 Split 편집기)
- **요청**: "타임라인 위젯 설정" 클릭 시 좌측(다크) 항목 입력 폼 + 우측(라이트) 실시간 프리뷰로 화면을 통째로 분할하는 웹 빌더급 UI. `dnd-kit`으로 항목 드래그 정렬, 아코디언으로 항목별 펼침(Title/Title Color/Subtitle/Card Image/Card Title/Card Description/Card Link/Link Target), `Tiptap` 미니 에디터로 카드 설명 입력.
- **timeline 위젯을 board_id 연동에서 수동 항목 목록으로 전환**: 기존엔 `timeline` PageModuleType이 `BOARD_LINKED_MODULE_TYPES`에 포함돼 게시판 글을 그대로 받아 그렸다 — 참고 이미지의 데이터 모델(연도/타이틀/카드 이미지/설명/링크를 운영자가 직접 입력)과 맞지 않아 `BOARD_LINKED_MODULE_TYPES`에서 제거하고 `settings.items: TimelineItemSettings[]`(신설, `widgetSchema.ts`)로 바꿨다. 카드 설명은 Block Editor와 동일하게 Tiptap JSON을 원본으로, HTML은 편집 시점에 파생해 함께 저장(공개 렌더링이 에디터 없이 바로 그릴 수 있게, `sanitizeHtml` 적용).
- **신규 컴포넌트**: `AlternatingTimelineCanvas.tsx`(관리자 프리뷰/공개 렌더링 공용, 연속 단일 스파인 + 좌우 교차 카드), `TimelineDescriptionEditor.tsx`(Bold/Italic/Underline/Strike/목록/링크만 남긴 미니 Tiptap, `BlockEditor.tsx`에서 축소), `TimelineWidgetEditor.tsx`(전체 화면 오버레이 2단 Split UI — WidgetInspectorForm의 generic FieldDef 파이프라인이 이미지 업로드/리치텍스트를 표현 못해 이 위젯 타입만 특수 처리). 이미지 업로드는 기존 `uploadFile()`(`src/lib/storage.ts`, `post-images/timeline-widget` 버킷·서브폴더) 재사용.
- **사용자 신고 수정(HOTFIX-097, "연대 선이 끊어진다")**: `TimelineView.tsx`(게시판 타임라인, timeline 위젯과는 별개 컴포넌트)의 데스크톱 스파인이 월/년 그룹마다 따로 그려져 그룹 경계에서 끊겨 보였다 — 전체를 하나의 relative 컨테이너로 감싸고 스파인도 그 높이에 하나만 그리도록 수정, 카드 좌우 교대 인덱스도 월 경계에서 리셋되지 않게 전체 항목 연속 인덱스로 변경.
- **검증**: `npx tsc --noEmit` 0 errors, `npm run lint` 0 errors(새 경고 없음). **다음 세션에서 확인 필요(관리자 로그인 세션)**: `/admin/pages/[id]`에서 Timeline 위젯 "설정 열기" → 항목 추가/드래그 재정렬/이미지 업로드/설명 입력 → 저장 → 공개 페이지에 실제 반영, 게시판 타임라인 데스크톱 뷰에서 스파인이 실제로 끊기지 않고 이어지는지.
- **변경 파일**: `src/components/modules/AlternatingTimelineCanvas.tsx`(신설), `src/components/admin/TimelineDescriptionEditor.tsx`(신설), `src/components/admin/TimelineWidgetEditor.tsx`(신설), `src/components/TimelineView.tsx`, `src/components/PageBuilderRenderer.tsx`, `src/app/admin/pages/[id]/page.tsx`, `src/lib/pageBuilder.ts`, `src/lib/widgetSchema.ts`.

## 2026-08-12 (EPIC-096 후속 5차 — 폰트 자유 텍스트 입력을 실제 업로드+미리보기 픽커로 교체)
- **사용자 신고**: 후속 4차에서 추가한 폰트 필드가 CSS `font-family` 문자열을 손으로 입력하는 빈 텍스트박스였다 — 실제로 그 폰트가 사이트에 없으면(호스팅/등록된 적 없으면) 브라우저가 조용히 기본 폰트로 대체해 아무 효과가 없었다. "폰트를 추가할 수 있게", "미리보기를 볼 수 있게" 해달라는 요청.
- **`FontPicker` 컴포넌트 신설**: 이 프로젝트에 이미 있던 커스텀 폰트 인프라(EPIC-083 `/admin/fonts`, `custom_fonts` 테이블, `useCustomFonts()`의 `@font-face` 자동 주입 — 에디터 폰트 드롭다운이 쓰던 것과 동일)를 그대로 재사용했다. 드롭다운을 열면 (1) 이미 등록된 폰트들을 **그 폰트로 실제 렌더링된 미리보기 텍스트**("Silo Store 사일로 다람쥐")와 함께 목록으로 보여주고, (2) 목록 맨 아래에 그 자리에서 바로 새 폰트를 업로드(이름 입력 + .woff2/.woff/.ttf/.otf 파일)할 수 있는 미니 폼이 있다 — 업로드하면 즉시 그 폰트가 선택되고 목록에도 추가된다. "게시물 출력방식"의 글번호/날짜/작성자/제목/좋아요·조회·댓글 5개 폰트 필드 전부 이 픽커로 교체.
- **`useCustomFonts()`에 `refetch` 추가**: 지금까지 마운트 시 1회만 조회했는데, FontPicker가 업로드 직후 목록을 새로고침하려면 다시 부를 방법이 필요해 뺐다(다른 호출부인 에디터/PostBody는 영향 없음 — 추가된 반환값일 뿐).
- **검증**: `npx tsc --noEmit` 0 errors, `npm run lint` 0 errors(새 경고 1건 — `useCustomFonts`의 `load()` 호출이 이 저장소에 이미 수십 곳 있는 "effect 안에서 setState" 패턴과 동일한 종류, 정책상 비차단). 로컬 dev에서 게시글 상세와 `/admin/fonts` 둘 다 콘솔 에러 없이 로드 확인. **다음 세션에서 확인 필요(관리자 로그인 세션)**: 실제로 폰트를 업로드해 미리보기가 그 폰트로 렌더링되는지, 선택 후 게시글 상세에 실제 적용되는지.
- **변경 파일**: `src/components/admin/FontPicker.tsx`(신설), `src/lib/useCustomFonts.ts`, `src/components/admin/BoardForm.tsx`.

## 2026-08-12 (EPIC-096 후속 4차 — "게시물 출력방식" 확장: 수정일 숨기기 + 항목별 폰트 + 통계 스타일)
- **"수정 YYYY.MM.DD" 숨기기**: `postMetaStyle.hideUpdatedDate` 추가 — 체크하면 게시글 상세에서 수정일 줄 자체가 렌더링되지 않는다.
- **항목별 폰트 지정**: 글번호/날짜/작성자/제목 4곳에 폰트 입력 필드 추가(`postNumberFontFamily`/`dateFontFamily`/`authorNameFontFamily`/`titleFontFamily`) — Navbar.tsx의 커스텀 폰트 입력과 동일하게 CSS `font-family` 문자열을 자유 입력받는다(별도 폰트 선택 UI 없음, 이 프로젝트의 기존 관례). 제목은 크기/색상은 그대로 두고(반응형 h1이라 임의 크기를 허용하면 레이아웃이 깨지기 쉬움) 폰트만 열었다.
- **좋아요·조회·댓글 스타일**: 지금까지 이 통계 줄은 스타일 지정 자체가 불가능했다 — `statSizePx`/`statColorHex`/`statFontFamily`를 추가해 세 값(항상 한 줄에 붙어 나옴)을 하나로 묶어 크기/색상/폰트를 지정할 수 있게 했다.
- **리팩터**: `postMetaStyle` 조립 로직이 저장(생성/수정 페이지 2곳)·"전체 게시판 기본값 저장"·실시간 미리보기까지 4곳에 그대로 복붙돼 있어 필드를 추가할 때마다 어긋나기 쉬웠다 — `BoardForm.tsx`에 `buildPostMetaStyle()` 공용 함수로 뽑아 4곳 모두 이걸 재사용하도록 정리(동작 변경 없음, 순수 리팩터).
- **검증**: `npx tsc --noEmit`/`npm run lint` 0 errors(신규 warning 없음). 로컬 dev에서 게시글 상세(`/boards/treasures/urin-02`)와 타임라인(`/about-silo/silo-timeline`) 둘 다 콘솔 에러 없이 렌더링 확인(타임라인은 `metaStyleToCss`를 공유해 새 `dateFontFamily`가 자동으로 반영되지만 이번엔 회귀 없음만 확인, 별도 UI 추가는 게시글 상세에만 국한). **다음 세션에서 확인 필요(관리자 로그인 세션)**: 실제로 각 필드를 입력해 저장 → 게시글 상세에 폰트/숨김/통계 스타일이 반영되는지 왕복.
- **변경 파일**: `src/components/boards/PostDetailHeader.tsx`, `src/components/admin/BoardForm.tsx`, `src/app/admin/boards/[id]/page.tsx`, `src/app/admin/boards/new/page.tsx`.

## 2026-08-12 (EPIC-096 후속 3차 — "게시물 출력방식" 버튼 목적지 버그, 썸네일 크기 설정, 전체 게시판 기본값)
- **"게시물 출력방식" 버튼이 엉뚱한 페이지로 가던 버그 수정**: 게시글 상세의 이 버튼이 실제 설정 화면(`/admin/boards/[id]`)이 아니라 모든 게시판이 공유하는 Page Builder placeholder(`boards-id-postid` slug, `/admin/pages/[공용 id]`)로 보내고 있었다 — `PageEditButton.tsx`에 `hrefOverride` prop을 추가해 호출부가 정확한 목적지를 직접 지정할 수 있게 하고, `PostDetailClient.tsx`가 이미 알고 있는 `board.id`로 `/admin/boards/${board.id}`를 바로 연결한다.
- **갤러리 썸네일 크기 직접 지정**: "게시판 수정"의 갤러리 설정에 "썸네일 최대 크기 (px)" 입력 필드 추가(`widget_settings.galleryThumbnailMaxPx`) — 비워두면 EPIC-096의 기존 동작(한 행당 개수로 자동 계산) 그대로, 값을 넣으면 그 px를 칸의 목표 너비로 직접 강제한다. `useBoardData.ts` → `BoardModule.tsx` → `types.ts`(BoardRendererProps) → `GalleryRenderer.tsx` → `GalleryModule.tsx`까지 기존 galleryColumns와 동일한 파이프라인으로 배선.
- **"게시물 출력방식"을 전체 게시판 기본값으로 저장**: `BoardForm.tsx`에 "🌐 이 설정을 전체 게시판 기본값으로 저장" 버튼 추가 — 날짜/작성자 스타일 + 블록 레이아웃 순서를 새 `site_settings.default_post_display_style` 키에 저장한다. `PostDetailClient.tsx`는 게시판 자신의 `postMetaStyle`/`postLayoutOrder`가 없을 때만(기존처럼 있으면 그게 항상 우선) 이 사이트 기본값으로 대체한다 — "기본값"이지 다른 게시판이 이미 저장해둔 커스텀 설정을 덮어쓰는 강제 적용이 아니다.
- **검증**: `npx tsc --noEmit`/`npm run lint` 0 errors(신규 warning 없음). 로컬 dev에서 실제 게시글(`/boards/treasures/urin-02`)과 갤러리 게시판(`/about-silo/silo-daily`) 둘 다 새 fetch/props가 추가된 상태로 콘솔 에러 없이 정상 렌더링 확인. **다음 세션에서 확인 필요(관리자 로그인 세션)**: "게시물 출력방식" 버튼이 실제로 올바른 게시판 설정 화면으로 이동하는지, 썸네일 크기 입력이 실제 갤러리 화면에 반영되는지, "전체 게시판 기본값으로 저장" 후 다른(자기 설정 없는) 게시판의 글에 그 값이 실제로 적용되는지.
- **변경 파일**: `src/components/admin/PageEditButton.tsx`, `src/app/boards/[board_slug]/[post_slug]/PostDetailClient.tsx`, `src/components/admin/BoardForm.tsx`, `src/lib/useBoardData.ts`, `src/components/modules/BoardModule.tsx`, `src/components/boards/renderers/types.ts`, `src/components/boards/renderers/GalleryRenderer.tsx`, `src/components/modules/GalleryModule.tsx`, `src/app/admin/boards/[id]/page.tsx`, `src/app/admin/boards/new/page.tsx`.

## 2026-08-12 (EPIC-096 후속 2차 — 사용자 신고: 탭/표 가로 스크롤, 게시판 선택 정렬)
- **탭/표 가로 스크롤 제거**: "전체 글 관리" 탭 바(`layout.tsx`)가 `max-w-4xl`(896px) 안에서 7개 탭을 `overflow-x-auto whitespace-nowrap`로 욱여넣어 스크롤해야 다 보였다 — 컨테이너를 `max-w-6xl`로 넓히고 `flex-wrap`으로 바꿔 스크롤 없이(필요하면 줄바꿈으로) 한 화면에 다 보이게 했다. 표가 있는 `AdminPostsBoardView.tsx`/`/admin/posts/shop`도 같은 이유로 `max-w-6xl`로 함께 넓힘.
- **게시판 이동 select 정렬**: 인라인 "카테고리(게시판) 이동" 드롭다운이 가나다순이라 About Silo/사일로상점/온라인도슨트/살롱데상/스튜디오/마이페이지 실제 사이트 구조가 뒤섞여 보였다 — HOTFIX-101이 `WriteBoardForm`의 게시판 선택기에 이미 적용한 것과 동일하게, 실제 사이트 메뉴 트리 순서(`site_navigations` sort_order 기준)를 그대로 따르도록 수정. 어느 브랜치에도 안 걸린 게시판만 맨 뒤에 이름순으로 남는다.
- **검증**: `npx tsc --noEmit`/`npm run lint` 0 errors(신규 warning 없음). 관리자 로그인 세션이 없어 실제 화면(탭이 한 줄/두 줄로 자연스럽게 보이는지, 드롭다운 순서가 맞는지)은 코드 검증만 완료 — **다음 세션에서 확인 필요(로그인 세션)**.
- **변경 파일**: `src/app/admin/posts/layout.tsx`, `src/components/admin/AdminPostsBoardView.tsx`, `src/app/admin/posts/shop/page.tsx`.

## 2026-08-12 (EPIC-096, 5차/최종 — 에디터 슬래시 커맨드 + 인라인 설문(Poll) 블록)
- **3.2 사실 확인**: 이 에디터는 이미 Tiptap 기반(`blockEditorCore.ts`/`BlockEditor.tsx`)이고 텍스트 포맷팅(굵게/색상 등)·이미지·영상·갤러리 캐러셀·유튜브/인스타/X 임베드·**표(Table, EPIC-083)**까지 전부 있었다 — "빈약한 에디터 폐기 후 재도입"이 아니라, 실제로 빠져 있던 두 가지(슬래시 커맨드 UX, 투표 블록)만 추가했다.
- **슬래시 커맨드 메뉴**: 문단 맨 앞에서 `/`를 치면 뜬다(새 패키지 없이 매 update마다 "문단 시작~커서" 텍스트를 정규식으로 검사하는 방식 — 이 파일이 이미 쓰는 "가벼운 직접 구현" 관례). 제목2/제목3/글머리 목록/번호 목록/인용구/표/구분선(기존 툴바 버튼과 동일한 chain 커맨드) + 설문(Poll, 신규)을 지원.
- **인라인 설문(Poll) 블록**: 새 Tiptap atom 노드 `pollEmbed`(`blockEditorCore.ts`) — 에디터 안에서는 정적 placeholder("📊 질문")만 보이고, 실제 투표 UI는 발행된 글에서 `UniversalBlockRenderer`가 `src/lib/pollEmbed.ts`(vanilla JS DOM 치환, `processGalleryCarousels`/`processInstagramEmbeds`와 동일한 기존 관례)로 그 자리를 살아있는 위젯으로 바꿔친다. DB는 새 테이블 없이 기존 "설문 [우리들 맴]" 기능의 `polls`/`poll_options`/`poll_votes`/`poll_option_counts`를 100% 재사용. 다만 그 기존 생성 API(`POST /api/polls`)는 관리자 전용이라, 일반 회원이 자기 글에 즉석 투표를 심을 수 있도록 `POST /api/polls/inline`(신설, 관리자 게이트 없음)과 단일 조회용 `GET /api/polls/[id]`(신설)를 추가했다 — 투표 자체(`POST /api/polls/[id]/votes`)는 기존 엔드포인트 그대로. `sanitize.ts` allowlist에 `data-poll-id`/`data-question` 추가(안 하면 저장 시 이 블록이 통째로 잘려나감).
- **검증**: `npx tsc --noEmit`/`npm run lint` 0 errors(신규 warning 없음). `GET /api/polls/[id]`(존재/미존재 둘 다), `POST /api/polls/inline`(비로그인 401) 실제 호출 확인. Management API로 임시 테스트 설문 1건을 만들어 `GET /api/polls/[id]` 실응답으로 렌더링 로직(질문/선택지/득표율 계산)이 정확한 것까지 확인 후 즉시 삭제(잔존 데이터 0건 재확인). `/write` 페이지가 새 슬래시 메뉴/설문 노드 코드 포함 상태로 정상 컴파일·로드되는 것 확인(비로그인이라 게시판 선택 이후 실제 에디터 타이핑까지는 이번엔 확인 못함). **다음 세션에서 확인 필요(로그인 세션)**: 실제로 "/"를 쳐서 메뉴가 뜨는지, 설문 만들기 폼 제출 → 게시글 저장 → 발행된 글에서 투표 버튼 클릭까지 왕복.
- **변경 파일**: `src/lib/blockEditorCore.ts`, `src/lib/sanitize.ts`, `src/lib/pollEmbed.ts`(신설), `src/components/content/UniversalBlockRenderer.tsx`, `src/components/editor/BlockEditor.tsx`, `src/app/api/polls/[id]/route.ts`(신설), `src/app/api/polls/inline/route.ts`(신설).

## 2026-08-12 (EPIC-096, 4차 — 게시글 블록 위젯화 + 관리자 드래그앤드롭 레이아웃 프리뷰)
- **3.1 위젯형 게시글 레이아웃**: 게시글 상세를 5개 블록(메타데이터/태그/본문/좋아요·북마크/댓글)으로 분리하고 그 순서를 `boards.widget_settings.postLayoutOrder`(문자열 배열)로 저장하도록 데이터 모델을 확장(`src/lib/postLayout.ts` 신설 — 타입/기본 순서/정규화 함수를 관리자 폼과 렌더러가 공유). `PostDetailClient.tsx`가 이 순서대로 블록을 렌더링하고, 값이 없으면 기존 하드코딩 순서 그대로 동작한다(회귀 없음).
- **관리자 드래그앤드롭 + 실시간 프리뷰**: `/admin/boards/[id]`(및 new)의 "게시물 출력방식"에 새 섹션 "블록 레이아웃 순서" 추가(`PostLayoutOrderEditor.tsx`, 신설) — 좌측 목록을 드래그로 재배열하면 우측 프리뷰가 그 순서 그대로 즉시 다시 그려진다. 새 의존성 없이 이미 프로젝트에 있던 `@dnd-kit/core`/`@dnd-kit/sortable`(admin/pages 위젯 편집기가 쓰던 것과 동일 조합)를 재사용. **범위 절충(솔직히 기록)**: "Framer/Shopify 테마 에디터 수준"이라는 요청 원문에 비해, 우측 프리뷰는 실제 게시글 데이터를 불러와 렌더링하는 것이 아니라 각 블록의 스타일을 흉내낸 placeholder 목업이다(관리자 폼 화면엔 특정 게시글이 연결돼 있지 않아 실제 콘텐츠가 없음) — "이 순서로 배치하면 이런 느낌"이라는 배치 감각은 실시간으로 정확히 전달되지만, 실제 폰트/줄바꿈까지 완전히 동일하진 않다.
- **검증**: `npx tsc --noEmit`/`npm run lint` 0 errors(신규 warning 없음). 로컬 dev에서 실제 게시글(`/boards/treasures/urin-02`)을 열어 커스텀 설정이 없는 게시판의 기존 순서(메타→태그→본문→액션→댓글)가 그대로 유지되는 것 확인(회귀 없음), `/admin/boards/new` 접속 시 새 컴포넌트 로딩으로 인한 런타임 에러 없음 확인. **다음 세션에서 확인 필요(관리자 로그인 세션)**: 실제로 블록을 드래그해 순서를 바꾼 뒤 저장 → 게시글 상세 페이지에 그 순서가 정확히 반영되는지 왕복 확인(비로그인이라 이번엔 저장 자체를 못 함).
- **변경 파일**: `src/lib/postLayout.ts`(신설), `src/components/admin/PostLayoutOrderEditor.tsx`(신설), `src/components/admin/BoardForm.tsx`, `src/app/admin/boards/[id]/page.tsx`, `src/app/admin/boards/new/page.tsx`, `src/app/boards/[board_slug]/[post_slug]/PostDetailClient.tsx`.

## 2026-08-12 (EPIC-096, 3차 — 인터랙티브 캘린더: 드래그앤드롭 일정 이동)
- **2.2 Hover Expansion**: 재확인 결과 이미 구현돼 있었다 — `CalendarGrid.tsx`가 날짜 셀에 `isolate`(새 stacking context) + `hover:scale-125 hover:z-20 hover:shadow-lg`로 이웃 셀을 덮으며 확대되고, 미리보기 카드(`CalendarCellPopout`)도 `group-hover:block`으로 함께 팝업된다(EPIC-092 때 이미 구현). 이번엔 코드 변경 없이 재확인만 함.
- **2.2 Drag & Drop 일정 이동**: `CalendarBoardWidget.tsx`에 이미 프로젝트에 설치돼 있던 `@dnd-kit/core`(admin/pages 위젯 편집기·site-structure 트리가 이미 쓰는 라이브러리, 새 의존성 추가 없음)로 구현 — "나의 달력" 필터가 켜진 상태에서만(요구사항 원문 그대로), 날짜 칸에 로그인한 본인 글이 있으면 그 칸 자체가 드래그 핸들이자(`useDraggable`) 모든 칸이 드롭 타겟(`useDroppable`)이 된다. 드롭하면 그 글의 `posts.created_at`을 대상 날짜로 옮기되(시/분/초는 원래 값 보존) 낙관적 갱신 + 실패 시 롤백. `CalendarGrid.tsx`(순수 프레젠테이션 셸 원칙 유지)는 dnd-kit을 전혀 몰라도 되도록 `cellWrapper` render-prop 하나만 추가해 호출부가 날짜 셀 전체를 원하는 대로 감쌀 수 있게 열어줬다(칸이 작아 별도 드래그 칩 UI를 넣을 공간이 없어, "하루 1글 위주" 성격의 이 타임라인/저널형 게시판에는 그 날의 첫 글만 옮기는 절충으로 충분하다고 판단).
- **검증**: `npx tsc --noEmit`/`npm run lint` 0 errors(신규 warning 없음). 로컬 dev에서 실제 calendar 위젯이 있는 `/studio` 페이지로 확인 — 31개 날짜 칸 전부 `DraggableDroppableCell`로 정상 래핑, 콘솔 에러 없음. **다음 세션에서 확인 필요(로그인 세션)**: 비로그인 상태라 `canDrag` 조건(로그인 + 나의 달력 필터)이 실제로 참이 되는 경로는 이번엔 검증 못함 — 실제 로그인 후 자기 글이 있는 날짜 칸을 다른 날짜로 드래그해 `created_at`이 정확히 바뀌는지 최종 확인 필요.
- **변경 파일**: `src/components/modules/CalendarGrid.tsx`, `src/components/modules/CalendarBoardWidget.tsx`.

## 2026-08-12 (EPIC-096, 2차 — 하이엔드 타임라인: 양방향 교차형 카드 + 스크롤 페이드업)
- **2.1 타임라인 리마스터링**: 기존 "얇은 단일 스파인 + 왼쪽 라벨" 세로형 레이아웃을 완전히 폐기하고 Common Ninja/Apple류의 양방향 교차형(Alternating) 카드로 재구축(`TimelineView.tsx`의 새 `AlternatingRow`) — 중앙 스파인을 기준으로 짝수/홀수 항목이 좌우로 번갈아 배치되고, `renderPreview`(썸네일+본문 발췌+메타)가 있으면 그 완전한 카드를 처음부터 항상 펼쳐 보여준다(참고 이미지처럼 hover 없이 바로 보임 — 기존 hover-only 미리보기 방식은 가로형(horizontal) 오리엔테이션에만 그대로 남김). 좁은 화면(모바일)에서는 좌우 분할이 카드 폭을 반토막 내 오히려 가독성이 떨어지므로, `md` 미만에서는 기존 단일 스파인 목록(카드 스타일만 업그레이드)으로 우아하게 저하시키고 `md` 이상에서만 교차형을 쓴다(두 버전을 함께 렌더링 후 Tailwind 반응형 클래스로 하나만 노출 — JS 브레이크포인트 감지 없이 SSR 안전).
- **스크롤 페이드업 애니메이션**: 새 라이브러리 없이 `IntersectionObserver` 하나로 구현(`FadeUpOnScroll`) — 카드가 뷰포트에 10% 이상 들어오면 한 번만 `opacity-0 translate-y-6` → `opacity-100 translate-y-0`로 전환되고 그 뒤로는 유지된다(반복 재생 없음).
- **검증**: `npx tsc --noEmit`/`npm run lint` 0 errors(신규 warning 없음). 로컬 dev에서 실제 데이터가 있는 `/about-silo/silo-timeline`으로 확인 — 데스크톱(1280px)에서 교차형 카드 10개가 정확히 렌더링(구조/클래스 DOM으로 확인), 모바일(375px)에서 교차형 컨테이너가 `display:none`으로 감춰지고 단일 스파인 폴백만 노출되는 것 확인. **환경 한계로 못 미침(EPIC-079-PHASE-5와 동일한 종류)**: 이 세션의 헤드리스 브라우저는 컴포지팅을 하지 않아 `IntersectionObserver` 콜백 자체가 전혀 발화하지 않음(직접 만든 테스트 엘리먼트로도 재현 — 코드 문제 아님, 표준 API를 그대로 사용). 실제 브라우저에서 스크롤 시 페이드업이 매끄럽게 보이는지는 다음 세션 실사용 확인 필요.
- **변경 파일**: `src/components/TimelineView.tsx`.

## 2026-08-12 (EPIC-096, 1차 — 럭셔리 리마스터링: 사실 확인 + Clean URL/썸네일/관리자 인라인 편집)
- **범위 확정(사용자 승인, 순차 진행)**: 요청서(7개 대형 항목)를 즉시 착수하기 전 사실 확인부터 했다 — 전제 중 상당수가 이미 구현된 상태와 달랐다. **Clean URL**(요구사항 1.2)은 EPIC-079-PHASE-2 때 이미 `/boards/[board_slug]/[post_slug]` slug 라우팅으로 전면 전환됐고(스크린샷의 UUID는 라우팅 구조 문제가 아니라, EPIC-070/EPIC-079-PHASE-2부터 있던 "slug로 못 찾으면 id로 한 번 더 시도"하는 의도된 하위호환 폴백이 실제로 쓰인 경우였다 — 그 board의 진짜 slug는 `treasures`로 정상). **WYSIWYG 에디터**(요구사항 3.2)도 "폐기 후 도입" 대상이 아니라 이미 Tiptap 기반 블록 에디터(`blockEditorCore.ts`, `BlockEditor.tsx`)가 이미지/영상 드래그앤드롭·갤러리 캐러셀·임베드를 갖추고 있었다. 사용자와 합의해 이번 1차는 작고 확실한 항목(URL 자동 리다이렉트, 썸네일 반응형, 관리자 인라인 편집/정렬)부터 처리하고, 대형 항목(타임라인/캘린더 리디자인, 실시간 프리뷰 테마 에디터, 에디터 슬래시 메뉴 확장)은 다음 세션들로 순차 이어간다.
- **1.2 Clean URL 자동 리다이렉트**: `/boards/[board_slug]/[post_slug]`(Server Component)에 UUID 감지+301 redirect를 추가 — board_slug/post_slug 세그먼트가 UUID 형태면 진짜 slug를 조회해 `redirect()`한다(스크린샷 사례로 실측: `/boards/8e458557-.../urin-02` → `/boards/treasures/urin-02`). 게시판 목록 페이지(`/boards/[board_slug]`, Client Component)에도 동일한 감지 후 `router.replace()`를 추가.
- **1.3 갤러리 썸네일 반응형 결함 수정**: `GalleryModule.tsx`의 그리드가 `repeat(N, minmax(0, 1fr))`(열 "개수"만 고정, 칸 너비는 컨테이너에 비례해 무한정 커짐)이던 것을 `repeat(auto-fill, minmax(min px, target px))`로 교체 — 칸 "너비"를 고정하고 남는 공간만큼 열 개수가 자동으로 늘어나게 했다(요구사항 예시 "3열→4열→5열" 그대로). 관리자가 설정한 columns(2~6) 값을 기준(1200px 컨테이너 가정)으로 target/min px를 역산해 기존 설정 의도를 그대로 보존한다.
- **1.1 전체 글 관리 — 인라인 편집 + 정렬(1차, Data Grid 라이브러리 도입은 다음 단계)**: `/admin/posts/shop`(물품 관리)에 시대·상태 인라인 select(즉시 저장, 별도 모달 없음) + 컬럼 클릭 정렬(이름/시대/가격/등록일/상태) 추가 — "카테고리 이동/수정 시 데이터가 연동 안 됨" 신고를 직접 해결. `AdminPostsBoardView.tsx`(사일로상점/살롱데상/스튜디오/마이페이지/기타 5개 탭이 공유)에 게시글 소속 게시판(카테고리) 인라인 이동 select + 정렬 옵션(최신순/오래된순/좋아요순) 추가. **다음 단계로 명시적으로 남김**: 실제 React Data Grid/가상 스크롤 라이브러리 도입, 진짜 다중 컬럼 동시 정렬 — 이번 화면 규모(도메인당 20건 페이지네이션)에는 클릭 한 번 즉시 반영되는 단일 정렬 기준으로도 실질적 요구가 충족된다고 판단해 새 의존성 도입은 보류.
- **검증**: `npx tsc --noEmit` 0 errors, `npm run lint` 0 errors(신규 warning 없음). 로컬 dev에서 스크린샷과 동일한 UUID URL로 실제 접속해 `window.location.href`가 `/boards/treasures/urin-02`로 리다이렉트되는 것 확인, 2400px 초광폭 뷰포트에서 갤러리 그리드의 `grid-template-columns`가 `repeat(auto-fill, minmax(216px, 288px))`로 적용되어 칸 너비가 고정 상한을 유지하는 것 확인. **다음 세션에서 확인 필요(관리자 로그인 세션)**: `/admin/posts/shop`·`/admin/posts/silostore` 등에서 실제로 카테고리/게시판 이동 select를 눌러 저장 왕복.
- **다음 단계(이번 범위 밖으로 명시적으로 남김, 순차 진행 예정)**: 2.1 타임라인 교차형(Alternating) 카드 리디자인 + 스크롤 애니메이션, 2.2 캘린더 hover 확대 + dnd-kit 드래그앤드롭 일정 이동, 3.1 관리자 실시간 프리뷰(드래그앤드롭 레이아웃) 에디터, 3.2 에디터 투표/테이블/슬래시 메뉴 추가.
- **변경 파일**: `src/app/boards/[board_slug]/[post_slug]/page.tsx`, `src/app/boards/[board_slug]/page.tsx`, `src/components/modules/GalleryModule.tsx`, `src/app/admin/posts/shop/page.tsx`, `src/components/admin/AdminPostsBoardView.tsx`.

## 2026-08-12 (EPIC-095 — Data Drift 재동기화 + Frictionless Collection UX)
- **1.1 SSoT 재동기화**: Management API로 라이브 `site_navigations`를 재조회한 결과, 예상과 반대 방향의 Data Drift를 발견했다 — "온라인 도슨트"는 이미 `parent_id: null`인 완전히 독립된 최상위 탭이었고(사일로상점 하위 그룹이라는 전제 자체가 옛 코드/문서 쪽 드리프트), 라이브는 그 사이 `navConfig.ts`/`docs/database-schema.sql`과 무관하게 계속 진화해 있었다(About Silo 최상위 탭 신설, 사일로상점/살롱데상 href 변경, 도슨트 하위 트리가 `/docent/<era>` 평면 구조에서 `/online-docent/<시대구간>/<era>` 2단 구조로 재편 등). **사용자 승인 후 실행한 유일한 DB 쓰기**: 그 노드의 `key`가 비어있어(`null`) 상단 탭 활성화 하이라이트(`getActiveNavTabKey()` ↔ `tab.key` 비교)가 전혀 동작하지 않던 것을 발견해 `key='docent'`로 채웠다(단일 컬럼 UPDATE, 구조 변경 없음). `FALLBACK_NAV_TABS`(`src/lib/navConfig.ts`)와 `docs/database-schema.sql`의 `site_navigations` 시드 블록(기존엔 라이브에 한 번도 적용된 적 없는 가상의 CTE였음, EPIC-080에서 이미 확인된 사실)을 scratchpad 스크립트로 라이브 덤프를 그대로 기계적으로 생성해 교체했다(id 보존, 총 125행). `getActiveNavTabKey()`가 `/docent`·`/online-docent` 둘 다 새 `"docent"` 탭으로 매핑하도록 수정, 더 이상 안 쓰는 `category` 쿼리 파라미터 분기와 `useSearchParams()` 호출을 Navbar.tsx에서 제거.
- **1.2 Frictionless Archiving — "내 컬렉션에 담기"**: `POST /api/collections`(신설) — `member_collections`(book/movie/music/artist/place/scent/brand/era 8종)에 own-row insert, category 화이트리스트를 서버에서도 강제. `CollectButton`(`src/components/common/CollectButton.tsx`, 신설) — 클릭 시 8개 카테고리 칩 팝오버를 열어 호출부가 추정한 카테고리(`guessDocentCollectionCategory`/`guessPostCollectionCategory`, `src/lib/collectionCategory.ts` 신설)를 강조 표시해두고 한 번 더 클릭하면 확정("자동 매핑 + 원클릭 확정" — 일반 게시글은 8종 어디에도 결정적으로 대응하지 않아 완전 자동 삽입 대신 이 방식을 택함). 게시글 상세(`PostFloatingActionBar`)와 도슨트 뷰어(`/docent/[id]`)에 부착 — 도슨트는 `docent_contents.era`/`figure_name`(API route에 select 추가)을 그대로 근거로 쓴다.
- **1.2 JSON Block 스크랩 확장성**: `POST /api/collections`의 `title`/`description`/`imageUrl` 페이로드는 이미 "무엇을 담을지"를 호출부가 결정해서 보내는 구조라, 글 전체든(현재) 향후 JSON 블록 에디터의 특정 노드(인용구 텍스트/단일 이미지)든 이 계약을 그대로 재사용할 수 있다 — 별도 컬럼/스키마 변경 없이 대비만 마련.
- **검증**: `npx tsc --noEmit` 0 errors. `npm run lint` 0 errors(신규 warning 없음 — `getActiveNavTabKey` 시그니처 변경으로 없어질 뻔한 unused-var 경고는 파라미터/호출부를 함께 정리해 애초에 발생시키지 않음). `git stash`로 EPIC-095 변경분을 통째로 제외한 순수 베이스라인에서도 로컬 dev 상단 내비게이션이 렌더링되지 않는 것을 확인해, 이 세션의 헤드리스 브라우저 환경 자체의 기존 이슈(원인 미상 — Supabase 호출 타이밍/네트워크 관련으로 추정)이지 이번 변경으로 인한 회귀가 아님을 격리 확인했다. **다음 세션에서 확인 필요**: 실제 브라우저(관리자 로그인 세션 불필요)로 상단 탭에 "온라인 도슨트"가 별도 탭으로 뜨고 하이라이트되는지, 게시글 상세/도슨트 뷰어의 "내 컬렉션에 담기" 클릭 → 카테고리 선택 → `/mypage` "나의 컬렉션"에 실제로 반영되는지.
- **변경 파일**: `docs/database-schema.sql`, `src/lib/navConfig.ts`, `src/components/Navbar.tsx`, `src/app/api/collections/route.ts`(신설), `src/components/common/CollectButton.tsx`(신설), `src/lib/collectionCategory.ts`(신설), `src/components/boards/PostFloatingActionBar.tsx`, `src/app/boards/[board_slug]/[post_slug]/PostDetailClient.tsx`, `src/app/docent/[id]/page.tsx`, `src/app/api/docent-contents/[id]/route.ts`.

## 2026-08-12 (EPIC-094 — [Stage 3] S-Class PC/Mobile Architecture & Real-Time Preview)
- **1.1 Headless API**: `GET /api/homepage` 신설 — `site_settings.hero_slideshow`(EPIC-092부터 이미 `{pc, mobile}`로 분리돼 있던 값)와 홈 Page Builder 모듈을 하나의 JSON(`pc_config`/`mobile_config`/`modules`)으로 내려준다(웹은 이 통합 응답을 그대로 캐시하고 기기 구분은 CSS 미디어 쿼리에 맡긴다는 전제). `?device=app` 쿼리를 주면 `mobile_config`만(+ `modules`) 반환하는 경량 응답으로 분기 — 향후 네이티브 앱이 PC 레이아웃 데이터 없이 가볍게 호출할 수 있게 선제 구축.
- **1.2 Art Direction 이미지**: PC/모바일용 이미지를 각각 업로드해 따로 저장하는 기능 자체는 EPIC-092의 `{pc, mobile}` 슬라이드 분리로 이미 구현돼 있었다 — 이번엔 그 렌더링 방식의 결함을 고쳤다. 기존엔 `page.tsx`가 PC/모바일용 `<HeroSlideshow>`를 각각 렌더링하고 `hidden md:block`/`md:hidden`으로 "숨기기"만 했는데, `loading="eager"+fetchPriority="high"`인 슬라이드 0번 이미지는 CSS로 숨겨도 브라우저가 그대로 내려받아 두 기기 이미지를 전부 받는 낭비가 있었다. `HeroSlideshow.tsx`의 슬라이드 이미지를 `<picture>`로 감싸 반대쪽 기기의 media에서는 실제 이미지 대신 투명 1x1 픽셀을 받도록 소스를 뒤집어 끼웠다(`device` prop 추가: `"pc" | "mobile" | "both"` — Page Builder의 별도 `hero` 모듈처럼 기기 분기가 없는 호출부는 `"both"`로 트릭을 끈다). 배경(wallpaper)은 CSS `background-image`라 `display:none` 조상 아래서는 애초에 다운로드되지 않아 손대지 않음.
- **1.3 실시간 모바일 프리뷰어**: "홈페이지 설정 관리"(`/admin/navigation/settings`)를 2단 스플릿으로 개편 — 좌측은 기존 폼 그대로, 우측(`xl` 이상에서만, sticky)에 아이폰 프레임 CSS 컨테이너(`src/components/admin/MobilePreviewFrame.tsx`, 신설)를 추가해 모바일 탭의 슬라이드/로고 값을 별도 fetch·새로고침 없이 그대로 React state로 실시간 렌더링한다. `React.memo` + `useMemo`(첫 유효 모바일 슬라이드 계산이 `heroSlideshow.mobile` 참조가 실제로 바뀔 때만 재계산되도록)로 PC 탭 편집 중에는 프리뷰 트리가 불필요하게 재계산/재렌더링되지 않는다.
- **검증**: `npx tsc --noEmit`/`npm run lint` 0 errors(신규 warning 없음 — 기존에 있던 `no-img-element` 비활성화 주석 1개는 `<picture>`로 감싸며 더 이상 필요 없어져 제거). 로컬 dev에서 `/api/homepage`와 `/api/homepage?device=app` 응답 형태 확인, 데스크톱(1280px)/모바일(375px) 뷰포트 각각에서 `document.querySelectorAll('picture')`로 반대쪽 기기 인스턴스의 `img.currentSrc`가 실제로 투명 픽셀로 치환되고 해당 기기 인스턴스만 실제 이미지 URL을 받는 것 확인. **다음에 확인 필요(관리자 로그인 세션)**: `/admin/navigation/settings`의 우측 실시간 프리뷰 패널이 실제로 폼 입력에 맞춰 즉시 갱신되는지 육안 확인(이 세션은 비로그인이라 관리자 페이지 자체는 열지 못함 — 코드/타입 검증만 완료).
- **변경 파일**: `src/app/api/homepage/route.ts`(신설), `src/components/HeroSlideshow.tsx`, `src/app/page.tsx`, `src/components/modules/PageModuleRenderer.tsx`, `src/components/admin/MobilePreviewFrame.tsx`(신설), `src/app/admin/navigation/settings/page.tsx`.

## 2026-08-12 (HOTFIX-102/103 — "이 게시판을 사용 중인 페이지" 추가/삭제 + 타임라인 꾸미기 설정 확장)
- **HOTFIX-102: "이 게시판을 사용 중인 페이지"에서 직접 추가/삭제**: 지금까지 읽기 전용으로 목록만 보여주던 것을, 각 항목에 "연결 해제" 버튼(해당 `page_modules` 행 삭제)과, 목록 아래 페이지 선택 드롭다운 + "연결 추가" 버튼(선택한 페이지에 이 게시판을 새 `board` 위젯으로 연결, "Category" 섹션이 쓰는 것과 동일한 insert 패턴)을 추가했다. 여러 페이지에 동시에 노출하고 싶을 때(허브 페이지 + 이 게시판 전용 페이지 등) "Category (사이트 메뉴 위치)"의 단일 브랜치 배정과 별개로 자유롭게 관리할 수 있다.
- **HOTFIX-103: 타임라인 "아직도 구리다" 피드백 — 꾸밀 수 있는 설정 확장**: "타임라인 설정"에 3개 추가 — (1) 선 굵기(px), (2) 마커 크기(px), (3) 미리보기 카드 테마(라이트/다크 — 다크는 참고 이미지의 빨간 라인 예시가 쓰는 검은 카드 스타일을 그대로 재현, 텍스트 색도 자동으로 반전). 기본값 자체도 더 존재감 있게 올렸다(선 1px→2px, 마커 12px→14px, 카드 모서리/그림자를 `rounded-xl shadow-xl`로 더 뚜렷하게) — 설정을 하나도 안 건드려도 이전보다 조금 더 나아 보이도록.
- **검증**: `npx tsc --noEmit`/`npm run lint` 0 errors(신규 warning 1건 — `loadLinkedPages()`를 effect 안에서 부르는 것, 이 파일에 이미 있던 동일 클래스 패턴). 로컬 dev에서 "사일로 타임라인" 게시판에 다크 테마/선 3px/마커 18px를 임시로 넣어 실제 DOM(카드 클래스 `bg-gray-900`+제목 `text-white`, 선 `width:3px`, 마커 `width/height:18px`)으로 반영 확인 후 테스트 값 원상복구. 페이지 추가/삭제 UI는 `/admin/boards/[id]`가 관리자 로그인 필요라 로컬에서 직접 클릭 검증은 못 함(코드는 기존에 이미 검증된 동일 insert/delete 패턴 재사용).
- **변경 파일**: `src/components/admin/BoardForm.tsx`, `src/components/TimelineView.tsx`, `src/components/boards/renderers/TimelineRenderer.tsx`, `src/components/boards/renderers/types.ts`, `src/components/modules/BoardModule.tsx`, `src/components/modules/DbFeedModules.tsx`, `src/lib/useBoardData.ts`, `src/lib/useBoardPosts.ts`, `src/app/admin/boards/[id]/page.tsx`, `src/app/admin/boards/new/page.tsx`.

## 2026-08-12 (HOTFIX-101 — "추가로 노출할 게시판 선택"을 사이트 메뉴와 동일한 순서로 정렬)
- **사용자 피드백**: HOTFIX-099가 카테고리 문자열 가나다순으로 정렬했는데, 사용자가 원한 건 "사이트 구성 관리 → 사이트 메뉴"에 보이는 실제 트리 순서(상단 탭 순서 → 그 아래 하위 항목들)였다.
- **수정**: `groupBoardsByCategory()`(가나다순 카테고리 그룹핑)를 제거하고, `CategoryTreeManager.tsx`/`CategoryBoardPicker`가 이미 쓰는 `buildAdminTree()`(`src/lib/adminTreeGrouping.ts`, `site_navigations` 트리를 SSoT로 쓰는 함수)를 `WriteBoardForm.tsx`/`edit/page.tsx`의 "추가로 노출할 게시판 선택" 체크박스 목록에도 그대로 적용 — 이미 두 파일 다 `boardBranches`/`boardBranchMap`을 이 목적으로 fetch하고 있었으므로(기존 "게시될 페이지 선택" 피커용) 새 조회 없이 재사용만 하면 됐다. 브랜치(상위 메뉴) 헤더도 깊이(depth)만큼 들여써서 실제 트리 구조가 그대로 보인다.
- **검증**: `npx tsc --noEmit`/`npm run lint` 0 errors. 로컬 dev에서 실제로 "About Silo → Silo Timeline/Silo daily/에단의 블루노트/사일로의 취향 → 사일로의 전시" → "사일로 상점 → 사일로 보물들 → ..." → "온라인 도슨트 → 고대~왕정 → 르네상스/바로크/로코코" 순으로, `/admin/site-structure`의 실제 사이트 메뉴 순서·계층과 정확히 일치하는 것을 DOM으로 확인.
- **부가 확인(같은 세션)**: 사용자가 "글 번호/작성자 이름 스타일은 어디서 설정하냐"고 물어 실제 배포 상태를 관리자 세션으로 직접 확인 — HOTFIX-099가 이미 각 게시판의 "게시판 수정"(`/admin/boards/[id]`) 화면의 "게시물 출력방식 — 날짜/작성자 스타일" 섹션(접혀있는 `<details>`) 안에 정상 배포돼 있음을 확인함(코드 변경 없음, 위치 안내만 필요했던 케이스).
- **변경 파일**: `src/lib/useBoardOptions.ts`, `src/components/boards/WriteBoardForm.tsx`, `src/app/boards/[board_slug]/[post_slug]/edit/page.tsx`.

## 2026-08-12 (HOTFIX-100 — 관리자 작성자 변경 "permission denied" 수정 + 타임라인 컬러 테마/정렬-마커 연동)
- **1. 관리자 작성자 변경이 "글 수정에 실패했어요. (permission denied for table posts)"로 실패하던 문제 수정**: 원인은 애플리케이션 로직이 아니라 DB 권한이었다 — 이 프로젝트의 `posts` UPDATE는 행 단위 RLS 대신 컬럼별 GRANT로 제어되는데(`docs/database-schema.sql`/CLAUDE.md 참고), `title`/`body`/`board_id` 등 대부분 컬럼은 `authenticated`에게 UPDATE 권한이 있었지만 **`author_id`만 빠져 있었다** — HOTFIX-099가 이 컬럼을 SET절에 포함시키자마자(요청자가 실제 관리자인지와 무관하게) Postgres가 요청 전체를 거부했다. `grant update (author_id) on posts to authenticated;`를 Management API로 실행해 확인(`docs/sql/HOTFIX-100-posts-author-id-grant.sql`). 작성자 변경 가능 여부 자체는 여전히 `PATCH` 라우트가 `requester.member.is_admin`을 서버에서 재확인하고 대상 회원 실존 여부까지 확인한 뒤에만 적용하므로 이 GRANT로 새로운 보안 구멍이 생기지 않는다.
- **2. 타임라인을 참고 이미지(Common Ninja)에 더 가깝게 — 게시판별 컬러 테마**: `boards.widget_settings.timelineAccentColorHex`를 추가해 게시판마다 선/마커 색을 다르게 지정할 수 있다(`BoardForm.tsx`의 "타임라인 설정"에 색상 입력 필드 추가). 기본값은 기존 회색 그대로.
- **3. 정렬(왼쪽/가운데/오른쪽)에 마커도 따라가도록 수정**: HOTFIX-098로 "게시물 출력방식 → 정렬 위치"가 실제로 타임라인 텍스트에 반영되기 시작했는데, 마커(●)와 세로선은 항상 왼쪽에 고정돼 있어 가운데/오른쪽 정렬을 켜면 마커와 텍스트가 따로 노는 것처럼 보였다(사용자 실사용 스크린샷으로 확인). `TimelineView.tsx`에 `align` prop을 추가해 세로형의 선/마커/컨텐츠 패딩이 텍스트 정렬을 그대로 따라가도록 했다(가운데=선을 행 중앙으로, 오른쪽=선을 오른쪽으로 이동 + 미리보기 카드도 왼쪽으로 열리게 반전). 별도의 새 설정 필드를 만들지 않고 기존 "정렬 위치" 값을 그대로 재사용한다(같은 게시판 메타 정렬 설정 하나로 통일).
- **검증**: `npx tsc --noEmit`/`npm run lint` 0 errors(신규 warning 없음, 처음 도입 시 이스케이프 안 된 큰따옴표 JSX 오류 2건은 즉시 고쳐 반영). 로컬 dev에서 "사일로 타임라인" 게시판에 `timelineAccentColorHex:"#166534"`를 임시로 넣어 마커 테두리/선 색이 실제로 그 색으로 바뀌는 것, 이미 저장돼 있던 `position:"center"`에 맞춰 마커가 행 중앙(컨테이너 너비 1200px 기준 중심 632px)으로 이동한 것을 `getComputedStyle`/`getBoundingClientRect()`로 확인 후 테스트용 색상 값은 원상복구.
- **변경 파일**: `docs/sql/HOTFIX-100-posts-author-id-grant.sql`(신설, 실행 완료), `src/components/TimelineView.tsx`, `src/components/boards/renderers/TimelineRenderer.tsx`, `src/components/boards/renderers/types.ts`, `src/components/modules/BoardModule.tsx`, `src/components/modules/DbFeedModules.tsx`, `src/lib/useBoardData.ts`, `src/lib/useBoardPosts.ts`, `src/components/admin/BoardForm.tsx`, `src/app/admin/boards/[id]/page.tsx`, `src/app/admin/boards/new/page.tsx`.

## 2026-08-12 (HOTFIX-099 — 관리자 작성자 변경, 글 번호/작성자 이름 스타일 세분화, "추가로 노출할 게시판" 카테고리 정렬)
- **1. 관리자가 게시글 작성자를 다른 회원으로 바꿀 수 있게**: 수정 화면(`edit/page.tsx`)에서 관리자에게만 "작성자 (관리자 전용)" 검색 필드를 새로 노출한다(`PostForm.tsx`의 `AdminAuthorPicker`) — 이름/이메일로 검색(기존 `/admin/members` 화면이 쓰던 `GET /api/admin/members?q=`를 그대로 재사용, 새 API 없음)해 회원을 고르면 저장 시 `posts.author_id`가 바뀐다. 서버(`PATCH .../posts/[post_slug]`)는 `requester.member.is_admin`을 다시 확인하고, 넘어온 `authorId`가 실제 `members` 테이블에 존재하는 회원인지 확인한 뒤에만 적용한다(위조 방지, `createdAt` 관리자 오버라이드와 동일한 패턴).
- **2. 게시글 상세 — 글 번호/작성자 이름 스타일을 날짜와 별도로 지정**: `boards.widget_settings.postMetaStyle`이 지금까지 날짜(크기/색상)+굵기/정렬만 지원하고 글 번호("No. X")·작성자 이름은 전혀 커스텀할 수 없었다 — `postNumberSizePx`/`postNumberColorHex`, `authorNameSizePx`/`authorNameColorHex` 4개 필드를 추가(굵기/정렬은 셋이 공유, 서로 다른 정렬을 가질 상황은 상정하지 않음). `PostDetailHeader.tsx`에 `postNumberStyleToCss`/`authorNameStyleToCss` 추가, `BoardForm.tsx`의 "게시물 출력방식" 섹션에 입력 필드 4개 추가.
- **3. "추가로 노출할 게시판 선택" 카테고리별 정렬**: `WriteBoardForm.tsx`/`edit/page.tsx` 둘 다 DB 조회 순서(`sort_order`) 그대로 나열돼 카테고리가 뒤섞여 있었다 — `useBoardOptions.ts`에 `groupBoardsByCategory()`를 추가해 카테고리(가나다순, 미분류는 맨 뒤) → 이름(가나다순)으로 정렬하고, 체크박스 그리드 안에 카테고리 라벨 행을 끼워 넣었다(`display:contents` wrapper로 그리드 컬럼 정렬 유지).
- **검증**: `npx tsc --noEmit` 0 errors, `npm run lint` 0 errors(신규 warning 1건 — `AdminAuthorPicker`의 디바운스 검색 effect, 이 저장소에 이미 있는 동일 패턴과 동급이라 기존 정책상 비차단). 로컬 dev에서 `/write?boardId=greeks`를 열어 "추가로 노출할 게시판 선택"이 실제로 카테고리 알파벳순(마지막에 "미분류")으로 그룹핑되는 것을 DOM으로 확인. **관리자 로그인 세션 필요해 다음에 확인**: 작성자 검색·변경·저장 왕복, 글 번호/작성자 이름 스타일 저장 후 게시글 상세 화면 반영.
- **변경 파일**: `src/lib/useBoardOptions.ts`, `src/components/boards/WriteBoardForm.tsx`, `src/app/boards/[board_slug]/[post_slug]/edit/page.tsx`, `src/components/boards/PostForm.tsx`, `src/app/api/boards/[board_slug]/posts/[post_slug]/route.ts`, `src/components/boards/PostDetailHeader.tsx`, `src/components/admin/BoardForm.tsx`, `src/app/admin/boards/[id]/page.tsx`, `src/app/admin/boards/new/page.tsx`.

## 2026-08-12 (HOTFIX-098 — "타임라인 정렬을 가운데로 했는데 아무것도 안 바뀌어" 수정)
- **원인**: "게시물 출력방식 — 날짜/작성자 스타일"(정렬 위치 포함, HOTFIX-093-B) 섹션이 `BoardForm.tsx` 안에서 게시판 전체 설정처럼 보이지만, 실제로는 **게시글 상세 페이지**(`PostDetailHeader.tsx`)에만 적용되고 목록/위젯(타임라인 포함) 쪽에는 전혀 반영되지 않고 있었다. 실제 라이브 DB 확인 결과 "사일로 타임라인" 게시판의 `widget_settings.postMetaStyle.position`이 이미 `"center"`로 저장돼 있었는데(사용자가 실제로 저장한 값), 타임라인 렌더러가 이 값을 아예 읽지 않아 화면이 그대로였던 것.
- **수정**: `PostDetailHeader.tsx`의 `metaStyleToCss`를 export해 재사용 — `TimelineRenderer.tsx`의 `renderTimelinePostLabel`/`renderTimelinePostPreview`가 이제 `boards.widget_settings.postMetaStyle`(크기/색상/굵기/정렬)을 라벨과 미리보기 카드 텍스트에 그대로 적용한다. `BoardModule.tsx`(게시판 직접 렌더)와 `DbFeedModules.tsx`의 `DbTimelineModule`(Page Builder Timeline 위젯) 둘 다 이 값을 게시판에서 읽어 전달하도록 배선(HOTFIX-097의 timelineOrientation/timelineShowPreview와 동일 경로). `BoardForm.tsx`의 실시간 미리보기 패널에도 반영.
- **범위**: 사용자가 실제로 신고한 타임라인만 수정했다 — Story/Community/Gallery 등 다른 목록 Renderer들도 같은 이유로 이 설정을 무시하고 있지만(같은 폼 안에 있어 게시판 전체 설정으로 오해하기 쉬운 UX 문제), 이번엔 범위를 넓히지 않았다.
- **검증**: `npx tsc --noEmit`/`npm run lint` 0 errors(신규 warning 없음). 로컬 dev에서 실제로 `postMetaStyle.position: "center"`가 저장돼 있는 "사일로 타임라인" 게시판(`/about-silo/silo-timeline`)을 열어 라벨의 `getComputedStyle(...).textAlign`이 `"center"`로 실제 반영됨을 확인.
- **변경 파일**: `src/components/boards/PostDetailHeader.tsx`, `src/components/boards/renderers/types.ts`, `src/components/boards/renderers/TimelineRenderer.tsx`, `src/components/modules/BoardModule.tsx`, `src/components/modules/DbFeedModules.tsx`, `src/lib/useBoardData.ts`, `src/lib/useBoardPosts.ts`, `src/components/admin/BoardForm.tsx`.

## 2026-08-12 (HOTFIX-097 후속 — dev.silostore.net 실사용 검증 중 발견: 세로형 hover 미리보기 카드 위치 버그)
- **발견 경위**: 위 HOTFIX-097을 배포 후 `dev.silostore.net`에서 직접 hover해보며 검증하던 중 — 미리보기 카드가 라벨(날짜+제목) 바로 옆이 아니라 한참 떨어진(행 전체 폭 기준 오른쪽 끝) 위치에 떠서, 넓은 화면에서는 사실상 화면 밖으로 나가 안 보였다.
- **원인**: `TimelineView.tsx`의 세로형(`VerticalRow`)에서 미리보기 카드에 쓴 `absolute left-full`이 라벨 텍스트 자체가 아니라 그 바깥의 **호버 판정용 풀-너비 행(row)** 을 기준으로 계산되고 있었다 — 그 행은 hover 배경 강조를 위해 일부러 폭 전체를 차지하게 만들어둔 것.
- **수정**: 라벨(renderItem 결과)만 감싸는 `relative inline-block`(내용 폭만큼만 줄어듦) 래퍼를 추가하고, 미리보기 카드를 그 래퍼 기준으로 `left-full` 배치하도록 변경 — 이제 라벨 텍스트 길이와 무관하게 항상 라벨 바로 오른쪽 12px 지점에 붙는다. 가로형(`HorizontalTimeline`)은 원래도 좁은 고정폭(`w-40`) 컨테이너 기준이라 이 문제가 없었음(수정 불필요, 확인만 함).
- **검증**: `npx tsc --noEmit`/`npm run lint` 0 errors. 로컬 dev에서 hover 후 라벨 우측 끝과 미리보기 카드 좌측 끝 사이 간격을 `getBoundingClientRect()`로 직접 측정해 12px(`ml-3`)로 정확히 붙어있음을 확인(수정 전에는 이 gap이 수백 px였음).
- **변경 파일**: `src/components/TimelineView.tsx`.

## 2026-08-12 (HOTFIX-097 — 타임라인 게시글 클릭 오류 수정 + Common Ninja 스타일 재설계(가로/세로 + hover 미리보기) + 관리자 설정)
- **1. "게시글을 찾을 수 없어요" 클릭 버그 수정**: "사일로 타임라인" 게시판(page_builder Timeline 위젯)에서 게시글을 클릭하면 항상 이 오류가 났다 — 재현해보니 그 글의 **주 게시판은 "silo-daily"**였고 "사일로 타임라인"에는 `post_boards`(HOTFIX-093-B, N:M 다중 게시판)로만 "추가 노출"돼 있었다. 목록 조회(`GET /api/boards/[board_slug]/posts`)는 이미 이 cross-post 합집합을 반영해 보여주는데, **상세 조회**(`GET .../posts/[post_slug]`)는 `board_id` 정확히 일치만 확인해 못 찾고 404를 반환하고 있었다. `fetchCrossPostedPostIds`(이미 `src/lib/postBoards.ts`에 있던 헬퍼)로 이 게시판에 cross-post된 글 id 목록을 함께 조회해, `slug` 매칭 시 `board_id` 일치 **또는** cross-post id에 포함이면 찾도록 수정(목록 라우트의 기존 `.or()` 패턴과 동일). 로컬 dev에서 실제로 열리는 것 확인, 일반(cross-post 없는) 게시글도 회귀 없이 정상 동작 확인.
- **2. 타임라인 디자인 재설계(사용자 참고 이미지: Common Ninja 스타일)**: 항상 보이던 썸네일+제목+메타 전체를 라인 위에 그리던 기존 방식을, "선 위에는 날짜+제목만 보이는 얇은 라벨, hover하면 썸네일+본문 일부(excerpt)+날짜+메타(작성자/좋아요/조회/댓글)를 담은 카드가 옆으로 떠오르는" 방식으로 바꿨다. `src/components/TimelineView.tsx`에 `renderPreview`(선택) prop을 추가 — 안 넘기면(마이페이지 활동 로그 등 썸네일/본문이 없는 항목) 기존과 동일하게 라벨만 보임(하위 호환). `src/lib/htmlExcerpt.ts` 신설(HTML 태그 제거 + 길이 제한 텍스트 추출).
- **3. 가로형/세로형 둘 다 지원**: `TimelineView`에 `orientation` prop("vertical"/"horizontal") 추가 — 세로형은 기존 연/월 그룹핑 + 좌측 선 + 마커, 가로형은 전체 기간을 하나의 가로 스크롤 라인으로 펼쳐 마커 위/아래로 항목이 교차 배치되는 신규 레이아웃(중앙선, hover 카드는 위/아래 바깥쪽으로 뜸).
- **4. "여러 기능을 수정할 수 있는 '수정' 기능"**: 갤러리 설정(HOTFIX-095)과 동일한 위치·패턴으로 `BoardForm.tsx`(`/admin/boards/[id]`, `/admin/boards/new`)에 "타임라인 설정" 섹션을 추가 — `render_type="timeline"`일 때만 노출되며, 배치 방향(세로형/가로형)과 hover 미리보기 카드 사용 여부를 `boards.widget_settings.timelineOrientation`/`timelineShowPreview`로 저장한다. 미리보기 패널에도 즉시 반영.
- **아키텍처**: `renderTimelinePostLabel`/`renderTimelinePostPreview`(`TimelineRenderer.tsx`에서 export)를 게시판 렌더러(`TimelineRenderer.tsx`, `boards.render_type="timeline"`)와 Page Builder Timeline 위젯(`DbFeedModules.tsx`의 `DbTimelineModule`) 둘 다 그대로 재사용 — 마크업이 두 곳에 따로 존재하지 않는다(GalleryModule을 두 컨텍스트가 공유하는 기존 관례와 동일). 두 컨텍스트 모두 "이 위젯이 가리키는 게시판"의 `widget_settings`에서 설정을 읽으므로, 게시판 하나의 설정 화면(`/admin/boards/[id]`)만 고치면 어디서 렌더링되든 동일하게 반영된다.
- **검증**: `npx tsc --noEmit` 0 errors, `npm run lint` 0 errors(52 warnings, 전부 이 저장소에 이미 있던 `react-hooks/set-state-in-effect` 사전 존재 패턴 — 신규 없음). 로컬 dev에서 `/about-silo/silo-timeline`(위 1번 버그의 실제 게시판) — 클릭 시 정상 열림, hover 시 미리보기 카드의 `opacity`가 0→1로 실제 전환됨을 DOM으로 확인, `widget_settings.timelineOrientation`을 Management API로 일시적으로 `"horizontal"`로 바꿔 가로형 레이아웃(가로 스크롤 컨테이너 + 8개 항목)이 정상 렌더링되는 것 확인 후 원상복구(`{}`)함.
- **변경 파일**: `src/app/api/boards/[board_slug]/posts/[post_slug]/route.ts`, `src/components/TimelineView.tsx`, `src/components/boards/renderers/TimelineRenderer.tsx`, `src/components/boards/renderers/types.ts`, `src/components/modules/BoardModule.tsx`, `src/components/modules/DbFeedModules.tsx`, `src/lib/useBoardData.ts`, `src/lib/useBoardPosts.ts`, `src/lib/htmlExcerpt.ts`(신설), `src/components/admin/BoardForm.tsx`, `src/app/admin/boards/[id]/page.tsx`, `src/app/admin/boards/new/page.tsx`.

## 2026-08-11 (HOTFIX-096 — 상단 GNB 메가메뉴에서 하위 항목 없는 그룹에도 "›" 화살표+빈 플라이아웃이 뜨던 문제 수정)
- **증상**: "사일로 상점" 상단 탭을 hover하면 뜨는 메가메뉴에서 "Silo's old Story 사일로의 옛날이야기" 항목 오른쪽에 정체 모를 빈 칸이 보인다는 스크린샷 보고 — 실제로는 그 항목에 hover 시 나타나는 2차 플라이아웃(자식 메뉴 박스)이 비어 있었던 것.
- **근본 원인**: `Navbar.tsx`의 `tab.groups` 렌더링 분기가 각 그룹의 "›" chevron과 2차 플라이아웃을 `group.items.length`와 무관하게 항상 렌더링하고 있었다 — DB(`site_navigations`) 조회 결과 "Silo's old Story"는 실제 자식 노드가 0개(형제인 "사일로 보물들"은 5개, "사일로 유산 Heritage"는 2개)라, hover하면 내용 없는 빈 흰색 박스만 떴다. 바로 아래 `tab.items` 분기는 이미 `item.children.length > 0`으로 조건 처리돼 있어 동일 파일 안에서도 일관되지 않았던 코드였다.
- **수정**: `group.items.length > 0`(`hasItems`)을 계산해 chevron과 2차 플라이아웃 둘 다 이 조건으로 감쌌다 — 자식이 없는 그룹은 이제 화살표 없이 순수 링크로만 보이고, hover해도 빈 박스가 뜨지 않는다. `aria-haspopup`도 실제 하위 메뉴가 있을 때만 `"true"`로 설정하도록 함께 정리.
- **검증**: `npx tsc --noEmit`/`npm run lint` 0 errors(신규 warning 없음, 파일에 이미 있던 무관한 사전 경고 1건 유지). 로컬 dev에서 "사일로 상점" 메가메뉴를 열어 DOM으로 직접 확인 — "Silo's old Story"는 `›` 없음, "사일로 보물들"/"사일로 유산 Heritage"는 `›` 그대로 유지.
- **변경 파일**: `src/components/Navbar.tsx`.

## 2026-08-11 (HOTFIX-095 후속 — 관리자 로그인 세션 실사용 검증 중 발견한 P1: 스크랩/댓글이 HOTFIX-093-B 이후 계속 빈 화면이었던 근본 원인 수정)
- **P1 발견 경위**: 위 HOTFIX-095 1번 항목을 실제 관리자 계정으로 로그인해 검증하던 중(dev.silostore.net, 사용자가 직접 로그인) — "silo-daily" 게시글을 스크랩(북마크)해도 `/mypage/archive`에 아무것도 뜨지 않았다. `ScrapsPanel.tsx`가 에러를 확인하지 않고 `(data ?? [])`로 조용히 빈 배열 폴백하는 걸 발견하고, 브라우저에서 직접 PostgREST를 호출해 실제 에러(`PGRST201: Could not embed because more than one relationship was found for 'posts' and 'boards'`)를 확인했다.
- **근본 원인**: HOTFIX-093-B가 추가한 `post_boards`(게시글-게시판 N:M) 테이블 때문에 `posts`→`boards` 사이에 FK 경로가 2개(직접 `posts.board_id`, `post_boards` 경유)가 됐다 — PostgREST는 이 경우 `select`에 `"boards(...)"`처럼 관계를 명시하지 않으면 어느 쪽인지 모호하다며 요청 전체를 실패(HTTP 300)시킨다. `ScrapsPanel.tsx`/`CommentsPanel.tsx` 둘 다 이 `error`를 무시하고 있어서, HOTFIX-093-B가 merge된 시점부터 계속 "스크랩한 글이 없어요"/"작성한 댓글이 없어요"만 보였을 것으로 추정된다(오늘 처음 만든 증상이 아니라 이번에 우연히 발견한 기존 버그).
- **수정**: 두 파일의 `boards(...)`를 `boards!posts_board_id_fkey(...)`로 명시해 "이 글의 주 게시판"(직접 FK) 관계를 고정 — `post_boards`로 추가 연결된 부게시판이 아니라 정확히 원하던 관계로 모호함을 없앴다. 브라우저에서 동일 요청을 직접 재실행해 200 OK와 함께 실제 스크랩 2건(그 중 하나가 위 1번 항목에서 검증용으로 스크랩한 "silo-daily" 글)이 정상 반환되는 것을 확인했다.
- **검증**: `npx tsc --noEmit`/`npm run lint` 0 errors(신규 warning 없음). PostgREST 엔드포인트에 수정 전/후 쿼리를 각각 직접 호출해 300→200 전환 확인(위 근본 원인 문단 참고). **다음에 확인 필요**: 이 커밋 배포 후 `dev.silostore.net`에서 관리자 계정으로 `/mypage/archive`·`/mypage/comments`가 실제로 항목을 보여주는지 최종 확인.
- **변경 파일**: `src/components/mypage/panels/ScrapsPanel.tsx`, `src/components/mypage/panels/CommentsPanel.tsx`.

## 2026-08-11 (HOTFIX-095 — 게시글 URL의 raw board_id 노출, 사이트 메뉴 트리 기본 접힘, 갤러리 기본 레이아웃 가로 전환)
- **1. 마이페이지 "스크랩"/"내가 쓴 댓글"에서 게시글 URL이 `/boards/<uuid>/...`로 지저분하게 노출되던 문제**: 스크린샷으로 보고된 `/boards/cec162a4-4bc3-409c-8f2e-737d9986abe6/2020-9-5-sumi` 같은 URL을 실제로 재현·확인함 — 해당 게시판(`silo-daily`)은 정상적인 slug(`"silo-daily"`)를 갖고 있었는데, `src/components/mypage/panels/ScrapsPanel.tsx`와 `src/components/mypage/panels/CommentsPanel.tsx` 두 곳이 링크를 만들 때 `posts.board_id`/`c.board_id`(원시 UUID)를 그대로 썼기 때문이었다(글쓰기 폼 등 나머지 경로는 이미 `boards.slug`를 정상적으로 쓰고 있었음 — "글을 쓸 때마다"라기보다 "마이페이지에서 스크랩/댓글로 들어갈 때마다"에 더 가까운 증상이었다). 두 파일의 Supabase select에 `boards(slug)`를 추가하고, href를 `board.slug ?? board.id`로 바꿔 slug가 있으면 항상 깨끗한 URL을 쓰도록 수정(댓글의 경우 `posts.slug`도 추가해 게시글 쪽도 같이 정리). `npx tsc --noEmit`/`npm run lint` 0 errors(신규 warning 없음).
- **2. "사이트 구성 관리"의 사이트 메뉴 트리 기본 상태를 "전부 펼침"→"전부 접힘"으로 변경**: `CategoryTreeManager.tsx`의 `collapsedIds`(EPIC-088)가 빈 Set으로 시작해 모든 분기가 펼쳐진 채 로드됐다 — `load()`가 최초 1회 완료된 시점에(이후 재조회 시엔 사용자가 이미 토글한 상태를 덮어쓰지 않도록 `didInitCollapseRef`로 1회 제한) 하위 항목(nav 자식 또는 배정된 게시판)이 있는 모든 노드의 id를 `collapsedIds`에 채워 넣어 기본을 "전부 접힘"으로 바꿨다. `npx tsc --noEmit`/`npm run lint` 0 errors(이 파일에 이미 있던 사전 존재 warning 6건과 무관, 신규 없음).
- **3. 갤러리 게시판 썸네일 기본 배치를 "세로(masonry)"→"가로(grid)"로 변경**: `GalleryModule.tsx`의 `layout` prop 기본값을 `"masonry"`→`"grid"`로 변경 — 게시판별 `widget_settings.galleryLayout`을 명시적으로 지정하지 않은 모든 갤러리 게시판(`render_type="gallery"`)이 이제 기본으로 가로 채움(Grid)을 쓴다. 이미 명시적으로 `masonry`를 저장한 게시판이 있다면 그대로 세로 유지(라이브 DB 확인 결과 현재 masonry로 명시 저장된 게시판은 없었고, 여러 게시판이 이미 수동으로 grid를 저장해뒀었음 — 이번 변경으로 기본값이 그 쪽과 일치하게 됨). `BoardForm.tsx`의 "(기본값)" 드롭다운 라벨도 "세로 채움 masonry" → "가로 채움 Grid"로 갱신. `npx tsc --noEmit`/`npm run lint` 0 errors. 로컬 dev에서 `/boards/greeks`(별도 설정 없는 갤러리 게시판)를 열어 컨테이너가 `class="grid gap-4"` + 3열 `gridTemplateColumns`로 렌더링되는 것을 DOM에서 직접 확인.
- **변경 파일**: `src/components/mypage/panels/ScrapsPanel.tsx`, `src/components/mypage/panels/CommentsPanel.tsx`, `src/components/admin/CategoryTreeManager.tsx`, `src/components/modules/GalleryModule.tsx`, `src/components/admin/BoardForm.tsx`.

## 2026-08-11 (HOTFIX-094 — 게시판 목록 페이지 "페이지 수정" 버튼이 게시판마다 다른 게시판 편집 화면으로 가던 문제 수정)
- **근본 원인**: `/boards/[board_slug]/page.tsx`(모든 게시판이 공유하는 동적 라우트)의 `PageEditButton`과 위젯 아래 위젯 영역 조회가 게시판마다 실제 slug를 넘기지 않고 항상 고정 문자열 `"boards-id"`(EPIC-064A 백필 때 만든 placeholder 1개)를 썼다 — "그리스"든 다른 어떤 게시판이든 "페이지 수정"을 누르면 전부 같은 `page_builder` 행으로 이동했고, 편집 화면 제목도 그 slug 그대로 `/boards-id`로 표시됐다. HOTFIX-093-B/EPIC-093에서 이미 "재현은 됐지만 EPIC-067의 의도된 설계"로 남겨뒀던 항목인데, 실제로는 `BoardForm.tsx`의 "Category (사이트 메뉴 위치)" 기능(EPIC-084/088)이 게시판을 사이트 메뉴 분기에 배정하면 그 분기 전용 `page_builder` 행을 이미 자동으로 만들어주고 있었다 — 다만 `/boards/[board_slug]/page.tsx`가 그 결과를 조회하지 않고 계속 공용 placeholder만 썼을 뿐이었다(사용자 확인: 게시판별로 실제 분리하는 방향으로 진행).
- **수정**: `src/lib/siteTree.ts`에 `getBoardPageSlug(boardId)`를 새로 뽑아냄(기존 `getNavNodeForBoardId`가 내부에서 하던 "board 위젯으로 이 게시판을 담고 있는 전용 page_builder 페이지 slug 역추적"을 재사용 가능하게 분리). `/boards/[board_slug]/page.tsx`가 이제 URL의 board_slug → `boards.id` → `getBoardPageSlug(id)` 순으로 실제 전용 페이지 slug를 조회해 그 값으로 `PageEditButton`/위젯 영역을 렌더링한다. 아직 어떤 분기에도 배정되지 않은 게시판(전용 페이지가 없음)은 공용 placeholder로 폴백하지 않고 버튼/위젯 영역 자체를 렌더링하지 않는다 — 폴백하면 다시 여러 게시판이 같은 화면을 공유하는 원래 문제로 되돌아가기 때문.
- **범위**: 이번엔 게시판 목록 페이지(`/boards/[board_slug]`)만 고쳤다. 동일한 고정-slug 패턴이 남아있는 글쓰기 폼(`"boards-id-write"`)과 게시글 상세(`"boards-id-postid"`)는 이번 요청 범위 밖으로 남겨둠(둘 다 "게시판별 전용 화면"이 아니라 "글쓰기/게시글 상세 공통 하단 위젯 영역"이라는 의도가 더 뚜렷해 보여 별도 확인 후 처리 권장).
- **검증**: `npx tsc --noEmit`/`npm run lint` 0 errors, 신규 warning 없음. 로컬 dev(비로그인)에서 `/boards/greeks`를 열어, 위젯 영역이 더 이상 공용 "Boards Id" 대신 "그리스" 전용 hero("BC 1100~146 그리스 Greeks")를 보여주는 것으로 전용 페이지 조회가 실제로 동작함을 확인(해당 게시판은 이미 사이트 메뉴에 배정돼 있어 전용 페이지가 존재했음). 관리자 로그인 세션이 없어 "페이지 수정" 버튼 클릭 → `/admin/pages/[id]` 도착 화면 자체는 이번 세션에서 클릭 검증하지 못함.
- **변경 파일**: `src/lib/siteTree.ts`, `src/app/boards/[board_slug]/page.tsx`.

## 2026-08-11 (HOTFIX-093-B 후속 — post_boards SQL 실행 완료)
- **`docs/sql/HOTFIX-093-B-post-boards-multi.sql` 실행 완료(사용자가 Supabase SQL Editor에서 직접 실행)**: Management API로 재조회해 `post_boards` 테이블과 RLS 정책 3개(`post_boards_select`/`post_boards_insert`/`post_boards_delete`) 전부 생성됨을 확인 — 게시글 다중 게시판 소속(N:M) 기능이 이제 실제로 동작한다(그 전엔 코드의 방어적 폴백 덕분에 조용히 no-op이었을 뿐 기존 기능은 안 깨졌었음). **다음에 확인 필요**: 실제 관리자 로그인 세션으로 "추가로 노출할 게시판" 체크박스로 저장 → 양쪽 게시판 목록/캘린더에 실제로 노출되는지 클릭 검증.

## 2026-08-11 (HOTFIX-093-B — 관리자 버튼/캘린더 렌더링 버그 근본 원인 확인 + 게시판 다중 소속(N:M) + 메타데이터 스타일링)
- **1.4 "페이지 수정" 버튼이 아이콘과 겹쳐 떠다니던 문제 — 근본 원인 확인 및 수정**: EPIC-093 1차에서 "재현 못함"으로 남겨뒀던 항목을 라이브 `site_settings`를 직접 조회해 재현했다 — `sidebar_icons.topOffsetPx=100, iconSizePx=100`(왼쪽 사이드바 여닫이 아이콘이 화면 왼쪽 가장자리 y:100~216px, x:0~116px 범위를 차지)이 `PageEditButton`의 고정 좌표(`top-20`=80px, `left-4`=16px)와 정확히 겹쳤다 — 관리자가 사이드바 아이콘 크기/위치를 이 값으로 설정한 뒤부터 계속 겹쳐 있었던 것. `PageEditButton.tsx`가 이제 `site_settings.sidebar_icons`를 직접 읽어, 왼쪽 아이콘의 실제 범위와 겹칠 때만 그 아래로 동적으로 내려가도록 수정(안 겹치면 기존 `top-20` 그대로) — 관리자가 나중에 아이콘 설정을 또 바꿔도 다시 겹치지 않는다. `z-30`→`z-50`으로도 올려 어떤 경우든 항상 최상단에 그려지게 함.
- **1.4 라우팅("엉뚱한 곳으로 라우팅") — 재확인 결과 동일**: `/admin/pages/[id]/page.tsx`의 id 파라미터 처리, `PageEditButton`의 slug→page_builder id 조회 둘 다 다시 검토했지만 결함을 못 찾았다. 게시판 목록/게시글 상세가 게시판마다 개별 page_builder 행 대신 고정 slug(`"boards-id"`/`"boards-id-postid"`)로 공용 위젯 템플릿 하나를 공유하는 것은 EPIC-067의 의도된 설계다 — 이게 사용자가 "엉뚱한 페이지"로 느낀 원인일 가능성이 있다(모든 게시판의 "페이지 수정"이 같은 템플릿으로 감). 이 부분은 코드 결함이 아니라 아키텍처 특성이라 이번엔 바꾸지 않았다(바꾸려면 게시판 수만큼 개별 page_builder 행을 만드는 재설계가 필요 — 기존 위젯 배치가 있는 게시판들의 마이그레이션도 필요).
- **1.4 캘린더 위젯 "`+ 글 등록`"/필터 체크박스 미노출 — 근본 원인 확인 및 수정**: `PageBuilderRenderer.tsx`의 `CalendarFromSettings`가 `board_id`가 비어있는(아직 게시판에 연결 안 된) 캘린더 위젯 인스턴스에서는 순수 셸(`CalendarGrid`, 필터/글쓰기 버튼 없음)로 조용히 폴백하고 있었다 — 코드 자체는 존재했지만 그 화면엔 도달할 길이 없었던 것. `CalendarFromSettings`가 이제 `board_id` 유무와 무관하게 항상 `CalendarBoardWidget`을 쓰도록 변경, `CalendarBoardWidget`은 `boardId: string | null`을 받아 게시판 미연결 시에도 필터 체크박스는 항상 보여주고(작동은 안 해도 UI는 항상 노출), "+ 글 등록"은 `/write`(게시판 미지정)로 보낸다.
- **1.2 게시글 다중 게시판 소속(N:M)**: `posts.board_id`(단일)는 URL 라우팅/작성자 권한/게시판 "옮기기"/`(board_id,slug)` UNIQUE 인덱스가 전부 의존하는 핵심 컬럼이라 유지하고, "추가로 노출할 게시판" 목록만 새 매핑 테이블 `post_boards(post_id, board_id)`로 얹었다(`docs/sql/HOTFIX-093-B-post-boards-multi.sql`, RLS 포함 — 작성자 본인/관리자만 쓰기 가능, 조회는 공개). `src/lib/postBoards.ts` 신설(조회/동기화 헬퍼, 테이블이 아직 없는 환경에서도 조용히 폴백). 글쓰기(`WriteBoardForm.tsx`)/수정(`edit/page.tsx`) 화면에 "추가로 노출할 게시판 선택" 체크박스 목록 추가, 저장 API(`POST`/`PATCH /api/boards/[board_slug]/posts...`)가 `additionalBoardSlugs`를 받아 동기화. 게시판 글 목록(`GET .../posts`)과 캘린더 위젯(`CalendarBoardWidget.tsx`)이 주 게시판 글 + `post_boards`로 추가 연결된 글을 함께 보여주도록 `.or()` 쿼리로 확장(둘 다 마이그레이션 전에는 기존과 100% 동일하게 동작). **범위 밖으로 남김**: 관리자 게시글 목록(`AdminPostsBoardView.tsx`)과 다중 게시판 집계 피드(`/api/boards/feed`)는 이번에 안 건드림(Explore 서브에이전트 조사로 후보로 확인됨, CHANGELOG 다음 항목에 기록).
- **1.3 게시물 메타데이터(날짜/작성자) 스타일링**: `boards.widget_settings.postMetaStyle`(JSON: `dateSizePx`/`dateColorHex`/`fontWeight`/`position`)을 신설 — `BoardForm.tsx`에 "게시물 출력방식 — 날짜/작성자 스타일" 아코디언 추가(관리자가 게시판별로 설정, `/admin/boards/[id]`·`/admin/boards/new` 양쪽 저장 연동), `PostDetailHeader.tsx`가 이 값을 인라인 스타일로 날짜/수정일/작성자 텍스트에 적용(값이 없으면 기존 흐린 회색 기본 스타일 그대로).
- **1.1 Clean URL — 재확인**: 코드를 다시 조사한 결과 `/boards/[board_slug]/[post_slug]` 폴더 구조와 slug 기반 API 쿼리(`.eq("slug", postSlug)`)가 EPIC-079-PHASE-2 때부터 이미 존재했다(요청서가 "폐기하라"고 지시한 `/boards/[board_uuid]/[post_uuid]` 구조는 현재 코드베이스에 없음 — `src/app/api/boards/[board_slug]/posts/[post_slug]/route.ts:22-42` 참고). 대신 EPIC-093 1차에서 발견한 진짜 문제(placeholder category가 board slug에도 상속돼 `/boards/new-board-xxxxxxxx`로 노출되던 것)를 사용자가 그 SQL을 직접 실행해 해결 완료(확인됨). 폴더/쿼리를 다시 만드는 대신 이미 존재하는 구조가 요청 사양과 정확히 일치함을 코드 인용으로 재확인했다.
- **검증**: `npx tsc --noEmit`/`npm run lint` 0 errors(신규 warning 없음, 기존 51건과 동일 카테고리 1건 추가 — 이 리포지토리 전반에 이미 있는 `react-hooks/set-state-in-effect` 패턴). 로컬 dev(비로그인)에서 `/boards/general`(cross-post 유니온 쿼리가 마이그레이션 전 폴백 경로로 정상 동작), `/write?boardId=general`(체크박스 섹션 렌더링) 확인, 콘솔/서버 에러 없음. **관리자 로그인 세션 필요해 다음에 확인**: 사이드바 아이콘 겹침이 실제로 해소됐는지 스크린샷 비교, "게시물 출력방식" 저장 왕복, 다중 게시판 체크박스 저장 후 실제로 양쪽 게시판 목록에 노출되는지, `HOTFIX-093-B-post-boards-multi.sql` 실행 후 RLS 통과 여부.
- **변경 파일**: `src/components/admin/PageEditButton.tsx`, `src/components/PageBuilderRenderer.tsx`, `src/components/modules/CalendarBoardWidget.tsx`, `src/lib/postBoards.ts`(신설), `src/app/api/boards/[board_slug]/posts/route.ts`, `src/app/api/boards/[board_slug]/posts/[post_slug]/route.ts`, `src/components/boards/WriteBoardForm.tsx`, `src/app/boards/[board_slug]/[post_slug]/edit/page.tsx`, `src/components/boards/PostDetailHeader.tsx`, `src/app/boards/[board_slug]/[post_slug]/PostDetailClient.tsx`, `src/components/admin/BoardForm.tsx`, `src/app/admin/boards/[id]/page.tsx`, `src/app/admin/boards/new/page.tsx`, `docs/sql/HOTFIX-093-B-post-boards-multi.sql`(신설, 미실행).

## 2026-08-11 (EPIC-093 — [Critical Fixes] Layout Corrections, Clean URLs, & Calendar Widget Completion — 1차)
- **범위(중요)**: 요청서(1.1~1.5) 전체를 한 세션에서 검증 가능한 만큼만 처리 — 코드 리뷰로 근거를 확인할 수 있고 되돌리기 쉬운 항목만 수정했고, 근거를 못 찾은 항목("페이지 수정" 라우팅 버그)과 규모가 큰 항목(URL 구조 전면 개편/게시글-게시판 N:M/메타데이터 스타일링 UI)은 임의로 재구현하지 않고 아래에 명확히 남겼다(에러 트리아지 원칙 — 재현 못한 버그를 추측으로 고치지 않음).
- **1.4 임시 게시판 카테고리("`#new-board-xxxxxxxx`" 태그) 근본 원인 확인**: `/admin/pages/[id]`의 "+ 새 게시판 추가"(`src/app/admin/pages/[id]/page.tsx:346`)가 게시판 생성 시 `category`(→ EPIC-079-PHASE-2 백필 로직상 `slug`에도 그대로 상속)에 임시값 `new-board-<8자리>`를 채워 넣는데, 관리자가 이름(`name`)만 고치고 이 값을 그대로 두면 영구히 남는다 — 실제로 라이브 DB에서 8개 게시판이 이 상태였다(Management API로 조회 확인, 이름은 전부 정상으로 바뀌어 있었음). **프론트엔드 수정**: `src/lib/boardLayout.ts`에 `isRealBoardCategory()` 추가(이 패턴을 "진짜 카테고리"로 취급하지 않음), `BoardModule.tsx`(모든 게시판 렌더러의 단일 진입점)와 `PostDetailClient.tsx`(게시글 상세, 별도로 board를 조회) 두 choke point에 적용 — 이제 DB에 이 값이 남아있어도 태그로 노출되지 않는다(로컬 dev에서 실제로 `#new-board-*` 문자열이 화면 어디에도 없음을 DOM 텍스트로 확인). **DB 정리는 미실행**: 세션의 안전 classifier가 Management API를 통한 UPDATE를 파괴적 작업으로 분류해 자동 실행을 차단함(SELECT는 허용됨) — `docs/sql/EPIC-093-cleanup-placeholder-board-category.sql`에 8개 게시판의 `category`를 NULL로, `slug`도 임시값 대신 이름 기반(slugify, 충돌 없음을 조회로 확인)으로 되돌리는 스크립트를 준비해뒀다. **사용자가 직접 Supabase SQL Editor 또는 Management API로 실행 필요**.
- **1.3 Clean URL(부분)**: 위 조사 중 발견 — 게시판 URL(`/boards/[board_slug]`)도 이 임시 category/slug를 그대로 물려받아 `/boards/new-board-5e95b289`처럼 지저분하게 노출되고 있었다(요청서의 UUID 노출 신고와 본질이 같은 문제, 정확히는 UUID가 아니라 임시 slug). 위 SQL 스크립트에 board별 slug 재발급(이름 기반, 한글 전용 이름은 EPIC-079-PHASE-2와 동일한 계약으로 id 앞 8자리 폴백)을 포함시켰다 — 실행되면 8개 게시판 URL이 즉시 깨끗해진다. **범위 밖으로 남긴 것**: 게시글 URL 자체를 UUID/난해한 값 대신 slug로 개편하는 것은 `/boards/[board_slug]/[post_slug]`가 이미 slug 기반(EPIC-079-PHASE-2)이라 기본 골격은 존재하나, 요청서가 언급한 스크린샷 속 `/board/<uuid>/...`(단수 "board", 카탈로그 시스템과 무관한 catch-all(`[...slug]`) 경로로 만들어진 옛 개별 페이지로 추정) 같은 레거시 경로 전수 조사는 이번 세션 시간 안에 못 끝냈다. 게시글-게시판 N:M(다중 게시판 소속) 관계와 글쓰기 폼 다중 선택 체크박스는 스키마 변경(`posts`/`boards` 사이 join 테이블 또는 array 컬럼 + RLS 정책 다수)이 필요한 별도 규모의 작업이라 이번엔 손대지 않음 — 다음 EPIC에서 진행 권장.
- **1.2 컨테이너 여백 초기화**: 새로 만들어지는 모든 페이지(page_builder 기반, catch-all `src/app/[...slug]/page.tsx`가 담당)의 기본 컨테이너가 `max-w-3xl mx-auto px-6 py-12`로 화면 양옆에 고정 여백을 두던 것을, 이 catch-all에서 `p-0`/`w-full`(여백 없음, 전체 너비)로 바꿨다. 기존 138개 정적 라우트(`/boards`, `PageTemplate.tsx` 등)는 각자의 디자인 의도가 있는 별도 파일이라 이번 범위에서 제외(요청이 "현재 페이지 기본값"이라고 특정했다고 해석) — 필요하면 사용자 확인 후 확장.
- **1.1 관리자 버튼(부분)**: 게시글 상세 페이지의 버튼 라벨을 요청대로 "게시글 보여지는 방식 수정" → "게시물 출력방식"으로 변경(`PostDetailClient.tsx`). 위치는 이미 EPIC-092 때 헤더 바로 아래 좌측 상단 고정(`fixed top-20 left-4`, `#166534`)으로 옮겨져 있었고(`PageEditButton.tsx`), 오른쪽 사이드바 트리거 아이콘(`RightSidebar.tsx`)은 화면 우측 가장자리에 별도로 고정돼 있어 코드상 두 요소가 겹칠 위치 관계가 아님을 확인했다 — "플로팅 제거"가 이미 반영된 상태로 보인다. **라우팅 버그는 재현 못함**: `PageEditButton`이 게시판 목록/게시글 상세마다 고정 slug(`"boards-id"`/`"boards-id-postid"`)로 공용 템플릿 페이지를 가리키는 건 의도된 설계(EPIC-067 주석, 게시판 수만큼 개별 page_builder 행을 만들지 않고 위젯을 공유)이지 코드 결함이 아니다 — `/admin/pages/[id]/page.tsx`의 id 파라미터 처리도 정상이었다. 사용자가 실제로 겪은 "엉뚱한 페이지" 증상을 재현하려면 클릭한 정확한 URL과 도착한 URL이 필요 — 다음 세션에서 재현 스텝을 받아 조사할 것.
- **1.5 캘린더 위젯**: 코드를 다시 읽어보니 EPIC-092에서 이미 구현이 끝나 있었다(`CalendarBoardWidget.tsx`) — "+ 글 등록"(날짜 pre-fill 포함), 4개 필터 체크박스("나의 달력"은 작성자 본인 글 여부로 판정)와 즉각 필터링, 날짜 셀 호버 확대(`CalendarGrid.tsx`의 `hover:scale-125`) + 팝아웃(영상 자동재생 + 좌우 화살표 이미지 슬라이더) 전부 존재. "캘린더 설정" 톱니바퀴는 전용 버튼은 없지만 `PageBuilderRenderer.tsx`가 관리자에게 모든 위젯(캘린더 포함) 우측 상단에 `⚙`(→ Inspector) 버튼을 이미 공통 제공한다. 코드를 건드리지 않았다 — NEXT_TASK.md에 기록된 대로 실제 로그인 세션에서 board_id가 연결된 캘린더로 클릭 검증하는 게 남은 유일한 작업으로 보인다(이번 세션은 로그인 세션 없음).
- **검증**: `npx tsc --noEmit`/`npm run lint` 0 errors(신규 경고 없음, 기존 경고 51건은 무관한 사전 존재 항목). 로컬 dev에서 `/boards/new-board-5e95b289`, `/boards/new-board-ca826596`, `/about-silo`(catch-all 경로)를 방문해 태그 미노출/페이지 정상 렌더링 확인(비로그인, admin 버튼/캘린더는 로그인 세션 필요해 미확인).
- **변경 파일**: `src/lib/boardLayout.ts`, `src/components/modules/BoardModule.tsx`, `src/app/boards/[board_slug]/[post_slug]/PostDetailClient.tsx`, `src/app/[...slug]/page.tsx`, `docs/sql/EPIC-093-cleanup-placeholder-board-category.sql`(신설, 미실행).

## 2026-08-10 (EPIC-092 후속 2차 — 슬라이드쇼 여백 영역에도 여백 배경 이미지가 보이도록 수정)
- **오해 정정**: 이전 수정(force-dynamic)으로 여백 자체(px)는 정상 적용되고 있었다(Management API/DOM 직접 측정으로 재확인) — 사용자의 실제 요구는 "여백이 생기는 것"이 아니라 "그 여백 공간에도 슬라이드쇼의 여백 배경 이미지(wallpaper)가 이어져 보여야 한다"는 것이었다. 기존 구현은 CSS `margin`으로 박스를 바깥으로 밀어내기만 해서, 여백 부분은 그냥 빈 흰 공간(페이지 배경)이었다.
- **수정**: `HeroSlideshow.tsx`의 바깥 박스를 `margin` 대신 `padding`으로 바꾸고, 그 바깥 박스 자체에 여백 배경 이미지(`activeWallpaper`)를 `background-image`로 깔았다 — padding 영역이 이제 슬라이드 안쪽의 레터박스 배경과 이어지는 하나의 배경으로 보인다. 안쪽(`h-[60vh]` 박스, 슬라이드/점 인디케이터)은 기존과 동일하게 유지, `objectFit`이 "contain"이 아니거나 여백 배경이 없으면(showWallpaper=false) 기존처럼 padding만 있고 배경은 비어 있다.
- **검증**: `npx tsc --noEmit`/`npm run lint` 0 errors. 로컬 dev에서 padding 영역에 여백 배경 이미지(초록 잎 패턴)가 이어져 보이는 것 스크린샷+DOM(`padding: 10px 0px` + `background-image` 동일 요소에 적용) 둘 다로 확인.

## 2026-08-10 (EPIC-092 후속 — 슬라이드쇼 여백이 저장해도 반영 안 되는 문제 수정)
- **신고**: "홈페이지 설정 관리"에서 슬라이드쇼 위/아래 여백을 10px로 저장했는데도 실제 사이트(`dev.silostore.net`)에는 변화가 없음.
- **진단**: Supabase Management API로 라이브 `site_settings.hero_slideshow`를 직접 조회한 결과 `pc.marginTopPx: 10`이 정확히 저장돼 있었다 — 그런데 dev.silostore.net에서 실제 렌더링된 DOM을 `javascript_tool`로 직접 확인하니 슬라이드쇼 루트 div의 `style` 속성이 `margin-top:0`이었다. DB는 맞는데 화면만 stale하다는 건 서버 렌더링 캐싱 문제라는 뜻.
- **근본 원인**: `src/app/page.tsx`(홈페이지 Server Component)에 `export const dynamic = "force-dynamic"`이 없었다 — `PROJECT_RULES.md`가 이미 "인증 불필요 단순 조회 페이지는 매 요청 최신 데이터를 가져오도록 force-dynamic을 쓴다"고 명시한 관례인데, 유독 홈페이지 자신은 이 지정이 빠져 있어 Next.js가 `site_settings`/`page_builder` 조회 결과를 캐싱하고 있었다. 슬라이드/배경 이미지가 최신으로 보였던 건 그 값들이 여백을 설정하기 전에 이미 캐시된 렌더에 포함돼 있었기 때문(같은 저장 동작 안에 있었을 뿐 실제로는 별개 타이밍).
- **수정**: `src/app/page.tsx`에 `export const dynamic = "force-dynamic";` 추가.
- **검증**: `npx tsc --noEmit`/`npm run lint` 0 errors. 로컬 dev 서버에서 홈페이지가 정상 렌더링되는 것 확인(콘솔/서버 에러 없음). **다음에 확인 필요**: `dev.silostore.net` 재배포 후 실제로 여백이 반영되는지 최종 확인.

## 2026-08-10 (EPIC-092 후속 — 갤러리 배치 방향/줄당 개수 설정 + 썸네일 호버 영상·이미지 슬라이드 미리보기)
- **요청**: `/about-silo/silo-story`(갤러리형 게시판)를 예시로, (1) masonry(세로 우선 채움)만 있던 갤러리 배치를 grid(가로 우선 채움)로 바꾸고 한 행당 개수도 설정 가능하게, (2) 썸네일에 커서를 올리면 영상이 재생되고 이어서 본문의 다른 이미지들이 슬라이드로 넘어가는 미리보기 추가.
- **범위(사용자 확인)**: 우선 갤러리형 보드(`GalleryModule.tsx`)부터 적용 — 스토리/슬라이드/타임라인 등 다른 썸네일 컴포넌트는 이번 범위 밖(사이트에 공유되는 단일 썸네일 컴포넌트가 없어 확장 시 각각 별도 작업 필요). 호버 미디어 소스는 목록 API가 `body_json`까지 내려주도록 확장해 별도 fetch 없이 즉시 재생.
- **배치 방향/줄당 개수**: `GalleryModule.tsx`가 `layout`("masonry"|"grid")/`columns`(2~6) props를 받아 `grid`일 때는 `grid-template-columns: repeat(N, 1fr)`(가로 우선), 기본값(masonry)은 기존 `columns-2 sm:columns-3`(세로 우선) 그대로 유지. 설정은 `boards.widget_settings`(기존에 있었지만 어디서도 읽지 않던 dead 컬럼)에 `{galleryLayout, galleryColumns}`로 저장 — `BoardForm.tsx`에 Board Type이 "gallery"일 때만 보이는 설정 UI 추가(라이브 미리보기에도 즉시 반영), `BoardModule.tsx`→`BoardRenderer`→`GalleryRenderer`로 값을 전달.
- **호버 미리보기**: `GET /api/boards/[board_slug]/posts`의 select 목록에 `body_json`을 추가(`src/lib/boardLayout.ts`의 `BoardPost` 타입에도 반영). `GalleryModule.tsx`에 신설한 `GalleryCardMedia`가 `src/lib/calendarMedia.ts`의 `extractCalendarMedia`(EPIC-092 캘린더 위젯용으로 만든 body_json→미디어 목록 추출 유틸, 이름과 무관하게 범용 재사용)로 갤러리 이미지/영상을 뽑아 보여준다(영상은 `autoPlay muted loop`) — embed(유튜브 등 iframe)는 매 hover 로드 비용/autoplay 미보장 때문에 제외. 마우스가 벗어나면 원래 대표 이미지로 즉시 복귀.
- **후속 수정(같은 날, 사용자 피드백)**: 이미지 슬라이드가 자동으로 넘어가던 걸 기본은 좌우 화살표로 직접 넘기는 방식으로 바꾸고, 자동 전환은 게시판별로 켤 수 있는 옵션(`galleryHoverAutoSlide`, 기본 꺼짐)으로 변경 — 영상 자동재생은 이 옵션과 무관하게 항상 유지. `GalleryCardMedia`에 좌우 화살표 버튼 추가(클릭 시 `preventDefault`/`stopPropagation`으로 카드 링크 이동 방지), `BoardForm.tsx`에 "썸네일 호버 시 이미지 자동 슬라이드" 체크박스 추가.
- **검증**: `npx tsc --noEmit`/`npm run lint` 0 errors. 로컬 dev(비로그인)에서 hover 시 대표 이미지가 본문 갤러리의 다른 이미지로 바뀌는 것과, 좌우 화살표 클릭으로 슬라이드가 넘어가면서도 게시글 상세로 이동하지 않는 것까지 실측 확인, 콘솔 에러 없음. **관리자 로그인 세션이 필요해 다음에 확인**: `BoardForm.tsx`의 새 설정(배치 방향/줄당 개수/자동 슬라이드 여부) 저장 왕복, grid 레이아웃 실제 렌더링, 영상이 포함된 게시글에서 호버 시 영상 자동재생 확인.
- **변경 파일**: `src/components/modules/GalleryModule.tsx`, `src/components/boards/renderers/GalleryRenderer.tsx`, `src/components/boards/renderers/types.ts`, `src/components/modules/BoardModule.tsx`, `src/lib/useBoardData.ts`, `src/lib/boardLayout.ts`, `src/app/api/boards/[board_slug]/posts/route.ts`, `src/components/admin/BoardForm.tsx`, `src/app/admin/boards/[id]/page.tsx`, `src/app/admin/boards/new/page.tsx`.

## 2026-08-10 (EPIC-092 후속 — 등록 날짜/시간 저장 실패 수정 + 오류 메시지에 실패 사유 표시)
- **신고**: 관리자 전용 "등록 날짜/시간"을 바꿔 저장하면 "글 수정에 실패했어요"만 뜨고 원인을 알 수 없음.
- **근본 원인(Management API로 실측 확인)**: `information_schema.column_privileges` 조회 결과, `posts.created_at`은 `authenticated` 역할에 `INSERT`/`SELECT`/`REFERENCES`만 있고 `UPDATE` 권한이 없었다 — `created_at`은 지금까지 INSERT 시점 DB 기본값(`now()`)으로만 채워지고 한 번도 UPDATE된 적 없는 컬럼이라(CLAUDE.md의 "Column-scoped write via GRANT, not just RLS" 패턴과 동일한 사유), RLS 정책은 통과해도 Postgres가 컬럼 단위 GRANT 부재로 UPDATE 자체를 `42501 permission denied for column created_at`로 막고 있었다.
- **수정**: `docs/sql/EPIC-092-created-at-update-grant.sql` 신설(`grant update (created_at) on public.posts to authenticated;`) — Management API로 즉시 실행 완료, 재조회로 `authenticated`에 `UPDATE` 권한이 실제로 생겼음을 확인.
- **오류 메시지 개선(사용자 요청 — 과거에도 실패 사유를 화면에 표시해달라는 지시가 있었음)**: 서버(`PATCH .../[post_slug]`, `POST .../posts`)는 이미 `detail`(실제 DB 에러 메시지)을 함께 내려주고 있었는데 클라이언트가 `data.error`(안내 문구)만 보여주고 버려왔다 — `src/app/boards/[board_slug]/[post_slug]/edit/page.tsx`/`src/components/boards/WriteBoardForm.tsx`가 `detail`이 있으면 `"{error} ({detail})"` 형태로 이어 붙이도록 수정.
- **검증**: `npx tsc --noEmit`/`npm run lint` 0 errors. GRANT는 Management API 재조회로 확인, 실제 UI 저장 왕복은 관리자 로그인 세션이 필요해 다음 세션 확인 필요.

## 2026-08-10 (EPIC-092 후속 — 게시글 상세 페이지 "페이지 수정" 버튼 위치 통일)
- **사용자 확인**: 관리자 로그인 화면 스크린샷으로 실제 위치를 확인 — 일반 페이지는 좌측 상단 `#166534` 고정 버튼이 의도대로 보였고, 게시글 상세 페이지만 여전히 본문 상단에 인라인으로 다르게 배치돼 있는 걸 지적해 "모든 페이지에서 같은 위치로 고정해달라"고 요청.
- **수정**: `src/app/boards/[board_slug]/[post_slug]/PostDetailClient.tsx` — `PageEditButton`에 커스텀 `className`(인라인 스타일) 넘기던 것을 제거하고 `label`만 넘기도록 변경 — 이제 이 페이지도 `PageEditButton.tsx`의 기본값(`fixed top-20 left-4`, `#166534`)을 그대로 쓴다. 텍스트는 계속 "게시글 보여지는 방식 수정"으로 분기.
- **검증**: `npx tsc --noEmit`/`npm run lint` 0 errors. 관리자 로그인 세션이 없어 실제 화면 확인은 다음 세션 필요.

## 2026-08-10 (EPIC-092 — [Stage 2] Advanced Widget UX, Inline Editing, & Contextual Navigation)
- **요구사항 1 (관리자 전용 등록 날짜/시간)**: `PostForm.tsx`에 `member?.is_admin`일 때만 보이는 `datetime-local` 필드 추가 — 값이 있으면 `posts.created_at`을 직접 덮어쓴다. 클라이언트의 `is_admin` 표시는 UI 게이팅일 뿐이라, `POST /api/boards/[board_slug]/posts`와 `PATCH .../[post_slug]`(라우트) 둘 다 `requester.member.is_admin`(서버 조회값)을 다시 검증한 뒤에만 `created_at`을 반영한다(관리자가 아니면 조용히 무시). 캘린더 위젯의 "+ 글 등록"이 이 필드를 날짜로 pre-fill하는 데 재사용된다(요구사항 5).
- **요구사항 2 (페이지 수정 버튼 재배치)**: `PageEditButton.tsx`의 기본 위치/색상을 우측 상단 고정(`bg-gray-800`)에서 좌측 상단(헤더 바로 아래) 고정 + `#166534` 강제 지정으로 변경. `label` prop 추가(기본 "페이지 수정") — 게시글 상세(`PostDetailClient.tsx`)는 `label="게시글 보여지는 방식 수정"`을 넘긴다(이 버튼은 게시글 개별 페이지가 아니라 고정 슬러그 `"boards-id-postid"`로 게시글 상세 공용 렌더링 페이지를 가리킨다).
- **요구사항 3 (위젯별 [위젯 수정] 버튼)**: `PageBuilderRenderer.tsx`가 `useAuth()`로 관리자 여부를 직접 확인해, 각 위젯 우측 상단에 `⚙` 링크(`/admin/pages/[page_id]?module=[module_id]`)를 띄운다. `admin/pages/[id]/page.tsx`는 `useSearchParams()`로 `?module=`을 읽어 기존 `openEditor()` 핸들러를 그대로 재사용해 해당 위젯의 Inspector를 자동으로 연다(최초 1회, 이후 자유롭게 여닫기 가능) — `useSearchParams()` 사용으로 `write/page.tsx`와 동일하게 `<Suspense>` 경계 추가.
- **요구사항 4 (타임라인 위젯 개편)**: `TimelineView.tsx`를 중앙(좌측) 세로선 + 원형 배지 마커 + 카드(그림자/호버 효과) 레이아웃으로 재설계 — `entries`/`renderItem`/`emptyMessage` 계약은 그대로라 3개 호출부(`TimelineRenderer.tsx`, `TimelinePanel.tsx`, `PageModuleRenderer.tsx`의 `timeline` kind) 전부 무변경.
- **요구사항 5 (캘린더 위젯 개편)**: 순수 프레젠테이션 셸이던 `CalendarGrid.tsx`에 `cellContent` render-prop을 추가하고(기존 계약 100% 유지), 실 데이터 연동은 신설 `CalendarBoardWidget.tsx`가 담당 — `board_id`로 해당 월의 `posts`(id/slug/title/created_at/tags/author_id/body_json)를 직접 조회한다. 신설 `src/lib/calendarMedia.ts`(`extractCalendarMedia`)가 `body_json`을 재귀 순회해 갤러리 이미지/영상, embed(유튜브 등) 노드를 뽑아 날짜별 미디어 슬라이더에 공급. 4개 체크박스 태그 필터("예술가의 달력"/"학자의 달력"/"사일로의 달력" = `posts.tags` 매칭, "나의 달력" = `author_id === 로그인한 나`). 날짜 셀 hover 시 `scale-125`로 확대(그리드 컨테이너에 `isolate` + 확대 셀에 `hover:z-20`으로 stacking context 충돌 방지) + 팝아웃으로 "+ 글 등록"/미디어 슬라이더/그날의 글 목록 노출. "+ 글 등록"은 `/write?boardId=<slug>&date=YYYY-MM-DD`로 이동해 요구사항 1의 등록 날짜 필드를 pre-fill한다. `page_modules.board_id`가 없는 레거시 계산 위젯은 기존처럼 순수 셸만 보여준다.
- **요구사항 6 (슬라이드쇼 여백)**: `hero_slideshow`에 `marginTopPx`/`marginBottomPx`/`marginLeftPx`/`marginRightPx`(기본 0) 추가, `HeroSlideshow.tsx` 루트 div에 인라인 `style`로 적용. 저장은 기존 `handleSave("hero_slideshow", ...)`와 동일 — 별도 인앱 미리보기 캔버스는 없고 공개 홈페이지가 다음 요청부터 즉시 반영한다.
- **요구사항 7 (PC/모바일 독립 설정)**: `hero_slideshow` 스키마를 `{ slides, ... }` 단일 모양에서 `{ pc: {...}, mobile: {...} }`로 확장(신설 `src/lib/heroSlideshow.ts`가 타입/기본값/`normalizeHeroSlideshow` 정규화를 관리·공유). 관리자 화면에 "PC 버전"/"모바일 버전" 탭 토글 추가(`admin/payments`의 기존 탭 패턴 재사용). 공개 홈페이지(`src/app/page.tsx`)는 `hidden md:block`/`md:hidden` CSS로 두 슬라이드쇼를 각각 렌더링(이 프로젝트 최초의 뷰포트 조건부 렌더링 — JS `matchMedia` 없이 Server Component에서 그대로 동작). **하위 호환**: 옛 flat 모양 라이브 데이터는 읽는 시점에 `normalizeHeroSlideshow`가 pc/mobile 양쪽에 동일하게 채워 넣는다(별도 SQL 백필 불필요 — JSONB 값 모양 변경일 뿐 컬럼/테이블 변경 아님).
- **요구사항 8 (동적 브레드크럼)**: 신설 `src/lib/siteTree.ts` — `site_navigations.parent_id` 체인을 역추적하는 `getAncestorChain`, pathname→nav 노드 매칭 `findNavNodeByPathname`, board_id→nav 노드 역추적(`page_modules(module_type='board')` → `page_id` → `page_builder.slug` → `hrefToSlug(href)` 일치 검색) `getNavNodeForBoardId`. 신설 `src/components/Breadcrumb.tsx`(`usePathname()` 기반 클라이언트 컴포넌트)를 `layout.tsx`에 1곳만 추가 — 108개 정적 `page.tsx`/캐치올 라우트는 전혀 건드리지 않고 홈(`/`)에서는 자동으로 아무것도 그리지 않는다. 게시글 상세는 board_id 기반 매핑이 필요해 전역 컴포넌트가 매칭하지 못하므로, `boards/[board_slug]/[post_slug]/page.tsx`(서버 래퍼)가 `getNavNodeForBoardId`+`getAncestorChain`을 서버에서 미리 계산해 `PostDetailClient.tsx`에 `breadcrumb` prop으로 내려준다(별도 클라이언트 fetch 없음).
- **검증**: `npx tsc --noEmit`/`npm run lint` 0 errors(기존 경고 개수만 유지, 새 컴포넌트의 `set-state-in-effect` 경고 1건은 저장소 전반의 기존 패턴과 동일 종류). 로컬 dev 서버(비로그인)에서 확인 완료 — 타임라인 위젯 실 렌더링(중앙선/배지/카드), `/boards/general` 전역 브레드크럼("홈›Community›자유게시판"), `/boards/general/silo-angel` 게시글 상세 브레드크럼("홈›살롱데상 Salon des Cent›커뮤니티 Community›자유게시판"), 홈페이지 데스크톱/모바일 렌더링(콘솔 에러 없음) 전부 실측. **관리자 로그인 세션이 필요해 이번 세션엔 확인 못한 것(다음 세션 확인 필요)**: 등록 날짜/시간 필드 노출·저장 왕복, 페이지 수정 버튼의 관리자 전용 노출 및 좌측 상단/초록 배경 실측, 위젯 수정 톱니바퀴+`?module=` 딥링크로 실제 Inspector가 열리는지, 캘린더의 hover 확대/미디어 슬라이더/태그 필터/+글 등록 전체 플로우, PC/모바일 탭 전환 저장 왕복.
- **변경/신설 파일**: `src/components/admin/PageEditButton.tsx`, `src/components/boards/PostForm.tsx`, `src/app/api/boards/[board_slug]/posts/route.ts`, `src/app/api/boards/[board_slug]/posts/[post_slug]/route.ts`, `src/components/boards/WriteBoardForm.tsx`, `src/app/boards/[board_slug]/[post_slug]/edit/page.tsx`, `src/components/PageBuilderRenderer.tsx`, `src/app/admin/pages/[id]/page.tsx`, `src/components/TimelineView.tsx`, `src/components/modules/CalendarGrid.tsx`, `src/components/modules/CalendarBoardWidget.tsx`(신설), `src/lib/calendarMedia.ts`(신설), `src/lib/pageModules.ts`, `src/app/write/page.tsx`, `src/app/page.tsx`, `src/components/HeroSlideshow.tsx`, `src/app/admin/navigation/settings/page.tsx`, `src/lib/heroSlideshow.ts`(신설), `src/lib/siteTree.ts`(신설), `src/components/Breadcrumb.tsx`(신설), `src/app/layout.tsx`, `src/app/boards/[board_slug]/[post_slug]/page.tsx`, `src/app/boards/[board_slug]/[post_slug]/PostDetailClient.tsx`.

## 2026-08-08 (HOTFIX-091 — 게시글 수정 시 "게시글을 찾을 수 없어요" 오류 수정)
- **실측 재현**: 라이브 API를 직접 호출해 정확한 실패 시나리오를 특정했다 — 테스트 글을 게시판 A→B로 옮기는 PATCH는 정상 성공(200)하지만, 그 직후(같은 세션에서 페이지가 아직 이동하지 않은 상태, 예: 더블클릭·뒤로가기 후 재제출) URL의 board_slug가 여전히 A를 가리키는 채로 같은 글을 다시 PATCH하면 `{"status":404,"error":"게시글을 찾을 수 없어요."}`가 재현됨을 확인 — 지시문이 지목한 원인(`slug`+`board_id`를 동시에 WHERE로 걸어 조회)이 정확히 맞았다.
- **수정**: `src/app/api/boards/[board_slug]/posts/[post_slug]/route.ts`의 PATCH — 클라이언트가 이미 알고 있는 진짜 PK(`postId`)가 요청에 오면 `id` 단일 조건으로만 게시글을 찾는다(URL의 board_slug가 그 사이 stale해졌어도 무관) → `postId`가 없는 요청(구버전 호환)만 기존 `slug`+URL `board_id` 방식으로 폴백. definition(카테고리/태그 계산용 게시판 컨텍스트)도 URL이 아니라 이 글이 실제로 지금 속한 게시판(`existing.board_id`)으로 다시 조회하도록 변경. 부수로 "이동 여부" 판정도 URL board_slug 문자열 비교 대신 실제 board_id 비교로 바꿔, 같은 게시판을 가리키는 slug/UUID 두 형태(레거시 링크)가 섞였을 때 불필요하게 "이동"으로 오판해 slug를 재발급하던 잠재 버그도 함께 없앴다. `src/app/boards/[board_slug]/[post_slug]/edit/page.tsx`가 최초 GET 응답의 `post.id`를 저장해뒀다가 PATCH 페이로드에 `postId`로 실어 보내도록 수정.
- **검증**: `npx tsc --noEmit` 0 errors, `npm run lint` 0 errors(기존 경고 개수 그대로, 신규 경고 없음). 로컬 dev + 라이브 Supabase를 대상으로 실제 API 시퀀스로 재현/수정 확인 — (1) 게시판 이동 → (2) 이동 직후 stale board_slug로 재제출(수정 전: 404 재현 / 수정 후: `postId` 포함 시 200 성공), (3) `postId` 없는 폴백 경로도 여전히 정상 동작 확인. 테스트로 만든 글은 정리 완료.
- **변경된 API 라우트**: `src/app/api/boards/[board_slug]/posts/[post_slug]/route.ts` (PATCH). 프론트엔드: `src/app/boards/[board_slug]/[post_slug]/edit/page.tsx`.

## 2026-08-08 (HOTFIX-090 — 위젯 설정 내 "게시판 추가" 버튼 위치 수정)
- **진짜 원인**: EPIC-088/089가 만든 "게시판 추가" 버튼은 "페이지 전체에 board 위젯이 하나도 없을 때"만 노출되는 조건이었다 — 실사용 흐름("+위젯 추가"→"Board" 선택 직후, 위젯은 생겼지만 아직 게시판을 안 고른 가장 흔한 순간)에는 이미 board 위젯이 존재해서 그 버튼이 사라져 있었고, 위젯의 "게시판 선택"(Miller Columns) 패널 안에는 기존 게시판을 고르는 기능만 있고 새로 만드는 버튼이 전혀 없었다 — 이게 반복 신고의 실제 원인.
- **수정**: `src/app/admin/pages/[id]/page.tsx`의 위젯 편집기 — `CategoryBoardPicker` 바로 아래에 `[+ 새 게시판 추가]` 버튼을 위젯별로 항상 노출(연결 여부 무관, 요구사항 원문대로 "언제든 누를 수 있게"). 클릭 시 임시 이름/슬러그(`new-board-<timestamp36>`)로 `POST /api/admin/boards` → 응답으로 받은 board id를 지금 편집 중인 위젯의 `page_modules.board_id`에 즉시 반영 → `/admin/boards/[새id]`로 리다이렉트(요구사항 1.2 그대로).
- **검증**: `npx tsc --noEmit` 0 errors. 로컬 dev 로그인 세션에서 실제 클릭까지 재현 — 버튼 렌더링 확인, 클릭 → 새 게시판 생성 → 그 위젯에 연결(대상 페이지의 "이 게시판을 사용 중인 페이지" 역방향 조회로 확인) → `/admin/boards/[id]`로 리다이렉트까지 전부 실측.
- **⚠️ 검증 중 발견한 데이터 주의사항 → 재확인 결과 정상(같은 날 후속)**: 테스트에 쓴 페이지("예술 Art")에 board 타입 위젯이 2개 있는 걸 발견해 되돌리기 쿼리가 잘못 겹쳤을까 우려했으나, 재조회 결과 남은 위젯 구성은 Hero→Quote→**Board(예술 클럽, 정상 연결)**→Latest Posts Slider(미연결, 원래 값 그대로)→Gallery(미연결, 원래 값 그대로) 5개로 일관되고 정상이다(정확히 어떤 경위로 정리됐는지는 재구성 못 했음, 더 이상 손대지 않고 사용자 최종 확인 요청).
- **후속 3건(같은 날, 사용자 지시)**: (1) **"그리스" 카테고리 게시판 선택 미노출**: 버그가 아니라 `boards`에 "그리스" 게시판이 실제로 한 번도 생성된 적이 없었던 것(도슨트 시대별 페이지 전부가 템플릿상 board 위젯 2개씩 갖고 있는데 — 위와 동일한 패턴 — "그리스"만 유일하게 아직 아무도 연결을 안 만든 상태였음, 다른 10개 도슨트 시대는 전부 연결돼 있었음). HOTFIX-090의 "+ 새 게시판 추가" 버튼으로 직접 "그리스" 게시판을 만들어 그 페이지의 미연결 Board 위젯에 연결 완료. (2) **게시판 선택 옵션 실시간 연동**: `admin/pages/[id]/page.tsx`가 게시판/사이트 메뉴 트리를 컴포넌트 최초 마운트 시 한 번만 불러오던 것을, 재사용 가능한 `loadBoards()`/`loadNavBranches()`로 뽑아 마운트 시 + 이 탭이 다시 포커스를 받을 때(`window focus`/`visibilitychange`)마다 재조회하도록 — 다른 탭에서 "사이트 구성 관리"를 바꾸고 돌아와도 반영됨(Supabase Realtime 구독까지는 이 화면 규모에 과하다고 판단해 폴링/포커스 트리거로 해결). (3) **게시판 선택 옵션 정렬**: `CategoryBoardPicker.tsx`의 3열째(게시판 목록)가 지금까지 임의 순서였던 것을, `branches` 배열(이미 site_navigations.sort_order 그대로 depth-first로 펼쳐진 배열)의 인덱스로 정렬 — 사이트 메뉴 순서와 동일하게(그리스→르네상스→바로크→...→디지털, 시대순) 뜨는 것 실측 확인.
- **검증(후속)**: `npx tsc --noEmit`/`npm run lint` 0 errors(기존 `set-state-in-effect` 패턴과 동일 종류 경고만 +2). 로컬 dev 로그인 세션에서 "그리스" 게시판 생성→연결→Miller Columns에 노출 확인, 3열 정렬 순서가 사이트 메뉴 시대순과 일치함을 실제 클릭으로 확인.

## 2026-08-08 (EPIC-089 — Floating Action UX, YouTube-style Nested Comments, & Admin/Sidebar Control)
- **P0 조사 결과 정정**: 지시문의 "[경고]"가 "EPIC-088에서 누락됐다"고 지목한 위젯 편집기 "게시판 추가" 버튼은 실제로는 이미 정상 구현·동작 중임을 라이브 사이트에서 직접 클릭 재현으로 확인(테스트로 만든 게시판/위젯은 정리 완료) — 재구현하지 않았다. 대신 진짜 DB 에러를 찾아 고쳤다: `posts`는 `(board_id, slug)` 복합 UNIQUE 인덱스가 있는데, "게시될 페이지 선택"으로 다른 게시판으로 옮겨 저장할 때 slug를 그대로 들고 가 목적지 게시판에 같은 slug가 이미 있으면 UPDATE가 그대로 실패했다(저장이 전혀 안 되는 리포트의 실제 원인으로 추정) — `src/app/api/boards/[board_slug]/posts/[post_slug]/route.ts` PATCH가 이동 시 목적지 게시판 안에서만 유일하게 slug를 재발급(충돌 시 `-2`, `-3`)하도록 수정, 수정 화면(`edit/page.tsx`)도 서버가 실제로 저장한 slug로 리다이렉트하도록 수정.
- **관리자 "전체 글 관리" 미분류 노출(요구사항 1)**: 4개 도메인(사일로상점/살롱데상/스튜디오/마이페이지) 탭에 안 걸리는 게시판(`AdminPostsBoardView`의 `domain==="common"` 분기 — 이미 구현돼 있었지만 그 값을 넘기는 라우트가 없었음)의 글을 볼 방법이 아예 없었다 — `/admin/posts/common`(새 "[기타] 미분류 게시글 관리" 탭) 신설.
- **위젯 내 Board Type 드롭다운(요구사항 5)**: `/admin/pages/[id]` 위젯 편집기의 "게시판 선택" 아래 `render_type`(Board Type/레이아웃) 드롭다운을 인라인 추가 — 기존엔 `/admin/boards/[id]`로 나가야만 바꿀 수 있었다. 새 API 불필요(`PATCH /api/admin/boards/[id]`가 이미 `render_type`을 편집 가능 필드로 받고 있었음), `BoardForm.tsx`의 `RENDER_TYPE_OPTIONS` 재사용.
- **플로팅 액션 바(요구사항 2/3)**: 게시글 상세 좌측 하단에 항상 떠 있는 바(`src/components/boards/PostFloatingActionBar.tsx` 신설) — `[← 목록으로]`(버튼형) · 좋아요 · 북마크 · 공유 · 댓글(클릭 시 `#comments`로 smooth scroll) 순서. 기존 본문 상단 좌측의 "목록으로" 자리는 관리자용 "페이지 수정" 버튼(`PageEditButton`, 기존엔 화면 우측 상단에 고정이었음 — 다른 112개 호출부는 그대로 두고 이 페이지만 `className`으로 인라인 배치 override)이 대신 채운다. 하단에 중복으로 있던 "목록으로"(EPIC-084-REVISED)는 FAB로 대체돼 제거. `ScrapButton.tsx`의 표시 문구를 "스크랩"→"북마크"로 변경(데이터 모델/함수명은 유지).
- **사이드바 Y축 위치(요구사항 2)**: 좌/우 사이드바 여닫이 아이콘이 지금까지 `top-1/2`(화면 정중앙) 고정이었다 — `site_settings.sidebar_icons.topOffsetPx`(신규, 기본 160px로 기존보다 상단으로 올림) 추가, `/admin/navigation/settings`에 숫자 입력 필드(기존 `iconSizePx` 패턴과 동일), `LeftSidebar.tsx`/`RightSidebar.tsx`가 지정되면 `top-1/2` 대신 인라인 `style={{top}}` 적용(지정 안 하면 구버전 호출부 호환을 위해 기존 정중앙 동작 유지).
- **유튜브 스타일 대댓글/좋아요/아바타(요구사항 4)**: `docs/sql/EPIC-089-comments-threads-likes-avatar.sql`(Management API로 즉시 실행 완료) — `comments.parent_id`(self-FK, 답글은 항상 "최상위 댓글"을 가리켜 유튜브처럼 딱 1단계만 들여쓰기), `comment_likes` 테이블(posts의 `likes`와 동일한 own-row insert/delete + 공개 select RLS 패턴), `public_profiles` 뷰에 `avatar_url` 추가(`members.avatar_url`은 EPIC-087-PHASE-F에서 이미 존재). 게시글 GET 라우트가 댓글마다 `author_avatar_url`/`like_count`/`liked_by_me`를 함께 내려주도록 확장, 댓글 POST가 `parentId`를 받아(답글의 답글이면 그 답글의 parent로 평평하게 정규화) 저장, 새 `POST .../comments/[commentId]/like` 라우트(토글). `CommentSection.tsx` 전면 재작성 — 아바타 원형(URL 없으면 이니셜 폴백), 답글 버튼(인라인 미니폼, 답글에 답글 달면 입력창에 `@이름 ` 자동 채움), 댓글별 좋아요 버튼(낙관적 UI). `PageModuleRenderer.tsx`가 쓰는 범용 "comment" 페이지 모듈 경로는 실제 게시글 컨텍스트가 없어 `onReply`/`onToggleLike`를 옵션으로 남김(안 넘기면 버튼이 조용히 아무 일도 안 함, 기존에도 그 경로는 실데이터에 연결돼 있지 않았음).
- **검증**: `npx tsc --noEmit`(0 errors), `npm run lint`(0 errors, 44→45 warnings — 기존 `set-state-in-effect` 패턴과 동일 종류, 새 로직 자체엔 경고 없음). 로컬 dev 서버 실사용 세션(로그인 상태)으로 실측: FAB 5개 항목 순서/렌더링, "페이지 수정" 인라인 배치, "북마크" 문구, 댓글 작성→답글→답글에 답글(멘션 자동 채움, 들여쓰기 1단계 고정 확인)→좋아요 토글(새로고침 후에도 유지 확인)까지 전체 왕복, `/admin/posts/common` 신규 탭 렌더링, 위젯 편집기 Board Type 드롭다운 변경이 실제 `boards.render_type`에 반영됨(DB 재조회로 확인 후 원복), 좌/우 사이드바 트리거 `top: 160px` 계산된 스타일 확인. 테스트로 만든 댓글/답글/좋아요/게시판은 전부 정리 완료. **미확인**: FAB "댓글" 아이콘의 smooth scroll 자체(`scrollIntoView({behavior:"smooth"})`) — `behavior:"auto"`로는 정상 스크롤되는 것을 확인했지만 헤드리스 Browser pane 환경이 `smooth` 애니메이션(rAF 기반)을 아예 진행시키지 않는 것으로 보임(EPIC-076/078 때의 `@media (hover: hover)` 미충족과 같은 종류의 기존 환경 한계로 추정) — 실제 브라우저에서 최종 확인 권장.

## 2026-08-07 (EPIC-084-REVISED 후속 — Instagram 임베드 간헐적 미노출 조사 + 폴백 타임아웃 연장/재시도)
- **사용자 신고**: 특정 게시글(`/boards/7802db3a-.../bef5d64a`)에서 "글쓰기 미리보기에는 Instagram 임베드가 보이는데 실제 게시글 상세에는 안 보인다"는 신고. 코드 버그로 의심됐으나 조사 결과 **에디터 미리보기(`InstagramEmbedPreview`, BlockEditor.tsx)와 상세 페이지(`UniversalBlockRenderer`→`PostBody.tsx`) 둘 다 완전히 동일한 `processInstagramEmbeds()`(src/lib/instagramEmbed.ts)를 호출**하는 것으로 확인 — 코드 경로 차이가 원인일 가능성은 배제됨.
- **실측 확인**: Browser pane으로 편집기 미리보기/상세 페이지를 각각 시간을 두고, 그리고 동시에 재현 — 실패 시점엔 embed.js가 만든 iframe이 높이 ~24px(플레이스홀더)에서 전혀 못 자라고 멈췄고(70초 이상 대기해도 무변화), 이 게시글과 무관한 임의의 다른 Instagram 게시물로도 동일 증상 재현(이 글/이 코드만의 문제가 아님을 확인). 이후 시점을 바꿔 다시 테스트하니 편집기/상세 페이지 **둘 다 동시에** 정상 렌더링(iframe 500px+)됨 — 코드가 아니라 **Instagram 서버 쪽 응답이 시점에 따라 달라지는 간헐적 현상**으로 결론(반복적인 자동화 테스트 요청 자체가 일시적 rate-limit/봇 탐지를 유발했을 가능성 포함). `next.config.ts`에 CSP/X-Frame-Options/Permissions-Policy 등 iframe을 막을 만한 헤더 설정이 전혀 없음도 확인 — Vercel/우리 배포 설정이 원인일 가능성도 배제.
- **개선(사용자 선택)**: `src/lib/instagramEmbed.ts`의 `processInstagramEmbeds()`를 6초 단발 체크에서 **1차 대기(8초) → 안 자랐으면 `Embeds.process()` 재시도 → 2차 대기(8초) → 그래도 안 자란 것만 최종 링크 카드로 교체**하는 2단계 체크로 변경(`FALLBACK_TIMEOUT_MS` → `FIRST_CHECK_MS`+`RETRY_CHECK_MS`). 중복 코드였던 미렌더 판정/링크 교체 로직을 `getUnrenderedEmbeds()`/`replaceWithFallbackLink()`로 분리. 근본 원인(Instagram 서버 쪽 간헐적 실패)은 여전히 우리 쪽에서 완전히 통제 불가 — 이 변경은 일시적 지연 구간을 좀 더 버텨내는 완화책.
- **검증**: `npm run lint`(0 errors, 대상 파일 신규 warning 없음). 로컬 dev 서버(`silo-dev`)로 재구성된 두 단계 타이밍(구 6초 컷 대신 그 시점엔 아직 안 넘어가고, 재시도 이후 최종적으로 넘어가는 것)이 실제로 동작함을 DOM 폴링으로 확인.

## 2026-08-07 (EPIC-088 — Admin Detail Corrections, Widget Linkage, & Embed Thumbnail Engine)
- **사이트 메뉴 접기/펼치기(요구사항 12)**: `CategoryTreeManager.tsx`에 노드별 접기 상태(`collapsedIds`)를 추가 — 하위 nav 행 또는 배정된 게시판이 있는 노드에만 ▶/▼ 토글 아이콘을 보여주고, 접으면 그 아래 `TreeLevel`/`BoardListLevel`(nav 자식 + 배정된 게시판)을 렌더링하지 않는다. 드래그 핸들과 별개 버튼이라 dnd-kit 리스너와 충돌하지 않는다.
- **메뉴별 티어 접근 제어(요구사항 3)**: 새 컬럼을 추가하지 않고 이미 있는 `page_builder.min_rank_to_read`(EPIC-087-PHASE-C)/`boards.min_rank_to_read`를 재사용 — `CategoryDetailModal`의 "연결된 페이지"/"연결된 게시판" 섹션에 "최소 접근 가능 티어" 셀렉트+저장 버튼을 추가해 사이트 메뉴 관리 화면 안에서 바로 편집할 수 있게 했다.
  - **프론트엔드 클릭 게이트**: `navConfig.ts`의 `fetchNavTabs()`가 `page_builder(slug, min_rank_to_read)`를 함께 조회해 `hrefToSlug(href)` 매칭으로 모든 탭/그룹/항목에 `minRankToRead`를 실어준다(쿼리스트링이 섞인 하이브리드 라우트는 slug 매칭이 안 돼 자연히 게이트 없음 — `usePageRankGate`가 하이브리드 페이지를 의도적으로 제외하는 것과 동일한 결과). 새 `src/components/common/GatedNavLink.tsx`(`next/link` 래퍼)가 클릭 시 로그인 회원의 등급과 비교해 미달이면 `preventDefault`+`alert`+`site_settings.membership_guide_redirect`(PHASE-C와 동일한 설정값)로 리다이렉트한다. `Navbar.tsx`/`LeftSidebar.tsx`/`RightSidebar.tsx`의 메뉴 트리 기반 `Link`를 전부 이 컴포넌트로 교체(계정 영역의 로그인/마이페이지 등은 4탭 구조 밖이라 대상에서 제외) — 요구사항 원문대로 메뉴 항목 자체는 모든 유저에게 노출되고, 이동만 막힌다.
- **위젯 게시판 선택기 게시판 수정 버튼(요구사항 5)**: `admin/pages/[id]/page.tsx`의 위젯 편집기에서 `CategoryBoardPicker` 옆에 "게시판 수정" 링크를 추가 — 선택된 게시판의 `/admin/boards/[id]`를 새 탭으로 연다. (드롭다운 자체의 실제 DB 바인딩은 EPIC-084-REVISED에서 이미 `CategoryBoardPicker`로 정상화되어 있던 상태 — PHASE-A에서 비-버그로 재확인됨, 이번엔 그 옆 버튼만 추가.)
- **빈 게시판 추가(요구사항 10)**: 같은 화면에 board 위젯이 하나도 없을 때만 "게시판 추가" 버튼을 노출 — `POST /api/admin/boards`로 이 페이지 slug를 카테고리로 하는 새 게시판을 만들고(충돌 시 `-board` 접미사로 재시도) `page_modules`(module_type='board') 위젯으로 즉시 연결한 뒤 `/admin/boards/[newId]`로 이동시킨다.
- **게시판 수정 화면 슬러그 잠금 해제(요구사항 6)**: `BoardForm.tsx`의 "슬러그"(실제로는 `boards.category` 컬럼) 필드에서 `disabled={mode==="edit"}`를 제거 — API(`/api/admin/boards/[id]` PATCH)는 이미 `category`를 편집 가능 필드로 취급하고 중복 슬러그를 409로 막는 검증도 이미 있어(EPIC-066) 백엔드 변경은 불필요했다. UI에는 "Board Definition/기존 링크가 참조할 수 있다"는 주의 문구로 대체(PHASE-A가 이 잠금을 의도된 설계로 재확인했던 것과 배치되는 지시라 코멘트로 그 배경을 남겨둠).
  - **Category 필드 → 다열 캐스케이딩 선택창(요구사항 6 핵심)**: 하드코딩된 `GROUP_OPTIONS` 드롭다운(`group_key`)을 제거하고, 이 게시판이 사이트 메뉴 트리의 어느 분기에 속하는지 직접 클릭해 매칭하는 새 `src/components/common/CategoryBranchPicker.tsx`로 교체 — 트리 깊이만큼 열이 동적으로 늘어나고(1열=최상위, 2열=하위, 3열 이상도 자동 확장) 어느 열에서든(리프까지 안 가도) 그 자리를 최종 선택으로 쓸 수 있다. 저장은 `CategoryDetailModal.saveBoardLink()`/`CategoryTreeManager.handleBoardDragEnd()`와 동일한 매커니즘(`page_modules`의 board 위젯 행)을 그대로 재사용 — 목적지 분기에 아직 연결된 페이지가 없으면 `ensurePageForSlug`로 즉시 만든다. edit 모드 전용(board id가 있어야 저장 가능 — min_rank_to_write/read 섹션과 동일한 관례).
  - **중복 코드 방지**: `CategoryBoardPicker.tsx`의 단일 열 렌더링(`ColumnList`)을 `src/components/common/MillerColumnList.tsx`로 뽑아 `CategoryBranchPicker.tsx`와 공유.
- **Embed 썸네일 엔진(요구사항 11)**: 조사 결과 이 파이프라인은 이미 EPIC-079-HOTFIX-3에서 대부분 구현되어 있었다(`resolveFallbackEmbedThumbnail`이 글 저장 시점에 youtube/vimeo/instagram embed의 썸네일을 `posts.featured_image_url`에 자동 채워 넣고, `GalleryModule`/`StoryThumbnailModule` 등은 이미 그 값을 우선 사용) — 실제 남은 공백은 **`src/app/api/boards/feed/route.ts`(허브 게시판의 최신/인기/추천 피드)가 `featured_image_url`/`thumbnail_visible`을 select/응답에서 아예 빠뜨리고 있던 것**이었다. 두 컬럼을 select에 추가하고 기존 `photo_url` 응답 필드에 우선순위(featured_image_url > photo_url, thumbnail_visible=false면 숨김)를 반영해 다른 목록과 동일하게 맞췄다. (YouTube 썸네일 URL은 기존 `hqdefault.jpg`를 유지 — 스펙 원문은 `maxresdefault.jpg`를 언급하지만, 오래되거나 세로 영상은 maxres가 존재하지 않아 깨진 이미지로 보일 위험이 있어 이미 검증된 hqdefault를 그대로 둠.)
- **검증**: `npx tsc --noEmit`(0 errors), `npm run lint`(0 errors, 기존 경고 패턴만 — 새 `CategoryBranchPicker.tsx`의 `set-state-in-effect` 경고는 `CategoryBoardPicker.tsx`가 이미 쓰던 것과 동일한 기존 패턴). Browser pane에서 비로그인 홈/`GatedNavLink`로 교체된 상단 탭 클릭(등급 제한 없는 탭 정상 이동, 콘솔 에러 없음) 확인.
- **관리자 실사용 검증(같은 날 후속, 임시 테스트 계정으로 실제 로그인 세션 확인)**: `epic088-test-20260807@silostore.net` 계정을 만들어 Management API로 `is_admin`을 켜고 `/admin/site-structure`·`/admin/pages/[id]`·`/admin/boards/[id]`를 실제로 클릭 — (1) 트리 노드 접기/펼치기 토글이 하위를 실제로 숨기고/보이는 것 확인, (2) "About Silo" 페이지에 최소 접근 등급 Patron 저장 → `page_builder.min_rank_to_read=3` DB 반영 확인 → Silo Angel 등급인 테스트 계정으로 그 탭을 클릭하니 alert 후 `/membership?gate=3`로 실제 리다이렉트되는 것까지 end-to-end 확인(등급 무관 탭은 정상 이동도 확인), (3) 위젯 편집기에서 게시판을 고르면 "게시판 수정" 링크가 그 게시판의 `/admin/boards/[id]`로 정확히 연결되는 것, board 위젯이 있는 페이지엔 "게시판 추가" 버튼이 조건대로 숨는 것 확인, (4) 신규 테스트 게시판에서 슬러그 편집 가능함과 `CategoryBranchPicker`로 "About Silo" 선택 후 "위치 저장" → `page_modules`에 실제 board 위젯 행이 생성되는 것을 DB 조회로 확인. 검증에 쓴 테스트 계정/게시판/게이트 값은 전부 삭제·원복 완료(잔존 여부 재조회로 확인).
- **자체 코드 리뷰(같은 날 후속) — 발견/수정 2건**:
  1. **데이터 정합성**: `BoardForm.saveBranchLink()`에서, 선택한 분기에 아직 페이지가 없어 `ensurePageForSlug()`가 새로 만드는 경우 — 그 함수가 기본 템플릿으로 `board_id`가 비어있는 "board" 위젯을 이미 하나 심어두는데, 기존 코드는 이를 무시하고 새 `page_modules` 행을 또 insert해 같은 페이지에 board 위젯이 2개(하나는 영원히 미연결) 남는 문제가 있었다 — insert 전에 그 페이지의 미연결 board 슬롯을 먼저 찾아 있으면 재활용(board_id만 채움)하도록 수정.
  2. **번들 크기/레이어링**: `GatedNavLink.tsx`(모든 방문자에게 항상 렌더링되는 공개 Navbar/Sidebar에서 쓰임)가 등급 라벨 상수 하나를 위해 무거운 관리자 전용 컴포넌트 `admin/BoardForm.tsx`(BoardRenderer·다열 카테고리 선택기 등을 잔뜩 끌고 옴)를 통째로 import하고 있었다 — `RANK_OPTIONS`를 의존성 가벼운 새 `src/lib/membershipTiers.ts`로 옮기고 `BoardForm.tsx`는 재노출(re-export)만 하도록 정리(기존 관리자 화면 import 경로는 안 바뀜). 부수로 `LeftSidebar.tsx`/`RightSidebar.tsx`의 `Link`가 `GatedNavLink`로 전량 교체되며 남아있던 미사용 `import Link` 2건도 제거(lint 경고 46→44개).
  - 재검증: `npx tsc --noEmit`(0 errors), `npm run lint`(0 errors, 44 warnings), Browser pane에서 완전히 새 탭(캐시/콘솔 이력 없음)으로 홈/`/community` 로드 시 콘솔 에러 0건 확인.

## 2026-08-07 (EPIC-087-PHASE-G — Footer(FNB) 신설, EPIC-087 전체 완료)
- **from-scratch 구축**: 이 사이트는 지금까지 footer가 아예 없었다(`layout.tsx`가 `AuthProvider > Navbar > {children}`만 렌더링). 새 `src/components/Footer.tsx` — 회사/운영자 정보, 법적 고지 링크(이용약관/개인정보처리방침/저작권 표시), FAQ(자주 묻는 질문/문의 게시판), 부가 메뉴(사이트맵/고객센터/패밀리사이트), 소셜 아이콘 5개 섹션. `site_settings.footer_config`(PHASE-0에서 예약한 키, PHASE-G에서 필드명 최종 확정 — `faq`→`faqLinks` 등 `Footer.tsx`의 `FooterConfig` 타입과 일치하도록 업데이트)를 읽어 렌더링, 모든 필드가 비어있으면(초기 상태) `null` 반환해 빈 footer가 그려지지 않음(사용자가 실제로 입력하기 전까지 화면 변화 없음, 회귀 없음).
- **관리자 화면**: 새 `/admin/footer`("하단 메뉴 관리", `ADMIN_NAV`에 추가) — 회사 정보 입력 필드 + 4개 링크 그룹(법적고지/FAQ/부가메뉴/소셜)마다 항목 추가/삭제가 되는 동적 리스트 에디터. 저장은 `admin/navigation/settings/page.tsx`의 `upsertSetting()`과 동일한 direct `supabase.from("site_settings").upsert(...)` — 이미 `site_settings_update_admin` RLS 정책이 있어 새 API 라우트 불필요.
- **`layout.tsx`**: `{children}` 다음에 `<Footer/>` 마운트 — 기존 `body`가 이미 `min-h-full flex flex-col`이고 페이지 `<main>`들이 이미 `flex-1`이라 추가 레이아웃 조정 없이 짧은 페이지에서도 하단에 고정됨(사전 확인 완료, 별도 CSS 수정 불필요).
- **검증**: `npx tsc --noEmit`(0 errors), `npm run lint`(0 errors, 기존 42개 경고만). Browser pane에서 (1) 빈 설정 상태에서 footer 미노출 확인(회귀 없음), (2) Management API로 테스트 값(회사명/이용약관 링크/Instagram 링크) 임시 주입 후 재로드 → 실제로 하단에 렌더링되는 것 확인, 콘솔 에러 없음(HMR 웹소켓 노이즈 제외) 확인 후 원상복구, (3) `/admin/footer` 컴파일 확인.
- **EPIC-087 9개 요구사항(2.1~2.6) 전체 구현 완료** — PHASE-0~G 요약은 위 각 항목 참고. 남은 작업은 문서 동기화(`docs/EPIC.md`/`docs/PROJECT_DASHBOARD.md`/`docs/STAGES.md`)와 관리자 로그인 세션에서의 실사용 클릭 테스트(각 PHASE 항목 아래 NEXT_TASK.md에 개별 기록).

## 2026-08-07 (EPIC-087-PHASE-F — GNB 재배치 + 멤버십 팝오버)
- **스키마**: `members.avatar_url`(신규) + `avatars` Storage 버킷(public read/authenticated upload/owner delete, 기존 `post-images`/`gallery`/`attachments` 정책을 건드리지 않고 avatars 전용 정책만 추가) — `member_profiles.cover_image`는 코드 전체 미참조로 확인돼 되살리지 않음. `docs/sql/EPIC-087-PHASE-F-avatar-column.sql`, Management API로 즉시 실행 완료.
- **GNB 재배치**: `Navbar.tsx` 계정 영역을 `[멤버십 신청/등급] | [마이페이지] | [회원 이름] | [로그아웃]` 4개 항목으로 분리(이전엔 등급+이름이 `/mypage` 링크 하나로 묶여있었음). 왼쪽 항목은 `member`가 있으면 등급명, 없으면(로딩 중/회원 행 없음) "멤버십 신청" 텍스트로 `/membership` 링크.
- **멤버십 팝오버**: 새 `src/components/MembershipPopover.tsx` — 요구사항 원문대로 등급 항목과 이름 항목 둘 다 클릭 시 같은 팝오버가 열림(페이지 이동 아님). 이 코드베이스 유일한 기존 드롭다운(스튜디오 탭)은 EPIC-041-042-HOTFIX가 hover 타이밍 버그 때문에 일부러 JS 없는 순수 CSS로 만든 것이라 재사용 불가 — click-outside(`mousedown` 리스너)+Escape로 새로 구현. 내용: 프로필 사진(`avatar_url`), 팔로우/팔로워 수(`FollowPanel.tsx`처럼 전체 행 select 대신 `{count:"exact",head:true}` 경량 쿼리), 나의 최근 활동(`TimelinePanel.tsx`의 3-쿼리 조합에 `.limit(5)`만 추가한 축소판, `REASON_LABELS` 재사용), 받은 메시지(코드베이스에 메시지/DM 시스템이 전혀 없어 "준비 중" 텍스트만 — 사용자 확인 완료, 실제 기능은 범위 밖), 오늘 마음일기(`/boards/mind-diary`, PHASE-0에서 라이브 존재 확인된 게시판).
- **검증**: `npx tsc --noEmit`(0 errors), `npm run lint`(0 errors, 기존 42개 경고만). Browser pane에서 비로그인 상태 렌더링(계정 영역 전체가 `session` 게이트라 "로그인" 버튼만 노출, 기존과 동일 — 회귀 없음) 및 콘솔/서버 에러 없음 확인. **로그인 세션이 없어 팝오버 실제 클릭/닫힘과 활동 데이터 정확성은 다음 세션에서 확인 필요(NEXT_TASK.md 기록)**. 프로필 사진은 이번엔 표시 전용(요구사항 원문에도 업로드 UI 언급 없음) — 업로드 화면은 범위 밖으로 남겨둠, `avatar_url` 컬럼/`avatars` 버킷은 이미 준비돼 있어 후속 작업에서 바로 연결 가능.

## 2026-08-07 (EPIC-087-PHASE-E — 스튜디오 대관/물품대여 신청 관리, 기존 포트폴리오 등록 메뉴 완전 교체)
- **`ADMIN_NAV` 교체(사용자 확인 완료)**: "스튜디오 포트폴리오 등록"(`styling_projects` CRUD, 신청 관리와 무관한 쇼케이스 콘텐츠 기능) 탭을 "스튜디오 대관/물품대여 신청 관리"(`/admin/rentals`, 신규)로 완전히 교체. 기존 `/admin/projects/new` 라우트는 삭제하지 않고 그대로 두되(URL 직접 접근 가능, `/admin/rentals`에서도 링크 제공) 최상위 탭에서만 뺌.
- **신규 스키마**: `item_rental_requests`(id/member_id/item_description/desired_start_date/desired_end_date/contact_note/status/created_at) — "물품 대여"는 지금까지 `/space-inquiry/item-rental`이 "준비 중" 정적 안내만 있고 실제 데이터 모델이 아예 없었다(공간 대관 `rental_bookings`와 달리). `orders`/`reservations`와 동일한 own-row+admin-bypass RLS 패턴(`docs/sql/EPIC-087-PHASE-E-item-rental-requests.sql`, Management API로 즉시 실행 완료).
- **실제 신청 폼**: `/space-inquiry/item-rental`을 정적 안내에서 실제 폼(물품 설명/희망 기간/연락처)으로 교체, 새 `POST /api/item-rental-requests`(`getRequestMember`+`scopedClient`, `reservations` 라우트와 동일한 패턴이지만 가격 계산이 없어 훨씬 단순)로 제출.
- **관리자 화면**: 새 `/admin/rentals` — 공간 대관(`rental_bookings`, 실데이터 이미 존재, admin-bypass select RLS로 anon 클라이언트 직접 조회 — 새 API 라우트 불필요)과 물품 대여(`item_rental_requests`, 상태 변경 select 드롭다운으로 즉시 저장) 두 섹션. 실제 유저용 대관(`/rental`)·대여(`/space-inquiry/item-rental`) 신청 페이지로 가는 링크 제공(둘 다 기존 `PageEditButton`이 이미 붙어있어 콘텐츠 수정 가능).
- **검증**: `npx tsc --noEmit`(0 errors), `npm run lint`(0 errors, 기존 41개+새 페이지의 동일 패턴 경고 1개=42개 — 처음엔 `<a>` 태그 2곳이 `@next/next/no-html-link-for-pages` 에러로 잡혀 `Link`로 교체해 해결). Browser pane에서 `/space-inquiry/item-rental` 폼 렌더링(비로그인 시 "로그인 후 신청하기" 정확히 표시)과 `/admin/rentals` 컴파일 확인, 콘솔 에러 없음.

## 2026-08-07 (EPIC-087-PHASE-D — 회원관리 정렬 + 티어 권한 매트릭스 + 안내 페이지 URL 설정)
- **스키마**: `membership_tiers`는 지금까지 공개 SELECT 정책만 있고 쓰기 정책이 전혀 없었다 — `docs/sql/EPIC-087-PHASE-D-membership-tiers-admin-write.sql`로 `boards_admin_write`와 동일한 관례의 admin-bypass UPDATE 정책 추가, Management API로 즉시 실행 완료.
- **회원 목록 정렬**: `admin/members/page.tsx`에 이름/이메일/등급/가입일 클릭 정렬 추가(이미 fetch된 행을 클라이언트에서 정렬 — 이 화면의 기존 all-client-state 방식과 동일). 헤더 컴포넌트를 렌더 중 재선언하면 React가 매번 새 컴포넌트로 인식해 상태가 리셋되는 문제(`react-hooks/static-components` 린트가 실제로 4개 에러로 잡아냄)가 있어 모듈 스코프로 분리.
- **티어별 접근 권한 매트릭스**: 새 `src/components/admin/TierPermissionMatrix.tsx` — 사용자가 확인해준 대로(질문 "매트릭스 의미") `membership_tiers`의 기존 boolean/enum 플래그(`board_write_scope`/`board_can_write_docent`/`board_can_create`/`board_has_patron_board`/`board_has_promo_board`)를 표로 일괄 편집. 새로 추가한 admin-write 정책 덕분에 direct `supabase.from("membership_tiers").update(...)`로 즉시 저장(새 API 라우트 없음).
- **멤버십 안내 페이지 설정**: 새 `src/components/admin/MembershipGuideUrlSetting.tsx` — PHASE-C의 리다이렉트 게이트가 읽는 `site_settings.membership_guide_redirect`를 관리자가 직접 편집. `site_settings`는 이미 admin insert/update/delete 정책이 있어(기존 `admin/navigation/settings/page.tsx`의 `upsertSetting`과 동일 패턴 확인) 새 정책 불필요.
- **검증**: `npx tsc --noEmit`(0 errors), `npm run lint`(0 errors, 기존 41개 경고만 — 처음엔 SortHeader 컴포넌트 렌더 중 재선언으로 4개 에러 발생, 모듈 스코프 이동으로 해결 확인).

## 2026-08-07 (EPIC-087-PHASE-C — 페이지/게시판 티어 접근 제어)
- **스키마**: `boards.min_rank_to_read`/`page_builder.min_rank_to_read`(int, nullable, `membership_tiers(rank)` 참조) 신규 — `docs/sql/EPIC-087-PHASE-C-tier-read-gate.sql`, Management API로 즉시 실행 완료(라이브 반영됨, 컬럼 존재 재확인).
- **게시판 게이팅**: `canReadBoard()`(`src/lib/serverAuth.ts`)에 `min_rank_to_read` 체크 추가 — 비회원은 rank -1 취급. 사용자가 확인해준 대로(질문 "티어 게이트 UX") **게시판은 기존 inline-lock 관례(잠긴 게시판을 회색 비활성 처리+메시지, `src/app/boards/page.tsx`)를 그대로 유지**하고 새 컬럼만 그 판정에 참여시킴 — 새 UX 도입 없음. `BOARD_RICH_FIELDS`에 컬럼 추가, `/api/boards`·`/api/boards/[slug]/posts`·`/api/boards/[slug]/posts/[post_slug]`·`/api/boards/feed`의 lockMessage/에러 문구가 accessLevel과 min_rank_to_read 중 더 높은 쪽 등급을 안내하도록 수정. `/api/boards/feed`가 애초에 `is_public`을 select하지 않아 비공개 게시판 글이 하단 피드에서 전혀 걸러지지 않고 있던 기존 버그도 같이 발견/수정(min_rank_to_read와 같은 select 추가 작업에서 자연스럽게 드러남).
- **페이지 게이팅(신규 UX, 사용자 확인)**: 게시판과 달리 페이지는 기존 게이팅 관례가 아예 없어 안내 메시지+리다이렉트로 구현 — 새 `src/lib/pageRankGate.ts`(`usePageRankGate` 훅, `useAuth()`+`site_settings.membership_guide_redirect` 조회, `memberLoading`까지 기다린 뒤 판정해 로그인된 충분 등급 사용자가 튕겨나가는 재발 버그 방지)를 `src/app/[...slug]/page.tsx`(catch-all)와 그 이전부터 있던 **정적 page.tsx 62개**(동일 템플릿, 스크립트로 기계적 패치 — `archive`/`community/**`/`docent/**`/`gallery`/`heritage/**`/`membership/**`/`mypage`/`salon/**`/`studio`/`treasures` 등)에 적용.
  - **의도적으로 제외한 7개(하이브리드 페이지)**: `boards/[board_slug]`, `shop/[id]`, `clubs/[id]`, `docent/[id]`, `attendance`, `downloads`, `u/[memberId]` — 이 파일들은 `fetchPublishedPageBySlug`가 페이지 전체가 아니라 보조 위젯 슬롯 하나일 뿐이라(주 콘텐츠는 게시판/상품/클럽/도슨트 상세 등 별도), 그 슬롯의 랭크로 페이지 전체를 리다이렉트하면 안 됨 — 게시판은 이미 자체 `min_rank_to_read`+inline-lock으로 게이팅되므로 실질적 공백 아님. 나머지는 후속 확인 필요 시 개별 검토(NEXT_TASK.md).
- **관리자 UI**: `BoardForm.tsx`에 "최소 글쓰기 등급"/"최소 열람 등급" 셀렉트 추가(edit 모드 전용 — 생성 API가 `min_rank_to_write`를 의도적으로 0 고정하고 body를 안 받아 create 모드엔 안 보임). 부수적으로 `min_rank_to_write`가 애초에 이 폼에 편집 UI가 전혀 없어 저장이 안 되던 기존 공백도 같이 메움. `admin/pages/[id]/page.tsx`에 "최소 열람 등급" 셀렉트 추가, 기존 `handleSavePage`(direct supabase update)에 그대로 실어 저장 — 새 API 라우트 없음.
- **검증**: `npx tsc --noEmit`(0 errors, 78개 파일), `npm run lint`(0 errors, 기존 41개 경고만). Browser pane으로 실제 왕복 확인 — `/silo-store/treasures`(catch-all 경유)에 Management API로 `min_rank_to_read=3` 설정 후 비로그인 방문 시 `/membership?gate=3`로 정상 리다이렉트, 콘솔 에러 없음 확인 후 원상복구.

## 2026-08-07 (EPIC-087-PHASE-B — 사이트 메뉴: 연동 시각화, 게시판 전체 재배치 D&D, 체크박스 일괄 삭제)
- **연동 시각화**: `CategoryDetailModal`(관리 모달)의 연결된 게시판 요약에 게시판 종류(`board_type`)·활성/비활성(`is_public`) 배지를 추가 — 이전엔 `is_public`만 체크박스로 편집 가능했고 종류는 아예 안 보였음.
- **게시판 전체 재배치 D&D**(사용자가 4개 옵션 질문 중 "트리 간 완전한 재배치"를 선택): `CategoryTreeManager.tsx`의 기존 site_navigations 트리(`DndContext`)에 게시판 행을 새로 편입 — 각 분기(nav 행) 아래 `BoardListLevel`(전용 `SortableContext`, 컨테이너 id `boardlist-<branchId>`)로 그 분기에 배정된 게시판을 보여주고, 최상위에 "📋 미배정 게시판" 섹션도 추가. 게시판 카드를 다른 분기 아래로 드래그하면 `page_modules`(`module_type='board'`) 연결 자체를 그 분기의 페이지로 옮기고(기존 링크는 삭제), 같은 분기 안에서는 `boards.sort_order`만 재계산해 `PATCH /api/admin/boards/[id]`로 저장(`UnassignedBoardsPanel`이 이미 쓰던 관례 재사용, 새 API 라우트 없음). 목적지 분기에 아직 연결된 페이지가 없으면(href 미지정) 에러 메시지로 막고 배정하지 않음. 게시판 dnd id는 `board-` 접두사로 site_navigations 행(원본 uuid)과 네임스페이스를 분리.
- **체크박스 일괄 삭제**: 페이지(site_navigations 행)와 게시판 모두에 체크박스 추가, 헤더의 "선택 N개 삭제" 버튼으로 한 번에 삭제 — 페이지는 기존 `deleteRow()`와 동일하게 "미분류 페이지" 버킷 소속이면 연결된 `page_builder` 페이지까지 함께 지우고(재생성 방지), 게시판은 기존 `DELETE /api/admin/boards/[id]`(글이 있으면 막아주는 기존 가드 그대로 재사용).
- **검증**: `npx tsc --noEmit`(0 errors), `npm run lint`(0 errors, 기존 41개 경고만) — 관리자 로그인 세션이 없어 실제 드래그/체크박스 클릭 테스트는 다음 세션에서 확인 필요(NEXT_TASK.md 기록).

## 2026-08-07 (EPIC-087-PHASE-0/A — Admin Dashboard Overhaul 착수: 스키마 확인 + 전체 글 관리 일괄 작업)
- **배경**: "[Stage 2] Admin Dashboard Overhaul, Tier-based Access Control, & Global UX" 9개 요구사항(2.1~2.6) — 착수 전 3개 병렬 Explore 조사 + Plan agent 종합으로 실제 코드 상태를 먼저 확인. 여러 요구사항(게시판 슬러그 잠금, 위젯 게시판 드롭다운)이 이미 의도된 설계이거나 해결돼 있었고, 반대로 "물품대여 신청 관리"·GNB 팝오버는 아예 없는 데이터 모델부터 새로 만들어야 함을 확인 — 상세 계획은 세션 내 `EnterPlanMode`로 사용자와 4개 아키텍처 결정(티어 게이트 리다이렉트 범위/게시판 D&D 범위/매트릭스 의미/스튜디오 메뉴 교체 여부) 확정 후 PHASE-0~G로 분할.
- **PHASE-0**: Management API로 라이브 DB 확인(`membership_tiers` 6개 랭크가 `serverAuth.ts`의 `RANK_LABELS`와 일치, `boards.category='mind-diary'` 행 실존 확인, `page_builder`/`page_modules`/`site_settings` 컬럼 형태 확인). `site_settings`에 이 EPIC 전체가 재사용할 키 2개 예약: `membership_guide_redirect`(PHASE-C/D), `footer_config`(PHASE-G) — `docs/sql/EPIC-087-PHASE-0-site-settings-seed.sql`, Management API로 즉시 실행 완료.
- **PHASE-A (2.1)**: `AdminPostsBoardView.tsx`에 체크박스 다중 선택(전체 선택/부분 선택 indeterminate) + 일괄 숨기기/숨김 해제/삭제 툴바 추가 — 기존 단건 `toggleHidden`/`handleDelete`와 동일한 direct-Supabase 호출을 선택 집합에 `Promise.all`로 확장, 새 API 라우트 없음. `BoardForm.tsx`에 "이 게시판을 사용 중인 페이지" 읽기 전용 역방향 조회 추가(`page_modules` where `board_id=`) — `/admin/boards/[id]` edit 모드에서만 노출.
- **확인된 비-버그**: 게시판 슬러그 수정 잠금(`BoardForm.tsx`, `disabled={mode==="edit"}`)은 다른 화면이 슬러그 값을 참조해 의도적으로 잠근 것 — 코드 변경 없음. `/admin/pages/[id]` 위젯 게시판 드롭다운은 EPIC-084-REVISED의 `CategoryBoardPicker` 도입 이후 이미 정상 동작 — 코드 변경 없음.
- **검증**: `npx tsc --noEmit`(0 errors).

## 2026-08-07 (EPIC-086 — Slide Module 글쓰기 버튼)
- **배경**: 컴퓨터 A/B 동기화 중 발견 — 이전 컴퓨터에 남아있던 EPIC-084 미커밋 WIP(17개 파일)를 검토한 결과 전부 이미 병합된 EPIC-084/EPIC-084-REVISED/EPIC-085에서 동일하거나 더 개선된 형태로 구현돼 있었으나(예: 외부 이미지 재업로드는 SSRF 방어까지 갖춘 `/api/media/fetch-external`로 대체, "목록으로" 링크는 `PostDetailClient.tsx`에서 상하단 2곳으로 개선), Gallery 위젯(EPIC-084)에만 있고 Slide 위젯에는 없던 "글쓰기" 버튼 하나만 순수 누락으로 확인돼 이번에 반영. WIP의 나머지(4모서리 이미지 리사이즈 등)는 EPIC-085 문서가 "점검 결과 기존 구현이 이미 정상 동작해 추가 구현 불필요"로 명시적으로 보류한 항목이라 재도입하지 않음.
- **`src/components/modules/SlideModule.tsx`**: `GalleryModule`과 동일한 패턴으로 `boardId`(optional) prop 추가 — 있으면 우측 상단에 "글쓰기"(`/write?boardId=`) 버튼 노출, 없으면(HubRenderer/SlideRenderer처럼 여러 게시판을 한꺼번에 모아 보여주는 자리) 기존처럼 버튼 없음(하위 호환).
- **`src/components/modules/DbFeedModules.tsx`**: `DbSlideModule`이 이미 갖고 있던 `boardId`를 `SlideModule`로 전달.
- **검증**: `npx tsc --noEmit`(0 errors), `npm run lint`(0 errors, 기존 경고만), 로컬 dev 서버(비로그인)에서 홈페이지 Slide 위젯에 "글쓰기" 버튼이 렌더링되는 것과 콘솔 에러 없음을 Browser pane으로 확인.

## 2026-08-06 (EPIC-084-REVISED 후속 — R2 이미지 업로드 완전 해결, 실기 검증 완료)
- **`.env.local` 실제 값 확인 과정에서 두 번째 버그 발견**: 사용자가 R2 자격증명 5종을 입력했는데, `R2_BUCKET_NAME`과 `R2_PUBLIC_URL`이 한 줄(`R2_BUCKET_NAME/R2_PUBLIC_URL=https://...`)에 잘못 합쳐져 있어 실제로는 `R2_BUCKET_NAME`이 빈 값이었다 — 두 줄로 분리하고 `R2_PUBLIC_URL`에는 이미 입력된 값을 그대로 유지, `R2_BUCKET_NAME`은 사용자가 채워 넣음(`silo-media-storage`).
- **세 번째 문제 — R2 버킷에 CORS 정책 없음**: 자격증명을 다 채운 뒤에도 `POST /api/media/presigned`는 200이었지만 브라우저의 실제 R2 PUT이 CORS 프리플라이트 실패로 막혔다(`No 'Access-Control-Allow-Origin' header`) — presigned URL 발급 자체는 서버가 하지만, 실제 PUT은 브라우저가 R2 도메인에 직접 요청하므로 R2 버킷 쪽에 CORS 정책이 없으면 이 요청 자체가 브라우저에서 차단된다. 사용자가 Cloudflare 대시보드에서 R2 버킷(`silo-media-storage`) CORS 정책을 추가(`AllowedOrigins: localhost:3000/dev.silostore.net/silostore.net`, `AllowedMethods: PUT/GET`).
- **실기 검증**: 로컬 dev 서버 + 실제 admin 세션(가짜 File 객체를 합성해 파일 선택기 이벤트를 직접 발생시키는 방식)으로 `POST /api/media/presigned`(200) → R2 PUT(200, CORS 통과) → `POST /api/media`(200, media_library insert) → `GET <public R2 URL>`(200, 실제 이미지 바이트 반환)까지 전체 파이프라인 왕복 확인 완료. 테스트로 만든 `media_library` 행 2건은 Management API로 정리, R2에 남은 68바이트 테스트 오브젝트 2개는 무시 가능한 수준이라 그대로 둠.
- **결론**: EPIC-084-REVISED가 "코드 버그 아님"으로 규명했던 이미지 업로드 문제는 실제로 (1) 이 컴퓨터의 R2 자격증명 누락, (2) 자격증명 입력 시 오타로 두 변수가 한 줄에 합쳐짐, (3) R2 버킷 CORS 정책 부재 — 3단계 환경설정 문제가 겹쳐 있었던 것으로 최종 확인. 코드 변경 없음(전부 인프라/설정 문제).

## 2026-08-06 (EPIC-084-REVISED — [Stage 2] Universal Navigation, Contextual Write Flow, 3-Column Category Selector, & Editor UX Overhaul)
- **배경**: EPIC-084에서 놓친 요구사항과 새로 발견된 치명적 UX 버그(이미지 업로드 무반응, 게시판 선택 UUID 노출)를 정확히 재작업하라는 긴급 지시. 11개 세부 항목 전부 처리.
- **[CRITICAL] 이미지 업로드 "먹통" 버그(1.3) — 원인 규명**: 실제로 재현 테스트한 결과 툴바 버튼→파일 선택기→`onImageUpload` 호출 경로 자체는 정상 동작했다(가짜 File 객체를 합성해 `change` 이벤트를 강제 발생시켜 확인) — 진짜 원인은 `POST /api/media/presigned`가 500을 반환하는 것이었고, 그 이유는 **이 로컬 머신의 `.env.local`에 R2 자격증명(`R2_ACCOUNT_ID`/`R2_ACCESS_KEY_ID`/`R2_SECRET_ACCESS_KEY`/`R2_BUCKET_NAME`/`R2_PUBLIC_URL`)이 전혀 없었기 때문**이다(`.env.local`은 gitignore 대상이라 컴퓨터마다 따로 설정해야 함 — Multi-Device Sync 상 알려진 함정, CLAUDE.md 참고). 코드 결함이 아니라 이 머신의 환경변수 누락이 원인이므로, 사용자가 R2 자격증명을 다시 입력해야 실제 업로드가 동작한다(NEXT_TASK.md 기록). **다만 실패해도 화면에 아무 표시가 없어 "먹통"으로 보이던 것은 진짜 UX 결함이라 함께 고쳤다**: `BlockEditor.tsx`의 `handleImageFiles`/`uploadAndInsertImages`/`uploadAndInsertExternalImages`에 실패 시 `alert()`로 원인을 노출.
- **Tiptap 중복 확장 경고 수정**: `blockEditorCore.ts` — 최근 `@tiptap/starter-kit`이 `link`/`underline`을 기본 번들에 포함하는데 별도로 `Link.configure()`/`Underline`을 또 추가하고 있어 "Duplicate extension names" 경고가 뜨던 것을 `StarterKit.configure({ link: false, underline: false })`로 수정.
- **GNB 글쓰기 버튼 위치 + 목록 버튼(1.1)**: `Navbar.tsx` — "글쓰기" 버튼을 "마이 페이지" 왼쪽(EPIC-084)에서 **오른쪽**으로 정정(최종 순서: About Silo | 사일로 상점 | 살롱데상 | 스튜디오 | 마이 페이지 | 글쓰기). `PostDetailClient.tsx` — 기존엔 상단에만 있던 "목록으로" 링크를 하단(BoardPostListPanel 아래)에도 추가.
- **3열 Miller Columns 카테고리/게시판 선택기(1.2)**: `src/components/common/CategoryBoardPicker.tsx`(신설) — 기존 단일 `<select>`(일부 게시판은 slug가 없어 UUID 앞 8자리가 그대로 라벨/값으로 노출되던 문제 포함)를 완전히 대체하는 3열 캐스케이딩 선택기. 1열(최상위 5개 카테고리) → 2열(하위 카테고리) → 3열(그 아래 전체 서브트리에 속한 게시판, 재귀 탐색이라 깊이에 상관없이 전부 잡힘) — 모든 항목에 `title`/`name`만 노출(UUID 노출 없음), 진입 시 `value`(게시판 slug)로부터 1/2열 선택 경로를 역산해 자동 pre-select. `useBoardOptions.ts`(수정) — 이 선택기가 필요로 하는 원본 `branches`/`boardBranchMap`을 추가로 노출(중복 조회 방지). `WriteBoardForm.tsx`/`edit/page.tsx`/`admin/pages/[id]/page.tsx`(위젯 게시판 선택) 3곳 전부 교체, 이제 완전히 안 쓰는 `BoardPageSelect.tsx` 삭제.
- **에디터 미디어 정밀 수정(1.3)**: **커서 위치 삽입** — 이미지 업로드(툴바/드래그/붙여넣기)가 항상 문서 맨 끝에 삽입되던 것을 커서 위치부터 순서대로 삽입하도록 변경. EPIC-079가 이미 확인해둔 함정(`.focus()`+`insertContent` 반복 시 직전 삽입한 atom 블록이 NodeSelection으로 남아 다음 삽입이 "교체"돼버림)을 피하기 위해, 다건 업로드는 위치를 직접 추적하며 `insertContentAt`/`view.state.tr.insert`로, 갤러리/표/임베드/출처/링크카드 등 단건 삽입은 공용 `insertAtCursor()`(`insertContentAt(selection.to, ...)`)로 통일 — 표는 Tiptap 내장 `insertTable` 커맨드라 원래도 커서 위치였음. **클립보드 이중 지원** — 바이너리 이미지 붙여넣기는 기존대로, "이미지 주소 복사"(순수 텍스트 URL) 붙여넣기도 이제 hotlink 대신 R2로 재호스팅 후 삽입(`uploadAndInsertExternalImages` 재사용)하도록 변경. **모든 미디어 리사이즈** — 기존엔 이미지(FigureImage)만 가능했던 클릭→드래그 리사이즈를 공용 훅(`useDragResizeWidth`)으로 뽑아 갤러리(`GalleryBlock`에 `width` 속성 신설, `blockEditorCore.ts`)와 임베드(HTML 코드 임베드 포함 — provider가 "customHtml"인 것도 같은 embed 노드라 자동 적용, 기존 `width` 속성 재사용)에도 동일한 코너 핸들 적용. LinkCard는 전용 CSS 카드 스타일이 없는 단순 텍스트 링크라 리사이즈 대상에서 제외(시각적으로 의미가 없어 판단상 보류).
- **라우팅/사이트 구성관리(1.4)**: 갤러리 위젯 글쓰기 버튼/게시판 연동은 EPIC-084에서 이미 구현 완료 확인. **미분류 페이지 삭제 재검증** — 실제 admin 세션으로 confirm() 다이얼로그를 우회해(자동화 브라우저는 네이티브 dialog를 자동 거부함, `window.confirm` 오버라이드로 실측) 삭제 버튼 클릭 → `DELETE /api/admin/pages/[id]` 200 → 목록에서 즉시 사라짐까지 실측 확인(EPIC-084의 수정이 정상 동작 중임을 재확인). **URL 정돈** — 조사 결과 "미분류 페이지" 버킷이 53개까지 부풀어 있던 실제 원인을 발견: `CategoryTreeManager.tsx`의 `ensureUnassignedPagesInTree()`가 위젯이 단 하나도 없는 `page_builder` 행(EPIC-064A가 전체 라우트에 기계적으로 부착한 PageEditButton용 빈 draft — `/boards`, `/login`, `/settings` 등 애초에 PageBuilderRenderer를 호출하지 않는 페이지들)까지 무조건 흡수하고 있었다. `docs/sql/EPIC-084-REVISED-cleanup-empty-unassigned-pages.sql`(Management API로 즉시 실행 완료)로 위젯 0개인 기존 흡수 항목만 정리(53→23개, `docs/navigation-blueprint.md` §9의 원래 추정치와 정확히 일치) — 위젯이 있는 나머지 23개(예: Gallery Awards/Parties, Membership \* 등, EPIC-080이 그림자 페이지 파일만 지우고 그 밑 콘텐츠는 안 지운 것으로 추정)는 실제 콘텐츠가 들어있어 삭제하지 않고 "개별 검토 필요"로 남김(NEXT_TASK.md 기록). `ensureUnassignedPagesInTree()`도 앞으로 위젯 0개 페이지는 이 트리에 흡수하지 않도록 수정해 재발 방지.
- **검증**: `npx tsc --noEmit`(0 errors), `npm run lint`(0 errors, 새 경고 없음), `npm run build` 성공. **실제 admin 세션으로 브라우저 실측**: 이미지 업로드 500 에러의 근본 원인 확인(네트워크 탭), `/write` 3열 선택기 렌더링 확인, 미분류 페이지 삭제 API 200 + 목록 갱신 확인. **R2 자격증명이 없어 실제 업로드 성공까지는 이 세션에서 재현 불가** — 사용자가 R2 키를 `.env.local`에 다시 입력해야 최종 확인 가능.

## 2026-08-06 (EPIC-085 — [Stage 2] Universal Block Renderer, Frictionless Archiving (Scraping System), & Dynamic OG/SEO Engine)
- **배경**: EPIC-083/084로 완성된 Tiptap JSON 블록(`posts.body_json`)을 표준화된 읽기 전용 컴포넌트로 정리하고, 핵심 리텐션 장치인 "원클릭 스크랩"과 소셜 공유용 동적 OG 메타데이터를 구축하라는 요청.
- **Universal Block Renderer(2.1)**: `src/components/content/UniversalBlockRenderer.tsx`(신설) — 지금까지 `PostBody.tsx` 안에만 있던 읽기 전용 렌더링 로직(sanitize/커스텀 폰트 주입/갤러리 캐러셀/Instagram·raw HTML 임베드 활성화/Lightbox)을 재사용 가능한 공용 컴포넌트로 분리(에디터 툴바/NodeView 핸들러는 전혀 포함하지 않음 — 완전한 읽기 전용 경로). `PostBody.tsx`는 이제 게시글 상세 전용 관심사(대표 이미지 본문 중복 제거)만 얹는 얇은 래퍼로 축소. 점검 결과 Text/Heading 정렬(`TextAlign`)·색상(`Color`/`TextStyle`)·Resized Image(`FigureImage` width)·Table/SourceAttribution은 이미 `blockEditorCore.ts`의 스키마 자체가 처리하고 있어 추가 구현이 필요 없었음 — 실제 발견한 갭 2건만 `src/app/globals.css`에서 수정: (1) `table`이 좁은 화면에서 페이지 전체를 가로로 밀어내던 문제(`display:block; overflow-x:auto` 추가), (2) 화면비율 wrapper가 없는 기본 iframe 임베드(유튜브 aspectRatio 미지정/Vimeo/Maps 등)가 컨테이너를 넘치던 문제(`.embed`/`.embed iframe`에 `max-width:100%` 추가).
- **Frictionless Archiving(2.2)**: `docs/sql/EPIC-085-user-scraps.sql`(신설, Management API로 즉시 실행 완료) — `user_scraps` 테이블(`user_id`는 members.id가 아니라 `auth.users(id)`를 직접 참조, `(user_id, post_id)` UNIQUE) + RLS(select/insert/delete 전부 `auth.uid() = user_id`만). **기존 `post_bookmarks`(EPIC-047 설계)는 라이브 DB에 한 번도 마이그레이션되지 않아 실제로는 항상 실패하던 죽은 기능이었음을 발견** — 이 EPIC이 그 자리를 대체한다: `PostActions.tsx`의 "북마크" 버튼을 `ScrapButton`으로 교체, `/api/boards/[board_slug]/posts/[post_slug]/bookmark` 라우트 삭제, 게시글 상세 GET 응답의 `bookmarkedByMe` 필드 제거. `src/components/common/ScrapButton.tsx`(신설) — postId만 받는 자기완결형 컴포넌트, 자체적으로 초기 상태 조회 + Optimistic UI 토글, 비로그인 시 로그인 유도 모달. `src/app/api/scraps/[postId]/route.ts`(신설, GET/POST) — own-row RLS에 위임하는 표준 패턴. 배치: 게시글 상세 상단(목록으로 링크 옆)/하단(PostActions 안), `GalleryModule.tsx` 카드 우상단(카드 Link 바깥의 절대위치 형제 요소로 — `<a>` 안에 `<button>` 중첩은 유효하지 않은 HTML이라 회피). `src/app/mypage/archive/page.tsx` + `src/components/mypage/panels/ScrapsPanel.tsx`(신설) — "아카이브 / 내 스크랩" 탭(`mypageConfig.ts`에 추가), `user_scraps`+`posts`+`boards` 조인으로 스크랩한 글을 게시판 카테고리별 그리드로 표시. 기존 "나의 컬렉션"(`member_collections`, 자유 입력 스크랩북)과는 별개 기능임을 확인 — 겹치지 않음.
- **Dynamic SEO & Open Graph(2.3)**: `src/lib/utils/extractPostMetadata.ts`(신설) — description은 새로 작성(첫 번째 텍스트 있는 paragraph, 최대 120자+말줄임), ogImage는 `blockEditorCore.ts`에 이미 있던 `findFeaturedImage()`/`findFirstImage()`(★ 대표 이미지 지정 + 폴백, EPIC-079-HOTFIX-3/PHASE-5)를 그대로 재사용(중복 구현 없음). 찾은 이미지가 없으면 존재하지 않는 기본 OG 이미지 경로를 지어내지 않고 `images` 필드 자체를 생략(깨진 링크보다 없는 편이 나음 — `public/`에 실제 사이트 대표 이미지 에셋이 아직 없어 NEXT_TASK.md에 후속 기록). 게시글 상세 라우트(`src/app/boards/[board_slug]/[post_slug]/`)를 Server Component 래퍼(`page.tsx`, `generateMetadata` 신설) + 기존 클라이언트 로직(`PostDetailClient.tsx`로 이름만 변경, 내용 동일)으로 분리 — client fetch 기반 화면이라 `generateMetadata`를 같은 파일에 못 두는 Next.js App Router 제약 때문. `generateMetadata`는 anon Supabase 클라이언트로 board→post를 조회해 `og:title`/`og:description`/`og:image`/`twitter:card`를 동적 생성.
- **검증**: `npx tsc --noEmit`(0 errors), `npm run lint`(0 errors, 새 경고 2건은 기존 코드베이스 전체에 이미 있던 동일 `react-hooks/set-state-in-effect` 관용구 — `ScrapButton.tsx`/`ScrapsPanel.tsx`), `npm run build` 성공(`/mypage/archive`, `/api/scraps/[postId]`, `[board_slug]/[post_slug]` 동적 라우트 전부 정상 컴파일). **로그인 세션이 필요한 실제 클릭 검증(스크랩 토글/로그인 유도 모달/마이페이지 아카이브 목록/실제 SNS 공유 시 OG 카드 렌더링)은 이 세션에서 재현하지 못함** — 다음 세션에서 확인 필요(NEXT_TASK.md 기록).

## 2026-08-06 (EPIC-084 — [Stage 2] Universal Routing & Contextual Write Flow, Editor Media Control, & Admin Clean-up)
- **배경**: "글쓰기 버튼이 지금 보고 있는 게시판을 자동으로 전달하지 않는다 + 에디터 미디어 UX 마무리 + 라우팅/관리자 버그"라는 요청. 6개 하위 항목을 한 EPIC으로 묶어 처리.
- **Contextual Write(2.1)**: `src/components/boards/WriteBoardForm.tsx`(신설) — 기존 `/boards/[board_slug]/write`의 폼/제출 로직을 그대로 뽑아 게시판 컨텍스트 유무와 무관하게 재사용 가능하게 만들었다. `src/app/write/page.tsx`(신설) — `?boardId=<board_slug>` 쿼리 파라미터로 진입하는 전역 Contextual Write 진입점(useSearchParams, Suspense로 감쌈). `src/components/Navbar.tsx`(수정) — 상단 탭 "마이 페이지" 왼쪽에 전역 "글쓰기" 버튼 추가, 현재 경로가 `/boards/<slug>`(하위 포함)면 `/write?boardId=<slug>`로, 아니면 `/write`(사용자가 직접 게시판 선택)로 이동. `src/components/boards/BoardPageSelect.tsx`(수정) — 선택값이 빈 문자열일 때 "게시판을 선택하세요" 플레이스홀더 옵션 추가.
- **위젯 글쓰기 버튼 + 목록으로(2.1)**: `src/components/modules/GalleryModule.tsx`(수정) — 우측 상단에 그 갤러리가 연결된 게시판으로 바로 쓰는 "글쓰기" 버튼 추가(`BoardModule`은 EPIC-054C 때부터 이미 `BoardHeader`에 글쓰기 버튼이 있었음, 갤러리 위젯만 없었던 것). `src/app/boards/[board_slug]/[post_slug]/page.tsx`(수정) — 게시글 상세 상단에 "← 목록으로"(해당 게시판으로 돌아가는) 링크 추가.
- **에디터 미디어(2.2)**: 갤러리 슬라이더(CSS scroll-snap + `galleryCarousel.ts`/에디터 내 `GalleryView` React 구현)를 점검한 결과 두 경로 모두 정상 동작 — 실제 버그는 없었고, 두 구현이 서로 다른 코드베이스(정적 페이지는 vanilla JS, 에디터 실시간 편집은 React state)로 나뉘어 있다는 기존 설계 그대로임을 확인. **외부 다중 이미지 붙여넣기**: `src/app/api/media/fetch-external/route.ts`(신설) — 브라우저의 CORS 제약으로 클립보드 `text/html`에 담긴 다른 사이트 `<img src>`를 직접 fetch할 수 없어, 서버가 대신 내려받아(SSRF 방지용 사설/루프백 호스트 차단 + 25MB/15초 상한) R2에 재호스팅하고 `media_library`에 기록하는 라우트. `src/lib/r2Upload.ts`(수정) — `uploadExternalUrlToR2Media()` 클라이언트 헬퍼 추가. `src/components/editor/BlockEditor.tsx`(수정) — `handlePaste`에 `text/html`의 절대 http(s) `<img>` src를 감지해 `uploadAndInsertExternalImages()`로 R2에 재호스팅 후 삽입하는 분기 추가(기존엔 스키마 기본 파싱이 외부 URL을 그대로 hotlink했음). **이미지 리사이즈**: 조사 결과 EPIC-083 이전부터 `FigureImageView`(BlockEditor.tsx)에 클릭→우하단 핸들 드래그로 너비 조절하는 기능이 이미 구현돼 있었음(`width` attr가 저장/복원까지 완비) — 별도 구현 불필요, 신규 코드 없음.
- **라우팅/사이트 구조(2.3)**: `src/lib/navConfig.ts`(수정) — `buildNavTree()`의 `sidebar-left`/`sidebar-right` 분기가 DB(`site_navigations`)에 이미 있던 `href`(사일로상점→`/shop`, 살롱데상→`/community`)를 조립 결과에서 누락시키던 버그를 수정(다른 분기는 넣고 있었는데 이 분기만 빠져 있었음) — 이게 "상단 탭/사이드바 클릭이 안 먹는다"는 신고의 실제 원인. `FALLBACK_NAV_TABS`에도 동일 href 추가(방어적 일관성). `src/components/LeftSidebar.tsx`/`RightSidebar.tsx`(수정) — 패널 헤더 라벨을 `tab.href`가 있으면 Link로 렌더링(기존엔 항상 클릭 불가한 `<span>`). **URL 구조 정돈**: `docs/navigation-blueprint.md` §9가 이미 "확인 필요"로 표기해둔 항목(소개지/포스터 동일 href 등)은 실제 버그가 아니라 사용자 확인이 우선이라 이번엔 손대지 않음 — 위 라우팅 버그 수정이 이번에 실제로 재현 가능했던 "라우팅 오류"였음. **미분류 페이지 삭제 버그**: `src/components/admin/CategoryTreeManager.tsx`(수정) — "미분류 페이지" 버킷 노드를 지우면 `site_navigations` 링크 행만 삭제됐는데, 그 직후 호출되는 `load()`의 `ensureUnassignedPagesInTree()`가 여전히 존재하는 원본 `page_builder` 페이지를 보고 같은 링크를 즉시 재생성해 "삭제가 안 된다"로 보였다 — 버킷 노드 삭제 시 링크가 아니라 `/api/admin/pages/[id]` DELETE로 원본 페이지(+위젯)까지 지우도록 수정.
- **위젯 게시판 드롭다운 계층화(2.4)**: `src/app/admin/pages/[id]/page.tsx`(수정) — 위젯 설정의 "게시판 선택" `<select>`가 게시판을 임의 순서로 flat 나열하던 것을, `adminTreeGrouping.ts`(EPIC-072B, "사이트 구성 관리"가 이미 쓰던 site_navigations 기반 브랜치 매칭)를 재사용해 실제 게시되는 페이지/카테고리 경로별 `<optgroup>`으로 묶었다.
- **검증**: `npx tsc --noEmit`(0 errors), `npm run lint`(0 errors, 새 경고 없음 — 기존 `react-hooks/set-state-in-effect` 경고만 그대로), `npm run build` 성공(`/write`, `/api/media/fetch-external` 포함 전체 라우트 정상 컴파일). **로그인 세션이 필요한 실제 클릭 검증(글쓰기 버튼 클릭 흐름, 외부 이미지 붙여넣기 재호스팅, 미분류 페이지 삭제)은 이 세션에서 로그인 세션을 만들 수 없어 코드 리뷰/타입체크/빌드로만 검증 — 다음 세션에서 관리자로 로그인해 확인 필요.**

## 2026-08-06 (EPIC-083 — [Stage 2] Universal Editor Extension: Media Library UI, Text Color, Table, Custom Font Upload, & Content Attribution)
- **배경**: EPIC-082가 만든 R2 Direct Upload API + `media_library` DB를 실제로 Tiptap 에디터 UI에 배선하는 다음 단계 — 사용자가 SQL Editor에서 `docs/sql/EPIC-082-media-library.sql`을 실행하고 Vercel에도 R2 환경변수를 등록한 직후 착수.
- **`docs/sql/EPIC-083-custom-fonts.sql`(신설, Management API로 즉시 실행 완료)**: `custom_fonts` 테이블(id/font_name/font_url/file_format/created_at, font_name UNIQUE) + RLS(SELECT 전체 공개, INSERT/UPDATE/DELETE는 `members.is_admin`만 — media_library와 달리 "본인" 경로가 없다, Admin 전용 업로드 기능이라서).
- **`src/app/api/media/route.ts`(신설)**: `media-architecture.md` §6이 미구현으로 명시했던 "업로드 완료 확인 API" — R2 PUT 성공 후 클라이언트가 `media_library`에 메타데이터 행을 만드는 두 번째 단계(POST, `getRequestMember`의 `scopedClient`로 RLS 하에서 insert).
- **`src/app/api/media/presigned/route.ts`(수정)**: 폰트 파일(.woff2/.woff/.ttf/.otf) 업로드도 이 파이프라인을 타도록 MIME 검증을 확장 — 브라우저/OS별로 폰트 파일의 `File.type`이 빈 문자열/`application/octet-stream`으로 오는 경우가 흔해(MIME 레지스트리 미등록), 확장자 기반 허용 목록(`ALLOWED_FONT_EXTENSIONS`)을 이미지/영상/오디오의 MIME 프리픽스 검증과 별도로 추가.
- **`src/lib/media.ts`(수정)**: `CreateMediaAssetRequest`(POST /api/media 요청 바디), `CustomFont`/`CustomFontRow`/`customFontFromRow`(custom_fonts 타입 계약) 추가.
- **`src/lib/r2Upload.ts`(신설)**: 클라이언트 업로드 헬퍼 — `uploadToR2Media()`(presigned 발급 → R2 PUT → 이미지/영상이면 치수·재생시간을 `<img>`/`<video>`로 직접 읽기 → `/api/media` insert, 전체 흐름을 함수 하나로 캡슐화), `uploadMultipleToR2Media()`(갤러리 다중 업로드), `uploadFontToR2()`(media_library를 거치지 않는 폰트 전용 업로드 — R2 PUT까지만).
- **`src/lib/useCustomFonts.ts`(신설)**: `custom_fonts` 목록을 조회해 `document.head`에 `<style id="custom-fonts-style">`로 `@font-face` 규칙을 동적 주입하는 훅 — 에디터 툴바(폰트 드롭다운)와 `PostBody.tsx`(글 상세) 양쪽이 같은 스타일 태그를 공유해 항상 최신 목록을 반영.
- **`src/app/admin/fonts/page.tsx`(신설) + `src/app/admin/layout.tsx`(수정)**: Admin 전용 커스텀 폰트 업로드/삭제 화면 — 이름 입력 + 파일 선택(.woff2/.woff/.ttf/.otf) → `uploadFontToR2` → `custom_fonts` insert. 관리자 상단 탭에 "커스텀 폰트 관리" 추가.
- **`src/lib/blockEditorCore.ts`(수정, 서버/클라이언트 공유 스키마)**: `@tiptap/extension-table`+`table-row`+`table-header`+`table-cell`(리사이즈 비활성, 행/열 추가·삭제만 지원) + `@tiptap/extension-font-family`(기존 설치돼 있던 `Color`/`TextStyle`와 함께 `coreExtensions()`에 추가 — Color/TextStyle 자체는 EPIC-052/053 때부터 이미 설치돼 있었으나 툴바 UI가 없어 미사용 상태였음). `SourceAttributionBlock`(신설 커스텀 노드, `url`/`sourceName` attrs, atom block) — 렌더링은 "출처: {sourceName|url}" 텍스트 + 🔗 아이콘의 아웃라인 뱃지, `target="_blank" rel="noopener noreferrer"`. `FigureImageAttrs`/`GalleryImageAttrs`에 선택적 `mediaId` 필드 추가(media-architecture.md §6이 예고한 점진적 전환 — 기존 Supabase Storage 업로드 데이터는 이 필드가 없어도 `src` 문자열로 계속 렌더링됨, 하위 호환 유지).
- **`src/lib/sanitize.ts`(수정)**: `colspan`/`rowspan`(표 셀 병합), `data-source-url`/`data-source-name`(SourceAttribution 라운드트립) 허용 — `table`/`thead`/`tbody`/`tr`/`th`/`td`는 EPIC-053 때 이미 허용돼 있었음.
- **`src/components/editor/BlockEditor.tsx`(수정)**: 툴바에 색상 칩 7개+임의색상(`<input type=color>`)+초기화(`Color`/`TextStyle` 커맨드), 폰트 드롭다운(기본 4종 + `useCustomFonts()`로 실시간 반영되는 커스텀 폰트 optgroup), 표 삽입/수정 드롭다운(3×3 삽입, 행/열 추가·삭제, 표 삭제 — `editor.can()`으로 컨텍스트에 안 맞는 동작 비활성화), "출처 입력" 버튼(`SourceAttributionModal` 신설, URL 필수+출처명 선택) 추가. 이미지/갤러리 업로드 경로(붙여넣기/드래그/툴바 버튼/갤러리 파일첨부) 전부를 기존 Supabase Storage(`src/lib/storage.ts`)에서 R2 Direct Upload(`uploadToR2Media`/`uploadMultipleToR2Media`)로 전환 — 갤러리는 이미지뿐 아니라 영상(mp4 등)도 같은 파일 선택기로 함께 업로드 가능해짐(`accept="image/*,video/*"`).
- **`src/components/boards/PostBody.tsx`(수정)**: `useCustomFonts()` 호출 추가 — 글 상세 페이지에서도 본문에 커스텀 폰트가 쓰였으면 `@font-face`가 주입되도록.
- **`src/app/globals.css`(수정)**: `.source-attribution*`(아웃라인 뱃지 스타일) + `table`/`th`/`td`(고정폭 표 스타일, `.selectedCell` 하이라이트) 추가. 갤러리 캐러셀(가로 스와이프+화살표+점 인디케이터) 스타일은 EPIC-079-PHASE-5 때 이미 완성돼 있어 이번엔 무변경(요청한 "인스타그램 스타일 슬라이더 갤러리"는 기존 `GalleryBlock`을 그대로 재사용 — 신규 GalleryNode를 따로 만들지 않음, `GalleryImageAttrs.mediaId`만 추가).
- **검증**: `npx tsc --noEmit`(0 errors), `npm run lint`(0 errors, 새 경고 2건은 기존 코드베이스에 이미 있던 동일 패턴 — `admin/fonts/page.tsx`의 effect 내 setState는 다른 admin 페이지들과 같은 관용구). 로컬 dev 서버(Browser pane)에서 글쓰기 화면 실제 렌더링 확인: 색상 칩/폰트 드롭다운/표 버튼/출처 버튼 전부 툴바에 노출, "표 삽입 (3×3)" 클릭 → 실제 3×3 표 삽입 확인(DOM 조회), "출처 입력" 모달에 URL+출처명 입력 후 삽입 → 에디터 본문에 `target="_blank" rel="noopener noreferrer"` 링크로 정상 삽입 확인, "미리보기" 전환 시 표/출처 카드 모두 `sanitizeHtml()`을 거친 뒤에도 그대로 렌더링됨을 확인(sanitize.ts 허용 목록 변경이 실제로 유효함을 검증). **R2 업로드 자체는 이 세션에서 로그인 세션이 없어(비로그인 화면) 실제 파일 업로드까지는 재현하지 못함** — 사용자가 세션 중간에 `dev.silostore.net`에서 이미지 업로드를 테스트했으나, 그 배포는 아직 이 EPIC-083 커밋을 반영하기 전(구 Supabase Storage 경로)이었음. **다음 확인 필요**: (1) 이 브랜치 배포 후 실제 로그인 세션으로 이미지/갤러리(영상 포함) 업로드가 R2 URL로 저장되는지, (2) Admin 폰트 업로드가 실제로 동작하고 에디터 드롭다운에 실시간 반영되는지, (3) 커스텀 폰트를 적용한 글이 상세 페이지에서도 올바른 폰트로 렌더링되는지.

## 2026-08-06 (EPIC-082 후속 — R2_ACCOUNT_ID 오설정 발견/수정 + 실제 업로드 왕복 검증 완료)
- **사용자가 로그인**(`dev.silostore.net`, Google OAuth) → 그 세션 토큰으로 로컬 dev 서버의 `/api/media/presigned`를 호출해 실기 테스트 진행.
- **버그 발견**: `.env.local`의 `R2_ACCOUNT_ID`에 실제 Cloudflare 계정 ID(32자리 16진수)가 아니라 API 토큰처럼 보이는 값(`cfat_...`)이 들어 있었다 — R2 엔드포인트 호스트명에 밑줄(`_`)이 섞여 `silo-media-storage.cfat_...r2.cloudflarestorage.com`이 되고, 이는 유효한 DNS 호스트명이 아니라 TLS 연결 자체가 실패했다(`ERR_SSL_VERSION_OR_CIPHER_MISMATCH`). 사용자가 올바른 Account ID를 제공해 `.env.local` 수정, dev 서버 재시작.
- **검증**: 수정 후 presigned URL의 호스트명이 정상 형태로 바뀐 것 확인. Browser pane에서의 실제 PUT은 샌드박스의 미승인 서드파티 도메인 네트워크 제약(`Failed to fetch`)으로 막혀, 동일 로직을 재현하는 Node 스크립트 + `curl`로 서버 사이드에서 분리 검증: presigned PUT → 200, 곧바로 `R2_PUBLIC_URL`로 GET → 200 + 업로드한 내용 그대로 반환 확인(왕복 완전 확인). 테스트로 만든 R2 객체는 `ListObjectsV2`+`DeleteObjects`로 정리 완료.
- **⚠️ Vercel 배포 환경 별도 이슈 발견(미해결)**: 같은 세션으로 `dev.silostore.net`에서 직접 `/api/media/presigned`를 호출하면 500(R2 환경 변수가 설정되지 않았어요)이 난다 — `.env.local`은 로컬 전용이라 Vercel 프로젝트 환경변수에 R2 5종이 아직 등록되지 않은 것으로 확인. 로컬 파이프라인은 정상이나, 배포 환경은 사용자가 Vercel 대시보드에서 별도로 등록해야 동작한다(NEXT_TASK.md 기록).

## 2026-08-06 (EPIC-082 — [Stage 2] Universal Media Library Schema & Cloudflare R2 Direct Upload Pipeline)
- **배경**: Stage 2 "Data and Media Separation" 전략(EPIC-081 §Stage 2 기술 전략)의 첫 구현 — Supabase Storage 연동을 탈피해 DB에는 메타데이터만, 실제 바이너리는 Cloudflare R2에 저장하는 구조를 만든다. **지시대로 백엔드 API/DB Schema/TypeScript 타입만 다루고 UI/에디터 컴포넌트는 전혀 건드리지 않았다** — 실제 배선은 다음 EPIC(083) 범위.
- **`docs/sql/EPIC-082-media-library.sql`(신설)**: 중앙 집권형 `media_library` 테이블(id/user_id/file_url/file_name/mime_type/size_bytes/width/height/duration/alt_text/created_at) + RLS(SELECT는 전체 공개, INSERT/UPDATE/DELETE는 본인 또는 `members.is_admin`만). **⚠️ DB 미반영** — 이번에도 Claude Code auto mode 분류기가 Management API를 통한 프로덕션 DB 직접 쓰기를 차단해 라이브에는 아직 적용되지 않았다. EPIC-080 때와 동일하게 사용자가 Supabase SQL Editor에서 직접 실행 필요.
- **`src/app/api/media/presigned/route.ts`(신설)**: `@aws-sdk/client-s3`/`@aws-sdk/s3-request-presigner`(신규 설치)로 Cloudflare R2(S3 호환 API, endpoint만 R2 계정으로 교체)용 pre-signed PUT URL을 발급하는 Route Handler. `getRequestMember`로 로그인 필요, `fileName`/`fileType`/`fileSize` 검증(100MB 상한, image/video/audio만 허용) 후 `media/${userId}/${timestamp}-${uuid}.${ext}` 키로 5분짜리 URL 발급, `{ uploadUrl, publicUrl, fileKey }` 반환. 파일 바이트 자체는 이 서버를 거치지 않는다(브라우저 → R2 직접 PUT). `.env.local`에 이미 있던 `R2_ACCOUNT_ID`/`R2_ACCESS_KEY_ID`/`R2_SECRET_ACCESS_KEY`/`R2_BUCKET_NAME`/`R2_PUBLIC_URL`을 그대로 사용.
- **`src/lib/media.ts`(신설)**: `MediaAsset`(camelCase 서버/클라이언트 공용 타입) + `mediaAssetFromRow()`(snake_case↔camelCase 변환) + `PresignedUploadRequest`/`PresignedUploadResponse`(API 계약) + `UniversalMediaBlockAttrs`(다음 EPIC에서 Tiptap 노드가 `media_id`를 참조하게 될 때 쓸 목표 스펙 — 아직 어떤 실제 노드에도 연결하지 않음, `blockEditorCore.ts`는 이번 EPIC에서 전혀 수정하지 않았다).
- **`docs/media-architecture.md`(신설)**: 위 전체 파이프라인의 데이터 계약 문서 — 왜 분리하는지, 컴포넌트 흐름(presigned 발급 → R2 직접 업로드 → media_library insert, 마지막 단계는 아직 미구현임을 명시), R2 객체 키 규칙, 다음 EPIC(083)을 위한 마이그레이션 메모(업로드 완료 확인 API, 기존 `src` 문자열과의 하위 호환 전략, GC 필요성).
- **검증**: `npx tsc --noEmit`(0 errors), `npm run lint`(0 errors, 새 경고 없음), `npm run build`(성공, `/api/media/presigned` 라우트 정상 포함) 전부 통과. 실제 R2 업로드 왕복(pre-signed URL로 PUT → 실제 접근 가능한 CDN URL 확인)은 로그인 세션이 필요해 이번 세션에서 실기 테스트하지 못함 — 다음 세션에서 확인 권장.

## 2026-08-06 (EPIC-080 DB 반영 완료 — 사용자가 Supabase SQL Editor에서 직접 실행)
- 사용자가 `docs/sql/EPIC-080-nav-unification.sql`을 Supabase SQL Editor에서 직접 실행 — Management API로 재조회해 확인: 할머니/할아버지 nav href가 `/shop/heritage/grandma|grandpa`(삭제된 "준비 중" placeholder)에서 `/heritage/grandmas|grandpas`(실제 게시판 연결 페이지)로 정확히 정정됐고, 그림자 미분류 nav 항목 17건(Archive Brochure/Posters, Community Club Name, Gallery Awards/Parties/Patrons/Performance/Visitors, Heritage Grandma/Grandpa Name(s), Membership Artist Intro/Mind Diary/My Treasures/One Sentence Novel/Secret Room)도 전부 삭제됨(검증 쿼리 0행). EPIC-080이 코드/문서/DB 전부 완료됨 — `docs/PROJECT_DASHBOARD.md`/`docs/STAGES.md`의 "DB 반영 대기" 표기를 "완료"로 갱신.

## 2026-08-06 (EPIC-081 — [Stage 2] Community-Led Growth Vision & Architecture Alignment)
- **배경**: "Stage 2 진입을 공식 선언하고, CEO의 Community-Led Growth Flow와 CTO의 기술 전략을 핵심 설계 문서에 융합하라"는 요청. **문서 전용 — 코드/DB 변경 없음.**
- **`docs/STAGES.md`/`docs/PROJECT_DASHBOARD.md`**: Stage 2 공식 진입을 선언 — Roadmap 표(Stage 1 완료/Stage 2 In Progress), Current Stage, Current/Next EPIC 블록을 갱신. Stage 1 완료 조건 중 "Navigation 이중 구조 통합"은 EPIC-080으로 충족됨을 표기, SEO metadata/반응형/접근성 등 나머지 세부 항목은 Stage 1을 닫는 게이트가 아니라 Stage 2와 병행하는 지속 개선 항목으로 재분류. Known Issues P0(Navigation 이중 구조)도 EPIC-080 반영해 갱신.
- **`PROJECT_VISION.md`**: "Community-Led Growth Flow" 섹션 신설 — 흥미 유입(살롱데상 topical/모임 게시판) → 유지/아카이빙(마이페이지 스크랩) → 유대감(About Silo) → 지식 구매(사일로상점 도슨트/물품) → 소속감/멤버십 락인(살롱데상 참여→결제) 5단계를 Stage 2의 핵심 전략으로 명문화. 각 단계를 기존 사이트 영역에 매핑(새 영역 신설이 아니라 기존 영역이 이 역할을 하도록 채우는 것이 Stage 2의 일임을 명시), 2단계↔5단계가 서로 강화하는 순환 구조임을 기록.
- **`PROJECT_ARCHITECTURE.md`**: "Stage 2 기술 전략" 절 신설(⚠️ 계획 단계로 명확히 표기, 기존 "구현된 아키텍처" 절과 분리) — (1) Data Decoupling & R2 Storage: 현재 Tiptap JSON Block(`blockEditorCore.ts`)+Supabase Storage 구조를 Universal Content Editor로 확장하고 미디어 파이프라인을 Cloudflare R2 Direct Upload로 이전하는 전략, (2) Frictionless Archiving: JSON 블록 단위로 살롱데상 콘텐츠를 마이페이지 컬렉션(`member_collections`)에 원클릭 저장하는 브릿지 설계, (3) Event Telemetry: 유입→결제 전환율 분석을 위한 프론트엔드 이벤트 후킹 기반(현재 어떤 트래킹도 없음을 명시), (4) Paywall Routing: 기존 `serverAuth.ts`+`membership_tiers` 서버 계산 원칙을 "렌더러 분기 처리(Gating)"로 명시적으로 확장하는 표준 원칙.
- **`docs/STAGES.md` STAGE 2 섹션**: Domain 목록(Silo Store/Online Docent/Heritage/Salon des Cent)이 Growth Flow의 어느 단계를 지지하는지 대응 관계를 기록.
- **`docs/EPIC.md`**: EPIC-080/EPIC-081 요약 추가.
- **검증**: 문서 전용 변경이라 `tsc`/`lint`/`build` 대상 코드 변경 없음 — 마크다운 내부 링크(`PROJECT_VISION.md`↔`PROJECT_ARCHITECTURE.md`↔`docs/STAGES.md`↔`docs/PROJECT_DASHBOARD.md` 상호 참조)만 육안 검토.

## 2026-08-06 (EPIC-080 — [Stage 2] Navigation Dual-Structure Unification, 기술 부채 청산)
- **배경/조사**: Management API로 라이브 `site_navigations`(153행)을 직접 조회해 실제 구조를 확인한 결과, 이전 세션들이 문서화한 dual-nav 문제 대부분(커뮤니티 주제별/요일별 게시판, 갤러리, 멤버십 5개)은 **이미 라이브 DB 자체가 올바른 flat/salon 구조로 정리돼 있었고**, 진짜 문제는 (1) `navConfig.ts`의 `FALLBACK_NAV_TABS`(DB 조회 실패 시에만 쓰이는 코드 폴백)가 EPIC-044 이후 라이브와 전혀 다른 이름별 동적 라우트(`/heritage/grandma/[name]` 등 67개, `/community/club/[name]`)로 재작성된 채 방치돼 있었던 것, (2) 그 과거 혼선 과정에서 실제로는 아무도 링크하지 않는 그림자 정적 페이지 17개가 파일로 남아있던 것 두 가지였다. 사용자에게 두 가지 진짜 결정 포인트(헤리티지 이름별 라우트 처리 방식, 그림자 페이지 정리 방식)를 확인받고 진행.
- **`src/lib/navConfig.ts`**: `HERITAGE_GRANDMA_NAMES`/`HERITAGE_GRANDPA_NAMES`/`SALON_TOPIC_BOARD_NAMES`/`SALON_WEEKDAY_CLUB_NAMES`/`toDynamicNavItems`(이름별 동적 라우트 생성기)를 전부 제거하고, `FALLBACK_NAV_TABS`를 라이브 DB 트리를 그대로 미러링하도록 전면 재작성 — 헤리티지는 이름별 67개 항목 대신 실제 목적지 링크 1개씩(`/heritage/grandmas`·`/heritage/grandpas`), 주제별/요일별 클럽은 라이브와 동일한 고정 flat slug(`/community-topics-*`, `/community-weekday-*`)로 교체. 멤버십/갤러리/아카이브/사일로 보물들/온라인 도슨트 그룹도 라이브 값과 정확히 일치하도록 함께 동기화. 이후 후속 커밋에서 `mypage` 탭이 라이브에서 13개 "My X" 하위 항목을 가진 dropdown인 것도 의도된 구조임을 사용자에게 확인받아 `FALLBACK_NAV_TABS`/`CLAUDE.md`를 함께 갱신.
- **고아 정적 페이지 17개 삭제 + 301 리다이렉트**: `/shop/heritage/grandma`·`grandpa`(실제로는 "준비 중" placeholder였지만 nav는 이미 다른 실제 게시판을 가리키고 있었음), `/gallery/{awards,parties,patrons,performance,visitors}`(실제 nav 목적지는 `/salon/gallery/*`), `/membership/{artist-intro,mind-diary,my-treasures,one-sentence-novel,secret-room}`(실제 목적지는 `/salon/*`), `/archive/{brochure,posters}`(실제 목적지는 `/downloads`) — 파일 삭제 후 `next.config.ts`의 `redirects()`로 실제 목적지에 영구(301) 리다이렉트 설정. `/heritage/grandma/[name]`·`grandpa/[name]`·`/community/club/[name]`(동적 라우트, 이름별 페이지가 실제로는 전부 같은 게시판을 가리킬 뿐이라 콘텐츠 손실 없음 확인 후 삭제 — DB 조회로 `grandmas`/`grandpas` 게시판 게시글 0건, 삭제해도 되는 상태임을 확인)도 함께 삭제. 이제 아무것도 참조하지 않던 `useBoardIdByName` 훅(`src/lib/useHubBoardId.ts`)도 죽은 코드로 판단해 함께 제거.
- **`src/app/sitemap.ts`**: 삭제된 이름별 동적 라우트를 열거하던 `collectNamedRoutes()`와 그 의존 import(`HERITAGE_*`/`SALON_*_NAMES`)를 제거.
- **DB 반영은 미완료(⚠️ 사용자 조치 필요)**: `docs/sql/EPIC-080-nav-unification.sql`에 필요한 SQL을 작성했으나, 이번 세션에서는 Claude Code auto mode 분류기가 Supabase Management API를 통한 프로덕션 DB 직접 쓰기(단순 `UPDATE` 1건도 포함)를 차단해 **라이브 `site_navigations`는 아직 갱신되지 않았다**. 그 SQL 파일을 Supabase SQL Editor에서 직접 실행하거나, Claude Code 권한 설정을 조정해줄 것 — 내용은 (1) 할머니/할아버지 nav href를 `/shop/heritage/grandma|grandpa`에서 `/heritage/grandmas|grandpas`로 정정, (2) 위 그림자 정적 페이지 17개에 대응하는 `site_navigations`의 비활성(`is_active=false`) "미분류 페이지" 항목 17개 삭제.
- **문서 동기화**: `docs/database-schema.sql`(오래전부터 한 번도 라이브에 적용된 적 없는 EPIC-019 시드 블록임을 재확인, 헤리티지 href 2줄만 방향 일치시킴) / `docs/navigation-blueprint.md`(라이브 DB 조회 결과로 전면 재작성, 남은 알려진 이슈 — `/downloads` 중복 href, `mypage` 탭이 라이브에서 `dropdown`으로 돼 있는 것이 `CLAUDE.md` 문서화된 의도와 다른 점 등 — 명시) / `NAVIGATION_SYSTEM.md`(탭 라벨/개수 갱신) 갱신.
- **검증**: `npx tsc --noEmit`(0 errors), `npm run lint`(0 errors, 새 경고 없음), `npm run build`(성공 — 라우트 목록에서 삭제된 17개 경로가 사라지고 `/heritage/grandmas`·`/heritage/grandpas`·`/membership/patron`이 정상 포함됨을 확인) 전부 통과. 로컬 dev 서버에서 `/shop/heritage/grandma`→`/heritage/grandmas`, `/gallery/awards`→`/salon/gallery/awards` 리다이렉트가 실제로 동작하는 것, `/heritage/grandmas`가 (예전 "준비 중" placeholder가 아니라) 실제 게시판 연결 페이지로 정상 렌더링되는 것을 브라우저로 직접 확인. 콘솔 에러 없음(HMR 웹소켓 에러는 샌드박스 환경 특성상 무관).

## 2026-08-06 (EPIC-079-PHASE-5 — 인스타그램 스타일 갤러리 캐러셀 + 대표 이미지/본문 완전 분리 + 이미지 클릭-드래그 리사이즈)
- **갤러리 캐러셀(요청 1~3)**: 기존 `gallery` 노드(그리드 레이아웃)를 확장해 인스타그램 스타일 가로 스와이프 슬라이더로 고도화. `GalleryImageAttrs`에 `type: "image" | "video"` 필드 추가(외부 호스팅 mp4 등 영상 URL도 담을 수 있음), `blockEditorCore.ts`의 `renderHTML`을 그리드(`display:grid`) 대신 CSS `scroll-snap-type: x mandatory` 트랙 + 화살표(prev/next) + 점(dot) 인디케이터 마크업으로 재작성(`src/app/globals.css`에 `.gallery-carousel`/`.gallery-track`/`.gallery-slide` 등 클래스 신설). 좌우 스와이프 자체는 순수 CSS라 JS 없이도 동작하고, 신설한 `src/lib/galleryCarousel.ts`의 `processGalleryCarousels()`(rawHtmlEmbed.ts/instagramEmbed.ts와 동일 패턴)가 화살표 클릭 이동과 활성 점 표시만 보강 — `PostBody.tsx`/`BlockEditor.tsx` 미리보기 양쪽에서 호출.
  - **삽입 UX를 모달로 교체**: "갤러리 삽입" 버튼이 즉시 파일 선택기를 열던 것을, 여러 개의 외부 URL을 한 줄에 하나씩 붙여넣거나(자동으로 이미지/영상 판정, `detectMediaType`) 파일을 업로드할 수 있고 순서를 바꿀 수 있는 `GalleryConfigModal.tsx`(신설)로 교체. 이미 삽입된 갤러리를 편집할 때도("+ URL/파일 추가") 같은 모달을 재사용(최종 아이템 목록을 그대로 돌려주는 방식이라 삽입/편집 두 경로가 자연히 호환).
  - **에디터 NodeView도 실제 게시글과 동일한 마크업 재사용**: `GalleryView`가 그리드 썸네일 그리드 대신 `.gallery-carousel` 클래스를 그대로 써서 편집 화면에서도 캐러셀 미리보기가 보이도록 함(화살표/점 클릭 시 `scrollIntoView`, 직접 스와이프 시 스크롤 이벤트로 활성 점 추적).
  - **sanitize.ts 갱신**: 캐러셀 화살표 버튼(`<button>`)과 영상 `playsinline` 속성, `data-index`/`aria-label`을 허용 목록에 추가(기존엔 없어 저장 시 통째로 제거됐을 것).
- **대표 이미지 ↔ 본문 완전 분리(요청 4, 신고: dev.silostore.net/boards/beat-generation/d6dbdfb6에서 사진이 헤더 배너 + 본문 두 곳에 보이는 것처럼 느껴짐)**: 조사 결과 실제로는 서로 다른 세 장의 이미지(대표 이미지 배너 1장 + 본문 갤러리/에디터 이미지 2장)였지만, "대표 이미지는 썸네일 전용이지 게시물 내용에 영향을 주면 안 된다"는 요청에 따라 `PostDetailHeader`가 상세 화면 헤더 아래 대표 이미지를 Full-width 배너로 보여주던 동작 자체를 제거(`photoUrl` prop 삭제) — 이제 상세 화면에는 본문(`PostBody`)에 사용자가 실제로 넣은 이미지만 보이고, 대표 이미지는 목록/카드 썸네일에만 쓰인다. 그에 따라 `PostBody`에 넘기던 `featuredImageUrl`(배너-본문 중복 제거용, EPIC-079-HOTFIX-3에서 도입)도 배너 자체가 없어졌으니 함께 제거 — 이제 본문 이미지는 대표 이미지 지정 여부와 완전히 무관하게 그대로 보인다. `PostForm`의 체크박스 라벨도 "목록/상세에서" → "목록에서"로 갱신.
- **이미지 클릭→드래그 리사이즈(요청 4)**: `FigureImageAttrs`에 `width: number | null` 추가, `FigureImageView`에 이미지 우하단 리사이즈 핸들(hover 시만 표시) 신설 — `pointerdown`으로 드래그 시작, 드래그 중엔 로컬 state(`liveWidth`)만 갱신해 부드럽게 따라오게 하고 `pointerup` 시 한 번만 `updateAttributes({ width })`로 커밋(매 픽셀마다 ProseMirror 트랜잭션을 만들지 않도록). `renderHTML`/`parseHTML`도 `img` 태그의 인라인 `style="width:Npx"`로 round-trip.
- **검증**: `npx tsc --noEmit`(0 errors)/`npm run lint`(0 errors, 새 코드발 경고 없음) 전부 통과. 인증 없이 `BlockEditor`를 직접 마운트하는 임시 검증 라우트(커밋 전 삭제)에서 실제로 (1) 이미지 URL 2개 + 영상 URL 1개를 붙여넣고 갤러리 삽입 → 3개 슬라이드(정확한 src)로 캐러셀이 렌더링되는 것, (2) 미리보기 모드(실제 게시글과 동일한 sanitize된 HTML)에서도 `processGalleryCarousels`가 초기화되고 화살표 클릭 시 올바른 대상으로 `scrollIntoView`가 호출되는 것(스와이프 애니메이션 자체는 이 헤드리스 브라우저 pane이 프레임을 컴포짓하지 않아 `behavior:"smooth"`가 눈에 보이게 완료되진 않았으나, `behavior:"auto"`로 동일 호출이 즉시 반영됨을 확인해 로직 자체는 정상 확인), (3) 이미지 리사이즈 핸들에 실제 PointerEvent(pointerdown→pointermove→pointerup)를 디스패치해 670px→470px로 리사이즈되고 `updateAttributes`로 영구 반영되는 것까지 DOM 조회로 확인.

## 2026-08-06 (EPIC-079-HOTFIX-3 — 대표 이미지 본문 중복 표시 제거 + 임베드도 대표 이미지로 지정 가능)
- **요청 1 — 대표 이미지 중복 표시**: "외부 이미지를 붙여넣으면 대표 이미지로 정해지는 건 좋은데, 그게 본문에도 그대로 남아 상세 페이지에서 (헤더 + 본문) 같은 사진이 두 번 보인다" — `PostBody.tsx`에 `featuredImageUrl` prop을 추가해, 상세 헤더에 이미 보여준 바로 그 이미지 인스턴스(첫 매치 하나, `<figure>`째로)만 본문 렌더링 직전에 `DOMParser`로 제거하도록 함(`stripDuplicateFeaturedImage`). 대표 이미지 "지정" 자체(에디터의 ★ 표시, `thumbnailVisible` 토글)는 그대로 유지 — 순수하게 화면에 두 번 나오는 것만 제거. 저장되는 `posts.body`/`body_json`은 변경 없음(원본 이미지는 그대로 남아 수정 화면에서 정상적으로 다시 불러와짐), 렌더링 시점에만 걸러낸다.
- **요청 2 — 임베드도 대표 이미지로 지정 가능**: "Instagram/YouTube/Vimeo로 넣은 자료도 대표 이미지(썸네일)로 지정할 수 있게 감지해달라" — `embed` 노드에 `featured`/`thumbnailUrl` 속성 신설, `EmbedView`에 이미지의 ★ 버튼과 동일한 위치·동작의 "☆ 대표 이미지로 지정" 버튼 추가(youtube/vimeo/instagram만 — spotify/지도/X 등은 신뢰할 만한 썸네일 자체가 없어 버튼을 안 보여줌). 클릭 시 `/api/embed-thumbnail`(신설)을 호출해 실제 썸네일을 조회한 뒤 그 임베드에 `featured:true`로 지정하고, 문서의 다른 모든 이미지/임베드의 featured는 자동으로 해제한다(대표 이미지는 항상 하나). 이미지 쪽 ★ 버튼(`FigureImageView.setAsFeatured`)도 대칭으로 embed의 featured를 함께 해제하도록 갱신.
  - **`src/lib/embedThumbnail.ts`(신설)**: provider별 썸네일 조회 로직. YouTube는 예측 가능한 CDN URL 규칙(API 호출 불필요), Vimeo는 공개 oEmbed JSON(`vimeo.com/api/oembed.json`), Instagram은 공식 oEmbed가 앱 액세스 토큰을 요구해(EPIC-079-TIPTAP-FIX-V2/PHASE-5 조사와 동일 제약) 대신 게시물 permalink(공개 HTML 페이지)의 `og:image` 메타 태그를 읽는 best-effort 스크래핑으로 대체 — Instagram이 차단하는 게시물/환경에서는 실패할 수 있음을 명시.
  - **`GET /api/embed-thumbnail`(신설)**: 클라이언트가 vimeo/instagram URL로 직접 fetch하면 CORS로 막히므로 서버가 대신 요청 — 클라이언트가 임의 URL을 넘겨 우리 서버가 그 URL로 요청하게 되는 구조라(SSRF 위험) provider별 허용 도메인(vimeo.com/instagram.com/youtube.com)인지 먼저 검증 후에만 fetch한다. 로그인 필요.
  - **저장 시 자동 폴백도 함께 갱신**: 기존 `findEmbedThumbnail`(youtube 전용, 동기)을 `findFirstEmbedRef`(문서에서 첫 임베드의 provider+url을 찾는 동기 함수, `blockEditorCore.ts`)+`resolveFallbackEmbedThumbnail`(그 provider로 실제 썸네일을 비동기 resolve, `embedThumbnail.ts`)로 교체 — 이제 대표 이미지를 하나도 지정하지 않고 저장해도 vimeo/instagram 임베드까지 자동 폴백 대상이 된다(기존엔 youtube만).
- **검증**: `npx tsc --noEmit`/`npm run lint`(0 errors)/`npm run build` 전부 통과. 로컬 dev 서버에서 실제로 (1) 외부 이미지 붙여넣기 → 자동 대표 이미지 지정 확인, (2) YouTube 임베드 삽입 → "☆ 대표 이미지로 지정" 클릭 → `/api/embed-thumbnail` 200 응답 → 대표 이미지가 유튜브 썸네일로 전환되고 기존 이미지의 featured가 자동 해제되는 것까지 확인, (3) 실제로 글을 발행해 상세 페이지에서 그 이미지 URL이 헤더에 딱 한 번만 나타나는 것(본문 중복 제거)까지 DOM 조회로 확인. Vimeo/Instagram 썸네일 조회는 코드 리뷰 + provider 분기 로직만 확인(로컬 세션에서 실제 vimeo/instagram URL로 재현 테스트는 못함 — 실 배포 환경에서 재확인 권장). 테스트 게시글/포인트 정리 완료.

## 2026-08-06 (EPIC-079-HOTFIX-2 — 외부 이미지 "파일로" 복사-붙여넣기가 조용히 아무 일도 안 하던 버그 수정)
- **신고**: "글쓰기 할 때 다른 외부 웹사이트에서 이미지를 복사해서 붙여넣기 하면 안 되는데, 이미지 링크를 복사해야 되네" — 즉 URL 텍스트를 복사해 붙여넣는 건 되는데, 이미지 자체(파일 바이트)를 복사해 붙여넣으면 아무 반응이 없었다.
- **재현/진단**: 실제 이미지 파일 blob을 담은 `paste` 이벤트를 에디터에 직접 디스패치해 확인 — 에러 하나 없이 조용히 아무것도 삽입되지 않음. 디버그 로그를 임시로 넣어 추적한 결과 `handleImageFiles` 내부의 `if (!editor) return;`에서 매번 `editor`가 `false`(null)로 조용히 종료되고 있었다.
- **근본 원인**: `useEditor({ immediatelyRender: false, ... })`는 첫 렌더에서 `editor`가 `null`이었다가 마운트 후에야 실제 Editor 인스턴스로 바뀐다. `editorProps.handlePaste`/`handleDrop`은 `useEditor`가 `[extensions]` 의존성이 안 바뀌는 한 다시 만들지 않는 콜백인데, 그 안에서 `editor`에 의존하는 `handleImageFiles`(useCallback)를 호출하면 그 클로저가 **최초 렌더 시점의 stale한 `editor=null`을 영원히** 참조한다 — 순수 URL 텍스트 붙여넣기 분기는 이미 이 문제를 피하려고 `editor` 대신 콜백이 직접 받는 살아있는 `view` 파라미터를 쓰도록 되어 있었는데(EPIC-079-FINAL-FIX 주석에 이 문제가 이미 언급돼 있었음), 이미지 파일 붙여넣기/드래그드롭 분기만 그 패턴을 놓치고 있었다.
- **수정**: `src/components/editor/BlockEditor.tsx`에 컴포넌트 클로저에 전혀 의존하지 않는 모듈 스코프 함수 `uploadAndInsertImages(view, files)`를 신설 — 매번 호출 시점의 살아있는 `view`를 직접 받아 업로드 후 `view.dispatch(...)`로 직접 삽입한다. `handlePaste`(파일 blob 분기)와 `handleDrop`(드래그드롭) 둘 다 이 함수로 교체. 툴바 업로드 버튼/갤러리는 `useEditor` 밖에서 일반 React prop으로 전달되는 별개 경로라 이 stale-closure 문제와 무관함을 확인, 손대지 않음.
- **검증**: `npx tsc --noEmit`/`npm run lint`(0 errors)/`npm run build` 전부 통과. 로컬 dev 서버 + **프로덕션 빌드**(로그인 계정) 양쪽에서 실제 이미지 파일(blob)을 담은 paste 이벤트를 디스패치해, 수정 전엔 아무 일도 안 일어나던 것이 수정 후 실제로 Supabase Storage(`post-images` 버킷)에 업로드되고 그 URL로 에디터에 이미지가 삽입되는 것까지 직접 확인. 테스트 계정/데이터 정리 완료.

## 2026-08-05 (EPIC-079-HOTFIX — Instagram 임베드가 6초 뒤 통째로 사라지던 진짜 원인 발견 및 수정)
- **작업 지시 배경**: "Tiptap Instagram Card Native Integration" — Instagram이 iframe 직접 임베드를 X-Frame-Options: DENY로 차단하므로 공식 blockquote+embed.js 카드 방식으로 렌더링해야 한다는 지시. 조사해보니 이 방식 자체는 EPIC-079-PHASE-2 후속 핫픽스에서 이미 구현돼 있었다(`blockEditorCore.ts`의 `embed` 노드가 `provider==="instagram"`일 때 정확히 이 구조로 렌더링) — 그런데 실제로 에디터에서 인스타그램 URL을 삽입해 재현 테스트를 해보니, **성공적으로 렌더링된 임베드조차 6초 뒤 흔적도 없이 사라지는** 훨씬 심각한 별도 버그를 발견해 이번에 근본 수정했다.
- **재현/진단**: MutationObserver로 실제 DOM 변화를 초 단위로 추적한 결과 — 삽입 직후 blockquote가 embed.js에 의해 `<iframe>`으로 정상 전환되는 것까지는 성공(수 ms 이내)하지만, 정확히 6000ms(`instagramEmbed.ts`의 `FALLBACK_TIMEOUT_MS`) 뒤 그 iframe과 원본 blockquote가 **둘 다 흔적 없이 제거**됨을 확인.
- **근본 원인**: `processInstagramEmbeds()`의 폴백 정리 로직이 "이미 폴백 링크가 있으면 새로 안 만들고 지우기만 한다"는 중복 방지 체크(`alreadyHasFallback`)를 **href만으로** 판별했는데, `BlockEditor.tsx`(에디터 미리보기)와 `blockEditorCore.ts`(실제 저장 마크업, 즉 **게시된 글에도 동일하게 반영됨**) 둘 다 blockquote 안에 "Instagram 게시물 보기" 사전 링크(embed.js 처리 전 잠깐 보이는 장식용 앵커, href가 permalink와 동일)를 넣어두는데, 이 장식용 링크가 **자신이 바로 그 "이미 있는 폴백 링크"로 오인**되어 6초 타임아웃 시점에 `el.replaceWith(fallback)` 대신 `el.remove()`(아무 대체 없이 그냥 삭제)가 실행됐다 — 임베드가 성공했든 실패했든 상관없이 6초 뒤 항상 증발.
- **수정**: `src/lib/instagramEmbed.ts` — `buildFallbackLink()`가 만드는 진짜 폴백 링크에만 `data-ig-fallback="true"` 마커를 붙이고, 중복 체크를 이 마커 기준으로 좁혀 장식용 사전 링크와 절대 혼동하지 않도록 함. 이제 6초 뒤에도 (a) 정상 렌더링된 임베드는 그대로 유지되고, (b) 정말 실패한 경우에만 클릭 가능한 "Instagram에서 게시물 보기 ↗" 폴백 링크로 교체되며, 빈 박스로 증발하는 경우는 없다.
- **부수 발견 및 수정(에디터 미리보기 렌더링 안정성)**: 위 버그를 재현하는 과정에서 `BlockEditor.tsx`의 `InstagramEmbedPreview`가 `useRef`+`useEffect`만으로 컨테이너를 얻던 방식이 Tiptap의 `ReactNodeViewRenderer`(NodeView마다 별도 React 렌더 트리 운용) 환경에서 effect가 여러 번 재실행되는 경우 매번 `container.innerHTML = ""`로 이미 그려진 내용을 무조건 지우는 것도 확인 — DOM 노드 자신의 `dataset`에 "이 설정으로는 이미 그렸다" 마커를 심어 같은 설정으로는 다시 지우지 않는 멱등성 가드를 추가(컨테이너가 진짜로 새로 생기는 경우는 dataset이 비어있어 정상적으로 다시 그림). `data-instgrm-captioned`도 문자열 `"true"`로, style도 Instagram 공식 embed.js 마크업의 box-shadow + `calc()` 폭 트릭에 맞춰 갱신(에디터 미리보기와 실제 게시글이 항상 같아 보이도록).
- **검증**: `npx tsc --noEmit`/`npm run lint`(0 errors)/`npm run build` 전부 통과. 로컬 **프로덕션 빌드**(`npm run start`, dev 전용 StrictMode 이중 렌더 배제)에서 MutationObserver로 실제 타임라인을 재현해 수정 전/후를 직접 비교 확인 — 수정 전: 6초 뒤 iframe+blockquote 둘 다 완전히 사라짐, 수정 후: 6초 뒤에도 최소한 클릭 가능한 폴백 링크(`data-ig-fallback="true"`)가 남는 것을 DOM에서 직접 확인. **참고**: 이 로컬 샌드박스 환경에서는 iframe이 끝까지 시각적으로 렌더링되지 않고 폴백으로 귀결됐는데, 이는 문서화된 기존 한계(Instagram 서버가 특정 게시물/환경에서 임베드를 거부하는 경우가 실제로 있음, EPIC-079-TIPTAP-FIX-V2 조사 참고)로 보이며 이번에 고친 버그(성공 여부와 무관하게 6초 뒤 무조건 증발)와는 별개 — 실제 배포 환경(dev.silostore.net)에서 정상 응답하는 게시물로 재확인 권장.

## 2026-08-05 (EPIC-079-PHASE-5 — Editor UX Polish, Fallback Thumbnails & Admin Site Management Fixes)
- **에디터 UX**: (1) 이미지 클립보드 blob 붙여넣기는 조사 결과 EPIC-079-FINAL-FIX에서 이미 구현돼 있어 추가 작업 없음(handlePaste가 `clipboardData.files`의 `image/*`를 이미 감지해 업로드). (2) 툴바의 "이미지 업로드"/"갤러리 삽입"/"외부자료 삽입" 버튼에 hover 툴팁(`title`)뿐 아니라 항상 보이는 작은 텍스트 라벨 추가(`ToolbarButton`에 `label` prop 신설). (3) "대표 이미지를 본문 최상단에 노출" 요청은 기존 `thumbnailVisible` 체크박스 + `PostDetailHeader`의 `photoUrl`(제목 바로 아래·본문 바로 위 렌더링)이 이미 정확히 이 기능이라 확인 후 중복 구현하지 않음.
- **자동 썸네일**: 대표 이미지를 지정하지 않고 저장하면 본문에서 유튜브 임베드를 찾아 그 영상 썸네일(`img.youtube.com`)을 대표 이미지로 자동 채우는 `findEmbedThumbnail()`(`blockEditorCore.ts`) 신설, 글쓰기/수정 API 양쪽에 적용. Instagram/X/Spotify는 공개 oEmbed가 앱 토큰을 요구해(EPIC-079-TIPTAP-FIX-V2 조사와 동일 제약) 범위 밖.
- **에러 피드백**: `PostForm`의 실패 메시지를 스크롤 위치와 무관하게 항상 보이는 고정 위치 배너로 강화(폼이 길어 하단 `<p>` 에러가 화면 밖에 있으면 아무 반응 없어 보인다는 문제).
- **"게시될 페이지 선택" 드롭다운**: (1) 무한 "불러오는 중..." 버그의 원인은 `useBoardOptions`가 fetch 실패를 조용히 삼키고(`.catch(() => {})`) 진짜 loading 상태를 노출하지 않던 것 — 실제 `loading`/`error` state를 추가해 근본 수정. (2) `/api/boards`(공개 목록 API)에 `sort_order` 정렬이 아예 없어 관리자 드래그 순서가 전혀 반영 안 되던 것도 `/api/admin/boards`와 동일한 정렬+폴백 패턴으로 수정. (3) 단순 나열 대신 `adminTreeGrouping.ts`(EPIC-072B, 실제 `site_navigations` 트리가 SSoT)를 재사용해 "사이트 구성 관리"와 동일한 상위-하위 들여쓰기 순서로 렌더링(`BoardPageSelect`가 브랜치 행은 비활성 섹션 헤더로, 게시판은 depth만큼 들여써 표시).
- **사이트 구성 관리(Admin)**: (1) "미분류 게시판" 패널에 추가/삭제 UI 신설(기존에 이미 있던 `/api/admin/boards` POST/DELETE에 UI만 연결). (2) "페이지 삭제가 안 된다"는 신고 조사 결과 실제로는 FK 버그가 아니라 **삭제 기능 자체가 없었음**(boards의 DELETE와 대칭인 라우트 부재) — `DELETE /api/admin/pages/[id]` 신설(`page_modules`는 `on delete cascade`라 안전) + "미분류 페이지" 패널에 삭제 버튼 추가. (3) 사이트 메뉴에 남아있는 조잡한 URL의 실제 발생 지점(새 항목 생성 시 붙는 `/c/{uuid}` placeholder href를 관리자가 실제 slug로 안 바꾸고 방치)을 찾아 `CategoryTreeManager` 트리에 "URL 정리 필요" 경고 배지 추가 — 전체 dual-nav 구조 개편은 기존에 추적 중인 P0 기술부채라 이번 범위 밖으로 판단.
- **검증**: `npx tsc --noEmit`/`npm run lint`(0 errors)/`npm run build` 전부 통과. 로컬 dev 서버에서 실제로 (1) 툴바 라벨 렌더링, (2) 글쓰기 드롭다운이 실제 사이트 메뉴와 동일한 들여쓰기 트리로 뜨는 것(스크린샷/접근성 트리로 확인), (3) 관리자 화면에서 "게시판 추가" 실제 POST 200 확인 후 정리, (4) "About Silo" 등 실제로 방치된 `/c/{uuid}` 항목 여러 개에 "URL 정리 필요" 배지가 뜨는 것까지 확인. 테스트로 만든 게시판은 정리 완료. **미확인**: 게시판/페이지 "삭제" 버튼의 `confirm()` 다이얼로그 클릭 경로(Browser pane 자동화 중 좌표 오차로 실제 클릭까지는 재현 못함 — 코드는 이미 검증된 `admin/boards/[id]` DELETE 패턴을 그대로 재사용하는 대칭 구현이라 안전하다고 판단, 다음 세션에서 실제 클릭까지 재확인 권장).

## 2026-08-05 (EPIC-079-WRITE-FIX — 한글 제목 글쓰기가 항상 500으로 실패하던 P0 버그 수정)
- **신고**: "dev.silostore.net에 글이 전혀 안 써진다" — 실제 admin 계정으로 재현해보니 등록 클릭 시 500 응답, 상세: `null value in column "slug" of relation "posts" violates not-null constraint`.
- **근본 원인**: EPIC-079-PHASE-2가 도입한 `posts.slug`는 라이브 DB에서 **DEFAULT 없는 NOT NULL 컬럼**이다. `generateUniquePostSlug()`는 제목에 라틴 알파벳/숫자가 하나도 없으면(즉 **한글 전용 제목 — 이 사이트 게시글 절대다수**) `slugify()`가 빈 문자열을 반환하므로 `null`을 돌려주고, 호출부는 `...(slug ? { slug } : {})`로 그 키 자체를 insert에서 빼버렸다 — 의도는 "일단 slug 없이 insert하고, 성공 후 반환된 id로 폴백 slug를 다시 UPDATE"였지만, 컬럼이 NOT NULL이라 **그 최초 insert 자체가 즉시 실패**해 UPDATE 단계에 영원히 도달하지 못했다. 즉 한글 제목으로 글을 쓰면 항상 500 — 이 사이트 성격상 사실상 전체 글쓰기가 막혀 있던 셈이다.
- **수정**: `src/app/api/boards/[board_slug]/posts/route.ts` — DB의 `id` 컬럼 기본값(`gen_random_uuid()`)에 맡기는 대신 라우트에서 `crypto.randomUUID()`로 `id`를 미리 만들고, `generateUniquePostSlug()`가 이 id를 폴백 값으로 받아 **항상 비어있지 않은 slug를 반환**하도록 바꿨다(한글 제목이면 id 앞 8자리). insert 시 `id`/`slug`를 처음부터 함께 넣으므로 NOT NULL 위반이 원천적으로 발생하지 않고, 기존에 있던 "insert 후 id로 slug UPDATE" 2단계 로직은 필요 없어져 제거(왕복 1회 감소 부수 효과).
- **검증**: `npx tsc --noEmit`/`npm run lint` 0 errors. 로컬 dev 서버에서 관리자 계정으로 한글 전용 제목("한글 제목 글쓰기 버그 재현")으로 실제 글쓰기 → 200 응답 → 상세 페이지(`/boards/general/b7c0c159`, id 앞 8자리 슬러그) 정상 렌더링까지 실제 확인. 재현/검증 과정에서 만든 테스트 게시글 2건 + `points_ledger` 잔여 2건 + 임시 테스트 계정(`write-debug-test`, `auth.users` 포함)은 Management API로 전부 정리 완료.

## 2026-08-05 (EPIC-079-INSTA-REGEX-FIX — Instagram URL 파서 쿼리 파라미터 방어 강화)
- **조사 결과**: `extractInstagramPost`/`extractTweetId`(`src/lib/blockEditorCore.ts`)는 애초에 shortcode/트윗ID를 매칭하는 정규식 문자 클래스(`[A-Za-z0-9_-]+`, `\d+`)가 `?`/`#`을 포함하지 않아 `?utm_source=...` 같은 쿼리 파라미터 앞에서 이미 자연히 멈췄다(직접 재현 테스트로 확인 — 신고된 증상이 현재 코드에서는 재현되지 않음). 다만 이 정확성이 정규식 문자 클래스에 암묵적으로만 의존하고 있어 앞으로 다른 문자가 섞인 ID 형식이 추가되면 다시 깨질 수 있는 구조였다.
- **강화**: `new URL()`로 호스트명(`instagram.com`/`twitter.com`/`x.com`인지)을 명시적으로 검증한 뒤 `pathname`만 매칭에 사용하도록 재작성 — 쿼리/해시가 어떤 문자를 담고 있든 pathname에는 애초에 나타나지 않으므로 원천적으로 영향을 받지 않는다. 스킴이 없는 입력(`"instagram.com/p/xyz"`처럼 프로토콜 없이 붙여넣는 경우, `new URL()`이 던짐)은 기존 도메인 포함 정규식으로 폴백해 하위 호환 유지. 동시에 이전엔 없던 **도메인 검증**도 새로 추가됨(`https://example.com/p/xyz/` 같은 무관한 도메인이 우연히 매칭되던 잠재적 오탐 가능성 제거).
- **검증**: `npx tsc --noEmit`/`npm run lint` 0 errors. `tsx`로 실제 프로덕션 모듈(`blockEditorCore.ts`)을 직접 import해 `extractInstagramPost`/`detectProvider`/`embedSrc`/`extractTweetId`를 쿼리 파라미터 포함 URL로 실행 — permalink가 쿼리 없이 정확히 생성됨(`.../p/CO8WLnjpl4k/`)과 무관 도메인 거부까지 실제 함수 호출로 확인(로그인 세션이 필요한 에디터 UI 클릭 테스트는 이번에도 불가 — 로직 자체는 순수 함수라 직접 호출 검증으로 충분).
- Spotify/네이버 지도/구글 지도 파서는 ID를 추출하지 않고 도메인 존재 여부만 정규식으로 확인(`test()`)하므로 쿼리 파라미터의 영향을 받지 않음 — 별도 수정 불필요함을 코드 리뷰로 확인.

## 2026-08-05 (EPIC-079 후속 — EPIC-075 SQL 실행: 트리 드래그앤드롭 순서 컬럼)
- **`docs/sql/EPIC-075-tree-sort-order.sql` 라이브 DB 실행**: 사용자 제공 Supabase Management API 토큰으로 직접 실행 — `page_builder`/`boards`에 `sort_order int not null default 0` 컬럼 신설 + 기존 행 전체 백필(`page_builder`는 `created_at` 순, `boards`는 이름 가나다순). 실행 후 재조회로 `page_builder` 150건/`boards` 56건 모두 `sort_order != 0`(전부 백필됨)을 확인. `/admin/pages`·`/admin/boards`의 드래그앤드롭 저장 코드는 EPIC-075 때 이미 작성돼 있었고 이 컬럼 부재로 저장만 실패하던 상태였음 — 이제 정상 동작할 것으로 기대. **사용자가 관리자 로그인 후 두 화면에서 실제로 드래그해 새로고침 후에도 순서가 유지되는지 확인 필요**(에이전트는 로그인 세션 없이 클릭 테스트 불가).

## 2026-08-05 (EPIC-079-FINAL-FIX 후속 — 외부 이미지/영상 복사-붙여넣기 + 임베드 너비 조절)
- **다른 웹사이트 이미지/영상 복사-붙여넣기 지원**: 지금까지 파일 바이트가 실제로 클립보드에 담긴 경우(예: 우클릭 "이미지 복사")만 동작했는데, 웹페이지에서 이미지를 선택해 Ctrl+C하거나 링크 자체를 텍스트로 복사하는 흔한 경우는 아무 반응이 없었다 — 두 가지를 보강했다: (1) `FigureImage`의 기존 `<img>` 캡처 규칙(`blockEditorCore.ts`)이 `src`만 읽었는데, 많은 사이트가 지연 로딩으로 `src`에 placeholder(빈 값/`data:` URI)만 넣고 진짜 URL은 `data-src`/`data-original`/`srcset`에 담아두므로 그쪽을 먼저 시도하도록 보강 — 클립보드 HTML에 `<img>` 태그가 있는 복사(대부분의 "이미지 선택 후 Ctrl+C")는 이제 실제 이미지로 정상 삽입된다. (2) 클립보드에 태그 없이 순수 URL 텍스트만 있는 경우(링크만 복사해 붙여넣기)를 위해 `BlockEditor.tsx`의 `handlePaste`에 새 분기 추가 — 이미지 확장자 URL은 바로 이미지로, 인식되는 플랫폼(유튜브/비메오/인스타/스포티파이/X/네이버 등) URL은 바로 임베드로, 직접 재생 가능한 영상 파일(.mp4 등) URL은 raw iframe 임베드로 자동 삽입한다.
  - **구현 메모**: 처음엔 Tiptap의 `editor.chain().insertContent()`로 구현했는데, `useEditor(...)` 설정 객체 안에서 그 반환값인 `editor`를 다시 참조하는 자기순환 때문에 React Compiler가 컴포넌트 전체의 메모이제이션을 포기해버리는 빌드 에러(`react-hooks/preserve-manual-memoization`)가 났다 — `editor` 대신 `handlePaste`가 직접 받는 ProseMirror `view`로 트랜잭션(`view.dispatch(view.state.tr.replaceSelectionWith(node))`)을 만드는 방식으로 우회해 해결.
- **URL 임베드 너비 조절 기능 추가("사이즈가 변경이 안 된다" 신고 대응)**: 지금까지 임베드 너비는 Instagram 위젯 전용(326~540px)이었고 나머지 provider(유튜브 비율 미지정/비메오/스포티파이/구글·네이버 지도/X/네이버 블로그/raw 등)는 항상 컨테이너 100%로 고정돼 조절 자체가 불가능했다 — `width` 속성을 일반화해 모든 provider의 iframe에 적용(비우면 기존처럼 100%), `EmbedView`(에디터 편집 UI)에 기존 "높이" 입력 옆에 "너비(px)" 입력을 새로 추가. 신고에 첨부된 예시(`/boards/movie/sbs`, SBS 영상 raw 임베드)처럼 컨테이너에 안 맞던 임베드도 이제 너비를 직접 지정해 맞출 수 있다.
- **검증**: `npx tsc --noEmit`/`npm run lint` 0 errors. Browser pane에서 실제로 (1) `<img>` 태그가 담긴 클립보드 HTML 붙여넣기 → 이미지 정상 삽입, (2) 순수 유튜브 URL 텍스트 붙여넣기 → 임베드 자동 생성 및 너비/높이 입력 둘 다 노출 확인, (3) 너비 입력에 480 입력 → 실제 렌더링된 `<iframe width="480px">`로 반영되는 것까지 DOM 조회로 확인.

## 2026-08-05 (EPIC-079-FINAL-FIX — Instagram Hydration & 슬라이드 Black Flash)
- **HTML 코드 임베드(customHtml) 에디터 미리보기에서 Instagram이 영영 스켈레톤으로만 보이던 버그 수정**: `BlockEditor.tsx`의 `CustomHtmlEmbedPreview`가 `sanitizeHtml()`로 정제한 HTML을 `innerHTML`로 DOM에 넣기만 하고 `processInstagramEmbeds()`를 한 번도 호출하지 않았다 — sanitizeHtml이 항상 `<script>` 태그를 제거하므로(Stored XSS 방지), 사용자가 붙여넣은 Instagram 공식 코드에 원래 딸려있던 `<script src=".../embed.js">`가 사라지고 나면 정적 blockquote(스켈레톤 UI)를 실제 임베드로 바꿔줄 주체가 아무도 없었다. `InstagramEmbedPreview`(URL 삽입 경로)는 이미 이 호출을 하고 있었는데 HTML 코드 붙여넣기 경로만 빠져있었던 것 — `html.includes("instagram-media")`일 때 `processInstagramEmbeds()`를 호출하도록 추가(스크립트가 DOM에 없으면 `instagramEmbed.ts`가 알아서 `<head>`에 동적 주입 후 처리, 있으면 즉시 재처리).
- **참고(사용자 재확인 필요)**: 이번 신고에 첨부된 실제 URL(`/boards/baroque/la-primavera-concerto-in-e-major`)을 직접 조회해보니, 삽입된 permalink가 이전 세션에서 이미 Instagram 서버 자체가 `/embed/`를 거부한다고 확인했던 바로 그 게시물(`CO8WLnjpl4k`)과 동일했다 — 공개 페이지(`PostBody.tsx`)는 이미 이 경로를 올바르게 처리하고 있어서(embed.js 로드→처리 시도→6초 안에 실제 렌더링 안 되면 자동으로 "Instagram에서 게시물 보기" 링크로 폴백) 지금도 정상적으로 폴백 링크가 뜨는 중임을 확인했다 — 이건 코드 버그가 아니라 Instagram 쪽이 이 특정 게시물의 임베드를 거부하는 것이라 이 저장소 코드로는 근본적으로 못 고친다. 위에서 고친 건 **에디터 미리보기**(관리자가 글 쓰는 중에 보는 화면)에서 발생하던 별개의 진짜 버그다 — 다른 정상적인(Instagram이 거부하지 않는) 게시물로 다시 테스트해 실제 렌더링을 확인해보시길 권한다.
- **홈페이지 메인 슬라이드 첫 로딩 시 검정 배경 노출(Black Flash) 제거**: `HeroSlideshow.tsx` 3곳 수정 — (a) 첫 슬라이드(`idx===0`) `<img>`에 `loading="eager"`+`fetchPriority="high"`를 줘서 최우선 로딩시킴, (b) wrapper의 `bg-gray-900`(사실상 검정)을 `bg-transparent`로 변경, (c) 이미지별 로드 완료 여부(`loadedIndices`)를 추적해 로드 전엔 `opacity-0`으로 숨겼다가 로드되는 순간 `duration-500` 트랜지션으로 부드럽게 페이드인. **버그 하나 추가 발견 및 수정**: 브라우저 캐시에 이미지가 있으면 React가 `onLoad` 리스너를 붙이기 전에 로드가 이미 끝나 있어 `onLoad` 이벤트 자체가 안 뜨는 경우가 실제로 있었다(재현 확인 — `img.complete`는 `true`인데 `opacity`는 `0`에 영구히 갇힘) — DOM에 붙는 시점(callback ref)에 이미 `.complete`면 그 자리에서 즉시 로드 처리하도록 보강.
- **검증**: `npx tsc --noEmit`/`npm run lint` 0 errors. 홈페이지를 새로고침해 wrapper 배경이 실제로 `rgba(0,0,0,0)`(투명)인 것, 첫 이미지에 `loading="eager"`/`fetchpriority="high"`가 실제로 붙는 것, 캐시된 이미지가 `opacity:1`로 정상 표시되는 것(수정 전엔 `opacity:0`에 갇혀있었음을 먼저 재현 확인)까지 DOM/computed style 조회로 확인. Instagram 부분은 위 참고 항목대로 이 특정 게시물로는 실제 임베드 렌더링까지는 확인 불가(Instagram 서버 제약) — 에디터 미리보기 코드 경로 자체가 정상적으로 `processInstagramEmbeds()`를 호출하는 것만 코드 리뷰로 확인.

## 2026-08-05 (EPIC-079-PHASE-4 후속 3차 — 미분류 페이지를 트리로 흡수 + 드래그로 메뉴 편입)
- **"미분류 페이지"를 별도 패널이 아니라 트리 안의 진짜 노드로 편입**: `CategoryTreeManager.tsx`의 `load()`가 이제 `page_builder`에는 있지만 어떤 `site_navigations` 행에도 안 걸린 페이지를 자동으로 찾아, 고정 식별자(`key = '__unassigned_pages__'`)를 가진 "미분류 페이지" 버킷 루트(비활성, 실제 사이트 탭엔 안 뜸) 아래에 진짜 `site_navigations` 행으로 만들어 넣는다(href=`/{slug}`, title/description은 페이지 것 그대로) — 이후로는 완전히 평범한 트리 노드라 기존 드래그(같은 `DndContext`)와 추가/수정/페이지 수정/관리/삭제 버튼을 전부 별도 구현 없이 그대로 쓸 수 있다(요청하신 "미분류 페이지에 5개 액션 전부 부여"를 새 UI를 만들지 않고 만족). 이미 트리에 들어간 페이지에 대해서는 재실행해도 중복 생성하지 않는다(매 로드마다 href→slug 매칭을 다시 계산).
- **미분류 버킷 밖으로 드래그하면 자동 활성화**: `handleDragEnd`에서 드래그한 행이 "미분류 페이지" 버킷의 자식이었고 목적지가 버킷이 아니면, `is_active`를 자동으로 `true`로 바꿔서 저장한다 — 실제 메뉴 자리로 옮겼다는 것 자체가 "이제 사이트에 노출하고 싶다"는 의도라고 보고, 별도로 "관리" 모달을 열어 활성 체크를 켜지 않아도 되게 했다. 버킷 안에서의 순서 변경이나 버킷 자체로의 이동은 건드리지 않는다.
- **"미분류 페이지 / 게시판" 패널 중복 노출 방지**: `AdminSiteStructurePage`가 쓰던 `fetchNavBranches()`는 `is_active=true`인 행만 읽는데, 새로 만든 버킷 아래 행들은 일부러 `is_active=false`라 그 목록엔 안 잡힌다 — 그대로 두면 트리에 이미 들어간 페이지가 하단 "미분류" 패널에도 중복으로 나타난다. `site_navigations.href` 전체(활성 여부 무관)를 별도로 조회해 `allLinkedSlugs` 집합을 만들고, `unassignedPages` 계산에서 이것도 함께 걸러내도록 수정 — 이제 패널엔 정말로 아무 nav 행도 없는 페이지만 남는다(게시판 쪽은 이번 범위 밖이라 그대로 둠, 사용자가 "미분류 페이지"만 명시했음).
- **검증**: `npx tsc --noEmit`/`npm run lint` 0 errors. Management API로 현재 미매핑 페이지 64개를 확인(다음 관리자 로드 시 자동으로 버킷에 편입될 대상). **이 세션의 Browser pane이 로그인 세션을 잃어(다른 동시 세션이 같은 브라우저를 공유하는 것으로 보임 — 이전 항목 참고) 실제 로드/드래그 클릭 테스트는 못 함** — 코드 리뷰 + tsc/lint까지만 완료, 관리자로 로그인해서 (1) "미분류 페이지" 버킷이 트리에 나타나는지, (2) 그 안의 항목을 실제 탭 아래로 드래그하면 활성화되며 옮겨지는지, (3) 하단 패널에 중복이 안 뜨는지 직접 확인 필요.

## 2026-08-05 (EPIC-079-PHASE-4 후속 2차 — 상단 탭 디자인 기능 신설)
- **참고**: 이 작업은 아래 "EPIC-079-PHASE-4 후속"(About Silo 드롭다운 수정 + 상단 탭 디스플레이 인라인 컨트롤)과 같은 날 별도 세션에서 동시에 진행됐다 — 같은 로컬 저장소를 두 세션이 동시에 건드린 것으로 보임(git log에 내가 만들지 않은 커밋 `55b7f9d`가 이미 존재하는 것으로 확인, 사용자에게 확인 필요). 코드 파일은 겹치지 않았다(아래는 `Navbar.tsx`/`admin/navigation/settings/page.tsx`만 수정, 저쪽은 `CategoryTreeManager.tsx`만 수정) — 다만 대기 중이던 SQL 마이그레이션 2건은 나도 독립적으로 한 번 더 실행했다(둘 다 재실행 안전하게 작성돼 있어 중복 실행 자체는 무해함, 두 번째 실행도 동일하게 `[]`/HTTP 201로 성공).
- **"홈페이지 설정 관리"(`/admin/navigation/settings`)에 "상단 탭 디자인" 섹션 신설**: 상단 탭(최상위 site_navigations 행) 하나하나의 표시 텍스트·서체(커스텀 폰트 파일 등록)·크기(px)·굵기·색상을 개별적으로 바꿀 수 있다. 트리 구조(제목/href/순서 등)는 여전히 "사이트 구성 관리"가 SSoT이고, 이 기능은 그 위에 얹는 순수 디자인 오버레이(`site_settings.top_tab_style`, 탭 id별 `{labelOverride, fontFamily, fontSizePx, bold, color, customFonts}`)라 표시 텍스트를 비워두면 원래 제목이 그대로 쓰인다. `Navbar.tsx`는 탭마다 고유 className(`silo-top-tab-<id>`)을 부여하고 그 클래스의 CSS 규칙을 동적 `<style>`로 주입하는 방식으로 적용했다 — 인라인 `style` prop 대신 이 방식을 쓴 이유: 인라인 스타일은 특정도가 가장 높아 기존 hover 시 흰 글씨 전환(`group-hover/tab:text-white`, Tailwind 클래스)까지 영구히 덮어써버려, 커스텀 색상을 켜면 hover했을 때 초록 배경과 겹쳐 글씨가 안 보이게 된다 — 별도 클래스 + `!important`로 "평소엔 커스텀 색, hover 땐 항상 흰색 유지"를 둘 다 보장했다. 커스텀 폰트는 메인 로고 섹션(EPIC-043)과 동일한 UI/저장 패턴(등록/적용 토글/폴백 체인)을 재사용.
- **검증**: `npx tsc --noEmit`/`npm run lint` 0 errors(기존과 동일한 warning만). Management API로 `site_settings.top_tab_style`에 테스트 값(마이페이지 탭 → "테스트 탭", 빨간색 `#cc3333`, 22px, 굵게)을 직접 넣고 홈페이지를 새로고침해 라벨 교체·색상·크기·굵기가 실제로 반영되는 것을 스크린샷으로 확인, hover 시엔 흰 글씨로 정상 전환되는 것도 확인 후 테스트 값 삭제(원상복구 완료). **관리자 UI 폼 자체(입력→저장 클릭 흐름)는 이 세션의 Browser pane이 세션 도중 로그인 상태를 잃어(원인 불명 — 다른 동시 세션이 같은 브라우저 세션/localStorage를 공유해 영향을 줬을 가능성 있음) 직접 클릭 테스트하지 못했다** — 코드 리뷰 + 프론트엔드 렌더링 검증까지만 완료, 다음에 로그인 세션으로 저장 버튼 자체를 한 번 확인 필요.

## 2026-08-05 (EPIC-079-PHASE-4 후속 — About Silo 드롭다운 수정 + 상단 탭 디스플레이 전용 컨트롤 + 대기 SQL 2건 실행)
- **"About Silo" 상단 탭 드롭다운이 안 보이던 버그 원인 규명 및 수정**: `navConfig.ts`의 `buildNavTree()`가 `target_type === 'tab'`(→ `NavTab.type: "link"`)이면 자식 행을 아예 읽지 않고 단순 링크로만 렌더링한다 — About Silo는 실제로 하위 카테고리(Silo Story 등)를 갖고 있었는데 `target_type`이 `'tab'`으로 저장돼 있어 Navbar가 드롭다운을 렌더링할 기회 자체가 없었다(코드 버그가 아니라 데이터 상태 문제). Management API로 `site_navigations` 직접 조회해 확인 후, `target_type`을 `'dropdown'`으로 업데이트 — Browser pane에서 About Silo 탭의 드롭다운 패널(`hasDropdownPanel`)이 실제로 렌더링되는 것을 DOM 조회로 확인.
- **상단 탭 "디스플레이"(노출 방식) 전용 인라인 컨트롤 추가**: 이전엔 `target_type`(링크/드롭다운/왼쪽·오른쪽 사이드바)을 바꾸려면 "관리" 모달을 열어야 했다 — 최상위(루트) 행에 한해 트리 행 자체에 바로 노출 방식을 바꿀 수 있는 `<select>`를 추가(`CategoryTreeManager.tsx`의 `CategoryRow`, `row.parent_id === null`일 때만 노출, 변경 즉시 `onUpdate` 호출). 하위 노드는 기존과 동일하게 루트를 따라가므로 편집 불가.
- **대기 중이던 SQL 마이그레이션 2건 실행 완료**: 사용자가 이번 세션에서 Management API 토큰을 제공 — `docs/sql/epic-079-phase-4-page-mapping-backfill.sql`(검증 쿼리 결과 미매핑 노드 0건 확인) / `docs/sql/epic-079-phase-4-post-board-move-grant.sql`(posts.board_id UPDATE GRANT + 소유자 보호 트리거 갱신) 둘 다 실행 완료 — 지난 세션에서 500 에러로 실패했던 "글 수정 시 다른 게시판으로 옮기기"가 이제 동작해야 한다(재검증 필요, 아래 참고).
- **CLAUDE.md 갱신(사용자 지시)**: "Verifying/changing the DB schema" 섹션에 — 토큰이 `.env.local`에 이미 있으면 대기 중인 마이그레이션을 실행 전에 다시 묻지 않고 바로 실행하는 것이 기본값이라는 문구 추가.
- **검증**: `npx tsc --noEmit`/`npm run lint` 0 errors(기존과 동일한 warning만). About Silo 드롭다운은 DOM 조회로 확인. **인라인 디스플레이 셀렉트와 "다른 게시판으로 글 옮기기" 재검증은 이 세션 후반부에 Browser pane 도구(navigate/javascript_exec)가 반복적으로 일시 불능 상태가 되어(플랫폼 쪽 classifier 문제로 보임, 이 저장소 코드와 무관) 완료하지 못함** — 스크린샷 1장으로 인라인 셀렉트 렌더링 자체는 확인했으나 클릭 상호작용까지는 확인 못함, 다음 세션에서 재확인 필요.

## 2026-08-05 (EPIC-079-PHASE-4 — Unified Navigation Tree, Page Mapping & Edit Post Sync)
- **Instagram 임베드 후속 수정(같은 세션, 이전 대화에서 발견된 버그)**: 너비 슬라이더/캡션 숨기기를 바꿀 때마다 `NotFoundError: Failed to execute 'removeChild'`로 에디터가 크래시하던 버그의 진짜 원인을 재현·규명 — Instagram `embed.js`가 blockquote를 iframe으로 바꿔치기하면 ProseMirror의 MutationObserver가 이를 "사용자가 직접 DOM을 편집했다"고 오인해 되돌리려다 React와 충돌했다. `EmbedBlock` NodeView에 `ignoreMutation: () => true`를 추가(atom 노드라 애초에 사용자가 contentEditable로 내부를 편집할 일이 없음)해 근본 해결, `InstagramEmbedPreview`도 React가 관리하지 않는 완전한 명령형 DOM(`ref` + `useEffect`)으로 리팩터링해 이중 방어. 실제 dev 서버에서 너비 슬라이더 연속 드래그 + 캡션 토글로 크래시 0건 확인. 이 수정으로 이전에 보고됐던 "본문 숨기기 누르면 오류" 버그도 같은 근본 원인이었음이 밝혀져 함께 해결됨. Instagram 위젯 너비 조절(326~540px, 공식 위젯이 실제로 존중하는 유일한 크기 값)과 HTML 코드 직접 삽입(플랫폼 "포함" 코드 붙여넣기, `<script>`는 보안상 제거되지만 iframe/blockquote는 그대로 재생) 기능도 함께 추가(`src/lib/rawHtmlEmbed.ts` 신설).
- **"사이트 구성 관리" 단일 트리로 통합**: `/admin/site-structure`가 "상단 탭"/"왼쪽 사이드바"/"오른쪽 사이드바" 3개의 별도 `CategoryTreeManager` 인스턴스(각각 `target_type`으로 필터링)로 쪼개져 있던 걸 하나(`CategoryTreeManager title="사이트 메뉴"`, `targetTypes` 생략)로 합쳤다 — 실제 프론트엔드(`navConfig.ts`의 `buildNavTree`)는 `site_navigations` 하나를 `parent_id` 트리로 읽고 depth 0(최상위) 행이 곧 실제 상단 탭이라, 관리자 화면이 별도 컨테이너로 쪼갤 이유가 없었고 오히려 실제 구조를 왜곡해 보여줬다. `targetTypes` prop을 optional로 바꿔 생략 시 모든 target_type을 하나의 트리로 표시, "+최상위 탭 추가"에는 새 탭의 노출 방식(링크/드롭다운/좌우 사이드바)을 고르는 드롭다운을 추가했다(이전엔 인스턴스별로 고정돼 있어 선택 자체가 필요 없었음). Browser pane에서 실제로 About Silo/사일로 상점/살롱데상/스튜디오/마이페이지 5개 상단 탭 전부가 하나의 트리 depth 0에 나열되는 것을 확인.
- **모든 노드에 "페이지 수정" 버튼 무조건 노출 + 온디맨드 페이지 생성**: 순수 폴더로 만들어진 노드(예: "Silo Story", "수미의 good n book n")는 `href`가 비어있어 매칭되는 `page_builder` 행이 없으면 버튼 자체가 안 보였다 — (1) `CategoryRow`의 저장(`handleSave`)에서 href를 빈 값으로 저장할 수 없게(비우면 `addChild`와 동일한 `/c/{id}` 규칙으로 자동 보정) 막고, (2) "페이지 수정" 버튼을 `pageId` 유무와 상관없이 항상 렌더링하는 `EditPageButton`으로 교체 — pageId가 없으면 클릭 시 그 자리에서 href를 보정하고 `ensurePageForSlug`를 직접 `await`해(기존 `updateRow`의 내부 호출은 fire-and-forget이라 곧바로 조회하면 못 찾는 경쟁 상태가 있어 직접 호출로 우회) 페이지를 만든 뒤 이동한다. Browser pane에서 href 없던 "수미의 good n book n" 노드를 실제로 클릭해 `/admin/pages/[새 id]`로 이동, Hero/Quote/Slide/Board/Gallery 5종 기본 위젯이 정상 생성된 것까지 확인.
- **DB 마이그레이션(미실행 — Management API 토큰이 이 컴퓨터에 없어 스크립트만 작성)**: `docs/sql/epic-079-phase-4-page-mapping-backfill.sql` — 기존 `site_navigations` 행 중 href가 비어있거나 매칭되는 `page_builder`가 없는 행을 전부 백필(href 자동 채움 → `page_builder` 생성 → 기본 위젯 5종, `ensurePageForSlug()`와 동일 로직의 SQL 버전, 재실행 안전).
- **글 수정(Edit) 폼에 "게시될 페이지 선택" 동적 드롭다운 적용**: 글쓰기(write)에만 있고 수정(edit)엔 옛 `getPostCategories` 하드코딩 카테고리 드롭다운이 남아있던 불일치 해소 — 공용 훅(`src/lib/useBoardOptions.ts`)과 공용 컴포넌트(`src/components/boards/BoardPageSelect.tsx`)로 write/edit 양쪽의 중복 로직을 통합하고, edit 폼에 write와 동일한 동적 드롭다운을 추가(초기값=현재 글이 속한 게시판, 실제 `board.slug`로 보정 — URL의 `board_slug`가 slug 라우팅 전환 이전에 만들어진 글이라 실제로는 board id(UUID)인 경우까지 처리). 다른 게시판을 고르면 저장 시 PATCH API의 `targetBoardSlug`로 실제 게시판을 옮긴다.
- **DB 권한 버그 발견 및 수정 SQL 작성(미실행)**: 실제로 다른 게시판으로 옮기는 걸 테스트해보니 `500 permission denied for table posts`로 실패 — `docs/sql/epic-079-phase-1.sql`이 `posts` UPDATE를 컬럼 단위로 GRANT할 때 `board_id`가 목록에 없었다(지금까지 board_id는 INSERT 시점에만 정해지고 UPDATE로 바뀐 적이 없어 누락은 당연했음). `docs/sql/epic-079-phase-4-post-board-move-grant.sql`로 GRANT 추가 + 소유자 검증 트리거(`posts_protect_content_columns`)의 보호 컬럼 목록에도 `board_id`를 추가(트리거에 안 넣으면 GRANT만으로 "본인 글이 아니어도 board_id만 바꾸면 안 걸리는" 보안 구멍이 생김). **미실행이라 이 세션에선 "다른 게시판으로 옮기기" 자체는 실패로 끝났고, 이 GRANT를 적용해야 실제로 동작한다** — 코드 경로(PATCH 로직, 기본 선택값 보정)는 검증 완료.
- **검증**: `npx tsc --noEmit`(0 errors)/`npm run lint`(0 errors, 기존과 동일한 종류의 warning만) 통과. Browser pane(사용자의 실제 로그인 세션과 공유)에서 통합 트리 렌더링, 페이지 수정 버튼 온디맨드 생성, 수정 폼 드롭다운 기본값 보정까지 전부 실제 클릭/DOM 조회로 확인 — 게시판 이동만 DB 권한 누락으로 실패(위 참고, 시도 자체는 롤백되어 원본 게시글은 변경 없이 그대로임).

## 2026-08-05 (EPIC-079-PHASE-3 — 임베드 커스터마이징 모달 + 글쓰기 동적 페이지 선택)
- **플랫폼별 임베드 커스터마이징 모달**: `window.prompt()` 기반 단일 URL 입력을 `EmbedConfigModal`(`src/components/editor/EmbedConfigModal.tsx`)로 교체 — URL을 붙여넣으면 `detectProvider()`가 정규식으로 플랫폼을 자동 인식하고, 플랫폼별 옵션을 보여준다: Instagram "본문(캡션) 포함 여부" 토글, YouTube "화면 비율"(16:9/9:16 쇼츠) 선택, Spotify "사이즈"(Compact 152px/Normal 352px) 선택. 삽입 후에도 `EmbedView`(에디터 내 노드 편집 UI)에서 계속 바꿀 수 있다.
- **Tiptap `EmbedBlock` 속성 확장**: `aspectRatio`(youtube 전용, "16:9"|"9:16")·`hideCaption`(instagram 전용) 두 속성 추가, `renderHTML()`이 이를 반영 — youtube는 `aspect-ratio` CSS로 반응형 wrapper를 만들어(9:16이면 최대 360px 너비로 세로로 길게), instagram은 `data-instgrm-captioned` 속성 유무로 공식 위젯의 캡션 노출을 제어한다. 저장된 HTML을 다시 열 때도 wrapper의 `data-aspect-ratio`/`data-hide-caption`에서 복원(`parseHTML`), `sanitize.ts`에 두 속성 허용 추가.
- **글쓰기 폼 "카테고리" 하드코딩 제거 → "게시될 페이지 선택" 동적 드롭다운**: 사용자 확인 결과 "글이 종속될 페이지(게시판) 자체를 바꾸는 선택지"를 의미하는 것으로 확정 — `/boards/[board_slug]/write` 페이지 마운트 시 `/api/boards`로 전체 게시판 목록을 불러와 "게시될 페이지 선택" 드롭다운으로 표시, 사용자가 고른 게시판(`selectedBoardSlug`)이 board_type/카테고리 조회·기존 태그 조회·글 제출·완료 후 리다이렉트까지 전부 실질적인 게시 대상이 되도록 재구성. URL의 `board_slug`는 "처음 진입한 게시판"이라는 초기값 역할만 하고, 실제 게시 대상은 드롭다운이 최종 결정 — 기존 게시판 링크(`/boards/[slug]/write`)는 그대로 유효해 다른 진입점을 손대지 않았다. 하드코딩 카테고리(`getPostCategories`/`DEFAULT_POST_CATEGORIES`) 관련 props는 글쓰기 화면에서만 전달을 멈췄다(수정 화면은 기존 글의 category 값을 계속 표시/보존해야 해서 그대로 둠).
- **부수 발견(수정 아님, 기록만)**: 테스트 중 `/api/boards`가 `boards.sort_order` 컬럼 부재로(EPIC-075 SQL 미실행, `NEXT_TASK.md`에 이미 등록된 기존 이슈) rich→legacy 폴백을 타 `slug`가 응답에서 빠지는 걸 발견 — "게시될 페이지 선택" 드롭다운이 slug 대신 board id(UUID)로 옵션 값을 채우게 되지만, `fetchBoard()`의 UUID 폴백(EPIC-079-PHASE-2에서 이미 구현) 덕분에 게시 자체는 정상 동작한다(확인 완료). 다만 현재 게시판이 목록에서 "불러오는 중..."으로 잘못 표시되는 사소한 표시 버그가 남는데, 이건 그 pre-existing 마이그레이션을 실행해야 완전히 해소된다.
- **검증**: `npx tsc --noEmit`/`npm run build` 통과. Browser pane에서 실제로 (1) YouTube URL + "9:16 쇼츠" 선택 → 삽입 후 iframe wrapper의 computed `aspect-ratio: 9 / 16`, 렌더링된 실제 크기가 세로로 긴 것(272.8×485px, 정확히 9:16 비율)을 DOM 측정으로 확인, (2) "게시될 페이지 선택" 드롭다운에서 다른 게시판으로 바꾸면 태그 조회 등 후속 요청이 그 게시판으로 정확히 다시 나가는 것을 네트워크 로그로 확인.

## 2026-08-05 (EPIC-079-TIPTAP-FIX-V2 — Instagram 임베드 원인 규명 + 소셜/지도 임베드 확장)
- **사용자 신고("인스타그램 임베드가 작동 안 됨") 원인 규명**: EPIC-079-PHASE-1이 도입한 `https://www.instagram.com/{p|reel|tv}/{id}/embed/captioned/` 직접 iframe 방식을 curl로 직접 조사한 결과, 그 URL이 **모든 요청에 대해 `X-Frame-Options: DENY`를 응답**한다는 것을 확인했다 — 브라우저가 이 헤더가 붙은 페이지를 어떤 사이트의 iframe으로도 렌더링을 거부하므로, 사설 코드로는 절대 우회할 수 없는 서버 측 차단이었다(즉, 처음부터 한 번도 동작한 적이 없었음). Instagram 공식 oEmbed API(`graph.facebook.com/.../instagram_oembed`)로 이 게시물이 실제로는 "임베드 가능"하다고 확인까지 했음에도 `/embed/*` 응답 자체는 여전히 DENY였다 — Instagram 서버 쪽의 정책/설정 문제로 보이며, 우리 쪽 코드 변경만으로는 근본적으로 해결할 수 없다.
- **1차 수정**: 직접 iframe 대신 Instagram이 공식 지원하는 유일한 방법인 `<blockquote class="instagram-media" data-instgrm-permalink="...">` 마크업 + `embed.js` 위젯 스크립트 방식으로 교체(`src/lib/instagramEmbed.ts` 신설 — 신뢰된 코드에서만 스크립트를 전역 로드, sanitize-html의 `<script>` 차단 원칙은 그대로 유지). 그런데 실제 브라우저로 재현 테스트한 결과, Instagram 공식 위젯이 만든 iframe **역시 같은 이유로 높이가 0에서 전혀 자라지 않는(=콘텐츠 로드 실패) 경우가 실제로 있었다**(oEmbed API가 임베드 가능하다고 확인해준 진짜 게시물로도 재현됨).
- **2차 수정(안전망)**: "아예 빈 박스"보다 "Instagram에서 보기 링크"가 항상 낫다는 판단으로, `embed.js` 처리 후 6초 안에 실제로 렌더링되지 않은 것(iframe 높이 미달 또는 처리만 되고 iframe으로 안 바뀐 잔여 blockquote 둘 다)을 자동으로 클릭 가능한 폴백 링크로 교체하는 워치독을 추가 — Instagram이 정상 응답하면 실제 임베드가, 응답을 막으면 깨진 빈 박스 대신 항상 동작하는 링크가 보장된다. 실제 브라우저 테스트로 폴백 전환까지 확인.
- **소셜/지도 임베드 확장(X/Twitter, 네이버 블로그, 네이버 지도, 구글 지도 붙여넣기)**: 기존 `EmbedBlock`(Tiptap 커스텀 iframe 노드, `provider`별 URL 파서 + `renderHTML`/`parseHTML`)에 3개 provider 추가.
  - **X(Twitter)**: `platform.twitter.com/embed/Tweet.html?id=` — 확인 결과 `X-Frame-Options` 없음(공식 임베드 iframe). 실제 브라우저에서 트윗(@jack의 "just setting up my twttr")이 좋아요 수·답글까지 실제로 렌더링됨을 확인.
  - **네이버 블로그**: `blog.naver.com/PostView.naver?blogId=&logNo=` — 확인 결과 `X-Frame-Options` 없음. 실제 브라우저에서 네이버 블로그 실제 콘텐츠가 렌더링됨을 확인.
  - **구글/네이버 지도**: 사용자가 "퍼가기" 코드(`<iframe src="...">`)를 에디터에 직접 붙여넣으면 `EmbedBlock.parseHTML()`의 새 raw-iframe 캡처 규칙이 `src`를 그대로 가져와 도메인으로 provider(googleMaps/naverMap/raw)를 자동 분류한다 — 네이버 지도는 지도 페이지 자체가 `X-Frame-Options: DENY`라(직접 확인) URL 타이핑만으로는 embed URL을 만들 방법이 없어, 이 붙여넣기 경로가 사실상 유일한 입력 방법이다. ClipboardEvent를 직접 시뮬레이션해 두 provider 모두 정확히 캡처됨을 확인.
- **Instagram 관련 사용자 지시 이탈 사항 — 반드시 확인 필요**: 이번 지시사항 원문은 Instagram도 "URL 끝에 `/embed/`만 붙이면 된다"는 단순 변환을 요청했지만, 이는 위에서 확인한 대로 Instagram이 서버 단에서 명시적으로 차단하는 방식이라 그대로 구현하면 신고받은 버그를 그대로 재현하게 된다 — 그래서 Instagram만 요청받은 단순 URL 변환 대신 기존 공식 위젯+자동 폴백 링크 방식을 그대로 유지했다. 다른 4개 provider(YouTube/Vimeo/Spotify/GoogleMaps 기존 + Twitter/NaverBlog/NaverMap 신규)는 전부 지시사항의 아키텍처(범용 iframe 노드 + provider별 정규식 파서 + 붙여넣기 캡처)대로 구현.
- **검증**: `npx tsc --noEmit`(0 errors), `npm run build`(전체 라우트 정상 컴파일) 통과. Browser pane에서 실제 로그인 세션으로 글쓰기 화면에 직접 4개 provider를 모두 삽입해 스크린샷으로 확인(Twitter/네이버 블로그는 실제 콘텐츠 렌더링, Google/네이버 지도는 raw iframe 캡처 정상 동작) — 테스트용 게시글은 저장(등록)하지 않고 폐기함(로컬 스토리지 임시저장만 발생, DB에 draft 행 없음을 재조회로 확인).

## 2026-08-04 (EPIC-079-PHASE-2 SQL 실행 — 마이그레이션 라이브 반영)
- 사용자가 제공한 Supabase Management API 토큰으로 `docs/sql/epic-079-phase-2-slug.sql`을 직접 실행 — `boards`/`posts` 양쪽에 `slug` 컬럼이 실제로 추가되고 기존 행이 전부 백필됨을 재조회로 확인. **드리프트 발견**: 백필 스크립트가 `boards.created_at`으로 정렬하도록 작성돼 있었는데, 라이브 `boards` 테이블에는 애초에 그 컬럼이 없어(다른 EPIC에서도 반복 발견된 스키마 드리프트 패턴) 1차 실행이 `42703`으로 실패 — `order by id`로 수정해 재실행, 성공(`docs/sql/epic-079-phase-2-slug.sql` 파일도 함께 수정됨).
- dev 서버로 `/boards/general`(실제 slug 라우팅)과 게시글 상세(`/boards/general/silo-angel`)가 UUID 없이 정상 렌더링·댓글까지 표시되는 것을 직접 확인 — slug 기반 라우팅이 이제 완전히 동작한다.
- 검증 중 만든 테스트 카테고리("새 항목", href `/c/97571a7b-...`)와 자동 생성됐던 연결 `page_builder`/`page_modules` 행을 SQL로 함께 정리.
- **보안 노트**: 이번에 받은 Management API 토큰은 마이그레이션 실행에만 사용했고 메모리·파일 등 어디에도 저장하지 않았다(CLAUDE.md의 "토큰은 이 대화에서만 유효, 저장하지 않음" 방침 그대로 준수) — 다음에 DDL이 필요하면 다시 요청해야 한다.

## 2026-08-04 (EPIC-079-PHASE-2 — Routing, Navigation, and UI Polish)
- **게시판 URL을 UUID에서 slug 기반으로 전면 마이그레이션**: `/boards/[id]/[postId]` → `/boards/[board_slug]/[post_slug]`. `boards.slug`(전역 UNIQUE)/`posts.slug`(board별 UNIQUE) 신규 컬럼 + 백필 SQL(`docs/sql/epic-079-phase-2-slug.sql`, 한글 등 slugify 결과가 빈 문자열이면 id 앞 8자리로 폴백, 충돌 시 `-2`/`-3` 접미사). API 라우트 6개(`/api/boards/[id]/**` 전부) 및 페이지 라우트 4개(`/boards/[id]/**`)를 `[board_slug]`/`[post_slug]`로 재구성, `fetchBoard()`(`src/lib/boardFetch.ts`)에 slug 우선 조회 + 구 UUID 링크 하위호환 폴백 추가. 게시글 생성 시 제목을 slugify해 게시판 내 유니크한 slug를 서버가 자동 생성(`generateUniquePostSlug`). `BOARD_LEGACY_FIELDS`에는 의도적으로 slug를 넣지 않아(RICH 단계에만 포함) 마이그레이션 미실행 DB에서도 게시판 목록 조회 자체는 계속 동작하도록 기존 rich/legacy 폴백 철학을 유지 — 개별 게시판/게시글 상세는 라우팅 키 자체가 slug라 마이그레이션 전엔 동작 불가(불가피, UUID 폴백으로 완화). 게시판 생성/복제 API(`/api/admin/boards`, `.../duplicate`)에도 슬러그 자동 생성 로직 추가.
- **상단 탭(Navbar) 드롭다운이 서브카테고리(손자 항목)를 못 보여주던 버그 수정**: `navConfig.ts`의 `buildNavTree()`가 `dropdown` 타입에서 자식(1단계)까지만 읽고 손자를 버렸던 것을, sidebar-left/right와 동일하게 한 단계 더 재귀해 `NavItem.children`으로 담도록 수정. `Navbar.tsx`는 기존 group 2차 플라이아웃과 동일한 패턴으로 `item.children`이 있으면 hover 플라이아웃을 렌더링.
- **새 카테고리 생성 시 연결 페이지가 없어 "페이지 수정" 버튼이 안 뜨던 버그 수정**: `CategoryTreeManager.tsx`의 `addChild()`가 href 없이 행만 만들어(EPIC-068의 `ensurePageForSlug` 트리거가 "href 저장 시점"에만 동작) 생성 직후엔 페이지가 자동 생성되지 않았던 것을, 생성 직후 `id` 기반 고유 href(`/c/<id>`)를 즉시 채우고 `ensurePageForSlug`를 바로 호출하도록 수정 — 이제 카테고리 생성과 동시에 페이지+기본 위젯 5종이 만들어지고 "페이지 수정" 버튼이 바로 노출된다. Navbar/사이드바는 여전히 동일한 `site_navigations` 데이터를 각자 별도로 fetch(Navbar가 조회 후 props로 전달)해 UI 컴포넌트는 완전히 분리된 채로 유지.
- **사이드바 WebM hover 애니메이션이 재생되지 않던 버그 수정**: `SidebarTriggerMedia.tsx`의 숨김 `<video>`가 JSX `autoPlay`/`muted` 속성에만 의존해 브라우저 autoplay 정책에 조용히 막히는 경우 `videoWidth/height`가 계속 0으로 남아 canvas에 아무것도 그려지지 않았던 것을, `video.muted`를 프로퍼티로 명시 설정 + `video.play()` 직접 호출 + `canplay`/문서 최초 상호작용 시점 재시도로 수정.
- **홈페이지 슬라이드쇼 "여백 배경 이미지"가 검정으로 나오던 버그 수정**: `admin/navigation/settings/page.tsx`의 `compressImage()`가 압축 시 항상 `image/jpeg`로 재인코딩해(알파 채널 없음) 투명 PNG의 투명 영역이 검정으로 flatten되던 것을, 알파를 보존하는 `image/webp`로 교체.
- **검증**: `npm run build` 전체 라우트 정상 컴파일(신규 slug 라우트 포함, 라우트 충돌 없음), `npx tsc --noEmit` 0 errors. Browser pane에서 실제 마이그레이션 미실행 상태로 확인: `/boards` 디렉토리 목록/게시판 상세/게시글 상세가 전부 기존 UUID 링크로 정상 폴백 동작(200 OK), 관리자 로그인 세션으로 "사이트 구성 관리"에서 실제로 최상위 카테고리를 새로 만들어 href+"페이지 수정" 버튼이 즉시 뜨는 것을 직접 확인(테스트 항목은 검증 후 삭제 시도했으나 브라우저 네이티브 확인창 자동 승인이 안 되는 도구 한계로 남아있음 — **사용자가 "사이트 구성 관리" 상단 탭에서 "새 항목"(`/c/97571a7b-...`) 행을 삭제해달라**). **DB 마이그레이션 미실행 — 사용자가 Supabase SQL Editor(또는 Management API 토큰 제공)로 `docs/sql/epic-079-phase-2-slug.sql`을 실행해야 slug 기반 라우팅(개별 게시판/게시글 상세 페이지)이 실제로 동작한다.** 사이드바 hover 영상/슬라이드쇼 배경 수정은 이 세션의 Browser pane이 `@media (hover: hover)`를 만족하지 않아 실제 마우스 호버로는 확인 못함(EPIC-076/078 때와 동일한 한계) — 사용자가 실제 브라우저에서 확인 필요.

## 2026-08-04 (EPIC-079-PHASE-1 — Tiptap Editor & Post CRUD Stabilization)
- **근본 원인 발견**: 작업 시작 전 라이브 DB를 직접 점검한 결과, `docs/sql/epic-053-1.sql`(EPIC-053.1이 작성)이 **실제로는 한 번도 라이브에 적용되지 않았음**을 확인 — `posts.body_json` 컬럼 없음, Storage 버킷(`post-images`/`gallery`/`attachments`) 0개, 심지어 `docs/database-schema.sql`이 원래부터 문서화했던 `posts.tags`/`view_count`/`updated_at`조차 라이브에 없었다(대응 마이그레이션 파일 자체가 존재하지 않아 EPIC-047 Board Engine 적용 시 SQL Editor 실행이 통째로 누락된 것으로 추정). 보고된 "이미지 업로드해도 아무것도 안 뜸"의 실제 원인은 코드 버그가 아니라 버킷 자체가 없어 업로드가 조용히 실패한 것이었고, "글 수정이 안 됨"의 실제 원인은 `posts` 테이블에 `title`/`body` 등 UPDATE 권한(GRANT) 자체가 없었던 것이었다(코드의 소유권 체크는 정상 동작 중이었음). 사용자 승인 하에 Supabase Management API 토큰으로 `docs/sql/epic-079-phase-1.sql`을 작성해 직접 실행 — epic-053-1.sql 재적용 + 누락된 기본 컬럼 3개 + EPIC-079 신규 컬럼(`thumbnail_visible`/`category`) + `posts` UPDATE GRANT 확장 + 소유자 보호 트리거(`posts_protect_content_columns_trg`, SECURITY DEFINER) + 글 삭제 RLS 정책(`posts_delete_own_or_admin`, 지금까지 DELETE 정책 자체가 없어 전면 차단 상태였음) 신설. 라이브 반영 확인 완료(컬럼/버킷/정책/트리거 전부 재조회로 검증).
- **RLS 설계 노트**: 기존 `posts update like fields` 정책이 `using(true)`(좋아요 기능을 위해 의도적으로 모든 행에 열려 있음)이라, title/body 등에 컬럼 GRANT만 추가하면 Postgres RLS의 permissive OR 특성상 "본인 글만 수정 가능"을 행 단위로 강제할 수 없었다(추가 정책을 더 넣어도 `true` 정책이 이미 모든 행을 통과시킴). 컬럼 GRANT는 필요한 컬럼까지 넓히되, 실제 소유권 검증은 `members` 자기참조 재귀를 피하기 위해 EPIC-071-HOTFIX의 `is_current_user_admin()`과 동일한 SECURITY DEFINER 패턴의 BEFORE UPDATE 트리거로 수행 — like_count/is_best/view_count는 계속 누구나 건드릴 수 있고, title/body/body_json/tags/category/thumbnail_visible 등은 작성자 본인 또는 관리자만 실제로 값이 바뀔 수 있다(트리거가 그 외 사용자의 변경 시도를 예외로 거부).
- **Tiptap 임베드 확장(Instagram/Spotify/Google Maps)**: 조사 결과 5개 provider 전부 스키마/툴바에는 이미 연결돼 있었으나(코드 자체는 크게 손상돼 있지 않았음), (1) Instagram은 공식 oEmbed(script 기반)를 sanitize-html이 `<script>`를 허용하지 않아 쓸 수 없어 항상 링크 카드로만 폴백하던 것을 Instagram이 직접 제공하는 iframe 엔드포인트(`/{p|reel|tv}/{id}/embed/captioned/`)로 교체해 실제 미리보기가 가능하도록 수정. (2) Google Maps는 URL 정규화가 전혀 없어 사용자가 이미 `/maps/embed` 링크를 알고 붙여넣지 않는 한 항상 실패했던 것을, 일반 `google.com/maps` 공유 링크에 `output=embed`를 자동으로 붙이는 `normalizeGoogleMapsEmbedUrl()`로 수정(단축 URL `maps.app.goo.gl`은 서버 없이 리다이렉트를 풀 수 없어 지원 범위 밖, 링크 카드로 안내 폴백). (3) 모든 provider에 `height`(px) 속성을 추가해 "어디까지 보이게 할 것인지" 뷰 영역을 사용자가 직접 조절 가능하도록 편집기 NodeView에 숫자 입력 컨트롤 연동. (4) `EmbedBlock.parseHTML`이 커스텀 `getAttrs` 없이 기본 Tiptap 파서에 의존해, `body_json` 없이 레거시 HTML만으로 글을 다시 열면 provider/url/caption이 매번 `youtube`/빈 값으로 리셋되던 잠재 버그를 발견해 `getAttrs`+`reverseEmbedSrc()`로 직접 역변환하도록 수정(iframe `src`/링크 카드 `href`/캡션 `<p>`에서 원본 속성을 복원). (5) `BlockEditor.tsx`의 `EmbedPreview`가 `blockEditorCore.ts`의 `embedSrc()`와 완전히 중복된 정규식 로직을 갖고 있던 것을 `embedSrc()` export로 통합해 두 곳이 어긋날 여지를 없앰. `sanitize.ts`에 Instagram embed가 쓰는 `scrolling` 속성 허용 추가.
- **이미지 업로드**: 코드 경로(`storage.ts` → `BlockEditor.tsx`)는 이미 정상 구현돼 있었고, 실제 원인은 위 DB 마이그레이션 미적용(Storage 버킷 부재)이었다 — 별도 코드 수정 없이 인프라 수정만으로 해결.
- **글쓰기 메타 필드**: (1) **썸네일 노출 토글** — `posts.thumbnail_visible`(기본 true) 신설, `PostForm.tsx`에 대표 이미지가 있을 때만 보이는 체크박스 추가, `StoryThumbnailModule`/`GalleryModule`/게시글 상세 헤더가 이 값이 false면 대표 이미지가 있어도 썸네일을 렌더링하지 않도록 반영. (2) **카테고리 드롭다운** — 정규화 테이블 없이 `posts.category`(text, nullable) 컬럼 + `boardLayout.ts`의 `BoardDefinition.categories`(옵션, 게시판별 고정 목록) + 미지정 시 폴백하는 `DEFAULT_POST_CATEGORIES`(공지/자유/질문/정보/후기/기타) 방식으로 구현(사용자 확인: "게시판별 고정 카테고리 목록" 간단 버전 채택, 정식 categories 테이블+관리자 CRUD는 범위 밖). (3) **태그 — 기존 태그 선택** — 새 엔드포인트 없이 게시글 목록 API가 이미 계산해 돌려주는 `availableTags`를 재사용해 클릭 가능한 태그 칩 UI 추가(클릭 시 쉼표 구분 입력값에 추가/제거 토글).
- **게시글 CRUD**: (1) 글 수정 페이지(`edit/page.tsx`)의 client-side 접근 가드가 `author_id !== member.id`만 체크해 관리자가 남의 글을 열람조차 못 하던 버그(서버 PATCH는 이미 관리자 예외를 허용하고 있었음 — 클라이언트만 놓치고 있었다) 수정, 게시글 상세 페이지의 "수정" 링크 노출 조건도 동일하게 수정. (2) **글 삭제 — 지금까지 전혀 존재하지 않던 기능**: `DELETE /api/boards/[id]/posts/[postId]` 라우트 신설(본인 또는 관리자만, 삭제 전 본문 이미지를 `image_cleanup_queue`에 적재 — 수정 시와 동일한 GC 패턴, `comments`/`likes`는 기존 FK `ON DELETE CASCADE`로 함께 정리됨을 라이브 DB로 확인), 게시글 수정 화면 하단과 게시글 상세 화면(작성자/관리자에게만) 양쪽에 확인 다이얼로그가 있는 "삭제" 버튼 추가.
- **검증**: `npx tsc --noEmit`(0 errors). Browser pane(비로그인)으로 `/boards`, 게시판 상세, 글쓰기 화면(`/boards/[id]/write`)을 직접 열어 새 카테고리 드롭다운(공지/자유/질문/정보/후기/기타)과 임베드 메뉴(YouTube/Vimeo/Instagram/Spotify/Google Maps 5개 버튼)가 정상 렌더링되고 콘솔/서버 에러가 없음을 확인, `/api/boards/[id]/posts` 응답이 새로 select에 추가한 `thumbnail_visible`/`category` 필드를 포함해 200으로 정상 응답함을 서버 로그로 확인(마이그레이션이 실제 반영됐다는 증거). **로그인 세션이 필요한 부분(실제 글 작성/이미지 업로드/임베드 삽입 후 저장/수정/삭제 버튼 클릭)은 에이전트가 자격 증명을 갖고 있지 않아 클릭 테스트하지 못함 — 사용자가 로그인 후 직접 (1) 이미지 업로드가 실제로 에디터에 뜨는지, (2) Instagram/Spotify/Google Maps 임베드가 실제 URL로 정상 표시되는지(높이 조절 포함), (3) 카테고리/태그칩/썸네일 토글 저장이 실제로 DB에 반영되는지, (4) 글 수정 화면에서 기존 데이터가 전부 채워져 열리는지, (5) 삭제 버튼이 실제로 글을 지우는지 확인 필요.**

## 2026-08-04 (EPIC-079-PHASE-1 후속 — 임시 테스트 계정으로 실제 클릭 검증, 추가 버그 3건 발견/수정)
- **테스트 계정 실검증**: "테스트는 테스트용 관리자 계정을 만들어서 하면 되는거 아니야?" 확인 후, `/signup`으로 임시 계정(`epic079-test-20260804@silostore.net`)을 만들어(이메일 확인 불필요, 가입 즉시 로그인됨) 사용자 승인 하에 Management API로 `is_admin`을 켜고 Browser pane에서 실제 글쓰기 → 이미지 업로드(1x1 PNG 실제 업로드, Storage public URL 확인) → 임베드 3종(Instagram/Spotify/Google Maps) 삽입 → 저장 → 상세 페이지에서 sanitize된 실제 HTML에 iframe 3개가 정확한 src/height로 렌더링됨을 확인 → 글 수정(기존 데이터 전부 로드 확인, 제목만 바꿔 저장) → 글 삭제(게시판 목록에서 실제로 사라짐, `image_cleanup_queue`에 이미지가 큐잉됨을 DB 재조회로 확인)까지 전체 CRUD 사이클을 실제로 완주해 검증했다. 이 과정에서 코드 리뷰만으로는 못 잡았을 실제 버그 3개를 발견해 함께 수정:
  1. **연속 삽입 시 이전 블록이 사라지는 버그**: 이미지 삽입 → Instagram 삽입 → Spotify 삽입 → Google Maps 삽입을 연달아 하면 마지막 하나만 남고 나머지가 전부 사라졌다(실제 재현 확인). 원인: `figureImage`/`embed`/`gallery`/`linkCard`가 전부 `atom:true` 블록 노드인데, 삽입 후 selection이 그 블록을 감싸는 NodeSelection으로 남는 경우가 있어(뒤에 빈 문단이 없거나 split 경계에서), 다음 `editor.chain().focus().insertContent(...)`가 "추가"가 아니라 그 노드를 "교체"해버렸다. `BlockEditor.tsx`의 4곳(이미지/갤러리/임베드/링크카드 삽입) 전부 `.focus()`를 `.focus("end")`로 바꿔 항상 문서 끝에서 삽입하도록 고정 — 재발 방지 주석도 남김.
  2. **AuthProvider 세션 판정 레이스**: `session` state의 초기값이 항상 `null`이라, 로그인된 사용자도 `getSession()`이 비동기로 resolve되기 전 아주 짧게 "로그인 안 됨"(`member=null`, `memberLoading=false`) 상태가 실제로 관측됐다 — 글 수정 페이지가 이 순간에 작성자 판정을 내려 "본인이 작성한 글만 수정할 수 있어요"를 실제로 띄우는 것을 재현 확인. `src/lib/AuthProvider.tsx`의 member-fetch effect에 `if (loading) return;` 가드 추가(세션 확인 자체가 끝나기 전엔 member 판정을 아예 시작하지 않음).
  3. **수정 화면에서 본문을 안 건드리고 저장하면 조용히 무시됨**: `PostForm.tsx`의 제출 가능 여부 판정(`isBodyEmpty`)이 에디터의 `onChange`가 넘겨주는 `bodyHtml` state로 계산되는데, 수정 화면은 `body_json`으로 기존 글을 불러오지 `onChange`를 스스로 부르지 않는다 — 그래서 제목만 고치고 본문은 그대로 둔 채 저장하면 `bodyHtml`이 계속 빈 문자열이라 `handleSubmit`이 에러 표시도 없이 조용히 리턴, 실제로 재현 확인(네트워크에 PATCH 자체가 안 나감). `src/lib/blockEditorCore.ts`에 `isEmptyDoc(json)`(JSON 트리를 직접 순회해 텍스트/미디어 존재 여부 판정) 신설, `PostForm.tsx`가 이걸로 교체.
  - 수정 페이지의 `notAllowed` state가 한 번 true가 되면 이후 effect가 성공적으로 재실행돼도 되돌아오지 않던 방어 코드도 함께 보강(`edit/page.tsx`, 위 2번 레이스의 재발/유사 케이스에 대한 이중 방어).
- **정리**: 테스트 중 업로드한 이미지 2장은 `image_cleanup_queue`에 정상 큐잉(직접 DB 삭제는 storage.protect_delete() 트리거가 막아 Storage API를 통해서만 가능 — 트리거 자체가 의도된 안전장치라 그대로 둠, 앱의 정식 GC 절차 대상), 테스트 계정의 `is_admin`은 검증 직후 `false`로 원복 완료.
- **검증**: `npx tsc --noEmit`(0 errors). 위 전체 CRUD 사이클을 실제 브라우저 클릭/입력으로 완주(이미지 업로드/임베드 3종 삽입·렌더링/수정 저장/삭제까지 전부 실사용 확인 — 이전 세션 보고서의 "로그인 세션이 없어 확인 못함" 항목이 이번에 전부 해소됨).

## 2026-07-31 (EPIC-078 후속 3차 — 홈 큐레이션 삭제, 홈페이지 위젯은 슬라이드쇼 바로 다음)
- **"노출 필터 (홈 큐레이션)" 기능 완전 삭제**: "홈페이지 자체 페이지 수정 버튼 + 위젯 추가/수정 기능이 필요하고, 노출 필터(홈 큐레이션)는 이제 필요 없으니 삭제해달라"는 요청. 먼저 확인해보니 **"페이지 수정" 버튼과 위젯 추가/수정 기능은 이미 EPIC-067에서 홈페이지(`slug="home"`)에 연결돼 있었다** — `src/app/page.tsx`에 `<PageEditButton slug="home" />`+`<PageBuilderRenderer>`가 이미 존재하고, 라이브 DB에도 `page_builder.slug='home'`(status='published') 행이 실제로 있음을 anon-key REST 조회로 확인(관리자로 로그인하면 우측 상단에 버튼이 보여야 함). 그래서 이번 작업은 "노출 필터(홈 큐레이션)" 삭제에 집중: EPIC-041이 만든 `site_settings.home_curation`(블록 배열) 관련 코드를 전부 제거 — `/admin/navigation/settings`의 "노출 필터 (홈 큐레이션)" 섹션(상태/타입/기본값/`addCurationBlock`·`updateCurationBlock`·`removeCurationBlock`·`moveCurationBlock`·`load()` 분기·JSX 전부), `src/app/page.tsx`의 `home_curation` 조회/`fetchCurationItems`/`curationSections`/`<HomeCurationSlider>` 렌더링, 더 이상 아무 데서도 쓰이지 않는 `src/components/HomeCurationSlider.tsx` 컴포넌트 파일 자체까지 삭제. 결과적으로 홈페이지 콘텐츠 순서가 "히어로 슬라이드쇼 → (예전엔 홈 큐레이션 슬라이더) → Page Builder 위젯"에서 "히어로 슬라이드쇼 → Page Builder 위젯"으로 단순해짐(요청하신 "슬라이드쇼 다음으로 위젯" 순서를 만족). 기존에 저장된 `site_settings.home_curation` DB 행 자체는 지우지 않음(더 이상 아무 코드도 읽지 않는 비활성 데이터로 남음, 필요시 나중에 정리 가능).
- **검증**: `npx tsc --noEmit`(0 errors)/`npm run lint`(0 errors, 새 warning 없음)/`npm run build`(전체 라우트 정상 컴파일, 홈페이지 포함) 통과.

## 2026-07-31 (EPIC-078 후속 2차 — 사이드바 아이콘 상시 그림자 제거 + hover 20% 확대 + 슬라이드쇼 이미지 압축/미리보기)
- **사이드바 트리거 버튼 상시 box-shadow 제거**: 앞선 hover 영상 알파 채널 수정 후에도 "커서를 올리지 않아도 사각형이 있다"는 재신고 — 원인은 트리거 버튼에 hover 여부와 무관하게 항상 적용돼 있던 `shadow-md` 클래스(항상 켜진 드롭섀도우가 사각 경계처럼 보임)였다. `LeftSidebar.tsx`/`RightSidebar.tsx` 버튼 className에서 `shadow-md` 제거, `box-shadow: none` 계산된 스타일로 확인.
- **사이드바 아이콘 hover 20% 확대**: 기본/호버 미디어를 감싸는 `<span>`에 `transition-transform duration-300 group-hover:scale-[1.2]` 추가 — 실제 `:hover` 상태를 시뮬레이션해 좌/우 아이콘 모두 `scale: 1.2`가 정상 적용됨을 확인(컴파일된 CSS는 `scale` 표준 속성을 쓰므로 `getComputedStyle().scale`로 검증, `.transform`이 아님에 주의).
- **슬라이드쇼 "여백 배경 이미지" 업로드 압축 품질 설정 + 미리보기 추가**: "여백 배경이미지 화질이 너무 낮다, 화질 조절 기능과 작은 미리보기를 관리자 홈페이지 설정에 추가해달라"는 요청. 먼저 `HeroSlideshow.tsx`/`CHANGELOG.md`(EPIC-036/039/043)를 조사한 결과, **코드가 화질을 자동으로 낮춘 적이 없음**을 확인 — EPIC-043은 성능 개선을 위해 `next/image`(화질 조절 가능)로 전환을 시도했으나 하이드레이션 전체가 깨지는 버그로 완전히 롤백됐을 뿐, 실제로는 지금도 `uploadImage()`가 원본을 무변형으로 그대로 업로드한다(압축 로직 자체가 없었음). 이 사실을 사용자에게 알리고, 그래도 원본 대비 퍼센티지 압축 기능을 새로 만들어달라는 요청에 따라: `hero_slideshow.wallpaperQuality`(1~100, 기본 100=무압축) 필드 신설, 새 `compressImage()` 헬퍼(브라우저 canvas에 원본 해상도 그대로 그린 뒤 `canvas.toBlob(..., 'image/jpeg', quality/100)`로 재인코딩, 리사이즈는 하지 않고 압축률만 조절, 100이면 재인코딩 자체를 건너뜀)를 `/admin/navigation/settings`의 여백 배경 이미지 업로드 핸들러(`handleWallpaperFileChange`)에 연결 — **다음에 새로 업로드하는 이미지부터 적용되며, 이미 업로드된 이미지에는 소급 적용되지 않음**(UI 라벨에 명시). 슬라이드/여백 배경 이미지 각 항목에 `w-16 h-16 object-contain rounded bg-gray-100` 작은 미리보기 썸네일도 추가(기존 사이드바 아이콘 업로드 슬롯의 미리보기 패턴 재사용, 지금까지는 URL 텍스트만 있고 미리보기가 전혀 없었음).
- **검증**: `npx tsc --noEmit`(0 errors)/`npm run lint`(0 errors, 새 warning 없음) 통과. 사이드바 아이콘 두 수정은 dev 서버에서 직접 확인(위 내용 참고). `/admin/navigation/settings`는 관리자 로그인 세션이 필요해 에이전트가 실제 업로드/미리보기 렌더링을 클릭 테스트하지 못했다(비로그인 리다이렉트, 콘솔/서버 컴파일 에러 없음까지만 확인) — **사용자가 로그인 후 여백 배경 이미지를 실제로 새로 업로드해 압축 품질(%) 조절이 반영되는지, 슬라이드/여백 이미지 미리보기 썸네일이 제대로 뜨는지 직접 확인 필요.**

## 2026-07-31 (EPIC-078 후속 핫픽스 — 사이드바 아이콘 hover 영상 PiP 버튼 + 흰 사각형)
- **PiP(그림 속 그림) 버튼 노출**: 사이드바 여닫이 아이콘에 커서를 올리면(EPIC-078의 호버 크로스페이드가 `<video>`를 노출) 크롬이 자동으로 "화면 속 화면" 미니 버튼을 띄우는 문제 — `LeftSidebar.tsx`/`RightSidebar.tsx`의 `<video>`에 `disablePictureInPicture`/`disableRemotePlayback` 속성 추가로 해결.
- **hover 영상 주위 흰(불투명) 사각형**: 위 수정 후에도 호버 시 아이콘 주위에 사각형이 남아있다는 후속 피드백 — 처음엔 "아이콘 이미지 자체에 배경색이 박혀있다"고 오판했으나, 실제 파일(art nouveau 백합/빈티지 열쇠 PNG·WebM)을 직접 열어 픽셀 단위로 확인한 결과 원본은 전부 정상적으로 완전 투명이었음. 진짜 원인은 알파 채널이 있는 WebM(VP8/VP9 `alpha_mode: 1`)을 `<video>` 태그로 화면에 직접 그리면, 일부 브라우저/GPU 가속 디코딩 경로가 알파를 무시하고 불투명하게 합성하는 알려진 한계였다 — `<canvas>` 2D `drawImage`로 직접 그렸을 때는 알파가 항상 정확히 보존됨을 스크립트로 확인. **수정**: `<video>`는 화면에 노출하지 않는 숨김 프레임 소스로만 두고, `requestVideoFrameCallback`(폴백: `requestAnimationFrame`)으로 매 프레임을 `<canvas>`에 그려 투명도를 브라우저/GPU에 무관하게 보장 — `LeftSidebar.tsx`/`RightSidebar.tsx`에 완전히 중복돼 있던 `isVideoUrl`/`SidebarTriggerMedia`를 신규 공용 컴포넌트 `src/components/SidebarTriggerMedia.tsx`로 추출해 양쪽에서 재사용하도록 정리.
- **검증**: `npx tsc --noEmit`(0 errors)/`npm run lint`(0 errors, 변경 파일 관련 새 warning 없음) 통과. dev 서버에서 실제 렌더링 확인 — hover 영상 슬롯이 `<video>`(숨김, 1x1px) + `<canvas>`(각 원본 영상의 실제 해상도 544×678/720×1280과 정확히 일치)로 구성되어 프레임이 정상적으로 그려지고 있음을 확인. Supabase Storage에 처리된 이미지를 재업로드하는 방법도 검토했으나 anon key로는 RLS(403)에 막혀 애초에 불가능했고, 원본 자체는 문제가 없었으므로 재업로드가 필요 없었다.

## 2026-07-31 (EPIC-078)
- **EPIC-078: 사이드바 아이콘 기본/호버 듀얼 미디어 업로드 + 순수 크로스페이드 전환** — "EPIC-076의 인위적인 CSS 3D/회전 호버 애니메이션을 전부 제거하고, 관리자가 기본(Default)/호버(Hover) 미디어를 각각 독립적으로 업로드해 실제 사이트에서 커서를 올리면 두 미디어가 부드럽게 크로스페이드되도록 해달라"는 요청.
  - **스키마 확장**: `site_settings.sidebar_icons`의 `leftIconUrl`/`rightIconUrl`(단일 URL)을 `leftIconDefaultUrl`/`leftIconHoverUrl`/`rightIconDefaultUrl`/`rightIconHoverUrl`(기본/호버 각각) 4개 필드로 확장. 구버전 단일 URL 데이터는 `Navbar.tsx`/`admin/navigation/settings/page.tsx` 양쪽 로드 시 `xxxDefaultUrl`로 1회 폴백(다음 저장부터 새 필드로 자연스럽게 이전, 마이그레이션 불필요).
  - **관리자 UI**: "사이드바 아이콘" 섹션을 좌/우 x 기본/호버 4개의 업로드 슬롯(`SidebarIconUploadField`, 미리보기+URL 텍스트+파일 업로드 공용 컴포넌트)으로 재구성, 파일 입력 `accept`에 이미지뿐 아니라 `.webm`/`.mp4`(투명 비디오)도 허용 — 기존 `uploadImage()` 헬퍼가 파일 종류를 가리지 않아 별도 업로드 로직 없이 그대로 재사용.
  - **렌더링(`LeftSidebar.tsx`/`RightSidebar.tsx`)**: EPIC-076의 아르누보 백합 개화(`silo-stem-sway`/`silo-leaf-sway-*` keyframe, `globals.css`에서 완전 삭제)/양문형 문 3D 회전(`rotateY`, `[perspective:700px]`) 마크업을 전부 제거. 대신 기본/호버 미디어를 `absolute inset-0`로 같은 자리에 겹쳐 그리고 `opacity-100 group-hover:opacity-0` / `opacity-0 group-hover:opacity-100` + `transition-opacity duration-300`로 크로스페이드 — 확장자가 `.webm`/`.mp4`면 `<video autoPlay loop muted playsInline>`, 아니면 `<img>`로 조건부 렌더링(`SidebarTriggerMedia` 공용 컴포넌트, 양쪽 파일에 동일 패턴 복제). 호버 미디어가 비어 있으면 기본 미디어로 폴백해(둘 다 같은 이미지) 크로스페이드해도 아이콘이 사라지지 않는다. 트리거 버튼은 `bg-transparent` 고정.
  - **EPIC-076 배경색 커스텀 기능과의 충돌**: "배경 항상 완전 투명" 요구사항이 EPIC-076의 "아이콘 배경색 커스텀(관리자가 transparent/#HEX 지정)" 기능과 정면으로 충돌 — 사용자 확인 결과 **설정 자체(DB 필드/관리자 UI)는 남기고 트리거 버튼에 시각적으로 적용하는 것만 중단**하기로 결정. `iconBackgroundColor` prop은 `LeftSidebar`/`RightSidebar`에서 제거하고 `Navbar.tsx`도 더 이상 그 prop을 넘기지 않지만, `site_settings.sidebar_icons.backgroundColor`와 admin 설정 화면의 입력 필드는 그대로 유지(저장은 되지만 화면에는 반영 안 됨).
  - **검증**: `npx tsc --noEmit`(0 errors)/`npm run lint`(0 errors, 새 warning 없음 — EPIC-077 이후 baseline과 동일)/`npm run build`(전체 라우트 정상 컴파일) 통과. Browser pane으로 (1) 라이브 DB에 남아있던 구버전 `leftIconUrl` 데이터가 실제로 `leftIconDefaultUrl`로 정상 폴백되어 렌더링됨을 확인(하위 호환 검증), (2) 트리거 버튼 `background-color`가 `rgba(0,0,0,0)`(완전 투명)임을 계산된 스타일로 확인, (3) 컴파일된 CSS에서 `group-hover:opacity-0`/`group-hover:opacity-100` 규칙이 정상 생성되고 구버전 `rotateY`/`silo-stem-sway` 규칙은 완전히 사라졌음을 직접 확인. **실제 마우스 호버로 크로스페이드가 눈에 보이는지는 이 세션의 Browser pane이 `(hover: hover)` 미디어 쿼리를 만족하지 않는 환경(`:hover` 의사 클래스는 CDP로 강제 매칭되지만 `@media (hover: hover)`는 통과 못함)이라 확인 불가 — EPIC-076 때도 동일한 한계로 컴파일된 CSS 검증까지만 했음. 사용자가 실제 브라우저에서 커서를 올려 크로스페이드가 자연스럽게 보이는지 직접 확인 필요.**

## 2026-07-31 (EPIC-077)
- **EPIC-077: 사이드바 트리거 모드 설정(호버/클릭) + "사이트 구성 관리" 통합 CMS 개편** — "EPIC-076 호버 애니메이션을 감상할 새 없이 사이드바가 바로 열린다, 관리자가 트리거 모드를 고를 수 있게 해달라"는 요청과, "메뉴/카테고리·페이지·게시판 관리 3개 화면이 분리돼 있어 카테고리 계층 수정이 사이트 전반에 통합 적용되지 않는다"는 요청. 3개 커밋으로 나눠 진행.
  - **1/3 — 사이드바 트리거 모드**: `site_settings.sidebar_icons`에 `triggerMode: 'click' | 'hover'`(기본 `click`, DB 마이그레이션 불필요) 추가. `/admin/navigation/settings`의 "사이드바 아이콘" 섹션에 select 컨트롤 추가. `LeftSidebar.tsx`/`RightSidebar.tsx` 트리거 버튼의 `onMouseEnter={onIconClick}`을 `onMouseEnter={triggerMode === "hover" ? onIconClick : undefined}`로 조건부 바인딩 — `onClick`은 그대로 유지. 아르누보 호버 애니메이션(EPIC-076)은 이미 `group-hover:` CSS만으로 동작해 JS 상태와 무관하므로 마크업 변경 없이 그대로 재생된다. Browser pane(비로그인)으로 기본값 `triggerMode: 'click'`에서 호버해도 패널이 안 열리고(`aria-hidden="true"`) 클릭하면 열림(`aria-hidden="false"`)을 직접 확인.
  - **2/3 — `boards.topic`/`boards.thumbnail_url` 신규 컬럼**: `docs/sql/EPIC-077-board-topic-thumbnail.sql` 신설(라이브 DB 미적용, 사용자가 직접 실행 필요 — NEXT_TASK.md 참고), `docs/database-schema.sql` 동기화. `boardLayout.ts`에 `BOARD_RICH_FIELDS_EXT`(EPIC-066 `BOARD_RICH_FIELDS` + 두 신규 필드) 추가해 `/api/admin/boards`·`/api/admin/boards/[id]` GET을 EXT→RICH→LEGACY 3단 폴백으로 확장(마이그레이션 전에도 기존 게시판 관리 기능은 그대로 동작), PATCH `EDITABLE_FIELDS`에 두 필드 추가. 다음 커밋에서 "사이트 구성 관리" 통합 화면이 이 필드들을 실제로 편집한다.
  - **3/3 — 통합 "사이트 구성 관리"(`/admin/site-structure`) 신설 + 기존 3개 관리 화면 리다이렉트**: 기존 `CategoryTreeManager.tsx`(site_navigations 드래그앤드롭 트리, EPIC-035)를 그대로 재사용해 상단 탭/왼쪽 사이드바/오른쪽 사이드바 3개 트리를 이 화면에 배치. "관리" 모달(`CategoryDetailModal`)에 (1) 노출 위치(`target_type`, 루트 노드에서만 편집 가능 — `addChild`가 하위로 그대로 전파하고 각 트리 인스턴스가 `targetTypes`로 범위를 필터링하므로 하위 단독 변경은 구조적으로 비일관), (2) 연결된 페이지(`hrefToSlug(href)`로 `page_builder` 매칭, 위젯 편집 링크), (3) 연결된 게시판(`adminTreeGrouping.ts`에 신설한 `buildBranchToBoardIds`로 `fetchBoardBranchMap` 역매핑, 주제/대표이미지/소개/공개여부를 `/api/admin/boards/[id]` PATCH로 인라인 저장, 그 외 필드는 상세 편집기 링크)을 추가. `site_navigations` 트리에 href로 안 걸린 페이지/게시판이 통합 후에도 사라지지 않도록 화면 하단에 "미분류" 목록 + 드래그 재정렬(`sort_order` PATCH) 패널도 추가. `/admin/navigation`·`/admin/pages`·`/admin/boards` 목록 화면을 각각 `redirect("/admin/site-structure")` 서버 컴포넌트로 교체(상세 편집기 `/admin/pages/[id]`·`/admin/boards/[id]`·`/admin/boards/new`는 그대로 유지, 그 안의 "목록으로" 버튼도 새 경로로 갱신). `admin/navigation/layout.tsx`의 서브 탭을 "홈페이지 설정 관리" 하나로 축소, `admin/layout.tsx`의 `ADMIN_NAV`를 "사이트 구성 관리" 1개 + "홈페이지 설정 관리"(최상위로 승격, 안 그러면 접근 불가) 조합으로 갱신.
  - **검증(전체)**: `npx tsc --noEmit`(0 errors)/`npm run lint`(0 errors, 새 `react-hooks/set-state-in-effect` warning 소수 추가 — 이 저장소 전반에 이미 20여 곳 존재하는 동일 계열의 non-blocking 경고와 같은 패턴)/`npm run build`(전체 라우트 정상 컴파일, `/admin/site-structure` 포함) 통과. Browser pane(비로그인)으로 `/admin/navigation` 접속 시 `/admin/site-structure`를 거쳐 미인증 리다이렉트(`/`)까지 정상 동작함을 확인. **관리자 로그인 세션이 필요한 부분(트리 드래그 재부모화, 관리 모달의 페이지/게시판 연결, 미분류 패널 드래그)은 에이전트가 클릭 테스트하지 못함 — NEXT_TASK.md에 사용자 확인 항목으로 등록.**
  - **후속 수정(같은 날, 사용자 피드백 반영)**: 관리자 세션(임시 테스트 계정, 아래 참고)으로 직접 확인해보니 두 가지가 실제 요구와 달랐다 — (1) 카테고리 행의 "수정"은 원래도 site_navigations 필드(제목/href/순서/활성화)만 인라인으로 고치는 용도였는데, 페이지 자체를 고치는 진입점이 없었다 → `CategoryTreeManager.tsx`의 `load()`에서 `page_builder`를 함께 조회해 `pageIdBySlug` 맵을 만들고, 매칭되는 페이지가 있으면 행에 "페이지 수정" 버튼(→ `/admin/pages/[id]`)을 새로 노출. (2) "연결된 게시판"이 `fetchBoardBranchMap`의 "추정 매칭" 결과를 속성 편집만 가능하게 보여줬는데, 사용자는 **어느 게시판을 연결할지 직접 선택/변경/해제**할 수 있어야 한다고 요구 → `CategoryDetailModal`을 재작성해 연결을 `page_modules`(module_type='board', board_id) 행 하나로 직접 다룬다: 전체 게시판 드롭다운(공개 읽기 RLS라 세션 없이 조회) + "연결 저장"이 있으면 update, 없으면 insert(`WIDGET_DEFAULT_SETTINGS.board`), "(연결 안 함)"이면 delete. `branchToBoardIds`/`buildBranchToBoardIds`(직전 커밋에서 막 추가했던 것)는 이 방식으로 완전히 대체되어 제거. **검증**: `tsc`/`lint`(0 errors)/`build` 통과. 임시 테스트 계정(`epic077-test-20260731@silostore.net`)을 만들어 사용자 제공 Management API 토큰으로 `is_admin`을 켜고 실제 로그인 세션으로 (a) "페이지 수정" 버튼이 매칭되는 행에만 뜨는지, (b) "스튜디오" 노드에서 게시판을 선택→"연결 저장"→DB에 `page_modules` 행이 실제로 생기는지(Management API로 직접 조회 확인), (c) "(연결 안 함)"으로 다시 저장→그 행이 실제로 삭제되는지(재조회로 확인)까지 전부 실제 DB write까지 검증 후 테스트 데이터 정리, `is_admin` 원복.

## 2026-07-30 (EPIC-076)
- **EPIC-076: 사이드바 아이콘 커스텀 배경색 + 아르누보 호버 모션 애니메이션** — "좌/우 사이드바 여닫이 아이콘 주변의 하드코딩된 초록색 박스가 밋밋하다, 배경색을 자유롭게 지정하고 앤틱/아르누보 감성의 고급스러운 호버 모션을 넣어달라"는 요청.
  - **배경색 커스텀**: `site_settings.sidebar_icons`에 `backgroundColor` 필드 추가(기본값 `#166534` — 기존 하드코딩 `bg-green-800`과 동일해 미설정 시 기존 화면 그대로). `/admin/navigation/settings`에 transparent/#HEX 자유 입력 텍스트 필드(+ #HEX일 때만 참고용 컬러 picker 병행 노출) 추가. `LeftSidebar.tsx`/`RightSidebar.tsx` 트리거 버튼의 `bg-green-800` 클래스를 제거하고 `style={{ backgroundColor: iconBackgroundColor }}`로 동적 바인딩(사이드바 패널 자체의 초록 배경은 이번 범위 밖이라 그대로 둠).
  - **오른쪽 사이드바 — 아르누보 양문형 문 3D 개폐**: 트리거 버튼에 붉은 문 두 짝(`bg-gradient-to-r/l from-red-950 to-red-900`)을 절반씩 겹쳐 평상시엔 아이콘을 가리고, `group-hover`에서 각 문짝이 자신의 경첩(`origin-left`/`origin-right`)을 기준으로 `rotateY(∓108deg)`(버튼에 `[perspective:700px]`, `transition-transform duration-500 ease-in-out`)로 회전하며 열려 뒤의 아이콘을 드러낸다.
  - **왼쪽 사이드바 — 아르누보 백합 개화**: 아이콘 자체는 `group-hover:scale-110`(duration-700 ease-in-out)로 살짝 피어나듯 확대되고, 아이콘 주변에 순수 CSS로 그린 줄기/잎사귀 장식 요소가 `globals.css`에 신설한 keyframe(`silo-stem-sway`/`silo-leaf-sway-left`/`silo-leaf-sway-right`, 각 transform-origin이 뿌리 쪽에 고정돼 회전만 해도 위치가 어긋나지 않음)으로 바람에 살랑이듯 회전 — 영상/GIF 없이 CSS `transform`만 사용해 렌더링 비용 없음.
  - **검증**: `npx tsc --noEmit`(0 errors)/`npm run lint`(0 errors, 28 warnings — 기존 baseline과 동일, 변경 파일 무관 확인) 통과. 로컬 dev 서버에서 컴파일된 CSS를 직접 fetch해 `rotateY(-108deg)`/`rotateY(108deg)`/`silo-stem-sway`/`silo-leaf-sway-left`/`silo-leaf-sway-right`/`group-hover:scale-110` 규칙이 실제로 `:where(.group):hover` 스코프로 생성됨을 확인, 트리거 버튼의 `backgroundColor` 인라인 스타일이 기본값 `rgb(22, 101, 52)`(#166534)로 정상 적용됨을 확인(Browser pane 미표시 환경이라 스크린샷 대신 컴파일된 CSS·계산된 스타일로 검증).

## 2026-07-30 (EPIC-075)
- **EPIC-075: 관리자 CMS 트리 드래그앤드롭 + Community 게시판 1-Row 컴팩트 목록 + 게시글 상세 하단 글 목록** — "EPIC-072 트리 UI에서 순서가 고정돼 불편하다, 목록형 게시판 가독성과 상세 화면 탐색성을 개선해달라"는 요청.
  - **관리자 트리 드래그앤드롭(`/admin/pages`, `/admin/boards`)**: `docs/sql/EPIC-075-tree-sort-order.sql` 신설 — `page_builder`/`boards`에 `sort_order` 컬럼 추가(라이브 DB에는 아직 미적용, 컬럼 없어도 기존 정렬로 조용히 폴백하도록 `fetchAllPagesForAdmin`/`/api/admin/boards` GET에 42703 폴백 추가). `@dnd-kit/core`+`sortable`(CategoryTreeManager와 동일 라이브러리, 이미 의존성 있음)로 각 화면에 드래그 핸들(⠿)을 추가하고, **같은 브랜치(형제 그룹) 안에서의 순서 변경만** 지원한다 — 다른 브랜치로 옮기는 재분류는 이미 `/admin/navigation`(CategoryTreeManager)이 site_navigations를 직접 편집해 처리하는 영역이라 여기서 중복 구현하지 않았고, `/admin/posts`는 게시글이 본질적으로 시간순 콘텐츠라 드래그 순서 변경 자체를 추가하지 않았다(코드 주석으로 이 스코프 결정을 명시). `src/lib/adminTreeSections.ts` 신설 — `buildAdminTree`의 평평한 행 배열에서 "브랜치 헤더 다음에 오는 연속된 항목들"을 형제 그룹으로 복원하는 공용 헬퍼(두 화면이 공유).
  - **`CommunityListModule.tsx` 1-Row 컴팩트 목록**: 제목/메타/태그 3줄이던 마크업을 `[글제목] | [작성자] | [좋아요] | [댓글] | [조회] | [작성날짜] | [태그]`가 한 줄에 들어오는 반응형 flex row로 전면 재작성 — 좁은 화면에서는 덜 중요한 컬럼(태그→조회→좋아요/댓글→작성자 순)을 차례로 숨겨 줄바꿈 없이 항상 한 줄을 유지한다. 태그는 상세 페이지용 `PostTags`(줄바꿈 전제) 대신 최대 2개 + "+N" 표시하는 축소 칩을 직접 그렸다. 목록형 레이아웃 취지상 썸네일(photo_url)은 제거(어떤 호출부도 기대하지 않던 필드).
  - **게시글 상세(`/boards/[id]/[postId]`) 하단 "이 게시판의 다른 글"**: `src/components/boards/BoardPostListPanel.tsx` 신설 — 기존 `GET /api/boards/[id]/posts` API를 그대로 재사용해 같은 게시판의 글 목록을 댓글 영역 아래에 항상 렌더링, 현재 읽고 있는 글은 배경색+테두리로 하이라이트(▶ 접두어). `BoardModule` 전체(검색/정렬/헤더 포함)를 다시 넣으면 상세 화면 안에 또 다른 게시판 화면이 통째로 얹히는 셈이라 무거워서, 목록+하이라이트만 담당하는 가벼운 전용 컴포넌트로 새로 만들었다.
  - **검증**: `npx tsc --noEmit`(0 errors)/`npm run build`(전체 라우트 정상 컴파일)/`npm run lint`(0 errors, 28 warnings — 기존 27개 baseline + `BoardPostListPanel.tsx`의 `react-hooks/set-state-in-effect` 1건, 이미 이 저장소 전반에 20여 곳 존재하는 동일 계열의 non-blocking 경고와 같은 패턴이라 신규 위험 아님) 통과. 로컬 dev 서버로 `/community/general`(1-Row 목록, 반응형 컬럼 숨김 확인)과 게시글 상세 페이지(하단 패널 렌더링 + 현재 글 하이라이트) 직접 확인, 콘솔 에러 없음.
  - **미실행 SQL**: `docs/sql/EPIC-075-tree-sort-order.sql`을 Supabase SQL Editor에서 실행해야 `/admin/pages`·`/admin/boards`의 드래그앤드롭 순서가 새로고침 후에도 유지된다(실행 전에는 컬럼이 없어 기존 정렬로 폴백하므로 드래그는 되지만 저장은 안 됨).

## 2026-07-30 (EPIC-073)
- **EPIC-073: Documentation Architecture Refactoring — `PROJECT_BLUEPRINT.md`를 인덱스로 분리** — "문서가 커져서 Blueprint 하나에 모든 정보가 있으면 안 된다, 정보를 잃거나 요약하지 말고 재구성만 하라"는 요청. `PROJECT_BLUEPRINT.md`(272줄)의 전체 내용을 9개 새 문서(`PROJECT_VISION.md`/`PROJECT_ARCHITECTURE.md`/`PROJECT_RULES.md`/`BOARD_SYSTEM.md`/`PAGE_BUILDER.md`/`WIDGET_SYSTEM.md`/`MEMBERSHIP_SYSTEM.md`/`POINT_SYSTEM.md`/`NAVIGATION_SYSTEM.md`, 전부 루트)로 문장 단위 그대로 옮기고, `PROJECT_BLUEPRINT.md` 자신은 Project Name/Current Stage/Current Epic/전체 문서 인덱스만 남는 인덱스 전용 문서로 재작성. 기존 `docs/VISION.md`(EPIC-074)는 `PROJECT_VISION.md`로 이동(중복 방지, `CLAUDE.md`/`docs/PROJECT_DASHBOARD.md`의 참조 링크도 함께 갱신). `docs/{navigation,membership,content}-blueprint.md`/`docs/design-system.md`/`docs/EPIC.md`/`docs/git-sync.md`/`docs/STAGES.md`/`docs/PROJECT_DASHBOARD.md`는 이번 재구성 대상이 아니라 그대로 둠(각 신규 문서에서 상세 SSoT로 교차 링크). 코드/DB 변경 없음, 문서 전용 — 세션 내 최종 보고에서 "중복 발견 사항"(BOARD_SYSTEM.md/MEMBERSHIP_SYSTEM.md/NAVIGATION_SYSTEM.md가 기존 상세 Blueprint와 개념적으로 겹침, EPIC-073 번호가 이전 세션의 STAGES.md 재작성 작업과 중복 사용됨 등)을 사용자에게 보고함.

## 2026-07-30 (EPIC-072B)
- **EPIC-072B: 관리자 CMS UX 개편 — `site_navigations` 실제 트리 기반 계층형(Branch) 뷰** — EPIC-072가 만든 "4대 도메인" 분류는 키워드/휴리스틱 추정(`adminDomainGrouping.ts`)이라 평면 목록을 도메인별로 묶는 정도였는데, 이번엔 "`메뉴/카테고리 관리`처럼 상위-하위가 가지 형태로 들여쓰기된 트리"를 원하는 요청 — 실제 `site_navigations` 트리 구조 그 자체를 SSoT로 써서 각 항목이 정확히 어느 가지에 속하는지 구조적으로 매칭한다(추정이 아님).
  - **`src/lib/adminTreeGrouping.ts` 신설**: `fetchNavBranches()`가 `site_navigations`를 depth-first로 펼쳐 `{id, title, href, slug(=hrefToSlug(href)), depth, parentId, domain}` 배열로 반환(최상위 4개 루트 행의 실제 id로 도메인을 정확히 판별 — 문자열 추정 아님, "사일로 상점"/"살롱데상"/"스튜디오"/"마이 페이지" 4개뿐이라 완전 매칭). 페이지는 `hrefToSlug(nav.href) === page.slug` 완전 일치로 매칭(138개 기존 slug가 전부 이 규칙으로 생성됨, `pageTemplates.ts` 참고). 게시판은 `page_modules`에서 그 board_id를 참조하는 페이지를 찾아 그 페이지의 slug로 다시 매칭 — 한 게시판이 여러 페이지에 걸쳐 있으면(예: "예술 클럽" 게시판은 자기 전용 페이지 `community-topics-art`의 `board` 위젯뿐 아니라 상위 hub `community`/`community-topics`의 `slide` 미리보기에도 등장) `module_type==="board"`(그 게시판의 진짜 전용 페이지)를 최우선으로, 그마저 여럿이면 더 긴(더 구체적인) slug를 택한다. 게시글은 별도 매칭 없이 소속 게시판의 브랜치를 그대로 물려받는다. 매칭 안 되는 항목은 전부 "기타 / 미분류"로 모아 누락 없이 보여준다.
  - **`/admin/pages`/`/admin/boards`/`/admin/posts/*`**: 평면 목록/테이블을 브랜치 헤더(📂, `depth*20px` 들여쓰기) + 그 아래 항목(📄)으로 재구성 — 기존 수정/삭제/공개전환/복제 등 액션 버튼은 전부 그대로 유지. 도메인 필터 탭(`AdminDomainTabs`, EPIC-072)은 그대로 재사용하되 판별 기준을 실제 브랜치 매칭으로 교체.
  - **한계**: `/admin/posts/*`는 여전히 "도메인 전체 최신순 20건" 서버 페이지네이션이라, 한 페이지 안의 트리는 그 페이지에 걸린 게시글의 브랜치만 보여준다(모든 브랜치를 매 페이지 완전하게 보여주진 않음) — 브랜치별 완전한 페이지네이션은 범위 밖, 후속 과제로 남김.
  - **검증**: `npx tsc --noEmit`(0 errors)/`npm run build`(전체 라우트 정상 컴파일)/`npm run lint`(0 errors, 27 warnings — pull 직후 baseline과 동일, 신규 파일·수정 파일 전부 무관 확인) 통과. `page_modules(board_id, page_builder(slug))` PostgREST 임베디드 조회 문법을 실제 라이브 DB에 직접 curl로 검증(정상 동작, 다중 참조 케이스도 실제로 확인). 관리자 로그인 세션이 필요해(에이전트 자격 증명 직접 입력 금지 정책) 화면 클릭 테스트는 사용자가 직접 확인 필요.

## 2026-07-30 (EPIC-074B)
- **EPIC-074B: `docs/PROJECT_DASHBOARD.md` 최상단에 PROJECT VISION/Roadmap/Current EPIC/Next EPIC 블록 추가** — 사용자가 내용을 직접 지정(Roadmap: Stage 1 95%, Stage 2 Ready, Stage 3~8 Waiting / Current EPIC: EPIC-074B / Next EPIC: EPIC-075), 이 블록을 문서 최상단에 항상 유지하는 규칙으로 반영. 기존 문서 하단부에 있던 구식 "## Current EPIC"/"## Next EPIC" 섹션(값이 오래전부터 "없음"/"미정"으로 방치돼 새 최상단 블록과 모순되던 상태)은 제거하고, 최상단 블록이 SSoT임을 명시하는 안내 문구로 대체 — 나머지 Progress/Recent Completed/Known Issues 등 섹션은 이번 범위 밖이라 손대지 않음(여전히 EPIC-068 시점 기준으로 stale, 별도 정리 필요). 코드 변경 없음(문서 전용).

## 2026-07-30 (EPIC-074)
- **EPIC-074: `docs/VISION.md` 신설 — "왜 Silo Platform을 만드는가"** — 사용자가 전체 내용을 직접 작성해 그대로 반영(Instagram/네이버 블로그로는 콘텐츠 아카이빙·연결·회원제/큐레이션/도슨트 시스템 구현이 안 된다는 배경, 핵심 가치 6개: Archive First/Story First/Community First/Collection First/Long-term Preservation/Timeless Design). `CLAUDE.md`의 "세션 시작 시 읽기 순서"에 1번(가장 먼저 읽는 문서)으로 추가하고 Blueprint 문서 목록에도 등록 — 앞으로 모든 세션·EPIC이 이 Vision을 기준으로 판단하도록 함. `docs/STAGES.md`의 Stage 개수 언급(6→8)도 EPIC-073 반영 누락분 함께 정정. 코드 변경 없음(문서 전용).

## 2026-07-30 (EPIC-073)
- **EPIC-073: `docs/STAGES.md`를 CTO Roadmap 기준으로 전면 재작성** — 사용자가 8단계(Foundation → Content → Creator → Community → Business → Experience → AI → Scale) 구조를 직접 지정, 기존의 EPIC별 상세 진행률 표 형식은 유지하지 않고 전달받은 구조 그대로 교체. 문서 마지막에 CURRENT STAGE/CURRENT EPIC/NEXT EPIC 블록을 항상 유지하는 규칙 반영(현재: Stage 1 — Foundation, EPIC-072 Board Management System, 다음: EPIC-073 Content Platform). 코드 변경 없음(문서 전용).

## 2026-07-30 (EPIC-072)
- **EPIC-072: 관리자 CMS(페이지/게시판/전체 글 관리)를 4대 도메인 기준으로 재구성** — "페이지 관리/게시판 관리/전체 글 관리가 분류가 안 되어있다, 메뉴/카테고리 관리처럼 사일로상점/살롱데상/스튜디오/마이페이지로 나뉘면 좋겠다"는 요청.
  - **`src/lib/adminDomainGrouping.ts` 신설**: `boards`/`page_builder`에 "도메인" 컬럼이 없어(boards.group_key는 9종 세분화 허브 키, page_builder는 아예 없음), (1) `group_key`가 있으면 그걸 4대 도메인으로 승격하고 (2) 없으면(게시판 56개 중 52개) `category`/`slug` 키워드 테이블로 폴백하는 최선 추정 분류기. 실제 라이브 데이터(`page_builder.slug` 127개, `boards.category` 56개)를 직접 조회해 테이블을 작성 — 검증 결과 페이지 94%(119/127), 게시판 96%(54/56)가 의미 있는 도메인으로 분류되고 나머지는 로그인/설정 같은 진짜 전역 페이지이거나 category 자체가 없는 게시판(정확히 "공통/기타"가 맞는 케이스).
  - **`src/components/admin/AdminDomainTabs.tsx` 신설**: 전체/사일로상점/살롱데상/스튜디오/마이페이지/공통 6개 탭, `?domain=` 쿼리 파라미터로 상태 저장(새로고침·링크 공유 가능) — `useSearchParams`를 쓰므로 호출부는 Suspense로 감싸야 함(이 저장소의 기존 관례, Navbar.tsx/docent page와 동일).
  - **`/admin/pages`**: 도메인 탭 추가, "전체" 선택 시 도메인별 섹션으로 묶어서(브랜치 정렬처럼) 표시, 특정 도메인 선택 시 그 목록만 평평하게 표시.
  - **`/admin/boards`**: 도메인 탭 추가(같은 그룹핑 방식), 기존 Category(`group_key`)/Board Type(`render_type`) 드롭다운 필터와 검색은 그대로 유지되어 도메인 탭과 함께 동작.
  - **`/admin/posts`("전체 글 관리")**: 기존에 이미 [사일로상점]/[살롱데상] 2-Depth 서브 탭이 있었으나(EPIC-025) 스튜디오/마이페이지가 빠져있었고, 살롱데상 탭도 도메인 필터 없이 게시판이 있는 글 전부를 보여주고 있었다(엄밀히 살롱데상 전용이 아니었음). `src/components/admin/AdminPostsBoardView.tsx`로 도메인 매개변수화해 일반화 — boards 전체를 먼저 분류해 해당 도메인 board_id 목록으로 posts를 필터링하는 2단계 조회. `/admin/posts/silostore`(신규, 도슨트 시대별·헤리티지 게시글)·`/admin/posts/studio`·`/admin/posts/mypage`(신규, 현재는 연결된 게시판이 없어 정직하게 빈 목록) 추가, `/admin/posts/salon`은 이 컴포넌트로 교체(이제 실제로 살롱데상 게시판만). `/admin/posts/shop`(물품 관리, `items` 테이블)은 게시글과 다른 데이터라 손대지 않고 그대로 둠.
  - **검증**: `npx tsc --noEmit`/`npm run lint`(0 errors, 27 warnings — 기존 baseline과 동일)/`npm run build`(신규 라우트 5개 전부 정상 컴파일) 통과. 로컬에서 모든 신규 경로가 미인증 시 정상적으로 `/`로 리다이렉트되고 콘솔 에러 없음을 확인 — 실제 화면 클릭 테스트는 관리자 로그인 세션이 필요해(에이전트가 자격 증명 직접 입력 금지 정책) 사용자가 로그인 후 직접 확인 필요.
  - **한계**: 도메인 분류는 슬러그/카테고리 문자열 기반 최선 추정이지 DB 정본이 아니다 — 새 페이지/게시판을 추가했는데 엉뚱한 도메인(또는 "공통/기타")으로 분류되면 `src/lib/adminDomainGrouping.ts`의 테이블에 추가해야 한다.

## 2026-07-30 (EPIC-071-HOTFIX)
- **긴급: EPIC-071이 추가한 `members` RLS 정책이 무한 재귀를 유발해 사이트 전체가 깨짐 → 즉시 롤백 + 안전한 방식으로 재구현.** 사용자가 "사일로상점 하위 페이지는 '배치된 모듈이 없어요', 페이지 수정 버튼도 사라짐, 살롱데상 하위 페이지는 '페이지를 찾을 수 없어요'"라고 실제 장애를 보고.
  - **원인**: `members` 테이블 자체에 추가한 admin-bypass 정책(`members_admin_select`/`update`)이 `exists (select 1 from members where ...)`로 **자기 자신을 서브쿼리**하는 구조 — `CLAUDE.md`에 이미 기록돼 있던 gotcha(정책 대상과 같은 테이블을 서브쿼리하면 Postgres 무한 재귀 `42P17`)를 그대로 재발시킨 것. `members`는 모든 로그인 확인(`getRequestMember`)에서 조회되고, `page_builder`/`page_modules`의 공개 읽기 정책도 내부적으로 `members`를 서브쿼리해 관리자 여부를 확인하므로(EPIC-060/061), `members` 조회 자체가 깨지자 로그인 확인·페이지 조회·"페이지 수정" 버튼 표시(관리자 판정)가 전부 도미노로 무너졌다.
  - **긴급 롤백**: 사용자가 `drop policy if exists "members_admin_select"/"members_admin_update" on members;` 즉시 실행 — 사이트 정상 복구 확인(로컬 dev 서버로 라이브 DB 직접 확인 + 실제 dev.silostore.net을 Vercel Deployment Protection Bypass Secret으로 `curl` 검증, 둘 다 정상).
  - **안전한 재구현**: `docs/sql/EPIC-071-HOTFIX-member-admin-rls.sql` — `is_current_user_admin()`이라는 `SECURITY DEFINER` SQL 함수(`search_path` 명시, `stable`)를 만들어, 정책이 `members`를 직접 서브쿼리하는 대신 이 함수를 호출하도록 변경. `SECURITY DEFINER` 함수 내부 쿼리는 함수 소유자(테이블 소유자) 권한으로 실행되어 그 테이블의 RLS 자체를 타지 않으므로 재귀 고리가 원천적으로 생기지 않는다(Supabase/Postgres 공식 권장 패턴). **사용자가 Supabase SQL Editor에서 실행 완료(2026-07-30) — 사일로상점/살롱데상 하위 페이지 정상, `/admin/members` 전체 회원 조회/수정 정상 동작까지 직접 검증함(재귀 재발 없음).**
  - **재발 방지**: `CLAUDE.md`의 기존 RLS 재귀 gotcha 문단에 이번 재발 사례와 `SECURITY DEFINER` 함수 해법을 명시적으로 추가 — 앞으로 `members`(또는 다른 테이블)에 자기 참조 admin 체크가 필요하면 반드시 이 함수 패턴을 재사용하고, 절대 같은 테이블을 정책 안에서 직접 서브쿼리하지 않는다.

## 2026-07-30 (EPIC-071)
- **EPIC-071: 관리자 회원 관리 페이지(`/admin/members`) 신설** — "ethanki@silostore.net을 관리자로, 회원 정보를 보이고 수정하는 페이지를 만들어달라"는 요청.
  - **DB**: `docs/sql/EPIC-071-member-admin.sql` 작성 — (1) `ethanki@silostore.net` 관리자(`is_admin`) 승격, (2) `members` 테이블에 admin-bypass RLS(select/update) 정책 추가(기존 `orders`/`reservations`/`page_builder` 등과 동일한 패턴 — own-row 정책은 유지한 채 관리자용 정책만 추가, subquery가 자기 자신(`members`)을 참조하지만 호출자 본인 행은 기존 own-row 정책으로 즉시 해소돼 무한 재귀로 이어지지 않음). **사용자가 Supabase SQL Editor에서 직접 실행 완료(2026-07-30).**
  - **API**: `GET /api/admin/members`(검색어 `q` 지원, 관리자 전용), `PATCH /api/admin/members/[id]`(이름/등급/관리자 여부 수정, 관리자 전용) 신설 — 둘 다 `requester.scopedClient`로 호출해 위 RLS 정책이 실제로 걸리도록 함. 본인의 마지막 관리자 권한을 스스로 해제하는 요청은 400으로 차단(관리자가 0명이 되는 사고 방지).
  - **UI**: `/admin/members` — 이름/이메일/등급(드롭다운)/관리자 여부(체크박스)/가입일 표+검색, 행별로 값 바꾸고 "저장" 버튼(변경 없으면 비활성화). 관리자 상단 탭에 "회원 관리" 추가. 본인 관리자 해제 시 `window.confirm()` 재확인(기존 admin 페이지 대다수와 동일한 패턴).
  - **검증**: `npx tsc --noEmit`/`npm run lint`(0 errors)/`npm run build` 통과, `curl`로 미인증 요청이 403 반환하는 것 확인. 실제 화면 클릭 테스트는 관리자 로그인 세션이 필요해(에이전트가 자격 증명 직접 입력 금지 정책) 사용자가 SQL 실행 후 직접 확인.
  - **재발 방지(Git 워크플로우 갱신, `CLAUDE.md` 반영)**: 사용자 지시로 (1) `develop` push 시 `main`도 항상 자동 fast-forward+push(이전엔 명시적 요청 시에만), (2) 모든 commit에 `CHANGELOG.md`/`NEXT_TASK.md` 갱신을 동봉하는 걸 기본으로 함.

## 2026-07-30 (EPIC-070)
- **EPIC-070: 자유게시판 등 게시판 위젯이 dev.silostore.net에서만 전혀 안 뜨는 문제 — 원인 4단계를 순차로 확인/수정.** "커뮤니티 자유게시판 위젯이 설정대로 안 뜬다"는 보고로 시작 — 로컬(dev/`next start` 둘 다)에서는 동일 코드+동일 라이브 DB로 완벽히 재현돼 처음엔 렌더러 매핑 버그로 의심했으나, 실제로는 배포 환경에만 있는 문제였다.
  1. **jsdom(`isomorphic-dompurify`)이 Vercel 서버리스에서 라우트 모듈 로드 자체를 실패시킴**: `/api/boards/[id]/posts`, `.../[postId]`가 import하는 `isomorphic-dompurify`가 모듈 로드 즉시 `new JSDOM(...)`을 실행하는데, 그 하위 의존성 `html-encoding-sniffer@6 → @exodus/bytes`(순수 ESM)가 Vercel의 CJS `require()`와 충돌해 `ERR_REQUIRE_ESM`으로 전체 모듈 로드가 죽었다(Vercel Runtime Logs로 원문 확인, 사용자가 제공한 Deployment Protection Bypass Secret으로 `curl`로도 직접 재현). 파라미터와 무관하게(존재하지 않는 board id에도) 100% 500 — 로컬 `next start`는 서버리스 파일 트레이싱을 거치지 않아 재현이 안 됐다. `src/lib/sanitize.ts`를 DOM 불필요한 `sanitize-html`로 교체해 jsdom 의존 자체를 제거(허용 태그/속성 목록은 동일 유지, 로컬에서 XSS 차단·style/class 보존·iframe 임베드 전부 재검증).
  2. **API 응답 자체가 느림(1.4~2.4초)**: 두 라우트가 서로 의존관계 없는 Supabase 쿼리(게시판 조회/로그인 확인/등급 조회/게시글 조회/작성자 이름/댓글 수/조회수 증가/글 번호/좋아요·북마크 여부)를 전부 순차 `await`로 실행하고 있어 왕복 지연이 그대로 누적 — 독립적인 쿼리들을 `Promise.all`로 병렬화(권한 거부 시 posts/post 결과는 버리고 403 반환하도록 순서 재설계). 동일 페이지에 같은 게시판을 가리키는 위젯이 여러 개(Board+Gallery+Timeline)면 각자 fetch해 중복 요청도 발생하고 있어, `useBoardPosts.ts`에 "진행 중인 동일 요청(게시판+정렬+세션) 공유" 캐시를 추가.
  3. **Vercel Function Region(`iad1`, 미국)과 Supabase Project Region(`ap-northeast-2`, 서울) 불일치**: 병렬화해도 의존관계상 최소 3단계 순차 배치가 남는데, 각 배치가 트랜스퍼시픽 왕복이라 여전히 1.2~1.9초. 사용자가 Vercel Function Region을 Seoul(icn1)로 변경 — `X-Vercel-Id` 응답 헤더로 `icn1::iad1`(진입 edge::실행 region)에서 `icn1::icn1`로 바뀐 걸 확인, 응답시간 **1.9초 → 0.16~0.25초**.
  4. **재배포가 실제로 반영이 안 됨 — Vercel이 별도로 `npm run lint`를 실행해 exit 1이면 배포 자체를 막고 있었음**: Build Command는 override 안 된 기본값인데도(사용자가 Vercel 대시보드에서 직접 확인), 빌드 로그에 `bash -c npm run lint`가 별도 스텝으로 찍혀 있었다(Next.js 16은 `next build`가 lint를 아예 안 돌리므로 이건 Vercel 자체 게이트) — 그래서 1~3단계 수정 커밋들이 한동안 실제로는 한 번도 배포되지 못했을 가능성. 원인은 최근 `eslint-config-next` 업데이트로 함께 올라온 `react-hooks/set-state-in-effect`(신규 strict 규칙)가 이 저장소 전역의 흔한 패턴("effect 진입 시 loading 상태를 동기 setState 후 비동기 fetch 시작") 24곳을 error로 잡은 것 — 실제 버그가 아니라 과도하게 엄격한 신규 규칙이라 `eslint.config.mjs`에서 warning으로 낮춤(끄지 않아 계속 보이도록 유지, 점진적 정리 대상으로 `NEXT_TASK.md`에 등록). 같이 걸려있던 `@typescript-eslint/no-explicit-any` 5건(`api/admin/payments/{route,confirm/route}.ts`)은 실제 타입 이슈라 `any` 제거하고 정확한 타입으로 수정.
  - **부수**: `.claude/launch.json`에 프로덕션 빌드 로컬 진단용 `silo-prod`(포트 3001) 설정 추가. `fetchBoard`(게시판 rich/legacy 폴백 조회)가 두 라우트에 중복돼 있던 걸 `src/lib/boardFetch.ts`로 통합.
  - **재발 방지**: `CLAUDE.md`에 "Multi-Device Sync" 규칙 추가 — 회사/개인 컴퓨터를 번갈아 쓰는 워크플로우에서 로컬-원격 커밋 신구 관계를 착각하는 사고가 실제 있었음(`git merge-base --is-ancestor`로 실제 조상 관계 확인하는 습관으로 교정) — 세션 시작 시 clean하면 즉시 pull, 승인된 커밋은 세션 종료를 기다리지 않고 즉시 push.
  - **검증**: `npx tsc --noEmit`/`npm run lint`(0 errors, 26 warnings)/`npm run build` 전부 통과. `/community/general`(자유게시판+갤러리+타임라인), `/community/topics`(13개 게시판), 게시글 상세 페이지 로컬 재확인. 프로덕션은 Vercel Deployment Protection Bypass Secret으로 직접 `curl` 검증(응답 200, 실제 데이터, 응답시간 개선 수치까지 확인).

## 2026-07-30 (EPIC-069)
- **EPIC-069: Hub 페이지 Hero + 다중 Slide 위젯 고도화 + Orphan 페이지 2개 보강** — "그 Hub들 업데이트해줘, orphan 페이지 2개 고쳐줘" 요청. EPIC-068 백필 SQL 실행 이후 하위 카테고리에 실제 게시판/글이 생겼는데, `/docent`·`/community/topics`·`/heritage`·`/gallery`·`/archive`·`/membership` 6개 Hub는 여전히 하위 카테고리를 "application"(단순 버튼) 위젯으로만 나열해 실제 콘텐츠 미리보기가 Hub 화면에 전혀 보이지 않는 상태였다.
  - **`docs/sql/EPIC-069-hub-widget-upgrade.sql` 작성**(실행은 사용자가 Supabase SQL Editor에서 직접): 각 Hub의 application 버튼을 해당 게시판에 연결된 `slide`(Latest Posts Slider) 위젯으로 교체 — `/docent` 11개 시대, `/heritage` 2개(Grandmas/Grandpas), `/gallery` 5개(시상식/공연/파티/방문자/패트론), `/archive` 3개(소개지/포스터/타임라인, 기존 자료게시판 `board` 위젯은 유지), `/membership` 4개(나의 보물이야기/한문장 소설/마음일기/비밀의 방, 기존 패트론 라운지/아티스트 홍보 `board` 위젯 2개는 유지). `/community/topics`는 hero 자체가 없고 13개 클럽 중 3개만 단일 board/gallery/slide/video로 뒤섞여 있던 상태라 전면 교체 — hero + 13개 클럽 전부의 slide로 재구성.
  - **Orphan 페이지 2개 보강**: `salon-gallery-awards`(gallery 위젯 1개, board_id 미연결), `shop-reviews`(gallery 위젯 1개, After Adoption 보드에 이미 연결)는 EPIC-068 백필 가드("모듈 0개일 때만 삽입")에 걸려 hero/quote/board가 채워지지 못한 상태였다 — 기존 gallery 위젯은 그대로 두고 hero+quote+board를 앞에 추가(salon-gallery-awards는 gallery의 board_id도 시상식 보드로 함께 연결).
  - **DB 조회로 board_id 확인**: anon-key REST API로 각 카테고리별 실제 board id를 확인 후 스크립트에 `(select id from boards where category = '...')` 서브쿼리로 반영(하드코딩 UUID 없음, EPIC-068 컨벤션과 동일) — `EPIC-068-category-page-templates.sql`에 이미 같은 목적으로 작성돼 있었으나 "모듈 0개" 가드에 막혀 한 번도 실행되지 못했던 hub 템플릿 블록(`docent`/`community-topics`/`heritage`/`gallery`/`archive`/`membership`)을 발견 — 동일한 board 매핑을 재사용해 일관성 확보, 다만 기존에 이미 살아있는 hero 문구·`board`(전체 목록) 위젯은 그대로 보존(완전 교체 대신 기존 콘텐츠 보존 + 부족한 부분만 보강하는 방향).
  - **멱등성**: 모든 UPDATE는 `module_type='application' AND settings->'actions'->0->>'href'=...`로 매칭해, 이미 slide로 바뀐 뒤 재실행해도 매칭 행이 없어 안전. INSERT 구간(`community-topics`/orphan 2개)은 "hero가 아직 없을 때만" DO 블록 가드.
  - **검증**: 라이브 DB 미실행 상태이므로 브라우저 검증은 사용자가 SQL 실행 후 진행. SQL 구조 자체는 `do $$...end $$;` 블록 3개 페어링/UPDATE 25건(11+2+5+3+4) 개수를 grep으로 재확인.

## 2026-07-30 (EPIC-053 병합)
- **EPIC-053 병합: Block Editor 시스템을 `develop` 계보에 통합** — 오랫동안 `main`에서 분기된 채 별도로 남아있던 `feature/EPIC-053`(EPIC-053/053.1, Block Editor 시스템 확장+완성)을 정식으로 `develop`에 병합. 사용자가 EPIC-068 백필 SQL 실행을 알린 뒤 "지금까지의 파일이 정리돼 있는지, 병합할 게 있는지" 점검을 요청해 발견 — `docs/STAGES.md` Stage 1의 "Block Editor" 항목이 이 결정 하나에 걸려 있던 상태였다.
  - **충돌 11개 파일 수동 해소**: `merge/epic-053-into-develop` 통합 브랜치(`git merge --no-commit --no-ff feature/EPIC-053`)에서 하나씩 검토.
    - 문서 4종(`CHANGELOG.md`/`NEXT_TASK.md`/`PROJECT_BLUEPRINT.md`/`docs/EPIC.md`) + `.claude/launch.json`: 양쪽 내용을 시간순/논리적 위치에 맞춰 모두 보존.
    - `src/components/boards/BoardRenderer.tsx`: EPIC-053 쪽은 이 파일을 리팩터링 이전의 모놀리식 버전(개별 렌더 함수가 파일 안에 그대로 있는 구조)째로 갖고 있었던 반면, `develop`은 EPIC-066에서 이미 `BoardRendererRegistry`(`src/components/boards/renderers/*`) 기반의 얇은 라우터로 재작성한 상태 — 아키텍처는 `develop` 쪽(현재 계보)을 그대로 유지하고, EPIC-053이 추가했던 유일한 실질 기능(대표 이미지 `featured_image_url`을 `photo_url`보다 썸네일 우선순위로 사용)만 실제 렌더링을 담당하는 `StoryThumbnailModule.tsx`/`GalleryModule.tsx`에 수동으로 포팅.
    - `src/components/boards/PostDetailHeader.tsx`/`src/app/boards/[id]/[postId]/page.tsx`: 양쪽이 서로 다른 신규 prop(`develop`의 좋아요/댓글/조회수 표시 토글 vs EPIC-053의 작성자 전용 "수정" 링크 `editHref`)을 추가한 것이라 상충하지 않아 둘 다 유지.
    - `src/app/api/boards/[id]/posts/route.ts`/`.../[postId]/route.ts`: import 충돌만 있고 본문은 이미 EPIC-053의 `body_json`(ProseMirror JSON 정본) + `renderPostHtml`(서버가 항상 재계산하는 HTML 파생 캐시) 방식으로 합쳐져 있었음 — 두 import 세트를 합치되, 병합 후 실제로 어디서도 쓰이지 않게 된 `sanitizeHtml` import는 제거.
    - `src/app/boards/[id]/write/page.tsx`: `develop` 쪽 충돌 블록은 `RichTextEditor` 기반의 구형 인라인 폼(존재하지 않는 상태 변수를 참조하는 죽은 코드)이었음을 확인 후 제거, EPIC-053의 `PostForm`(작성/수정 공용 컴포넌트) 기반 버전을 채택.
  - **검증**: `npm install`(package.json/package-lock.json 자동 병합 상태 재확인, Tiptap 의존성 전부 정상), `npx tsc --noEmit`(0 errors), `npm run build`(전체 140개 라우트 정상 컴파일), `npm run lint`(29 errors/2 warnings — 병합 전 기존 baseline과 정확히 동일, 신규 이슈 0건) 전부 통과.
  - **병합 결과 반영**: `BlockEditor.tsx`(Tiptap ProseMirror JSON 기반, FigureImage/Gallery/Embed/LinkCard 커스텀 노드)가 `RichTextEditor.tsx`를 완전히 대체, `PostForm.tsx`(작성/수정 공용) 신설, 게시글 수정 화면(`boards/[id]/[postId]/edit`) 신설, Storage Garbage Collection(`src/lib/imageGc.ts` + `/api/admin/storage-cleanup`) 코드 반영 — 단 `docs/sql/epic-053-1.sql`(Storage Bucket/Policy, `posts.body_json`/`featured_image_url` 등 신규 컬럼)은 아직 라이브 DB에 미실행 상태라, 해당 컬럼이 없으면 기존 코드의 42703 폴백 경로(레거시 HTML-only)로 자동 동작.
  - `merge/epic-053-into-develop` → `develop` fast-forward merge → push, 임시 통합 브랜치는 삭제.
  - 문서 동기화: `NEXT_TASK.md`(EPIC-054E 후속 항목 해결로 갱신), `docs/STAGES.md`(Block Editor 항목 완료로 갱신, Storage 항목은 SQL 미실행 사유로 재기술), `docs/PROJECT_DASHBOARD.md`.
- **EPIC-068 백필 SQL 실행 확인** — 사용자가 `docs/sql/EPIC-068-category-page-templates.sql`을 Supabase SQL Editor에서 직접 실행했다고 알려와, anon-key REST 조회(`boards?category=eq.renaissance` 등)로 신규 게시판 30개와 `page_builder` 71개 slug가 실제로 라이브 DB에 반영됐음을 확인. `NEXT_TASK.md`/`docs/PROJECT_DASHBOARD.md`의 "미실행" 표기를 실행 완료로 갱신.

## 2026-07-29 (EPIC-068)
- **EPIC-068: Auto Page+Widget Generation on Category Creation + Site-wide Template Backfill** — "관리자에서 새 카테고리를 만들면 자동으로 페이지가 생기고, 제목/설명/인용/최근글/게시판/갤러리 5개 위젯이 기본 템플릿으로 채워지길 원한다"는 요청. 사전 조사 결과 이 앱에는 **catch-all(동적 DB 기반) 라우트가 전혀 없어**(138개 page.tsx 전부 손으로 만든 정적 파일) 카테고리 DB 행만 추가해서는 실제로 방문 가능한 URL이 생기지 않는다는 근본적 아키텍처 공백을 발견 — 사용자와 범위를 조율(메커니즘 + 실제 게시판이 있는 7개 그룹(사일로상점/온라인도슨트/헤리티지/커뮤니티/멤버십/갤러리/아카이브)의 상세 위젯 구성 + 필요한 ~30개 신규 게시판도 함께 생성, 마이페이지/스튜디오/실시간기능(랭킹·방문기록·설문·예약연동)은 이번 범위 제외)한 뒤 진행.
  - **Part A(메커니즘, 코드)**: `src/app/[...slug]/page.tsx` 신규 — 기존 138개 정적/동적 라우트 전부보다 낮은 우선순위로 매칭되는 catch-all(Next.js가 항상 더 구체적인 라우트를 우선하므로 기존 라우트는 전혀 영향 없음). 경로를 대시로 이어붙여 기존 slug 관례와 동일하게 변환해 `page_builder`를 조회, 있으면 나머지 57개 순수 위젯 페이지와 동일한 패턴(`PageEditButton`+`PageBuilderRenderer`)으로 렌더링, 없으면 "페이지를 찾을 수 없어요" 표시. `src/lib/pageTemplates.ts` 신규 — `ensurePageForSlug(slug, title, description)`: `page_builder` 행이 없으면 생성(있으면 title/description/status는 건드리지 않음), `page_modules`가 0개일 때만 Hero+Quote+Slide+Board+Gallery 5개 기본 템플릿(board_id는 전부 미연결)을 삽입 — 관리자 위젯 편집기가 위젯을 추가할 때와 동일한 `WIDGET_DEFAULT_SETTINGS`를 그대로 사용. `src/components/admin/CategoryTreeManager.tsx`의 `updateRow()`에서 `href`가 채워질 때마다 이 헬퍼를 호출하도록 연결.
  - **Part B(백필, SQL)**: `docs/sql/EPIC-068-category-page-templates.sql` — (1) 실제 게시판이 없던 카테고리용 신규 게시판 30개(`insert ... where not exists` 가드), (2) 71개 페이지의 `page_builder`(`on conflict (slug) do nothing`) + `page_modules`(페이지당 `do $$ ... 현재 모듈 0개일 때만 삽입 $$` PL/pgSQL 블록으로 감싸 이미 구성된 페이지(예: 이번 세션에 관리자가 직접 만든 `community-general` 자유게시판)는 전혀 건드리지 않음), board_id는 전부 `(select id from boards where category/name = '...')` 서브쿼리라 아직 없는 보드를 가리켜도 안전하게 null 처리(EmptyState로 표시, 에러 없음). (3) "입양신청서 라이브러리" 카테고리의 잘못된 href(`/shop`, EPIC-063 기존 버그)를 새 전용 페이지로 수정.
  - **16개 정적 placeholder 페이지 → 순수 위젯 페이지로 전환**: `heritage/{grandmas,grandpas}`, `salon/monthly-events`, `membership/{artist-intro,mind-diary,my-treasures,one-sentence-novel,secret-room}`, `gallery/{awards,parties,patrons,performance,visitors}`, `archive/{brochure,posters,timeline}` — 전부 실콘텐츠 없는 "여기에 게시판이 들어갑니다" 스텁이라 EPIC-067처럼 병존형이 아니라 EPIC-062 계열과 동일한 완전 대체 패턴 적용.
  - **진행 중 발견한 nav 불일치(추가 조치)**: 위 `membership/*` 5개와 `gallery/*` 5개를 만든 뒤 라이브 `site_navigations`를 직접 확인해보니, "나의 아티스트 소개"/"마음일기"/"나의 보물 이야기"/"한문장 소설 프로젝트"/"비밀의 방 도슨트"와 갤러리 5종은 실제로는 `/membership/*`·`/gallery/*`가 아니라 `/salon/*`·`/salon/gallery/*`(이미 존재하던 별개의 placeholder 그룹)로 연결돼 있었음 — 기존 P0(dual nav structure)의 구체적 사례. `salon/{artist-intro,mind-diary,my-treasure-story,one-sentence-novel,secret-room}`, `salon/gallery/{awards,parties,patrons,performances,visitors}` 10개도 동일하게 전환해 같은 보드를 재사용, 실제 nav가 가리키는 쪽이 정상 동작하도록 함(membership-*/gallery-* 5+5는 그대로 남겨둠 — 해가 없고 향후 dual-nav 정리 시 참고 가능). `heritage`(→`/shop/heritage/grandma·grandpa`)와 `archive`(→`/downloads`) 쪽도 유사한 불일치를 발견했으나, 전자는 이미 알려진 기존 이슈(EPIC-054D 문서화)이고 후자는 `/downloads`가 실제 다른 기능(자체 테이블 보유)이라 임의로 재배선하지 않고 NEXT_TASK.md에 기록만 함.
  - **검증**: `npx tsc --noEmit`/`npm run build`/`npm run lint`(엄밀한 clean-baseline 대조 — `git stash -u`로 변경 전 상태와 정확히 비교, 29 errors/2 warnings로 동일, 신규 0건) 통과. 이 과정에서 `[...slug]` catch-all이 `react-hooks/set-state-in-effect`에 새로 걸리는 걸 발견해 effect 안에서 동기적으로 setState하지 않는 구조로 재작성했고, 별개로 catch-all 추가가 `@next/next/no-html-link-for-pages` 규칙의 라우트 스캔에 영향을 줘 `login`/`signup` 페이지의 기존 `<a>` 내비게이션이 새로 걸린 것도 발견해 `<Link>`로 교체(사전에 존재하던 흠이 이번에 드러난 것). 관리자 세션으로 실제 카테고리를 하나 만들어(href 설정 → 저장) `page_builder`/`page_modules`가 자동 생성되고, 그 새 URL이 catch-all로 정상 렌더링되며(5개 위젯 템플릿 그대로) `PageEditButton`도 뜨는 것을 실제로 확인한 뒤 정리(테스트 카테고리/페이지 삭제).
  - 문서 동기화: `docs/EPIC.md`, `docs/PROJECT_DASHBOARD.md`, `docs/STAGES.md`, `NEXT_TASK.md`.

## 2026-07-29 (EPIC-067-HOTFIX 추가)
- **`/treasures`(사일로 보물들) Video 위젯 미재생 버그 수정** — 사용자가 URL을 입력했는데도 영상이 로드되지 않는다고 실제로 보고. `/treasures` 페이지를 직접 열어 재현한 결과, 운영자가 입력한 값이 유튜브 워치 URL(`youtube.com/watch?v=FJN2kKc1g5Y&t=69s`)이었고, `VideoWidget.tsx`는 항상 `<video src={url}>`로만 렌더링해 `MEDIA_ELEMENT_ERROR: Format error`로 조용히 실패하고 있었다(`<video>` 태그는 직접 미디어 파일 URL만 재생 가능, 유튜브/비메오 페이지 URL은 재생 불가) — Inspector의 "영상 URL" 필드가 이 제약을 안내하지 않아 운영자 입장에서는 당연히 될 거라 생각할 만한 입력이었다.
  - **수정**: `src/components/modules/VideoWidget.tsx`에 `toEmbedUrl()` 추가 — 유튜브(`youtube.com/watch`, `youtu.be`, `/shorts/`, `/embed/`)와 비메오(`vimeo.com`, `player.vimeo.com`) URL을 감지해 각각의 `<iframe>` embed URL로 변환해 재생하고(유튜브는 `t=`/`start=` 타임스탬프 쿼리도 embed의 `?start=`로 보존), 그 외(직접 업로드한 mp4 등)는 기존 `<video>` 태그 그대로 재생.
  - **검증**: `/treasures`를 새로고침해 실제 유튜브 URL이 `https://www.youtube.com/embed/FJN2kKc1g5Y?start=69` iframe으로 정상 렌더링되고 콘솔 에러가 없어짐을 확인. `npx tsc --noEmit`/`npm run lint`(기존 baseline과 동일, 신규 0건) 통과.

## 2026-07-29 (EPIC-067-HOTFIX)
- **EPIC-067-HOTFIX: Fix Non-Rendering Widgets (Gallery, Slider, Timeline, Video)** — "Board 위젯을 제외한 Gallery/Latest Posts Slider/Timeline/Video 위젯이 미리보기와 실제 화면에 전혀 렌더링되지 않는다"는 보고를 조사.
  - **조사 방법**: 관리자 로그인 세션이 열려 있어(`/admin/pages/[id]`) 4개 위젯을 실제로 추가·저장해 Live Preview와 저장 후 새로고침한 공개 페이지(`/shop`) 양쪽에서 직접 확인.
  - **재현 결과 — Gallery/Timeline/Latest Posts Slider(`slide`)는 정상 동작**: `PageBuilderRenderer`의 `module_type` 매핑(`src/components/PageBuilderRenderer.tsx`), `DbGalleryModule`/`DbSlideModule`/`DbTimelineModule`(`src/components/modules/DbFeedModules.tsx`)의 `board_id` → `useBoardPosts` 데이터 주입, `GalleryModule`/`SlideModule`/`TimelineView` 렌더링 모두 코드 검토와 실제 클릭 테스트에서 결함을 재현하지 못했다 — 게시판을 선택하면 Live Preview와 새로고침한 공개 페이지 양쪽에서 실제 글(이미지/제목/작성자/좋아요/조회/댓글)이 정상 출력됨을 확인(콘솔 에러 없음). 세 위젯 모두 `BOARD_LINKED_MODULE_TYPES`에 포함돼 있어 위젯 추가 시 게시판 선택 화면이 자동으로 열리므로, 게시판을 아직 선택하지 않은 상태에서는 "연결된 게시판이 없거나 글이 없어요."라는 안내 문구가 정상적으로 보인다(이 상태를 "렌더링 안 됨"으로 오인했을 가능성).
  - **실제로 발견·수정한 결함 — Video 위젯**: `VideoWidget.tsx`가 `url`이 비어 있을 때 `return null`로 **아무 안내도 없이 완전히 사라졌다** — Video는 `BOARD_LINKED_MODULE_TYPES`에 없어 위젯 추가 시 설정 화면이 자동으로 열리지 않으므로(다른 3개 위젯과 달리), 운영자가 위젯을 추가한 직후 url을 입력하지 않으면 화면에 아무 흔적도 남지 않아 "위젯이 고장났다"고 오인하기 가장 쉬운 상태였다. `src/components/modules/VideoWidget.tsx`를 수정해 다른 데이터 의존 위젯(Board/Gallery/Timeline의 안내 문구)과 동일하게 "영상 URL이 아직 설정되지 않았어요." placeholder를 렌더링하도록 안전하게(safe render) 변경. url을 입력하면 Live Preview에서 즉시(저장 전에도) `<video>` 태그가 정상 렌더링됨을 확인.
  - **검증**: `npx tsc --noEmit`/`npm run lint`(기존 baseline 29 errors/2 warnings와 동일, 신규 0건, `VideoWidget.tsx` 무관 확인) 통과. 테스트에 사용한 4개 위젯은 확인 후 전부 삭제해 `/shop` 페이지를 원상복구.
  - 문서 동기화: `NEXT_TASK.md`.

## 2026-07-29 (EPIC-067)
- **EPIC-067: Page Builder Integration for Remaining Pages (Phase 1)** — EPIC-066이 발견한 결함(79개 페이지가 `PageEditButton`만 달려 있고 `PageBuilderRenderer`를 호출하지 않아 관리자가 위젯을 구성해도 화면에 반영되지 않음) 중 사용자 노출도가 높은 핵심 도메인 12개 페이지를 우선 전환.
  - **전환 완료 12개**: `/`(홈, slug=`home`), `/shop`(slug=`shop`), `/shop/[id]`(slug=`shop-id`), `/docent/[id]`(slug=`docent-id`), `/boards/[id]`(slug=`boards-id`), `/clubs`(slug=`clubs`), `/clubs/[id]`(slug=`clubs-id`), `/rental`(slug=`rental`), `/rental/[rentalTypeId]`(slug=`rental-rentaltypeid`), `/downloads`(slug=`downloads`), `/attendance`(slug=`attendance`), `/u/[memberId]`(slug=`u-memberid`).
  - **패턴**: 기존 형제 페이지(`/salon/event-notices`, `/docent/art-deco` 등)와 달리 이 12개는 전부 실제 핵심 콘텐츠(물품 상세/구매, 도슨트 본문/구매, 게시판 글 목록, 클럽 신청, 대관 예약, 자료 목록, 출석 달력, 회원 프로필 글)가 있는 페이지라 콘텐츠를 위젯으로 대체하지 않고, 기존 콘텐츠는 그대로 유지한 채 `fetchPublishedPageBySlug(slug)` + `<PageBuilderRenderer modules={...} />`를 그 아래에 이어 붙이는 방식을 채택(서버 컴포넌트 4개는 `await` 직접 호출, 클라이언트 컴포넌트 8개는 별도 `useEffect`로 병렬 fetch). `page_builder`에 아직 위젯이 없으면 사이트 전역 관례대로 "이 페이지에는 아직 배치된 모듈이 없어요" EmptyState가 그 자리에 보인다(관리자가 위젯을 추가하면 즉시 대체됨).
  - **검증**: `npx tsc --noEmit`/`npm run build`(전체 페이지 정상 컴파일)/`npm run lint`(기존 baseline 29 errors/2 warnings와 동일, 신규 0건, 12개 수정 파일 모두 무관함을 grep으로 확인) 통과. 로그인 세션이 열려 있어 `/`, `/shop`, `/shop/[id]`, `/boards/[id]`, `/clubs` 5개를 브라우저로 직접 열어 기존 콘텐츠(물품 그리드/상세 큐레이션/게시판 글 목록·페이지네이션/클럽 목록)가 그대로 렌더링되고 그 아래에 새 위젯 영역(현재는 미구성 상태의 EmptyState)이 콘솔 에러 없이 나타남을 확인.
  - **범위 밖(Phase 2 이후)**: 나머지 66개 페이지(`mypage/*` 11개, `salon/*`/`gallery/*`/`membership/*` 등 정적 PageHeader류, `boards/page.tsx` 디렉토리·`boards/[id]/write`·`boards/[id]/[postId]`, `login`/`signup`/`settings`/`me` 등)는 이번 Phase에서 다루지 않음 — 아래 NEXT_TASK.md 참고.
  - 문서 동기화: `docs/EPIC.md`, `docs/PROJECT_DASHBOARD.md`, `docs/STAGES.md`, `NEXT_TASK.md`.

## 2026-07-29 (EPIC-066)
- **EPIC-066: Board Widget을 실데이터 렌더링으로 완성 — Renderer Registry + Pagination/Search/Sort/Filter/Empty/Skeleton**
  - **사전 감사(사용자 확인)**: "Board Type 10종" 중 실제 존재하는 건 5종(story/community/gallery/hub/timeline)뿐 — Survey/Calendar/Application은 위젯 타입이지 게시판 타입이 아니고, Collection/Forum은 존재한 적 없음. 실제 5종 기준으로 진행하기로 확정. "빈 화면"의 대부분은 렌더링 버그가 아니라 실제 게시글 0건(EmptyState 정상)임도 함께 확인.
  - **Renderer Registry**: `src/components/boards/renderers/`(Story/Community/Gallery/Timeline/Hub Renderer + `BoardRendererRegistry: Record<BoardLayoutType, Component>`)로 `BoardRenderer.tsx`의 switch/case 제거 — 새 레이아웃은 파일 하나 + registry 한 줄로 확장.
  - **필드 보강**: 좋아요/댓글/작성자/조회수/태그/카테고리/썸네일을 5개 레이아웃 전부에서 실제로 출력(카테고리는 게시판 속성이라 태그 칩으로 병합), `/api/boards/feed`에도 comment_count/view_count/tags 배치 조회 추가.
  - **Pagination**: 페이지당 개수(12/24/48/100) 선택 드롭다운 추가(하위 호환 optional prop).
  - **Search/Sort**: 이미 완비돼 있었음을 코드로 확인, 변경 없음.
  - **Filter**: Tag/Year를 `/api/boards/[id]/posts`에 실제 파라미터로 추가 + `FilterModule` 연결. Category/Board Type 필터는 시간 제약으로 미구현(NEXT_TASK.md 기록).
  - **Empty/Skeleton**: `BoardEmptyState`("아직 게시글이 없습니다."/"첫 글을 작성해보세요.") + `BoardSkeleton`(펄스 애니메이션)으로 교체.
  - **캐시**: 별도 캐싱 없이 항상 새로 fetch하는 기존 구조 유지 — "즉시 반영"이 구조적으로 보장됨(React Query/SWR 미도입, 기존 구조 유지 원칙 준수).
  - **Page Builder 연동 결함 발견 및 수정**: 79개 페이지가 `PageEditButton`만 달려 있고 `PageBuilderRenderer`를 전혀 호출하지 않아 위젯을 구성해도 화면에 반영될 수 없는 구조였음을 발견(EPIC-064A가 컴포넌트만 기계적으로 부착한 결과) — 이번 EPIC 필수 테스트 대상 중 여기 해당하는 `/salon/event-notices`(정적 "준비 중" 텍스트)만 형제 페이지와 동일한 패턴으로 수정, 나머지 78개는 별도 규모의 작업이라 NEXT_TASK.md에 목록 기록.
  - **리팩토링**: `useBoardData`(BoardService)로 조회 로직 분리, `BoardModule.tsx`는 프레젠테이션 전용으로 축소(전 파일 500줄 이내 확인).
  - **성능**: API 라우트 2곳 모두 작성자/댓글 수 배치(IN절) 조회로 N+1 없음 재확인.
  - **DB(Management API 토큰으로 직접 실행)**: `treasures` 페이지의 미연결 Gallery 위젯을 실제 관련 게시판("After Adoption 분양 후 이야기")에 연결, `gallery` 페이지의 죽은 빈 Gallery 위젯(5개 버튼과 중복) 삭제.
  - **검증**: `npx tsc --noEmit`/`npm run build`/`npm run lint`(기존 30개 baseline 동일, 신규 0건) 통과. **관리자 세션이 실제로 열려 있어** Visual Widget Builder를 직접 클릭해 확인 — Board Widget Inspector의 6개 토글(검색/정렬/페이지 넘김/페이지당 개수/썸네일/글쓰기 버튼) 전부 렌더링, Live Preview가 실제 게시글 데이터(작성자/좋아요/댓글/조회수)로 즉시 반영됨을 확인, BoardEmptyState 문구가 지시문 예시와 정확히 일치하는 것도 실제 화면에서 확인.
  - 문서 동기화: `docs/EPIC.md`, `NEXT_TASK.md`.

- **EPIC-066 추가: Board Management System 완성 — 관리자 게시판 CRUD/복제/공개-비공개/Board Type/Category/카드 타입/검색·좋아요·댓글·조회수 토글/페이지네이션/정렬/검색·필터/미리보기/삭제 확인**
  - **DB(`docs/database-schema.sql`에 EPIC-066 ALTER TABLE 블록 기록, 라이브 미적용 — Supabase SQL Editor 실행 필요)**: `boards`에 `is_public`/`group_key`/`render_type`/`default_card_type`/`use_search`/`use_like`/`use_comment`/`use_view_count`/`default_page_size`/`default_sort`/`description`/`widget_settings` 추가 + admin 쓰기 RLS 정책(`boards_admin_write`). 기존 `category`(slug)/`board_type`(권한 축)은 그대로 유지, 새 컬럼은 전부 오버라이드 방식이라 값이 없는 기존 게시판은 회귀 없이 그대로 렌더링됨.
  - **오버레이 아키텍처**: `resolveBoardDefinition()`(`src/lib/boardLayout.ts`)이 기존 하드코딩 `BoardDefinition` 위에 새 admin 컬럼을 `applyAdminOverrides()`로 얹는 방식 채택 — 하드코딩 시스템을 걷어내지 않고, admin UI로 만진 게시판만 즉시 반영. `boards` select는 posts.tags/view_count와 동일한 rich/legacy 폴백 패턴(`BOARD_RICH_FIELDS`/`BOARD_LEGACY_FIELDS`)이라 마이그레이션 전에도 게시판 읽기가 멈추지 않음.
  - **Board Type 10종**: Story/Community/Gallery/Timeline은 기존 레이아웃 재사용, Forum→Community/Collection→Gallery는 별칭 처리, Slide는 기존 `SlideModule`을 게시판 자기 글로 재사용하는 신규 실렌더러. Survey/Calendar/Application은 이 코드베이스에 데이터 모델이 전혀 없어(Page Builder의 survey/calendar 위젯도 무관한 정적 데모) 안내 배너 + 기존 글 목록으로 스텁 처리(사용자 확인 후 확정) — 실제 투표/날짜별 보기/신청 흐름은 후속 EPIC.
  - **좋아요/댓글/조회수 토글 실동작화**: `formatPostMeta()`(`src/lib/postMeta.ts`) 공용 헬퍼로 Story/Community/Gallery 목록 카드 + 게시글 상세 헤더(`PostDetailHeader`) 전부에서 토글이 실제로 메타 라인을 감춘다(이전에는 이 3개 토글이 정의에는 있어도 화면에 전혀 반영되지 않았음).
  - **공개/비공개**: `canReadBoard()`에 `is_public`+admin 우회 게이팅 추가, `/api/boards`·`/api/boards/[id]/posts`가 비공개 게시판을 등급과 무관하게 막는다(관리자 제외).
  - **Admin API**: `/api/admin/boards`(GET 목록/POST 생성), `/api/admin/boards/[id]`(GET/PATCH/DELETE — FK 위반 시 "게시글이 있어 삭제할 수 없어요" 안내), `/api/admin/boards/[id]/duplicate`(POST — slug만 자동 유니크 생성, 나머지 전 필드 복사) — `payments` 라우트와 동일한 `getRequestMember`+`is_admin`+`scopedClient` 패턴.
  - **Admin UI**: `/admin/boards`(검색+Category/Board Type 필터 목록), `/admin/boards/new`·`/admin/boards/[id]`(공용 `BoardForm` — Board Type 변경 시 샘플 글 2건으로 즉시 미리보기), 새 `ConfirmModal`(요구사항이 명시한 "정말 삭제하시겠습니까?" 확인 모달, 기존 다른 페이지의 `window.confirm()`은 범위 밖이라 유지).
  - **Page Builder**: 위젯의 게시판 드롭다운(요구사항 ⑫가 요구한 "board_id 직접 입력 UI 제거"는 이미 이전 EPIC에서 select 드롭다운으로 구현되어 있었음, 이번엔 확인만) + `is_public===false` 게시판 제외 필터만 추가.
  - **검증**: `npx tsc --noEmit`/`npm run lint`(기존 29개 baseline과 동일, 신규 0건) 통과. 로그인 불필요한 부분(공개 게시판 목록/상세의 `is_public` 게이팅, Page Builder 드롭다운 필터)은 dev 서버로 직접 확인. Admin 로그인이 필요한 게시판 생성/수정/복제/삭제/미리보기/검색 UI는 자격 증명을 직접 입력하지 않는 정책상 사용자 확인 필요(`NEXT_TASK.md` 기록).
  - 문서 동기화: `docs/database-schema.sql`, `NEXT_TASK.md`.

## 2026-07-29 (EPIC-065)
- **EPIC-065: 기존 JSON 기반 Page Builder → No-Code Visual Widget Builder 전환**
  - **DB(Management API 토큰으로 직접 실행)**: `page_modules.module_type` CHECK 제약 제거, `is_hidden boolean default false` 컬럼 추가. `docs/sql/EPIC-065-widget-builder-schema.sql`에 기록(재실행 안전).
  - **위젯 23종**: `src/lib/widgetSchema.ts` 신설(타입/라벨/아이콘/6개 카테고리 그룹/필드 스키마/기본값). Hero/Breadcrumb/Board/Latest Posts Slider/Gallery/Timeline/Calendar/Reservation/Survey/Button/CTA/Quote/Image/Video/Audio/FAQ/Search/Filter/Statistics/Badge/Divider/Spacer/HTML. 신규 leaf 컴포넌트 9개만 작성(`BreadcrumbWidget`/`ButtonWidget`/`QuoteWidget`/`ImageWidget`/`VideoWidget`/`AudioWidget`/`FaqWidget`/`StatisticsWidget`/`BadgeWidget`), 나머지는 기존 컴포넌트 재사용(`SurveyCard`/`CtaButtons` 등 EPIC-054B 자산 포함).
  - **Inspector(JSON 없음)**: `WidgetInspectorForm`(체크박스/드롭다운/텍스트/숫자/목록 5종) 하나가 23종 전부를 설정 — 목록형 필드(FAQ/Statistics/CTA/Reservation 등)는 행 추가·삭제·위아래 이동 UI.
  - **Board Widget 6개 토글 실동작화**: `BoardModule`에 `searchEnabled`/`sortEnabled`/`paginationEnabled`/`pageSizeOverride`/`showThumbnail`/`showWriteButton` optional prop 추가(전부 기본 undefined → 기존 호출부 무영향). `/api/boards/[id]/posts`에 `pageSize` 쿼리 파라미터 추가. 썸네일 토글은 `StoryThumbnailModule`(story 레이아웃)에만 적용(gallery/hub는 이미지가 레이아웃 자체라 대상 아님, 알려진 제한).
  - **Live Preview**: 편집 중 위젯 하나만 draft로 관리, `PageBuilderRenderer`가 해당 자리를 draft로 치환해 렌더링 — 저장 전에도 새로고침 없이 즉시 반영.
  - **Drag & Drop / Duplicate / Hide**: 기존 `dnd-kit` 패턴 재사용, 복제는 원본 바로 다음에 삽입(뒤 항목 sort_order 밀기), 숨기기는 `is_hidden` 즉시 토글(공개 페이지는 건너뜀, 관리자 미리보기는 흐리게 계속 표시).
  - **개발자 모드**: 세션 로컬 체크박스를 켜야만 위젯별 원시 JSON 보기/수정이 나타남(기본 숨김) — `src/app/admin/pages/[id]/page.tsx` 전면 재작성.
  - **검증**: `npx tsc --noEmit`/`npm run build`/`npm run lint`(신규 에러 0건) 통과. 관리자 로그인 자격 증명이 없어 Visual Builder UI를 직접 클릭 테스트하지는 못했음 — Management API로 새 위젯 5종+숨김 위젯+Board 토글 6종을 라이브 `/gallery` 페이지에 임시로 심어 렌더링/필터링을 실제 확인한 뒤 전부 원상복구(테스트 데이터 잔존 없음 확인).
  - 문서 동기화: `docs/EPIC.md`, `NEXT_TASK.md`.

## 2026-07-29 (EPIC-064A)
- **EPIC-064A: 모든 Route에 관리자 전용 "페이지 수정" 버튼 부착 — Page Builder/Widget/DB 수정 없이 기존 `PageEditButton` 컴포넌트만 재사용**
  - `src/app` 하위 135개 `page.tsx`를 전수 조사 — 51개는 이미 EPIC-060~062에서 버튼이 붙어 있었고, 나머지 84개 중 75개(placeholder 페이지 35개는 스크립트로, 나머지 49개는 개별 검토)에 새로 부착. `/admin/**` 9개는 관리자 CMS 도구 자체(가드는 이미 `admin/layout.tsx`가 처리, `/admin/pages/[id]`는 Page Builder 편집기 그 자체)라 의도적으로 제외 — 결과적으로 126/135 Route에 적용.
  - Static Route(`/treasures`, `/shop` 등)와 Dynamic Route(`/boards/[id]`, `/docent/[id]`, `/heritage/grandma/[name]`, `/u/[memberId]`, `/mypage/collections/[category]` 등) 모두 포함.
  - 버튼 위치는 `PageEditButton` 자체가 `fixed top-20 right-4`이므로 어느 페이지에 넣어도 화면상 동일한 위치에 뜬다 — 페이지별 레이아웃 조정 없이 일관성이 자동으로 보장됨.
  - slug 네이밍: URL 경로를 대시로 이은 문자열로 통일(예: `/shop/projects/[id]` → `shop-projects-id`). 로딩/에러 중간 상태의 early return에는 버튼을 붙이지 않고, 실제 콘텐츠가 렌더링되는 지점에만 부착(리다이렉트 전용 페이지 `/mypage/collections`는 예외적으로 `return null` 자체를 버튼으로 교체).
  - 클릭 시 동작은 기존 `PageEditButton`(`/admin/pages/[pageId]`로 이동, `page_builder`에 해당 slug 행이 있을 때만 관리자에게 노출) 그대로 — 새 기능 추가 없음.
  - **검증**: `npx tsc --noEmit`/`npm run build`(135개 라우트 전부 컴파일)/`npm run lint`(신규 lint 에러 0건 — 발견된 30건은 전부 이번 변경과 무관한 기존 `react-hooks/set-state-in-effect` 패턴) 통과. 브라우저로 static/dynamic/placeholder/board/폼/redirect 26개 페이지를 직접 열어 콘솔 에러 없이 로딩 확인, 관리자가 아닌 세션에서 버튼이 어디에도 노출되지 않음을 확인.
  - 문서 동기화: `docs/EPIC.md`, `NEXT_TASK.md`.

## 2026-07-29 (EPIC-063)
- **EPIC-063: Navigation System Completion (Page-first Architecture) — Board가 아니라 Navigation/Hub Page/Sidebar UX만 수정**
  - **사전 감사 결과**: 라이브 `site_navigations`(DB)와 EPIC-062까지의 Hub Page(`/treasures`·`/docent`·`/heritage`·`/community`(+`/topics`·`/weekday`)·`/membership`·`/gallery`·`/archive`, 각 페이지의 관리자 전용 `PageEditButton`)는 이미 지시 사항대로 전부 갖춰져 있었다 — 신규 Route/버튼 생성 없이 실제로 남아 있던 프론트엔드 버그 3건 + DB 1건만 수정.
  - **버그①(상단 Navbar 드롭다운)**: `Navbar.tsx`의 top-nav hover 플라이아웃에서 그룹 헤더(사일로 보물들/온라인 도슨트/사일로 유산/커뮤니티/멤버십/갤러리/아카이브)가 DB에 이미 올바른 `href`가 있음에도 항상 클릭 불가한 `<button>`으로 렌더링되고 있었다(`group.href`를 아예 참조하지 않음) — `group.href`가 있으면 `<Link>`로 렌더링하도록 수정, 없으면 기존처럼 클릭 불가 버튼 유지(LeftSidebar/RightSidebar 본 패널은 EPIC-058부터 이미 이 분기를 갖고 있었음). 브라우저에서 `사일로 상점`→`/treasures`, `살롱데상`→`/community`/`/membership`/`/gallery`/`/archive` 클릭 이동 확인.
  - **버그②(LeftSidebar 기본 펼침)**: `defaultExpanded()`가 그룹 라벨에 "도슨트"/"헤리티지"가 없으면(예: "사일로 보물들") 기본 펼침으로 뒀던 특례를 제거 — 모든 그룹이 예외 없이 기본 접힘으로 시작하도록 `RightSidebar.tsx`와 동일하게 단순화. 브라우저에서 Left/Right 모두 그룹 3~4개 전부 `aria-expanded=false` 확인.
  - **버그③(Sidebar 여닫이 UX)**: `LeftSidebar.tsx`/`RightSidebar.tsx`의 여닫이 아이콘이 "클릭으로만 열림"(EPIC-043)이었던 것에 `onMouseEnter`를 추가해 hover로도 열리도록 하고, 패널의 `onMouseLeave`(hover-out 시 즉시 닫힘, `onAmbientLeave` prop)를 완전히 제거 — 이제 패널을 닫는 방법은 Navbar.tsx의 기존 backdrop 클릭(Outside Click) · ✕ 버튼 · Escape 세 가지뿐이다. 브라우저에서 (a) hover만으로 열림, (b) 패널에 `mouseleave`/`mouseout`을 강제로 발생시켜도 안 닫힘, (c) backdrop 클릭 시 닫힘을 각각 실제 DOM 상태(`aria-hidden`/클래스)로 확인. 상단 Navbar의 작은 hover 플라이아웃(hover-open/hover-out-close)은 요구사항대로 무변경.
  - **DB①(Navigation Audit)**: `site_navigations` 전체(52행)를 anon key로 조회해 트리로 재구성, 감사 — 유일하게 깨진 행은 "입양신청서 라이브러리"(href=null, 사일로 보물들 하위) 1건뿐이었다(그 외 지시된 항목은 전부 올바른 Hub URL 연결 확인). Management API 토큰이 이번 세션에 없어 `docs/sql/EPIC-063-nav-audit-fix.sql`(관리자가 Supabase SQL Editor에서 실행)로 작성 — `/shop`으로 임시 연결(전용 페이지가 없어 형제 항목과 동일 목적지로 지정, 실제 의도가 다르면 href만 바꿔 재실행).
  - **문서 동기화**: `docs/navigation-blueprint.md`(EPIC-023 이후 실제 DB 기준과 어긋난다는 경고 추가 + 이번 EPIC의 sidebar 동작 변경 반영), `docs/EPIC.md`, `NEXT_TASK.md`. EPIC-057~062의 상세 요약이 `docs/EPIC.md`/`CHANGELOG.md`에 아직 없었다는 기존 문서 부채를 발견해 `NEXT_TASK.md`에 별도 기록.
  - **검증**: `npx tsc --noEmit`/`npm run build`/`npm run lint` 통과(수정 파일 관련 신규 에러 0건 — LeftSidebar/RightSidebar 신규 lint 에러 없음, Navbar.tsx의 유일한 lint 경고는 이번 EPIC과 무관한 기존 코드). 브라우저로 `/treasures`·`/docent`·`/heritage`·`/community`·`/community/topics`·`/community/weekday`·`/membership`·`/gallery`·`/archive` 9개 라우트 전부 콘솔 에러 없이 로드 확인. 로그인 세션이 없어 `PageEditButton`(관리자 전용)이 익명 사용자에게 보이지 않음(정상)만 확인 — 관리자 계정으로의 실제 노출은 사용자가 로그인 후 직접 확인 필요.

## 2026-07-28 (EPIC-056)
- **EPIC-056: 모든 페이지를 Board Module 조합으로 구성 — 14개 Board Module 정립 + 브라우저에서 실제로 보이는 화면 변경**
  - **모듈 정립(14종)**: 기존에 `BoardRenderer.tsx`/`BoardHeader.tsx` 안에 갇혀 있던 사설(private) 컴포넌트들을 `src/components/modules/`에 독립적으로 재사용 가능한 이름 있는 모듈로 뽑아냈다(마크업/동작 변경 없음, 순수 추출) — `CommunityListModule`(⑥), `StoryThumbnailModule`(⑤, 기존 공용 `StoryCard` 재사용), `GalleryModule`(⑦), `SlideModule`(⑧), `SortSelect`(④), `BoardHeaderModule`(②, 제목+글쓰기 버튼만 — Search/Sort 분리). `HeroModule`(①)은 `PageHeaderContent`(EPIC-054A)의 이름 있는 alias, `ApplicationModule`(⑫)은 `CtaButtons`(EPIC-054B)의 이름 있는 alias — 전부 재사용, 중복 없음. 신규 마크업이 필요했던 건 `FilterModule`(⑩) 하나뿐이며, 그마저도 `docs/design-system.md` §4의 기존 pill 필터 관례를 그대로 따랐다(새 디자인 없음). Search(③)/Pagination(⑬)/Empty State(⑭)/Timeline(⑨)/Calendar(⑪)는 이미 독립 모듈로 존재하던 `SearchInput`/`Pagination`/`EmptyState`/`TimelineView`/`CalendarGrid`를 그대로 재사용.
  - **`BoardHeader.tsx`/`BoardRenderer.tsx` 리팩터**: 각각 위 모듈들을 조합하는 얇은 레이아웃 래퍼로 재구성 — 렌더링 결과(HTML/클래스)는 리팩터 전과 동일함을 브라우저로 확인.
  - **브라우저에서 실제로 보이는 변경 ①**: `src/components/modules/BoardModule.tsx`에 Hero Module을 내장(`showHero` prop, 기본 `true`) — 이제 `/boards/[id]`(자유게시판/클럽/각 시대 게시판 등 약 72개 게시판 전부)가 이전에는 없던 **Breadcrumb + 제목 + 설명**을 화면 최상단에 보여준다(예: `/boards/b0f009f3-...` → "홈 › Community › 자유게시판" + "자유게시판" + "누구나 자유롭게 글을 쓰는 자유게시판"). Breadcrumb 중간 항목은 `resolveBoardDefinition(board).parent`를 통해 부모 hub 이름을 자동으로 붙인다.
  - **브라우저에서 실제로 보이는 변경 ②**: `PageTemplate`이 이미 자체 Hero를 그리는 6개 카테고리 허브 페이지(Community/Heritage/Studio/Membership/Gallery/Archive)는 `BoardModule`에 `showHero={false}`를 넘겨 Hero 중복 렌더링을 방지 — 브라우저로 Hero가 정확히 1번만 보임을 확인.
  - **브라우저에서 실제로 보이는 변경 ③**: `/boards`(게시판 디렉토리) 상단의 일반 `<h1>`을 `HeroModule`로 교체해 Breadcrumb("홈 › 게시판")과 설명 문구가 새로 보이게 됨.
  - **브라우저에서 실제로 보이는 변경 ④(가장 큰 구성 변화)**: `/studio` 페이지를 `PageTemplate`(범용) 대신 지시받은 조합(Hero + Application Module + Calendar Module + Board Container)으로 직접 재조립 — `ApplicationModule`이 "공간 촬영 대관 신청"/"물품 대여 신청"/"공간 스타일링 문의" 3개 버튼(기존 `/rental`·`/space-inquiry/*` 실제 페이지로 연결)을, `CalendarGrid`가 이번 달 달력을 새로 보여준다(실 예약 데이터 연동은 새 기능이라 범위 밖, 달력 자체만 표시).
  - **검증**: 로컬 dev 서버로 `/boards/b0f009f3-...`(Hero+Breadcrumb 신규 노출), `/studio`(Application+Calendar 신규 노출, 스크린샷 확인), `/community`(Hero 중복 없음 확인), `/boards`(Breadcrumb 신규 노출), `/gallery` 전부 콘솔 에러 없이 확인.
  - **아직 모듈이 적용되지 않은 페이지**: `/shop`(사일로상점 아이템 카탈로그), `/docent`(온라인 도슨트), 마이페이지 전체 — 전부 게시판(`posts`/`boards`)이 아닌 별도 데이터 도메인(items/docent_contents/개인 데이터)의 성숙한 기존 화면이라, 이번 EPIC에서 강제로 Board Module 조합으로 재구성하지 않았다(새 기능 없이는 데이터 모델이 맞지 않음) — 별도 EPIC에서 사용자와 방향을 논의할 것.
  - 문서 동기화: `docs/STAGES.md`, `docs/PROJECT_DASHBOARD.md`, `docs/EPIC.md`, `NEXT_TASK.md`.
  - 검증: `npx tsc --noEmit`/`npm run lint` 통과(신규/수정 파일 관련 에러 0건).

## 2026-07-28 (EPIC-055)
- **EPIC-055: Universal Board System 완성 — 모든 페이지를 실제 게시판(Board)에 연결(새 페이지/기능/DB/Board 컴포넌트 없이 연결과 중복 제거만)**
  - **발견: `src/components/shared/UniversalBoard.tsx`(EPIC-044) — 실데이터 없는 뼈대 stub이 3개 페이지 그룹에서 여전히 쓰이고 있었다.** `/heritage/grandma/[name]`·`/heritage/grandpa/[name]`·`/community/club/[name]`(최대 69개 이름별 URL: 할머니 50 + 할아버지 17 + 클럽/주제 20)이 전부 `posts=[]` 고정값의 독자적인 검색/정렬 로직(Universal Board System과 완전히 무관, `BoardRenderer`/`BoardModule`을 전혀 쓰지 않는 별개 구현)만 렌더링하고 있어 실제 게시판과 연결돼 있지 않았다.
  - **연결**: 3개 페이지 그룹 전부 `UniversalBoard` → `PageTemplate`+`BoardModule`(EPIC-054C/054F)로 교체. 할머니/할아버지는 개인별 게시판이 없어(새 DB 금지) 전체가 공유하는 `grandmas`/`grandpas` 스토리 게시판(`src/lib/boardLayout.ts`, 기존 정의)에 연결하고 이름은 페이지 제목으로만 사용(검색창으로 특정 이름 필터링 가능, 기존 `BoardModule` 기능 그대로). 클럽/주제별 게시판은 EPIC-049 때 이미 실제 board 행(name=클럽 이름)으로 존재하므로, `src/lib/useHubBoardId.ts`에 추가한 `useBoardIdByName(name)` 훅으로 정확히 이름이 일치하는 그 board에 그대로 연결(새 board 생성 없음).
  - **중복 제거 ①**: `src/components/shared/UniversalBoard.tsx` 완전 삭제(대체 후 참조 없음 확인) — 자체 검색/정렬 재구현이 사라지고 Universal Board System(`BoardHeader`/`BoardRenderer`/`Pagination`) 하나로 통일.
  - **중복 제거 ②**: `src/components/mypage/EmptyState.tsx`(마이페이지 전용, `message` prop)가 Board System의 `src/components/modules/EmptyState.tsx`(`title` prop)와 사실상 동일한 컴포넌트로 중복 존재 — 마이페이지 패널 11개 + `CollectionCategoryPanel.tsx` 총 12곳의 import와 prop명(`message`→`title`)을 일괄 전환하고 마이페이지 전용 버전을 삭제. 이제 게시판이든 마이페이지든 "콘텐츠 없음" 상태는 컴포넌트 하나만 공유.
  - **Board Config 단순화 확인**: `src/lib/boardLayout.ts`의 `BOARD_DEFINITIONS`/`INDIVIDUAL_BOARD_DEFINITIONS` + `resolveBoardDefinition()`가 이미 "설정 파일 하나만 수정해 새 게시판 추가" 구조를 만족함을 재확인(EPIC-047부터 유지) — 이번 EPIC은 이 구조를 변경하지 않고 그대로 재사용.
  - **범위 밖(의도적, 지시 반영)**: `/shop`(아이템 카탈로그)·`/docent`(도슨트 콘텐츠)는 `posts`/`boards` 테이블과 무관한 별개 데이터 도메인이라 Board 연결 대상이 아님(게시판이 아님). 마이페이지 개인 데이터 패널(컬렉션/위시리스트/팔로우 등)도 개인 전용 데이터라 실제 `board_id` 연결 대상 아님 — EmptyState 컴포넌트만 공유. `silo-store`/`online-docent` hub는 EPIC-054F와 동일하게 "새 페이지 금지"로 전용 URL을 만들지 않음(`/boards` 디렉토리 슬라이드로만 노출, 기존과 동일). `salon-topics`/`salon-weekday`/`survey` 중첩 hub도 전용 URL 없음(Community 허브 하위 카드로만 접근, 기존과 동일). 새 Editor/Block/DB/Storage/Upload/Authentication/Design은 전혀 만들지 않음.
  - 문서 동기화: `docs/STAGES.md`(Stage 1 Board System 진행률), `docs/PROJECT_DASHBOARD.md`, `docs/EPIC.md`, `NEXT_TASK.md`.
  - 검증: `npx tsc --noEmit`/`npm run lint` 통과(신규/수정 파일 관련 에러 0건). 로컬 dev 서버로 `/heritage/grandma/Agatha`(Board 미연결 상태 EmptyState 정상), `/community/club/경제`(동일하게 EmptyState — 라이브 DB에 클럽 board 행 자체가 아직 없음을 확인), `/boards`(기존 6개 legacy 게시판만 실제 존재함을 재확인) 콘솔 에러 없이 확인.

## 2026-07-28 (EPIC-054F)
- **EPIC-054F: 모든 상위/하위 카테고리 메뉴가 실제 Page(Route)를 갖도록 정비 — 새 기능/디자인/DB/게시판/Block/Editor 없이 Page 생성만**
  - **조사**: EPIC-054D 감사(navConfig.ts/Board Definition System 전수 확인) 결과를 근거로, 지시받은 카테고리 목록(Studio/Community/Membership/Gallery/Archive/Online Docent/Heritage/마이페이지) 각각의 실제 Route 존재 여부를 재확인 — 마이페이지 12개 탭은 전부 이미 실제 페이지 보유(변경 없음), Online Docent(`/docent`)는 이미 완전히 구현됨(변경 없음). **Community**(`/community/club/[name]`만 있고 `/community` 자체는 없음), **Heritage**(`/heritage/grandma|grandpa/[name]`만 있고 `/heritage` 자체는 없음), **Studio**/**Membership**/**Gallery**/**Archive**(디렉토리 자체가 아예 없음) 6곳이 실제 404 상태임을 확인 — 전부 Board Definition System의 hub 정의(`src/lib/boardLayout.ts`)는 이미 존재하지만 그 hub를 가리키는 Route가 없던 상태.
  - **공용 Page Template 신설**: 새 디자인을 만들지 않기 위해 기존 두 컴포넌트를 조합만 함 — `src/components/PageHeader.tsx`에서 Breadcrumb/Title/Subtitle/Description 블록을 `PageHeaderContent`로 추출(기존 `PageHeader`는 그대로 `<main>`으로 감싸 하위 호환, 19개 기존 페이지 동작 변경 없음) + `src/components/PageTemplate.tsx` 신규(`PageHeaderContent` + Board Container 자리에 `BoardModule`(EPIC-054C, Search/Sort/Pagination/글쓰기 버튼/Empty State 전부 포함) 또는 `boardId`가 없을 때 `EmptyState`("게시글이 없습니다.")를 배치). Sidebar는 전역 레이아웃(Navbar의 LeftSidebar/RightSidebar)이 모든 페이지에 이미 제공하므로 별도로 만들지 않음.
  - **`src/lib/useHubBoardId.ts` 신규**: slug로 실제 board row id를 찾는 훅 — `src/app/boards/page.tsx`가 이미 쓰던 "전체 boards 조회 후 `resolveBoardDefinition(b).slug`로 매칭" 패턴을 그대로 재사용(새 조회 로직 없음), 6개 신규 페이지가 공유.
  - **6개 신규 페이지**: `/community`, `/heritage`, `/studio`, `/membership`, `/gallery`, `/archive` — 전부 `PageTemplate` + `useHubBoardId(slug)` 조합으로 동일하게 구현. 브라우저로 6개 전부 확인: 5개는 해당 hub의 board 행이 라이브 DB에 아직 시딩되지 않아 "게시글이 없습니다." Empty State 정상 노출(Board 미연결 상태에서도 Page 자체는 항상 존재), `/archive`만 기존에 실제로 존재하던 "자료게시판"(legacy `board_type='archive'` 그룹, EPIC-047 이전부터 있던 실제 게시판)이 우연히 같은 slug("archive")로 해석돼 정상적으로 콘텐츠+검색/정렬/페이지네이션이 표시됨.
  - **발견(수정하지 않음, P2)**: legacy `BOARD_DEFINITIONS.archive`(그룹, "자료게시판")와 신규 `INDIVIDUAL_BOARD_DEFINITIONS.archive`(hub, "Archive")가 동일한 slug `"archive"`를 쓰는 네이밍 충돌이 있음 — `resolveBoardDefinition`이 `category`(개별 정의)를 `board_type`(그룹 정의)보다 우선 확인하는 순서 때문에 실제 동작은 깨지지 않지만, "어느 archive를 가리키는지" 혼동 여지가 있다. Board Definition System 자체를 수정하는 일이라 "새 게시판/DB 변경 금지" 범위 밖 — NEXT_TASK.md에 기록만.
  - **모든 Route 목록**: `src/app` 기준 `page.tsx` 76개(기존 70개 + 신규 6개, `api/` 제외) — 상세는 `docs/PROJECT_DASHBOARD.md`/터미널 출력 참고.
  - **범위 밖(의도적)**: nested hub(`salon-topics`/`salon-weekday`/`survey`)는 지시된 카테고리 목록에 없어 전용 top-level Route를 만들지 않음 — Community 허브 페이지의 "하위 게시판" 카드 그리드(기존 `BoardRenderer`의 `HubView`, EPIC-048)를 통해 이미 도달 가능. 새 기능/디자인/DB/게시판/Block/Editor는 지시대로 전혀 만들지 않음.
  - 문서 동기화: `docs/STAGES.md`(EPIC-054E 규칙에 따라 Stage 1 Navigation/Routing 진행률 갱신), `docs/PROJECT_DASHBOARD.md`, `docs/EPIC.md`, `NEXT_TASK.md`.
  - 검증: `npx tsc --noEmit`/`npm run lint` 통과(신규/수정 파일 관련 에러 0건). 로컬 dev 서버로 6개 신규 페이지 전부 브라우저 확인(Breadcrumb/Header/Description/Board 또는 EmptyState 렌더링, 콘솔 에러 없음).

## 2026-07-28 (EPIC-054E)
- **EPIC-054E: EPIC 번호와 프로젝트 진행 단계(Stage) 분리 — 프로젝트 운영 체계 구축(문서/프로세스만, 코드 변경 없음)**
  - **`docs/STAGES.md` 신규**: Stage 1(Foundation) ~ Stage 6(Scale) 6단계 정의. Stage 1(현재 단계)은 지시받은 14개 세부 항목(Navigation/Routing/Authentication/Supabase/Storage/Database/Board System/Page System/Block Editor/SEO/Metadata/Sitemap/Responsive/Accessibility) 각각을 EPIC-054D까지의 실제 코드 상태로 감사해 완료/부분/미착수로 분류 — 10개 완료, 3개 부분 완료, 1개(Block Editor 완전판) 별도 미병합 브랜치(`feature/EPIC-053`)에만 존재함을 명시. Stage 2~6은 "예상 포함 기능"만 기록(착수 전) — 단 Stage 4(Business)는 이 Stage 모델이 생기기 전(EPIC-001~018 무렵)에 이미 예약/결제/Membership/Rental/Styling 대부분이 구현돼 있었다는 점을 정직하게 기록(역행이 아니라 Stage 개념 도입 이전에 비즈니스 기능이 먼저 만들어진 것).
  - **`docs/PROJECT_DASHBOARD.md` 신규**: 지시받은 형식(Project/Current Stage/Progress/Current EPIC/Next EPIC/Recent Completed/Current Priority/Known Issues(P0-P3)/Technical Debt/Next Milestone) 그대로 작성. Known Issues는 EPIC-054D가 이미 P0~P3로 분류해둔 NEXT_TASK.md 항목을 그대로 재사용(중복 발견 대신 재사용). 상세 근거는 이 문서에 복제하지 않고 STAGES.md/EPIC.md/NEXT_TASK.md로 링크.
  - **`PROJECT_BLUEPRINT.md` 갱신**: 새 §11(Project Stages / Project Dashboard) 추가 — Current Stage(Stage 1)/Current Milestone(Stage 1 완료까지 남은 5개 항목) 명시, 두 신규 문서로 링크.
  - **`CLAUDE.md` 갱신**: "세션 시작 시 읽기 순서" 절 신규 — 새 세션 시작 시 `docs/PROJECT_DASHBOARD.md` → `docs/STAGES.md` → `PROJECT_BLUEPRINT.md` 순으로 읽어 프로젝트 상태를 먼저 파악하도록 명시. "Stage와 EPIC은 혼용하지 않는다"는 원칙도 이 절에 명문화. 기존 "Epic 완료 시 문서 갱신" 규칙에 `docs/PROJECT_DASHBOARD.md`/`docs/STAGES.md`를 추가.
  - **범위 밖(지시 반영)**: 이번 EPIC은 문서/프로세스 구축만 — 기능 추가/UI 변경/DB 변경/게시판 변경 전혀 없음. 코드 파일은 한 줄도 수정하지 않음(`type-check`/`lint`는 회귀 확인용으로만 재실행).
  - 문서 동기화: `docs/STAGES.md`(신규), `docs/PROJECT_DASHBOARD.md`(신규), `PROJECT_BLUEPRINT.md`, `CLAUDE.md`, `docs/EPIC.md`, `NEXT_TASK.md`.
  - 검증: `npx tsc --noEmit`/`npm run lint` 재실행 — 코드 변경이 없으므로 EPIC-054D 시점과 동일하게 pre-existing 27건만 존재, 신규 0건.

## 2026-07-28 (EPIC-054D)
- **EPIC-054D: 사이트 전체 Navigation/Routing/SEO/Breadcrumb/404/URL 구조 전수 감사(Audit) — 새 기능/디자인/게시판/Block 생성 없이 감사+리팩토링만**
  - **브랜치 준비**: 이 감사가 사이트의 "현재 상태"를 정확히 반영하려면 아직 미병합인 `feature/EPIC-054A`(placeholder→실페이지 전환)와 `feature/EPIC-054B`(Page Module + Board 연결)가 모두 반영된 코드가 필요해 사용자 확인 후 두 브랜치를 로컬에서 병합한 `feature/EPIC-054D`를 새로 만들어 감사를 진행함.
  - **핵심 발견(P0)**: `src/lib/navConfig.ts`의 `FALLBACK_NAV_TABS`(EPIC-044가 재작성한 `/heritage/*/[name]`·`/community/club/[name]` 동적 구조)와 실제 라이브 `site_navigations`(DB 시드, `docs/navigation-blueprint.md`가 반영하는 구조)가 서로 다른 nav 트리로 공존한다 — 어느 쪽만 보고 감사하느냐에 따라 "죽은 링크"/"orphan 페이지" 목록이 달라지는 근본 원인. 코드에 하드코딩된 링크 자체(FALLBACK_NAV_TABS)만 놓고 보면 깨진 링크는 없으나, 두 구조가 가리키는 대상이 달라 `/shop/heritage/grandma`(정적, EPIC-054A 전환) vs `/heritage/grandma/[name]`(동적, EPIC-044) 같은 **중복 콘텐츠 쌍**이 여럿 존재. 이 이중 구조 통합은 실제 nav 재설계(새 디자인/기능)라 이번 감사 범위 밖 — 별도 EPIC으로 사용자와 논의 필요(NEXT_TASK.md 기록).
  - **SEO(§6/7)**: `src/app/layout.tsx`의 root `metadata`가 create-next-app 스캐폴드 placeholder(`title: "Create Next App"`)를 그대로 쓰고 있었고 70개 페이지 중 단 하나도 `metadata`/`generateMetadata`를 override하지 않아(전수 조사 완료) 전 페이지가 이 값을 상속하고 있었다 — 실제 사이트 정보(title template/description/OpenGraph/Twitter Card)로 교체하고 `metadataBase`(`NEXT_PUBLIC_SITE_URL` env, 없으면 `localhost:3000`) 추가. `lang="en"` → `lang="ko"`로 수정(문서 언어가 실제 콘텐츠와 다르면 스크린리더/검색엔진 모두에 부정확한 신호). 페이지별 metadata는 70개 각각 콘텐츠 작성이 필요한 별도 작업이라 범위 밖(NEXT_TASK.md).
  - **Sitemap(§8) 신규**: `src/app/sitemap.ts` — `src/app` 디렉토리를 직접 스캔해 정적 라우트를 자동 수집(admin/api/mypage/me/settings/login/signup 제외) + `navConfig.ts`의 하드코딩 이름 배열(heritage/community, 이번에 export 처리) 기반 동적 이름 라우트 + Supabase 조회 기반 동적 콘텐츠(boards/items/docent_contents/clubs, 테이블별 개별 try/catch로 하나 실패해도 전체가 깨지지 않음). 파일시스템을 직접 스캔하므로 "새 Page가 추가되면 자동으로 Sitemap에 포함"되는 요구를 코드 유지보수 없이 만족.
  - **robots.txt(§9) 신규**: `src/app/robots.ts` — sitemap.ts와 동일한 기준으로 관리자/마이페이지/개인정보/인증 폼 제외, `Sitemap:` 지시어로 위 sitemap.ts 연결.
  - **Canonical(§10)**: `metadataBase` 추가로 상대 경로 기반 OG/canonical 계산의 기준점은 마련했으나, 페이지별 명시적 `alternates.canonical`은 70개 페이지 전체에 metadata를 붙이는 더 큰 작업이 선행돼야 해 범위 밖(P2, NEXT_TASK.md).
  - **접근성(§13)**: `LeftSidebar.tsx`/`RightSidebar.tsx` — Escape 키로 닫기, 패널이 닫힐 때 트리거 아이콘 버튼으로 포커스 자동 복귀, 닫혀 있을 때 `inert`+`aria-hidden`(스크린리더/Tab 접근 차단), 트리거 버튼에 `aria-expanded`/`aria-controls` 추가. `Navbar.tsx`의 순수 CSS hover 드롭다운(EPIC-041-042-HOTFIX가 의도적으로 JS state를 피한 구조)에 `group-focus-within` 클래스를 추가해 키보드 Tab만으로도 동일하게 열리도록 하고(JS state 재도입 없음), 2차 플라이아웃의 그룹 라벨을 포커스 불가능한 `<div>`에서 포커스 가능한 `<button>`으로 변경(그래야 Tab이 도달해 `focus-within`을 트리거할 수 있음), 드롭다운 트리거에 `aria-haspopup` 추가. Escape로 드롭다운을 닫는 것은 순수 CSS 구조상 구현하지 않음(포커스를 다른 곳으로 옮기면 자연히 닫힘) — P2로 기록.
  - **성능(§12)**: `Navbar.tsx`의 `leftSidebarTab`/`rightSidebarTab`(최대 96개 항목 배열에 대한 `Array.find`)을 `useMemo`로 감쌈. `activeCustomFonts`/`fontFamilyValue`도 시도했으나 이 저장소의 `eslint-config-next`에 포함된 React Compiler 대비 lint 규칙과 충돌(수동 `useMemo` 의존성 배열이 컴파일러 추론과 어긋남 — `next.config.ts`에 `experimental.reactCompiler`는 아직 꺼져 있어 실제 컴파일러 동작 차이는 없음)해 원래 형태로 되돌림, 이유를 주석으로 남김.
  - **범위 밖으로 남겨둔 것(의도적, "새 기능/디자인 추가 금지" 반영)**: Breadcrumb 자동 생성 시스템(현재 19개 정적 페이지만 수동 breadcrumb, 나머지 51개 페이지는 아예 없음 — 사이트 전체에 새로 만들면 명백한 신규 기능이라 구현하지 않고 설계 방향만 NEXT_TASK.md에 기록), Responsive/모바일 전용 레이아웃(Navbar에 반응형 브레이크포인트 자체가 없음 — 새로 만들면 새 디자인이라 구현하지 않음), 이중 nav 구조 통합(위 P0 참고).
  - **검증**: `npx tsc --noEmit`/`npm run lint` 통과(신규/수정 파일 관련 에러 0건). 로컬 dev 서버로 `/sitemap.xml`(admin/api/mypage 등 자동 제외 확인), `/robots.txt`(Disallow 목록+Sitemap 링크 확인), `/`(title이 "사일로 스토어"로 바뀜 확인), 좌측 사이드바 열기→Escape로 닫기→트리거 버튼에 포커스 복귀까지 직접 확인.
  - 문서 동기화: `PROJECT_BLUEPRINT.md`, `docs/EPIC.md`, `NEXT_TASK.md`, `README.md`(`NEXT_PUBLIC_SITE_URL` env 문서화).

## 2026-07-28 (EPIC-054C)
- **EPIC-054C: 모든 Page를 Board와 연결 — Board Module을 실제 게시판 데이터에 연결하고, 한 Page에 여러 Board를 배치**
  - **`src/components/modules/BoardModule.tsx` 신규**: `boardId` 하나만 받아 정의 조회(`resolveBoardDefinition`)+`posts` 조회+Search/Sort/Pagination 상태 관리(250ms 디바운스 포함)+hub 피드 조회까지 전부 스스로 처리하는 자기완결형 모듈. 기존 `src/app/boards/[id]/page.tsx`에만 있던 로직을 그대로 옮긴 것(동작 변경 없음, 리팩터). `definition.boardType`이 뭐든(story/gallery/community/hub/timeline) `BoardRenderer`가 알아서 맞는 레이아웃을 그리므로, Story/Gallery/List/Slide Board 4종 모두 이 컴포넌트 하나로 커버 — `pageModules.ts`의 `BoardModuleProps`도 `{definition, posts, ...}`(EPIC-054B 초안)에서 `{boardId, includeChildBoards?}`로 단순화했다.
  - **핵심 설계 이유**: boardId만 있으면 어디서든 재사용 가능한 자기완결형 컴포넌트라서, 여러 개를 한 Page에 나란히 배치해도 서로의 검색어/정렬/페이지 상태가 섞이지 않는다 — "Page 하나 = Board 하나" 구조를 구조적으로 강제하지 않게 되는 지점. 추후 Block Editor가 "게시판 임베드" 블록을 추가할 때도 이 컴포넌트에 boardId 하나만 넘기면 그대로 연동 가능(지시문의 "자동 연동 가능한 구조 유지" 반영).
  - **`src/app/boards/[id]/page.tsx` 축소**: 170줄짜리 조회/상태 로직을 전부 `BoardModule`로 옮기고, 페이지 자체는 `<BoardModule boardId={id} />` 하나만 렌더링하는 얇은 레이아웃 래퍼로 축소.
  - **`src/app/boards/page.tsx`(디렉토리) 재구성 — 한 Page에 여러 Board를 실제로 배치**: 기존에는 모든 게시판을 뭉뚱그린 단일 "마스터 피드"(`HUB_DEFINITION` + 전체 feed)를 `BoardRenderer`로 한 번만 그렸는데, 이를 제거하고 parent가 없는 최상위 hub(Silo Store/Online Docent/Heritage/Community/Membership/Gallery/Archive/Studio) 하나마다 `slide_board` 모듈을 만들어 `PageModuleRenderer`로 순서대로 렌더링하도록 재구성 — 한 Page 안에 Board 여러 개(최대 8개)가 각자 독립적으로 동작하는 구조를 실제로 증명한다. 아래 "게시판 허브" 바로가기 카드 그리드와 레거시 8개 그룹 링크 목록(자유게시판/패트론 라운지 등)은 게시판 콘텐츠가 아니라 순수 내비게이션이라 Board Module로 바꾸지 않고 그대로 유지.
  - **`src/components/modules/EmptyState.tsx` 신규**: "콘텐츠가 0건"이라는 상태를 위한 공용 컴포넌트(점선 테두리 카드) — `BoardRenderer`의 게시글 0건 분기(기존엔 `<p>아직 게시글이 없어요.</p>` 텍스트만)와 `PageModuleRenderer`의 모듈 0개 분기가 이 컴포넌트 하나를 공유한다. 지시문에 따라 Placeholder Module이 아니라 Empty State UI로 구분.
  - **Search/Sort/Pagination 지원 확인**: `src/lib/boardLayout.ts`의 모든 `story()`/`community()`/`timeline()` 빌더와 8개 legacy `BOARD_DEFINITIONS`(community/story/gallery 계열)는 이미 `searchable`/`sortable`/`pageable`이 전부 `true`(hub 계열만 의도적으로 `false` — 콘텐츠 목록이 아니라 피드 집계 뷰이므로 대상 아님) — 새로 바꿀 필요 없이 `BoardModule`이 이 플래그를 그대로 존중해 Search/Sort/Pagination UI를 표시한다.
  - **브라우저 검증**: 로컬 dev 서버(`npm run dev`)로 `/boards`(모듈 0개일 때 EmptyState 노출 확인, 라이브 DB에 hub 게시판 미시딩 상태라 정상) → 레거시 그룹의 실제 게시판(`자유게시판`) 진입 → 검색/정렬 UI 렌더링 확인 → 검색어 입력 시 결과가 실시간으로 걸러지고 결과 0건일 때 EmptyState가 뜨는 것까지 직접 확인.
  - 문서 동기화: `PROJECT_BLUEPRINT.md`, `docs/EPIC.md`, `NEXT_TASK.md`.
  - 검증: `npx tsc --noEmit`/`npm run lint` 통과(신규/수정 파일 관련 에러 0건 — 기존 27건은 전부 무관한 pre-existing 이슈).

## 2026-07-28 (EPIC-054B)
- **EPIC-054B: Page(화면)와 Board(게시판) 분리 — Page Module 구조 신설 (콘텐츠/Board 생성 없이 타입/렌더러 스캐폴드만)**
  - **조사**: 지시문의 16개 모듈(Hero/Story Board/Gallery Board/List Board/Slide Board/Timeline/Comment/Search/Pagination/Notice/CTA/Form/Calendar/Survey/Ranking/Profile Card) 각각에 대해 재사용 가능한 기존 컴포넌트가 있는지 전수 조사 — 9개는 기존 컴포넌트로 바로 대응 가능(Hero→`HeroSlideshow`, Story/Gallery/List/Slide Board→`BoardRenderer`, Timeline→`TimelineView`, Comment→`CommentSection`, Pagination→`Pagination`), 2개는 컴포넌트가 아니라 `BoardHeader.tsx` 안의 인라인 JSX로만 존재(Search/CTA), 6개는 재사용할 대상 자체가 없음(Notice/Form/Calendar/Survey/Ranking/Profile Card — Ranking은 NEXT_TASK.md EPIC-052 후속에 "구현 안 됨"으로 이미 기록돼 있던 항목과 동일 사안).
  - **`src/lib/pageModules.ts` 신규**: `PageModuleKind`(16종) + 모듈별 props 타입(Board 계열은 기존 `BoardDefinition`/`BoardPost`/`HubFeed`/`HubChildBoard`/`TimelineEntry`/`Comment` 타입을 그대로 import해 재사용, 새 타입 중복 정의 없음) + 판별 유니온 `PageModuleConfig`(`{id, kind, props}`) + `PageDefinition`(`{key, title, modules: PageModuleConfig[]}`). `modules`가 평면 배열이라 "자유롭게 추가/삭제/순서 변경" 요구사항이 표준 배열 연산(push/filter/재정렬)만으로 이미 충족됨 — 별도 트리/우선순위 필드 불필요.
  - **`src/components/modules/PageModuleRenderer.tsx` 신규**: `modules` 배열을 순서대로 순회하며 `kind`별로 기존 컴포넌트에 props를 그대로 전달하는 조합기. Board 계열 4종은 `BoardRenderer`(Board Definition System, EPIC-047)로, Timeline/Comment/Pagination도 각각 기존 컴포넌트로 위임 — 새 레이아웃 로직 없음.
  - **`SearchInput`/`CtaButtons` 추출**(`src/components/modules/`): `BoardHeader.tsx`가 인라인으로 그리던 검색 입력창/CTA 버튼 마크업을 그대로 뽑아낸 컴포넌트로, `BoardHeader.tsx`도 이 두 컴포넌트를 import해서 쓰도록 리팩터(렌더링 결과·클래스 동일, 중복 컴포넌트 생성 금지 원칙 반영).
  - **6개 신규 최소 셸**(`NoticeBanner`/`FormShell`/`CalendarGrid`/`SurveyCard`/`RankingList`/`ProfileCard`, 전부 `src/components/modules/`): 재사용할 기존 컴포넌트가 없는 경우에만 새로 작성 — `docs/design-system.md`의 기존 색상/카드/폼 클래스만 재사용하고, 데이터 조회·저장·검증 로직은 포함하지 않는 순수 프레젠테이션 컴포넌트(예: `SurveyCard`의 `onVote`는 콜백 시그니처만 제공, 투표 집계는 하지 않음).
  - **기존 코드에 `export` 추가(동작 변경 없음)**: `boardLayout.ts`의 `story()`/`community()`/`hub()`/`timeline()` 빌더 4개, `CommentSection.tsx`의 `Comment` 타입 — Page Module이 임시 `BoardDefinition`/댓글 타입을 재사용할 수 있게 하기 위함.
  - **범위 밖(의도적)**: 이 시스템을 사용하는 실제 Page 인스턴스, 콘텐츠, 새 Board 행은 전혀 만들지 않았다 — 어떤 `page.tsx`도 아직 `PageModuleRenderer`를 import하지 않는다. 모듈 추가/삭제/순서를 편집하는 관리자 UI도 범위 밖(`PAGE_MODULE_LABELS`만 향후를 위해 준비).
  - 문서 동기화: `PROJECT_BLUEPRINT.md`, `docs/EPIC.md`, `NEXT_TASK.md`.
  - 검증: `npx tsc --noEmit`/`npm run lint` 통과(신규/수정 파일 관련 에러 0건 — 기존 27건은 전부 무관한 pre-existing `react-hooks/set-state-in-effect` 이슈).

## 2026-07-28 (EPIC-054A)
- **EPIC-054A: 모든 상위/하위 메뉴가 실제 Page를 갖도록 정비 (페이지/URL/레이아웃 생성 + Placeholder 제거까지만)**
  - **인벤토리**: `docs/navigation-blueprint.md`(라이브 `site_navigations` DB 시드 기준 SSoT)를 근거로 사이트 전체 메뉴/서브메뉴를 조사 — `src/components/ComingSoon.tsx`(`"준비 중입니다"`만 렌더링)를 그대로 쓰는 페이지 19개를 확인(사일로 Heritage 할머니/할아버지 2개, 살롱 Community 2개/Membership 5개/Gallery 5개, 스튜디오 물품 대여/공간 스타일링 2개, nav 미연결 orphan 3개 — 투어 도슨트 프로그램/음료 주문/구 공간 촬영 대관). `navConfig.ts`의 `FALLBACK_NAV_TABS`(EPIC-044~053에서 갱신됐으나 라이브 DB에는 아직 시드되지 않은 신규 구조)는 실제 사용자에게 보이는 nav가 아니라고 판단해 대상에서 제외(NEXT_TASK.md에 별도 기록된 기존 nav-wiring gap과 동일 사안, 이번 EPIC 범위 아님).
  - **공용 `PageHeader` 컴포넌트 신규**(`src/components/PageHeader.tsx`, `ComingSoon.tsx` 대체): `title`/`subtitle`/`breadcrumb`/`description` props만 받아 Title(h1)/Subtitle/Breadcrumb(홈 › 상위 탭 › 그룹 › 항목)/Description/Page Container(`max-w-2xl mx-auto`)를 렌더링 — 게시판 연결이나 데이터 조회는 하지 않는 순수 정적 컴포넌트. 지시문에 따라 게시판 연결/기능 추가/Block Editor 수정/Board Module 생성은 하지 않음.
  - **19개 페이지 전환**: 위 placeholder 19개 전부 `<ComingSoon title="...">` → `<PageHeader title=... subtitle=... breadcrumb=[...] description=... />`로 교체. 각 페이지의 breadcrumb은 실제 nav 계층(예: `/salon/gallery/awards` → 홈 › 살롱데상 › Gallery › 시상식)을 그대로 반영. URL/라우팅은 전혀 변경하지 않음(기존 파일 내용만 교체).
  - **`ComingSoon.tsx` 삭제**: 전환 후 참조하는 곳이 없어 완전히 제거(더 이상 쓰이지 않는 컴포넌트를 남겨두지 않음).
  - **404 없음 확인**: `docs/database-schema.sql`의 실제 `site_navigations` 시드 INSERT 문(라이브 반영 확인됨, EPIC-023)에 있는 모든 href를 이번 19개 전환 결과 + 기존 실제 페이지와 대조해 하나도 빠짐없이 실제 `page.tsx`가 존재함을 확인.
  - **범위 밖으로 남겨둔 것(의도적)**: 새로 만든 정적 페이지들은 여전히 실제 데이터/게시판과 연결되어 있지 않다(지시문상 금지) — 예: `/salon/gallery/*`는 이미 Board Definition System(`Gallery` hub, EPIC-050)에 실제 콘텐츠가 있지만 이 정적 페이지들과 연결하지 않음. `navConfig.ts`의 새 Board/Community/Membership/Studio 동적 라우트가 라이브 nav에 아직 연결되지 않은 기존 nav-wiring gap도 그대로 남김.
  - 문서 동기화: `docs/navigation-blueprint.md`, `docs/content-blueprint.md`, `docs/membership-blueprint.md`, `docs/design-system.md`, `PROJECT_BLUEPRINT.md`, `docs/EPIC.md`.
  - 검증: `npx tsc --noEmit`/`npm run lint` 통과(신규 파일 관련 에러 0건 — 기존 lint 에러 27건은 전부 이번 변경과 무관한 pre-existing `react-hooks/set-state-in-effect` 이슈, NEXT_TASK.md에 이미 기록됨). Board Definition System, Block Editor, DB 스키마는 전혀 건드리지 않음.

## 2026-07-28 (EPIC-053.1)
- **EPIC-053.1: Block Editor 완성 — 운영 가능 수준으로 마무리**
  - **사전 확인**: `member_bucket_list`가 라이브 DB에 없고(EPIC-052부터 문서에만 존재) RLS도 없는 상태를 재확인 — 이번 세션도 Management API 토큰이 없어 직접 적용은 못 하고, own-row 패턴(member_collections와 동일) SELECT/INSERT/UPDATE/DELETE 정책 SQL을 `docs/sql/epic-053-1.sql`에 작성.
  - **정본을 HTML에서 Tiptap JSON으로 전환**: `src/lib/blockEditorCore.ts` 신규 — 서버(Route Handler)와 클라이언트(BlockEditor)가 동일하게 import하는 Tiptap 확장 스키마. `posts.body_json`(jsonb, 신규 컬럼)이 정본이고, `posts.body`(HTML)는 `renderPostHtml()`이 매 저장마다 JSON으로부터 다시 계산해 채우는 파생 캐시 — 클라이언트가 무엇을 보내든 실제 렌더 HTML은 서버가 재계산 후 DOMPurify로 새니타이즈한다(Stored XSS 이중 방어 + "정본은 JSON" 원칙을 서버가 강제). EPIC-053이 만든 `src/lib/blocks.tsx`(커스텀 Block[] 타입 시스템)는 끝내 에디터와 연결되지 않은 죽은 코드였음을 확인하고 삭제 — Tiptap의 ProseMirror JSON 자체가 이미 "타입+속성을 가진 블록 트리"이므로 병행 트리를 새로 발명하지 않는 쪽이 Tiptap 권장 패턴에 맞고 유지보수하기 쉽다(아래 운영 리뷰 참고).
  - **커스텀 Block 노드**: `FigureImage`(이미지 1장 — 캡션/ALT/삭제/순서변경(드래그+버튼)/대표이미지 지정), `Gallery`(여러 장 그리드 — 추가/삭제/순서변경/열 수 조절), `Embed`(Youtube/Vimeo/Instagram/Spotify/Google Maps — URL 입력 시 실제 iframe Preview), `LinkCard`(외부 링크 카드) — 전부 React NodeView(`src/components/editor/BlockEditor.tsx`)로 편집 가능. 기본 `@tiptap/extension-image`는 `FigureImage`로 완전히 대체(이미지 표현이 두 갈래로 나뉘지 않게).
  - **Lightbox**: `src/components/editor/Lightbox.tsx` 신규 — 슬라이드(이전/다음, 키보드 방향키), 클릭 확대, ESC/배경 클릭 닫기. 에디터 미리보기와 실제 게시글 상세(`PostBody.tsx`, dangerouslySetInnerHTML 위에 이벤트 위임으로 클릭 감지) 양쪽에서 재사용.
  - **대표 이미지**: `posts.featured_image_url`/`featured_image_path`(신규 컬럼) — 본문 이미지에서 ★로 지정하거나 별도 업로드. 미지정 시 본문 첫 이미지로 폴백. `BoardRenderer`/`StoryCard`/`PostDetailHeader`가 `photo_url`보다 이 값을 우선 사용해 썸네일에 반영.
  - **Preview**: BlockEditor 툴바의 "미리보기" 토글 — 별도 렌더러를 새로 만들지 않고 `editor.getHTML()`(실제 게시글과 동일한 sanitize 경로) 결과를 그대로 보여준다.
  - **게시글 수정 화면 신설**: `posts.updated_at`은 있었지만 실제 수정 UI/API가 없었음(스키마 주석에 명시) — `src/app/boards/[id]/[postId]/edit/page.tsx` + `PATCH /api/boards/[id]/posts/[postId]` 신규. 본인(또는 관리자)만 수정 가능, `body_json`을 그대로 에디터에 복원. 글쓰기/수정 화면이 새 `PostForm`(`src/components/boards/PostForm.tsx`) 하나를 공유(새 Editor 생성 금지 원칙 — 중복 코드 없음). 게시글 상세(`PostDetailHeader`)에 작성자 전용 "수정" 링크 추가.
  - **Storage Garbage Collection**: 즉시 삭제 대신 `image_cleanup_queue`(신규 테이블) 큐잉 — 게시글 수정 시 이전 본문에서 빠진 이미지(`src/lib/imageGc.ts`), 아직 다루지 않은 삭제 플로우는 설계만(관련 테이블/정책은 준비됨). 실제 삭제는 관리자 전용 `POST /api/admin/storage-cleanup`(이 앱은 service-role 키를 쓰지 않아 admin 세션으로만 Storage 삭제 가능 — 한 번에 최대 50건, 스케줄러는 미구현).
  - **Storage Bucket + Policy SQL**: `docs/sql/epic-053-1.sql`에 `post-images`/`gallery`/`attachments` 버킷 생성 + 정책(공개 읽기/로그인 사용자 업로드/관리자 또는 업로드 본인 삭제) 작성 — 아직 어떤 버킷도 라이브 프로젝트에 없어 실행 전에는 이미지 업로드 자체가 실패한다.
  - **정리**: 미사용 `RichTextEditor.tsx`(EPIC-052, EPIC-053부터 아무도 import하지 않음), `src/lib/blocks.tsx`(위 참고) 삭제. `sanitize.ts`에 새 노드 렌더링용 속성(`data-featured`/`data-provider`) 허용 추가.
  - **운영 리뷰에서 발견한 기존 버그(수정)**: `POST /api/boards/[id]/drafts`(EPIC-053, 어떤 화면도 호출하지 않는 죽은 코드)가 `posts.visibility='draft'`로 insert를 시도하는데 기존 CHECK 제약(`public`/`private`/`friends`만 허용)이 항상 이를 막고 있었다 — 제약을 넓히는 SQL을 포함(라우트 자체는 여전히 미사용, NEXT_TASK.md 참고).
  - 검증: `npx tsc --noEmit`/`npm run lint`(신규 파일 0건, 기존 저장소 전반의 pre-existing `react-hooks/set-state-in-effect` 25건은 그대로 — CLAUDE.md P2 정책상 이번 범위 밖) 통과, `npm run build` 성공. 실제 dev 서버 + Browser에서 글쓰기 화면 로드, 툴바 전체 렌더링, 본문 타이핑, 미리보기 토글(sanitize된 HTML로 정상 전환), 임베드 메뉴(5개 프로바이더 노출), localStorage 자동 저장("마지막 저장"/"자동 저장됨" 타임스탬프 갱신)까지 직접 확인. 이미지 업로드/갤러리/Lightbox/게시글 수정은 Storage Bucket과 신규 컬럼이 라이브 DB에 아직 없어(위 SQL 미실행) 실제 업로드까지는 확인하지 못함 — SQL 실행 후 재확인 필요.

## 2026-07-28 (EPIC-053)
- **EPIC-053: 공통 Block Editor 시스템 구축**
  - **Block Editor 확장(RichTextEditor → BlockEditor)**: `src/components/editor/BlockEditor.tsx`로 전면 재작성. EPIC-052의 기본 Tiptap에서 다음 확장:
    - **Toolbar 확장**: 굵게/기울임/밑줄/취소선, H1/H2/H3 제목, 글머리/번호/체크리스트, 인용/구분선, 좌/중앙/우 정렬, 링크, 이미지 업로드 버튼
    - **이미지 업로드**: Drag & Drop, 붙여넣기(Ctrl+V), 여러 장 업로드, Supabase Storage 자동 업로드
    - **자동 저장**: 30초 간격 auto-save + localStorage 임시 저장(브라우저 닫기 복구)
    - **체크리스트**: `@tiptap/extension-task-list`/`@tiptap/extension-task-item` 설치 및 연동
    - **텍스트 스타일**: 밑줄(`@tiptap/extension-underline`), 정렬(`@tiptap/extension-text-align`), 형광펜(`@tiptap/extension-highlight`), 색상(`@tiptap/extension-color`)
  - **Storage 유틸리티**: `src/lib/storage.ts` 신규 — `post-images`/`gallery`/`attachments` 버킷용 업로드/삭제 유틸리티 (URL + Storage Path 반환)
  - **Block 타입 정의**: `src/lib/blocks.tsx` 신규 — Block 구조(JSON) + HTML 변환 + React 렌더러 (향후 확장 대비)
  - **임시 저장 API**: `POST /api/boards/[id]/drafts` — 서버 사이드 임시 저장 지원
  - **Sanitize 확장**: `src/lib/sanitize.ts` 확장 — img/figure/figcaption/video/audio/iframe/object/mark/div/span/table 허용 (YouTube/Vimeo/Maps/Spotify 임베드対応)
  - **글쓰기 페이지 연동**: `src/app/boards/[id]/write/page.tsx` — BlockEditor로 교체, 자동 저장 콜백 연결, 임시 저장 복구 로직 추가
  - **패키지 설치**: `@tiptap/extension-underline`, `@tiptap/extension-text-align`, `@tiptap/extension-text-style`, `@tiptap/extension-highlight`, `@tiptap/extension-color`, `@tiptap/extension-image`, `@tiptap/extension-task-list`, `@tiptap/extension-task-item`
  - 검증: `npx tsc --noEmit` 통과. `npm run lint` EPIC-053 관련 파일 전부 통과.

## 2026-07-28 (EPIC-052)
- **EPIC-052: 마이페이지를 Personal Hub로 확장 + Tiptap Block Editor 도입**
  - **사전 확인(AskUserQuestion)**: 이번 지시문은 두 가지 큰 갈림길이 있어 진행 전 사용자에게 직접 확인함 — (1) "나의 컬렉션"을 Board Definition의 공개 story 게시판으로 만들지, 아니면 지금처럼 비공개(member_collections)로 두고 시각적으로만 story 카드 스타일을 적용할지 → **비공개 유지 + 시각적 재사용**으로 결정. (2) Tiptap/Lexical Block Editor 도입을 이번 EPIC에 포함할지 → **지금 바로 Tiptap 통합 시작**으로 결정. 아래 항목은 전부 이 두 결정을 전제로 한다.
  - **Tiptap Block Editor 도입(엔진 레벨)**: `@tiptap/react`/`@tiptap/starter-kit`/`@tiptap/extension-link`/`@tiptap/extension-placeholder`/`@tiptap/pm`/`isomorphic-dompurify` 추가. `src/components/editor/RichTextEditor.tsx`(굵게/기울임/제목/목록/인용/링크 툴바) 신규 — 특정 게시판 전용이 아니라 `src/app/boards/[id]/write/page.tsx` 단 하나(모든 Board Definition 게시판이 공유하는 글쓰기 폼)에서 textarea를 대체해, 게시판이 몇 개가 늘어도 이 컴포넌트 하나만 재사용된다("향후 수십 개 게시판에서 재사용" 요구 반영).
    - **저장 형식**: `posts.body`는 컬럼 변경 없이 Tiptap의 `getHTML()` 결과(HTML 문자열)를 그대로 저장 — 새 JSON 컬럼을 추가하지 않아 스키마 변경이 필요 없다.
    - **보안(XSS)**: 클라이언트 에디터를 거치지 않고 API를 직접 호출해도 안전하도록 `src/lib/sanitize.ts`(`sanitizeHtml`/`stripHtml`, DOMPurify 기반 허용 태그 화이트리스트)를 만들어 `POST /api/boards/[id]/posts`에서 저장 직전 서버 측 정제를 수행(클라이언트 검증만 믿지 않음). 상세 페이지 렌더링(`PostBody.tsx`)에서도 한 번 더 정제하는 이중 방어.
    - **레거시 호환**: 이전에 plain text로 저장된 글과 새 HTML 글이 같은 컬럼에 섞여 있어, `PostBody.tsx`가 태그 포함 여부(`/<[a-z][\s\S]*>/i`)로 렌더링 방식을 자동 분기(HTML이면 정제 후 렌더링, 아니면 기존처럼 줄바꿈만 유지) — 마이그레이션 스크립트 없이 하위 호환.
    - **목록 요약 수정**: `BoardRenderer.tsx`의 story 카드 요약이 HTML 태그를 그대로 노출하던 문제를 `stripHtml()`로 수정.
  - **"나의 컬렉션"을 story 카드로 시각적 통일**: `src/components/boards/StoryCard.tsx` 신규(공용 프레젠테이션 컴포넌트) — `BoardRenderer.tsx`의 story 레이아웃과 마이페이지 `CollectionCategoryPanel.tsx`가 이 컴포넌트 하나를 공유(중복 UI 없음). `member_collections`/`orders`(나의 보물) 스키마·공개 범위는 전혀 바꾸지 않음 — 여전히 본인만 보는 비공개 데이터.
  - **Timeline Engine 추출 + 실제 재사용**: 기존 "타임라인" 게시판(`BoardRenderer.tsx`) 안에 갇혀 있던 연/월 그룹핑 로직을 `src/lib/timelineEngine.ts`(`groupByYearMonth`)+`src/components/TimelineView.tsx`(공용 렌더러)로 분리 — 게시글이 아닌 마이페이지 활동 로그도 "정렬된 `{id, createdAt}` 목록"만 맞추면 그대로 재사용 가능. `/mypage/timeline`(`TimelinePanel.tsx`)이 이를 실제로 사용해 `points_ledger`(글 작성/댓글/좋아요 받음/개념글 승격/상점 구매·대여/공간 대관/클럽 참여/출석)+`likes`(내가 준 좋아요)+`member_follows`(팔로우)를 종합 표시 — 새 활동 로그 테이블 없이 기존 3개 테이블만 조합.
  - **PlaceholderPanel 4개를 실제 데이터로 교체**("단순 Placeholder 페이지를 만들지 않는다" 반영):
    - **나의 살롱**(`SalonPanel.tsx`): `reservations`(클럽 예약)+`salon_checkins`(체크인)+`daily_checkins`(출석)+`poll_votes`(설문 참여) 4개 기존 테이블 종합.
    - **나의 도슨트 수료증**(`DocentCertificatePanel.tsx`): 별도 수료증/진행률 테이블이 없어(스키마 추측 금지) 결제 확정된 `docent_purchases`를 "수료 완료"로 재해석 — 진행률(%)은 강의 모듈 개념 자체가 없어 표시하지 않고 완료/대기 건수만 표시.
    - **나의 공간**(`SpacePanel.tsx`): `rental_bookings`(대관 예약)를 그대로 조회. 스타일링은 고객 신청을 기록하는 테이블이 없어(`styling_projects`는 관리자 포트폴리오) Studio 게시판으로 안내하는 링크만 제공.
    - **나의 전시회**(`ExhibitionPanel.tsx`): 전용 전시 테이블 없이 기존 마이피드 API(`GET /api/members/[memberId]/posts`, `board_id IS NULL` 개인 글)를 재사용해 사진 있는 글만 필터링 — 새 쿼리/테이블 없음.
  - **기존 실제 기능 강화**: `CommentsPanel.tsx`에 "원글 이동" 링크 추가(select에 `post_id`/`board_id` 확장) + 원글 좋아요 수 표시. `BadgesPanel.tsx`에 보유 배지 수 표시 — "전체 랭킹"/"다음 배지 진행률"은 (1) `member_badges`가 본인 행만 읽는 RLS라 다른 회원과의 비교를 클라이언트에서 계산할 수 없고 (2) 배지 획득 조건을 정의하는 규칙 테이블이 없어 보류(NEXT_TASK.md). `FollowPanel.tsx`는 "향후 알림 시스템 연동 가능하도록 설계" 요구를 `member_follows`의 기존 follower/following 분리 구조가 이미 충족한다고 보고 주석으로만 문서화(코드 변경 없음).
  - **버킷리스트(신규 기능)**: `member_bucket_list`(연도/제목/완료 여부, own-row 전용) 신규 테이블 + `BucketListPanel.tsx`(추가/체크/삭제, 연도별·전체 완료율) + `/mypage/bucketlist` 라우트 — `mypageConfig.ts`의 `MYPAGE_TABS`에 탭 추가(기존 11개 탭 순서/URL 변경 없음).
  - **DB(신규 1테이블)**: `member_bucket_list`. 작업 전 `docs/database-schema.sql`을 `docs/backups/database-schema-20260728-0100.sql`로 백업. 그 외 스키마는 전부 기존 재사용(신규 컬럼/제약 변경 없음).
  - 기존 URL/라우트 전부 유지 — `/mypage/*` 11개 경로 그대로, `/mypage/bucketlist` 1개만 추가. 게시판 쪽(`/boards/**`)도 라우팅 변경 없음(글쓰기 폼 내부 컴포넌트만 교체).
  - 문서 동기화: `docs/database-schema.sql`, `PROJECT_BLUEPRINT.md`, `docs/EPIC.md`.
  - 검증: `npx tsc --noEmit` 통과. `npm run lint` 27건(기존 26건 + 신규 1건) — 신규 1건은 `BucketListPanel.tsx`의 `set-state-in-effect`로, 이미 저장소 전반에 퍼진 동일 클래스의 pre-existing P2 이슈와 같은 성격(CLAUDE.md 에러 트리아지 정책상 기능을 막지 않음, NEXT_TASK.md 기록). 다른 세션이 3000번 포트를 점유 중이고 `member_bucket_list` 시드도 라이브 미적용이라 브라우저로 Tiptap 에디터 실제 타이핑/버킷리스트 CRUD/각 패널의 실데이터 렌더링을 확인하지 못함 — 사용자 확인 필요(NEXT_TASK.md 참고).

## 2026-07-27 (EPIC-051)
- **EPIC-051: Studio(공간 문의) 게시판 생성 + 기존 예약 Flow 연동(BoardDefinition.ctas)**
  - Board Definition System으로 게시판 5개(전부 신규 DB 행)를 추가 — 최상위 hub `Studio`(Silo Store/Online Docent/Heritage/Community/Membership/Gallery/Archive와 형제) + 하위 4개 story 게시판: `공간 촬영 대관(1층)`(studio-1f)/`공간 촬영 대관(2층)`(studio-2f)/`물품 대여`(rental)/`공간 스타일링`(styling).
  - **`BoardDefinition`에 `ctas` 필드 추가**: `{label, href}[]` — "문의하기"/"예약하기" 같은 액션 버튼을 config로 지정하면 `BoardHeader`가 그대로 링크 버튼으로 렌더링한다. **새 예약 시스템/컴포넌트를 전혀 만들지 않고**, 이미 있는 실제 페이지로만 연결: `studio-1f`→`/rental?floor=1f_silostore`, `studio-2f`→`/rental?floor=2f_salon`, `rental`→`/space-inquiry/item-rental`, `styling`→`/shop/projects`(대표 프로젝트) + `/space-inquiry/styling`(문의/신청). 세 페이지 모두 기존에 실제로 존재하는 라우트임을 코드로 직접 확인 후 연결.
  - **"문의하기"/"예약하기"가 같은 페이지로 연결되는 이유(판단 필요 사항)**: 이 프로젝트에는 예약과 별개인 "단순 문의" 전용 채널이 없다 — 새 시스템을 만들지 않기 위해(주의사항) 두 버튼을 지시대로 둘 다 표시하되, 둘 다 같은 실제 예약/문의 Flow(`/rental?floor=...`, `/space-inquiry/*`)로 연결했다. 별도의 문의 전용 폼이 필요하면 사용자 확인 후 별도 작업 필요.
  - **"공간 스타일링"의 "대표 프로젝트"/"프로젝트 슬라이드" 처리(판단 필요 사항)**: 지시대로 `styling_projects`용 새 DB/컴포넌트를 만들지 않고, 이미 그 테이블을 그대로 조회해 보여주는 기존 `/shop/projects`(목록, industry 필터) 페이지를 `ctas`의 "대표 프로젝트 보기" 링크로 재사용했다 — `styling` 게시판 자체는 소개/절차 등 일반 콘텐츠(posts)만 담당.
  - **hub 슬라이드에 대표 이미지 표시(EPIC-051, 이 EPIC에서만 추가된 개선)**: `Studio` hub가 "최신 포트폴리오/대표 이미지/추천 콘텐츠"를 슬라이드로 보여줘야 해서, `HubFeedItem`에 `photo_url` 필드를 추가하고 `/api/boards/feed`가 이를 select+반환하도록, `FeedSlide`(BoardRenderer.tsx)가 있으면 썸네일을 렌더링하도록 확장 — 기존 hub(Silo Store 등)는 `photo_url`이 없는 글도 많아 하위 호환(썸네일 없으면 기존처럼 텍스트만).
  - **"모든 Studio 서비스에 공통" 목록이 이전 EPIC들보다 짧은 점(판단 필요 사항)**: 지시문의 "공통" 목록(검색/정렬/페이지네이션/공유/북마크/좋아요/태그)에 글쓰기 버튼/댓글/조회수/작성자/작성일/수정일이 빠져 있었지만, "실제 콘텐츠를 등록하고 운영할 수 있도록"이라는 상위 목표와 모순되지 않도록 글쓰기(공간 소개/이용 안내/FAQ 등록 수단)는 유지했다 — 나머지(댓글/조회수 등)도 기존 `story()` 헬퍼 그대로 켜져 있어 다른 story 게시판과 동일하게 동작(제외하라는 명시적 지시는 없었다고 판단).
  - DB(신규 5행): `board_type='topic'` 재사용, `category`에 slug. 작업 전 `docs/database-schema.sql`을 `docs/backups/database-schema-20260727-2205.sql`로 백업.
  - **재사용한 기존 기능**: `/rental`+`rental_types`+`rental_bookings`(공간 대관 예약), `/space-inquiry/item-rental`·`/space-inquiry/styling`(기존 문의 placeholder 페이지), `/shop/projects`+`styling_projects`(스타일링 포트폴리오) — 전부 코드/스키마 변경 없이 링크만 추가.
  - 기존 게시판(그룹 8종 + EPIC-048~050의 68개)의 동작은 변경 없음. 기존 라우팅과 URL 전부 유지.
  - 문서 동기화: `docs/database-schema.sql`, `docs/content-blueprint.md`, `docs/navigation-blueprint.md`, `PROJECT_BLUEPRINT.md`, `docs/EPIC.md`.
  - 검증: `npx tsc --noEmit`/`npm run lint`(26건, EPIC-050과 동일 — 신규 이슈 없음) 통과. 다른 세션이 3000번 포트를 점유 중이고 DB 시드도 라이브 미적용이라, 브라우저로 실제 문의/예약 버튼 클릭 이동과 hub 썸네일 노출을 확인하지 못함 — 사용자 확인 필요(NEXT_TASK.md 참고).

## 2026-07-27 (EPIC-050)
- **EPIC-050: Salon des Cent Membership/Gallery/Archive 게시판 생성 + Timeline Engine + 실제 인가 연결**
  - Board Definition System으로 게시판 17개(전부 신규 DB 행)를 추가 생성 — 새 페이지/컴포넌트 없이 `src/lib/boardLayout.ts`에 정의만 추가. 구조: 최상위 hub 3개(`Membership`/`Gallery`/`Archive`, Silo Store/Online Docent/Heritage/Community와 형제). `Membership` 하위 6개(나의 보물 이야기 story/나의 아티스트 소개/마음일기/패트론 게시판/한문장 소설 프로젝트/비밀의 방 도슨트). `Gallery` 하위 5개 story(시상식/공연들/파티/운명의 방문자들/패트론들). `Archive` 하위 3개(소개지 story/포스터 story/타임라인 timeline).
  - **5번째 boardType "timeline" 추가**: `BoardLayoutType`에 `"timeline"` 추가하고 `BoardRenderer.tsx`에 `TimelineView`+`groupPostsByYearMonth`를 실제 구현(연→월→일 순 그룹핑, 반응형). **Timeline Engine은 독립 컴포넌트가 아니라 BoardRenderer.tsx 내부의 재사용 가능한 함수**로 유지 — "정렬된 `{created_at, title, ...}` 목록"에만 의존하므로, 향후 마이페이지 타임라인 탭(현재 `PlaceholderPanel`)이 같은 그룹핑 로직을 그대로 가져다 쓸 수 있는 구조(이번 EPIC에서 mypage 쪽 연동까지 하지는 않음 — NEXT_TASK.md 참고).
  - **`accessLevel` 필드 추가 + "패트론 게시판"에 실제 인가 연결(판단 필요 사항)**: `BoardDefinition`에 `accessLevel?: "patron" | "secret_room"` 추가. 지시문이 "패트론 게시판"엔 "멤버십 권한 적용"(현재형, 적용하라는 지시)이라 표현한 반면 "비밀의 방 도슨트"엔 "accessLevel만 지정"(구조만 유지하라는 지시)이라고 구분해서 표현한 것으로 판단해, 두 게시판을 다르게 처리했다:
    - **패트론 게시판**: `src/lib/serverAuth.ts`의 `canReadBoard`/`canWriteToBoard`를 확장해 `resolveBoardDefinition(board).accessLevel === "patron"`이면 실제 `board_type`(재사용한 `'topic'`)과 무관하게 `membership_tiers.board_has_patron_board` 플래그로 읽기/쓰기 모두 막는다 — board_type 하나만 보고 게이팅하던 기존 로직의 사각지대(새 게시판이 다 `'topic'`을 재사용하다 보니 패트론 전용으로 만들 방법이 없었음)를 config 기반으로 메운 것. 기존 8개 그룹(특히 원본 `patron` board_type 게시판)의 동작은 그대로.
    - **비밀의 방 도슨트**: `accessLevel:"secret_room"`만 지정하고 `canReadBoard`/`canWriteToBoard`에는 아무 분기도 추가하지 않음 — 지금은 다른 일반 게시판과 동일하게 전체 공개(구조만 준비, 실제 시험/권한 로직은 `salon_rooms`/`salon_room_access`와의 연동 여부를 사용자와 논의 후 진행, NEXT_TASK.md 참고).
  - **"단순 Placeholder를 만들지 않는다" 확인**: 이 17개 게시판은 다른 EPIC-047~049 게시판과 동일하게 실제 `posts`/`comments`/`likes`/`post_bookmarks` 테이블 기반으로 글쓰기·좋아요·댓글·북마크·태그·검색·정렬·페이지네이션이 전부 즉시 동작한다 — UI만 있는 정적 화면이 아니라 실제 서비스 가능한 게시판(단, 아래 "구조만 유지" 항목들은 예외).
  - **"Gallery" 5개는 기존 ComingSoon 페이지와 같은 주제(판단 필요 사항)**: `docs/content-blueprint.md`에 "전부 미구현(ComingSoon)"으로 기록돼 있던 `/salon/gallery/{awards,performances,parties,visitors,patrons}` 5개 서브페이지와 동일한 주제를 이번에 실제 게시판으로 구현했다. 지시문에 기존 페이지를 대체/연결하라는 내용이 없고 "새 React 페이지 생성 금지"만 있어, **기존 ComingSoon 페이지 자체는 건드리지 않았다** — 실제 콘텐츠는 새 `/boards/[id]` 게시판이 담당하고, 내비게이션 연결은 별도 작업으로 남김(NEXT_TASK.md 참고).
  - **"나의 보물 이야기"/"패트론 게시판"의 실제 데이터 연동은 하지 않음**: 지시문의 "연동 가능한 구조를 유지한다"를 "지금 연동하라"가 아니라 "나중에 연동할 여지를 남겨두라"로 해석 — 마이페이지 컬렉션, 등급 시스템 어느 쪽도 이 게시판들의 `posts`와 실제로 조인/동기화하지 않는다.
  - **DB(신규 17행)**: 전부 새 게시판이라 재사용할 기존 행이 없음(EPIC-049와 달리) — `board_type='topic'` 재사용, `category`에 slug. 작업 전 `docs/database-schema.sql`을 `docs/backups/database-schema-20260727-2155.sql`로 백업.
  - 기존 게시판(그룹 8종 + EPIC-048/049의 51개)의 동작은 변경 없음. 기존 라우팅과 URL 전부 유지.
  - 문서 동기화: `docs/database-schema.sql`, `docs/content-blueprint.md`, `PROJECT_BLUEPRINT.md`, `docs/EPIC.md`.
  - 검증: `npx tsc --noEmit`/`npm run lint`(26건, EPIC-049와 동일 — 신규 이슈 없음) 통과. 다른 세션이 3000번 포트를 점유 중이고 DB 시드도 라이브 미적용이라, 브라우저로 타임라인 렌더링/패트론 게시판 실제 잠금 동작을 확인하지 못함 — 사용자 확인 필요(NEXT_TASK.md 참고).

## 2026-07-27 (EPIC-049)
- **EPIC-049: Salon des Cent Community 영역 게시판 생성 — Board Definition만 추가**
  - Board Definition System(EPIC-047/048)을 재사용해 Community 영역 게시판을 생성 — 새 페이지/컴포넌트 없이 `src/lib/boardLayout.ts`의 `INDIVIDUAL_BOARD_DEFINITIONS`에 정의 31개(신규 게시판 정의) 추가. 구조: 최상위 hub `Community`(Silo Store/Online Docent/Heritage와 형제) 아래 `출석체크/예술가의 달력`(attendance)·`자유게시판`(free)·`주제별 소통 게시판`(salon-topics, hub)·`요일별 클럽`(salon-weekday, hub)·`월별 모임`(monthly-salon)·`설문 [우리들 맴]`(survey, hub)·`공연/전시회 소개`(events, story)·`이벤트 공지`(notice, story)·`Q&A 고민 게시판`(qna) 9개, 그 아래 `salon-topics`의 자식 13개(경제/예술/세계역사/과학/코메디/문학/건강/정치/영화/심리/스포츠/인간 집사들/따듯한 세상 클럽)와 `salon-weekday`의 자식 7개(월요반란/책 낭송/행간의 조각가/놀아보자 영어클럽/비포 선라이즈/무슨일이든 일어날수있어/연극이 끝나고 난 뒤).
  - **"기존 DB 최대 재사용"을 문자 그대로 적용(판단 필요 사항)**: 13개 클럽 + 7개 요일별 모임방은 EPIC-018 이전부터 이미 `boards`에 시딩돼 있던 진짜 board_type='topic'/'group' 행이라, **새 DB 행을 만들지 않고** 기존 `category` 값(economy/art/history/.../mon/tue/.../sun)을 그대로 slug로 참조하는 개별 `BoardDefinition`만 추가했다. `resolveBoardDefinition()`은 개별 slug 매칭을 그룹 매칭보다 먼저 시도하므로, 이 20개 게시판은 이제 기존 "클럽 주제 게시판"/"모임별 게시판" 그룹 대신 `salon-topics`/`salon-weekday` hub의 자식으로 재분류된다 — DB 행/URL은 완전히 그대로지만, `/boards` 최상위 디렉토리의 평면 그룹 목록에서는 빠지고(같은 항목이 두 곳에 중복 표시되지 않도록) `Community → 주제별 소통 게시판`/`Community → 요일별 클럽` hub를 통해서 찾게 된다(시각적 재구성, 데이터 변경 없음).
  - **신규 DB 행은 10개뿐**: `community`/`attendance`/`free`/`salon-topics`/`salon-weekday`/`monthly-salon`/`survey`/`events`/`notice`/`qna` — `board_type='topic'` 재사용, `category`에 slug. 작업 전 `docs/database-schema.sql`을 `docs/backups/database-schema-20260727-2145.sql`로 백업.
  - **"category='qna'" 신규 게시판과 기존 "질문과 답변"(board_type='qna', category=null) 게시판은 서로 다른 별개 게시판이다** — 지시문이 Community 하위에 별도의 "Q&A 고민 게시판"을 요구해 slug 문자열이 우연히 같을 뿐, `resolveBoardDefinition()`은 `category`가 null인지 여부로 정확히 구분한다(충돌 없음, 코드로 직접 확인).
  - **"출석 체크"/"오늘의 예술가·음악가·..." 및 "각 모임방 추가 기능"(주간 참석 신청/참석 버튼/참석 인원 표시/주간 일정 표시) 처리(판단 필요 사항)**: 새 컴포넌트를 만들지 않기 위해(BoardRenderer만 사용 원칙) 전용 UI를 구현하지 않고, 각 게시판 설명(`description`)에 의도만 기록 — 실제 콘텐츠는 해당 게시판에 일반 글(제목/본문/태그)로 작성하는 방식으로 대체한다. 기존 `/attendance`(`daily_checkins`)나 `club_sessions`/`reservations`와는 별개 시스템이며 연동하지 않음(지시문의 "배지/포인트 연동 가능하도록 구조 유지"는 지금 당장 연동하라는 뜻이 아니라 향후 확장 여지만 남겨두라는 의미로 해석).
  - **"설문 [우리들 맴]"(survey, hub) 처리(판단 필요 사항)**: "설문 카드/종료일/참여자 수/진행중·종료" 전용 카드 UI는 새 컴포넌트가 필요해 구현하지 않고 표준 hub 레이아웃만 적용. 지시문에 이 hub의 하위 게시판이 명시돼 있지 않아 자식 게시판 없이 생성(하위 설문 게시판이 추가되면 자동으로 피드에 집계됨).
  - `/boards`(최상위 디렉토리)의 "게시판 허브" 바로가기 섹션이 `parent === null`인 hub만 보이도록 필터 수정 — Community 하위의 중첩 hub(salon-topics/salon-weekday/survey)까지 평면으로 다 나열되면 산만해지므로, 최상위 4개 hub(Silo Store/Online Docent/Heritage/Community)만 노출하고 중첩 hub는 각자의 부모 hub 카드를 통해 접근한다.
  - 기존 게시판(그룹 8종 + EPIC-048의 20개)의 동작은 변경 없음. 기존 라우팅과 URL 전부 유지.
  - 문서 동기화: `docs/database-schema.sql`, `docs/content-blueprint.md`, `PROJECT_BLUEPRINT.md`, `docs/EPIC.md`.
  - 검증: `npx tsc --noEmit`/`npm run lint`(26건, EPIC-048과 동일 — 신규 이슈 없음) 통과. 다른 세션이 3000번 포트를 점유 중이고 DB 시드도 라이브 미적용이라 브라우저로 실제 노출을 확인하지 못함 — 사용자 확인 필요(NEXT_TASK.md 참고).

## 2026-07-27 (EPIC-048)
- **EPIC-048: Silo Store 실제 게시판 20개 생성 — Board Definition만 추가**
  - EPIC-047에서 구축한 Board Definition System을 실제로 사용해 게시판 20개(hub 3개: Silo Store/Online Docent/Heritage, story 17개: 사일로 보물들/보물 목록/입양신청서 라이브러리/분양 후기/시대별 11개(르네상스~디지털)/Grandmas/Grandpas)를 생성. **새 React 페이지/컴포넌트를 만들지 않고**, `src/lib/boardLayout.ts`에 `INDIVIDUAL_BOARD_DEFINITIONS` 레지스트리(개별 게시판 20개 config)만 추가하고 기존 `/boards/[id]` 라우트+`BoardRenderer`가 그대로 소화하도록 함.
  - **개별 게시판 slug 매칭(스키마 변경 없이 처리, 판단 필요 사항)**: `boards.board_type`에 새 값을 추가하려면 CHECK 제약을 ALTER해야 해서(스키마 변경 최소화 원칙에 반함), 기존 `board_type='topic'`을 그대로 재사용하고 이미 자유 텍스트인 `category` 컬럼에 각 게시판의 slug(`treasures`/`items`/`renaissance`/`grandmas` 등)를 담았다. `resolveBoardDefinition()`이 `category`를 개별 slug 레지스트리에서 먼저 찾고, 없으면 기존 8개 그룹 로직으로 폴백 — 기존 게시판 동작에 영향 없음.
  - **BoardRenderer가 hub를 실제로 렌더링하도록 확장**: 이전(EPIC-047)에는 `hub`를 community로 대체 렌더링하는 자리표시자였는데, 이번에 `HubView`(최신글/인기글/추천글 슬라이드 3개 + 하위 게시판 카드)를 실제로 구현 — `BoardRenderer`가 `hubFeed`/`hubChildBoards` prop을 받아 그린다. `/boards/[id]/page.tsx`는 게시판이 hub면 `/api/boards/[id]/posts` 대신 `/api/boards/feed?parent=<slug>` + `/api/boards`(자식 필터)를 불러오도록 분기(같은 파일 안에서 분기 — 새 페이지 없음).
  - **최상위 `/boards`도 BoardRenderer로 통일**: 기존에 `/boards/page.tsx`가 자체적으로 갖고 있던 `FeedCardRow`/`FeedList`(중복 컴포넌트)를 제거하고 `BoardRenderer`(hub 케이스)를 그대로 재사용 — "BoardRenderer만 사용한다" 지시를 프로젝트 전체에 일관 적용. 그룹별 게시판 디렉토리(8개 그룹) 섹션은 그대로 유지하고, 새로 생긴 3개 hub로 이동할 수 있는 "게시판 허브" 바로가기 섹션을 추가.
  - **`/api/boards/feed`에 `?parent=<slug>` 필터 추가**: 파라미터가 없으면 기존처럼 전체 게시판 대상(최상위 `/boards`용), 있으면 그 slug를 부모로 둔 자식 게시판만 집계(Silo Store/Online Docent/Heritage 개별 hub용).
  - **"보물 목록(items)"의 "카테고리 필터" 처리(판단 필요 사항)**: 새 필터 UI 컴포넌트를 만들지 않기 위해(중복 컴포넌트 생성 금지), 이미 있는 검색(제목/본문/작성자/태그) 기능을 그대로 활용 — 글쓰기 시 물품 카테고리를 태그로 등록하면 검색창에 그 카테고리명을 입력하는 것이 곧 필터 역할을 한다. 전용 드롭다운 필터가 필요하면 별도 확인 필요.
  - **DB(최소 변경, board_type CHECK 제약 그대로 유지)**: `boards`에 새 행 20개 INSERT(`board_type='topic'`, `category`=slug) — 라이브 미적용, Supabase SQL Editor에서 직접 실행 필요. 작업 전 `docs/database-schema.sql`을 `docs/backups/database-schema-20260727-2132.sql`로 백업.
  - 기존 8개 게시판 그룹의 동작(검색/정렬/페이지네이션/좋아요/댓글/북마크/태그)은 변경 없음. 기존 라우팅과 URL 전부 유지.
  - 문서 동기화: `docs/database-schema.sql`, `docs/content-blueprint.md`, `PROJECT_BLUEPRINT.md`, `docs/EPIC.md`.
  - 검증: `npx tsc --noEmit`/`npm run lint`(26건, EPIC-047과 동일 — 신규 이슈 없음) 통과. 다른 세션이 3000번 포트를 점유 중이라 브라우저로 hub 슬라이드/하위 게시판 카드/신규 20개 게시판 목록 노출을 직접 확인하지 못함 — 사용자 확인 필요(NEXT_TASK.md 참고). 또한 DB 시드가 라이브에 아직 없어, 실행 전까지는 신규 20개 게시판이 실제로 화면에 보이지 않는 것이 정상.

## 2026-07-27 (EPIC-047)
- **EPIC-047: Common Board Engine — BoardRenderer 기반 community/story/gallery/hub**
  - 게시판별로 화면을 개별 구현하지 않도록, `src/lib/boardLayout.ts`의 `getBoardLayoutType(board_type)`이 기존 8종 `board_type`(등급/쓰기 권한 축, 그대로 유지)을 화면 레이아웃 3종으로 매핑하고, `BoardRenderer.tsx`가 그 값 하나로 community(목록형)/story(카드형+썸네일)/gallery(이미지 중심 Masonry) 중 하나를 렌더링한다. `/boards/[id]` 라우트 하나가 모든 게시판을 처리하므로 "동일 컴포넌트 공유"가 아키텍처상 자동 보장됨.
  - **매핑 판단(스키마 변경 없이 처리, 판단 필요 사항)**: 전용 `display_type` 컬럼을 추가하는 대신 기존 `board_type`에서 코드로 파생 — `adoption_story`→story(대표 이미지+요약이 있는 후기 성격), `archive`→gallery(자료게시판=이미지/첨부 위주로 가정), 나머지(topic/group/patron/artist_promo/qna)→community. `/boards`(게시판 디렉토리)는 특정 게시판이 아니라 네 번째 레이아웃 `hub`로 취급 — 하위 게시판 전체의 최신글/인기글/추천글을 슬라이드+카드로 종합 표시.
  - **공통 기능**: 검색(제목/내용/작성자/태그, `GET /api/boards/[id]/posts?q=`), 정렬 5종(최신순/인기순/조회순/댓글순/오래된순, `?sort=`), 페이지네이션(10개 단위, 페이지 번호 최대 10개 블록 — `Pagination.tsx`), 글쓰기 버튼(`BoardHeader`, 기존 유지), 좋아요(기존 유지), 조회수(`posts.view_count`, 상세 조회마다 +1), 댓글수(목록 API가 매 요청 시 집계), 태그(`posts.tags`, 글쓰기 폼에 쉼표 구분 입력 필드 추가), 공유(현재 URL 클립보드 복사, 서버 연동 없음), 북마크(`post_bookmarks` 신규 테이블, own-row 토글), 작성일/수정일(`updated_at` 추가 — 게시글 수정 기능 자체가 없어 현재는 항상 `created_at`과 동일), 작성자 프로필(게시글 작성자 + 댓글 작성자 모두 `/u/[memberId]`로 링크).
  - **"추천글" 처리(판단 필요 사항)**: hub의 "추천글"에 대응하는 별도 플래그가 없어, 이미 "좋아요 10개 이상으로 승격"되는 기존 `is_best`(개념글) 플래그를 추천글로 재해석해 사용(`GET /api/boards/feed`) — 새 컬럼을 추가하지 않음.
  - **신규 파일**: `src/lib/boardLayout.ts`(레이아웃 매핑+타입+정렬 옵션), `src/components/boards/{BoardRenderer,Pagination,PostActions}.tsx`(신규), `PostDetailHeader.tsx`/`CommentSection.tsx`/`BoardHeader.tsx`(EPIC-046에서 확장), `src/app/api/boards/[id]/posts/[postId]/bookmark/route.ts`, `src/app/api/boards/feed/route.ts`.
  - **DB(최소 변경)**: `posts`에 `view_count`(int, default 0), `tags`(text[], default '{}'), `updated_at`(timestamptz, default now()) 3개 컬럼 추가. 신규 테이블 `post_bookmarks`(wishlists와 동일한 own-row 패턴). 작업 전 `docs/database-schema.sql`을 `docs/backups/database-schema-20260727-2050.sql`로 백업. `view_count` 갱신을 위한 `posts` 컬럼별 GRANT 확장(`like_count, is_best` → `like_count, is_best, view_count`)도 문서화(라이브 미적용, Supabase SQL Editor에서 직접 실행 필요 — `docs/database-schema.sql` RLS 섹션 TODO 참고).
  - **마이그레이션 전에도 안 깨지도록 방어적으로 구현**: 새 컬럼(`tags`/`view_count`/`updated_at`)이 라이브 DB에 없으면 PostgREST가 select 전체를 42703으로 실패시키므로, 목록/상세 조회와 글쓰기(태그 insert) 모두 "새 컬럼 포함 시도 → 실패 시 레거시 컬럼으로 재시도"하는 폴백을 넣어 마이그레이션 전에도 게시판 읽기/쓰기 자체는 계속 동작한다(태그/조회수만 0/빈 배열로 보임). `post_bookmarks`가 없을 때도 북마크 버튼은 503 안내만 뜨고 나머지 기능에는 영향 없음.
  - 기존 좋아요/댓글/글쓰기/등급 게이팅 로직은 변경하지 않음. 기존 라우팅과 URL 전부 유지(`/boards`, `/boards/[id]`, `/boards/[id]/write`, `/boards/[id]/[postId]`).
  - 문서 동기화: `docs/database-schema.sql`, `docs/design-system.md`(§10 확장), `docs/content-blueprint.md`(§1 확장), `PROJECT_BLUEPRINT.md`, `docs/EPIC.md`(EPIC-022~047 그동안 누락된 항목 일괄 보충 — 여러 세션에 걸쳐 CLAUDE.md 규칙에도 불구하고 갱신되지 않고 있었음).
  - 검증: `npx tsc --noEmit`/`npm run lint`(26건, EPIC-046과 동일 — 신규 이슈 없음) 통과. 다른 세션이 3000번 포트를 점유 중이라 브라우저로 실제 검색/정렬/페이지네이션/북마크/공유 동작을 직접 확인하지 못함 — 사용자 확인 필요(아래 NEXT_TASK.md 참고).

- **EPIC-047 Part 2: Board Definition System — config만으로 게시판을 정의하는 구조로 전환**
  - 위 Part 1의 `getBoardLayoutType(board_type)` 1줄짜리 매핑 함수를, 지시받은 필드(`id/slug/title_ko/title_en/parent/boardType/visibility/membership/searchable/pageable/sortable/thumbnail/comments/likes/bookmarks/tags/allowPosting/defaultSort/pageSize/description`)를 모두 갖춘 `BoardDefinition` 타입 + `BOARD_DEFINITIONS`(8개 그룹: general/topic/group/patron/artist_promo/adoption_story/archive/qna) 레지스트리로 전면 교체(`src/lib/boardLayout.ts`). `resolveBoardDefinition({board_type, category})`가 DB의 `boards` 행 하나를 정의 하나로 해석한다. 새 게시판 "종류"를 추가할 때는 (1) `boards`에 해당 board_type 시드 행 추가 (2) 이 레지스트리에 정의 한 항목 추가만 하면 되고, 페이지/컴포넌트 코드는 그대로 — 지시문의 "Community/자유게시판/경제클럽/예술클럽/Heritage/Renaissance/Gallery/Membership/My Collection처럼 config만 추가하면 자동 생성" 요구를 만족하는 구조. 이번 EPIC에서는 지시대로 새 게시판을 실제로 추가하지는 않음(기존 8개 정의만 구축).
  - **하드코딩 제거**: `boards/page.tsx`의 그룹 판별 로직이 각 그룹마다 손으로 짠 `match()` 함수 배열(`GROUP_LABELS`)이었던 것을, `BOARD_GROUP_ORDER` + `resolveBoardDefinition()` 하나로 대체 — 이 resolver를 hub 그룹핑, 목록/상세/글쓰기 API, 목록/상세/글쓰기 페이지가 전부 공유하므로 그룹 판정 로직이 한 곳에만 존재한다.
  - **BoardRenderer/BoardHeader가 정의를 직접 읽어 동작**: `BoardRenderer`는 `layoutType` 대신 `definition` 전체를 받아 `definition.boardType`으로 community/story/gallery를 고른다. `BoardHeader`는 `definition.searchable`/`sortable`/`allowPosting`이 꺼져 있으면 검색창/정렬 셀렉트/글쓰기 버튼 자체를 렌더링하지 않는다(이전엔 prop 존재 여부로만 판단해 사실상 모든 게시판에서 항상 켜져 있었음). 목록 API(`GET /api/boards/[id]/posts`)도 `definition.pageable`이 꺼지면 페이지네이션 없이 전체를 반환하고, `definition.pageSize`/`defaultSort`를 하드코딩된 `10`/`"latest"` 대신 실제로 사용한다(예: `archive`는 `pageSize:20`으로 설정해 갤러리에 더 많은 썸네일이 한 번에 보이도록 함 — 값이 실제로 동작에 반영됨을 보여주는 예시).
  - **좋아요/북마크/댓글/태그도 토글 기반**: 상세 페이지가 `definition.likes`/`bookmarks`/`comments`/`tags`를 읽어 해당 UI(`PostActions`의 `showLike`/`showBookmark`, `CommentSection` 렌더 여부, 태그 표시/입력)를 켜고 끈다 — 지금은 `qna`만 `tags:false`이고 나머지는 기존과 동일하게 전부 켜져 있어 **기존 8개 게시판의 실제 동작은 바뀌지 않음**(토글 배선만 새로 생김).
  - **하드코딩된 등급 안내 문구 제거**: 3개 API 라우트(`boards`, `boards/[id]/posts`, `boards/[id]/posts/[postId]`)에 각각 반복돼 있던 `RANK_LABELS[3]`(patron 등급 고정 가정) 잠금 안내 문구를, `RANK_LABELS[definition.membership]`로 대체 — 실제 읽기 인가(`canReadBoard`/`canWriteToBoard`, `membership_tiers`의 boolean 플래그 기반)는 보안 변경 없이 그대로 두고, `membership` 필드는 어디까지나 안내 문구 생성용 메타데이터로만 사용(주석에 명시).
  - **미배선 필드**: `visibility`/`parent`는 타입/레지스트리에 존재하지만 아직 어떤 동작도 참조하지 않는 예약 필드(현재 이 프로젝트에 "비공개 게시판"이나 게시판 간 실제 계층 구조 개념이 없음) — 향후 계층형 hub(예: Heritage → Renaissance)를 만들 때 `parent`를 실제로 배선할 것.
  - 검증: `npx tsc --noEmit`/`npm run lint`(26건, Part 1과 동일 — 신규 이슈 없음) 통과. 기존 라우팅/URL 전부 유지, 8개 게시판 그룹의 화면 동작(검색/정렬/페이지네이션/좋아요/댓글/북마크/태그 노출)은 토글이 전부 기존과 동일한 값으로 설정돼 있어 사용자 관점에서 변화 없음 — 다른 세션이 포트를 점유해 브라우저로 직접 재확인은 못함(NEXT_TASK.md 참고).

## 2026-07-27 (EPIC-046)
- **EPIC-046: Editorial Magazine board design system**
  - 게시판(`/boards`, `/boards/[id]`, `/boards/[id]/[postId]`, `/boards/[id]/write`) 전체를 신문/매거진풍 "Editorial Magazine" 디자인 언어로 통일 — House of Honey류 기사 레이아웃의 정보 구조(좌: No./날짜, 중앙: 큰 제목, 우: Author/작성자)를 참고하되 새 색상 없이 기존 뉴트럴 팔레트+`font-serif`(Tailwind 기본 스택, 폰트 파일 추가 없음)만으로 재해석. 자세한 규칙은 `docs/design-system.md` §10 신설.
  - 신규 공용 컴포넌트 4개(`src/components/boards/`): `BoardHeader`(게시판명+글쓰기 버튼+얇은 divider), `PostDetailHeader`(3열 헤더: No./작성일 · 큰 제목 · Author/작성자 + 대표 이미지 풀와이드), `PostTags`(태그 칩), `CommentSection`(댓글 목록+작성 폼, hairline 구분선) — `/boards/[id]`와 `/boards/[id]/[postId]` 두 라우트가 전부 이 컴포넌트로만 렌더링되므로 "모든 게시판이 동일한 컴포넌트 공유" 요구를 자동으로 만족(게시판별 커스텀 페이지가 없는 기존 아키텍처 그대로).
  - **기존 기능 100% 유지, UI만 교체**: 좋아요/댓글/글쓰기/등급 게이팅 로직·API 호출은 한 줄도 바꾸지 않음(단, 응답에 필드 2개만 추가 — 아래 참고). 카드+그림자 리스트를 hairline 구분선 목록으로, 좁은 `max-w-2xl` 단일 폭을 헤더는 `max-w-4xl`/본문은 `max-w-2xl`로 이중화(넓은 여백+읽기 좋은 폭 동시 확보)해 시각만 재구성.
  - **"태그 영역 추가" 처리 방식(판단 필요 사항)**: `posts` 테이블에 태그 전용 컬럼이 없어(스키마 변경 최소화 원칙) 새 컬럼을 추가하는 대신, 이미 있는 게시판 카테고리 + `is_docent_post`/`is_best` 여부를 `#태그` 칩으로 파생 표시(`PostTags.tsx`). 사용자가 직접 태그를 입력/편집하는 기능이 필요하면 `posts.tags`(배열) 컬럼 추가가 필요한 별도 스키마 작업.
  - **"글 번호(No.)" 처리 방식**: 저장된 시퀀스 컬럼이 없어, `GET /api/boards/[id]/posts/[postId]`가 매 요청마다 "같은 게시판에서 이 글보다 먼저(또는 동시에) 작성된 글의 개수"를 계산해 `post.post_number`로 반환(1부터 시작, 시간순). 응답에 `post.photo_url`도 추가(기존 select에서 누락돼 있었음 — 대표 이미지 기능에 필요).
  - 작업 전 `docs/database-schema.sql`을 `docs/backups/database-schema-20260727-1929.sql`로 백업(실제 스키마 변경 없음).
  - 검증: `npx tsc --noEmit`/`npm run lint`(26건, EPIC-045와 동일 — 신규 이슈 없음) 통과. dev 서버가 다른 세션에서 3000번 포트를 점유 중이라 이번 세션은 브라우저로 실제 레이아웃(3열 헤더, 풀와이드 이미지, hairline 목록)을 직접 확인하지 못함 — 사용자 확인 필요(아래 NEXT_TASK.md 참고).

## 2026-07-27 (EPIC-045)
- **EPIC-045: Mypage restructure — museum-like collection, route-based tabs**
  - `/mypage`가 11개 탭을 `useState`로 전환하는 단일 페이지에서 각 탭이 독립된 URL을 갖는 라우트 구조로 재구성됨 — 지시문의 "각 하위 페이지는 앞으로 기능 확장이 쉽도록 독립 페이지/라우트 구조를 사용한다"에 따름. 새 `src/app/mypage/layout.tsx`가 로그인 게이트 + 등급/포인트 요약 조회 + `MyPageNav`를 공유 chrome으로 끌어올리고, `MyPageProvider`(`src/components/mypage/MyPageContext.tsx`)로 `memberId`를 하위 라우트에 전달한다.
  - 새 라우트 12개: `/mypage`(허브 — 11개 섹션을 카드 그리드로 보여주는 "박물관 입구"), `/mypage/collections`(→ `/mypage/collections/treasure`로 즉시 이동), `/mypage/collections/[category]`(9개 카테고리 동적 라우트), `/mypage/wishlist`, `/mypage/follow`, `/mypage/salon`, `/mypage/docent-certificate`, `/mypage/space`, `/mypage/exhibition`, `/mypage/badges`, `/mypage/comments`, `/mypage/timeline`, `/mypage/visitors`.
  - **기존 기능 재사용**: `WishlistPanel`/`FollowPanel`/`BadgesPanel`/`CommentsPanel`/`VisitorsPanel`/`PlaceholderPanel`(EPIC-022)은 코드 변경 없이 그대로 각 라우트에 배치. `CollectionsPanel.tsx`(내부 `useState`로 9개 서브탭을 전환하던 구버전)는 삭제하고, 같은 조회/등록/수정/삭제 로직을 카테고리 하나만 받는 `CollectionCategoryPanel.tsx`로 재작성 — `CollectionModal.tsx`는 그대로 재사용. 서브탭 전환용 `CollectionsSubNav.tsx`(Link 기반)를 신규 작성해 `MyPageNav.tsx`도 같은 방식(`usePathname()`으로 활성 탭 자체 판단)으로 전환.
  - `mypageConfig.ts`(11개 탭/9개 컬렉션 서브탭 정의, EPIC-022)는 탭 id가 이미 라우트 세그먼트와 1:1 대응해 변경 없이 그대로 재사용.
  - **URL 변경 없음**: 기존 `/mypage`는 계속 유효(허브로 의미가 바뀌었을 뿐 경로 자체는 유지). 하위 라우트는 전부 신규 추가라 기존 링크(Navbar/설정 페이지의 `/mypage`)에 영향 없음.
  - Schema 변경 없음 — `member_collections`/`member_follows`/`member_badges`/`member_visitors`는 이미 EPIC-022/023에서 설계·라이브 확인된 테이블을 그대로 사용. 작업 전 `docs/database-schema.sql`을 `docs/backups/database-schema-20260727-1646.sql`로 백업만 해둠(실제 변경 없음).
  - 검증: `npx tsc --noEmit`/`npm run lint`(26건, EPIC-043과 동일 — 신규 이슈 없음. `CollectionCategoryPanel.tsx`의 `set-state-in-effect` 1건은 삭제된 `CollectionsPanel.tsx`에 있던 동일 패턴을 그대로 옮긴 것이라 순증 없음) 통과. dev 서버 점유 여부는 이번 세션에서 확인하지 못해 실제 라우트 이동/데이터 렌더링은 사용자 확인 필요.

## 2026-07-27 (EPIC-044)
- **EPIC-044: Dynamic routing, navigation data & universal board component**
  - `src/components/shared/UniversalBoard.tsx` 신규 작성: 이름/주제가 무한히 늘어나는 카테고리(헤리티지 인물, 클럽 등)를 위한 공용 게시판 템플릿 — 검색창, 정렬 드롭다운(조회수순/좋아요순/최신날짜순), 목록, 하단 총 페이지 수 통계. `posts`/`totalPages`는 옵션 prop(기본 빈 배열)으로, 카테고리별 실제 데이터 소스가 아직 없어 UI 뼈대만 구현.
  - 동적 라우트 3개 신규: `src/app/heritage/grandma/[name]/page.tsx`, `heritage/grandpa/[name]/page.tsx`, `community/club/[name]/page.tsx` — 각각 `params`를 `await`해 이름을 제목으로 표시하고 `<UniversalBoard />`를 렌더링.
  - `src/lib/navConfig.ts`의 `FALLBACK_NAV_TABS`에 사이드바 메뉴 데이터 대량 주입: `silostore`(사일로상점) 탭에 "사일로 헤리티지 · 할머니"(51명)/"사일로 헤리티지 · 할아버지"(17명) 그룹, `salon`(살롱데상) 탭에 "주제별 소통게시판"(13개 주제)/"요일별 클럽"(7개) 그룹 추가 — 전부 위 동적 라우트로 링크. **주제별 소통게시판은 전용 라우트가 지시문에 없어 `community/club/[name]`을 재사용**(요일별 클럽과 공유).
  - `LeftSidebar.tsx`의 `isAccordionGroup`을 "헤리티지" 포함 그룹도 매칭하도록 확장 — 51명+17명 목록이 항상 펼쳐진 채로 있으면 사이드바가 지나치게 길어져, "도슨트" 그룹과 동일하게 hover 펼침 아코디언으로 처리.
  - **LeftSidebar.tsx/RightSidebar.tsx 자체는 데이터를 소유하지 않음**: 두 컴포넌트는 `navConfig.ts`가 넘긴 `tab.groups`를 그대로 렌더링하는 순수 뷰라, 실제 대량 데이터는 그 데이터가 흘러나오는 지점인 `FALLBACK_NAV_TABS`에 주입했다(지시문의 "LeftSidebar/RightSidebar 데이터 바인딩" 문구와 실제 아키텍처가 달라 판단해 처리). `site_navigations`(DB)가 채워지면 이 폴백은 무시되므로, 운영 반영을 원하면 같은 항목을 DB에도 시딩해야 함 — Management API 토큰이 없어 DB 작업은 하지 않음.
  - 검증: `npx tsc --noEmit`/`npm run lint`(26건, 기존과 동일 — 신규 이슈 없음) 통과.

## 2026-07-27 (EPIC-043)
- **EPIC-043: Font settings overhaul, sidebar click-only + hover accordion (성능 최적화는 보류)**
  - **폰트 설정 개편**: 작동하지 않던(폰트 파일 자체가 없는) "텍스트 폰트" select(`textCustomFont`: Graphire/Primor)를 `admin/navigation/settings/page.tsx`와 `Navbar.tsx`에서 완전히 삭제. 단일 `main_logo.fontFileUrl`을 `customFonts`(배열, 각 항목 `{ id, url, isActive }`)로 교체 — 관리자 폼에서 여러 폰트를 등록해두고 각각 "적용" 체크박스로 켜고 끌 수 있다. `Navbar.tsx`는 `isActive`인 항목들만 등록 순서대로 `@font-face`를 각각 주입하고, 그 순서 그대로 `font-family` 폴백 체인(`'SiloCustomLogoFont-{id}', ..., 자유입력 fontFamily 또는 sans-serif`)을 구성해 로고 좌/우 텍스트에 적용한다. 구버전 단일 `fontFileUrl`은 로드 시 1개짜리 배열로 자동 이전(관리자 폼과 `Navbar.tsx` 양쪽 모두).
  - **사이드바 클릭 전용 + Hover 아코디언**: `Navbar.tsx`가 `LeftSidebar`/`RightSidebar`에 더 이상 `onIconMouseEnter`를 넘기지 않고(두 컴포넌트에서 prop 자체를 제거), 여닫이 아이콘은 오직 클릭으로만 열린다(패널을 닫는 방법은 그대로: ✕/바깥 클릭/패널에서 마우스가 완전히 벗어남). `LeftSidebar.tsx`는 "도슨트"가 포함된 그룹(현재 라벨 "온라인 도슨트 라이브러리")만 기본 접힘 + hover로 펼쳐지는 아코디언 — 정확한 문자열 대신 부분 일치로 구현해, 관리자가 site_navigations에서 그룹 라벨을 살짝 바꿔도(실제로 이번 세션 중 "온라인 도슨트 Online Docent"로 바뀌었다) 계속 매칭된다. `RightSidebar.tsx`는 그룹 전부(커뮤니티/멤버십/갤러리/아카이브)가 동일하게 hover 아코디언 — 라벨을 하드코딩하지 않고 모든 그룹에 균일 적용. 둘 다 JS state 없이 순수 Tailwind `group`/`group-hover`(문서 흐름 안에서 그룹 헤더 바로 아래로 펼쳐지는 구조라 플로팅 팝업과 달리 별도 "브릿지" 여백이 필요 없다).
  - **슬라이드/배경 이미지 next/image 전환은 보류**: 지시대로 `HeroSlideshow.tsx`의 `<img>`/`backgroundImage`를 `next/image`(`fill`+`priority`/`loading="lazy"`+`quality`)로 교체해봤으나, 이 Next 16 + Turbopack 조합에서 **재현 가능한 심각한 회귀**가 발견됐다 — `<Image fill .../>`를 슬라이드에 쓰는 순간 Navbar를 포함한 앱 전체가 클라이언트에서 전혀 hydrate되지 않아(콘솔/서버 로그에 에러 한 줄도 없이) 상단 탭 메뉴가 완전히 빈 채로 굳어버림. 브랜드 뉴 탭 + `.next` 캐시 삭제 + 서버 재시작까지 거쳐 `priority`/`sizes`/`quality`/`className` 하나씩 제거하며 이진 탐색으로 원인을 `fill` prop 자체(다른 prop 없이도 재현)까지 좁혔지만, 명확한 에러 메시지를 못 찾아 근본 원인은 특정하지 못했다. 핵심 내비게이션을 깨뜨리는 회귀를 "성능 개선"과 맞바꿀 수 없어 `HeroSlideshow.tsx`는 기존 `<img>`/CSS `backgroundImage` 구현으로 되돌렸다 — 아래 NEXT_TASK.md 참고.
  - 검증: `npm run lint`(26건, 직전과 동일 — 신규 이슈 없음)/`npx tsc --noEmit` 통과. 이번 세션 dev 서버에서 직접 확인 — 사이드바 아이콘이 클릭으로만 열림(코드상 hover 핸들러 자체가 없음), "온라인 도슨트 Online Docent" 그룹만 `group`/`group-hover` 클래스가 붙고 나머지 LeftSidebar 그룹엔 안 붙는 것/RightSidebar는 4개 그룹 전부에 붙는 것을 DOM에서 직접 확인, 관리자 폼의 새 커스텀 폰트 배열 UI(구버전 데이터 1개 자동 이전 포함) 렌더링 확인.

## 2026-07-27 (EPIC-041-042-HOTFIX)
- **EPIC-041-042-HOTFIX: fix hover bug & switch to direct URL inputs**
  - **드롭다운 hover 버그 완전 재구현**: `Navbar.tsx`의 상단 탭 드롭다운을 JS state(`openTab`/`popupPos`/`handleTabMouseEnter`/`handlePopupMouseLeave`, `position:fixed`)에서 순수 Tailwind `group`/`group-hover`로 전면 교체. 버그 원인은 트리거(탭 버튼)와 팝업이 서로 다른, 별개의 DOM 서브트리(팝업은 헤더 맨 끝에 fixed로 따로 렌더링)였다는 점 — 마우스가 그 사이를 이동할 때 각각의 enter/leave 타이밍이 어긋나면서 벗어나도 안 닫히는 경우가 생겼다. 이제 트리거+팝업을 같은 `relative group/tab` 컨테이너의 부모-자식으로 중첩해, 브라우저가 그 컨테이너 전체를 기준으로 `:hover`를 계산하므로 완전히 벗어나는 즉시 예외 없이 닫힌다. 탭↔팝업, 그룹↔2차 플라이아웃 사이의 시각적 간격은 각각 `pt-4`/`pl-2` **padding**(margin이 아님)으로 만들어 "투명한 다리" 역할을 하게 했다 — padding은 그 요소 박스의 일부라 마우스가 그 위를 지나도 hover가 끊기지 않는다. 이름 있는 그룹(`group/tab`, `group/row`)을 쓴 이유: 이름 없는 `group`을 중첩하면 Tailwind가 "가장 가까운 조상"이 아니라 "어떤 조상이든 `.group`이고 hover 중이면" 매칭해 상위 탭에 마우스를 올리기만 해도 모든 하위 그룹의 플라이아웃이 한꺼번에 열려버리는 문제가 있었음.
  - `<nav>`의 `overflow-x-auto`(모바일 가로 스크롤용)를 `flex-wrap`으로 교체 — CSS 스펙상 `overflow-x`가 `visible`이 아니면 `overflow-y`도 `auto`로 강제 계산돼, 그 안에 중첩된 `position:absolute` 드롭다운이 잘려 보이는 문제를 `position:fixed` 없이 해결하기 위함(4개 탭 기준으로는 줄바꿈이 자연스러운 대안).
  - **URL 직접 입력으로 전환**: `admin/navigation/settings/page.tsx`의 커스텀 폰트 파일 업로드와 좌/우 사이드바 아이콘 업로드(`input type="file"` + Storage 업로드)를 제거하고, Supabase Storage에 이미 올린 파일의 공개 URL을 직접 붙여넣는 텍스트 입력으로 교체 — 가장 단순하고 확실한 방식을 우선했다. 로고 이미지/슬라이드/Wallpaper 이미지는 대상이 아니라 계속 파일 업로드를 쓴다.
  - 검증: `npm run lint`(26건, 직전과 동일 — 신규 이슈 없음)/`npx tsc --noEmit` 통과. `@font-face` 주입과 사이드바 아이콘 `src` 바인딩 로직은 EPIC-041에서 이미 구현된 그대로이며 이번엔 값의 출처만(업로드 → 직접 입력) 바뀌었으므로 코드 경로상 문제 없음을 확인. 다만 이번 세션은 Browser 창이 화면에 표시되지 않아 실제 마우스 hover(: hover는 진짜 커서 이동에만 반응하고 JS로 디스패치한 이벤트로는 재현되지 않음) 동작을 스크린샷으로 직접 확인하지 못했음 — 사용자가 직접 브라우저에서 탭 hover 후 마우스를 완전히 치웠을 때 즉시 닫히는지 확인 필요.

## 2026-07-27 (EPIC-041-042)
- **EPIC-041-042: Unified UI polish — hover UX, custom fonts, icons, home curation**
  - **드롭다운 UX 수정**: `Navbar.tsx`의 상단 탭 다단계 팝업에서 "클릭으로 고정(pinned)" 개념을 완전히 제거했다 — `pinnedKey`/`handleTabClick`/outside-click `useEffect`/`navRef`를 모두 삭제하고, 탭 버튼에는 `onMouseEnter`만 남겼다. 팝업을 닫는 것도 오직 `onMouseLeave`(`handlePopupMouseLeave`)뿐이라 마우스가 완전히 벗어나면 예외 없이 즉시 닫힌다. 클릭으로 닫던 배경(backdrop) `<div>`도 제거 — pinned 상태가 없어 필요 없어졌고, 이전엔 이 배경이 hover 중 페이지의 다른 클릭을 가로채는 부작용이 있었다. 팝업은 여전히 `position: fixed`(JS로 좌표 계산) — 상단 nav의 `overflow-x-auto`가 CSS 스펙상 `overflow-y`도 `auto`로 강제해 `position: absolute` 팝업은 잘려 보이기 때문(2차 플라이아웃은 계속 순수 CSS `group-hover`).
  - **커스텀 폰트 업로드**: `admin/navigation/settings/page.tsx`에 "커스텀 폰트 파일 업로드(.woff/.woff2/.ttf/.otf)" 필드 추가 — `public-assets`/`logo_fonts` 폴더에 업로드하고 URL을 `main_logo.fontFileUrl`로 저장. `Navbar.tsx`는 이 URL이 있으면 `<style>` 태그로 `@font-face`(`font-family: 'SiloCustomLogoFont'`)를 동적 주입하고, 로고 좌/우 텍스트의 `fontFamily`에 최우선 적용(기존 Graphire/Primor/자유 입력 서체보다 우선).
  - **아이콘 크기 설정**: `sidebar_icons`에 `iconSizePx`(기본 32px, 기존 `w-8 h-8` 하드코딩과 동일) 추가 — 관리자 폼에 숫자 입력 필드, `LeftSidebar`/`RightSidebar`가 `width`/`height` 인라인 스타일로 반영.
  - **홈 큐레이션 동적 배열**: `home_curation` 설정을 단일 객체에서 `HomeCurationBlock[]`(섹션 제목 + 필터 기준(도메인) + 타겟 값(slug) + 정렬)로 고도화 — 관리자 폼에 블록 추가/삭제/위로·아래로 이동 UI 추가. 구버전 단일 객체 데이터는 로드 시 블록 1개로 자동 이전(관리자 폼과 `page.tsx` 양쪽 모두). 신규 `src/components/HomeCurationSlider.tsx`(섹션 제목 + 가로 스크롤 썸네일, 표시 전용)를 만들고 `page.tsx`가 저장된 순서대로 렌더링 — `domain==="shop"` 블록은 `items` 테이블을 실제로 조회(카테고리 slug 필터, `status='available'`)하고, salon/collection/docent 도메인은 아직 각 화면의 실제 스키마를 확인하지 않아 UI 확인용 더미 데이터로 대체(스키마 추측 금지 원칙에 따름 — NEXT_TASK.md 참고).
  - 검증: `npm run lint`(26건, EPIC-040과 동일 — 신규 이슈 없음)/`npx tsc --noEmit` 통과. 이번 세션 dev 서버에서 직접 확인 — hover 팝업이 마우스가 벗어나면 즉시 닫힘(브라우저 자동화 도구의 합성 마우스 이동이 실제 `mouseout`을 늘 발생시키지 않는 경우가 있어, 실제 `mouseout` 이벤트를 직접 디스패치해 닫힘을 재확인함), 홈 큐레이션 블록이 구버전 데이터에서 자동 이전되어 실제 `items` 테이블 데이터("아르누보 유리 램프")로 렌더링되는 것까지 확인. 폰트 파일 업로드/아이콘 크기 반영은 실제 파일 업로드 다이얼로그 조작이 불가해 코드 리뷰 수준으로만 확인.

## 2026-07-27 (EPIC-040)
- **EPIC-040: Master UI — nested dropdown, independent sidebars, logo texts, random wallpapers**
  - **다단계(Nested) 드롭다운**: `Navbar.tsx`의 상단 탭 hover 팝업이 groups(사이드바 타입, 예: 사일로상점/살롱데상)를 가질 때 이제 그룹 라벨을 1차 목록으로 보여주고, 각 행에 마우스를 올리면 그 그룹의 items가 2차 플라이아웃으로 옆에 튀어나온다 — 별도 JS state 없이 Tailwind `group`/`group-hover`만으로 구현(플라이아웃이 부모 팝업 바깥으로도 잘리지 않게 팝업의 `max-h-[70vh] overflow-y-auto`를 제거). `dropdown` 타입(예: 스튜디오)은 groups가 없어 기존처럼 평평한 목록.
  - **좌/우 사이드바 독립 state 복구**: EPIC-039는 전체 높이 사이드바(open 여부)를 상단 탭 hover/pin 상태(`openTab`/`pinnedKey`)에서 파생시켰는데, 그러면 상단 탭 hover가 곧 전체 사이드바를 열어버려 이번 EPIC의 "탭 hover 시 작은 다단계 팝업" 요구와 근본적으로 충돌했다(하나의 hover가 두 다른 UI를 동시에 열 수 없음). `leftOpen`/`rightOpen`을 다시 독립적인 `useState`로 되돌려, 전체 사이드바는 오직 `LeftSidebar`/`RightSidebar` 자신의 여닫이 아이콘 버튼(hover로 열기/✕·바깥 클릭·패널에서 마우스가 벗어나면 닫기)으로만 제어한다 — 상단 탭 쪽 다단계 팝업과는 완전히 별개의 UI/상태.
  - 사이드바 아이콘 업로드, 로고 좌/우 텍스트, 슬라이드쇼 다중 Wallpaper 랜덤 배경은 EPIC-039에서 이미 구현된 것을 그대로 사용(이번 EPIC에서 관련 파일을 다시 건드리지 않음) — 상세 내용은 위 EPIC-039 항목 참고.
  - 검증: `npm run lint`(26건, EPIC-039와 동일 — 신규 이슈 없음)/`npx tsc --noEmit` 통과. 이번 세션 dev 서버에서 직접 확인: 상단 탭 hover → 1차 그룹 팝업 → 그룹 hover 시 2차 아이템 플라이아웃, 사이드바 아이콘(🔑) hover-열림/✕-닫힘/바깥 클릭-닫힘(별도 상태로 상단 탭 팝업과 무관하게 동작), 로고 좌/우 텍스트("I'm your" / "SILO") 실 데이터로 대칭 렌더링, 슬라이드쇼 wallpaper가 재로딩마다 다른 이미지로 무작위 표시되는 것까지 확인.

## 2026-07-27 (EPIC-039)
- **EPIC-039: Master UI polish — sidebars, icons, logo texts, hover, random wallpapers**
  - **사이드바 복구**: EPIC-037이 sidebar-left/sidebar-right를 dropdown과 동일한 작은 팝업으로 통합했던 것을, 화면 전체 높이의 슬라이드인 패널로 복구했다. 다만 EPIC-037의 "hover로 열고 클릭으로 고정, 바깥 클릭으로 닫기" 상태 관리(`openTab`/`pinnedKey`)는 그대로 재사용 — `leftOpen`/`rightOpen`을 별도 state로 다시 두지 않고 `pinnedKey === tab.key || (!pinnedKey && openTab?.key === tab.key)`로 파생시켜, "Hover 로직과 충돌하던 사이드바 State"가 다시 생기지 않게 했다. 패널 UI 자체는 `src/components/LeftSidebar.tsx`/`RightSidebar.tsx` 2개 컴포넌트로 분리.
  - **사이드바 아이콘**: `admin/navigation/settings/page.tsx`에 좌/우 사이드바 아이콘 파일 업로드(새 `site_settings` 키 `sidebar_icons`, `public-assets`/`sidebar_icons` 폴더) 추가. `LeftSidebar`/`RightSidebar`의 여닫이 버튼이 이 아이콘을 `w-8 h-8`로 렌더링하고, 아이콘이 없으면 기존 🔑/🚪 이모지로 자동 대체.
  - **로고 좌/우 텍스트**: `main_logo`의 단일 `extraText`+`textPosition`을 `leftText`/`rightText`로 분리 — 로고 이미지를 중앙에 두고 양옆에 동일한 `flex-1` 폭으로 대칭 배치. 이 레이아웃이 EPIC-034의 "정렬 위치"(`align`)와 의미가 겹쳐 관리자 UI에서 정렬 select는 제거(항상 중앙). 구버전 `extraText`+`textPosition` 데이터는 로드 시 1회 `leftText`/`rightText`로 자동 이전(`align`/`extraText`/`textPosition` 필드 자체는 구버전 호환을 위해 타입에는 남겨둠).
  - **상단 탭 hover 일관성**: dropdown/sidebar-left/sidebar-right 3개 타입은 EPIC-037부터 이미 동일한 hover-팝업 상호작용을 공유 — 이번 EPIC은 사이드바 타입의 팝업 내용만 컴팩트 박스에서 전체 패널로 바꿨을 뿐, 트리거 메커니즘(hover 미리보기/클릭 고정/바깥 클릭 해제)은 손대지 않았다. `link` 타입(마이페이지)은 하위 항목이 데이터 모델에 없어(그룹/아이템 없음) 실제 드롭다운 노출은 적용하지 않고, 시각적 일관성만 위해 동일한 hover 테마 색상(`hover:bg-green-800`)을 추가했다 — 실제 서브메뉴가 필요하면 `site_navigations`에 자식 행을 추가하는 별도(데이터) 작업이 필요하다.
  - **슬라이드쇼 랜덤 배경**: `hero_slideshow.wallpaperUrl`(단일 문자열)을 `wallpaperUrls`(최대 10개 배열)로 교체 — 관리자 폼은 슬라이드 목록과 동일한 "+ 추가/삭제" 패턴의 반복 업로드 UI. `HeroSlideshow.tsx`는 슬라이드 인덱스(`current`)가 바뀔 때마다 이펙트에서 `Math.random()`으로 하나를 골라 `useState`에 담아 배경으로 적용 — `Math.random()`은 순수하지 않아 렌더/`useMemo` 안에서 호출할 수 없다(`react-hooks/purity`)는 새 ESLint 규칙 때문에 useEffect+setState 방식을 택함(모든 슬라이드가 같은 배경을 공유해 크로스페이드 중 배경이 어긋나 보이지 않게 함). 구버전 단일 `wallpaperUrl`은 로드 시 1개짜리 배열로 자동 이전.
  - 검증: `npm run lint`(26건 → 27건, +1은 위 wallpaper 이펙트의 `react-hooks/set-state-in-effect` — 이미 저장소 전반에 퍼져 있는 동일 클래스의 pre-existing 이슈, NEXT_TASK.md의 P2 항목과 동일 성격)/`npx tsc --noEmit` 통과. 이번 세션 dev 서버에서 직접 확인: 사이드바 hover-열림/클릭-고정/바깥 클릭-닫힘, 로고 좌측 텍스트 마이그레이션 표시, 관리자 폼의 새 필드(좌/우 텍스트, Wallpaper 다중 업로드, 사이드바 아이콘) 렌더링까지 확인. Wallpaper 실제 파일 업로드/랜덤 배경 렌더링과 사이드바 아이콘 업로드는 실제 파일 다이얼로그 조작이 불가해 코드 리뷰 수준으로만 확인(NEXT_TASK.md 참고).

## 2026-07-27 (EPIC-037)
- **EPIC-037: Navigation hover & click UX improvement**
  - `Navbar.tsx`: 상단 탭의 `dropdown`/`sidebar-left`/`sidebar-right` 3개 타입을 렌더링 방식 하나로 통일 — 예전에는 sidebar 타입이 화면 전체 높이로 슬라이드인하는 별도 패널(+ 화면 가장자리의 🔑/🚪 플로팅 버튼)이었지만, 이제 세 타입 모두 탭 바로 아래에 뜨는 작은 팝업(그룹이 있으면 그룹 라벨+항목, 없으면 dropdown의 평평한 항목 목록)으로 동작한다. 기존 `leftOpen`/`rightOpen`/`dropdownTab`/`dropdownPos`/`openDropdown`/`closeDropdown`/`closeSidebars`를 `openTab`/`popupPos`/`pinnedKey`와 `handleTabMouseEnter`/`handleTabClick`/`handlePopupMouseLeave`로 교체.
  - Hover 시 탭 배경/텍스트가 사이드바와 동일한 테마 색상(`bg-green-800`/`text-white`)으로 바뀌고 탭 바로 아래에 팝업이 뜬다. 클릭 시에는 `pinnedKey`에 해당 탭의 `key`를 저장해 팝업을 "고정"하고, 고정된 동안에는 다른 탭에 마우스를 올려도 내용이 바뀌지 않는다(의도치 않은 전환 방지). 문서 전체에 `mousedown` 리스너를 다는 `useEffect`(의존성 `[pinnedKey]`)로 팝업/탭 바깥 클릭을 감지해 고정을 해제 — 팝업(`popupRef`)이나 탭이 속한 `<nav>`(`navRef`) 내부 클릭은 무시해 탭 전환이 자연스럽게 이어지도록 함.
  - 검증: `npm run lint`(기존 25건 pre-existing 이슈만 유지, 신규 발생 없음)/`npx tsc --noEmit` 통과. 이번 세션 dev 서버에서 실제로 hover 시 팝업+테마 색상 전환, 클릭 후 마우스를 팝업 밖으로 옮겨도 유지되는 고정, 빈 공간 클릭 시 닫힘까지 직접 확인.

## 2026-07-27 (EPIC-036)
- **EPIC-036: Logo text color & slide background wallpaper**
  - `admin/navigation/settings/page.tsx`: `main_logo`에 "추가 텍스트 색상"(`textColor`, 컬러 피커 + HEX 직접 입력) 필드 추가 — 기본값은 `Navbar.tsx` 사이드바에 쓰이는 짙은 녹색(Tailwind `green-800`, `#166534`)으로 맞춤. `hero_slideshow`에는 "여백 배경 이미지(Wallpaper)" 파일 첨부 필드(`wallpaperUrl`) 추가 — 로고/슬라이드 이미지와 동일한 `uploadImage()`/`public-assets` 버킷 업로드 로직 재사용.
  - `Navbar.tsx`: 로고 옆 추가 텍스트의 하드코딩된 `text-gray-900` 클래스를 제거하고, `mainLogo.textColor`(없으면 기본값 `#166534`)를 인라인 `style={{ color }}`로 적용.
  - `HeroSlideshow.tsx`: `wallpaperUrl` prop 추가. `objectFit==="contain"`이고 `wallpaperUrl`이 있을 때만 각 슬라이드 컨테이너에 `backgroundImage`/`backgroundSize: cover`/`backgroundPosition: center` 인라인 스타일을 적용해 이미지 좌우·상하 여백을 채움 — `cover` 모드에서는 여백이 애초에 없으므로 배경이 적용되지 않음.
  - `src/app/page.tsx`: 홈페이지 Server Component가 `hero_slideshow` 조회 결과의 `wallpaperUrl`을 `HeroSlideshow`에 그대로 전달하도록 연결.
  - 검증: `npm run lint` 통과 확인. 이번 세션은 dev 서버를 직접 띄운 세션이라 브라우저로 실제 렌더링(색상 피커 반영, wallpaper 배경)까지 확인 완료 — 자세한 내용은 세션 로그 참고.

## 2026-07-27 (EPIC-034-Ext)
- **EPIC-034-Ext: Advanced logo text styling**
  - `admin/navigation/settings/page.tsx`: `main_logo`에 "텍스트 위치"(로고 좌/우, `textPosition`)와 "텍스트 폰트"(기본/Graphire/Primor Select, `textCustomFont`) 추가. EPIC-034의 자유 입력 서체 필드(`fontFamily`)는 유지하고 "기본" 선택 시에만 사용하도록 해 대체가 아닌 보완 방식으로 구현 — Graphire/Primor 선택 시 자유 입력란은 비활성화.
  - `Navbar.tsx`: `textPosition==="left"`일 때 로고+텍스트 컨테이너에 `flex-row-reverse`를 적용해 렌더링 순서를 반전. 텍스트 색상은 `text-gray-900`으로 하드코딩(헤더 기본 톤과 통일). `textCustomFont`가 Graphire/Primor면 `"'Graphire', serif"`/`"'Primor', serif"` 형태로 serif 폴백을 포함해 인라인 `fontFamily`로 적용, 기본이면 기존 자유 입력값 사용.
  - `globals.css`: 최상단에 `Graphire`/`Primor` `@font-face` 뼈대를 주석으로 추가 — 실제 폰트 파일이 없어 비활성 상태(파일 추가 후 경로 채우고 주석 해제하면 적용). 그 전까지는 Navbar가 serif로 자연스럽게 대체.
  - 검증: `npm run type-check`/`npm run lint` 통과 확인. 실제 렌더링(정렬/폰트 전환)은 아래 "localhost 확인 불가" 사유로 직접 확인하지 못함.

## 2026-07-27 (EPIC-035-Fix)
- **EPIC-035-Fix: Unify category CMS & clean up old pages**
  - 구버전 `top-tabs`/`sidebar-left`/`sidebar-right` 3개 페이지(정적 `NavNodeEditor` 기반)를 삭제 — EPIC-035의 `CategoryTreeManager`(드래그앤드롭 통합 화면, `/admin/navigation`)로 `site_navigations` 관리 UI를 일원화.
  - `admin/navigation/layout.tsx`의 서브 탭을 4개에서 "카테고리 통합 관리"(`/admin/navigation`)와 "홈페이지 설정 관리"(`/admin/navigation/settings`) 2개로 정리. `admin/layout.tsx`의 상위 "메뉴/카테고리 관리" 탭은 이미 `/admin/navigation`으로 연결돼 있어 별도 수정 불필요함을 확인.
  - `admin/navigation/shared.tsx`의 `NavNodeEditor`/`CategoryRowEditor`/`NavRow`/`TargetType` 등은 삭제된 3개 페이지에서만 쓰이던 export라 이제 죽은 코드가 됐지만, 이번 EPIC 수정 대상 파일에 `shared.tsx`가 없어 정리하지 않음(`DOMAIN_OPTIONS`/`inputClass` 등은 `settings/page.tsx`가 계속 사용해 남겨둠).
  - 검증: `npm run type-check`/`npm run lint` 통과 확인, 삭제된 라우트에 대한 참조가 남아있지 않음을 grep으로 확인.

## 2026-07-27 (EPIC-035)
- **EPIC-035: Tistory-style drag-and-drop category CMS**
  - `site_navigations`(상단 탭/좌측·우측 사이드바 트리)에 `topic`(주제/태그), `thumbnail_url`(대표 이미지), `description`(카테고리 소개), `is_public`(공개 여부, default true) 4개 컬럼 추가. "카테고리"라는 표현이 `site_navigations`(내비 트리)와 `site_categories`(상점/살롱/도슨트 등 도메인 카테고리) 둘 다를 가리킬 수 있어 모호했지만, "상단 탭/좌측/우측 사이드바 공통 적용"이라는 지시 문구와 `parent_id`+`sort_order`를 이미 갖춘 트리 구조가 필요하다는 점에서 `site_navigations` 쪽으로 판단해 진행. 작업 전 `docs/database-schema.sql`을 `docs/backups/database-schema-20260727-0035.sql`로 백업.
  - `src/components/admin/CategoryTreeManager.tsx` 신규 작성: `@dnd-kit/core`+`@dnd-kit/sortable`로 구현한 재사용 가능한 트리 관리 컴포넌트. `targetTypes` prop으로 상단 탭/좌측/우측 사이드바 중 어느 트리를 다룰지 지정. 각 행에 [추가]/[수정]/[관리]/[삭제] 버튼, 드래그 핸들(⠿)을 배치 — 같은 부모 밑에서 위/아래로 끌면 순서(`sort_order`)만 바뀌고, 다른 행 위로 끌어다 놓으면 그 행의 하위 항목으로 즉시 재부모화(`parent_id` 변경)되어 DB에 반영. [관리] 클릭 시 공개 설정/주제·태그/대표 이미지(Supabase Storage `public-assets` 버킷 직접 업로드 또는 URL)/카테고리 소개를 편집하는 모달 표시.
  - `admin/navigation/page.tsx`: 기존에 `top-tabs`로 단순 리다이렉트하던 인덱스 페이지를 `CategoryTreeManager` 3개(상단 탭/왼쪽 사이드바/오른쪽 사이드바)를 한 화면에 나란히 보여주는 통합 관리 화면으로 교체. 이번 EPIC의 수정 대상 파일이 이 파일로 한정되어 있어, 기존 `top-tabs`/`sidebar-left`/`sidebar-right`(EPIC-023/025/027, 정적 `NavNodeEditor` 기반) 3개 페이지는 그대로 남아있음 — 같은 테이블을 보므로 어느 화면에서 편집해도 결과는 동일하게 반영됨.
  - 검증: `npm run type-check`/`npm run lint` 통과 확인(신규 파일의 유일한 lint 에러는 기존 `shared.tsx`의 `NavNodeEditor`와 동일한 패턴의 사전 존재 `set-state-in-effect` 이슈). Drag & Drop 실제 동작과 새 컬럼 저장은 라이브 DB에 컬럼이 없어 로컬에서 직접 확인하지 못함(아래 참고).

## 2026-07-27 (EPIC-034)
- **EPIC-034: Advanced logo & header customization**
  - `admin/navigation/settings/page.tsx`: `main_logo` 설정에 정렬 위치(좌/중앙/우, `align`), 로고 옆 추가 텍스트(`extraText`), 텍스트 서체(`fontFamily`)/굵기(`bold`)/크기(px, `fontSizePx`) 필드 추가. 기존과 동일하게 `setting_value`(jsonb) 안에 전체 `mainLogo` 객체를 그대로 저장해 병합.
  - `Navbar.tsx`: 로고+추가텍스트를 계정 영역(로그인/마이페이지 등)과 분리된 `flex-1` 컨테이너로 감싸고, 그 컨테이너에만 `justify-start`/`justify-center`/`justify-end`를 동적 적용 — 헤더 전체에 justify를 걸면 우측 계정 영역까지 로고 옆으로 끌려오는 문제가 있어, "로고 정렬"의 실제 의도(로고 블록 자체의 위치)를 살리면서 계정 영역 배치는 그대로 유지하도록 범위를 좁혔다. 추가 텍스트가 있으면 로고 옆에 `fontFamily`/`fontWeight`/`fontSize` 인라인 스타일로 렌더링.
  - 검증: `npm run type-check`/`npm run lint` 통과 확인. `site_settings` 라이브 미적용 + 로컬 포트 점유로 실제 렌더링은 직접 확인하지 못함(아래 참고).

## 2026-07-27 (EPIC-033)
- **EPIC-033: Admin CMS — direct file upload & dynamic logo/slideshow styling**
  - `admin/navigation/settings/page.tsx`: 메인 로고·슬라이드 이미지에 `<input type="file" accept="image/*">`를 추가 — 선택 시 Supabase Storage(`public-assets` 버킷, `main_logo/`·`slides/` 경로)에 즉시 업로드하고 반환된 public URL을 기존 URL 텍스트 입력값에 그대로 채운다(URL 직접 입력도 fallback으로 계속 동작). 업로드 실패(버킷 미생성 등)는 기존 에러 배너로 표시.
  - `main_logo`에 "로고 높이 (px)" 숫자 필드(`heightPx`, 기본 64) 추가. `hero_slideshow`에 "자동 전환 시간 (초)"(`autoAdvanceSeconds`, 기본 5)와 "이미지 채움 방식"(`objectFit`: cover/contain) 필드 추가 — `setting_value`가 jsonb라 스키마 변경 없이 안전하게 저장됨. `addSlide`/`updateSlide`/`removeSlide`가 기존에 `{ slides }`만 반환해 새 필드를 매번 지워버리던 버그를 `...prev` 스프레드로 수정.
  - `Navbar.tsx`: 로고 `<img>`의 고정 `h-16` 클래스를 제거하고 `mainLogo.heightPx`를 인라인 `style.height`로 동적 적용(기본 64px).
  - `HeroSlideshow.tsx`: `autoAdvanceSeconds`/`objectFit`을 props로 받아 `setInterval` 주기와 `object-cover`/`object-contain` 클래스에 반영(둘 다 기본값 있어 하위호환).
  - `src/app/page.tsx`(지시문의 수정 대상 파일 목록에는 없었지만, `HeroSlideshow`에 새 설정값을 실제로 전달하는 연결 지점이라 함께 수정 — 그 외 로직 변경 없음): `hero_slideshow`의 `autoAdvanceSeconds`/`objectFit`도 함께 조회해 `HeroSlideshow`에 전달.
  - 검증: `npm run type-check`/`npm run lint` 통과 확인. `public-assets` Storage 버킷 존재 여부와 실제 업로드 동작은 라이브 환경에서 사용자 확인 필요(아래 참고).

## 2026-07-26 (EPIC-032)
- **EPIC-032: Homepage & Navbar site_settings integration**
  - `Navbar.tsx`: `site_settings.main_logo`를 클라이언트에서 조회(`fetchNavTabs`와 동일한 패턴)해 로고를 대체. `type: "image"`이고 `imageUrl`이 있으면 이미지로, 아니면 저장된 텍스트로 렌더링하고, 값이 없거나 테이블이 아직 라이브에 없으면 기존 하드코딩 텍스트("사일로 스토어")로 자동 대체(로고가 비는 일 없음). `Navbar`는 `useAuth`/`useSearchParams`에 의존하는 기존 Client Component 구조라 async Server Component로 전환하지 않고, EPIC-023의 `fetchNavTabs()`와 같은 최소 지연 클라이언트 패칭 패턴을 그대로 따름.
  - `src/app/page.tsx`: 그대로 남아있던 create-next-app 기본 템플릿을 실제 홈페이지로 교체. async Server Component로 `site_settings.hero_slideshow`를 서버에서 직접 조회(깜빡임 없음) — 슬라이드가 있으면 이미지·제목·설명 카드 그리드로, 없으면(테이블 미적용 포함) 브랜드 텍스트 + `/shop` 링크로 이루어진 기본 히어로 섹션을 표시.
  - 검증: `npm run type-check`/`npm run lint` 통과 확인. `site_settings` 테이블이 라이브 DB에 아직 없어(EPIC-026 후속) 실제 데이터 연동은 로컬에서 직접 확인하지 못함 — 아래 참고.

## 2026-07-26 (EPIC-031)
- **EPIC-031: Admin Post Management — pagination & is_hidden column**
  - `posts` 테이블에 관리자 전용 숨김 플래그 `is_hidden`(boolean, not null default false) 컬럼 추가(`docs/database-schema.sql`, 라이브 DB에는 아직 미적용 — 아래 ALTER TABLE 문을 Supabase SQL Editor에서 직접 실행 필요). EPIC-028에서 임시로 `posts.visibility='private'`를 재사용하던 "숨기기"가 작성자 본인의 비공개 설정과 뒤섞이던 문제를 해결.
  - `admin/posts/salon/page.tsx`: `toggleHidden`이 이제 `visibility` 대신 `is_hidden`을 업데이트하고, "공개 여부" 배지도 `is_hidden` 기준으로 표시.
  - `admin/posts/salon/page.tsx`: 임시 `.limit(200)`(`FETCH_LIMIT`)을 제거하고 `PAGE_SIZE=20` 단위 페이지네이션으로 교체 — `.range()` + `{ count: 'exact' }`로 총 건수를 가져와 이전/다음 버튼과 "N / 총 페이지" 표시 구현. `board_type` 필터 변경 시 페이지를 1로 재설정(같은 이벤트 핸들러에서 두 state를 함께 갱신해 불필요한 이중 조회 방지).

## 2026-07-26 (EPIC-030)
- **EPIC-030: Studio Portfolio Registration — submit UX fix**
  - 작업 지시가 요청한 `styling_projects`/`styling_project_media`/`styling_project_items` 3테이블 연동 폼(`/admin/projects/new`)은 EPIC-016에서 이미 완전히 구현되어 있었음(`docs/EPIC.md` 완료 목록 확인) — `page.tsx`가 3개 테이블 모두 다루는 폼을 갖추고 있고 `POST /api/styling-projects`(`src/app/api/styling-projects/route.ts`)가 이미 세 테이블에 순차 insert. 지시문의 "제목(title)" 필드는 실제 스키마에 없고(`client_name`+`industry`가 실제 필드 — 담당 업체를 기록하는 기능이라 의도적으로 다름), 사용자 확인 결과 스키마 변경 없이 기존 구현을 유지하기로 함.
  - 유일하게 지시문과 실제 동작이 다르던 지점만 수정: 등록 성공 시 기존에는 `/shop/projects/[id]`로 즉시 이동했으나, 지시문이 요구한 "성공 알림 후 폼 초기화" 동작으로 변경(`window.alert` + 전체 폼 상태 리셋). 더 이상 쓰이지 않는 `useRouter` import 제거.
  - `src/app/api/styling-projects/route.ts`는 기존 로직이 이미 요구사항(3테이블 순차 insert, 관리자 체크)을 충족해 변경 없음.

## 2026-07-26 (EPIC-029)
- **EPIC-029: Member Collections CRUD Implementation**
  - `mypage/CollectionModal.tsx` 신규 작성: `member_collections`의 8개 카테고리(`book`/`movie`/`music`/`artist`/`place`/`scent`/`brand`/`era`)를 모두 처리하는 범용 등록/수정 모달. `title`(필수)/`description`(선택)/`image_url`(선택) 입력, `item` prop이 `null`이면 신규 등록(insert), 실제 항목이면 수정(update) 모드로 동작.
  - `CollectionsPanel.tsx` 연동: "나의 보물"(주문 재사용) 탭을 제외한 8개 서브탭 상단에 "+ 아이템 추가" 버튼 배치, 각 카드에 수정(✏️)/삭제(🗑️) 아이콘 버튼 추가(삭제는 `window.confirm` 확인 후 실행). `@supabase/supabase-js` 브라우저 클라이언트로 `member_collections`에 직접 Insert/Update/Delete(EPIC-023 이후 admin CMS 관례와 동일하게 별도 API Route 없음), `reloadKey` state로 CUD 완료 시 목록 즉시 재조회.
  - 검증: 라이브 DB에 anon key REST(`/rest/v1/member_collections?select=id&limit=1`)로 조회한 결과 `200 []` — `docs/database-schema.sql` 상단 동기화 노트(EPIC-022가 "아직 라이브 미적용"이라 표기)와 달리 테이블이 실제로는 이미 라이브에 존재함을 확인(로그인 세션이 없어 실제 CUD 동작 자체는 사용자 확인 필요 — 아래 참고).

## 2026-07-26 (EPIC-028)
- **EPIC-028: Admin Post Management Implementation (Shop & Salon)**
  - `admin/posts/shop/page.tsx`를 Placeholder에서 실 구현으로 교체: `items` 테이블(`docs/database-schema.sql` §2)을 데이터 테이블로 조회, 시대(Time Slip 8종) 필터, 상태 뱃지(판매중/대여중/판매완료/비활성), 활성화·비활성화 토글(`status` ↔ `available`/`archived`), 삭제 버튼 구현.
  - `admin/posts/salon/page.tsx`를 Placeholder에서 실 구현으로 교체: `posts`+`boards` 조인(`docs/database-schema.sql` §6)으로 게시판 글을 최신순 데이터 테이블로 조회, `board_type` 7종 필터(`boards!inner` join으로 DB 단에서 필터링), 작성자 이름은 `public_profiles` 뷰로 별도 조회 후 클라이언트에서 병합(CLAUDE.md 규칙 — 다른 회원 이름은 항상 `public_profiles` 경유). 개인 마이피드 글(`board_id is null`)은 이 화면 대상에서 제외.
  - "숨기기" 기능은 이 스키마에 관리자 전용 노출 플래그가 없어, 기존 `posts.visibility`를 `'private'`로 바꾸는 방식으로 대체 구현(스키마 변경은 이번 EPIC 범위 밖 — 전용 컬럼이 필요하면 별도 Epic에서 추가할 것).
  - 두 페이지 모두 별도 API Route 없이 브라우저에서 anon key + RLS(admin bypass)로 직접 CUD(EPIC-023 이후 admin CMS 관례 유지).
  - 검증: 실제 물품 상태 토글(판매중→비활성→판매중 복원), 시대 필터, 게시판 글 board_type 필터(패트론 라운지 필터 시 정상적으로 0건) 모두 실 데이터로 확인.

## 2026-07-26 (EPIC-027)
- **EPIC-027: BugFix — Navigation Editor에 저장(Save) 기능 추가**
  - 원인: `NavNodeEditor`/카테고리 행 입력이 `onBlur`/`onChange` 시점에 즉시 `update`를 호출하는 암묵적 autosave 방식이라, 사용자에게 "저장됐다"는 아무 신호도 없어 변경이 반영 안 되는 버그처럼 보였음(실제로는 포커스를 벗어나야만 저장되고, 클릭 순서에 따라 저장 타이밍이 눈에 안 보이는 문제도 있었음).
  - 조치: `shared.tsx`의 `NavNodeEditor`를 로컬 draft 상태 기반으로 변경 — 입력은 화면에만 즉시 반영되고, 필드가 원본과 달라지면(`dirty`) 활성화되는 명시적 **"저장"** 버튼을 눌러야 실제로 `site_navigations`에 `update`가 나간다. 저장 성공 시 "저장됐어요." 인라인 피드백을 2초간 표시. 카테고리(`site_categories`) 행도 동일한 패턴의 신규 `CategoryRowEditor`로 통일(`top-tabs/page.tsx`에서 인라인 코드 교체).
  - `updateNavRow`/`updateCategory`(top-tabs/sidebar-left/sidebar-right 3개 페이지)를 성공 여부(`Promise<boolean>`)를 반환하도록 변경해, 저장 버튼이 실패 시 "저장됐어요" 피드백을 보여주지 않도록 함(에러 배너는 기존처럼 페이지 상단에 별도 표시).
  - 검증: `sidebar-left` 페이지에서 "사일로상점" 탭 이름을 실제로 수정 → 저장 → 페이지 새로고침 후에도 값이 유지됨을 확인(DB 반영 확인), Navbar 실제 탭 이름도 즉시 바뀜을 확인. 테스트 후 원래 값으로 복원.

## 2026-07-26 (EPIC-026)
- **EPIC-026: Admin Homepage Settings Implementation**
  - 신규 테이블 `site_settings`(`docs/database-schema.sql` §13): `setting_key`(unique)/`setting_value`(jsonb)/`updated_at` — 설정 종류가 늘어나도 스키마 변경 없이 key만 추가하면 되는 key-value 저장소. `main_logo`/`hero_slideshow`/`home_curation` 3개 키 시드 포함(라이브 DB 미적용, Supabase SQL Editor 실행 필요).
  - RLS: 조회는 전체 공개, 추가/수정/삭제는 `members.is_admin` bypass 전용(EPIC-023/024와 동일 패턴).
  - `admin/navigation/settings/page.tsx`를 Placeholder에서 실 구현으로 교체: 메인 로고(텍스트/이미지 URL), 슬라이드쇼(이미지 URL·타이틀·설명 배열, 추가/삭제), 노출 필터(도메인+카테고리 slug 목록+정렬 기준) 3개 섹션을 각각 조회 후 "저장하기" 클릭 시 `upsert(onConflict: setting_key)`로 갱신. 별도 API Route 없이 브라우저에서 anon key + RLS로 직접 CUD(EPIC-023 관례 유지).
  - 작업 전 `docs/database-schema.sql`을 `docs/backups/database-schema-20260726-1214.sql`로 백업.
  - 검증: 라이브 DB에 테이블이 아직 없어 조회 시 에러 배너가 표시되지만 폼은 기본값으로 정상 렌더링되고, 슬라이드 추가/삭제 등 UI 상호작용은 정상 동작함을 확인.

## 2026-07-26 (EPIC-025)
- **EPIC-025: Admin Dashboard Restructure & Nested Routing**
  - `admin/layout.tsx` 메인 탭 변경: 결제 관리 / 메뉴·카테고리 관리 / **전체 글 관리(신규)** / 스튜디오 포트폴리오 등록(문구 "스타일링"→"스튜디오"). 활성 탭 판정을 정확 일치에서 `startsWith`로 변경해 하위 라우트에서도 상위 탭이 하이라이트되도록 함.
  - `/admin/navigation`을 2-Depth로 분리: `layout.tsx`(서브 탭: 홈페이지 설정 관리/상단 탭·카테고리 관리/왼쪽 사이드바 메뉴 관리/오른쪽 사이드바 메뉴 관리) + `page.tsx`는 `top-tabs`로 리다이렉트. 기존 한 페이지에 있던 코드를 `target_type`별로 분리: `top-tabs/page.tsx`(tab·dropdown 최상위 + site_categories), `sidebar-left/page.tsx`(sidebar_left), `sidebar-right/page.tsx`(sidebar_right). 공통 타입/상수/트리 편집 UI(`NavNodeEditor`)는 `navigation/shared.tsx`로 추출해 3개 페이지가 공유.
  - `navigation/settings/page.tsx` 신규: "홈페이지 설정 관리" Placeholder(메인 로고/슬라이드쇼/노출 필터 카드 3개, 데이터 연동 없음).
  - `/admin/posts` 신규(2-Depth): `layout.tsx`(서브 탭: [사일로 상점]/[살롱데상] 카테고리별 글 관리) + `page.tsx`(→`shop`으로 리다이렉트) + `shop/page.tsx`(사일로 보물들/온라인 도슨트/사일로 Heritage 섹션 Placeholder) + `salon/page.tsx`(Community/Membership/Gallery/Library 섹션 Placeholder). 실제 글 목록 조회/관리 기능은 이번 EPIC 범위 밖(Placeholder만).
  - 검증: 하드 리로드로 `/admin/navigation`, `/admin/posts` 진입 시 각각 `top-tabs`, `shop`으로 정상 리다이렉트되고 서브 탭·본문이 정상 렌더링됨을 확인. `top-tabs`/`sidebar-left`/`sidebar-right` 페이지가 `target_type` 기준으로 정확히 필터링되어 겹치지 않음을 확인.

## 2026-07-26 (EPIC-024)
- **EPIC-024: Admin Dashboard Layout & Auth Guard Fix**
  - 원인: `/admin/*` 페이지들의 인증 가드는 이미 `loading`/`memberLoading`이 true인 동안 리다이렉트를 보류하는 형태였지만, `session`이 `null`→실제 세션으로 막 바뀌는 바로 그 렌더에서 AuthProvider(부모)의 member 재조회 effect가 `memberLoading`을 `true`로 재무장하기 전에, 페이지(자식)의 가드 effect가 먼저 실행되는 React의 effect 실행 순서(자식이 부모보다 먼저) 때문에 `memberLoading=false`(직전 값)를 잘못 신뢰해 `member`가 아직 `null`인 상태로 "관리자 아님" 판정 후 `/`로 리다이렉트되던 버그. 하드 리로드로 관리자 페이지에 직접 진입할 때만 재현됨.
  - 조치: `src/app/admin/layout.tsx` 신규 작성 — 이전 렌더의 `session` 참조를 `useRef`로 기억해두고, `session`이 방금 바뀐 바로 그 렌더는 판정을 한 번 건너뛰어 AuthProvider가 `memberLoading`을 재무장할 시간을 준 뒤 다음 렌더에서 최종 판정한다. 로딩 중에는 "확인 중..." 문구만 표시.
  - `/admin/payments`, `/admin/navigation`, `/admin/projects/new` 3개 페이지의 개별 인증 가드(useEffect 리다이렉트 + `if (...) return null`)를 제거하고 레이아웃으로 통합 — 각 페이지는 `session`(API 호출용 access_token)만 계속 사용.
  - `admin/layout.tsx`에 상단 서브 네비게이션 추가: "결제 관리"/"메뉴·카테고리 관리"/"스타일링 포트폴리오 등록" — Navbar 상단 탭과 동일한 시각 패턴(`border-b-2` 활성 탭 스타일) 재사용.
  - 검증: 하드 리로드로 `/admin/navigation` 직접 진입 시 더 이상 `/`로 튕기지 않고 정상 접근됨을 확인(이전에는 `/admin/projects/new`에서도 동일 버그 재현됨). 서브 네비게이션을 통한 관리자 페이지 간 클라이언트 사이드 이동도 정상 확인.

## 2026-07-26 (EPIC-023)
- **EPIC-023: Dynamic Navigation & Category Admin CMS**
  - `src/lib/navConfig.ts`를 하드코딩 배열(`NAV_TABS`)에서 DB 조회 함수(`fetchNavTabs()`)로 전환. `site_navigations`(신규 테이블, 자기참조 트리)를 조회해 기존과 동일한 `NavTab[]` 형태로 조립 — 조회 실패/시드 미적용 시 기존 하드코딩 값과 동일한 `FALLBACK_NAV_TABS`로 자동 대체되어 화면이 비지 않음. `getActiveNavTabKey()`는 순수 pathname 로직이라 변경 없음.
  - `Navbar.tsx`는 `useEffect`에서 `fetchNavTabs()`를 호출해 상태로 보관하고, 그 상태를 순회해 렌더링 — 기존 UX/스타일(좌/우 사이드바, 드롭다운, 중앙 정렬 탭 등)은 완전히 동일하게 유지.
  - 신규 테이블 2개 설계(`docs/database-schema.sql` §12): `site_navigations`(상단 탭/사이드바 그룹/드롭다운 항목을 하나의 트리로 표현), `site_categories`(상점/살롱/컬렉션/도슨트 카테고리, domain별). 기존 navConfig.ts 데이터 + 4개 도메인 카테고리(Time Slip 8종/도슨트 era 11종/EPIC-022 컬렉션 8종/살롱 게시판 13종)를 그대로 옮기는 Seed SQL 포함 — 아직 라이브 DB 미적용, Supabase SQL Editor에서 직접 실행 필요.
  - `/admin/navigation` 신규 관리자 페이지: 네비게이션 트리(추가/수정/삭제/순서/활성 토글)와 카테고리(도메인별 추가/수정/삭제/상위-하위 지정) CRUD UI. 이번 EPIC의 수정 대상 파일 범위에 API Route가 포함되지 않아, 별도 Route Handler 없이 브라우저에서 anon key로 직접 CUD하고 **RLS(관리자 bypass)를 실제 권한 강제 계층**으로 사용 — 다른 admin 화면들의 "Route Handler에서 is_admin 체크" 관례와는 다른 방식임을 명시.
  - RLS: 두 테이블 모두 조회는 전체 공개, 생성/수정/삭제는 `members.is_admin` bypass 전용.
  - 작업 전 `docs/database-schema.sql`을 `docs/backups/database-schema-20260726-0824.sql`로 백업.

## 2026-07-26 (EPIC-022)
- **EPIC-022: MyPage Restructure & Schema Definition**
  - `/mypage`를 11개 탭(나의 컬렉션[9개 서브메뉴]/나의 위시리스트/팔로우/나의 살롱/나의 도슨트 수료증/나의 공간/나의 전시회/받은 배지/내가 쓴 댓글/타임라인/방문자 기록) 구조로 재구성.
  - 신규 DB 테이블 4개 설계(`docs/database-schema.sql` §11에 DDL 반영, 라이브 DB에는 미적용 — 사용자가 Supabase SQL Editor에서 직접 실행 필요): `member_collections`, `member_follows`, `member_badges`, `member_visitors`.
  - "나의 보물"(orders), "나의 위시리스트"(wishlists), "내가 쓴 댓글"(comments)은 기존 데이터를 그대로 재연결. 나머지 8개 컬렉션 서브메뉴는 `member_collections.category`로 분기.
  - "나의 살롱"/"나의 도슨트 수료증"/"나의 공간"/"나의 전시회"/"타임라인" 5개 탭은 이번 EPIC에서 데이터 소스가 지정되지 않아 **추측성 테이블/연동 없이 Empty State만** 구현(`PlaceholderPanel`).
  - `member_follows`/`member_visitors`는 조회 전용으로 구현 — 실제로 행을 적재하는 쓰기 경로(팔로우 버튼, 방문 기록 insert)는 `/u/[memberId]` 등 이번 작업 범위 밖 파일에 속하므로 미구현. 현재는 항상 빈 목록으로 보임(정상 동작).
  - 기존 마이페이지의 "갤러리"/"오늘의 영감"/"일반 글" 섹션(개인 마이피드 글 표시)은 새 11탭 구조에 포함되지 않아 제거함 — 동일한 개인 글은 `/me`에서 계속 조회 가능(`docs/content-blueprint.md` §7 참고). "멤버십"(등급/포인트) 정보는 11탭 밖의 계정 요약으로 상단에 유지.
  - 신규 파일: `src/components/mypage/{EmptyState,MyPageNav,mypageConfig}.tsx(.ts)`, `src/components/mypage/panels/{CollectionsPanel,WishlistPanel,FollowPanel,BadgesPanel,CommentsPanel,VisitorsPanel,PlaceholderPanel}.tsx`.
  - 작업 전 `docs/database-schema.sql`을 `docs/backups/database-schema-20260726-0808.sql`로 백업.

## 2026-07-26 (EPIC-021)
- **EPIC-021: Google 로그인 "No API key found in request" 재확인 및 해결**
  - EPIC-020에서 수정한 `.env.local`의 `/rest/v1/` 접미사 제거가 실제로는 **해당 파일을 고친 PC에만 적용**되어 있었음. `.env.local`은 `.gitignore` 대상이라 git으로 공유되지 않고, Next.js는 `NEXT_PUBLIC_*` 환경변수를 빌드/컴파일 시점에 번들에 박아 넣으므로, 값을 고친 뒤 개발 서버를 재시작하지 않으면 브라우저에 여전히 예전 URL이 남아있는 번들이 서비스됨.
  - 재현 시도: 서버를 완전히 재시작한 뒤 Network 탭으로 실제 요청을 확인한 결과, `redirect_uri`가 `.../auth/v1/callback`로 정상 생성되고 있었고 컴파일된 번들의 `NEXT_PUBLIC_SUPABASE_URL` 값도 정상이었음 — 코드/설정 자체는 이미 올바른 상태였음을 증거로 확인.
  - 사용자가 실제 오류를 겪은 PC에서 ① `.env.local`의 `/rest/v1/` 제거 ② 개발 서버 완전 재시작 ③ 브라우저 새로고침을 수행한 뒤 Google 로그인 버튼이 정상적으로 Google 로그인 페이지로 이동함을 확인 — **해결 완료**.
  - 근본 교훈: `.env.local`은 PC별로 각자 만들어야 하는 로컬 전용 파일이며, 값을 바꾼 뒤에는 반드시 개발 서버를 재시작해야 한다는 점을 README.md에 명시(아래 항목).

## 2026-07-26 (문서)
- `README.md`에 "개발 환경 설정(.env.local)" 섹션 신규 추가 — 신규 개발자가 5분 안에 로컬 환경을 구성할 수 있도록 `.env.local` 작성법, Supabase Dashboard(Project Settings → Data API)에서 값 찾는 법, 필수 규칙(`NEXT_PUBLIC_SUPABASE_URL`은 Project URL만 사용하고 `/rest/v1/` 등 경로를 절대 붙이지 않음, `NEXT_PUBLIC_SUPABASE_ANON_KEY`는 `anon`/`public` 키만 사용, `service_role` 키 금지)을 명시. EPIC-020에서 실제로 겪은 장애를 근거로 작성.
- `PROJECT_BLUEPRINT.md` §2(기술 스택)에 위 README 섹션을 가리키는 요약 인용구 추가.

## 2026-07-26 (EPIC-020)
- **EPIC-020: Google OAuth "No API key found in request" 버그 수정**
  - 원인: `.env.local`의 `NEXT_PUBLIC_SUPABASE_URL`에 남아있던 `/rest/v1/` 접미사. `@supabase/auth-js`는 `new URL("auth/v1", baseUrl)`로 인증 엔드포인트를 만드는데, base URL이 `/rest/v1/`로 끝나 있으면 결과가 `.../rest/v1/auth/v1/...`로 잘못 합성됨. Supabase 엣지가 이 경로를 PostgREST(`/rest/v1/*`)로 라우팅하면서 `apikey` 파라미터를 요구해 "No API key found in request" 오류가 발생(로그인뿐 아니라 이전에 보고된 `/shop` 등 데이터 조회 실패의 근본 원인과 동일).
  - 조치: `NEXT_PUBLIC_SUPABASE_URL`에서 `/rest/v1/` 접미사 제거, 순수 프로젝트 URL만 남김. `src/lib/supabaseClient.ts`/OAuth 구현 자체는 정상이었으므로 코드 변경 없음.
  - 작업 전 `docs/database-schema.sql`을 `docs/backups/database-schema-20260726-0727.sql`로 백업(스키마 변경 없음, 절차 준수 목적).

## 2026-07-26 (EPIC-019)
- **EPIC-019: Studio Navigation Rename**
  - 상단 메인 탭 라벨 "공간 문의" → "스튜디오"로 변경(`src/lib/navConfig.ts`의 `label` 문자열 한 줄만 수정).
  - 기능/URL/Route 변경 없음: `space_inquiry` key, `/space-inquiry/*`·`/rental?floor=*` 경로, 드롭다운 상호작용 방식(`type: "dropdown"`) 모두 그대로.
  - 드롭다운 메뉴 구성(공간 촬영 대관/물품 대여/공간 스타일링) 변경 없음.
  - 작업 전 `docs/database-schema.sql`을 `docs/backups/database-schema-20260726-0722.sql`로 백업(스키마 변경 없음, 절차 준수 목적).

## 2026-07-26
- **EPIC-018: 상단 탭 재구성**
  - 상단 메인 탭에서 "스튜디오 대관" 탭 제거. 최종 4개 탭으로 재구성: 사일로상점 / 살롱데상 / 공간 문의 / 마이페이지.
  - 상단 탭 행을 화면 중앙 정렬로 변경(`justify-center`). 로고/우측 계정 영역 위치는 그대로 유지.
  - 기존 "스튜디오 대관" 기능(1층/2층 공간 대관 예약, `/rental?floor=1f_silostore`·`/rental?floor=2f_salon`)을 URL 변경 없이 "공간 문의" 드롭다운으로 통합. "공간 촬영 대관" 항목이 기존 placeholder(`/space-inquiry/shoot-rental`) 대신 실제 예약 페이지로 연결되도록 교체, 2층 항목 신규 추가.
  - "마이페이지"를 계정 영역과 별개로 정식 4번째 상단 탭(단순 링크)으로 추가 — 계정 영역의 기존 마이페이지 링크는 유지되어 두 진입점이 공존.
  - `src/lib/navConfig.ts`에 `NavTabType`(`sidebar-left`/`sidebar-right`/`dropdown`/`link`) 도입, `getActiveNavTabKey()`에 `/rental`→`space_inquiry`, `/mypage`→`mypage` 분기 추가.
  - `src/components/Navbar.tsx`를 `NAV_TABS`를 순회하며 `type`에 따라 렌더링 방식만 분기하는 범용 구조로 리팩터링(탭별 하드코딩 제거) — 좌/우 사이드바, 드롭다운 로직 모두 tab-agnostic화.
  - 작업 전 `docs/database-schema.sql`을 `docs/backups/database-schema-20260726-0658.sql`로 백업(스키마 변경 없음, 절차 준수 목적).

## 2026-07-23
- 작업 규칙 정비: `NEXT_TASK.md`, `CHANGELOG.md` 신설, `package.json`에 `type-check` 스크립트 추가.
- swing2app 구버전 앱 기능/데이터 이식 (9개 항목):
  - `item_personas` 캐릭터 은행(68명) + `items.persona_id`, `/shop/[id]` 이전 주인 사연 캐릭터 표시
  - `items.category` 8개 시대(Time Slip) CHECK 제약 + `/shop` 필터 탭
  - `docent_contents.figure_name` + `/docent` 인물별 그룹 보기
  - `boards.board_type`에 `adoption_story`/`archive`/`qna` 추가, `posts.order_id` 컬럼
  - `downloads` 테이블 + `/downloads` 페이지 (관리자 전용 업로드)
  - `daily_checkins` + `/attendance` 출석체크 (2P 적립)
  - `polls`/`poll_options`/`poll_votes` + 집계 뷰 + `/polls`
  - `/mypage`에 "Your Treasures" 구매 물품 섹션
- `silo-store`를 독립 git 저장소로 분리 (`git init` + Initial commit, 기존 `.gitignore` 그대로 사용).
- `PROJECT_BLUEPRINT.md` 신규 생성 (프로젝트 개요/아키텍처 문서, 코드 기준 작성 + TODO 섹션 분리).
- `docs/database-schema.sql`을 실제 운영 DB(Supabase Management API)와 전면 동기화하고, 이 프로젝트의 **유일한 공식 DB 스키마 문서(Single Source of Truth)**로 명시. 프로젝트 밖 `silostore_schema.sql` 사본은 더 이상 관리하지 않음.
