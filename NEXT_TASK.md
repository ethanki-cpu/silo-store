# NEXT_TASK

## 진행 중
- **HOTFIX-147.18(구현+실측 완료, 2026-08-27)**: 오른쪽 패널 스크롤 회귀(HOTFIX-147.15의 접기 기능 추가 때 wrapper에 높이를 안 줘서 발생) 수정 + 표지/이벤트 5개 화면(고대~왕정 표지 + 그리스/르네상스/바로크/로코코) 전부에 "제목"/"설명" 입력창을 설정 패널에 추가(`SlideOverlayConfig.description` 신설). 상세는 CHANGELOG.md 동명 항목 참고. `tsc`/`lint` 0 errors(78 warnings). 로컬에서 스크롤 실측(1183/551, 스크롤 가능) + 제목/설명 입력 → 캔버스 반영 → elementFromPoint로 실제 최상단 렌더링 확인 + 이벤트 드롭다운 4개가 서로 설정이 안 섞이는 것 확인. **다음에 확인 필요**: 실제 운영 환경에서 관리자가 5개 화면 각각에 실제 제목/설명/슬라이드를 채워보고 기대한 대로 보이는지 확인.
- **HOTFIX-147.17(구현+실측 완료, 2026-08-27)**: 타임라인 표지 오버레이가 `opacity:1`인데도 실제로는 TL3 자체 `.tl-slide-content-container`(z-index:3, 그 안 `.tl-slider-touch-mask`는 25)에 완전히 가려져 화면엔 한 번도 안 보이고 있었다 — HOTFIX-147.12 이후 이 오버레이를 opacity/DOM 존재로만 "보인다"고 판단해왔고 실제 화면(`elementFromPoint`/스크린샷)으로는 이번에 처음 검증했다. `TimelineCoverOverlay`에 `z-30` 명시로 수정. **HOTFIX-147.16에 적었던 "저장을 안 눌러서 그렇다"는 진단은 틀렸음 — 철회.** 상세는 CHANGELOG.md 동명 항목 참고. `tsc`/`lint` 0 errors(78 warnings). 스크린샷으로 수정 전(흰 배경만)/후(사용자가 보낸 그리스 그림 실제 표시) 직접 대조 확인. **다음에 확인 필요**: dev.silostore.net 배포 반영 후 "고대~왕정" 실제로 표지 이미지가 보이는지, 나머지 3개 카테고리(혁명~제국/프로이트~대중문화/디지털~AI문화)도 표지 자유편집을 켠 경우 동일하게 확인. **사용자에게 확인 요청함**: 이 세션 중 "그리스" 이벤트 오버레이 테스트로 두 차례 저장했는데, 그 시점에 사용자가 동시 편집 중이었다면 서로의 저장이 충돌했을 수 있음 — 작업 내용 유실 여부 확인 필요.
- **HOTFIX-147.16(구현+실측 완료, 2026-08-27)**: "각 요소들의 위아래 폭을 드래그로 조정하게 해달라" — `EditableBlockFrame`에 하단 드래그 핸들 추가(`frameHeightPx`, 더블클릭으로 자동 높이 복귀). 블록 파일 7개를 안 건드리고 공유 프레임 하나만 고쳐 전체 적용. 상세는 CHANGELOG.md 동명 항목 참고. `tsc`/`lint` 0 errors(78 warnings). 로컬에서 519px→619px 드래그 확장 + 더블클릭 원복 확인.
- **HOTFIX-147.15(구현+실측 완료, 2026-08-27)**: HOTFIX-147.14로도 "삭제 버튼이 안 보인다"는 신고가 재발 — 사용자가 직접 지목한 대로 오른쪽 SettingsSidebar 오버레이(z-20)가 툴바(opacity:1이어도 z-10)를 시각적으로 덮고 있던 게 진짜 원인이었다. 툴바 z-index를 z-40으로 올려 해결 + 사용자 추가 요청으로 좌우 패널(Toolbox/SettingsSidebar) 접기/펼치기 토글도 추가. 상세는 CHANGELOG.md 동명 항목 참고. `tsc`/`lint` 0 errors(78 warnings). 로컬 dev 서버에서 `document.elementFromPoint()`로 툴바 좌표의 최상단 요소가 실제로 툴바 버튼인 것, 패널 토글이 정상 작동하는 것 확인. **다음에 확인 필요**: 실제 운영 환경에서 관리자가 직접 컨테이너 삭제 버튼을 클릭해 실제로 삭제까지 되는지(이번 세션은 프로덕션 콘텐츠 훼손을 피하려 클릭 직전까지만 검증하고 실제 삭제는 실행하지 않음), 좌우 패널 접기가 실제 화면에서 자연스러운지 확인.
- **HOTFIX-147.14(구현+실측 완료, 2026-08-27)**: Craft 에디터에서 블록(특히 컨테이너)을 클릭해 선택해도 삭제 버튼이 안 보인다는 신고 — 툴바가 순수 CSS 호버로만 나타나 선택 상태와 무관하게 마우스가 벗어나면 사라지던 게 원인. `EditableBlockFrame`이 Craft.js의 선택 상태(`node.events.selected`)도 함께 보도록 수정해, 한 번 선택하면 마우스 위치와 무관하게 계속 보이게 함. 상세는 CHANGELOG.md 동명 항목 참고. `tsc`/`lint` 0 errors(78 warnings). 로컬 dev 서버로 컨테이너 선택 후 툴바 opacity가 1로 유지되는 것 직접 확인. 모든 Craft 블록이 공유하는 컴포넌트라 전체에 자동 적용됨. **다음에 확인 필요**: 없음(구조적으로 명확한 버그였고 수정도 직접적).
- **HOTFIX-147.13(구현+실측 완료, 2026-08-27)**: 표지 전용이던 자유편집(배경 슬라이드+드래그 텍스트+폰트/색상 등)을 그리스/르네상스/신고전주의 같은 개별 이벤트 화면에도 적용 가능하게 확장 — 새 `eventOverlays` 맵(이벤트 unique_id별 독립 설정) + 게시판 설정에 "개별 화면(이벤트) 자유 편집" 이벤트 드롭다운 추가. 상세는 CHANGELOG.md 동명 항목 참고. `tsc`/`lint` 0 errors(78 warnings). 로컬 dev 서버 + 관리자 세션으로 "혁명~제국" 페이지에서 "신고전주의" 이벤트를 골라 자유편집 켜고 실제 이미지 업로드 → TimeNav로 그 이벤트에 실제로 이동해 오버레이가 뜨는 것, 설정 안 한 "리전시"에서는 안 뜨는 것, 표지로 돌아가면 표지 자체 설정으로 독립적으로 전환되는 것까지 전부 확인. 4개 온라인 도슨트 카테고리 페이지가 전부 같은 컴포넌트를 공유해 자동 적용됨(카테고리별 구현 없음). **다음에 확인 필요**: 실제 운영 환경(dev.silostore.net)에서 관리자가 그리스/르네상스/바로크/로코코 등 원하는 이벤트를 직접 골라 슬라이드/텍스트를 채워보고 기대한 대로 보이는지 확인 — 로컬 검증은 "신고전주의" 1곳으로만 했고 저장(DB persist)까지는 이번 세션에서 하지 않음(테스트 데이터를 프로덕션에 남기지 않으려 의도적으로 저장 안 함).
- **HOTFIX-147.12(구현+실측 완료, 2026-08-27)**: 타임라인 "표지(첫 화면) 자유편집"에 슬라이드/텍스트/폰트를 넣어도 절대 반영이 안 되던 근본 원인 2건 수정 — (1) 표지 판별 기준이 고정 문자열 `"title-headline"`이었는데 TL3가 실제로는 headline을 slugify해 페이지마다 다른 unique_id를 스스로 생성해 한 번도 일치한 적이 없었음(콘솔 로그로 실측 확인) → 알려진 이벤트 id 목록에 없는 슬라이드는 전부 표지로 판정하도록 로직 반전. (2) 관리자가 이미 렌더링된 타임라인을 보다가 체크박스를 켜는 실사용 순서에서 MutationObserver가 "새로 추가되는 순간"을 영영 못 봐 오버레이가 안 붙었음 → effect 시작 시 이미 존재하는지 먼저 확인하도록 수정. 상세는 CHANGELOG.md 동명 항목 참고. `tsc`/`lint` 0 errors(77 warnings). 로컬 dev 서버 + 관리자 세션으로 "혁명~제국" 페이지 Craft 에디터에서 실제로 체크박스 on → 배경 이미지 업로드(진짜 Supabase Storage 업로드, 합성 File 객체로 검증) → 새로고침 후 체크박스만 켜도 즉시 배경/텍스트가 보이는 것까지 전부 확인. 4개 온라인 도슨트 카테고리 페이지가 전부 같은 컴포넌트를 공유해 자동 적용됨. **다음에 확인 필요**: 실제 운영 환경(dev.silostore.net)에서 관리자가 직접 다시 한번 확인 권장(로컬 재현/수정과 별개로). 함께 요청됐던 "컨테이너/섹션 삭제", "컨테이너에 사진 슬라이드 넣기"는 이미 구현돼 있음을 확인함(코드 변경 없음) — 삭제는 모든 블록에 🗑 아이콘으로, 슬라이드는 Toolbox의 "슬라이드쇼" 블록을 드래그해 이미 가능. "그리스/르네상스/바로크/로코코 등 하위 이벤트 화면에도 같은 자유편집을" 요청은 아직 미구현 — TL3가 이벤트 슬라이드를 완전히 자체 렌더링해 표지처럼 오버레이를 얹을 훅이 현재 없고, 이벤트당 별도 오버레이 시스템을 새로 설계해야 하는 규모가 큰 신규 기능이라 사용자 확인 후 착수 필요(현재는 site_navigations의 "대표 이미지"/"카테고리 소개" 1장짜리 편집만 가능, HOTFIX-147.5).
- **HOTFIX-147.11(구현+실측 완료, 2026-08-27)**: "썸네일 호버 시 이미지 자동 슬라이드"가 사이트 전체 갤러리 게시판에서 한 번도 작동한 적이 없던 근본 원인(콘텐츠가 전부 인스타그램 캐러셀 embed라 기존 추출 로직이 media 0개로 계산) 발견+수정 — 인스타그램 캐러셀 media_urls를 hover 슬라이드 소스로 배치 조회하도록 `GalleryModule`/`instagramFeed.ts`/`instagramShortcode.ts` 추가. 69개 게시판 전체 `widget_settings.galleryHoverAutoSlide`를 `true`로 백필 + 코드 기본값도 true로 전환. 상세는 CHANGELOG.md 동명 항목 참고. `tsc`/`lint` 0 errors(77 warnings). 로컬 dev 서버에서 `daily-silo`(비디오 슬라이드 자동재생 확인)/`silo-s-exhibition`(10장 캐러셀 순환 확인) 2개 게시판 직접 hover 검증 완료. **다음에 확인 필요**: (1) 69개 게시판 중 나머지 gallery-타입 게시판들(af06e86e/silo-story/treasures/ethan-s-bluenotes/art/comedy/fashion/history/movie/science/warmth 등)도 관리자가 직접 dev.silostore.net에서 마우스 hover로 육안 확인 권장 — 메커니즘은 동일하고 DB 백필도 전부 확인했지만 개별 화면 확인은 2곳만 함. (2) 게시글 하나에 인스타그램 embed가 여러 개 있는 경우 첫 번째 embed의 permalink만 사용(`extractInstagramPermalink`) — 이 프로젝트 콘텐츠 특성상 게시글당 embed 1개가 일반적이라 문제 없을 것으로 보이나, 여러 개인 글이 있다면 두 번째부터는 hover 슬라이드에 반영 안 됨.
- **HOTFIX-147.10(구현+전체 재동기화 완료, 2026-08-26)**: 인스타그램 캐러셀에서 영상이 사진으로 보이던 문제의 근본 원인을 실측으로 특정 — Graph API가 특정 오래된 영상 자식의 media_url을 영구히 안 줌(우회 불가, 외부 API 한계). "IMAGE로 강등 저장" 대신 정직하게 "VIDEO_THUMBNAIL"로 저장 + 재생 버튼 아이콘 표시로 수정. 상세는 CHANGELOG.md 동명 항목 참고. `tsc`/`lint` 0 errors. dev.silostore.net 관리자 세션으로 전체 CAROUSEL_ALBUM(230개) 재동기화 직접 실행 완료 — **52개 게시물**이 이 버그를 갖고 있었고 전부 정정됨(에러 0). 바로크 Act 1 실제 화면에서 재생 버튼 아이콘 노출 확인(스크린샷). **다음에 확인 필요**: 나머지 51개 게시물 중 몇 곳을 골라 재생 버튼 아이콘이 잘 보이는지, 대표 이미지(featured_image_url)가 깨지지 않는지 육안 확인.
- **HOTFIX-147.9(구현 완료, 관리자 육안 확인 필요, 2026-08-26)**: 타임라인(TL3) 표지 슬라이드가 모바일에서 텍스트 오버랩되던 버그를 표지 전용 자유배치 오버레이(배경 슬라이드/영상 + 드래그 가능 텍스트, `SiloTimelineEmbedBlock`)로 해결 + Craft `TextBlock`/타임라인 표지 텍스트에 색상피커(`input type=color`)와 폰트 업로드(`FontPicker` 재사용) 추가. 상세는 CHANGELOG.md 동명 항목 참고. `tsc`/`lint` 0 errors(77 warnings, 기존 기준선). **다음에 확인 필요**: 관리자 세션으로 "혁명~제국"(또는 다른 그룹/leaf) 타임라인 Craft 설정에서 "표지 자유 편집" 체크 → 텍스트 더블클릭 편집 → 드래그 이동/리사이즈 → 배경 이미지/영상 업로드까지 실제로 눌러보고, 모바일 뷰(375px)에서 겹침 없이 보이는지, 다른 역사적 사실 클릭 시 표지가 사라지고 되돌아오면 다시 나타나는지 확인 — 이번 세션은 로컬 dev 서버에 admin 세션을 안전하게 옮길 방법이 없어(다른 오리진 간 인증 토큰 이전 차단) 로그인 필요한 조작까지는 못 함.
- **HOTFIX-147.8(구현 완료, dev 실측 진행 중, 2026-08-26)**: HOTFIX-147.4의 순서 수정이 앞으로의 동기화에만 적용되고 이미 저장된 행(바로크 Act 1 포함)은 그대로였던 문제 — `/api/admin/instagram/resync-carousels`에 `igMediaId` 옵션을 추가해 특정 게시물 하나만 즉시 재동기화할 수 있게 함(기존 전체 페이지네이션 방식은 유지). `tsc` 0 errors. **다음에 확인 필요**: `develop` 배포 반영 후 바로크 Act 1(`ig_media_id: 17873896711921843`)에 대해 `igMediaId` 타겟 재동기화를 실행해 `media_item_types` 순서가 실제 인스타그램(영상이 첫 항목)과 일치하는지 확인, 다른 신고된 게시물(아르데코 Gertrude Lawrence 등)도 같은 방식으로 개별 확인 필요.
- **HOTFIX-147.7(구현+실측 완료, 2026-08-26)**: 4개 그룹 페이지의 히어로를 "고정 중앙정렬 텍스트"에서 "배경 슬라이드쇼(HeroSlideshowWidgetBlock) + 자유 배치 텍스트(TextBlock, 크기/색상/폰트 굵기 지정 가능)" 조합으로 재구성 + TextBlock에 PC/모바일 독립 드래그 위치(`mobilePosition`, `DeviceModeContext`, `freePositionResponsiveAttrs` + globals.css `@container` 규칙) 신규 지원. 상세는 CHANGELOG.md 동명 항목 참고. `tsc`/`lint` 0 errors. 관리자 세션으로 실제 Craft 에디터에서 텍스트 선택→좌표 확인→모바일 모드 전환→별도 위치 저장→PC 모드 복귀 시 원래 값 유지까지 직접 조작해 확인. **다음에 확인 필요**: (1) 실제 이미지(placehold.co 아닌 진짜 업로드 이미지)로 배경을 채웠을 때 텍스트 색상/위치가 보기 좋은지 육안 확인 — 이번 세션은 외부 placehold.co가 샌드박스에서 차단돼 배경 이미지 자체는 못 봤음(좌표/색상 계산은 JS로 직접 검증함). (2) 나머지 6개 자유배치 블록(컨테이너/이미지/버튼/영상/슬라이드쇼/게시판연동)에도 모바일 전용 위치가 필요해지면 TextBlock과 동일한 패턴(`supportsMobileOverride`, `mobilePosition` prop 추가)으로 손쉽게 확장 가능 — 인프라는 이미 범용으로 만들어둠.
- **HOTFIX-147.6(완료, 2026-08-26, DB 전용)**: 온라인 도슨트 리프 페이지 13개 전부에서 미설정 `quote`/`slide`/여분 `board` 위젯이 실제 방문자에게 placeholder 문구를 그대로 노출하던 문제 + 12개 페이지의 `gallery` 위젯이 board_id 미연결이던 문제를 사용자 승인 받아 Management API로 정리(37개 위젯 삭제 + 12개 gallery 위젯 연결). 코드 변경 없음. 로컬 dev 서버로 바로크 페이지 실측 확인.
- **HOTFIX-147.5(구현+실측 완료, 2026-08-26)**: "혁명~제국" 등 2단계 카테고리 타임라인이 하위 게시판 전체의 게시글을 집계해 보여주던 걸, 사용자 지시대로 "이 그룹 바로 아래 카테고리(페이지) 하나당 슬라이드 하나"로 교체 — `src/lib/timelineGroup.ts`의 `fetchNavGroup()`이 직계 자식만 가져오고, 각 카테고리 제목의 연대 접두사("1750~1850 신고전주의...")를 `parseCategoryTitle()`로 파싱해 TL3 날짜로 쓴다. 대표 이미지/소개는 site_navigations.thumbnail_url/description(이미 "사이트 구성 관리" → 카테고리 → 관리에서 편집 가능)을 쓰고 비었으면 그 카테고리 게시판의 첫 게시글 이미지로 대체. 리프 게시판 타임라인의 "표지(title) 슬라이드"(아무 이벤트도 선택 안 했을 때 보이는 첫 화면)에 이미지/설명이 전혀 없어 어디서 고치냐던 질문에도 대응 — `board.widget_settings.timelineHeroSlides[0]`(게시판 수정 → Timeline NG → "첫 화면 대표사진 히어로")을 그대로 재사용하도록 연결(새 편집 UI 불필요). `tsc`/`lint` 0 errors. 로컬 dev 서버 실측: "혁명~제국"(4개 카테고리, 정확한 연대·링크·대부분 폴백 썸네일), "고대~왕정"(4개), "프로이트~대중문화"(5개) 전부 확인. **다음에 확인 필요**: 관리자가 실제로 site_navigations의 "대표 이미지"/"카테고리 소개"를 채워보고 타임라인에 반영되는지, 그리고 게시판 수정에서 히어로 슬라이드를 설정한 뒤 그 리프 게시판 타임라인의 표지 슬라이드에도 반영되는지 육안 확인.
- **HOTFIX-147.4(구현 완료, 실측 미검증 — 2026-08-26)**: HOTFIX-143.5로 캐러셀 "항목 누락"은 고쳤지만, 바로크 Act 1이 여전히 첫 항목(영상)이 사진으로 보인다는 재신고 — DB 실측으로 `media_item_types`에 VIDEO가 항상 배열 맨 뒤로 밀려 저장되는 걸 확인(누락이 아니라 순서 자체가 틀어지는 별개 버그). Graph API `children{...}`처럼 자식 서브필드를 중첩 요청하면 순서가 깨지는 게 원인으로 보여, `children`을 서브필드 없는 bare edge로만 요청하고(순서 보존) 각 자식의 실제 media_type/media_url은 기존 `resolveChildMediaUrl()`의 개별 조회로 채우도록 수정(`/api/instagram/fetch`, `/api/admin/instagram/resync-carousels` 둘 다). `tsc` 0 errors. **로컬엔 `IG_ACCESS_TOKEN`이 없어 이 가설을 실제 Graph API로 검증 못 함** — 관측 패턴과 일치하는 근거 있는 수정이지만 확신은 아님. **사용자가 직접 확인해야 하는 것**: 배포 후 `/admin/instagram`에서 "캐러셀 누락 항목 일괄 복구"를 다시 클릭 → 바로크 Act 1의 캐러셀 순서가 실제 인스타그램과 일치하는지 확인, 여전히 틀리면 알려줄 것(원인을 더 조사해야 함).
- **HOTFIX-147.3(구현+DB 반영 완료, 2026-08-26)**: Timeline NG 리프 게시판에 히어로 슬라이드쇼(기존 `HeroSlideshow.tsx` 재사용, `boards.widget_settings.timelineHeroSlides`+`BoardForm.tsx` 편집 UI) 추가, TimeNav 마커 상시 썸네일 버그 수정(`media.thumbnail` 누락), 그리고 온라인 도슨트 2단계 카테고리 페이지 4개(고대~왕정/혁명~제국/프로이트~대중문화/디지털~A.i 문화)를 Craft 에디터 가능한 페이지로 전환 + 하위 3단계 게시판 전부를 모으는 통합 타임라인(`src/lib/timelineGroup.ts` `resolveGroupBoardIds()`, `/api/timeline/events?group=`, 신규 Craft 블록 `SiloTimelineEmbedBlock`) 적용 완료. 상세는 CHANGELOG.md 동명 항목 참고. `tsc`/`lint` 0 errors. 로컬 dev 서버 + Claude Browser 툴로 "고대~왕정"(히어로+통합 타임라인+상시 썸네일 스크린샷 확인, 15건), "혁명~제국"(15건, 사용자가 원래 지목한 페이지), "프로이트~대중문화"(30건) 실측 확인. **다음에 확인 필요**: (1) 관리자로 로그인해 `/admin/pages`에서 이 4개 페이지의 "Craft 에디터 열기"가 정상 동작하는지(코드는 기존 `online-docent` 페이지와 동일한 범용 셸 재사용이라 구조적으로는 확인됨, 실제 클릭까지는 admin 세션이 없어 이번 세션에서 못 함). (2) "디지털~A.i 문화"는 site_navigations에 하위 카테고리가 아직 없어 타임라인이 "카테고리를 찾지 못했어요"만 보여줌(정직한 빈 상태, 크래시 아님) — slug가 "digital"인 게시판("대중 문화 pop culture")이 있는데 현재 어떤 nav에도 연결 안 돼 있음, 이게 원래 디지털~A.i 문화 자리에 들어갈 게시판이 맞는지 사용자 확인 필요(이전 세션 NEXT_TASK.md에도 이미 같은 미해결 항목으로 남아있던 것). (3) 리프 게시판 히어로는 관리자가 실제로 "게시판 관리"에서 슬라이드를 등록해봐야 화면에 나타남(기본은 비어있어 기존 화면과 동일) — 육안 확인 필요.
- **HOTFIX-147.1(구현+DB 권한 승인 완료, 2026-08-26)**: 아르데코 "Gertrude Lawrence" 게시글에서 타임라인 연대를 수정해도 저장이 안 된다는 신고 — 원인은 `posts.timeline_year`/`timeline_end_year`/`timeline_display_date` 3개 컬럼에 `authenticated` 역할의 UPDATE 권한이 애초에 빠져있었던 것(INSERT는 있어서 "새 글 작성"은 되지만 "수정"만 실패). 컬럼 권한은 테이블 전체에 걸리므로 `render_type='timeline_ng'`인 14개 게시판(온라인 도슨트 leaf 13개 + 사일로 타임라인) 전부 동일하게 재현되는 구조적 버그였음(게시판별 문제 아님). 추가로 `src/app/api/boards/[board_slug]/posts/[post_slug]/route.ts`의 수정 라우트가 UPDATE 에러 원인을 안 가리고 무조건 title/body만 남긴 축소 재시도로 조용히 넘어가 200 OK를 반환하던 위험한 폴백도 발견해 `updateError.code === "42703"`일 때만 타도록 수정(그 외엔 이제 실제 실패로 보임) — 이 폴백 자체가 body_json/featured_image_url/category/tags/thumbnail_visible까지 조용히 누락시키고 있었다. 사용자 승인 받아 `grant update (timeline_year, timeline_end_year, timeline_display_date) on posts to authenticated;` 실행 완료(`information_schema.column_privileges`로 재확인). `tsc` 0 errors. **다음에 확인 필요**: Gertrude Lawrence 게시글 및 다른 timeline_ng 게시판 글에서 타임라인 연대 수정 → 저장 → 타임라인 반영까지 실측 확인.
- **HOTFIX-143.5(구현 완료, 2026-08-26) — 사용자 액션 1개 남음**: 바로크 "Act 1"/"Act 2" + 아르데코 "Gertrude Lawrence" 게시글에서 인스타그램 캐러셀 항목이 통째로 사라지던 버그 — Graph API의 `children{...}` 서브필드가 특정 자식(특히 VIDEO)의 `media_url`을 비워서 내려주면 기존 코드가 그 항목을 조용히 건너뛰던 게 원인(Gertrude Lawrence는 3개 중 2개가 이렇게 사라져 캐러셀이 단일 이미지로 보였음). `src/lib/instagramGraph.ts`(신규)의 `resolveChildMediaUrl()`이 빈 자식을 단독 재조회+thumbnail 폴백으로 복구하도록 `/api/instagram/fetch`를 수정(앞으로 새 동기화부터 적용) + 기존에 이미 손상되어 저장된 캐러셀 행을 고치는 `/api/admin/instagram/resync-carousels`(신규)를 `/admin/instagram`에 "캐러셀 누락 항목 일괄 복구" 버튼으로 노출. `tsc`/`lint` 0 errors. 로컬엔 `IG_ACCESS_TOKEN`이 없어 Supabase Management API로 `instagram_feeds` 원본 데이터만 조회해 문제를 확인했고 실제 Graph API 재조회 동작은 로컬 검증 불가. **사용자가 직접 해야 하는 것**: 실제 IG 환경 변수가 있는 배포(dev.silostore.net 등)의 `/admin/instagram`에서 "캐러셀 누락 항목 일괄 복구" 버튼 클릭 — 클릭 후 이 세 게시글(및 다른 캐러셀 전부)에서 모든 사진/영상이 순서대로 나오는지 확인 필요.
- **EPIC-147-후속 2차(구현 완료, 2026-08-25)**: 사용자가 스크린샷+화면 녹화 영상으로 "Timeline NG(Svelte) 스타일 말고 클래식 TimelineJS3 스타일 + 하단 스크롤 줌"을 재지시 — 아래 EPIC-147의 Svelte 기반 `@knight-lab/timeline-ng`를 완전히 걷어내고 순수 JS 라이브러리 `@knight-lab/timelinejs`(TimelineJS3)로 교체했다(`svelte`/`timeline-ng`/`patch-package` 전부 제거). 번들링 문제 2건(raw source의 `.less` import로 Turbopack 빌드 실패, 미리 빌드된 dist를 `import`해도 번들러 스코프 때문에 `window.TL`이 안 채워짐)을 실측으로 발견해 `public/vendor/timelinejs/`에 자체 호스팅 + `<script src>`/`<link>` 직접 주입으로 해결. 추가로 (1) TimeNav 위 마우스 휠로 `zoomIn()`/`zoomOut()` 호출, (2) `posts.timeline_year`/`timeline_end_year`/`timeline_display_date`(신규 컬럼) — 게시글 작성일과 독립적인 타임라인 연대를 글쓰기 폼에서 직접 지정, (3) 타임라인 본문을 100자 발췌(임베드 블록 제거)로 축소, (4) `boards.widget_settings.timelineNgStageHeightPx`로 슬라이드 영역 높이 설정 가능, (5) "위젯처럼 추가/제거"는 기존 Page Builder "Board" 위젯이 이미 `TimelineNgRenderer`로 위임되는 구조라 별도 구현 없이 이미 가능함을 확인. 상세는 CHANGELOG.md 동명 항목 참고. `tsc`/`lint` 0 errors, 77 warnings(기존 기준선 — `public/vendor` ESLint 오탐 수백 건은 `eslint.config.mjs`에 `public/**` 무시 추가로 해결, 이 프로젝트는 그 항목이 next 기본 무시 목록을 완전히 대체하고 있어 빠져있었다). 로컬 dev 서버 + Claude Browser 툴로 바로크 게시판 실측: 클래식 스타일 스크린샷 대조, `WheelEvent`로 줌 양방향 동작, 발췌 100자+임베드 텍스트 미노출, "자세히 보기" 클릭 시 새로고침 없는 이동, 글쓰기 화면의 "타임라인 연대" 섹션 노출까지 전부 확인. **다음에 확인 필요**: (1) 기존 도슨트 글들은 `timeline_year`가 비어있어 지금은 작성일 기준으로 배치됨 — 실제 역사적 연대로 보이려면 관리자가 글마다 수동으로 채워야 함(자동화 불가). (2) 슬라이드 영역 높이 설정을 실제 게시판 관리 화면에서 조정해보고 육안 확인. (3) `public/vendor/timelinejs`는 버전 자동 동기화가 없어 향후 `@knight-lab/timelinejs`를 업그레이드하면 이 폴더도 수동 재복사 필요.
- **EPIC-147(구현 완료, 2026-08-25) — 위 2차로 UI 라이브러리가 교체됨, 아래는 최초 구현 기록**: Knight Lab "Timeline NG"(Svelte 5, `@knight-lab/timeline-ng`)의 JSON 구조/프론트엔드를 그대로 써서 사일로 게시글을 인터랙티브 타임라인으로 렌더링 — 게시판 목록(List)을 대체하는 새 `"timeline_ng"` BoardLayoutType. 가장 큰 난관은 업스트림 패키지 자체의 버그 2건(SSR 시 DOMPurify 크래시 → `next/dynamic({ssr:false})`로 우회, `effect_orphan` 런타임 에러 → `packages/player/vite.config.ts`의 `external` 목록이 Svelte 4 시절 `'svelte/internal'` 경로만 있고 Svelte 5의 실제 경로 `'svelte/internal/client'`가 빠져있던 게 원인, GitHub에서 소스 clone해 직접 고쳐 재빌드 후 `patch-package`로 고정) — 상세 진단 과정은 CHANGELOG.md 동명 항목 참고. `/api/timeline/events?board=`(신규 API, DB posts → TLTimeline JSON 실시간 변환) + `<SiloTimeline />`(신규 Svelte→React 브릿지, `src/components/timeline/`) + `TimelineNgRenderer`(BoardRendererRegistry 신규 등록) + 컨테이너 레벨 클릭 가로채기로 "자세히 보기" 링크/미디어 클릭 시 새로고침 없이 상세 페이지 이동. `boards.render_type` CHECK 제약 확장(사용자 승인 받아 실행) 후 사일로 타임라인/르네상스/바로크 3개 게시판을 `timeline_ng`로 전환. `tsc`/`lint` 0 errors(신규 경고 없음). 로컬 dev 서버 + Claude Browser 툴로 바로크 게시판(실제 프로덕션 글 6건, 비발디/베르메르 등)을 로드해 SlidePlayer 마운트+TimeNav 실제 날짜+본문 HTML 렌더링+"자세히 보기" 클릭 시 `window` 전역 마커 유지된 채(=새로고침 없음) 상세 페이지로 정상 이동하는 것까지 전부 실측 확인. **EPIC-147-후속(2026-08-25, 같은 날 완료)**: 사용자 지시로 "온라인 도슨트" leaf 게시판 전부를 일괄 전환 — 처음엔 `group_key='docent'`로만 찾아 르네상스/바로크 2개뿐인 줄 알았으나, 실제로는 나머지 11개(그리스/로코코/신고전주의/리젠시/빅토리아/인상파/아르누보/아르데코/비트세대/카운터컬처/대중문화)가 `group_key` 없이 존재 — 전체 boards 테이블을 site_navigations 트리와 대조해 찾아 동일하게 `render_type='timeline_ng'`로 전환(코드 변경 없음, DB만). 상세는 CHANGELOG.md "EPIC-147-후속" 항목 참고. 이제 온라인 도슨트 leaf 13개 + 사일로 타임라인 전부 Timeline NG. **다음에 확인 필요**: (1) 업스트림(`NUKnightLab/timeline-ng`)에 이 `svelte/internal` 외부화 버그를 이슈로 등록할지 검토(`npx patch-package @knight-lab/timeline-ng --create-issue`). (2) 관리자 계정으로 실제 화면을 보고 슬라이드/미디어/링크가 기대한 대로 보이는지, 특히 여러 미디어 타입(영상/임베드)이 섞인 게시판에서도 문제없는지 육안 확인. (3) "대중 문화"(category="digital") 게시판은 slug 매칭이 다른 것들과 달라(원래 "digital-ai" 리프에 대응 추정) 정말 맞는 게시판인지 사용자 확인 권장.
- **EPIC-146(구현 완료, 2026-08-25)**: "홈페이지 설정 관리"에 PC/태블릿/모바일 3단 토글 복원(EPIC-136 때 실전용성 낮은 "미리보기 전용" 태블릿이 정리되며 함께 빠졌던 것) + 사용자 요청대로 태블릿을 pc/mobile과 완전히 동등한 독립 설정 슬롯으로 승격. 설정 타입 6개(`mainLogoSettings`/`sidebarIconsSettings`/`topTabStyleSettings`/`accountMenuStyleSettings`/`headerLayoutPositions`/`topSidebarSettings`/`heroSlideshow`)에 `tablet` 슬롯 추가(기존 저장값은 pc 값을 물려받아 승격 즉시 화면이 바뀌지 않음), `Navbar.tsx`에 새 `useDeviceTier()`(768px/1024px 경계)로 실제 방문자 폭에서 3단 판정, "홈페이지 설정 관리"에 태블릿 토글+820px 캔버스 프레임 추가, 홈페이지 히어로 슬라이드쇼도 2-way(`hidden md:block`/`md:hidden`)에서 3-way로 확장. 탭 복제 시 스타일도 복사하는 `duplicateNavItem()`이 `["pc","mobile"]`로 하드코딩돼 있던 것도 발견해 함께 수정(안 고쳤으면 탭 복제마다 태블릿 스타일이 조용히 누락될 뻔). 상세는 CHANGELOG.md 동명 항목 참고. `tsc`/`lint` 0 errors(신규 경고 없음). 로컬 dev 서버 + Claude Browser 툴로 태블릿 토글→캔버스 820px 리사이즈 확인, 태블릿 탭에서 로고 텍스트를 바꿔도 PC/모바일 탭에 전혀 영향 없는 것(3단 완전 독립) 실측 확인(프로덕션 DB에는 저장 안 하고 되돌림), 실제 820px 폭 공개 홈페이지 렌더링에 새 콘솔 에러 없음 확인. **다음에 확인 필요**: (1) 관리자 계정으로 로그인해 실제로 태블릿 탭에서 로고/헤더 위치/슬라이드쇼 등을 커스터마이징해보고 저장 후 실제 태블릿 기기(또는 768~1023px 브라우저 폭)에서 그대로 반영되는지 확인, (2) 이 사이트의 홈페이지는 현재 `builder_type='craft'`라 히어로 슬라이드쇼 3-way 분기 자체는 이 사이트에서 당장 눈에 보이지 않음(`native` 모드 페이지에서 실제로 쓰이는 코드 경로) — Craft 모드가 아닌 다른 페이지나 향후 `native`로 전환 시 확인 필요, (3) 태블릿 프레임 기준폭 820px가 실제 사용자가 쓰는 태블릿(아이패드 등)과 충분히 가까운지 사용자 확인.
- **HOTFIX-141.22(구현 완료, 2026-08-25, 작업 브랜치 `feature/EPIC-146`)**: "홈페이지 설정 관리" PC preview와 Craft 에디터 preview가 실제 홈페이지 출력과 다르다는 반복 신고(HOTFIX-141.1~.21로도 재발)의 진짜 근본 원인 2건 특정+수정. (1) PC 헤더의 '스튜디오'/'관리자'가 화면 밖으로 사라짐 — 실제 프로덕션 DB에 `refWidthPx` 없이 저장된 레거시 드래그 오프셋(dxPx=-747/-735)을 HOTFIX-141.15가 "배율 1로 그대로 적용"했던 게 원인, `HeaderSlot.tsx`/`LeftSidebar.tsx`/`RightSidebar.tsx`를 "기준 폭 모르면 무시(dx=0)"로 수정해 DB 값 손대지 않고 자가 치유. (2) Craft 에디터(홈페이지 포함) PC 모드 — Toolbox/SettingsSidebar가 캔버스와 flex로 폭을 나눠 써 캔버스가 실제 사이트보다 528px 좁게 렌더링되던 게 원인(컨테이너 쿼리 반응형 기준 폭이 어긋남), HOTFIX-141.9와 동일한 "오버레이로 전환" 패턴으로 수정. 모바일 preview는 실측 결과 이미 정확히 일치해 별도 버그를 재현하지 못함. 상세는 CHANGELOG.md 동명 항목 참고. `tsc`/`lint` 0 errors, 로컬 dev 서버 + Claude Browser 툴로 실제 프로덕션 데이터 기준 수정 전/후 좌표 직접 비교 확인(PC 헤더 오프스크린 재현→해결, Craft 캔버스 폭 316px→829px). **다음에 확인 필요**: (1) 관리자 계정으로 실제 화면을 보고 '스튜디오'/'관리자'가 원하는 위치에 있는지(기본 flex 위치로 리셋됐으므로, 마음에 드는 자리로 다시 드래그하면 이제 `refWidthPx`가 저장돼 다시는 화면 밖으로 안 나감), (2) 홈페이지 Craft 에디터에서 실제로 블록 레이아웃(Editorial Grid 등)이 라이브와 동일하게 보이는지 육안 확인, (3) 사용자가 모바일에서 여전히 어긋남을 본다면 정확한 기기/폭을 알려달라고 요청(390px 기준으로는 완전히 일치함을 확인함).
- **HOTFIX-144.8(구현 완료, 2026-08-25)**: "거대한 하늘색 덩어리"가 HOTFIX-144.7 이후에도 여전히 재발 — 진짜 근본 원인은 별개 버그였다: `ModelSelectionOutline`이 복제본 전체를 원점 기준 균일 확대하는 방식이라, 원점에서 멀리 뻗은 부분(키 큰 식물의 가지 끝 등)이 거리에 비례해 거대하게 부풀어 올랐다(실측: "오브젝트 3" 식물 스캔으로 재현). 정점을 법선 방향으로 고정된 양만큼만 미는 정점 셰이더로 교체해 근본 해결. 찌꺼기 메쉬 판정에 "삼각형 30개 미만 & 전체의 2% 미만"인 빈 자리표시자 규칙도 추가하고, 스케일 정규화(바운딩 박스 계산)에도 동일 판정을 적용해 "스케일 최대로 올려도 작다" 불일치를 막음. 상세는 CHANGELOG.md 동명 항목 참고. `tsc`/`lint` 0 errors, 로컬 dev 서버에서 재현됐던 식물 오브젝트로 수정 전/후 직접 비교 확인. **다음에 확인 필요**: 나머지 라이브 오브젝트들(venus de milo/vintage camera/patrick/rose/우주 오브젝트 8·9)도 이상 없는지, 관리자 계정으로 "먼 우주 배경" glb 실제 업로드 성공 확인(HOTFIX-144.7에서 미확인 채로 남음).
- **HOTFIX-144.7(구현 완료, 2026-08-25)**: 오브젝트 선택 시 "거대한 하늘색 덩어리"가 뜨던 문제 — 다운로드 .glb에 흔한 장식용 받침대 메쉬가 아웃라인 셸에 그대로 드러난 게 원인, 얇고 넓은 메쉬를 아웃라인에서 제외하는 휴리스틱 추가(GLB 직접 파싱으로 실측 검증). Universe Settings의 우주 오브젝트/별똥별 업로드(HOTFIX-144.5에서 빠뜨림)도 R2로 이관. 상세는 CHANGELOG.md 동명 항목 참고. `tsc`/`lint` 0 errors, 로컬 dev 서버에서 실제 laputa garden robot 선택해 수정 전/후 육안 비교 확인. **다음에 확인 필요**: 다른 .glb 오브젝트들도 동일 휴리스틱으로 문제없이 아웃라인이 그려지는지(받침대가 없는 정상 모델을 잘못 숨기지 않는지), 관리자 계정으로 "먼 우주 배경" glb 실제 업로드 성공 확인.
- **HOTFIX-144.6(구현 완료, 2026-08-25)**: Silo Planet 오브젝트를 드래그로 회전할 수 있게 — `UniverseObject.yaw` 필드 신설, ObjectInspectorPanel에 "이동/회전" 토글, TransformControls를 로컬 Y축(표면 법선/우주 identity) 하나로 제한한 회전 모드 추가. 우주 공간 이미지 빌보드(sprite kind)는 billboarding 때문에 회전이 화면상 무의미해 토글을 숨김(모델 kind만 지원). 상세는 CHANGELOG.md 동명 항목 참고. `tsc`/`lint` 0 errors. 로컬 dev 서버 + Claude Browser 툴로 실제 오브젝트("laputa garden robot") 선택 → 회전 토글 → 드래그 회전 → 재선택 시 orientation 유지 확인(사용자가 직접 "업로드 확인됐어" 이후 요청, 로그인 세션 없이도 클릭/드래그 시각 검증은 가능해 실제로 수행함). **다음에 확인 필요**: 관리자 계정으로 로그인해 실제로 저장(DB persist) 후 새로고침해도 회전이 유지되는지, 여러 오브젝트를 연속으로 회전했을 때 서로 간섭이 없는지 확인.
- **HOTFIX-144.5(구현 완료, 2026-08-24)**: Silo Planet(행성 텍스처/위성 디자인/캐릭터 .glb/장식 오브젝트/오브젝트 썸네일) 업로드가 Supabase Storage(무료 플랜 50MB 고정 상한)를 쓰고 있던 걸 이미 있던 R2 direct-upload 파이프라인(EPIC-082/083, 100MB 상한 기설정)으로 이관 — 별도 상한 조정 없이 100MB로 올라감. presigned route에 `.glb`/`.gltf` 확장자 허용 추가. 상세는 CHANGELOG.md 동명 항목 참고. `tsc`/`lint` 0 errors. **다음에 확인 필요**: 실제 관리자 계정으로 로그인해 50~100MB 사이 실제 .glb 파일을 Silo Planet 설정창에서 업로드해 성공하는지 육안 확인(이번 세션은 로그인 세션 없이는 재현 불가해 코드 리뷰+타입체크+무관한 에러 없음 확인까지만 함).
- **HOTFIX-144.4(구현 완료, 2026-08-24)**: 메인 로고 hover 이미지 업로드 추가 — LeftSidebar/RightSidebar 여닫이 아이콘과 동일한 크로스페이드 패턴(EPIC-078), 비어있으면 기존과 동일(하위 호환). 상세는 CHANGELOG.md 동명 항목 참고. `tsc`/`lint` 0 errors, 로컬 dev 서버에서 `?__adminPreview=1`로 값 주입해 DOM 구조(크로스페이드 마크업/opacity 초기값) 확인 완료. **다음에 확인 필요**: 관리자 로그인 세션에서 직접 실제 마우스 hover로 이미지가 바뀌는지 육안 확인(구조는 검증했지만 실제 hover 인터랙션 자체는 못 눌러봄).
- **HOTFIX-144.3(구현 완료, 2026-08-24)**: 상단 탭 1차/2차 드롭다운 상하(Y) 위치 설정 추가(기존 좌우(X)와 동일 패턴) + Controls 패널 hover 모션 select 5곳 전부에 실제 CSS 미리보기 스와치 추가(마우스를 올려 바로 확인 가능, Navbar.tsx와 동일한 `tabHoverMotionCss()` 재사용) + "호버 모션 테마" 일괄 적용 버튼에도 동일 미리보기. 상세는 CHANGELOG.md 동명 항목 참고. `tsc`/`lint` 0 errors. **다음에 확인 필요**: 관리자 로그인 세션에서 직접 확인 못 함 — (1) 상단 탭 Controls에서 1차/2차 상하 위치를 실제로 조정했을 때 드롭다운/플라이아웃이 기대한 방향(양수=아래, 음수=위)으로 움직이는지, (2) hover 모션 select 옆 미리보기 스와치에 마우스를 올렸을 때 실제 상단 탭과 동일하게 보이는지 육안 확인 필요.
- **HOTFIX-144.2(구현 완료, 2026-08-24)**: "About Silo"/"온라인 도슨트"에 왼쪽/오른쪽 사이드바 노출 체크를 해도 화면에 반영 안 되던 문제 — 이미 그 슬롯을 쓰던 "사일로 상점"/"살롱데상"과 경합해 `Array.find()`가 하나만 골라 나머지를 무시하던 게 원인, `mergeSidebarTabs()`로 같은 슬롯의 탭들을 groups 기준으로 합치도록 수정. 상세는 CHANGELOG.md 동명 항목 참고. `tsc`/`lint` 0 errors, 로컬 dev 서버에서 왼쪽(12개)/오른쪽(11개) 사이드바 그룹이 기존+신규 전부 뜨는 것 확인. **다음에 확인 필요**: 없음(브라우저로 직접 재현+검증 완료).
- **HOTFIX-144.1(구현 완료, 2026-08-24)**: 게시판 설정 폼의 "슬러그"(실제로는 `boards.category`, nullable) 입력란에 `required`가 잘못 붙어있어, `category`가 비어있는 기존 게시판(11개, 사일로 타임라인 포함) 설정을 저장할 때마다 브라우저 필수 입력 경고가 매번 떠서 막던 문제 수정 — `required` 제거. 상세는 CHANGELOG.md 동명 항목 참고. `tsc`/`lint` 0 errors. **다음에 확인 필요**: 관리자 로그인 세션에서 직접 확인 못 했음(자동화 불가 영역) — 사용자가 `category`가 비어있던 게시판(예: 사일로 타임라인) 설정 화면을 열어 다른 값만 바꾸고 저장했을 때 더 이상 경고 없이 저장되는지 확인 필요.
- **EPIC-144(구현 완료, 2026-08-24)**: Silo Planet 3D 우주 — 모션 10종 추가(+ spin이 이미지 빌보드에서 안 보이던 버그 수정), 별똥별을 orbit(곡선 궤적)+반짝이는 tail+커스텀 오브젝트로 강화, 우주 공간 오브젝트에 "먼 우주 배경" 배치 모드(은하수/블랙홀/네뷸러용, 크기 최대값도 확대) 추가, 줌 감도/행성 크기 설정 노출, 설정창 3개 전부 바깥 클릭 시 닫히게(단 3D 캔버스 클릭은 예외). 상세는 CHANGELOG.md 동명 항목 참고. `tsc`/`lint` 0 errors, 로컬 dev 서버에서 실제 저장된 설정으로 렌더링/모션 14종 순회/바깥 클릭 닫힘·캔버스 클릭 예외 모두 프로그래밍적으로 검증. **다음에 확인 필요**: 실제 관리자 화면에서 (1) 새 모션 10종이 각각 기대한 대로 보이는지 육안 확인(특히 스프라이트에 회전 계열 모션을 걸었을 때), (2) 별똥별 곡선 궤적+꼬리가 자연스러운지, 커스텀 머리(.glb/이미지) 업로드가 실제로 잘 보이는지, (3) "먼 우주 배경"으로 옮긴 오브젝트가 기대한 거리감으로 보이는지(스케일을 충분히 키워야 함), (4) 행성 크기를 크게/작게 조절했을 때 표면 오브젝트/위성/캐릭터가 파묻히거나 붕 뜨지 않는지, (5) `AboutMeSatellites`(User Page 하단 About Me 고리)는 행성 크기와 별개로 그대로인 게 맞는지.
- **HOTFIX-143.4(구현 완료, 2026-08-24) — 사용자 액션 1개 남음**: HOTFIX-143.3 백필 이후에도 About Silo 4개 + Silo Daily 17개 등 전체 ~76개 썸네일이 여전히 깨져있던 원인 특정 — 캐러셀 첫 항목이 영상인 경우 `media_urls[0]`(.mp4)을 대표 이미지로 잘못 저장하던 버그, `media_item_types`로 실제 IMAGE만 골라 쓰도록 수정 + 백필 라우트의 "깨짐" 판정에 `.mp4` 패턴 추가. 상세는 CHANGELOG.md 동명 항목 참고. `tsc`/`lint` 0 errors. **사용자가 직접 해야 하는 것**: `/admin/instagram`에서 "깨진 썸네일 일괄 복구" 버튼을 다시 한번 클릭 — 이번엔 `.mp4`로 저장된 것들도 재대상에 포함되므로 About Silo/Silo Daily 포함 전체가 잡힐 것으로 예상. **다음에 확인 필요**: 클릭 후 About Silo(4개)와 Silo Daily 페이지에서 실제로 이미지가 뜨는지, 캐러셀 전체가 영상뿐이라 대표 이미지가 아예 없는(null) 케이스도 있을 수 있음(깨진 것보다는 낫지만 사용자가 원하면 별도로 수동 지정 필요).
- **HOTFIX-143.3(구현 완료, 2026-08-24)**: Instagram이 og:image 메타 태그를 아예 안 주게 바뀌어 스크래핑 폴백이 죽어있던 것 확인, `embedThumbnail.ts`가 `instagram_feeds` 캐시를 먼저 보도록 수정 + `_silo_store` 계정 히스토리 전체(381개 신규, 총 389개) 동기화 완료. 상세는 CHANGELOG.md 동명 항목 참고. `tsc`/`lint` 0 errors, 브라우저로 실제 R2 이미지 렌더링 확인. **사용자가 직접 해야 하는 것**: `/admin/instagram` 페이지에서 "깨진 썸네일 일괄 복구" 버튼 클릭 — 나머지 247개 게시글의 `featured_image_url`을 고치는 이 작업은 DB 트리거(`posts_protect_content_columns()`)가 실제 로그인 세션만 허용해서 스크립트로 대신할 수 없었음(의도된 보호 장치). 이 클릭 한 번이면 게시판 목록 썸네일도 전부 정상화될 것으로 예상.
- **HOTFIX-143.2(구현 완료, 2026-08-24)**: HOTFIX-143.1 배포 후에도 사용자가 "로딩에서 멈춘다"고 재신고 — 로컬 dev 서버 + Claude Browser 툴 라이브 디버깅으로 진짜 원인 특정: `nativeInstagramEmbed.ts`의 `createRoot()`가 앱 메인 트리와 분리된 별도 React 트리라 `useAuth()`가 실제 `<AuthProvider>`를 못 만나고 Context 기본값(`loading: true` 고정)만 봤다 — HOTFIX-143.1의 "로그인 세션 확인 전 요청" 진단은 틀렸음(정정). `nativeInstagramEmbed.ts`가 `<AuthProvider>`로 한 번 더 감싸도록 수정. 상세는 CHANGELOG.md 동명 항목 참고. 로컬에서 직접 재현+수정 확인(로딩 멈춤 해소, "Instagram에서 보기" 폴백으로 정상 전환). `tsc`/`lint` 0 errors. **다음에 확인 필요**:
  1. `dev.silostore.net`이 이 커밋까지 반영된 빌드를 서빙할 때까지 기다린 뒤(이전에도 배포 반영이 예상보다 오래 걸림 — Vercel 대시보드에서 `develop` 배포 상태 확인 권장), 실제로 로딩이 멈추지 않고 최종 상태(네이티브 렌더링 또는 폴백 링크)로 넘어가는지 확인.
  2. 로그인 상태에서 `/api/instagram-post`가 이제 401 없이 정상 호출되는지(스크래핑 자체 성공 여부는 별개 — EPIC-133 스크래핑이 이 특정 게시물에 대해 성공하는지는 Instagram 차단 여부에 달림, 이번 수정 범위 밖).
  3. 이 Context-격리 패턴(`createRoot()`로 앱 트리 밖에 마운트)은 향후 이 안에서 다른 Provider 기반 훅(useAuth 외에)을 새로 쓸 때도 똑같이 재발할 수 있는 구조적 함정 — 비슷한 코드를 추가할 때 이 케이스를 참고할 것.
- **HOTFIX-143.1(구현 완료, 2026-08-24)**: dev.silostore.net에서 직접 브라우저로 재현 디버깅 — (1) 배포가 이전 디버그 커밋(`0a2387d`)에 멈춰있었음(코드 문제 아님, Vercel 배포 지연으로 판단, 재확인 필요), (2) `InstagramMediaSlider.tsx`가 세션 확인 끝나기 전에 요청을 보내 로그인 사용자도 401 나던 레이스 컨디션 수정. 상세는 CHANGELOG.md 동명 항목 참고. `tsc`/`lint` 0 errors. **다음에 확인 필요**:
  1. `dev.silostore.net`이 이 커밋까지 반영된 새 빌드를 서빙하는지(배포 후 실제 콘솔에 `[nativeIgDebug]` 로그가 더 이상 안 뜨는지) 재확인.
  2. 인증 수정 후에도 `/api/instagram-post`가 실제로 200을 내려주는지, 아니면 여전히 스크래핑 자체가 404로 실패하는지 — 후자라면 이건 EPIC-133의 기존 한계이고 이번 수정 범위 밖(근본 해결은 `/admin/instagram` R2 동기화 커버리지 확대뿐, 단 사일로 스토어 자사 계정 게시물에만 적용 가능).
  3. `instagram_feeds`에 없는(=폴백 경로를 타는) 다른 계정 게시물 임베드는 스크래핑이 막히면 "Instagram에서 게시물 보기 ↗" 외부 링크로만 보일 수 있음 — 이게 사용자가 기대하는 최소 허용선인지 확인 필요(완전한 네이티브 렌더링을 모든 임베드에 보장할 수는 없다는 구조적 한계를 사용자에게 명확히 전달할 것).
- **EPIC-143-후속-2(구현 완료, 2026-08-24)**: "about silo 썸네일 안 나옴 + 게시판 전체(silo daily 등) 썸네일 다 깨짐" 신고 대응. (1) Instagram 임베드 치환이 잠깐 됐다가 원래 blockquote로 되돌아가는 재도색 버그 — 원인은 특정 못했지만(RSC 재도색 추정) `nativeInstagramEmbed.ts`를 1회성 스캔에서 MutationObserver 상시 감시로 바꿔 몇 번을 다시 그려도 즉시 재처리하게 함(`UniversalBlockRenderer.tsx`가 정리 함수 관리). (2) 사이트 전체 285개 글 중 248개의 `featured_image_url`이 서명 만료/핫링크 보호가 걸린 `scontent-*.cdninstagram.com` 직링크였던 게 진짜 썸네일 근본 원인 — `src/lib/r2Server.ts`(신규, 공용 다운로드+R2 업로드 헬퍼)를 뽑아 `embedThumbnail.ts`의 Instagram og:image 처리가 이제 R2에 재호스팅한 사본 URL을 저장(앞으로 저장되는 글부터 적용, `/api/instagram/fetch/route.ts`도 이 공용 헬퍼로 리팩터). (3) 이미 깨져있는 기존 248개 글은 저장 로직만 고쳐선 소급 안 되므로, `/api/admin/instagram/backfill-thumbnails`(신규, 관리자 전용, 커서 페이지네이션)를 만들어 `/admin/instagram` 페이지에 "깨진 썸네일 일괄 복구" 버튼으로 노출 — `body_json`에서 첫 임베드를 다시 찾아(`resolveFallbackEmbedThumbnail`) R2로 재계산. `tsc`/`lint` 0 errors. **다음에 확인 필요**:
  1. 브라우저에서 "about silo"/"silo daily" 등 여러 게시판을 새로고침해 임베드 치환이 더 이상 원상복구되지 않고 유지되는지 직접 확인(재도색 트리거 자체는 아직 root-cause 특정 못 함 — 워크어라운드만 검증된 상태).
  2. `/admin/instagram`에서 "깨진 썸네일 일괄 복구" 버튼을 실제로 눌러 248개 게시글이 처리되는지, 복구 후 각 게시판(특히 사용자가 직접 지목한 Silo Daily) 목록에서 썸네일이 실제로 보이는지 확인.
  3. `resolveFallbackEmbedThumbnail`은 본문의 "첫 번째" 임베드만 본다 — 한 게시글에 임베드가 여러 개면 원래 지정됐던 것과 다른 임베드의 썸네일로 바뀔 수 있음(깨진 것보다는 낫다는 절충, 완벽한 재현은 아님). 복구 후 육안 확인 시 참고.
- **EPIC-143-후속(구현 완료, 2026-08-23)**: 게시글 본문의 Instagram 임베드(iframe/blockquote)를 실시간으로 사일로 네이티브 UI로 치환 — 기존/향후 게시글 전부 소급 적용됨. 상세는 CHANGELOG.md 동명 항목 참고. `tsc`/`lint` 0 errors, permalink 매칭 실데이터로 검증. **다음에 확인 필요**:
  1. 실제 게시판 글(Instagram 임베드가 포함된 기존 글)을 브라우저로 열어 네이티브 UI로 정상 치환되는지 직접 확인(이 세션은 dev 서버 브라우저에서 Supabase REST가 막혀 직접 클릭 재현을 못 함).
  2. 사일로 스토어 소유가 아닌 타 계정 Instagram 링크를 본문에 넣었을 때 `InstagramMediaSlider` 폴백이 실제로 동작하는지(EPIC-133 스크래핑이 이 배포 환경에서 성공하는지는 그 EPIC 자체가 "미확인"으로 남아있었음 — 별도 확인 필요).
  3. `BlockEditor.tsx`(글쓰기 화면 미리보기)는 의도적으로 그대로 뒀음(여전히 공식 embed.js) — 편집 중 미리보기도 네이티브로 통일할지는 사용자 결정 대기.
  4. `instagram_feeds`에 아직 동기화 안 된 사일로 스토어 자사 게시물(전체 412개 중 8개만 동기화됨)은 당분간 폴백 경로를 타므로, `/admin/instagram`에서 계속 동기화를 진행할수록 네이티브 적중률이 올라감.
- **EPIC-143(파이프라인 구현+실데이터 동기화 검증 완료, 2026-08-23)**: Instagram Graph API로 `_silo_store` 계정 게시물을 R2에 재호스팅해 네이티브로 렌더링하는 기반 파이프라인(`instagram_feeds`/`/api/instagram/fetch`/`/admin/instagram`). 상세는 CHANGELOG.md 동명 항목 참고. `tsc`/`lint` 0 errors, 실제 8개 게시물 라이브 동기화 확인(나머지 약 404개는 `/admin/instagram`에서 계속 동기화 가능). **정정**: 이 파이프라인의 실제 소비처는 별도 피드 페이지가 아니라(그건 오해였음) 게시글 본문 내 인스타그램 임베드 치환(아래 "EPIC-143-후속" 항목) — `InstagramNativeFeed`(그리드 컴포넌트)는 구축은 돼 있지만 어디에도 아직 안 쓰임, 필요해지면 그때 배치.
- **HOTFIX-141.21(구현 완료, 2026-08-22)**: "모바일에 상단 사이드바 세션정보 컬럼만 보인다" — column 0의 `hidden md:block`이 시뮬레이션이 아닌 실제 브라우저 창 폭을 봐서 어긋난 게 원인, isMobileViewport prop 전달 + 모바일이면 4컬럼 세로 스택으로 변경. 상세는 CHANGELOG.md 동명 항목 참고. `tsc`/`lint` 0 errors. **다음에 확인 필요**: 새로고침 후 모바일 탭에서 상단 사이드바 열었을 때 4개 컬럼(이미지/세션정보/링크목록/하위목록)이 세로로 전부 보이는지, PC 탭은 기존 가로 4열 그대로인지. column 3(하위 목록)은 hover 기반이라 실제 터치 모바일에서는 별도 개선(탭하면 펼치기 등) 필요할 수 있음 — 후속 검토 대상.
- **HOTFIX-141.20(구현 완료, 2026-08-22)**: "화면 밖이라 드래그로 못 옮긴다" — Controls "위치" 섹션에 가로/세로 좌표 숫자 입력 필드 추가, Elements 패널에서 선택만 하면 캔버스 클릭 없이 좌표로 옮길 수 있음. 상세는 CHANGELOG.md 동명 항목 참고. `tsc`/`lint` 0 errors. **다음에 확인 필요**: 새로고침 후 모바일 탭에서 "상단 사이드바 열기 버튼"을 Elements 패널로 선택 → 새 가로/세로 입력으로 화면 안까지 옮겨서 실제로 보이는지 확인, 그 후 로고 트리오처럼 "고정"까지 걸어두기.
  - **더 근본적인 후속 검토 필요**: 이번엔 "옮길 수단"만 고쳤을 뿐, 애초에 모바일 헤더 한 줄(로고+탭+계정메뉴 5개+글쓰기+아이콘)이 390px에 안 들어가는 근본 레이아웃은 그대로다 — 매번 새 헤더 요소가 추가될 때마다 관리자가 일일이 손으로 위치를 잡아야 하는 구조. 장기적으로는 이 줄에 `flex-wrap`을 주거나, 모바일에서 우선순위 낮은 요소를 기본적으로 숨기는 등 구조적 개선을 고려할 것(이번엔 여러 transform 트릭과 얽혀있어 리스크가 커 보류함).
- **HOTFIX-141.19(구현 완료, 2026-08-22)**: "살롱데상 1차/2차 좌우 위치가 적용 안 된다" — dropdownAlign="right"일 때 marginLeft가 shrink-to-fit 너비 계산에 흡수돼 무효화되던 CSS 버그, transform: translateX로 교체해 해결. 상세는 CHANGELOG.md 동명 항목 참고. `tsc`/`lint` 0 errors. **다음에 확인 필요**: 새로고침 후 살롱데상(모바일, 오른쪽 기준) 1차 좌우 위치 50px이 실제로 왼쪽으로 밀리는지(오른쪽 기준이므로 양수=오른쪽 방향이지만 화면상으로는 기준선에서 안쪽/바깥쪽 어느 쪽인지 실제로 눈으로 확인 필요) — "왼쪽 기준" 탭(온라인 도슨트 등)도 회귀 없는지 함께 확인.
- **HOTFIX-141.18(구현 완료, 2026-08-22)**: "Elements/Controls 패널이 안 보인다"는 신고 — 사이드바/상단 사이드바 패널이 열려있으면 z-50이 관리자 패널(z-40)을 덮어버리는 게 진짜 원인이었음. 관리자 패널을 z-[100]으로 올려 해결. 상세는 CHANGELOG.md 동명 항목 참고. `tsc`/`lint` 0 errors. **다음에 확인 필요**: 새로고침 후 사일로상점/살롱데상 사이드바나 상단 사이드바를 캔버스에서 열어둔 채로도 Elements/Controls 패널이 계속 보이고 클릭 가능한지.
- **HOTFIX-141.17(구현 완료, 2026-08-22)**: "로고 좌우 텍스트+로고를 하나로 그룹화해서 고정되게 해달라" — `MainLogoConfig.groupSideTexts`/`groupGapPx` 신설, 켜면 셋이 하나의 HeaderSlot(logo-group)으로 합쳐져 항상 같은 간격으로 함께 움직임. 상세는 CHANGELOG.md 동명 항목 참고. `tsc`/`lint` 0 errors. **다음에 확인 필요**: 새로고침 후 "로고" Controls에서 그룹화 체크박스를 켜고 (1) 캔버스에서 셋이 실제로 하나처럼 움직이는지, (2) Elements 패널에 "로고 그룹(위치)" 버튼이 나타나는지, (3) 그룹 상태에서도 "로고 왼쪽/오른쪽 텍스트" 선택 시 텍스트 내용/서체 편집이 정상 동작하는지(단 캔버스 위 선택 하이라이트는 그룹 안에서는 안 뜨는 게 의도된 트레이드오프).
- **HOTFIX-141.16(구현 완료, 2026-08-22)**: "1차/2차 드롭다운 방향 말고 좌우 위치를 정하게 해달라" — dropdownOffsetXPx/subDropdownOffsetXPx(px, marginLeft로 적용) 신설, Controls에 "1차/2차 좌우 위치(px)" 입력 추가. 상세는 CHANGELOG.md 동명 항목 참고. `tsc`/`lint` 0 errors. **다음에 확인 필요**: 새로고침 후 온라인 도슨트 등에서 이 값을 바꿨을 때 실제로 드롭다운이 좌우로 미세 이동하는지.
- **HOTFIX-141.15(구현 완료, 2026-08-22)**: "창 폭을 줄이면 로고 좌우 텍스트/스튜디오/관리자/상단 사이드바 아이콘이 겹친다"는 신고 — 상세는 CHANGELOG.md 동명 항목 참고. 드래그 오프셋을 폭 비율로 스케일링(refWidthPx)하도록 고쳤고, 1차/2차 드롭다운 방향을 따로 지정할 수 있게 함. `tsc`/`lint` 0 errors. **다음에 확인 필요**: 사용자가 새로고침 후 (1) 창 폭을 좁혔다 넓혔다 하면서 로고 좌우 텍스트가 겹치지 않는지, (2) 스튜디오/관리자/상단 사이드바 아이콘도 폭에 따라 같이 움직이는지, (3) 새 "2차 펼치는 방향" 선택이 실제로 하위 카테고리의 하위 카테고리에 적용되는지. 참고로 이 스케일링은 요소가 처음 드래그된(또는 재드래그된) 순간의 폭을 기준으로 삼으므로, 과거에 저장된 오프셋(refWidthPx 없음)은 배율 1로 취급돼 이번 수정 이전과 동일하게 동작 — 완전히 반영하려면 해당 요소를 한 번 다시 살짝 드래그(또는 고정 해제 후 재고정)해야 함.
- **HOTFIX-141.14(부분 구현, 큰 후속 작업 2건 남음, 2026-08-22)**: 상세는 CHANGELOG.md 동명 항목 참고. 이번에 된 것: 헤더 요소 전체 어디든 클릭-드래그(HeaderSlot.tsx), 마지막 그룹/항목 2차 플라이아웃 위로 열림(Navbar.tsx), 2차 플라이아웃 폭 개별 설정(subDropdownWidthPx). **다음에 반드시 필요**:
  1. **"상단 사이드바"(TopSidebarPanel.tsx) 실제 시각적 메가메뉴에 진짜 드래그 재정렬 구현** — 지금 있는 건 `/admin/navigation/settings` Controls 패널의 별도 텍스트 목록(TopSidebarControls, "⠿" 그립+HTML5 DnD)뿐이고, 캔버스/실제 사이트에 뜨는 메가메뉴 자체는 드래그가 전혀 안 됨(과거 커밋 메시지가 부정확했음, Explore 조사로 확인). 호버로만 열리는 패널이라 편집 모드에서 강제로 열어둔 채 드래그해야 하는 설계가 필요 — HeaderSlot과 비슷한 dx/dy 오프셋 패턴을 재사용하거나, Controls 목록의 순서를 캔버스 클릭으로도 바꿀 수 있게 양방향으로 잇는 방법 검토.
  2. **모든 드롭다운/사이드바 하위 카테고리 플라이아웃을 자유롭게 드래그로 재배치** — 사용자가 "모든 상위 카테고리와 하위 카테고리에도 적용해줘 자유롭게 움직이도록" 명시적으로 요청. 지금은 dropdownAlign(왼쪽/오른쪽 이진 토글)만 있음 — 살롱데상→커뮤니티처럼 오른쪽으로 잘리는 경우는 이 기존 설정으로 당장 해결 가능(Controls에서 "오른쪽 기준"으로 전환)하다고 안내했지만, 진짜 자유 드래그는 아직 없음. #1과 마찬가지로 호버 전용 패널을 편집 모드에서 계속 열어두는 설계가 선행되어야 함.
  3. **모바일에서 화면 밖으로 밀린 요소(로고 오른쪽 텍스트/상단 사이드바 아이콘 등)를 시각적으로 표시/안내하는 UI** — 지금은 Elements 패널에서 선택은 가능하지만, 화면 밖에 있다는 걸 보여주는 표시가 없어 사용자가 위치를 가늠하기 어려움. 화살표/가장자리 배지 같은 걸 고려.
  4. **`header_positions`에 남아있을 수 있는 과거 극단 오프셋 정리** — 세션 도중 사용자가 직접 라이브로 계속 드래그/저장하고 있어 이번 세션에서 DB를 못 고쳤음(고쳐도 몇 초 만에 되돌아감, 마커 값으로 재현 확인) — 사용자가 작업을 마친 뒤 한 번 전체적으로 훑어서 이상한 값 없는지 확인 권장.
  검증: `tsc`/`lint` 0 errors, 실제 클릭 재현은 이 세션 환경 제약(Supabase REST 403)으로 못 함.
- **HOTFIX-141.13(구현+DB 수정 완료, 사용자가 열린 탭 새로고침 후 재확인 필요, 2026-08-22)**: "로고 양옆 텍스트 간격이 일정하지 않다"는 신고 — 상세는 CHANGELOG.md 동명 항목 참고. 원인은 `header_positions`(PC) 드래그 오프셋 불일치(logo +95.8px/왼쪽텍스트 +390.4px/오른쪽텍스트 -186.4px)였고, DB로 되돌려도 사용자가 열어둔 다른 탭의 "홈페이지 설정 관리" 세션이 예전 값을 계속 재저장해 되돌아갔다 — **사용자에게 그 탭을 새로고침(또는 닫기)해달라고 요청함, 안 하면 예전 값으로 다시 되돌아갈 수 있음**. `HeaderSlotOffset.locked` 필드 신설 + 캔버스/Controls 패널에 잠금 토글 추가(모든 HeaderSlot 기반 요소 + 좌우 사이드바 아이콘 공통) — 로고 3요소를 dx=0+locked=true로 고정해뒀다. `tsc`/`lint` 0 errors. **다음에 확인 필요**: 사용자가 새로고침 후 (1) PC에서 세 요소 간격이 실제로 대칭인지, (2) Controls "위치" 섹션에 새 "이 위치에 고정" 체크박스가 보이는지, (3) 잠근 상태에서 캔버스에 ✥ 핸들이 안 뜨는지, (4) 좌/우 사이드바 아이콘도 잠금이 먹히는지 확인.
- **HOTFIX-141.12(구현 완료, 다음 세션 실사용 검증 필요, 2026-08-22)**: "상단 탭 최상위/하위 카테고리 텍스트 개별 커스터마이징을 모든 탭으로 확장 + 로고 좌/우 텍스트 독립 스타일" — 상세는 CHANGELOG.md 동명 항목 참고. 사일로상점(`sidebar_left`)/살롱데상(`sidebar_right`)이 `/admin/navigation/settings` Elements 목록에서 아예 걸러져 있던 게 진짜 원인이었음(라이브 DB 조회로 확인) — 필터 제거로 다른 탭과 동일하게 편집 가능해짐. 표시 텍스트를 textarea로 바꿔 줄바꿈 지원, 그룹/항목별 `subLabelOverrides` 신설, 로고 좌/우 텍스트에 독립 서체/굵기/크기/색상/커스텀폰트 필드 추가(비면 로고 상속, 기존 데이터 호환). `tsc`/`lint` 0 errors. **다음에 확인 필요**: 이 세션은 브라우저 프리뷰에서 Supabase REST 호출이 막혀(환경 문제, 이번 변경과 무관) 실제 클릭 재현을 못 했음 — 로그인 후 (1) 사일로상점/살롱데상 탭이 Elements 목록에 뜨는지, (2) 표시 텍스트에 줄바꿈 넣었을 때 버튼이 여러 줄로 잘 나뉘는지, (3) 하위 카테고리 오버라이드가 실제 드롭다운에 반영되는지, (4) 로고 좌/우 텍스트 서체/색상 변경이 모바일 캔버스에서 반영되는지 확인 필요.
- **HOTFIX-141.11(구현+실사용 검증 완료, 2026-08-22)**: "live preview가 실제 사이트와 계속 다르던 두 번째 근본 원인 — 로고/사이드바 아이콘/상단 탭/계정 영역 스타일이 캔버스에 실시간 반영된 적이 없었다" — 상세는 CHANGELOG.md 동명 항목 참고. `dropdownAlign`을 캔버스에서 테스트하다가 발견 — Controls의 select 값은 바뀌는데 실제 렌더링은 안 바뀜. 원인: `Navbar.tsx`에 `positionsOverride`/`topSidebarOverride` 딱 두 개만 캔버스-우선 override prop이 있었고, 로고/사이드바 아이콘/상단 탭 스타일/계정 영역 스타일은 EPIC-136 이전 iframe 시절의 죽은 postMessage 코드만 남아있어 저장+새로고침 전까지 캔버스에 전혀 반영 안 됐다 — 이번 세션 "live preview가 다르다" 신고의 상당 부분이 이 설계 공백 때문이었을 가능성. `mainLogoOverride`/`sidebarIconsOverride`/`topTabStyleOverride`/`accountMenuStyleOverride` 4개 신설, `page.tsx`에서 이미 들고 있던 최상위 state를 그대로 전달 — 이제 Controls 편집이 저장 없이 캔버스에 즉시 반영된다. `tsc`/`lint`/`build` 0 errors. **dev.silostore.net에서 실제 로그인 세션으로 검증 완료**: "온라인 도슨트" 탭의 "펼치는 방향"을 Controls에서 "오른쪽 기준"으로 바꾼 즉시(저장/새로고침 없이) 드롭다운 flyout의 className이 `left-0`→`right-0`으로 실시간 변경됨을 확인, 모바일(390px) 캔버스에서 같은 설정으로 오버플로우가 +102px → -48px(정상 범위)로 실제 해소됨을 측정 확인.
- **HOTFIX-141.10(구현 완료, 배포 후 재확인 필요, 2026-08-22)**: "모바일 드롭다운 잘림/로고 텍스트 실종/상단 사이드바 하위 목록 드래그" — 상세는 CHANGELOG.md 동명 항목 참고. (1) 모바일(390px) 캔버스에서 드롭다운 실측 결과 "온라인 도슨트" 102px, "사일로 상점" 6px 오른쪽으로 넘침 확인 → `dropdownAlign`("left"/"right") 신설, 화면 오른쪽 탭은 "오른쪽 기준"으로 바꾸면 왼쪽으로 펼쳐져 해결. (2) 로고 좌우 텍스트가 flex-1 찌그러짐으로 폭 0이 되던 걸 확인 → 로고에서 완전히 분리해 독립 HeaderSlot("logo-left-text"/"logo-right-text")으로 — 다른 헤더 요소처럼 자유 드래그 가능, PC/모바일 각각 독립. (3) 상단 사이드바 column 3(하위 목록)에 그립+드래그 순서변경 추가(column 2는 141.7에서 이미 있었음). `tsc`/`lint`/`build` 0 errors. **다음에 확인 필요**: 배포 후 로그인 세션으로 모바일 드롭다운 정렬 전환/로고 텍스트 드래그/하위 목록 드래그 전부 클릭 재현 필요.
- **HOTFIX-141.9(구현+실사용 검증 완료, 2026-08-22)**: "live preview가 실제 사이트와 계속 다르던 진짜 이유 — 캔버스가 항상 320px 좁게 렌더링" — 상세는 CHANGELOG.md 동명 항목 참고. 사용자가 좌상단에 드래그한 "스튜디오"/"관리자"가 캔버스에서 사라진 걸 재신고("근본적인 문제를 해결해") — 실측 결과 DOM엔 정상 존재(z-index:30, transform 정확)하고 **실제 공개 홈페이지에서는 원하는 위치에 정확히 보였다**. 진짜 원인: `/admin/navigation/settings`가 왼쪽 320px 패널과 캔버스를 flex로 폭을 나눠 써서, 캔버스가 항상 실제 사이트보다 320px 좁았다 — 절대 픽셀 드래그 오프셋이 이 좁은 폭 기준으로 계산되니 왼쪽으로 갈수록 오차가 커져, 캔버스 콘텐츠가 아니라 순수 에디터 UI인 왼쪽 패널 밑에 깔려버렸다. 141.5~141.8이 전부 값만 고치고 재발했던 이유가 바로 이 구조적 문제(기준 폭 자체가 다름)였다는 것 확인. 레이아웃을 flex(폭 분할)에서 relative+absolute 오버레이로 전면 교체 — 캔버스는 항상 전체 폭, 패널은 그 위에 뜨는 오버레이(◀ 숨기기/▶ 보기 토글 추가, 숨기면 실제 사이트와 동일 폭으로 확인 가능). `tsc`/`lint`/`build` 0 errors. **dev.silostore.net에서 실제 로그인 세션으로 최종 확인 완료**: 배포 확인(panel div의 className이 `absolute inset-y-0 left-0 z-40 ...`로 바뀐 것 직접 확인) → "◀ 패널 숨기기" 클릭 → 캔버스 폭이 1167px로 확장됨을 실측 → "스튜디오"/"관리자" 둘 다 캔버스 좌상단(실제 사이트와 동일한 위치)에 `visibility:visible`/`opacity:1`로 정상 렌더링됨을 DOM + 텍스트 추출로 확인. **남은 것**: 아주 넓은 화면(1600px+)에서 `max-w-[1600px]` 잔차 체감 여부(경미, 필요시 별도 확인).
- **HOTFIX-141.8(구현+실사용 검증 완료, 2026-08-22)**: "'관리자'/'스튜디오' 클릭 불가 재발의 진짜 근본 원인 — HeaderSlot의 영구 z-index 격상 버그" — 상세는 CHANGELOG.md 동명 항목 참고. 세 번의 오프셋 리셋(141.5/141.6/141.7)에도 재발한 진짜 이유: `HeaderSlot.tsx`가 `value.raised`(드래그 한 번 하면 영구 true로 저장, 절대 false로 안 돌아감)만으로 z-index:30을 계속 부여했다 — `flex-1`이라 헤더 폭 전체를 차지하는 로고 슬롯이 과거에 한 번 드래그된 이력 때문에 이 상태였고, 그 보이지 않는 전체너비 박스가 "스튜디오"(tier1, z-10 스태킹 컨텍스트라 항상 짐)를 계속 가렸다. "관리자"는 별개로 트리거 아이콘 크기가 100px(예전 테스트 잔재)로 남아있어 `top-sidebar-trigger`에 가려짐. 격상 조건을 `moved || (value.raised && editable)`→`moved || (selected && editable)`로 수정(과거 드래그 이력이 아니라 "지금 선택됨"만 근거로) — 이 버그 클래스 자체를 근본적으로 막는다. DB의 0,0-raised:true 잔여 슬롯 전부 키 삭제(진짜 기본값 복귀), `triggerIconSizePx` 100/80→20. GIF는 `.../gif_rose.webp`(파일명-확장자 불일치)로 배포 전 업로드된 증거를 확인 — 코드는 이미 고쳐졌으니 재업로드하면 해결됨(기존 압축본은 소급 복구 불가). `tsc`/`lint`/`build` 0 errors, dev.silostore.net 1280/1440px 양쪽에서 캔버스 헤더 슬롯 9개 전수 재검사 통과. **다음에 확인 필요**: 실제 마우스 클릭 재확인, 모바일 잔여 오프셋 정리 여부.
- **HOTFIX-141.7(구현 완료, 배포 후 재확인 필요, 2026-08-22)**: "GIF hover 애니메이션 정지 + 상단 탭 hover 텍스트 실종 + 드롭다운 폭/폰트/색상 설정 + 링크 드래그 순서변경" — 상세는 CHANGELOG.md 동명 항목 참고. (1) `compressImage()`가 GIF의 첫 프레임만 캡처해 애니메이션이 사라지던 회귀(HOTFIX-141.4가 원인) 수정 — GIF는 압축 건너뜀. (2) "상단 사이드바" 링크 목록에 실제 드래그 순서변경(⠿ 그립, 네이티브 HTML5 DnD) 추가. (3) "관리자"/"스튜디오" 오프셋이 다시 극단값으로 바뀌어 있었는데 이번엔 `updated_at`이 방금 갱신된 정밀한 소수점 값이라 디버그 잔재가 아니라 실제 드래그 활동으로 확인 — 재초기화했고, 드래그 자체는 정상 동작함을 확인(결과가 마음에 안 들면 "원래 위치로 되돌리기" 사용 안내 필요). (4) 상단 탭 hover 시 글씨가 흰-바탕-흰-글씨로 안 보이던 실제 CSS 버그 발견·수정(강제 흰색 hover 색상 규칙이 배경 투명화 이후에도 안 지워져 있었음), 드롭다운/하위 카테고리 폭·서체·크기·색상을 탭별로 설정 가능하게 신설(모바일 390px 프레임에서 2차 플라이아웃이 잘리던 문제의 근본 대응책). `tsc`/`lint`/`build` 0 errors. **이번 커밋에 포함 안 됨**: (5) 홈페이지 "페이지설정" → Craft(Footer) 에디터의 live preview 불일치·비결정적 렌더링 — 원인 조사를 아직 시작 못 함, 별도 세션에서 제대로 다룰 필요(Craft.js 상태 직렬화 문제일 가능성). **다음에 확인 필요**: 배포 후 실제 로그인 세션으로 GIF 애니메이션/드롭다운 hover 텍스트/링크 드래그/드롭다운 폭 설정 전부 클릭 재현 필요(이번엔 코드 리뷰로만 검증).
- **HOTFIX-141.6(구현+실사용 검증 완료, 2026-08-22)**: "'관리자'/'스튜디오' 여전히 클릭 안 되던 문제 — 뷰포트 너비 전체에서 재검증" — 상세는 CHANGELOG.md 동명 항목 참고. HOTFIX-141.5가 784px 뷰포트에서만 검증해 놓쳤다 — 뷰포트를 1440px로 넓히자 "스튜디오"가 이번엔 로고(`dx:176.8px`, 안 건드렸던 값)에 가려져 있었다(절대 픽셀 오프셋이라 반응형 레이아웃에서 뷰포트별로 다른 요소와 겹칠 수 있음). "적당히 커 보이는" 나머지 탭/계정 영역 오프셋(로고 포함 pc 6개, mobile 7개)까지 전부 `{dx:0,dy:0,raised:true}`로 초기화(sidebar:left/right, top-sidebar-trigger는 명백히 의도적인 값이라 유지). **검증 방법 자체를 바꿈**: 이번엔 1280/1440/1920px 세 너비에서 캔버스 안 9개 헤더 슬롯 전원을 `elementFromPoint`로 전수 확인(신고받은 요소 두 개만 보는 방식은 폐기) — 전부 통과, "스튜디오" 클릭 시 Controls 패널도 정상 표시 재확인. DB만 수정, 코드 변경 없음. **여전히 미확인**: hover flyout 자체는 이 도구의 합성 마우스 한계로 육안 재현은 못 했음(클릭 가능해졌다는 건 확인 — 원래 원인인 "화면 밖/가려짐"은 해소).
- **HOTFIX-141.5(구현+실사용 검증 완료, 2026-08-22)**: "'관리자' 클릭 불가 + '스튜디오' 드롭다운 안 뜨는 버그 수정, HOTFIX-141.4가 만든 회귀" — 상세는 CHANGELOG.md 동명 항목 참고. HOTFIX-141.4에서 `account:admin`/`write-button` 오프셋을 통째로 지우면서 `raised:true`(z-index 30)까지 같이 없어져, 여전히 드래그된 채 raised인 로고에 "관리자"가 가려져 클릭이 안 됐다(`document.elementFromPoint`로 실측). "스튜디오"(내부 키 `space_inquiry`)는 지난번 발견하고 범위 밖이라 남겨둔 `dx:-615.8px`가 화면 밖(x:-154)으로 밀어내 hover할 대상이 없었을 뿐, flyout 자체는 멀쩡했다. 이번엔 오프셋을 지우지 않고 `{dxPx:0,dyPx:0,raised:true}`로 값만 초기화(DB만, 코드 변경 없음) — dev.silostore.net에서 `elementFromPoint`로 둘 다 정상 클릭 가능/화면 안 위치임을 재확인. **다음에 같은 패턴 주의**: header_positions 슬롯을 리셋할 땐 항상 raised:true를 유지한 채 dx/dy만 0으로 — 통째로 지우면 이 회귀가 재발한다.
- **HOTFIX-141.4(구현+실사용 검증 완료, 2026-08-22)**: "글쓰기/드래그 오프셋 버그 수정 + 실제 드래그 스냅 + 로그인/로그아웃을 상단 사이드바로 이동" — 상세는 CHANGELOG.md 동명 항목 참고. (1) "마이 페이지" 탭의 `target_types`가 `[]`라 탭 자체가 안 그려지면서 그 옆에 붙던 "글쓰기" 버튼까지 부수 효과로 사라졌던 걸 발견, `writeButtonEl`을 탭 렌더링에서 완전히 분리(항상 한 번, 독립 위치). (2) "마이페이지" 계정 영역 항목은 이미 Elements→Controls→"복원"으로 접근 가능함을 확인(버그 아님). (3) `header_positions`에 남아있던 극단적 드래그 오프셋(`account:admin` PC -694px/모바일 -275.8px, `write-button` PC -624px/모바일 -207px) 4건을 DB에서 직접 제거 — 모바일에서 안 보이던 것과 live-preview-실사이트 불일치의 상당 부분 원인. (4) 드래그 가이드라인에 실제 스냅(6px 반경) 추가 — 지금까지는 시각 안내선만 있고 실제 위치 보정은 없었음. (5) 로그인/로그아웃 버튼을 계정 영역에서 상단 사이드바 column 1로 이동, 독립 폰트 업로드/크기/색/hover 모션 Controls 신설, 계정 영역 쪽은 hiddenKinds로 기본 숨김(복원 가능). `tsc`/`lint`/`build` 0 errors. **배포 후 dev.silostore.net에서 실제 로그인 세션(브라우저에 남아있던 관리자 세션)으로 직접 확인 완료**: 관리자 캔버스 PC/모바일 양쪽에서 "관리자"/"글쓰기"가 정상 위치에 렌더링됨을 DOM으로 확인, 상단 사이드바 패널을 실제로 열어 "로그아웃" 버튼이 column 1 맨 위에 진짜 세션 데이터(이름/등급/팔로잉·팔로워/최근 활동)와 함께 렌더링됨을 확인, Controls 탭에서 신규 "로그인/로그아웃 버튼" 섹션(서체/커스텀 폰트/글자 크기/색상/hover 모션)이 전부 표시됨을 확인. **다음에 확인 필요**: 드래그 스냅은 실제 마우스로 끌어보는 손맛까지는 미검증(자동화 클릭 도구로 정밀한 드래그 시퀀스 재현이 어려움) — 로직 자체는 리뷰 완료. `header_positions.pc.tab:space_inquiry`(dx:-615.8px)도 비슷하게 의심스러운 값인데 이번 신고 범위 밖이라 그대로 뒀음 — 필요하면 별도 확인.
- **HOTFIX-141.3(구현 완료, 배포 반영 후 재확인 필요, 2026-08-21)**: "Supabase Storage 업로드가 한글/공백 포함 파일명에서 'Invalid key'로 실패하던 버그 수정" — 상세는 CHANGELOG.md 동명 항목 참고. HOTFIX-141.2의 alert 덕분에 사용자가 dev.silostore.net에서 실제 에러(`Invalid key: top_sidebar_trigger/...ChatGPT Image 2026년 8월 22일 오전 03_59_23.webp`)를 확인 → `src/lib/adminImageUpload.ts`의 `uploadImage()`가 저장 경로에 원본 파일명을 그대로 쓰는 게 원인(한글/비-ASCII 공백이 Supabase Storage S3 호환 키에서 거부됨) — 이 함수는 관리자 페이지 전체 업로드가 공유해 같은 문제가 어디서든 재현될 수 있는 구조적 버그였다. `sanitizeFileName()`으로 파일명을 영숫자/`-`/`_`+확장자로 정규화해 한 곳에서 전체 수정. `tsc`/`lint` 0 errors. **다음에 확인 필요**: `develop`→`dev.silostore.net` 재배포 후 원래 실패했던 파일로 실제 업로드 재시도.
- **HOTFIX-141.2(구현 완료, 실사용 클릭 검증 미완료, 2026-08-21)**: "상단 사이드바 여닫이 트리거 아이콘 업로드가 preview에 반영 안 되던 버그 수정" — 상세는 CHANGELOG.md 동명 항목 참고. DB(storage.objects vs site_settings.top_sidebar) 직접 대조 + 사용자 확인(콘솔 무에러, 선택 즉시 미반영)으로 "큰 파일 업로드가 서버는 성공, 클라이언트는 조용히 실패로 처리"가 원인임을 확정 — `handleTriggerIconFile`에 업로드 전 `compressImage(file, 85)` 추가, 3개 핸들러(트리거 아이콘/링크 이미지/이미지 뱅크) 전부 실패 시 alert로 사유 표시하도록 통일. `tsc`/`lint` 0 errors. **다음에 확인 필요(로그인 세션 필수)**: 실제로 다시 업로드해서 이번엔 preview(왼쪽 썸네일+오른쪽 live)에 정상 반영되는지, 만약 여전히 실패한다면 이제 뜨는 alert의 실제 에러 메시지 확인(타임아웃이 아닌 다른 원인일 가능성 포함) — 필요시 후속 HOTFIX.
- **HOTFIX-141.1(구현+실사용 검증 완료, 2026-08-21)**: "상단 사이드바 아이콘 크기/컬럼1→2 이동, '노출위치'에 상단 사이드바 추가, hover 모션 5종 추가, 모바일 프레임 클리핑 버그, 드래그 정렬 안내선, site_navigations target_type NOT NULL 버그, About Silo/Silo Planet 페이지 분리" — 상세는 CHANGELOG.md 동명 항목 참고. DB 버그(사이트 메뉴 새 카테고리 추가 실패) 즉시 수정, 모바일 프리뷰 프레임에 `overflow-hidden` 추가로 좌우/상단 사이드바 클리핑 버그 해결, 상단 사이드바 트리거 아이콘 크기·트리거 hover 모션 5종·'노출위치 top_sidebar' 태그·드래그 정렬 안내선·상단 사이드바 바깥 클릭 닫힘 신설, About Silo(`/about-silo`)를 EPIC-113 이전처럼 갤러리+게시판 page_builder 페이지로 되돌리고 3D 우주는 신규 `/silo-planet`로 분리. `tsc`/`lint`/`build` 0 errors. **이번엔 브라우저에 관리자 세션이 남아있어(사용자가 세션 사이에 직접 로그인해 테스트한 것으로 보임) Browser pane으로 관리자 Controls까지 실제로 열어 확인** — 상단 사이드바 컬럼 너비/순서, 트리거 아이콘 크기, column1→2 이동 버튼, 11종 hover 모션, 계정 영역 복제/삭제/숨김 복원 UI 전부 정상 렌더링 확인. DB에 사용자가 이미 저장한 `hiddenKinds`/`hoverMotion`/업로드 이미지들이 들어있어 EPIC-141의 새 필드가 저장→새로고침 왕복까지 실제로 동작 중임을 확인. **다음에 확인 필요**: 정렬 안내선의 실제 드래그 손맛(로직 검증했지만 사람이 마우스로 직접), Silo Planet 페이지가 실제 배포(dev.silostore.net)에서도 정상 렌더링되는지, `/about-silo`의 gallery 모듈에 연결된 게시판이 없어("연결된 게시판이 없거나 글이 없어요") 관리자가 원하면 게시판 연결 필요.
- **EPIC-141(구현+부분 검증 완료, 2026-08-21)**
- **EPIC-141(구현 완료, 공개 사이트만 부분 검증, 2026-08-21)**: "좌우 사이드바 자유 설정+드래그 이동, 상단 사이드바 아이콘/폰트/컬럼 편집, hover 모션 마스킹 버그 3건 수정, 전역 실행취소, 계정 영역 복제/삭제" — 상세는 CHANGELOG.md 동명 항목 참고. 좌우 사이드바 바깥 클릭 시 닫힘(편집 캔버스), hover 모션이 하드코딩된 `hover:underline`/`hover:bg-green-800`에 가려 "적용 안 되는 것처럼" 보이던 버그(탭·상단 사이드바·계정 영역 3곳) 수정, 상단 사이드바 트리거 아이콘 기본/hover 이미지 + 폰트 파일 업로드 신설, 좌우 사이드바 패널 스타일(배경/텍스트색/서체/hover 모션, 좌우 독립) + 아이콘 드래그 이동 신설, 계정 영역 5항목+글쓰기 버튼 복제/삭제(숨김+사본 모델), 이 페이지 설정 7종 state를 하나로 묶은 전역 실행취소/다시실행, 상단 사이드바 컬럼 너비/순서(정확한 숫자 입력+버튼 — 픽셀 드래그는 실 사이트 전역 컴포넌트라는 위험 때문에 의도적으로 뺌). `tsc`/`lint`/`build` 0 errors, 새 warning 없음. **검증**: Browser pane으로 로그아웃 상태 공개 홈페이지에서 상단 사이드바/좌측 사이드바 열기, 콘솔 무에러, 컬럼 순서·패널 스타일 기본값 유지까지 확인. **세션 중 사고 기록**: 검증 도중 좌표 캐시 어긋남으로 실수로 "Google로 계속하기" 로그인 버튼을 클릭해 실제 Google 로그인 페이지가 열렸음 — 아무 것도 입력하지 않고 즉시 localhost로 이탈, 자격증명 유출 없음. **다음에 확인 필요(관리자 로그인 세션 필수, 이번 세션은 로그인 자격증명이 없어 관리자 Controls UI 자체를 열어보지 못함)**: 좌/우 사이드바 Controls의 패널 스타일 입력, 아이콘 드래그 이동 손맛, 상단 사이드바 트리거 아이콘/폰트 업로드, 컬럼 너비 입력·순서 버튼, 계정 영역/글쓰기 버튼 복제·삭제·복원 버튼, Ctrl+Z가 탭 위치/색상 변경도 실제로 되돌리는지. 사용자가 원하면 컬럼 드래그를 진짜 픽셀 드래그로 바꾸는 후속 작업 가능(현재는 숫자+버튼 방식).
- **HOTFIX-140.4(구현+부분 검증 완료, 2026-08-21)**: "About Silo 3D 우주: 클릭 선택 시 하늘색 구체 제거 + 얇은 표면 하이라이트, 오브젝트별 모션 설정, 별똥별 옵션화" — 상세는 CHANGELOG.md 동명 항목 참고. 맥동하던 하늘색 글로우 구체(`SpaceObjectGlow`)를 삭제하고 종류별 아웃라인(행성: drei `<Outlines>`, GLB 오브젝트: 신규 `ModelSelectionOutline` BackSide-shell 기법, 스프라이트: 확대+재사용 텍스처 실루엣)으로 교체, `UniverseObject.motion`(none/twinkle/bob/spin, 스케일은 카메라 거리 계산과 얽혀 의도적으로 제외) 신설 + `ObjectInspectorPanel` 셀렉트 추가, `ShootingStars`를 하드코딩 4개에서 `UniverseConfig.shootingStars`(활성화/개수/색상/속도)로 설정 가능하게. `tsc`/`lint`/`build` 0 errors. Browser pane으로 `/about-silo` 실제 로드+three.js/drei 청크·텍스처 blob 전부 200 OK+클릭 시 하늘색 구체 미노출까지 스크린샷으로 확인, 콘솔의 일시적 `/login` 연결거부/WebSocket HMR 오류는 연속 파일 저장으로 인한 Turbopack 재컴파일 중 발생한 것으로 판단(직후 재요청 전부 200 OK 정상 복구), 원인 미상 403(P2, 네트워크 로그에 대응 URL 없음)은 등급 정책상 보류. **의도적으로 범위 밖**: 성운(nebula)/초신성(supernova)/별자리 — 코드베이스에 전혀 없는 신규 VFX 시스템이라 이번 범위에서 뺐다(요청 시 별도 EPIC). **다음에 확인 필요(관리자 로그인 세션 필수)**: 모션 셀렉트/별똥별 설정 UI 실제 클릭 왕복 및 저장 후 값 유지.
- **HOTFIX-140.3(구현 완료, DB 수정은 즉시 적용, 코드 부분 미검증, 2026-08-21)**: "여백 배경 이미지 업로드 RLS 실패 수정 + 관리자 이미지 미리보기 hover 확대" — 상세는 CHANGELOG.md 동명 항목 참고. `storage.objects`에 `public-assets` 버킷용 RLS 정책이 아예 없었던 걸 발견(다른 버킷은 다 있었는데 이 버킷만 없음 — 사이트 설정 관리 업로드 대부분이 쓰는 버킷이라 광범위한 잠재 버그였음), admin insert/update/delete + 공개 select 정책 4개를 Management API로 즉시 추가(배포 불필요, 이미 반영됨). 공용 `ImageThumb` 컴포넌트에 hover 시 `position:fixed` 오버레이로 원본을 최대 80vw/80vh로 확대해 보여주는 기능 추가(로고/슬라이드/여백 배경/사이드바 아이콘/상단 사이드바 이미지 뱅크·링크 이미지 7곳 전부 자동 적용). `tsc`/`lint`/`build` 0 errors, RLS 정책 생성은 재조회로 확인 완료. **다음에 확인 필요(관리자 로그인 세션 필수)**: 실제로 여백 배경 이미지 업로드가 성공하는지, hover 확대 UI가 의도대로 보이는지.
- **HOTFIX-140.2(구현 완료, 부분 검증, 2026-08-21)**: "'홈페이지 설정 관리' 캔버스에서 상단/좌우 사이드바를 실제로 열고 설정 가능하게 + 상단 사이드바 콘텐츠·스타일 확장" — 상세는 CHANGELOG.md 동명 항목 참고. 직전 세션(HOTFIX-137.1)의 "편집 모드 클릭 무조건 차단" 수정이 상단 사이드바 트리거 열기까지 막던 회귀 버그를 `HeaderSlot`의 신규 `interactive` prop으로 해결, 좌우 사이드바(`LeftSidebar`/`RightSidebar`)를 EPIC-136이 헤더에 적용한 것과 같은 fixed↔absolute 전환 패턴으로 편집 캔버스에 실제로 렌더링(가짜 "대표 미리보기" 삭제), 상단 사이드바 column 1에 팔로잉·실제 최근 활동 3건·Mind Diary/Studio/Silo Planet 고정 링크 추가, `TopSidebarConfig`에 배경색/텍스트색/서체/hover 모션/이미지 뱅크(무작위 노출) 필드 신설 + 관리자 Controls, 링크 목록에 "복제" 버튼 추가. `tsc`/`lint`/`build` 0 errors. **검증**: 이번 세션은 Browser pane이 정상 동작해(직전 세션의 sandbox 제약이 재현되지 않음) 실제 공개 홈페이지에서 상단/좌측 사이드바 열기·Mind Diary 등 신규 링크·콘솔 무에러까지 직접 클릭으로 확인했다 — 단 **관리자 로그인 자격 증명이 없어 `/admin/navigation/settings` 편집 캔버스 자체(클릭 시 실제로 열리는지, Controls 연동, 새 스타일/이미지 뱅크 반영, 좌우 사이드바가 캔버스 박스 안에 올바르게 잘리는지)는 이 세션에서 열어보지 못했다** — 로그인 가능한 브라우저에서 사람이 직접 확인 필요. **의도적으로 범위 밖으로 미룸**: 컬럼/이미지/요소를 마우스로 자유롭게 드래그해 옮기는 완전 자유 배치(지금은 순서 변경 화살표까지만), 좌우 사이드바 콘텐츠를 site_navigations 대신 별도 목록으로 이원화하는 것.
- **HOTFIX-140.1(구현+검증 완료, 2026-08-21)**: "상단 사이드바 트리거 아이콘이 실제 홈페이지에 안 보이던 버그 수정" — 상세는 CHANGELOG.md 동명 항목 참고. 사용자가 "상단 사이드바 아이콘이 실제 홈페이지에는 안보여"로 신고 → 원인은 EPIC-140에서 넣은 `TopSidebarConfig.enabled`(기본값 `false`) 마스터 스위치. 다른 모든 헤더 요소는 "항상 보이고 내용만 관리자가 채운다"는 원칙이라 이 패턴 자체가 설계 불일치였음을 확인, DB 값만 뒤집지 않고 `enabled` 개념을 아예 제거(`topSidebarSettings.ts`/`Navbar.tsx`/admin `TopSidebarControls`). `tsc`/`lint`/`build` 0 errors. Browser pane으로 실제 클릭까지 완전 검증: 로그아웃 상태 홈페이지에서 트리거 아이콘 노출 확인 → 클릭 → 스크린샷으로 패널이 정상 슬라이드다운되고 ✕/"로그인" 표시 확인(등록된 링크가 없어 컬럼2/3은 비어있는 게 정상) → ✕ 클릭 → 정상 닫힘 확인. **다음에 확인 필요(로그인 세션 필수, EPIC-140 항목과 동일)**: 컬럼2 링크를 실제로 등록한 뒤 hover 시 이미지·컬럼3 전환이 의도대로 되는지, 관리자 편집 캔버스 안에서 패널이 `absolute` 전환으로 잘리지 않는지.
- **EPIC-140(구현 완료, Browser pane 검증 부분적, 2026-08-21)**: "상단 탭/사용자 메뉴 추가·복제·삭제 + 드롭다운 z-index 버그 수정 + 신규 '상단 사이드바'(Kinfolk형 메가 메뉴)" — 상세는 CHANGELOG.md 동명 항목 참고. (1) EPIC-136이 헤더 wrapper의 `position:fixed`를 편집 모드에서 걷어낼 때 `z-40`까지 같이 지워 드롭다운이 슬라이드쇼에 가려지던 버그를 stacking context 조상 체인 직접 추적으로 원인 확정 후 수정(`relative` → `relative z-40`). (2) EPIC-138의 `user_menu` target_type 발견 덕에 "상단 탭 추가"와 "사용자 메뉴 항목 추가"가 사실 같은 `site_navigations` CRUD임을 확인, `CategoryTreeManager.tsx`의 `addChild`/`deleteRow`와 동일 컬럼/흐름으로 `addNavItem`/`duplicateNavItem`/`deleteNavItem` 신설 + Elements/Controls에 버튼 배선. (3) 신규 `TopSidebarPanel.tsx`+`topSidebarSettings.ts` — 헤더 우측 새 ☰ 아이콘(`HeaderSlot` "top-sidebar-trigger")을 누르면 위→아래 슬라이드하는 3열 메가 메뉴(컬럼1 실제 세션 데이터/컬럼2 관리자 링크 목록/컬럼3 hover cascade, 좌측 hover 이미지), 관리자 페이지에 `TopSidebarControls`(추가/삭제/순서/이미지 업로드) 신설, `site_settings.top_sidebar` 저장 연결. `tsc`/`lint`/`build` 0 errors. **검증 한계**: 이번 세션은 저장된 Supabase 세션이 전혀 없는 새 브라우저 프로필이라 로그인 자체를 못 해 관리자 캔버스 내 클릭/드래그를 직접 확인 못함(z-index 버그는 DOM 스택 조상 체인 직접 추적으로, 탭 CRUD는 이미 검증된 CategoryTreeManager 로직 재사용으로 대체) — 로그아웃 상태의 실제 공개 홈페이지 무회귀와 `top_sidebar` 비활성 시 트리거 미노출만 DOM으로 확인. **다음에 확인 필요(로그인 세션 필수)**: 드롭다운이 실제로 슬라이드쇼 위에 보이는지, 탭 추가/복제/삭제 버튼 실제 클릭, 상단 사이드바 트리거 클릭 시 실제 슬라이드다운 애니메이션 + hover 시 이미지/컬럼3 전환, 편집 모드에서 패널이 `absolute` 전환으로 잘리지 않는지. (트리거 미노출 자체는 HOTFIX-140.1로 해결됨.)
- **EPIC-139(구현 완료, Browser pane 검증 미완료, 2026-08-20)**: "헤더 편집 캔버스 버그 3건(글쓰기 드래그 불가/로그아웃 오작동/여백 배경 이미지 업로드 무피드백) 수정 + 드롭다운·메가 드롭다운·실행취소·삭제 기능 추가" — 상세는 CHANGELOG.md 동명 항목 참고. HeaderSlot.tsx의 클릭 처리를 캡처 단계로 옮겨 편집 모드에서 children의 실제 동작(로그아웃 등)이 실행되지 않게 수정, "글쓰기" 버튼을 HeaderSlot으로 감싸 드래그 가능하게, 이미지 업로드 실패를 alert로 표면화, MAX_WALLPAPERS 10→100, 탭 Controls 패널에 site_navigations.target_types 체크박스(EPIC-138과 동일 데이터) 직접 편집 추가, 신규 "메가 드롭다운" 표시 옵션(TopTabStyleEntry.megaDropdown), 하단 메뉴(Footer) Craft 블록에 삭제/위아래 이동 버튼, Craft.js 내장 Undo/Redo(actions.history) 연결(⌘/Ctrl+Z 단축키 포함). **의도적으로 범위에서 뺀 것**: "복제"(duplicate) 액션 — Craft.js `addNodeTree`가 전달된 트리의 노드 id를 그대로 재사용해 기존 state.nodes를 덮어쓴다는 걸 번들 소스 리버스엔지니어링으로 확인했고, 안전하게 만들려면 트리 전체의 id를 재생성하는 로직이 필요한데 이 세션엔 Browser pane 검증이 불가능해 실사용 중인 하단 메뉴(모든 페이지 노출)를 망가뜨릴 위험을 감수하지 않았다 — 다음 세션에서 Browser pane이 정상 작동할 때 구현할 것. 또한 사용자가 첨부한 BuilderJS 레퍼런스 스크린샷 9장 중 인라인 리치 텍스트 서식 툴바(B/I/U/S/링크/폰트크기/하이라이트), 인라인 이미지 크롭 도구, 그룹/배너가 있는 위젯 팔레트 재설계, `BaseWidget` 서브클래스 확장 시스템, 폼/가격표/체크아웃 프리미티브 카탈로그는 각각 별도 설계·구현이 필요한 규모라 이번 범위 밖(요청하면 별도 EPIC으로 진행). `tsc`/`lint`/`build` 0 errors. **검증 한계(EPIC-138과 동일한 sandbox 환경 제약, 다음에 반드시 확인 필요)**: 이 세션 dev 서버 브라우저에서 `site_navigations` client-side fetch가 실행되지 않아 관리자 캔버스를 실제로 열어보지 못했다(코드 리뷰 + Craft.js 번들 소스 직접 확인으로 대체) — 실제 로그인 가능한 브라우저에서 (1) 글쓰기 버튼 드래그, (2) 로그아웃 버튼이 더 이상 실제로 로그아웃시키지 않는지, (3) 탭 노출 위치 체크박스/메가 드롭다운 렌더링, (4) 하단 메뉴 삭제·이동 버튼, (5) Undo/Redo 단축키를 반드시 직접 확인해야 한다.
- **EPIC-138(구현 완료, Browser pane 검증 미완료, 2026-08-20)**: "사이트 구성관리 > 사이트메뉴에서 최상위 카테고리 노출 위치에 1단/2단 상단탭·사용자 메뉴 추가 + 복수 선택 가능하게" — 상세는 CHANGELOG.md 동명 항목 참고. `site_navigations.target_type`(단일) → `target_types`(배열) 스키마 변경(Management API로 실행+백필+anon REST로 검증 완료), CategoryTreeManager.tsx의 노출 위치 셀렉트를 체크박스 다중선택으로 교체(신규 TargetTypePopoverSelect/TargetTypeCheckboxList), navConfig.ts의 buildNavTree가 `{tabs, userMenuItems}`를 반환하도록 확장(모양은 dropdown/sidebar_left/sidebar_right, 배치는 tier1_tab/tier2_tab로 독립적인 두 축), Navbar.tsx는 예전 `/admin/navigation/settings`의 별도 tier 셀렉트(이중 설정)를 제거하고 카테고리 자체의 target_types로 tier1Tabs/tier2Tabs를 직접 필터링, "마이페이지"를 클릭 시 여는 신규 UserMenuDropdown(사용자 메뉴로 태그된 카테고리 링크 목록)으로 전환. `tsc`/`lint` 0 errors. **검증 한계(다음에 반드시 확인 필요)**: 이 세션의 dev 서버 브라우저에서 Navbar의 `fetchNavTabs()`가 `site_navigations`를 client-side로 조회하는 fetch 자체가 발생하지 않아 상단 탭이 빈 채로 렌더링됨을 발견했는데, **`git stash`로 이 변경을 완전히 되돌린 원본 코드에서도 동일 증상이 재현**돼 이번 변경과 무관한 sandbox 환경 제약(EPIC-133과 같은 계열)임을 확인했다 — 즉 코드 자체의 결함이 아니라는 근거는 있지만, 실제로 사람이 로그인 가능한 브라우저(또는 dev.silostore.net 배포)에서 (1) 사이트 메뉴 관리 화면의 체크박스 UI 동작, (2) 카테고리를 1단/2단/사용자 메뉴로 복수 태그했을 때 공개 홈페이지에서 실제로 여러 위치에 동시 노출되는지, (3) "마이페이지" 클릭 시 새 드롭다운이 정상적으로 열리고 닫히는지를 직접 확인해야 한다.
- **EPIC-137(구현+검증 완료, 2026-08-20)**: "'홈페이지 설정 관리'를 화면 전환 없는 단일 실시간 캔버스 + Elements/Controls/Page/Themes 4탭 패널로 재구성" — 상세는 CHANGELOG.md 동명 항목 참고. 사용자가 EPIC-136도 여전히 "4개의 화면"이라고 재차 거부, BuilderJS 레퍼런스대로 화면 전환 없는 단일 캔버스+4탭 왼쪽 패널을 명시적으로 요구. 섹션 전환 버튼 완전 제거, 캔버스 하나에 Navbar(헤더)→슬라이드쇼→사이드바 아이콘 대표 미리보기→하단 메뉴(Footer, 이제 인라인)까지 실제 페이지 순서로 이어 붙임. 왼쪽 패널을 Elements(요소 목록+하단 메뉴 블록 추가)/Controls(선택된 요소 설정, 이미지 필드마다 실제 썸네일 신규 추가)/Page(안내)/Themes(호버 모션 일괄 적용) 4탭으로 재편. Craft.js(하단 메뉴)와 커스텀 슬롯 시스템(헤더/슬라이드쇼/사이드바) 두 선택 체계를 `CraftBridge`로 하나의 selection 상태에 통합. `tsc`/`lint`/`build` 0 errors, Browser pane 실측으로 캔버스 연속성/이미지 썸네일/Elements→Controls 자동 전환/Craft·슬롯 selection 통합/저장→새로고침 왕복까지 확인. **다음에 확인 필요**: 사이드바 아이콘 "대표 미리보기" 방식에 대한 추가 피드백 가능성, 하단 메뉴 블록의 실제 드래그 추가 조작감(클릭 추가만 검증), Page 탭 콘텐츠 보강 여부.
- **EPIC-136(구현+검증 완료, 2026-08-20)**: "헤더 편집 캔버스를 '스타일 전용 복제품'에서 실제 `<Navbar>` 그 자체로 전환 + 진짜 자유 드래그" — 상세는 CHANGELOG.md 동명 항목 참고. 사용자가 EPIC-135 캔버스를 "실제 사이트랑 다르다", "블록 클릭해도 왼쪽에 설정이 없다", "숫자 슬라이더 말고 진짜 드래그로 아무 요소나 옮기게 해달라"고 재차 지적 → "완전 재구축" 확정. `Navbar.tsx`에 편집 모드 prop을 얹어 관리자 캔버스가 실제 컴포넌트를 그대로 렌더링(클론 폐기, 드리프트 구조적으로 불가능해짐), 각 요소를 `HeaderSlot`으로 감싸 클릭 선택+`transform: translate(dx,dy)` 기반 자유 드래그(홈페이지 콜라주 `useFreePosition`이 아니라 EPIC-117 tier1Tabs 트릭을 일반화 — Navbar의 기존 relative 구조를 안 건드리는 더 안전한 방식). 실측 중 `position:fixed` 헤더가 편집 캔버스 안에서 뷰포트로 튀어올라 안 보이던 버그를 발견해 수정. `tsc`/`lint`/`build` 0 errors. Browser pane 실측 — 실제 홈페이지 로그인 세션/메가메뉴 무회귀 확인, 합성 드래그가 안 먹히는 이 환경 한계(이 세션에서 반복 확인된 문제) 때문에 `PointerEvent` 직접 디스패치로 우회해 드래그 로직 자체는 확실히 검증(탭 이동→저장→새로고침→관리자 캔버스/실제 공개 홈페이지 양쪽에서 동일 transform 유지, PC/모바일 독립 저장 확인), 슬라이드쇼/사이드바 아이콘/하단 메뉴 섹션도 실제 데이터 정상 로드 확인. 테스트 오프셋은 Management API로 원상복구. **다음에 확인 필요**: 사람이 실제 마우스로 드래그 핸들을 잡고 옮기는 손맛(로직은 검증했지만 실제 제스처 미검증), 계정 메뉴 5항목 스타일을 위치처럼 개별화할지(지금은 위치만 개별, 스타일은 여전히 5항목 공통 하나), 사이드바 아이콘을 이 캔버스에 포함시켜 달라는 요청이 나올 수 있음(이번엔 의도적으로 범위 밖).
- **EPIC-135(구현+검증 완료, 2026-08-20)**: "'홈페이지 설정 관리' 4개 분리 화면 + EPIC-134 GrapesJS를 전부 폐기하고 실제 페이지 순서 그대로 쌓은 craft.js 통합 캔버스 하나로 전환" — 상세는 CHANGELOG.md 동명 항목 참고. 사용자가 GrapesJS 접근을 재차 거부("정말 내가 원하는게 뭔지 모르는구나")하며 로고→사용자 메뉴→상단 탭(1/2단)→슬라이드쇼→좌우 사이드바 아이콘을 한 화면·왼쪽 설정 패널로 편집하는 통합 캔버스를 명시적으로 요구, 신규 `ChromeSlideshowBlock`(실제 `HeroSlideshow.tsx` 재사용)/`ChromeDeviceToggle`(PC·태블릿·모바일)/로고 커스텀 폰트 관리 UI 추가, `page.tsx` 대규모 재작성(HOTFIX-134 탭 드래그 재정렬 로직 복원 포함), `HeaderGrapesEditor.tsx` 삭제. 실측 중 Craft `<Frame>`에 React.Fragment를 직접 자식으로 넣으면 죽는 버그(`.name`/`.displayName` 없는 Symbol이라 "component (undefined)"로 실패)를 새로 발견해 수정. `tsc`/`lint`/`build` 0 errors, Browser pane으로 통합 캔버스 5개 블록 전부(클릭 선택→왼쪽 패널 반영)+저장→홈페이지 새로고침 왕복까지 실사용 검증 완료. **다음에 확인 필요**: 실제 마우스로 탭 칩 드래그 재정렬(자동화 도구의 구조적 한계로 미검증, 로직은 HOTFIX-134에서 이미 검증된 것과 동일), 로고 커스텀 폰트 관리 UI 실사용, 태블릿 폭 미리보기 체감.
- **HOTFIX-134.6(구현+검증 완료, 2026-08-20)**: "GrapesJS 헤더 에디터 스타일시트 누락으로 패널 전체가 '처참하게' 보이던 버그 수정" — 상세는 CHANGELOG.md 동명 항목 참고. `HeaderGrapesEditor.tsx`가 GrapesJS JS 모듈만 로드하고 자체 CSS(`grapesjs/dist/css/grapes.min.css`)는 import한 적이 없어 패널/아이콘/드롭다운이 전부 브라우저 기본 스타일 그대로 나오던 것 — CSS import 한 줄로 해결. `tsc`/`lint`/`build` 0 errors, Browser pane으로 수정 전/후 비교(아이콘 78개 정상 축소, 드롭다운 커스텀 스타일 적용) 확인.
- **HOTFIX-134.5(구현+검증 완료, 2026-08-19)**: "프리셋 배경 점 그리드의 진짜 원인 제거: 화면에 고정된 2D 별 레이어를 완전히 삭제" — 상세는 CHANGELOG.md 동명 항목 참고. HOTFIX-134.4는 별 점 SVG 확대 배율(크기) 버그만 고쳤을 뿐, "화면에 고정된 2D CSS 레이어라 카메라 패닝과 무관하게 그대로 있다"는 진짜 원인은 놓쳤다 — 사용자가 "패닝할 때마다 따라다닌다"고 재신고해 발견. 이미 있던 진짜 3D 별(`UniverseParticles`, `<Canvas>` 안, 카메라 이동에 정상 시차)이 같은 역할을 하고 있어 2D 레이어(`PresetBackground.tsx`) 자체를 삭제. `tsc`/`lint`/`build` 0 errors, Browser pane으로 패닝 전/후 스크린샷 비교까지 완료. **환경 노트**: 다른 컴퓨터 커밋 pull 후 `npm install` 필요했음(grapesjs 등 새 의존성, node_modules 갱신 안 돼 있었음) — 코드 버그 아님, 멀티 디바이스 동기화 시 항상 확인할 것. "프리셋 배경 별 점 그리드 버그 실제 재현·수정" — 상세는 CHANGELOG.md 동명 항목 참고. 사용자가 "그리드 버그가 있는건 확실하니까, 원인을 찾아서 해결해"라고 재지시 — `PresetBackground.tsx`의 별 점을 SVG로 바꿔보는 과정에서 "SVG 고유 크기(8px)와 타일 간격(120~340px)이 안 맞으면 브라우저가 확대해서 채운다"는 진짜 메커니즘을 실제로 재현(Browser pane 스크린샷으로 확인 — 신고된 것과 같은 종류의 원형 얼룩 그리드). SVG 고유 크기를 타일 크기와 항상 일치시키도록 수정해 확대율을 1:1로 고정 — 수정 후 스크린샷으로 정상(작고 옅은 별 점) 확인 완료. `tsc`/`lint`/`build` 0 errors.
- **HOTFIX-134.3(구현 완료, 2026-08-19)**: "우주 공간 오브젝트 사본 간 크기/반짝임 위상 다양화" — 상세는 CHANGELOG.md 동명 항목 참고. HOTFIX-134.2가 진단한 "프리셋 배경 그리드처럼 보이는 문제"를 사용자가 "디자인/개수는 유지, 다른 방법으로 해결" 지시에 따라 수정 — 같은 디자인의 사본들이 `obj.id`만 시드로 써서 완전히 같은 크기·반짝임 위상이었던 게 진짜 원인, `index`를 시드에 섞어 사본마다 크기(0.7~1.3배)/위상을 자연스럽게 다양화(디자인·개수·위치 분포는 그대로 유지). `tsc`/`lint`/`build` 0 errors, Browser pane으로 재렌더링 확인(오브제 크기 다양화 확인, 콘솔 무해한 노이즈만).
- **HOTFIX-134.2(부분 완료, 2026-08-19)**: "오브젝트 선택 글로우 정적화+opacity 설정, 업로드 실패 무음 처리 수정" — 상세는 CHANGELOG.md 동명 항목 참고. (2) 선택 시 맥동하던 하늘색 글로우를 정적 파스텔 레이어로 교체 + `selectionGlowOpacity` 설정 추가 완료. (3) 업로드 실패 시 조용히 무시되던 버그 수정 완료(alert로 사유 표시) + 근본 원인이 Supabase 무료 플랜의 전역 50MB 파일 용량 상한(API로도 못 올림, 유료 전환 필요)임을 Management API로 확인·문서화. (1)은 HOTFIX-134.3에서 처리 완료.
- **HOTFIX-134.1(구현+실사용 검증 완료, 2026-08-19)**: "통합 헤더 레이아웃 PC/모바일 독립 설정" — 상세는 CHANGELOG.md 동명 항목 참고. EPIC-134가 "범위 밖"으로 미뤘던 PC/모바일 독립 설정을 다른 설정들과 동일한 `{pc,mobile}` shim 패턴으로 추가. `tsc`/`lint`/`build` 0 errors. 관리자 세션으로 PC/모바일 각각 저장 → DB에서 `pc`/`mobile`이 독립적으로 저장됨(서로 덮어쓰지 않음) 확인, 두 뷰포트(데스크톱/375px) 각각 새로고침해 올바른 쪽을 읽어 렌더링함을 DOM으로 확인, 테스트 데이터는 원상복구. **다음에 확인 필요**: PC/모바일에 실제로 서로 다른 순서·스타일을 넣어 두 화면이 실제로 다르게 보이는지(이번엔 구조적 독립성만 검증, 내용을 다르게 만들어 보지는 않음).
- **EPIC-134(구현+실사용 검증 완료, 2026-08-19)**: "GrapesJS 기반 통합 헤더 캔버스 에디터 — 상단 탭/사용자 메뉴 폐기·통합" — 상세는 CHANGELOG.md 동명 항목 참고. `grapesjs` 신규 설치, 커스텀 컴포넌트 타입(`header-item`/`header-row`)만 허용하는 제한된 구조적 에디터로 설계해 저장 데이터를 `HeaderLayoutItem[]`(실제 NavTab.key/고정 메뉴 키 참조)로 직렬화 — GrapesJS raw HTML을 사이트에 직접 주입하지 않는다. `Navbar.tsx`는 `unified_header_layout`이 없으면 100% 기존 3분할 레이아웃 그대로(완전 opt-in), 있으면 기존 `renderTab()`/신규 `renderMenuItem()`(기존 로직 그대로 함수 추출)을 재사용해 탭+계정 메뉴를 뒤섞은 한 줄로 렌더링. `tsc`/`lint`/`build` 0 errors. **실사용 검증(관리자 로그인 세션)**: 헤더 섹션 진입→블록/레이어 매니저 실제 데이터 확인→저장→홈페이지 새로고침까지 전체 파이프라인을 DOM으로 직접 확인(탭+계정메뉴 12개 항목이 실제로 한 줄에 뒤섞여 렌더링됨), 테스트로 저장한 DB 값은 Management API로 삭제해 원상복구(배포돼도 관리자가 직접 저장하기 전까진 기존 레이아웃 유지). **범위 밖으로 미룸**: 로고/슬라이드쇼/하단 메뉴는 아직 GrapesJS 블록으로 통합 안 함(기존 폼 유지), PC/모바일 독립 레이아웃 없음(공용 단일 목록). **다음에 확인 필요**: 실제 마우스 드래그로 블록 순서 섞기 조작감, 통합 레이아웃 활성 시 멤버십 팝오버 위치(계정 영역이 비어 화면 우상단에 앵커 — 기능은 정상, 트리거와 시각적으로 멀 수 있음).
- **HOTFIX-132.2(구현 완료, 스크린샷 검증 못함, 2026-08-19)**: "오브젝트 클릭 줌인 거리 슬라이더가 전혀 작동 안 하던 버그 수정" — 상세는 CHANGELOG.md 동명 항목 참고. 두 가지 원인 동시 수정: (1) 전역 `<CameraControls minDistance={0.6}>` 하한이 매 프레임 강제돼 작은 오브젝트(대부분)는 배율을 바꿔도 실제 거리가 늘 0.6으로 눌리던 것 — 오브젝트 포커스 중에는 `controls.minDistance`를 그 오브젝트가 필요로 하는 거리로 임시 하향하고 포커스를 벗어나면 복원, (2) 슬라이더 `onChange`가 state만 바꾸고 카메라를 재호출하지 않아 재클릭 전까지 아무 변화가 없던 것 — `handleSelectedObjectChange`가 `focusDistanceMultiplier` 변경 시 즉시 `focusCameraOnObject`를 재호출하도록 배선. `tsc`/`lint` 0 errors(신규 warning 없음). **검증 한계**: 이번에도 Browser pane이 "표시되지 않음" 상태라 스크린샷으로 실제 줌 반응을 확인 못함(`/about-silo` 정상 로드 + 콘솔 무에러까지만 확인) — 코드/수치 분석(정규화된 오브젝트 크기 대 `minDistance` 상수 대조)과 drei `CameraControls` 소스 확인으로 근거를 대체했다. **다음에 확인 필요**: 사람이 직접 오브젝트 클릭 → 슬라이더 1배~8배 드래그 → 카메라 거리가 즉시 반응하는지, "자동으로 되돌리기" 버튼 반영, 패널 닫고 행성/게시물 클릭 시 정상 거리로 복원되는지.
- **EPIC-133(구현 완료, 실제 스크래핑 성공 미확인, 2026-08-19)**: "인스타그램 비공식 서버 사이드 스크래핑 + 리버스 프록시 + 미니멀 슬라이더" — 상세는 CHANGELOG.md 동명 항목 참고. `instagramScraper.ts`(캐러셀 우선 파싱→og 태그 폴백)/`/api/proxy-ig`(CDN 화이트리스트 리버스 프록시)/`/api/instagram-post`(인증 필요 스크래핑 API)/`InstagramMediaSlider.tsx`(gallery-carousel 스타일 재사용 슬라이더) 4개 신규 파일. `tsc`/`lint` 0 errors, 신규 라우트 3개는 curl로 401/400 응답까지 직접 확인. **중요한 검증 한계**: 이 서버 sandbox의 아웃바운드 IP로 실제 instagram.com permalink(임의/실존 게시물, 여러 UA)를 curl로 직접 가져와봤지만 매번 og:image/캐러셀 JSON이 전혀 없는 빈 앱 셸만 돌아옴 — 클라우드 IP 차단으로 추정되며, Vercel 배포에서도 동일하게 막힐 가능성이 있다(코드는 best-effort 설계라 실패해도 링크 폴백으로 안전하게 동작함). **다음에 확인 필요**: (1) 실제 배포 환경(dev.silostore.net)에서 로그인 세션으로 `/api/instagram-post?url=<실제 공개 게시물>`을 호출해 미디어가 정말 추출되는지 확인 — 이게 안 되면 이 접근 자체의 실효성을 재검토해야 함, (2) 안 될 경우 대안(oEmbed 앱 토큰, 수동 업로드 병행 등) 검토, (3) `InstagramMediaSlider`를 게시글 작성 플로우 어디에 노출할지(기존 `instagramEmbed.ts` 공식 위젯 경로를 이걸로 교체할지 등) 결정 — 기존 게시글 렌더링에 영향을 주는 문제라 이번 세션은 의도적으로 배선하지 않음.
- **HOTFIX-132.1(구현 완료, 검증 부분적, 2026-08-19)**: "넘버링 규칙 변경 + Craft 캔버스 선택 시 테두리/이름표 표시 + 오브젝트 줌인 거리를 크기 비례로 수정" — 상세는 CHANGELOG.md 동명 항목 참고. 이번 항목부터 새 넘버링 규칙(HOTFIX는 `HOTFIX-<EPIC>.<n>` 형식, CLAUDE.md에 명문화) 적용. 4개 Craft 블록에 선택/호버 시 파란 테두리+이름표 pill 추가(`ChromeSelectionOverlay`, BuilderJS 대비 가장 눈에 띄던 시각적 격차). 3D 오브젝트 클릭 시 줌인 거리를 고정값 대신 오브젝트 실제 바운딩박스 크기에 비례하도록 수정 + 오브젝트별 배율 오버라이드 슬라이더 추가. `tsc`/`lint`/`build` 0 errors, 선택 테두리는 DOM에서 실제 렌더링 확인. **검증 한계**: 이 세션의 스크린샷 기능이 계속 실패해("Browser pane이 표시되지 않음") 3D 줌 거리 수정 결과를 시각적으로 확인 못함(코드 리뷰 + 무에러 로드까지만 확인) — 사람이 직접 여러 오브젝트를 클릭해 거리감을 확인 권장.
- **HOTFIX-134(구현 완료, 검증 부분적, 2026-08-19)**: "상단 탭 같은 단 안에서 좌우 순서 드래그 + 로고/탭 프리뷰 커스텀 폰트 누락 수정 + 인스타그램 CDN 원인 확인" — 상세는 CHANGELOG.md 동명 항목 참고. `TopTabStyleEntry.order`(fractional index) 신설로 같은 단 안에서도 칩을 특정 탭 앞에 드래그해 끼워 넣을 수 있게 확장(HOTFIX-133은 어느 단인지만 바꿀 수 있었음), 로고/상단 탭 프리뷰의 커스텀 폰트 폴백 체인 누락 수정(실제와 다르게 보이던 원인), 인스타그램 CDN 이미지 깨짐이 실제로 서명 만료(403 확인)임을 진단(코드 수정은 범위 밖이라 보류, 사용자 확인 필요). `tsc`/`lint`/`build` 0 errors, 로고 폰트 일치는 `getComputedStyle` 직접 비교로 확인, 인스타그램 403은 서버 curl로 확인. **검증 한계**: 같은 단 안 드래그 재정렬은 크롬이 신뢰되지 않은(비-사용자 제스처) drop 이벤트에서 `dataTransfer.getData()`를 빈 값으로 반환하는 보안 정책 때문에 합성 이벤트/CDP 드래그 둘 다로 자동화 검증이 불가능함을 확인 — 로직은 코드 리뷰로 재확인했지만 실제 마우스 드래그는 사람이 직접 확인 필요. "글쓰기" 버튼은 이번 드래그 시스템 대상 아님(하드코딩 항목이라 범위 밖).
- **HOTFIX-133(구현 완료, 검증 부분적, 2026-08-19)**: "3D 카메라 버그 3건 수정 + 우주 오브젝트 무작위 개수 설정 + 상단 탭 드래그앤드롭 배치" — 상세는 CHANGELOG.md 동명 항목 참고. 화살표 키 상하 반전 수정, 더블클릭 줌 실제 원인(Stars/Sparkles 파티클이 raycast를 막고 있었음) 수정, 오브젝트 클릭 시 카메라 포커스 신설, 상단 탭 1단/2단을 실제 HTML5 드래그앤드롭으로 배치(Craft.js Frame 리마운트 트릭 필요했음), 우주 오브젝트 개수/재배치 설정. `tsc`/`lint`/`build` 0 errors, 세션 전용 프로덕션 서버(3001)에서 더블클릭 줌/드래그앤드롭 실제 동작 확인. **다음에 확인 필요**: little prince.mp4 레퍼런스(이번 세션은 동영상 파일을 읽을 수 없어 못 봄) 반영 여부, 화살표 키/오브젝트 포커스 실제 마우스 손맛, 드래그 중 시각적 피드백 보강 여부.
- **EPIC-132(구현 완료, 컷오버 남음, 2026-08-18)**: "홈페이지 설정 관리" 4개 섹션(메인 로고/사이드바 아이콘/상단 탭 디자인/사용자 메뉴 디자인)을 craft.js 클릭-선택 WYSIWYG로 전환 — 상세는 CHANGELOG.md 동명 항목 참고. Navbar.tsx는 전혀 안 건드리고 독립 프레젠테이션 뷰+Craft 블록으로 구성, 저장은 여전히 site_settings(setProp+기존 shim setter 이중 배선). `tsc`/`lint`/`build` 0 errors, Browser pane 실측으로 4개 섹션 전부 클릭→설정 패널→실시간 반영까지 확인(레이아웃 버그도 실측 중 발견해 수정). **다음에 확인 필요**: 실제 파일 업로드 왕복, PC/모바일 토글 리마운트, 저장 버튼 DB 반영, 그리고 B6(옛 폼 UI 제거) 컷오버 — 이번 세션은 새 캔버스를 기존 폼과 나란히 유지만 하고 제거는 안 함(다음 세션에서 실사용 확인 후 진행 권장).
- **HOTFIX-131(구현 완료, 검증 부분적, 2026-08-18)**: "My Page 행성 캐릭터 크기 버그 + 설정 패널 드래그 이동 + 오브제 기즈모 안내 + SILO 오브제 크기 + 키보드/터치 카메라 조작" — 상세는 CHANGELOG.md 동명 항목 참고. 캐릭터 GLB 바운딩박스(키 기준) 정규화, 3개 설정 패널 드래그 가능(신규 `useDraggablePanel` 훅), 기즈모 옆 "여기를 클릭해서 이동" 안내(`SelectionHint`), 오브제 목표 크기를 행성 반지름에 비례하도록 수정 + 기본 scale 0.3→0.6, 화살표 키 패닝, 두 손가락 팬(TOUCH_TRUCK)+빈 우주 더블클릭 줌 인/아웃 토글. `tsc`/`lint` 0 errors, 다른 세션의 dev 서버에 붙어 `/about-silo` 콘솔 에러 없이 로드 확인했지만 Browser pane이 "표시되지 않음" 상태라 스크린샷 실측은 못함. **다음에 확인 필요**: 실제 캐릭터 업로드 비율/패널 드래그 손맛/SILO 오브제 가시성/화살표 키·모바일 제스처 손맛 — 사람이 직접 확인 권장.
- **EPIC-130(구현 완료, 검증 부분적, 2026-08-18)**: "사용자 메뉴(계정 영역) 디자인+hover 모션 설정 탭 + 모션 프리뷰 샘플" — 상세는 CHANGELOG.md 동명 항목 참고. 신규 accountMenuStyleSettings.ts + Navbar.tsx의 silo-account-menu-item 공유 클래스로 관리자/등급/마이페이지/이름/로그아웃 5개 항목에 서체/색/hover 모션 일괄 적용. 신규 MotionPreviewSamples.tsx로 6가지 모션을 나란히 비교하는 스와치를 상단 탭 디자인+사용자 메뉴 디자인 양쪽에 배치. `tsc`/`lint`/`build` 0 errors. **검증 한계**: 세션 후반부에 Browser pane이 "not displayed" 상태(document.visibilityState="hidden")가 되어 실제 화면 스크린샷으로 hover 모션이 보이는지까지는 확인 못 함 — DOM 직접 조회로 CSS 주입/컨테이너 존재까지만 확인. 다음 세션에서 반드시 실제 로그인 상태로 화면 확인 필요.
- **EPIC-129(구현+검증 완료, 2026-08-18)**: "홈페이지 설정 관리: 메인 로고/사이드바 아이콘/상단 탭 디자인 PC·모바일 독립 설정" — 상세는 CHANGELOG.md 동명 항목 참고. hero_slideshow의 {pc,mobile} 패턴을 3개 설정에 동일 적용, 신규 공용 lib 3개(mainLogoSettings.ts/sidebarIconsSettings.ts/topTabStyleSettings.ts)로 관리자 페이지+Navbar.tsx가 정규화 로직 공유. 관리자 페이지는 "읽기/쓰기 shim" 패턴으로 기존 JSX 필드 바인딩을 하나도 안 건드리고 PC/모바일 토글만 추가. Navbar.tsx는 신규 useIsMobileViewport() 훅으로 실제 뷰포트에 맞는 변형을 고름. `tsc`/`lint`/`build` 0 errors, Browser pane으로 PC/모바일 뷰포트 둘 다 Navbar 정상 렌더 확인(사이트 전체 영향 변경이라 특히 신경 씀) + 관리자 페이지에서 PC/모바일 값이 실제로 독립적으로 유지되는 것 DOM으로 확인. **다음에 확인 필요**: 실제 모바일 기기로 저장된 모바일 전용 값이 반영되는지, tier1/tier2 오프셋 같은 레이아웃 민감 필드가 모바일에서 이상하게 겹치지 않는지.
- **HOTFIX-128(구현+검증 완료, 2026-08-18)**: "SILO 고정 카테고리 마커 삭제 + 오브제 상단 클러스터 배치 + 게시판 연동 정보 카드" — 상세는 CHANGELOG.md 동명 항목 참고. CORE_CATEGORIES(4종 하드코딩 마커)+chestVariant/cameraVariant 완전 삭제. 오브제 기본 배치를 피보나치 균등 분포→북극(행성 맨 위) 클러스터로 교체. `UniverseObject.boardSlug` 신설 — 연결하면 `ObjectInfoCard`가 그 게시판 실제 name/description/thumbnail_url을 자동 표시(수동 입력이 있으면 그게 우선). `tsc`/`lint`/`build` 0 errors, Browser pane으로 마커 사라짐+상단 클러스터 배치 확인. **다음에 확인 필요**: 실제로 게시판을 연결해 정보 카드에 게시판 설명/썸네일이 정상 표시되는지(이번 세션은 boardSlug가 설정된 실제 오브제로 왕복 테스트는 안 함), 클러스터 배치 각도(maxAngle=0.5rad)가 오브제 개수가 많아질 때도 적당히 조밀한지.
- **HOTFIX-127(구현+검증 완료, 2026-08-18)**: "우주 공간 오브젝트(별/은하수/소행성) 업로드 + 하단 메뉴 관리 병합 + 홈페이지 설정 관리 프리뷰 실시간화" — 상세는 CHANGELOG.md 동명 항목 참고. 신규 `SpaceObject`(행성 표면 중력 없이 우주에 자유롭게 뜨는 오브젝트, 3D 모델 또는 이미지 스프라이트)를 `UniverseSettingsPanel`(전역)에서 업로드 가능. `/admin/footer`의 Craft 풀스크린 에디터를 "홈페이지 설정 관리"의 5번째 섹션으로 흡수(라우트는 남기고 상단 탭에서만 제거). 프리뷰 안 보이던 버그(2xl→xl 브레이크포인트)와 저장 전에는 안 보이던 문제(postMessage 실시간 오버라이드, `?__adminPreview=1`)를 함께 수정. `tsc`/`lint`/`build` 0 errors, Browser pane으로 실시간 반영(iframe DOM 직접 확인)/Craft 에디터 열기·닫기/공간 오브젝트 업로드 UI 렌더링까지 실측. **알려진 한계**: "슬라이드쇼" 섹션은 홈페이지가 이미 Craft로 완전히 렌더링돼서(`CraftHomeRenderer`) 이 프리뷰 방식으로는 실시간 반영이 안 될 수 있음(기존부터 있던 구조적 불일치) — 안내 문구로 고지, 근본 해결은 별도 판단 필요. **다음에 확인 필요**: 실제 .glb/이미지 파일로 우주 오브젝트 업로드 왕복(이번 세션은 UI 렌더링까지만 확인, 실제 업로드는 미실행), 소행성/별 기본 스캐터 위치가 시각적으로 자연스러운지, 저장 후 새로고침해도 우주 오브젝트 위치가 유지되는지.
- **EPIC-126(구현+검증 완료, 2026-08-18)**: "'홈페이지 설정 관리'를 섹션 클릭형 3단 레이아웃으로 재편 + 상단 탭 hover 모션 6종 + 드래그 슬라이더" — 상세는 CHANGELOG.md 동명 항목 참고. **명시적으로 범위 밖으로 미룸**: 전역 크롬(로고/사이드바/상단 탭) 렌더링 자체를 Craft.js로 완전히 이전하는 것과 그 안에 요소를 자유 배치로 드래그해 넣는 진짜 Craft 기능 — 사이트 전체 블라스트 반경 때문에 이번엔 관리자 편집 UX만 업그레이드(드래그 슬라이더/hover 모션/좌측 섹션 네비/큰 프리뷰)하고 렌더링 경로(`Navbar.tsx`+`site_settings`)는 그대로 뒀다. 사용자가 여전히 전역 크롬의 완전한 Craft 이전을 원하면 별도 EPIC으로 착수 필요. `tsc`/`lint`/`build` 0 errors, Browser pane으로 hover 모션 CSS 주입/3단 레이아웃/드래그 슬라이더 값 계산까지 실측 확인(드래그 자체는 자동화 도구 한계로 합성 PointerEvent 디스패치로 검증). **다음에 확인 필요**: 실제 마우스 드래그 손맛, hover 모션이 기존 배경색 hover와 겹쳤을 때 시각적 만족도, 2xl 미만 좁은 화면에서 프리뷰가 숨겨지는 게 괜찮은지.
- **HOTFIX-125(구현+검증 완료, 2026-08-18)**: "게시글 위성 hover/클릭 무반응 + 행성 색·텍스처 블렌드 안 되던 버그 수정" — 상세는 CHANGELOG.md 동명 항목 참고. 위성 hover/클릭 무반응은 자동 페이드 썸네일(HOTFIX-123)이 실제 클릭 대상(작은 별)과 다른 화면 좌표에 pointerEvents:none으로 떠 있던 게 원인 — 사진 자체를 진짜 인터랙션 대상으로 전환. 텍스처 블렌드는 반투명 구 2장 겹치기(WebGL 알파 블렌딩, 조명/각도 의존적)를 2D Canvas 픽셀 합성으로 교체해 슬라이더 비율 그대로 섞이게 함. `tsc`/`lint`/`build` 0 errors, Browser pane으로 블렌드 실제 변화(60/40 스크린샷)와 DOM 직접 클릭으로 SelectedPostPanel 정상 표시까지 확인. **다음에 확인 필요**: 실제 마우스로 궤도 회전 중인 위성에 hover했을 때 box-shadow 글로우가 체감상 자연스러운지(자동화는 궤도 회전 때문에 좌표 기반 hover 테스트가 불안정해 DOM 이벤트 디스패치로만 클릭 검증).
- **EPIC-124(구현 완료, 2026-08-18)**: "행성별 독립 설정 창(SILO/유저) + 오브제 정보 카드 + 게시글 위성 자동 페이드/디자인 업로드 + SILO 오브제 정리" — 상세는 CHANGELOG.md 동명 항목 참고. `UniverseConfig`를 `planets: { silo, user }`로 재구성(하위 호환 마이그레이션 포함), 장식 오브젝트를 행성 자전 그룹 안에 로컬 좌표로 중첩(자전 동반 + 표면 클리핑 근본 해소), 오브젝트 클릭 시 썸네일/요약/링크 정보 카드, 게시글 위성 CSS 자동 페이드+디자인 이미지 업로드, 유저 행성도 SILO와 동일 구조(별도 `PlanetSettingsPanel` 인스턴스로 분리), 전역 패널은 배경/공통 설정만. 라이브 DB의 SILO 오브젝트를 "venus de milo" 하나만 남기고 새 스키마로 마이그레이션 완료(Management API로 직접 실행+재조회 확인). `tsc`/`lint`/`build` 0 errors. **다음에 확인 필요(이번 세션은 Browser pane이 표시되지 않아 스크린샷/클릭 검증 불가 — 코드 레벨 검증만 완료)**: 행성 클릭 시 전용 설정 창이 실제로 뜨는지, 위성 썸네일 자동 페이드가 자연스러운지, 유저 행성 오브젝트/위성 디자인 업로드 왕복, 오브제 정보 카드 실제 표시, 저장 버튼으로 새 스키마가 실제로 site_settings에 반영되는지(자동저장 5분 대기 또는 수동 저장으로 왕복 확인). 사람이 직접 브라우저로 확인 권장.
- **HOTFIX-123(구현+검증 완료, 2026-08-15)**: "행성 드래그 스냅백/오브제 glow/저장 안 됨/표면 중력·클리핑/썸네일 반짝임 5건 수정" — 상세는 CHANGELOG.md 동명 항목 참고. 행성 드래그 후 스냅백은 실제로 행성이 움직이는 기능이 없어서가 아니라 드래그(카메라 회전) 후 마우스를 뗄 때 `onClick`이 그대로 발동해 고정 프레이밍으로 되돌리던 것이 원인 — `useDragAwareClick`(이동거리 임계값)으로 분리. 오브제/썸네일 glow는 임의 `.glb` 머티리얼을 안 건드리고 가산 블렌딩 오라+Bloom 포스트프로세싱으로 구현. 저장 안 되던 문제는 (a) 에러 스왈로우 (b) 오브제 인스펙터에 저장 버튼 자체가 없었던 UX 문제 둘 다 수정. SILO 표면 클리핑은 드래그 후 위치/방향(quaternion)이 어긋나던 게 근본 원인 — 매 렌더 구면 재투영으로 수정("중력"). `tsc`/`lint` 0 errors, Browser pane으로 드래그-후-스냅백-없음(직후/1.5초 후 스크린샷 비교)과 정상 클릭 포커스 둘 다 실측 확인. **다음에 확인 필요**: 실제 마우스로 오브제를 표면 위에서 드래그해보는 손맛(TransformControls 기즈모 조작 자체는 EPIC-121부터 자동화로 확인 못 하는 한계 동일), glow/트윙클 애니메이션의 실제 시각적 강도가 사용자 취향에 맞는지, 저장 버튼을 실제로 눌러 site_settings에 반영되는지 왕복(로컬 dev DB 쓰기까지는 이번 세션에서 실행 안 함 — 코드 경로만 확인), CORE_CATEGORIES(4대 카테고리) 확장이 "SILO 최상위 카테고리 5개" 요청의 의도와 맞는지 사용자 확인.
- **HOTFIX-122(구현+검증 완료, 2026-08-15)**: "게시글 마커를 sphere에서 hover-썸네일 별 모양으로 + 클릭 시 깜빡임 버그 수정" — 상세는 CHANGELOG.md 동명 항목 참고. 카메라~SILO 거리 기반 opacity 페이드(옛 "내핵" LOD 연출의 잔재, EPIC-121에서 카테고리는 이미 뗐지만 게시글 마커에는 남아있던 것)가 클릭 시 카메라가 다가가면서 깜빡임을 유발하던 원인 — 메커니즘 전체 제거. 마커를 별 모양(hover 시 원형 사진 미리보기)으로 통합(`OrbitStarMarker`), 이제 텍스처 로딩 자체가 필요 없어져 코드 단순화. `tsc`/`lint`/`build` 0 errors, 실측으로 hover 미리보기와 클릭 선택 둘 다 확인. **다음에 확인 필요**: 실제 마우스로 여러 마커를 빠르게 오가며 hover했을 때 미리보기가 자연스럽게 전환되는지(사람 손 테스트 권장).
- **EPIC-121(구현+검증 완료, 2026-08-15)**: "행성 줄무늬 제거/자유 회전/오브젝트 표면 배치+드래그/UI 통합 등" — 상세는 CHANGELOG.md 동명 항목 참고. toon 셰이딩 제거+색/텍스처 블렌드, 카메라 극각 무제한, Leva→UniverseSettingsPanel 통합+우측 ObjectInspectorPanel 신설, 내핵 오브젝트를 표면에 항상 노출, `fibonacciSphere` 극점 편중 버그 수정(venus de milo 안 보이던 추가 원인), TransformControls 드래그 이동, 게시글 썸네일 sphere화. `tsc`/`lint`/`build` 0 errors, 스크린샷으로 전부 실측 확인. **명시적으로 범위 밖으로 미룸**: 행성끼리 실제로 공전하며 거리가 벌어지는 것(SILO_CENTER를 고정 월드 좌표로 쓰는 현재 구조상 큰 리팩터 필요, 카메라/LOD/연결선 동시 재작업 위험) — 별도 EPIC 제안, 사용자 확인 필요. **다음에 확인 필요**: 실제 마우스로 카메라를 행성 아래→위로 자유롭게 돌려보는 것(자동화 한계로 폴리곤 각도 값만 확인, 실제 드래그 손맛 미검증), TransformControls로 오브젝트를 실제로 마우스 드래그해 옮겨보고 저장 후 새로고침해도 위치가 유지되는지.
- **HOTFIX-120(구현+검증 완료, 2026-08-15)**: "업로드한 장식 오브젝트(.glb)가 화면에 반영 안 되던 버그 수정" — 상세는 CHANGELOG.md 동명 항목 참고. 라이브 DB에 실제 저장돼 있던 사용자의 "venus de milo"/"vintage camera" 두 파일을 직접 내려받아 진단 — 파일 자체는 정상, 원인은 원본 모델 단위가 파일마다 10배씩 달라(0.28 vs 2.80) 고정 scale 곱셈으로는 하나는 너무 작고 하나는 행성보다 커지던 것 + 구면 방향 정렬이 없어 표면에 파묻히던 것. 바운딩 박스 기반 자동 정규화 + 법선 방향 정렬로 수정, 로드 실패 시 관리자에게 "⚠ 로드 실패" 표시도 추가. `tsc`/`lint`/`build` 0 errors, 실제 사용자 파일 2개로 재현 검증 완료. **다음에 확인 필요**: 사용자가 실제 화면에서 두 오브젝트가 이제 보이는지 육안 확인(이번엔 크기/방향 계산 로직을 콘솔 로그로 수치 검증했지만, Browser pane 스크린샷 해상도 한계로 "눈으로 명확히 보인다"까지는 완전히 확인 못함).
- **EPIC-119(구현+검증 완료, 2026-08-15)**: "About Silo 3D 우주를 [3D World Builder]로 전면 개편" — 상세는 CHANGELOG.md 동명 항목 참고. 캐릭터/장식 오브젝트/행성 텍스처를 관리자가 `.glb`/이미지로 직접 업로드하면(에셋 없을 땐 기존 절차적 결과물 폴백) 교체되는 구조로 전환, 행성 클릭 시 카메라 포커싱, 실뜨개 선 드래그 인터랙션(lerp 근사, 실제 물리 엔진 아님), Stars/Sparkles/별똥별 배경, 유튜브 프리셋 배열 편집, 궤도 데이터소스 게시판 드롭다운, `site_settings.about_silo_universe`에 5분 자동저장+수동저장+토스트. `tsc`/`lint`/`build` 0 errors, 로컬 dev에서 캔버스 렌더링/설정 패널/저장 성공까지 확인. **다음에 확인 필요(실제 에셋 필요, 이 세션엔 없었음)**: 관리자가 실제 캐릭터 `.glb`(클립 포함)/장식 오브젝트/행성 텍스처를 직접 업로드해 로딩·마운트·애니메이션 재생이 실제로 되는지, 실뜨개 드래그의 실제 손맛(마우스), 저장된 설정이 새로고침 후에도 정확히 복원되는지 왕복 확인.
- **EPIC-118(구현+검증 완료, 2026-08-15)**: "1단 탭 계정 영역 충돌 수정 + 실제 홈페이지 기반 PC/모바일 미리보기로 교체" — 상세는 CHANGELOG.md 동명 항목 참고. 1단 탭을 absolute+right-4에서 계정 영역 앞 flex 형제로 바꿔 로그인 상태 충돌 해결, `tier2OffsetPx` 신설(2단도 위치 조정 가능), 가짜 미리보기(`MobilePreviewFrame`)를 실제 홈페이지 iframe 기반 `LivePreviewFrame`(PC+모바일)으로 전면 교체 — 저장 시 자동 새로고침. `tsc`/`lint`/`build` 0 errors, 실측으로 충돌 해소·미리보기 실동작·저장 후 자동 반영까지 확인. **다음에 확인 필요**: 좁은 모바일 폭(실제 375px 뷰포트)에서 1단 탭+계정 영역이 줄바꿈 없이 잘 들어가는지, 1단에 탭이 더 많아졌을 때 로고 줄이 세로로 늘어나는지.
- **EPIC-117(구현+검증 완료, 2026-08-15)**: "상단 탭 1단/2단 배치, 1단은 로고 섹션과 겹치게" — 상세는 CHANGELOG.md 동명 항목 참고. 탭별 `tier`(1단/2단) + `tier1OffsetPx` 신설, 1단 탭 줄이 로고 줄 하단 경계선에 걸치도록(`translate-y-1/2`) 배치. `/admin/navigation/settings`에 탭별 "표시 위치" select + "1단 겹침 정도" 입력 추가. `tsc`/`lint`/`build` 0 errors, 실제 온라인 도슨트/스튜디오/마이 페이지를 1단으로 설정해 홈페이지에서 사용자 레퍼런스 이미지와 일치하는 배치를 스크린샷으로 확인(현재 라이브 DB에 이 설정이 적용된 상태로 남아있음 — 사용자 요청 그대로). **다음에 확인 필요**: 모바일 폭에서 1단 탭이 로고와 겹쳐 너무 좁아지지 않는지, 1단에 탭을 더 많이(4개 이상) 배치했을 때 줄바꿈이 자연스러운지.
- **HOTFIX-116(구현+검증 완료, 2026-08-15)**: "`next build` 타입 에러로 EPIC-113부터 dev.silostore.net 배포가 전부 실패하던 문제 수정" — 상세는 CHANGELOG.md 동명 항목 참고. `SettingsSidebar.tsx`의 `<selected.Settings />` 타입 추론 붕괴(`never`)로 `next build`가 계속 실패하고 있었는데 `tsc --noEmit`은 이 에러를 재현하지 않아 EPIC-113/114/115 세 번 다 "무관, 영향 없음"으로 지나쳤던 것 — 실제로는 이 시점부터 Vercel 배포가 전부 조용히 실패했을 가능성이 큼. `ComponentType<Record<string, never>>` 캐스팅으로 수정, 로컬 `npm run build` 최초 성공 확인(`/about-silo`가 정적 라우트로 정확히 잡힘). **다음에 확인 필요(레포 밖, agent가 못 봄)**: main/develop push 이후 실제 Vercel 배포 성공 여부 + `dev.silostore.net/about-silo`가 3D 우주로 바뀌는지 — 사용자 확인 필요. **교훈**: 이후 Craft 블록/설정 패널 관련 변경은 `tsc --noEmit`만으로 안심하지 말고 `npm run build`까지 실행해 검증할 것.
- **EPIC-115(구현 완료, 2026-08-14, 브랜치 `feature/EPIC-115`, EPIC-114 위에 이어서 진행, 아직 main/develop 병합 전)**: "어린 왕자 감성 렌더링 오버홀 + Leva 설정 패널" — 상세는 CHANGELOG.md 동명 항목 참고. 절차적 수채화 텍스처(캔버스 블롭+노이즈), 궤도 사진 기본 숨김(가벼운 마커로 대체), maxDistance 5000, About Me 위성 5종 신설, 캐릭터 절차적 아이들 모션(머리/다리, AnimationMixer 대신 — 실제 GLTF 클립이 없어서), 살롱데상 오브제에 폴라로이드 통합, `leva` 설정 패널(배경 유튜브/프리셋 토글, 행성 컬러/썸네일/속도, 캐릭터 A/B/C, 내핵 오브젝트 변형). `tsc`/`lint` 0 errors(렌더 단계 클로저 재대입을 잡아낸 React Compiler lint 에러 1건을 순수 해시 PRNG로 교체해 해결). Browser pane 실측으로 전체 씬 렌더링/Leva 패널 노출/`showThumbnails` 토글 안정성/About Me 위성 공전까지 확인. **다음에 확인 필요**: 이번엔 자동화 클릭 도구가 이 페이지에서 반복적으로 타임아웃(연속 렌더 루프가 있는 WebGL 캔버스와의 상호작용 특성으로 추정)이 나 Leva의 프리셋 배경 전환/컬러 피커/슬라이더/드롭다운 각각의 값 변경, 내핵 오브젝트 실제 클릭→라우팅, 실제 마우스 휠 dolly(EPIC-113부터 이어지는 동일 한계)를 사람이 직접 확인해야 함. 사용자 승인 시 커밋/develop·main fast-forward.
- **EPIC-114(구현 완료, 2026-08-14, 브랜치 `feature/EPIC-114`, EPIC-113 위에 이어서 진행, 아직 main/develop 병합 전)**: "3D 우주 감성 렌더링 강화(유튜브 배경 + 씬 재배치 + 내핵 오브젝트)" — 상세는 CHANGELOG.md 동명 항목 참고. 유튜브 몰입형 배경(8종 중 무작위 자동재생), SILO/유저 행성 동시 등장 + CatmullRomCurve3 연결선 + 앉은 실루엣, maxDistance 120으로 확대, 내핵 4종 형상(보물상자/비너스상/다이얼전화기/필름카메라)+3D Text 라벨, `@react-three/postprocessing`(Noise+Vignette)로 감성 톤. **실측 중 발견해 고친 실제 버그**: `CameraControls`가 Canvas 초기 카메라를 재해석해 의도한 홈 구도와 어긋나던 문제 + 그 보정 effect가 `cameraControlsRef`가 아직 null인 타이밍에 실행돼 무효했던 문제(requestAnimationFrame 재시도로 수정) — 이 과정에서 "행성 내부 진입 시 셸이 사라지고 내핵이 드러난다"는 메커니즘 자체는 Three.js 기본 프런트페이스 컬링으로 이미 올바르게 동작함을 카메라를 SILO 중심 안쪽으로 임시 이동시켜 실제로 확인(보물상자/전화기 2종 스크린샷 확인, 나머지 2종은 동일 코드 경로라 리뷰로 확인). `tsc`/`lint` 0 errors. **다음에 확인 필요(실제 사람 손 마우스 휠 테스트, EPIC-113부터 이어지는 한계)**: 헤드리스 브라우저의 synthetic WheelEvent를 `camera-controls`가 인식하지 않아 실제 휠 dolly 조작감(특히 SILO 내부까지 자연스럽게 줌인되는 체감)은 여전히 자동화로 확인 못함 — `setLookAt` 직접 호출로 "내핵 도달 자체"는 검증했지만 사람이 실제로 휠을 굴렸을 때의 느낌은 별개. maxDistance=120 실제 줌아웃 스케일감, 유튜브 배경 8종 전체 로드 여부(1종만 실측)도 다음에 볼 것. 사용자 승인 시 커밋/develop·main fast-forward.
- **EPIC-113(프로토타입 구현 완료, 2026-08-14, 브랜치 `feature/EPIC-113`, 아직 main/develop 병합 전)**: "/about-silo를 3D 우주(Universe)로 전면 개편" — 상세는 CHANGELOG.md 동명 항목 참고. `three`/`@react-three/fiber`/`@react-three/drei` 신설 도입, 피보나치 구면 배치 + 툰 셰이딩 행성 + 클릭 fly-to + 거리 기반 LOD(궤도 사진↔핵심 카테고리 노드 전환) + 유저 위성. `framer-motion-3d`는 fiber v9와 설치 충돌해 지시문의 대안(`CameraControls`)만 사용. `tsc`/`lint` 0 errors, Browser pane 실측으로 렌더링/클릭-fly-to/요약 패널/리셋/CORS 이미지 자리표시자 대체까지 확인. **다음에 확인 필요(실제 사람 손 마우스 휠 테스트)**: 헤드리스 브라우저의 synthetic WheelEvent를 `camera-controls` 라이브러리가 인식하지 않아(신뢰된 입력만 처리하는 것으로 추정) 휠로 행성 내부 진입 → 카테고리 노드 페이드인 전환을 자동화로 재현하지 못했다 — 코드 리뷰로 로직만 확인, 실제 마우스 스크롤 확인 필요. 그 외 성능(실제 기기에서 프레임 드랍 여부)·대표 이미지 데이터 개수(현재 DB에 사진 있는 게시글이 몇 개인지 미확인)도 다음에 볼 것. 사용자 승인 시 커밋/develop·main fast-forward.
- **EPIC-112(구현 완료, 2026-08-14, 브랜치 `feature/EPIC-112`, 아직 main/develop 병합 전)**: "[Stage 2] Universal JSON Editor 도입(Tiptap + SunEditor Classic UI)" — 상세는 CHANGELOG.md 동명 항목 참고. 데이터 파이프라인(JSON 정본 저장, 구형 HTML 폴백)은 EPIC-053.1부터 이미 구축돼 있었음을 확인, 실제 작업은 `BlockEditor.tsx`의 `Toolbar` 컴포넌트를 SunEditor `buttonList` 그룹핑 문법으로 재배치(기능 삭제 없음, `lucide-react` 아이콘화, undo/redo 버튼 신규 추가)만 진행 — 사용자가 "6버튼 예시는 문법 참고용, 기존 기능은 전부 유지"라고 명시적으로 확인해준 범위. `tsc`/`lint` 0 errors, 클린 dev 서버 기동 확인. **다음에 확인 필요(로그인 세션, 이번 세션은 로그인 자격 증명이 없어 `/write` 접근 시 홈으로 리다이렉트됨만 확인)**: 실제 글쓰기/수정 화면에서 새 아이콘 툴바 그룹/구분선 렌더링, undo/redo 클릭 동작, 표/갤러리/임베드/색상/폰트/출처 삽입이 기존과 동일하게 전부 동작하는지 육안 확인. 사용자 승인 시 커밋/develop·main fast-forward.
- **EPIC-111(구현+검증 완료, 2026-08-14)**: "자유 배치 콘텐츠 사라짐/비율 미적용 근본 원인 수정 + 픽셀 직접 입력" — 상세는 CHANGELOG.md 동명 항목 참고. `EditableBlockFrame`(편집 모드 전용 wrapper)에 `h-full`이 없어 자유 배치 블록의 콘텐츠가 0 높이로 무너지던 근본 원인 수정(슬라이드쇼 위 슬라이드쇼가 안 보이던 문제·리사이즈 "높이(%) 이상함" 둘 다 이 한 가지 원인), `ImageBlock`의 `aspectRatio`/`objectFit`이 실제로 반영되지 않던 별도 버그 수정, "가로세로 비율 직접 입력"을 "픽셀 직접 입력"(너비/높이 숫자 입력)으로 교체. `tsc`/`lint` 0 errors, 실제 관리자 세션에서 4가지 모두 실측 확인(테스트 값은 저장 안 해 DB 영향 없음). **다음에 확인 필요**: 사용자가 이전에 만들어 둔 실제 자유 배치 슬라이드쇼(높이 5%로 찌그러진 상태로 남아있던 것)를 직접 열어 이제 정상적으로 리사이즈되는지, 저장까지 왕복 확인.
- **EPIC-110(구현+검증 완료, 2026-08-14)**: "자유 배치 리사이즈 UX 개선 + 슬라이드쇼/로고/상단 탭 섹션 높이 설정" — 상세는 CHANGELOG.md 동명 항목 참고. `FreePositionHandles`가 선택된 블록에서만 4개 모서리 핸들+테두리를 보여주도록 전면 개편, 숫자 입력 0-스냅 버그 수정, `heroSlideshow.heightVh`/`mainLogo.rowHeightPx`/`topTabStyle.rowHeightPx` 3개 섹션 높이 설정 신설. `tsc`/`lint` 0 errors, 실제 관리자 세션에서 4모서리 리사이즈+선택 게이팅+로고/탭 섹션 높이 저장 반영까지 실측 확인(슬라이드쇼 heightVh는 배선만 코드 리뷰 확인). **다음에 확인 필요**: 슬라이드쇼 섹션 높이를 실제로 저장해 공개 홈페이지에서 육안 확인, 마우스로 직접 드래그하는 실사용 UX(자동화는 pointer 이벤트 직접 디스패치로 검증).
- **EPIC-109(구현+검증 완료, 2026-08-14)**: "자유 배치 이미지 리사이즈에 비율 유지/직접입력/프리셋 추가" — 상세는 CHANGELOG.md 동명 항목 참고. `ImageBlock`에 `lockAspectRatio` 토글+비율 프리셋 드롭다운+직접입력, `FreePositionHandles`가 컨테이너 실제 픽셀 크기로 환산해 비율을 정확히 유지하도록 확장. `tsc`/`lint`/`build` 0 errors, 클린 세션에서 1:1 잠금 리사이즈가 픽셀 단위로 정확함을 확인. **다음에 확인 필요**: 실제 관리자 세션에서 마우스로 직접 드래그해보는 것.
- **EPIC-108(구현+검증 완료, 2026-08-13)**: "자유 배치(콜라주) + 홈페이지 슬라이드쇼 위젯 관리자 연동" — 상세는 CHANGELOG.md 동명 항목 참고. 7종 블록(컨테이너/텍스트/이미지/버튼/영상/슬라이드쇼/게시판연동)에 이미지 위에 이미지처럼 자유롭게 겹쳐 드래그하는 배치 기능 신설(`FreePositionHandles`/`FreePositionSettingsSection`), `HeroSlideshowWidgetBlock`에 관리자 "홈페이지 설정 관리"와의 실시간 연동 토글 추가. `tsc`/`lint`/`build` 0 errors, 임시 진단 라우트로 이미지 위 이미지 겹침·드래그 이동·리사이즈·실제 관리자 슬라이드 데이터 로딩까지 전부 실측 확인. **다음에 확인 필요(관리자 로그인 세션)**: 실제 Craft 에디터에서 핸들을 마우스로 직접 드래그해보는 것(자동화로는 pointer 이벤트 직접 디스패치로 검증, 실제 마우스 UX는 미확인), `syncWithSiteSettings` 저장 후 새로고침해도 유지되는지.
- **EPIC-107(구현+검증 완료, 2026-08-13) — EPIC-101~107 로드맵 완결**: "홈페이지 슬라이드쇼를 Craft 위젯으로 전환" — 상세는 CHANGELOG.md 동명 항목 참고. 신규 `HeroSlideshowWidgetBlock`이 기존 `HeroSlideshow.tsx`를 그대로 감싸 Craft 캔버스 어디든 삽입 가능. `tsc`/`lint`/`build` 0 errors, 임시 진단 라우트로 실제 DOM 구조 일치 확인. **다음에 확인 필요(관리자 로그인 세션)**: Toolbox에서 이 블록을 실제로 드래그해 캔버스에 놓고, 설정 패널에서 슬라이드 이미지 업로드/여백/자동전환 설정 후 저장→공개 페이지 반영까지 왕복 확인. 이것으로 사용자가 요청한 EPIC-101~107 전체 로드맵이 완료됨 — 다음 작업은 사용자 지시 대기.
- **EPIC-106(구현+검증 완료, 2026-08-13)**: "게시판 연동 블록 카테고리 필터" — 상세는 CHANGELOG.md 동명 항목 참고. `GET /api/boards/[board_slug]/posts`에 `category` 파라미터+`availableCategories` 응답 추가, `useBoardPosts.ts` 4번째 인자로 노출, `BoardEmbedBlock`/`TimelineEmbedBlock` 설정 패널에 카테고리 드롭다운 신설. `tsc`/`lint`/`build` 0 errors, 존재하지 않는 카테고리로 필터링 시 0건 확인(파라미터 실제 적용 검증). **다음에 확인 필요**: 라이브 DB에 `posts.category`를 채운 실제 글이 아직 없어 양성 매치(실제 카테고리로 필터링 → 그 카테고리 글만 나옴) 케이스는 데이터가 생긴 뒤 확인 필요.
- **EPIC-105(구현+검증 완료, 2026-08-13)**: "하단 Footer를 Craft.js 비주얼 에디터로 전환" — 상세는 CHANGELOG.md 동명 항목 참고. 신규 Craft 페밀리(`FooterCompanyInfoBlock`/`FooterLinksRowBlock`), `GlobalFooter.tsx`(신규 Server Component)로 `Footer.tsx` 대체(삭제), `/admin/footer` 전면 재작성. 기존 `site_settings.footer_config` 실데이터를 `footerDefaultTree` 초깃값으로 마이그레이션, `page_builder(slug='footer')` 행 신규 생성. `tsc`/`lint`/`build` 0 errors, 클린 서버 재시작 후 홈/`/shop` 양쪽에서 실렌더링 확인. **다음에 확인 필요(관리자 로그인 세션)**: `/admin/footer`에서 실제로 Craft 에디터가 열리고 저장이 되는지, 링크 행 추가/삭제 UI 왕복.
- **EPIC-104(구현+검증 완료, 2026-08-13)**: "헤더 스크롤 모션" — 상세는 CHANGELOG.md 동명 항목 참고. `useHideOnScroll.ts` 신설, `Navbar.tsx`를 fixed+transform으로 전환(LeftSidebar/RightSidebar는 별도 형제로 분리해 좌표계 안 깨짐), 개발 중 실제로 겪은 ResizeObserver 되먹임 루프(메뉴 전체가 영원히 로딩 상태로 멈추는 버그) 원인 특정+수정. `tsc`/`lint`/`build` 0 errors, 로컬 dev에서 scrollTo 시퀀스로 hide/show 로직 정확성 확인 + 사이드바 열림 시 z-index stacking(헤더<백드롭<사이드바) 확인. 사용자 승인 시 커밋/push/develop·main fast-forward.
- **EPIC-103(구현 완료, 2026-08-13)**: "Kinfolk 16블록 홈 레이아웃" — 상세는 CHANGELOG.md 동명 항목 참고. `tsc`/`lint`/`build` 0 errors, 로컬 진단 라우트로 16블록 전체 실데이터 렌더링 확인. **아직 라이브에 반영 안 됨** — 홈페이지에 이미 저장된 `craft_state`가 있어 기본 트리 변경만으로는 실제 사이트가 안 바뀐다. 다음 확인 필요: (1) `page_builder.craft_state`를 null로 초기화해 이 16블록 레이아웃을 실제로 노출할지 사용자 결정, (2) 게시판을 참조하는 블록(3/6/9/14/15/16)에 실제 게시판 연결(관리자 세션 필요), (3) 플레이스홀더 이미지/텍스트를 실제 화보/카피로 교체.
- **EPIC-102(구현 완료, 2026-08-13)**: "Craft.js 프리폼 에디터 코어" — 상세는 CHANGELOG.md 동명 항목 참고. 원자 블록 8종(Container/Text/Image/Button/Video/Slideshow/BoardEmbed/TimelineEmbed) + 설정 패널(`craft.related`, 이 코드베이스 최초 도입) + 공통 모션(fade/slide-up/slide-left) + 진짜 드래그 툴박스(`connectors.create`) + 홈페이지 에디터를 공용 셸로 마이그레이션. `tsc`/`lint`/`build` 0 errors, 로컬 dev 공개 홈페이지 무회귀 확인. **다음 세션에서 확인 필요(관리자 로그인 세션 없어 미확인)**: Toolbox에서 실제로 컨테이너를 캔버스로 드래그 → 그 안에 텍스트/이미지/버튼 드래그 → 우측 설정 패널에서 값 변경 → 모션(fade 등) 설정 → 저장 → 공개 페이지에 반영되고 스크롤 시 모션이 실제로 재생되는지 왕복 확인, 기존 6개 섹션 블록이 여전히 정상 동작하는지(레거시 `craft_state` 호환) 확인. 사용자 승인 시 커밋/push/develop·main fast-forward.
- **EPIC-101(구현+검증 완료, 2026-08-13)**: "홈페이지 Craft.js 이미지 크기 버그 긴급 수정" — 상세는 CHANGELOG.md 동명 항목 참고. `CraftHomeRenderer.tsx`에 `@container` 클래스 한 줄 추가, 로컬 dev에서 그리드 열 수 정상화 확인 완료. 커밋(`830caa5`) 완료, develop/main fast-forward push 완료.
- **다음 예정(EPIC-103~107, 계획 파일 `C:\Users\nasdo\.claude\plans\fancy-mapping-eich.md` 참고)**: EPIC-103 Kinfolk 16블록 홈 레이아웃(EPIC-102 프리미티브 조합), EPIC-104 헤더 스크롤 모션(hide on scroll down/show on scroll up, 이 레포 전례 없음), EPIC-105 `/admin/footer` → Craft.js 비주얼 에디터 전환(기존 `footer_config` 데이터 마이그레이션 필요), EPIC-106 `BoardEmbedBlock` 카테고리 필터(`GET /api/boards/[board_slug]/posts`에 `?category=` 파라미터 추가 필요, 현재 없음), EPIC-107 `HeroSlideshow`/`heroSlideshow.ts` 위젯화(`craft_hero_slideshow`).
- **EPIC-100(구현 완료, 2026-08-13, 브랜치 `feature/EPIC-100`, 아직 main/develop 병합 전)**: "Craft.js 에디토리얼 레이아웃 모바일 반응형 교정 + LCP 최적화 + 관리자 PC/모바일 반응형 빌더 모드" — 상세는 CHANGELOG.md 동명 항목 참고. **중요한 설계 결정**: 지시문의 `md:`/`lg:` 뷰포트 유틸리티를 그대로 썼다면 관리자 PC/모바일 토글(항목 3)이 실제로는 레이아웃에 아무 영향을 못 줬을 것(뷰포트 미디어쿼리는 브라우저 창 크기 기준이지 미리보기 캔버스 div 너비 기준이 아님)이라는 걸 설계 중 발견해, Tailwind v4 컨테이너 쿼리(`@container`+`@[768px]:`)로 대체 — 공개 페이지에서는 기존과 동일하게 동작하면서 관리자 토글에서는 실시간으로 재계산됨을 실측 확인. `EditableImage`/`EditableResponsiveImage`에 `priority` prop 추가(히어로 6종만 우선 로드, 나머지 전부 lazy). `tsc`/`lint` 0 errors. **검증 완료**: 공개 페이지 이미지 속성(`fetchpriority`/`loading`) DOM 확인 + 무회귀(전체 너비에서 기존과 동일한 열 수), 관리자 에디터 모바일 모드 토글로 `/silo-store` 그리드/스플릿 실시간 재배치 확인, 실제 375px 좁은 뷰포트(`resize_window`)로 재접속해서도 동일하게 재배치되는 것 확인(컨테이너 쿼리가 실제 모바일 기기에서도 뷰포트 쿼리와 동등하게 동작함을 증명), 콘솔 에러 없음. **다음에 확인 필요**: 나머지 4개 허브 페이지(온라인 도슨트/살롱데상/스튜디오/마이 페이지)와 홈페이지도 같은 코드 패턴이라 회귀 가능성은 낮지만 육안 확인은 못함(`/silo-store` 위주로 실측). 사용자 승인 시 커밋/develop·main fast-forward.
- **EPIC-099(전체 완료, 2026-08-13, 브랜치 `feature/EPIC-099`, main/develop 병합 완료, docs/EPIC.md·docs/PROJECT_DASHBOARD.md·docs/STAGES.md 동기화 완료)**: EPIC-098에서 미룬 3개 후속(제안서 기준) 전부 완료 — **1/3** 뉴스레터 실제 구독 저장, **2/3** Craft 에디터 Toolbox UI(복제/삭제/드래그 재정렬/"+ 섹션 추가"), **3/3** 다른 허브 페이지로 Craft 확장(**Phase 1** 사이트 구성 관리 토글, **Phase 2** 5개 허브 페이지 전용 Craft 블록 각 3종 + 공용 셸 일반화, **Phase 3** 공용 Craft 블록 4종을 Native Page Builder 위젯 타입(`craft_hero`/`craft_directory`/`craft_newsletter`/`craft_footer`)으로도 노출 — 사용자 확인 후 "공용 블록 4종만"으로 범위 확정, 페이지 전용 블록 21종은 범위 밖). 상세는 CHANGELOG.md 각 Phase 항목과 docs/EPIC.md 참고. **재사용 가능한 발견사항(다음 EPIC에서 참고)**: (1) 정적 라우트 파일이 있는 페이지(`/studio`/`/mypage` 등, 홈페이지와 같은 유형)는 catch-all의 registry 등록만으로는 Craft가 적용되지 않는다 — 각 정적 라우트 파일에 직접 `builder_type`/`craft_state` 분기를 추가해야 함. (2) Craft 블록은 `useNode()`를 쓰므로 `<Editor><Frame>` 컨텍스트 밖에서 못 그린다 — Native 위젯 안에 하나만 심을 땐 `CraftWidgetShell`(단일 블록용 최소 Editor+Frame) 패턴 재사용. `tsc`/`lint` 매 단계 0 errors. **검증 완료**: 5개 허브 페이지 공개 렌더링(6섹션)/관리자 Craft 에디터(`/studio`)/Native 페이지("About Silo")에 Craft 위젯 추가→미리보기→Inspector 폼까지 로컬 dev 실측, 콘솔 에러 없음(테스트로 추가한 위젯은 정리 완료). **남은 것**: Toolbox 드래그 핸들(⠿)의 실제 순서 재정렬은 자동화 브라우저로 마우스 드래그 재현이 어려워 코드 리뷰로만 확인함 — 관리자가 직접 드래그해서 최종 확인 권장. `docs/EPIC.md`의 EPIC-090~097 구간 소급 작성이 밀려 있음(이번 세션 범위 밖, `docs/PROJECT_DASHBOARD.md` 각주에 기록).
- **EPIC-098/EPIC-098 후속(완료, main/develop 병합 완료)**: "홈페이지 전용 Craft.js 에디토리얼 빌더(Two-Track)" + "PC·모바일 이미지 분리" — 상세는 CHANGELOG.md 동명 항목들 참고.
- **EPIC-097 후속(구현 완료, 2026-08-13, 브랜치 `feature/EPIC-097`, 아직 main/develop에 병합 전)**: "타임라인 항목을 기존 게시글에서 가져오기" — 상세는 CHANGELOG.md 동명 항목 참고. `TimelinePostPicker` 신설(게시판 선택 + 검색 + 썸네일 미리보기 목록), 선택한 게시글의 썸네일/제목/본문 요약/링크를 항목에 한 번에 채우는 "가져오기" 버튼 추가, `TimelineDescriptionEditor`의 외부 content 변경 미반영 버그 수정. `tsc`/`lint` 0 errors. **다음 세션에서 확인 필요(관리자 로그인 세션)**: 실제로 게시글 검색·선택 → 필드 자동 채움 → 저장 → 공개 페이지 반영까지 실제 확인, 사용자 승인 시 커밋/develop·main fast-forward.
- **EPIC-097(완료, 2026-08-13, main/develop 병합됨, 브라우저로 실사용 검증 완료)**: "타임라인 위젯 — 수동 항목 입력 + 프로 레벨 2단 Split 편집기" — 상세는 CHANGELOG.md 동명 항목 참고. timeline 위젯을 board_id 연동에서 운영자가 직접 입력하는 항목 목록(`settings.items`)으로 전환, `TimelineWidgetEditor`(dnd-kit 드래그 정렬 + 아코디언 + Tiptap 미니 에디터 + 이미지 업로드)와 `AlternatingTimelineCanvas`(프리뷰/공개 렌더링 공용) 신설. 게시판 타임라인(`TimelineView.tsx`)의 "연대 선이 끊긴다" 신고도 함께 수정. dev.silostore.net에서 항목 추가/실시간 프리뷰/저장/공개 페이지 반영까지 실제 확인 완료.
- **EPIC-096(전 항목 완료 + 실사용 신고 5건 후속 수정, 2026-08-12, 브랜치 `feature/EPIC-096`, main/develop 병합됨)**: "럭셔리 리마스터링" — 상세는 CHANGELOG.md 동명 항목들(1차~5차 + 후속 1~5차) 참고. 7개 요구사항 순차 완료 후, 사용자가 실제 배포(dev.silostore.net)에서 써보고 신고한 문제들을 순서대로 수정: **후속 1차** — 전체 글 관리 P0(posts/boards embed 관계 모호성) + About Silo/온라인 도슨트 도메인 탭 누락(4→6 확장). **후속 2차** — 탭 바/표 가로 스크롤 제거, 게시판 이동 select를 사이트 메뉴 트리 순서로 정렬. **후속 3차** — "게시물 출력방식" 버튼 목적지 버그 수정, 갤러리 썸네일 최대 크기(px) 필드, "전체 게시판 기본값으로 저장" 기능. **후속 4차** — "게시물 출력방식"에 수정일 숨기기 토글 + 글번호/날짜/작성자/제목 폰트 지정 + 좋아요·조회·댓글 크기/색상/폰트 스타일 신설. **후속 5차** — 4차의 폰트 필드가 자유 텍스트 입력이라 실제 효과가 없다는 신고로, 기존 커스텀 폰트 업로드 인프라(EPIC-083)를 재사용한 `FontPicker`(실제 폰트로 렌더링되는 미리보기 + 그 자리에서 바로 업로드)로 전면 교체. `tsc`/`lint` 매 단계 0 errors. **다음 세션에서 확인 필요(로그인 세션)**: 후속 2~5차 전부 관리자 화면이라 이번엔 육안 확인 못함(후속 1차만 REST API 직접 검증 완료) — 새로고침 후 실제로 확인 필요, 특히 5차는 실제 폰트 업로드→선택→게시글 반영 왕복. 그 외 남은 것은 이전 항목과 동일(저장/투표 등 실제 쓰기 왕복).
- **EPIC-095(완료, 2026-08-12, main/develop 병합됨)**: "Data Drift 재동기화 + Frictionless Collection UX" — 상세는 CHANGELOG.md 동명 항목 참고. (1) 라이브 `site_navigations` 재조사 결과 "온라인 도슨트"는 이미 최상위 탭이었음을 확인(예상과 반대 방향 드리프트), `key='docent'`만 사용자 승인 후 채움(단일 컬럼) + `FALLBACK_NAV_TABS`/`docs/database-schema.sql` 시드를 라이브 그대로 재생성. (2) `POST /api/collections` + `CollectButton`으로 게시글 상세/도슨트 뷰어에 "내 컬렉션에 담기" 원클릭(카테고리 팝오버) 부착. **다음에 확인 필요**: 실제 브라우저로 "온라인 도슨트" 탭 표시/하이라이트, "내 컬렉션에 담기" 클릭→카테고리 선택→`/mypage` "나의 컬렉션" 반영 확인.
- **EPIC-094(구현 완료, 2026-08-12, 브랜치 `feature/EPIC-094`, 아직 main/develop에 병합 전)**: "[Stage 3] S-Class PC/Mobile Architecture & Real-Time Preview" — 상세는 CHANGELOG.md 동명 항목 참고. (1) `GET /api/homepage`(+`?device=app`) Headless API 신설, (2) `HeroSlideshow.tsx`의 슬라이드 이미지를 `<picture>`로 감싸 반대쪽 기기 media에서는 투명 픽셀만 받도록 해 EPIC-092부터 있던 PC/모바일 분리 이미지가 실제로 "완전히 다른 파일"로만 내려가도록 수정, (3) "홈페이지 설정 관리"에 실시간 모바일 프리뷰어(아이폰 프레임, `MobilePreviewFrame.tsx`) 추가. `tsc`/`lint` 0 errors, 로컬 dev에서 데스크톱/모바일 뷰포트로 `<picture>` 소스 스위칭 실제 확인. **다음 세션에서 확인 필요(관리자 로그인 세션)**: `/admin/navigation/settings` 우측 실시간 프리뷰 패널 육안 확인, 사용자 승인 시 커밋/develop·main fast-forward.
- **HOTFIX-102/103(완료, 2026-08-12)**: (1) "이 게시판을 사용 중인 페이지"에서 직접 추가/삭제(`page_modules` insert/delete). (2) 타임라인 "타임라인 설정"에 선 굵기/마커 크기/카드 테마(라이트·다크) 추가, 기본 스타일도 더 뚜렷하게(선 2px, 마커 14px, `rounded-xl shadow-xl`). 상세는 CHANGELOG.md 동명 항목 참고. `tsc`/`lint` 0 errors, 로컬 dev에서 다크 테마/선 굵기/마커 크기 DOM으로 확인(테스트 값은 원상복구). **다음에 확인 필요(관리자 로그인 세션)**: `/admin/boards/[id]`에서 "이 게시판을 사용 중인 페이지" 추가/삭제 버튼 실제 클릭, 다크 테마를 실제로 저장한 뒤 화면에서 육안 확인.
- **HOTFIX-101(완료, 2026-08-12)**: "추가로 노출할 게시판 선택"을 카테고리 가나다순(HOTFIX-099) 대신 "사이트 구성 관리 → 사이트 메뉴"와 동일한 실제 트리 순서로 정렬(`buildAdminTree()` 재사용). 상세는 CHANGELOG.md 동명 항목 참고. `tsc`/`lint` 0 errors, 로컬 dev에서 실제 사이트 메뉴 순서와 일치하는 것 DOM으로 확인. 같은 세션에서 "글 번호/작성자 이름 스타일"이 이미 `/admin/boards/[id]` → "게시물 출력방식"에 정상 배포돼 있음을 관리자 세션으로 재확인(코드 변경 없음).
- **HOTFIX-100(완료, 2026-08-12)**: (1) HOTFIX-099의 관리자 작성자 변경이 "permission denied for table posts"로 실패하던 DB 권한 문제 수정(`posts.author_id`에 `authenticated` UPDATE GRANT 누락 — 실행 완료). (2) 타임라인 게시판별 컬러 테마(`timelineAccentColorHex`). (3) 정렬(왼쪽/가운데/오른쪽)에 세로형 선/마커 위치도 함께 따라가도록 수정. 상세는 CHANGELOG.md 동명 항목 참고. `tsc`/`lint` 0 errors, 로컬 dev에서 색상 반영 + 마커 중앙 이동 `getComputedStyle`/`getBoundingClientRect()`로 확인(테스트 값은 원상복구). **다음에 확인 필요(관리자 로그인 세션)**: 실제로 작성자 변경 저장이 이제 성공하는지, "타임라인 설정"에서 색상 저장 왕복, 오른쪽 정렬 시 마커/미리보기 카드가 실제로 오른쪽으로 잘 뒤집히는지(이번엔 가운데 정렬만 실측, 오른쪽은 코드 리뷰로만 확인).
- **HOTFIX-099(완료, 2026-08-12)**: (1) 관리자가 게시글 작성자를 다른 회원으로 검색해 바꿀 수 있는 기능(`edit/page.tsx`, `PostForm.tsx`의 `AdminAuthorPicker`, `PATCH .../posts/[post_slug]`의 `authorId` 처리). (2) "게시물 출력방식"에 글 번호/작성자 이름 크기·색상 필드 4개 추가(`PostDetailHeader.tsx`, `BoardForm.tsx`). (3) "추가로 노출할 게시판 선택"을 카테고리별로 그룹 정렬(`groupBoardsByCategory()`). 상세는 CHANGELOG.md 동명 항목 참고. `tsc`/`lint` 0 errors, 로컬 dev에서 카테고리 그룹핑 DOM 확인. **다음에 확인 필요(관리자 로그인 세션)**: 작성자 검색 자동완성 실제 클릭 → 저장 → 게시글 상세에 새 작성자로 표시되는지, 글 번호/작성자 이름 스타일 저장 왕복.
- **HOTFIX-098(완료, 2026-08-12)**: "타임라인 정렬을 가운데로 했는데 아무것도 안 바뀌어" 신고 수정 — `boards.widget_settings.postMetaStyle`(HOTFIX-093-B)이 게시글 상세에만 적용되고 타임라인 라벨/미리보기에는 반영 안 되던 것을 고침. 상세는 CHANGELOG.md 동명 항목 참고. `tsc`/`lint` 0 errors, 로컬 dev에서 실제 저장돼 있던 `position:"center"` 값이 반영되는 것(`getComputedStyle().textAlign`) 확인. **범위 밖으로 남김**: Story/Community/Gallery 등 다른 목록 Renderer도 같은 이유로 이 설정을 무시 — 이번엔 사용자가 신고한 타임라인만 고침, 필요시 확장 검토. **다음에 확인 필요(관리자 로그인 세션)**: `/admin/boards/[id]`에서 정렬/크기/색상/굵기 값을 바꿔가며 타임라인 화면에 실시간 반영되는지 최종 확인.
- **HOTFIX-097(완료+배포 후 실사용 검증까지 완료, 2026-08-12)**: (1) 타임라인 게시글 클릭 시 "게시글을 찾을 수 없어요" 오류 수정(post_boards cross-post 상세 조회 미반영이 원인) — `dev.silostore.net`에서 실제로 클릭해 정상 열리는 것 확인. (2) 타임라인을 Common Ninja 스타일로 재설계 — hover 시 썸네일+본문 일부+날짜+메타 미리보기 카드. (3) 가로형/세로형 둘 다 지원(`widget_settings.timelineOrientation`). (4) `/admin/boards/[id]`(및 new)에 "타임라인 설정" 섹션 추가(배치 방향/미리보기 카드 on-off). 상세는 CHANGELOG.md 동명 항목 참고. **후속 수정(같은 날, 배포 후 실사용 검증 중 발견)**: 세로형 미리보기 카드가 라벨 옆이 아니라 행 전체 폭 기준 오른쪽 끝(화면 밖일 수도 있음)에 뜨던 위치 버그 수정 — `getBoundingClientRect()`로 라벨-카드 간격이 정확히 12px임을 확인. `tsc`/`lint` 0 errors. **다음에 확인 필요(관리자 로그인 세션)**: `/admin/boards/[id]`에서 "타임라인 설정" 저장 왕복, 마이페이지 "타임라인" 탭(활동 로그, renderPreview 없음)이 회귀 없이 그대로 보이는지.
- **HOTFIX-096(완료, 2026-08-11)**: 상단 GNB 메가메뉴에서 하위 항목 없는 그룹("Silo's old Story")에도 "›" 화살표와 hover 시 빈 플라이아웃이 뜨던 문제 수정 — 상세는 CHANGELOG.md 동명 항목 참고. `Navbar.tsx`의 `tab.groups` 렌더링을 `group.items.length > 0`일 때만 chevron/플라이아웃을 그리도록 수정. `tsc`/`lint` 0 errors, 로컬 dev DOM으로 확인(자식 있는 그룹은 그대로 `›` 유지, 자식 없는 그룹만 사라짐). **다음에 확인 필요**: `dev.silostore.net` 배포 후 실제 hover로 최종 확인.
- **HOTFIX-095 후속(완료+배포 후 실사용 검증까지 완료, 2026-08-11)**: 위 HOTFIX-095 1번을 관리자 로그인 세션(사용자 직접 로그인, dev.silostore.net)으로 검증하다 발견한 P1 — HOTFIX-093-B의 `post_boards`(N:M) 테이블 때문에 `posts`→`boards` 관계가 모호해져(PGRST201) `ScrapsPanel.tsx`/`CommentsPanel.tsx`의 select가 항상 조용히 실패하고 빈 배열로 폴백되던 것을 수정 — 상세는 CHANGELOG.md 동명 항목 참고. `boards(...)`를 `boards!posts_board_id_fkey(...)`로 명시. **배포 후 최종 확인 완료**: `dev.silostore.net` 재배포(신규 Vercel deployment id 확인) 후 `/mypage/archive`(스크랩 2건: `/boards/silo-daily/2020-9-5-sumi`, `/boards/baroque/la-primavera-concerto-in-e-major`)와 `/mypage/comments`(댓글 2건, `/boards/baroque/...` 링크)에서 실제로 항목이 뜨고 링크가 slug 기반으로 깨끗한 것까지 확인.
- **HOTFIX-095(완료, 2026-08-11, 브랜치 `feature/HOTFIX-095`)**: (1) 마이페이지 스크랩/댓글 패널이 게시글 링크에 `board_id`(원시 UUID)를 그대로 쓰던 문제 수정(`boards.slug` 사용하도록) — 상세는 CHANGELOG.md 동명 항목 참고. (2) "사이트 구성 관리" 사이트 메뉴 트리 기본 상태를 전부 접힘으로 변경. (3) 갤러리 게시판 썸네일 기본 배치를 가로(Grid)로 변경. `tsc`/`lint` 0 errors. **관리자 로그인 세션으로 실사용 검증 완료(2026-08-11, 사용자 직접 로그인)**: (1) "그리스" 보드에서 스크랩 → `/mypage/archive` 링크가 `/boards/silo-daily/2020-9-5-sumi`처럼 깨끗하게 뜨는 것 확인(단, 이 검증 도중 HOTFIX-095 후속 항목의 P1을 발견해 별도로 수정함). (2) `/admin/site-structure` 첫 진입 시 최상위 탭 전부 `▶`(접힘) 상태로 뜨는 것 확인. (3) `/boards/silo-daily`(갤러리, `galleryColumns:4`)가 4열 그리드로 렌더링되는 것 DOM에서 확인.
- **HOTFIX-094(완료, 2026-08-11, 브랜치 `feature/HOTFIX-091`)**: 게시판 목록 페이지(`/boards/[board_slug]`)의 "페이지 수정" 버튼/위젯 영역이 모든 게시판에서 공용 placeholder(`"boards-id"`)만 가리키던 문제 수정 — 상세는 CHANGELOG.md 동명 항목 참고. `getBoardPageSlug()`(`src/lib/siteTree.ts`)로 게시판별 전용 `page_builder` 페이지를 실제로 조회하도록 변경, 전용 페이지가 없는(사이트 메뉴에 아직 안 배정된) 게시판은 버튼을 아예 숨김. `tsc`/`lint` 0 errors. **관리자 로그인 세션으로 실사용 검증 완료(2026-08-11)**: `/boards/greeks`에서 "페이지 수정" 클릭 → `/admin/pages/a6aca889-...`(그리스 전용 페이지, Board 위젯이 실제로 "그리스"에 연결됨)로 정확히 이동하는 것 확인. **범위 밖으로 남김**: 글쓰기 폼(`"boards-id-write"`)/게시글 상세(`"boards-id-postid"`)의 동일 패턴은 이번에 안 건드림.
- **HOTFIX-093-B(구현+DB 실행 완료, 2026-08-11, 브랜치 `feature/HOTFIX-093-B`)**: 상세는 CHANGELOG.md 동명 항목 참고. **완료**: PageEditButton 아이콘 겹침 근본 원인 확인+동적 회피 로직, 캘린더 위젯 필터/글쓰기 버튼 무조건 노출(CalendarFromSettings가 board_id 없어도 CalendarBoardWidget 사용), 게시글 다중 게시판(N:M) 스키마+API+에디터 체크박스 UI, 게시물 메타데이터(날짜/작성자) 스타일링 UI. **`docs/sql/HOTFIX-093-B-post-boards-multi.sql` 실행 완료(2026-08-11, 사용자가 Supabase SQL Editor에서 직접 실행)**: Management API로 재조회해 `post_boards` 테이블 및 RLS 정책 3개(select/insert/delete) 전부 생성 확인 — 다중 게시판 기능이 이제 실제로 동작한다. **못 찾음**: "페이지 수정" 라우팅이 "엉뚱한 페이지로 간다"는 신고 — 재조사했지만 코드 결함을 못 찾음, 여러 게시판이 같은 위젯 템플릿(page_builder slug 공유)을 쓰는 EPIC-067의 의도된 설계가 원인일 가능성. **재확인**: 1.1 Clean URL은 이미 EPIC-079-PHASE-2 때부터 slug 기반 폴더/쿼리 구조였음을 코드 인용으로 재확인(요청서가 지시한 "폐기해야 할 UUID 구조"는 현재 코드베이스에 없음). **다음 세션에서 확인 필요(관리자 로그인 세션)**: 사이드바 겹침이 실제로 해소됐는지, "게시물 출력방식" 저장 왕복, 다중 게시판 체크박스로 저장한 글이 실제로 양쪽 게시판 목록/캘린더에 노출되는지 실사용 클릭 검증. `tsc`/`lint` 0 errors.
- **EPIC-093(1차, 부분 완료, 2026-08-11, 브랜치 `feature/EPIC-093`)**: "[Critical Fixes] Layout Corrections, Clean URLs, & Calendar Widget Completion" — 상세는 CHANGELOG.md 동명 항목 참고. **완료**: 1.4 임시 게시판 카테고리(`new-board-xxxxxxxx`)가 태그로 노출되던 문제 프론트엔드 수정(`isRealBoardCategory`), 1.2 새 페이지(catch-all) 기본 컨테이너 좌우 여백 제거, 1.1 게시글 상세 버튼 라벨을 "게시물 출력방식"으로 변경. **DB 실행 대기**: `docs/sql/EPIC-093-cleanup-placeholder-board-category.sql` — 세션 안전 classifier가 자동 UPDATE 실행을 막아 사용자가 직접 실행해야 함(SELECT로 8개 대상 게시판 확인 완료, slug 충돌 없음 확인 완료). 실행되면 `/boards/new-board-*` 형태의 지저분한 URL 8개가 이름 기반 slug로 즉시 바뀐다(1.3의 일부). **못 찾음/재현 못함**: "페이지 수정 버튼이 엉뚱한 페이지로 간다"는 1.1의 라우팅 버그 — 관련 코드(`PageEditButton.tsx`, `/admin/pages/[id]/page.tsx`)는 정상으로 보이고, 공용 slug(`"boards-id"`/`"boards-id-postid"`)로 여러 게시판이 같은 위젯 템플릿을 공유하는 건 EPIC-067의 의도된 설계 — **다음 세션에서 사용자가 클릭한 정확한 URL과 실제 도착한 URL을 받아 재현할 것**. **범위 밖으로 남김(다음 EPIC 권장)**: 1.3의 게시글 URL 전면 개편(요청서 스크린샷의 `/board/<uuid>/...` 레거시 경로 전수 조사 포함) 및 게시글-게시판 N:M 다중 소속 관계(스키마 변경 필요), 1.4의 메타데이터(날짜/작성자) 커스텀 스타일링 UI, 1.5 캘린더 위젯은 코드 재검토 결과 EPIC-092에서 이미 구현 완료 상태로 확인됨(+ 글 등록/4개 필터/호버 확대/미디어 슬라이더 전부 존재, 위젯 공통 ⚙ 버튼도 이미 있음) — **관리자 로그인 세션으로 실제 클릭 검증만 남음**(코드 변경 없음). `tsc`/`lint` 0 errors. 커밋/main·develop fast-forward는 사용자 확인 후 진행.
- **EPIC-092 후속(같은 날) — 여백에 여백 배경 이미지가 안 보이는 문제 수정**: 여백(px) 자체는 정상 반영 중이었는데, 사용자가 원한 건 그 여백 공간에도 슬라이드쇼의 여백 배경 이미지가 이어져 보이는 것 — `HeroSlideshow.tsx`를 margin 대신 padding + 바깥 박스 배경으로 재구성해 해결. `tsc`/`lint` 0 errors, 로컬 dev에서 padding 영역에 배경 이미지가 이어지는 것 스크린샷+DOM 확인. **다음에 확인 필요**: `dev.silostore.net` 재배포 후 최종 확인.
- **EPIC-092 후속(같은 날) — 슬라이드쇼 여백이 저장해도 반영 안 되는 문제 수정**: Management API로 DB 값(`marginTopPx:10`)은 정확히 저장됐는데 실제 사이트 DOM은 `margin-top:0`이던 근본 원인을 직접 확인 — `src/app/page.tsx`(홈페이지)에 `export const dynamic = "force-dynamic"`이 빠져 있어 Next.js가 `site_settings` 조회를 캐싱하고 있었다(`PROJECT_RULES.md`가 이미 명시한 관례를 홈페이지만 놓침). 추가해서 수정 완료. `tsc`/`lint` 0 errors. **다음에 확인 필요**: `dev.silostore.net` 재배포 후 여백이 실제로 반영되는지 최종 확인.
- **EPIC-092 후속(같은 날) — 갤러리 배치 방향/줄당 개수 + 썸네일 호버 미리보기**: `/about-silo/silo-story` 예시로 요청 — 갤러리형 보드(`GalleryModule.tsx`)에 masonry/grid 배치 전환 + 줄당 개수(2~6) 설정(`boards.widget_settings`, `BoardForm.tsx`에 UI 추가), 썸네일 hover 시 본문 갤러리 영상 재생 + 이미지 미리보기(목록 API에 `body_json` 추가, `extractCalendarMedia` 재사용). **후속 수정(사용자 피드백)**: 이미지 슬라이드는 기본 좌우 화살표로 직접 넘기고, 자동 전환은 게시판별 옵션(`galleryHoverAutoSlide`, 기본 꺼짐)으로 변경 — 영상 자동재생은 그대로 유지. 우선 갤러리형 보드부터 적용(스토리/슬라이드/타임라인은 범위 밖). `tsc`/`lint` 0 errors, 로컬 dev(비로그인)에서 hover 미리보기+화살표 클릭 네비게이션 실제 동작 확인(콘솔 에러 없음). **다음에 확인 필요(관리자 로그인 세션)**: `BoardForm.tsx`의 새 설정 3종 저장 왕복, grid 레이아웃 실제 화면, 영상 포함 게시글의 hover 자동재생, 자동 슬라이드 옵션 on/off 동작.
- **EPIC-092 후속(같은 날) — 등록 날짜/시간 저장 실패 수정**: `posts.created_at`에 `authenticated` UPDATE 권한이 없어(한 번도 UPDATE된 적 없던 컬럼) 저장이 항상 실패하던 근본 원인을 Management API로 확인·수정(`docs/sql/EPIC-092-created-at-update-grant.sql` 실행 완료, 라이브 반영 확인). 오류 메시지도 `detail`(실제 DB 에러)까지 화면에 보이도록 개선. **다음에 확인 필요**: 실제 관리자 로그인 후 등록 날짜 변경 → 저장 성공 확인.
- **EPIC-092(코드 완료, main/develop에 병합됨, 브랜치 `feature/EPIC-092`)**: "[Stage 2] Advanced Widget UX, Inline Editing, & Contextual Navigation" — 상세는 CHANGELOG.md 동명 항목 참고. `tsc`/`lint` 0 errors. **후속(같은 날, 사용자가 관리자 로그인 스크린샷으로 확인 후 요청)**: 게시글 상세 페이지만 "페이지 수정" 버튼을 본문 상단에 인라인으로 예외 배치하던 것을 제거 — 이제 모든 페이지가 완전히 동일한 위치/스타일(헤더 바로 아래 좌측 상단 고정, `#166534`)을 쓴다(`PostDetailClient.tsx`, 텍스트만 "게시글 보여지는 방식 수정"으로 계속 분기). **다음에 확인 필요(관리자 로그인 세션 필요)**: (1) `PostForm.tsx`의 관리자 전용 "등록 날짜/시간" 필드 저장 왕복(생성/수정 양쪽), (2) `PageBuilderRenderer`의 위젯별 ⚙ 버튼 → `/admin/pages/[id]?module=` 딥링크로 Inspector가 자동으로 열리는지, (3) 캘린더 위젯을 실제 `board_id`와 연결해 hover 확대 팝아웃/미디어 슬라이더/4개 태그 체크박스 필터("나의 달력" 포함)/"+ 글 등록" → 날짜 pre-fill 전체 플로우, (4) "홈페이지 설정 관리"의 PC/모바일 탭 전환 저장이 실제로 `site_settings.hero_slideshow`에 `{pc,mobile}` 모양으로 반영되는지, 여백(px) 입력 반영 여부.
- **HOTFIX-091(완료, 브랜치 `feature/HOTFIX-091`)**: 게시글 수정 시 "게시글을 찾을 수 없어요" 오류 수정 — 상세는 CHANGELOG.md 동명 항목 참고. `tsc`/`lint` 0 errors, 실제 API 시퀀스로 버그 재현 후 수정 확인까지 완료. **다음에 확인 필요**: 실제 에디터 UI(브라우저 폼)로 카테고리를 바꾼 뒤 저장 → 다시 수정 화면 진입 → 재저장까지 사람이 직접 클릭해서 최종 한 번 더 확인 권장(이번엔 API 레벨 시뮬레이션으로 검증, UI 클릭 경로 자체는 미확인).
- **HOTFIX-090(구현+후속 3건 전부 완료, 브랜치 `feature/HOTFIX-090`)**: 위젯 설정 내 "게시판 추가" 버튼 위치 수정 + 후속으로 "그리스" 게시판 생성/연결, 게시판 선택 옵션 실시간 연동(탭 focus 시 재조회), 정렬을 사이트 메뉴 순서와 일치시킴 — 상세는 CHANGELOG.md 동명 항목 참고. `tsc`/`lint` 0 errors, 로컬 dev 로그인 세션 실측 검증 완료. **데이터 주의사항 재확인됨**: "예술 Art" 페이지의 board 위젯 중복 우려는 재조회 결과 정상 상태로 확인(Board→예술 클럽 정상 연결, 나머지는 원래 미연결 상태 그대로) — 그래도 사용자가 한 번 직접 봐주면 좋음. **다음에 확인 필요**: 이 브랜치는 자동 커밋/push/develop·main fast-forward 정책(사용자 지시, 2026-08-08)에 따라 처리됨 — CHANGELOG.md 커밋 해시 참고.
- **EPIC-089(구현 완료, 브랜치 `feature/EPIC-089`, 아직 커밋/병합 전)**: "[Stage 2] Floating Action UX, YouTube-style Nested Comments, & Admin/Sidebar Control" — 상세는 CHANGELOG.md 동명 항목 참고. `tsc`/`lint` 0 errors, 로컬 dev 로그인 세션으로 FAB/댓글 답글·좋아요·아바타/Board Type 드롭다운/사이드바 Y오프셋/`/admin/posts/common`까지 실측 확인 완료(테스트 데이터 정리 완료). **다음에 확인 필요**: (1) FAB "댓글" 아이콘의 `scrollIntoView({behavior:"smooth"})`이 헤드리스 테스트 환경에서 전혀 동작 안 해(behavior:"auto"는 정상 확인) 실제 브라우저에서 부드럽게 스크롤되는지 최종 확인 필요(에이전트 쪽 환경 한계로 추정, 코드 자체는 표준 API 그대로 사용), (2) 카테고리(게시판) 이동 후 저장 시 slug 재발급 로직은 실제 slug 충돌 케이스를 라이브 데이터로 재현하지 않고 코드 리뷰+타입체크로만 검증함(실제 게시글로 다른 게시판 이동 저장까지 눌러서 최종 확인 권장), (3) 사용자 승인 시 커밋 후 develop/main으로 fast-forward.
- **EPIC-084-REVISED 후속(완료, 2026-08-07 — Instagram 임베드 간헐적 미노출)**: 상세는 CHANGELOG.md 동명 항목 참고. 근본 원인은 Instagram 서버 쪽 간헐적 응답 실패로 결론(에디터 미리보기/상세 페이지 코드 경로는 동일함을 확인, Vercel/CSP 헤더 등 우리 쪽 배포 설정도 원인 아님을 확인). `src/lib/instagramEmbed.ts`의 폴백 타임아웃을 6초 단발 → 8초+재시도+8초 2단계로 연장. **다음에 확인 필요**: 사용자가 실제 크롬에서 며칠간 사용하며 폴백(링크로 대체) 빈도가 체감상 줄었는지 — 근본적으로 Instagram이 계속 막으면 이 완화책으로도 못 고치므로, 만약 계속 빈번하면 (a) 타임아웃을 더 늘리거나 (b) `src/lib/embedThumbnail.ts`의 og:image 스크래핑 기반 정적 카드로 아예 전환하는 방안을 다음에 검토할 것.
- **EPIC-088(구현+실사용 검증 완료, 브랜치 `feature/EPIC-088`, 아직 develop/main 병합 전)**: "[Stage 2] Admin Detail Corrections, Widget Linkage, & Embed Thumbnail Engine" — 상세는 CHANGELOG.md 동명 항목 참고. `tsc`/`lint` 통과. **실사용 검증(2026-08-07, 같은 세션 후속)**: 임시 테스트 계정(`epic088-test-20260807@silostore.net`)을 만들어 Supabase Management API로 `is_admin`을 켜고 실제 관리자 로그인 세션으로 전부 확인 — (1) `/admin/site-structure` 트리 노드 접기/펼치기 토글이 실제로 하위(nav 자식+배정된 게시판)를 숨기고 다시 펼침 확인, (2) `CategoryDetailModal`에서 "About Silo" 페이지의 최소 접근 가능 티어를 Patron으로 저장 → `page_builder.min_rank_to_read=3` DB 반영 확인, (3) Silo Angel 등급인 테스트 계정으로 "About Silo" 탭 클릭 → alert("이 메뉴는 Patron 등급부터 이용할 수 있어요") 발생 + `/membership?gate=3`로 실제 리다이렉트 확인(등급 무관 탭은 정상 이동도 확인), (4) `/admin/pages/[id]` 위젯 편집기에서 게시판 선택 시 "게시판 수정" 링크가 나타나고 `/admin/boards/[id]`로 정확히 연결됨 확인, board 위젯이 이미 있는 페이지에는 "게시판 추가" 버튼이 안 뜨는 조건부 로직도 확인, (5) 신규 테스트 게시판을 만들어 슬러그 필드가 편집 가능함과 `CategoryBranchPicker`에서 "About Silo"를 선택 후 "위치 저장" → `page_modules`에 실제 board 위젯 행이 생성됨을 DB 조회로 확인. **테스트 중 만든 계정/게시판/설정값은 전부 원상복구 완료**(테스트 계정+auth 사용자 삭제, 테스트 게시판+page_modules 링크 삭제, About Silo의 min_rank_to_read를 다시 null로). Embed 썸네일 파이프라인(요구사항 11)은 대부분 기존 구현 재확인 + `boards/feed` 라우트 공백 수정뿐이라 별도 실사용 검증은 우선순위상 생략. **다음에 할 일**: 사용자 승인 시 `develop`/`main`으로 병합.
- **EPIC-087(코드 완료, `develop`/`main`에 이미 병합됨 — EPIC-088 시작 시 `git branch -vv`로 재확인, 문서만 "진행 중"으로 뒤처져 있던 것을 여기서 바로잡음. PHASE별 실사용 클릭 검증은 아래 항목대로 여전히 미완)**: "[Stage 2] Admin Dashboard Overhaul, Tier-based Access Control, & Global UX" — 9개 요구사항(2.1~2.6)을 PHASE-0~G로 분할 진행 중. PHASE-0/A/B 완료, 이후 단계는 CHANGELOG.md에 순차 기록.
  - **PHASE-A에서 확인된 비-버그(요구사항 2.1 원문과 다름, 코드 수정 안 함)**: (1) 게시판 슬러그(`category`) 수정 잠금은 `BoardForm.tsx`의 의도된 설계(다른 화면이 슬러그 값을 참조) — 버그 아님. (2) `/admin/pages/[id]` 위젯 게시판 선택 드롭다운은 EPIC-084-REVISED의 `CategoryBoardPicker` 도입 이후 이미 정상 데이터 바인딩됨 — 재확인만 하고 코드 변경 없음. "카테고리 드롭다운에서 연결된 페이지 목록"으로 실제 해석해 `BoardForm.tsx`에 읽기 전용 역방향 조회("이 게시판을 사용 중인 페이지")를 대신 추가함.
  - **PHASE-B 다음 세션 확인 필요(관리자 로그인 세션 없어 미확인)**: `/admin/site-structure`에서 (1) 게시판 카드를 실제로 드래그해 다른 분기로 옮겼을 때 `page_modules` 연결이 정확히 갱신되는지(Management API로 직접 확인 권장), (2) 같은 분기 안에서 게시판 순서를 바꿨을 때 `boards.sort_order`가 저장되는지, (3) 페이지+게시판 체크박스 다중 선택 후 "선택 N개 삭제"가 둘 다 정확히 지우는지, (4) "미배정 게시판" 섹션에서 분기로 드래그했다가 다시 미배정으로 되돌리는 왕복 동작.
  - **PHASE-C 후속 확인 필요**: 하이브리드 페이지 7개(`boards/[board_slug]`, `shop/[id]`, `clubs/[id]`, `docent/[id]`, `attendance`, `downloads`, `u/[memberId]`)는 의도적으로 페이지 전체 리다이렉트 게이트에서 제외했다(보조 위젯 슬롯만 `fetchPublishedPageBySlug`를 쓰고 주 콘텐츠는 별도라서) — 이 페이지들에도 티어 제한이 필요해지면 개별 설계 필요(예: 슬롯만 조건부 숨김, 주 콘텐츠 자체의 별도 게이팅 등). 관리자 로그인 세션으로 `/admin/boards/[id]`·`/admin/pages/[id]`에서 실제로 등급 셀렉트를 바꾸고 저장까지 되는지, `/mypage`처럼 로그인 사용자만 보는 페이지에 게이트를 걸었을 때 등급 충분한 로그인 사용자가 잘못 튕겨나가지 않는지(memberLoading race) 실사용 확인 필요.
  - **PHASE-D 다음 세션 확인 필요(관리자 로그인 세션 없어 미확인)**: `/admin/members`에서 (1) 컬럼 헤더 클릭 정렬이 실제로 동작하는지, (2) 티어 권한 매트릭스 체크박스/드롭다운을 바꾸면 즉시 저장되는지(방금 추가한 `membership_tiers_admin_write` RLS 정책이 실제로 통과하는지 실사용 확인 필요 — Management API 조회로는 postgres 슈퍼유저 권한이라 RLS를 우회하므로 검증되지 않음), (3) "멤버십 안내 페이지 설정" 모달 저장이 PHASE-C 리다이렉트 게이트에 실제로 반영되는지.
  - **PHASE-E 다음 세션 확인 필요(관리자 로그인 세션 없어 미확인)**: `/space-inquiry/item-rental`에서 실제 로그인 후 신청 제출→`item_rental_requests` insert 확인, `/admin/rentals`에서 두 목록이 정확히 뜨는지(신규 `item_rental_requests_admin_select` RLS 정책 포함) 및 상태 변경 드롭다운 저장(`item_rental_requests_admin_update` 정책), `/admin/rentals`의 공간 대관 섹션이 기존 `rental_bookings` admin-bypass RLS(CLAUDE.md 문서화됨)로 정상 조회되는지.
  - **PHASE-F 다음 세션 확인 필요(로그인 세션 없어 미확인)**: 실제 로그인 후 GNB에서 등급/이름 클릭 시 팝오버가 열리는지, 바깥 클릭·Escape로 닫히는지, 팔로우/팔로워 수와 최근 활동이 실제 데이터와 맞는지, `/boards/mind-diary` 링크가 정상 이동하는지. 프로필 사진 업로드 UI는 이번 범위 밖(표시 전용) — 필요해지면 `avatar_url`/`avatars` 버킷은 이미 준비돼 있음.
  - **PHASE-G 다음 세션 확인 필요(관리자 로그인 세션 없어 미확인)**: `/admin/footer`에서 실제로 항목을 입력/저장한 뒤 사이트 전체 footer에 반영되는지, 항목 추가/삭제 버튼이 정상 동작하는지.
  - **EPIC-087 문서 마무리 아직 필요**: 브랜치는 이미 `develop`/`main`에 병합된 상태로 확인됨(위 참고)이지만, `docs/EPIC.md`(진행중→완료 이동)/`docs/PROJECT_DASHBOARD.md`(Current EPIC 갱신)/`docs/STAGES.md` 동기화는 아직 안 함 — 다음에 EPIC.md 등 정리할 때 함께 반영할 것.
- **EPIC-086(완료, 2026-08-07 — 브랜치 `feature/EPIC-086-slide-write-button`, 아직 main/develop에 병합 전)**: "Slide Module 글쓰기 버튼" — 상세는 CHANGELOG.md 동명 항목 참고. 다른 컴퓨터에 남아있던 EPIC-084 미커밋 WIP를 검토해 이미 병합된 작업과 중복되지 않는 유일한 항목(Gallery에는 있고 Slide에는 없던 글쓰기 버튼)만 반영. `tsc`/`lint` 통과, 로컬 dev(비로그인)에서 렌더링 확인 — 로그인 세션에서 실제 클릭→`/write?boardId=` 정상 이동은 다음 확인 대상.
- **EPIC-084-REVISED(완료, 2026-08-06 — main/develop에 병합됨)**: "[Stage 2] Universal Navigation, Contextual Write Flow, 3-Column Category Selector, & Editor UX Overhaul" 요청 — 상세는 CHANGELOG.md 동명 항목 참고. **✅ R2 업로드 P0 완전히 해결됨(같은 날 후속)**: 사용자가 이 컴퓨터의 `.env.local`에 R2 자격증명 5종을 입력 — 처음엔 `R2_BUCKET_NAME`/`R2_PUBLIC_URL`이 한 줄에 잘못 합쳐져 있던 것을 발견해 분리(빈 `R2_BUCKET_NAME` 확인), 사용자가 실제 값 입력 후 재시도 → `POST /api/media/presigned` 200은 됐지만 실제 R2 PUT이 CORS 에러로 실패(R2 버킷에 CORS 정책이 아예 없었음) → 사용자가 Cloudflare 대시보드에서 R2 버킷 CORS 정책 추가(AllowedOrigins: localhost:3000/dev.silostore.net/silostore.net, AllowedMethods: PUT/GET) → **실제 브라우저에서 presigned 발급→R2 PUT→media_library insert→public URL GET까지 전체 왕복 200 확인 완료**(테스트로 만든 media_library 행 2건은 정리 완료, R2에 남은 테스트 오브젝트 2개는 68바이트라 무시 가능한 수준이라 남겨둠). **다음 세션에서 로그인 세션으로 확인 필요**: (1) 3열 카테고리 선택기로 실제 글쓰기 진입 시 pre-select가 정확한지, (2) 갤러리/임베드 리사이즈 핸들 드래그 체감. **후속 검토 필요(P2)**: "사이트 구성 관리"의 "미분류 페이지" 버킷에 아직 남은 23개(위젯 콘텐츠 있음 — Gallery Awards/Parties, Membership \* 등)와 About Silo 드롭다운의 "사일로 상점"/"살롱데상" 중복 콘텐츠 페이지(`/c/d30d149d-...`, `/c/e9ab4e8d-...`, 각각 위젯 5개 보유)는 실제 콘텐츠가 있어 이번엔 손대지 않음 — 사용자와 함께 개별 검토 후 정리 또는 보존 결정 필요.
- **EPIC-085(완료, 2026-08-06 — main/develop에 병합됨)**: "[Stage 2] Universal Block Renderer, Frictionless Archiving (Scraping System), & Dynamic OG/SEO Engine" 요청 — 상세는 CHANGELOG.md 동명 항목 참고. `docs/sql/EPIC-085-user-scraps.sql`은 Management API로 즉시 실행 완료(라이브 반영됨). **다음 세션에서 확인 필요**: (1) `ScrapButton` 토글과 비로그인 유저 로그인 유도 모달, (2) `/mypage/archive` 카테고리별 그리드, (3) 실제 SNS 공유 시 동적 OG 카드. **후속 과제(P2)**: `public/`에 실제 사이트 대표 OG 이미지 에셋이 없어 본문에 이미지가 하나도 없는 글은 `og:image` 자체가 생략됨 — 기본 OG 이미지 파일을 준비해 `extractPostMetadata.ts`의 fallback으로 연결하면 좋음.
- **EPIC-084(완료, 2026-08-06 — main/develop에 병합됨)**: "[Stage 2] Universal Routing & Contextual Write Flow, Editor Media Control, & Admin Clean-up" 요청 — 상세는 CHANGELOG.md 동명 항목 참고.
- **EPIC-083(완료, 2026-08-06 — main/develop에 병합됨)**: "[Stage 2] Universal Editor Extension" 요청 — 상세는 CHANGELOG.md 동명 항목 참고. `custom_fonts` SQL은 Management API로 즉시 실행 완료(라이브 반영됨).

## 다음 작업
- **EPIC-082(코드/문서 완료, 2026-08-06 — DB는 위 "진행 중" 참고)**: "[Stage 2] Universal Media Library Schema & Cloudflare R2 Direct Upload Pipeline" 요청 — 상세는 CHANGELOG.md 동명 항목 참고. `docs/sql/EPIC-082-media-library.sql` 신설(media_library 테이블+RLS), `src/app/api/media/presigned/route.ts` 신설(R2 pre-signed PUT URL 발급, `@aws-sdk/client-s3`+`@aws-sdk/s3-request-presigner` 신규 설치), `src/lib/media.ts` 신설(MediaAsset 등 공용 타입), `docs/media-architecture.md` 신설(데이터 계약 문서). 지시대로 UI/에디터 컴포넌트는 전혀 건드리지 않음(`blockEditorCore.ts` 무변경). `tsc`/`lint`/`build` 전부 통과. **R2 업로드 왕복 실기 검증 완료(같은 날 후속, 사용자 로그인 세션으로 확인)** — 과정에서 `.env.local`의 `R2_ACCOUNT_ID` 오설정(API 토큰 값이 잘못 들어가 있던 것)을 발견/수정, presigned PUT→R2 저장→public URL GET까지 전부 정상 동작 확인(CHANGELOG.md "EPIC-082 후속" 항목 참고). **남은 확인 필요**: `docs/sql/EPIC-082-media-library.sql` 실행 후 RLS 정책이 의도대로 동작하는지(본인 업로드만 수정/삭제 가능, 조회는 전체 공개), Vercel 배포 환경 R2 환경변수 등록(위 "진행 중" 참고). **다음 EPIC(083 예상)**: `docs/media-architecture.md` §6 마이그레이션 메모 참고 — 업로드 완료 확인 API(media_library insert 연동), 기존 게시글의 `src` 문자열 URL과 `media_id` 참조 간 하위 호환 전략 결정, 에디터/갤러리/히어로 위젯 UI 배선.
- **EPIC-080 DB 반영 완료(2026-08-06, 사용자가 Supabase SQL Editor에서 직접 실행)**: `docs/sql/EPIC-080-nav-unification.sql`을 사용자가 직접 실행 완료 — Management API로 재조회해 확인함: 할머니/할아버지 nav href가 `/heritage/grandmas`·`/heritage/grandpas`로 정확히 정정됐고, 그림자 미분류 nav 항목 17건도 전부 삭제됨(검증 쿼리 0행 확인). EPIC-080이 코드/문서/DB 전부 완료됨.
- **EPIC-081(완료, 2026-08-06 — 문서 전용)**: "Community-Led Growth Vision & Architecture Alignment(Stage 2 진입 선언)" 요청 — 상세는 CHANGELOG.md 동명 항목 참고. `PROJECT_VISION.md`에 5단계 Community-Led Growth Flow를 핵심 전략으로 명문화, `PROJECT_ARCHITECTURE.md`에 이를 뒷받침할 Stage 2 기술 전략 4가지(Universal Content Editor+R2, Frictionless Archiving, Event Telemetry, Paywall Routing — 전부 ⚠️계획 단계로 명시, 아직 미구현)를 별도 절로 추가. `docs/STAGES.md`/`docs/PROJECT_DASHBOARD.md`의 Current Stage를 Stage 2로 갱신, `docs/EPIC.md`에 EPIC-080/081 요약 추가. 코드/DB 변경 없음. **다음 세션 참고**: 다음 EPIC은 위 4개 기술 전략 중 우선순위를 사용자와 논의해 결정할 것 — 이번 EPIC은 방향 명문화만 하고 어느 것을 먼저 구현할지는 결정하지 않았다.
- **EPIC-080(코드/문서/DB 전부 완료, 2026-08-06)**: "Navigation Dual-Structure Unification(기술 부채 청산)" 요청 — 상세는 CHANGELOG.md 동명 항목 참고. Management API로 라이브 `site_navigations`(153행)를 직접 조사한 결과, 커뮤니티/갤러리/멤버십 dual-nav는 이미 라이브 DB가 올바르게 정리돼 있었고 진짜 문제는 `FALLBACK_NAV_TABS`(코드 폴백)가 라이브와 다른 이름별 동적 라우트 구조를 쓰고 있던 것 + 과거 혼선의 잔재인 그림자 정적 페이지 17개였다. `navConfig.ts`를 라이브 트리와 동기화, 그림자 페이지 17개 삭제+301 리다이렉트(`next.config.ts`), 죽은 훅(`useBoardIdByName`) 제거, `sitemap.ts`/`docs/database-schema.sql`/`docs/navigation-blueprint.md`/`NAVIGATION_SYSTEM.md` 동기화. `tsc`/`lint`/`build` 전부 통과, 로컬 dev 서버에서 리다이렉트 동작과 `/heritage/grandmas` 실제 렌더링까지 확인. **사용자에게 확인받은 결정**: 헤리티지 이름별 67개 항목은 단일 링크로 통합(코드에서 삭제), 그림자 페이지는 파일 삭제+301 리다이렉트. **남은 것(이번 범위 밖, `docs/navigation-blueprint.md` §1/§9에 기록)**: (1) 소개지/포스터가 여전히 같은 `/downloads`로 수렴 — 의도 확인 필요, (2) `site_navigations`의 "미분류 페이지" 버킷에 남은 나머지 ~23개 항목은 명백한 dual-nav 패턴이 아니라 개별 검토가 필요해 이번엔 손대지 않음. **확인 완료(2026-08-06)**: `mypage` 탭이 `target_type='dropdown'`(13개 "My X" 하위 항목)인 것은 사용자가 의도적으로 원한 구조임을 확인 — `navConfig.ts`의 `FALLBACK_NAV_TABS`와 `CLAUDE.md` Architecture 절도 이 구조에 맞춰 갱신함.
- **EPIC-079-PHASE-5(완료, 2026-08-06 — 인스타그램 스타일 갤러리 캐러셀)**: "여러 이미지/영상 URL을 인스타그램처럼 스와이프되는 슬라이더로 보여달라 + 대표 이미지가 본문에 영향을 주면 안 된다 + 이미지 클릭-드래그 리사이즈" 요청 — 상세는 CHANGELOG.md 동명 항목 참고. `gallery` 노드를 그리드에서 CSS scroll-snap 캐러셀(화살표+점 인디케이터, 영상 지원)로 재작성, `GalleryConfigModal.tsx` 신설(URL 붙여넣기+파일 업로드+순서변경), `PostDetailHeader`의 대표 이미지 배너 렌더링 완전 제거(목록 썸네일 전용으로 분리), `FigureImage`에 클릭→드래그 리사이즈 핸들 추가. `tsc`/`lint` 0 errors, 인증 없는 임시 검증 라우트(커밋 전 삭제)에서 갤러리 3슬라이드 렌더링/캐러셀 초기화/리사이즈 PointerEvent까지 DOM 조회로 확인. **다음 세션에서 확인 필요**: (1) 실제 로그인 세션으로 게시글 작성 → 발행 → 상세 페이지에서 캐러셀 좌우 스와이프(실제 터치/마우스 드래그, 이 세션은 헤드리스 pane이라 smooth-scroll 애니메이션 자체는 시각적으로 재현 못함 — 로직은 `behavior:"auto"`로 검증)와 화살표 클릭이 실제로 자연스럽게 넘어가는지, (2) 이미지 리사이즈 핸들을 실제 마우스로 드래그했을 때 체감상 매끄러운지(PointerEvent 디스패치로는 확인했으나 실제 마우스 드래그는 미확인), (3) 대표 이미지 배너 제거가 목록/카드 썸네일에는 영향 없는지(그쪽 파일은 손대지 않았지만 실사용 화면에서 최종 확인 권장).
- **EPIC-079-HOTFIX-3(완료, 2026-08-06)**: "대표 이미지가 본문에도 중복 표시됨 + Instagram/YouTube/Vimeo 임베드도 대표 이미지로 지정 가능하게 해달라" 요청 — 상세는 CHANGELOG.md 동명 항목 참고. `PostBody.tsx`에서 헤더에 이미 보여준 대표 이미지를 본문에서 제거(DOMParser), `embed` 노드에 `featured`/`thumbnailUrl` 추가 + `/api/embed-thumbnail` 신설로 youtube/vimeo/instagram ★ 지정 지원. `tsc`/`lint`/`build` 통과, 로컬 dev에서 이미지 붙여넣기→자동 지정, 유튜브 ★ 클릭→전환, 실제 발행 후 상세 페이지 중복 제거까지 확인. **다음 세션에서 확인 필요**: Vimeo/Instagram 썸네일 조회는 실제 URL로 재현 테스트 못함(provider 분기 로직만 검토) — 실 배포 환경에서 재확인 권장. Instagram은 og:image 스크래핑 방식이라 일부 게시물/환경에서 실패할 수 있음(기존에 알려진 한계와 동일선상).
- **EPIC-079-HOTFIX-2(완료, 2026-08-06)**: "외부 웹사이트 이미지를 파일로 복사-붙여넣기 하면 안 되고 링크를 복사해야만 된다" 신고 — 상세는 CHANGELOG.md 동명 항목 참고. `useEditor`의 `handlePaste`/`handleDrop`이 stale한 `editor=null` 클로저를 영원히 참조해 `handleImageFiles`가 조용히 아무 일도 안 하던 게 원인 — 모듈 스코프 `uploadAndInsertImages(view, files)`로 교체해 근본 해결. `tsc`/`lint`/`build` 통과, dev+프로덕션 빌드 양쪽에서 실제 이미지 blob paste → Storage 업로드 → 삽입까지 확인.
- **EPIC-079-HOTFIX(완료, 2026-08-05)**: "Tiptap Instagram Card Native Integration" 지시로 재현 테스트하다 발견한 훨씬 심각한 버그 — 정상 렌더링된 Instagram 임베드가 6초 뒤 통째로 사라지는 문제를 근본 수정. 상세는 CHANGELOG.md 동명 항목 참고. `src/lib/instagramEmbed.ts`의 폴백 중복 체크가 우리 자신의 장식용 사전 링크를 "이미 있는 폴백"으로 오인해 `el.remove()`(대체 없이 삭제)를 실행하던 게 원인 — `data-ig-fallback` 마커로 구분해 해결. `tsc`/`lint`/`build` 통과, 로컬 프로덕션 빌드에서 MutationObserver로 수정 전/후 실제 타임라인 비교 확인. **dev.silostore.net 배포 후 정상 응답하는 실제 Instagram 게시물로 최종 재확인 권장**(로컬 샌드박스는 iframe이 끝까지 안 뜨고 폴백으로 귀결됨 — 별개의 기존 알려진 한계, Instagram 서버 쪽 문제로 추정).

## 다음 작업
- **EPIC-079-PHASE-5(완료, 2026-08-05)**: "Editor UX Polish, Fallback Thumbnails & Admin Site Management Fixes" 요청(4개 그룹) — 상세는 CHANGELOG.md 동명 항목 참고. `tsc`/`lint`/`build` 전부 통과, 로컬 dev 서버에서 툴바 라벨/드롭다운 트리 계층/게시판 추가(POST 200)/URL 정리 필요 배지까지 실제 확인. **다음 세션에서 확인 필요**: (1) 관리자 화면 게시판/페이지 "삭제" 버튼의 `confirm()` 다이얼로그 클릭까지 실제 재현(이번 세션은 코드 리뷰로만 안전성 판단 — boards[id] DELETE의 이미 검증된 패턴을 그대로 재사용하는 대칭 구현), (2) 대표 이미지 없이 유튜브 임베드만 있는 글을 실제로 저장해 대표 이미지가 자동으로 채워지는지, (3) 전체 dual-nav/조잡한 URL 구조 개편은 이번에 범위 밖으로 남겨둠(기존 P0 기술부채, PROJECT_DASHBOARD.md 참고) — "URL 정리 필요" 배지로 가시성만 확보.
- **EPIC-079-WRITE-FIX(완료, 2026-08-05)**: "dev.silostore.net에 글이 전혀 안 써진다" 신고 — 상세는 CHANGELOG.md 동명 항목 참고. 한글 전용 제목이면 `posts.slug`(NOT NULL, 기본값 없음)가 null이 되어 INSERT 자체가 즉시 500으로 실패하던 P0 버그. `id`를 라우트에서 미리 생성해 slug 폴백에 쓰도록 수정해 근본 해결. `tsc`/`lint` 0 errors, 로컬 dev 서버에서 한글 제목으로 실제 글쓰기 성공까지 확인, 테스트 데이터/계정 전부 정리 완료. **배포(develop/main push) 후 dev.silostore.net에서 실제 사용자가 한글 제목으로 다시 확인 권장.**
- **EPIC-079-INSTA-REGEX-FIX(완료, 2026-08-05)**: "쿼리 파라미터 붙은 Instagram URL 임베드 파싱 실패" 신고 조사 — 상세는 CHANGELOG.md 동명 항목 참고. 실제로는 기존 정규식이 이미 쿼리 파라미터 앞에서 멈췄지만(재현 안 됨), `new URL()` 기반 호스트명 검증 + pathname 매칭으로 재작성해 더 견고하게 만들고 부수로 무관 도메인 오탐 가능성도 제거. `tsc`/`lint` 0 errors, `tsx`로 실제 모듈을 직접 import해 함수 호출 검증 완료. **로그인 세션이 있는 다음 세션에서 실제 에디터 UI로 쿼리 파라미터 붙은 Instagram/X URL을 붙여넣어 최종 확인 권장**(순수 함수 로직 검증은 완료, UI 클릭 경로는 미확인).
- **EPIC-079 후속 — EPIC-075 SQL 실행 완료(2026-08-05, 사용자 제공 Management API 토큰으로 직접 실행)**: `docs/sql/EPIC-075-tree-sort-order.sql` 적용 완료 — `page_builder`/`boards`에 `sort_order` 컬럼 신설 + 기존 행 전체(`page_builder` 150건, `boards` 56건) 백필까지 완료(재조회로 `sort_order = 0`인 행이 0건임을 확인). 이제 `/admin/pages`·`/admin/boards`의 드래그앤드롭 순서 변경이 새로고침 후에도 유지될 것으로 기대됨(코드 자체는 EPIC-075 때 이미 작성돼 있었고 컬럼 부재로 저장만 실패하던 상태) — **다음에 관리자로 로그인해 두 화면 모두 실제로 드래그해 순서가 저장/유지되는지 확인 필요**(에이전트는 로그인 세션을 못 만들어 클릭 테스트 불가).
- **EPIC-079-FINAL-FIX 후속(완료, 2026-08-05)**: "외부 사이트 이미지/영상 복사-붙여넣기 + URL 임베드 너비 조절" 요청 — 상세는 CHANGELOG.md 동명 항목 참고. FigureImage의 `<img>` 캡처 규칙에 지연 로딩(`data-src`/`srcset`) 폴백 추가, `handlePaste`에 순수 URL 텍스트 자동 인식(이미지/알려진 플랫폼/영상 파일) 추가, 모든 provider의 임베드 너비를 조절 가능하게 일반화. `tsc`/`lint` 0 errors, Browser pane에서 이미지/유튜브 URL 붙여넣기와 너비 입력 반영까지 실제 확인 완료.
- **EPIC-079-FINAL-FIX(완료, 2026-08-05)**: "Instagram 임베드 hydration + 슬라이드 black flash" 요청 — 상세는 CHANGELOG.md 동명 항목 참고. `CustomHtmlEmbedPreview`(HTML 코드 붙여넣기 임베드의 에디터 미리보기)가 `processInstagramEmbeds()`를 아예 호출 안 하던 진짜 버그를 수정, `HeroSlideshow.tsx`는 첫 이미지 eager/high-priority 로딩 + wrapper 투명화 + 로드 완료 후 페이드인(+ 캐시 이미지 onLoad 미발생 케이스 보강)까지 적용. `tsc`/`lint` 0 errors, DOM/computed style 조회로 실제 반영 확인. **신고에 첨부된 특정 게시물(la-primavera-concerto-in-e-major)의 Instagram embed는 Instagram 서버가 그 permalink 자체를 거부하는 게 이미 확인된 상태라(이전 세션) 이 게시물로는 실제 렌더링 확인이 불가능 — 다른(Instagram이 거부하지 않는) 게시물로 HTML 코드 임베드를 다시 테스트해볼 것.**
- **EPIC-079-PHASE-4 후속 3차(완료, 2026-08-05)**: "미분류 페이지를 사이트 메뉴로 드래그 앤 드롭 + 모든 미분류 페이지에 추가/수정/페이지 수정/관리/삭제 부여" 요청 — 상세는 CHANGELOG.md 동명 항목 참고. 별도 UI를 새로 만드는 대신, 미분류 페이지를 "미분류 페이지" 버킷 루트 아래의 진짜 site_navigations 행으로 자동 흡수해 기존 트리 드래그/버튼을 그대로 물려받게 했다. `tsc`/`lint` 0 errors. **로그인 세션을 잃어 실제 클릭/드래그 테스트 못 함 — 다음에 관리자로 로그인해서 미분류 페이지 버킷 노출, 드래그 시 활성화, 하단 패널 중복 제거 3가지 직접 확인 필요.**
- **주의 — 동시 세션 흔적 발견**: 이 세션에서 커밋 `55b7f9d`(About Silo 드롭다운 수정 + 상단 탭 디스플레이 인라인 셀렉트 + SQL 실행)가 내가 만들지 않았는데도 로컬 git history에 이미 존재하는 걸 발견했다 — 같은 컴퓨터에서 다른 Claude Code 세션이 동시에 같은 저장소를 작업 중이었던 것으로 보인다. 이번 커밋까지는 파일이 안 겹쳐 문제 없었지만, **다음 세션 시작 시 원격/로컬 상태를 다시 한번 fetch+status로 확인하고, 혹시 두 세션이 계속 동시에 켜져 있다면 사용자에게 의도한 것인지 확인할 것.**
- **EPIC-079-PHASE-4 후속 2차(완료, 2026-08-05)**: "홈페이지 설정 관리에 상단 탭 디자인(텍스트/폰트/크기) 기능 추가" 요청 — 상세는 CHANGELOG.md 동명 항목 참고. `site_settings.top_tab_style` 신설 + `/admin/navigation/settings`에 편집 섹션 추가 + `Navbar.tsx` 렌더링 반영. `tsc`/`lint` 0 errors, 프론트엔드 렌더링은 Management API로 테스트 값 직접 넣어 확인·원복 완료. **관리자 폼 자체(저장 버튼 클릭)는 로그인 세션을 잃어 미확인 — 다음에 로그인 후 실제로 저장까지 눌러서 확인 필요.**
- **재확인 필요(Browser pane 도구가 세션 후반 일시 불능이라 못 끝냄)**: (1) `CategoryTreeManager.tsx`에 새로 추가한 최상위 행 인라인 "노출 방식" `<select>`가 클릭으로 실제 target_type을 바꾸고 저장하는지, (2) SQL GRANT 실행 완료된 "글 수정 → 다른 게시판으로 옮기기"가 이제 실제로 성공하는지(저장 후 새 게시판 URL로 리다이렉트되는지까지) — 둘 다 코드/SQL은 완료됐고 tsc/lint만 통과 확인, 클릭 상호작용 재검증이 남음.
- **EPIC-079-PHASE-4 SQL 실행 완료(2026-08-05, 사용자 제공 Management API 토큰으로 직접 실행)**: `docs/sql/epic-079-phase-4-page-mapping-backfill.sql`(검증 쿼리 결과 미매핑 노드 0건) / `docs/sql/epic-079-phase-4-post-board-move-grant.sql`(board_id GRANT + 트리거) 둘 다 적용 완료. 부수로 About Silo `target_type`이 `'tab'`으로 잘못 저장돼 있어(하위 카테고리가 있는데도 드롭다운이 안 뜨던 원인) `'dropdown'`으로 SQL로 직접 수정 — Browser pane DOM 조회로 드롭다운 패널 렌더링 확인.
- **EPIC-079-PHASE-4(완료, 2026-08-05)**: "관리자 사이트 구성 트리 통합 + 모든 노드에 페이지 매핑 강제 + 글 수정 폼 동적 페이지 선택 + 상단 탭 디스플레이 전용 컨트롤" 요청 — 상세는 CHANGELOG.md EPIC-079-PHASE-4/후속 참고. `/admin/site-structure`를 3개 트리→1개 통합 트리로 재작성, 모든 노드에 "페이지 수정" 버튼 무조건 노출(href 없으면 온디맨드 생성), 글 수정 폼에 write와 동일한 동적 게시판 선택 드롭다운 추가, 최상위 행에 노출 방식 전용 인라인 셀렉트 추가. `npx tsc --noEmit`/`npm run lint` 0 errors.
- **EPIC-075 SQL 실행 필요(재확인, EPIC-079-PHASE-3 중 재발견)**: `docs/sql/EPIC-075-tree-sort-order.sql`이 아직도 미실행 상태 — `/api/boards`가 `boards.sort_order` 컬럼 부재로 rich select가 매번 42703 실패해 legacy로 폴백하고, 그 결과 `slug`를 포함한 여러 필드가 응답에서 빠진다. 새로 추가한 글쓰기 화면의 "게시될 페이지 선택" 드롭다운이 게시판을 slug 대신 UUID로 표시하게 되는 부작용 확인(기능 자체는 fetchBoard의 UUID 폴백 덕분에 정상 동작, 표시만 어색함). `/admin/pages`·`/admin/boards`의 드래그앤드롭 순서 저장도 여전히 안 됨(원래 EPIC-075때부터 알려진 문제).
- **EPIC-079-PHASE-3(완료, 2026-08-05)**: "임베드 커스터마이징 모달 UI + 글쓰기 폼 동적 페이지 선택" 요청 — 상세는 CHANGELOG.md EPIC-079-PHASE-3 참고. `npx tsc --noEmit`/`npm run build` 통과, Browser pane에서 YouTube 9:16 세로 렌더링(DOM 측정으로 정확한 비율 확인)과 게시될 페이지 선택 드롭다운 전환까지 실제 확인. **사용자 확인 필요**: (1) Instagram 캡션 숨기기 토글이 실제 게시글에서 캡션을 감추는지, (2) Spotify Compact/Normal 사이즈 프리셋이 의도한 높이로 저장되는지 — 이 세션에서는 YouTube 9:16만 직접 DOM 측정으로 검증했고 나머지 둘은 코드 경로만 확인함.
- **EPIC-079-TIPTAP-FIX-V2(완료, 2026-08-05)**: "Instagram 임베드가 작동 안 한다"는 사용자 신고 조사 + X(Twitter)/네이버 블로그/네이버 지도/구글 지도 붙여넣기 임베드 확장 요청 — 상세는 CHANGELOG.md EPIC-079-TIPTAP-FIX-V2 참고. **핵심**: Instagram의 `/embed/*` 엔드포인트가 `X-Frame-Options: DENY`를 항상 응답해(Instagram 공식 oEmbed API가 "임베드 가능"이라고 확인해준 게시물로도 재현) 사설 코드로는 절대 못 고치는 서버 측 차단임을 curl로 직접 확인 — 공식 위젯(blockquote+embed.js) 방식 + 6초 안에 렌더링 실패 시 자동으로 "Instagram에서 보기" 링크로 바꿔주는 폴백 워치독을 추가해, 항상 무언가는 보이도록 만들었다(완전한 인라인 임베드는 Instagram이 열어주지 않는 한 보장 불가). X(Twitter)/네이버 블로그는 실제 콘텐츠가 렌더링되는 것까지 확인(각각 `platform.twitter.com/embed/Tweet.html`, `blog.naver.com/PostView.naver` — 둘 다 X-Frame-Options 없음 확인). 구글/네이버 지도는 "퍼가기" iframe 코드를 에디터에 직접 붙여넣으면 자동 캡처(네이버 지도는 URL 입력만으로는 embed URL을 만들 방법이 없어 이 경로가 유일). `npx tsc --noEmit`/`npm run build` 통과, Browser pane에서 4개 provider 전부 실제 삽입해 확인(테스트 게시글은 저장하지 않고 폐기). **사용자가 실제 인스타그램 게시물로 결과를 확인해달라** — 이 세션에서 테스트한 특정 게시물은 폴백 링크로 처리됐는데, 다른 게시물/실제 배포 환경(로컬이 아닌 실서버 IP)에서는 Instagram이 정상 응답해 실제 임베드가 뜰 수도 있다(원인이 Instagram 서버 쪽 정책이라 이 저장소 코드만으로는 100% 예측 불가).
- **EPIC-079-PHASE-2 SQL 실행 완료(2026-08-04, 사용자 제공 Management API 토큰으로 직접 실행)**: `docs/sql/epic-079-phase-2-slug.sql` 적용 완료 — 실행 중 `boards` 테이블에 라이브에 `created_at` 컬럼이 없어(문서화된 스키마와 드리프트) 백필 정렬 기준을 `order by id`로 수정해 재실행, 성공. `boards.slug`/`posts.slug` 백필 결과를 재조회로 확인(한글 제목은 id 앞 8자리 폴백, 영문/혼합 제목은 slugify 정상 적용). dev 서버로 `/boards/general`(실제 slug)과 게시글 상세(`/boards/general/silo-angel`)가 UUID 없이 정상 렌더링됨을 실제 확인 완료 — slug 라우팅이 이제 완전히 동작한다. 검증 중 만든 테스트 카테고리("새 항목", `/c/97571a7b-...`)와 그 `page_builder`/`page_modules` 행도 SQL로 함께 정리 완료.
- **EPIC-079-PHASE-2(완료, 2026-08-04 적용 및 SQL 실행까지 완료)**: "게시판 URL을 slug 기반으로 개편 + 상단 탭 드롭다운 서브카테고리 미출력 + 새 카테고리 생성 시 페이지 자동 연결 안 됨 + 사이드바 WebM hover 미재생 + 슬라이드쇼 여백 배경 검정" 4건 요청 — 상세는 CHANGELOG.md EPIC-079-PHASE-2 참고. `npm run build`/`npx tsc --noEmit` 통과, Browser pane으로 slug 라우팅 전체 흐름(목록→상세→댓글까지)과 카테고리 생성 시 즉시 페이지 연결까지 실제 확인. **사용자 확인 필요(남은 것)**: (1) 사이드바 아이콘 hover 시 WebM이 실제로 재생되는지, (2) 홈페이지 슬라이드쇼 여백 배경 이미지를 새로 업로드했을 때 더 이상 검정으로 안 나오는지(기존에 이미 검정으로 저장된 이미지는 소급 재인코딩되지 않음, 재업로드 필요) — 둘 다 이 세션의 Browser pane이 `@media (hover: hover)`를 만족하지 않아 실제 마우스 호버로는 확인 못함(EPIC-076/078 때와 동일한 한계).
- **보안 참고**: 이번에 받은 Supabase Management API 토큰은 CLAUDE.md 방침대로 마이그레이션 실행에만 쓰고 메모리/파일 등 어디에도 저장하지 않았다 — 다음 세션에서 DDL이 필요하면 다시 새로 요청할 것.
- **EPIC-079-PHASE-1 후속 — 실제 테스트 검증 완료(2026-08-04)**: 임시 테스트 계정으로 글쓰기→이미지 업로드→임베드 3종(Instagram/Spotify/Google Maps) 삽입→저장→수정→삭제까지 전체 CRUD를 실제로 완주 검증(상세는 CHANGELOG.md 참고). 이 과정에서 추가로 발견/수정한 실버그 3건: (1) 이미지+임베드를 연달아 삽입하면 이전 블록이 사라지는 버그(`BlockEditor.tsx`, `focus("end")`로 수정), (2) AuthProvider 세션 판정 레이스로 로그인된 사용자도 수정 화면에서 순간적으로 "본인 글만 수정 가능" 오판(`AuthProvider.tsx`, `loading` 가드 추가), (3) 수정 화면에서 본문을 안 건드리고 제목만 고쳐 저장하면 조용히 무시되는 버그(`PostForm.tsx`/`blockEditorCore.ts`의 `isEmptyDoc()`). 이전 항목의 "사용자 확인 필요" 목록은 이번 실검증으로 대부분 해소됨 — 아래는 이번에도 확인하지 못한 나머지: 썸네일 노출 토글이 실제 목록/상세 카드에서 이미지를 숨기는지(코드는 반영했으나 이번 테스트 게시판엔 카드형 썸네일 레이아웃이 아니라 육안 확인 못함), 관리자가 "타인이 쓴 글"을 실제로 수정/삭제하는 admin-bypass 경로(이번엔 본인 글로만 테스트).
- **EPIC-079-PHASE-1(완료, 2026-08-04 적용됨)**: "Tiptap Editor & Post CRUD Stabilization" 요청 — 상세는 CHANGELOG.md EPIC-079-PHASE-1 참고. 핵심: 라이브 DB 점검 결과 `docs/sql/epic-053-1.sql`이 한 번도 적용되지 않아(`body_json` 없음, Storage 버킷 0개, `tags`/`view_count`/`updated_at`조차 없음) 보고된 증상 대부분이 인프라 누락이 원인이었음을 발견 — 사용자 제공 Supabase Management API 토큰으로 `docs/sql/epic-079-phase-1.sql`(재적용+누락 컬럼+EPIC-079 신규 컬럼+GRANT 확장+소유자 보호 트리거+삭제 RLS)을 직접 실행해 라이브 반영 완료(재조회로 검증). 코드 변경: Instagram(iframe 직접 임베드)/Google Maps(URL 정규화)/embed 높이 조절 UI/embed 레거시 HTML round-trip 버그 수정, 썸네일 노출 토글, 카테고리 드롭다운(게시판별 고정 목록, `DEFAULT_POST_CATEGORIES` 폴백), 태그 "기존 태그 선택" 칩, 글 수정 admin 접근 가드 버그 수정, 글 삭제 기능 신설(API+양쪽 화면 버튼). `tsc`(0 errors) 통과. **사용자가 로그인 후 직접 확인 필요**: (1) 이미지 업로드가 에디터에 실제로 뜨는지, (2) Instagram/Spotify/Google Maps 임베드가 실제 URL로 정상 표시되는지(높이 조절 포함), (3) 카테고리/태그칩/썸네일 토글 저장이 DB에 실제로 반영되는지, (4) 글 수정 화면에서 기존 데이터가 전부 채워져 열리는지, (5) 삭제 버튼이 실제로 글을 지우는지 — 에이전트는 로그인 세션이 없어 클릭 테스트를 하지 못했다(비로그인 화면 렌더링/콘솔 에러 없음/API 200 응답까지만 확인).
- **EPIC-078 후속 3차(완료, 2026-07-31 적용됨)**: "홈페이지 페이지 수정 버튼 + 위젯 기능 필요, 노출 필터(홈 큐레이션)는 삭제" 요청 — 조사 결과 페이지 수정 버튼/위젯 기능은 EPIC-067에서 이미 홈페이지에 연결돼 있었음(라이브 DB `page_builder.slug='home'` 존재 확인). "노출 필터(홈 큐레이션)" 기능(EPIC-041)만 완전히 삭제 — `/admin/navigation/settings` 섹션, `page.tsx`의 조회/렌더링, `HomeCurationSlider.tsx` 파일까지 전부 제거. 홈페이지 콘텐츠 순서는 이제 "히어로 슬라이드쇼 → Page Builder 위젯"으로 단순화(요청하신 순서 충족). `tsc`/`lint`/`build` 모두 통과(전체 라우트 정상 컴파일 확인). **사용자가 관리자로 로그인해 홈페이지 우측 상단에 "페이지 수정" 버튼이 실제로 보이는지, 위젯을 추가/수정하면 슬라이드쇼 바로 아래에 반영되는지 직접 확인 필요**(에이전트는 로그인 세션을 못 만들어 클릭 테스트 불가 — `npm run build` 성공까지만 확인함).
- **EPIC-078 후속 2차(완료, 2026-07-31 적용됨)**: (1) 사이드바 트리거 버튼의 상시 `shadow-md`(hover 여부 무관하게 항상 켜져 있던 드롭섀도우)를 제거 — 이게 "커서를 안 올려도 사각형이 보인다"는 재신고의 실제 원인이었다. (2) 사이드바 아이콘 hover 시 20% 확대(`group-hover:scale-[1.2]`) 추가. (3) 슬라이드쇼 "여백 배경 이미지" 업로드에 압축 품질(%) 설정(`hero_slideshow.wallpaperQuality`, 기본 100=무압축, 캔버스로 원본 해상도 그대로 JPEG 재인코딩) + 슬라이드/여백 이미지 각각 작은 미리보기 썸네일 추가 — **주의**: 조사 결과 코드가 화질을 자동으로 낮춘 적은 없었음(EPIC-043의 `next/image` 전환 시도가 하이드레이션 버그로 롤백된 것과는 별개), 기존 저화질 이미지는 원본 자체가 저해상도일 가능성이 높고 이번 압축 기능은 소급 적용되지 않는다. `tsc`/`lint`(0 errors) 통과. **사용자가 관리자 로그인 후 여백 배경 이미지 새로 업로드 시 압축 품질이 반영되는지, 슬라이드/여백 이미지 미리보기 썸네일이 정상 렌더링되는지 직접 확인 필요**(에이전트는 로그인 세션을 못 만들어 클릭 테스트 불가).
- **EPIC-078 후속 핫픽스(완료, 2026-07-31 적용됨)**: 사이드바 아이콘 hover 영상에서 (1) 크롬 네이티브 PiP 버튼 노출, (2) 아이콘 주위 흰(불투명) 사각형 두 가지를 사용자가 스크린샷으로 신고. (1)은 `disablePictureInPicture`로 즉시 해결. (2)는 처음에 이미지 자체 문제로 오판했다가(원본 재확인 결과 정상), `<video>` 태그의 알파 채널 합성이 GPU 가속 디코딩 경로에서 무시되는 브라우저 한계로 최종 확인 — `<canvas>`에 프레임을 직접 그려 투명도를 보장하는 방식(`src/components/SidebarTriggerMedia.tsx` 신설, `LeftSidebar.tsx`/`RightSidebar.tsx`의 중복 코드 제거하고 공용화)으로 수정. `tsc`/`lint` 통과, dev 서버에서 canvas가 각 영상의 실제 해상도로 정상 렌더링됨을 확인. **사용자가 실제 브라우저에서 커서를 올려 사각형이 완전히 사라졌는지 최종 확인 필요**(이 세션의 Browser pane은 `@media (hover: hover)`를 만족하지 않아 실제 마우스 호버 확인 불가 — EPIC-076/078 때와 동일한 한계).
- **EPIC-078(완료, 2026-07-31 적용됨)**: "EPIC-076 CSS 호버 애니메이션 제거 + 사이드바 아이콘 기본/호버 듀얼 미디어 업로드(이미지+투명 비디오)" 요청. `site_settings.sidebar_icons`에 `leftIconDefaultUrl`/`leftIconHoverUrl`/`rightIconDefaultUrl`/`rightIconHoverUrl` 추가(구버전 `leftIconUrl`/`rightIconUrl`은 로드 시 `xxxDefaultUrl`로 1회 폴백, DB 마이그레이션 불필요). `/admin/navigation/settings`에 좌/우 x 기본/호버 4개 업로드 슬롯(이미지+`.webm`/`.mp4` 허용) 추가. `LeftSidebar.tsx`/`RightSidebar.tsx`에서 EPIC-076 아르누보 백합/양문형 문 CSS 애니메이션(및 `globals.css`의 관련 keyframe)을 완전히 삭제하고, 기본/호버 미디어를 `absolute` 겹침 + `opacity` `group-hover` 크로스페이드(`transition-opacity duration-300`)로 교체 — 확장자로 비디오/이미지 조건부 렌더링. 트리거 버튼은 `bg-transparent` 고정. **EPIC-076 배경색 커스텀 기능과 충돌 발견 후 사용자 결정**: `iconBackgroundColor`는 시각적으로 더 이상 적용하지 않되(prop 자체를 컴포넌트에서 제거), `site_settings`의 `backgroundColor` 필드와 관리자 입력 UI는 그대로 남겨둠(저장은 되지만 반영 안 됨 — 데이터 손실 방지). `tsc`/`lint`(0 errors, 새 warning 없음)/`build` 통과. Browser pane으로 라이브 DB의 구버전 `leftIconUrl` 폴백 렌더링 확인, `background-color: rgba(0,0,0,0)` 확인, 컴파일된 CSS에서 `group-hover:opacity-*` 규칙 생성 및 구버전 `rotateY`/`silo-stem-sway` 완전 제거 확인. **사용자가 직접 확인 필요**: 이 세션의 Browser pane이 `@media (hover: hover)`를 만족하지 않는 환경이라(EPIC-076 때와 동일한 한계) 실제 마우스로 호버했을 때 기본→호버 미디어 크로스페이드가 매끄럽게 보이는지, 그리고 관리자 설정 화면에서 실제 이미지/webm 파일을 업로드해봤을 때 미리보기와 저장이 의도대로 동작하는지 확인 필요.
- **EPIC-077(완료, 2026-07-31 적용됨, 3개 커밋 + 같은 날 후속 수정 1건, 전부 관리자 세션으로 직접 검증)**: "사이드바 트리거 모드 설정(호버/클릭) + 메뉴/카테고리·페이지·게시판 관리를 '사이트 구성 관리' 하나로 통합" 요청. **1/3**: `site_settings.sidebar_icons.triggerMode`(`'click'`|`'hover'`, 기본 `click`, DB 마이그레이션 불필요)를 `/admin/navigation/settings`에서 설정 가능하게 하고 `LeftSidebar.tsx`/`RightSidebar.tsx`의 `onMouseEnter`를 이 값에 따라 조건부로 바인딩. **2/3**: `boards.topic`/`boards.thumbnail_url` 신규 컬럼(`docs/sql/EPIC-077-board-topic-thumbnail.sql`, **라이브 DB에 이미 적용 확인됨** — `information_schema.columns` 조회로 확인) + `boardLayout.ts`/`/api/admin/boards*`의 3단 폴백. **3/3**: `/admin/navigation`·`/admin/pages`·`/admin/boards` 3개 목록 화면을 `/admin/site-structure`("사이트 구성 관리")로 흡수. **후속 수정(사용자 피드백)**: 1차 배포 후 사용자가 직접 확인해보니 (a) 카테고리 행 "수정"이 페이지 편집으로 안 이어지고, (b) "연결된 게시판"이 추정 매칭된 게시판의 속성만 편집 가능할 뿐 어느 게시판을 연결할지 직접 고를 수 없다는 문제 제기 — `CategoryTreeManager.tsx`에 행별 "페이지 수정"(→`/admin/pages/[id]`) 버튼 추가, `CategoryDetailModal`의 게시판 섹션을 `page_modules`(module_type='board') 행을 직접 만들고/바꾸고/지우는 드롭다운 선택형으로 재작성(`branchToBoardIds`/`buildBranchToBoardIds`는 이 과정에서 제거). **검증(관리자 세션)**: 임시 테스트 계정(`epic077-test-20260731@silostore.net`)을 만들어 사용자 제공 Supabase Management API 토큰으로 `is_admin`을 켜고 끄기를 반복하며 실제 로그인 세션으로 검증 — 3개 트리/미분류 패널(페이지 62·게시판 14) 정상 렌더링, "관리" 모달의 노출 위치/연결된 페이지/연결된 게시판 섹션 매칭, 게시판 주제 저장이 실제 DB에 반영됨(`topic` 값 직접 조회 확인), "페이지 수정" 버튼이 매칭 행에만 노출됨, 게시판 연결(드롭다운 선택 → "연결 저장")이 `page_modules` insert로 실제 DB에 반영됨, "(연결 안 함)"으로 재저장하면 그 행이 실제로 delete됨 — 전부 Management API로 DB를 직접 재조회해 확인 후 테스트 데이터/권한 원복. `tsc`/`lint`(0 errors)/`build` 통과. **미확인**: 트리 드래그 재부모화(코드 자체는 EPIC-035 이후 무변경, 저위험)와 "미분류" 패널의 드래그 재정렬 — 후자는 `boards.sort_order`/`page_builder.sort_order` 컬럼이 아직 라이브 DB에 없어(EPIC-075 SQL 미실행, 아래 항목) 지금 드래그해도 저장이 실패한다.
- **EPIC-076(완료, 2026-07-30 적용됨)**: "사이드바 아이콘 커스텀 배경색 + 아르누보 호버 모션" 요청 — `site_settings.sidebar_icons.backgroundColor`(기본 `#166534`, `/admin/navigation/settings`에서 transparent/#HEX로 자유 설정) + 우측 사이드바 "붉은 양문형 문 3D 개폐"(`rotateY`)/좌측 사이드바 "백합 개화"(`scale-110`+줄기·잎 sway keyframe) 호버 모션. 상세는 CHANGELOG.md EPIC-076 참고. **DB 변경 없음**(site_settings는 이미 존재하는 key-value 테이블, 새 컬럼 아님) — 즉시 반영. Browser pane이 스크린샷을 지원하지 않는 환경이라 컴파일된 CSS/계산된 스타일로 검증했음 — **사용자가 실제 화면에서 좌/우 사이드바 아이콘에 마우스를 올려 문 열림/꽃 개화 모션이 의도대로 보이는지, 관리자 설정에서 배경색을 바꿔 실제 반영되는지 직접 확인 필요**.
- **EPIC-075 SQL 실행 필요**: `docs/sql/EPIC-075-tree-sort-order.sql`을 Supabase SQL Editor에서 실행해야 `/admin/pages`·`/admin/boards`의 드래그앤드롭 순서 변경이 새로고침 후에도 유지됨(실행 전에는 컬럼이 없어 매번 기존 정렬로 되돌아감 — 드래그 자체는 되지만 저장 안 됨). 실행 후 관리자 로그인 세션으로 두 화면 모두 실제로 드래그해 순서가 저장/유지되는지 확인 필요(에이전트는 로그인 세션을 못 만들어 실제 드래그 클릭 테스트 불가).
- **EPIC-075(완료, 2026-07-30 적용됨)**: "관리자 CMS 트리 드래그앤드롭 + Community 게시판 1-Row 컴팩트 목록 + 게시글 상세 하단 글 목록" 요청 — 상세는 CHANGELOG.md EPIC-075 참고. **스코프 결정**: 브랜치(📂) 재분류(다른 카테고리로 옮기기)는 구현 안 함(`/admin/navigation`이 이미 담당) — 같은 브랜치 안 순서 변경만 지원. `/admin/posts`는 드래그 자체를 추가하지 않음(게시글은 시간순 콘텐츠라 수동 순서가 안 맞다고 판단).
- **EPIC-073(완료, 2026-07-30 적용됨 — 문서 전용, 코드/DB 변경 없음)**: "Documentation Architecture Refactoring" 요청 — `PROJECT_BLUEPRINT.md`를 인덱스 전용으로 재구성하고 9개 신규 루트 문서(`PROJECT_VISION.md`/`PROJECT_ARCHITECTURE.md`/`PROJECT_RULES.md`/`BOARD_SYSTEM.md`/`PAGE_BUILDER.md`/`WIDGET_SYSTEM.md`/`MEMBERSHIP_SYSTEM.md`/`POINT_SYSTEM.md`/`NAVIGATION_SYSTEM.md`)로 정보를 전부 이동. 상세(이동한 섹션/중복 발견 사항)는 세션 보고 및 CHANGELOG.md EPIC-073 참고. **참고**: 이 작업번호(EPIC-073)는 사용자가 다음 턴에서 "EPIC-074"로 다시 지칭했는데, EPIC-074/EPIC-074B는 이미 다른 완료 작업(VISION.md 신설/PROJECT_DASHBOARD.md 블록 추가)에 쓰인 번호라 — 번호 혼선이 있다는 점만 기록, 임의로 재번호하지 않음.
- **EPIC-072B(완료, 2026-07-30 적용됨 — DB 변경 없음, 즉시 반영)**: "페이지/게시판/전체 글 관리를 메뉴/카테고리 관리처럼 상위-하위 가지 형태 트리로 보여달라"는 요청 — `src/lib/adminTreeGrouping.ts` 신설(실제 `site_navigations` 트리 + `page_builder.slug`/`page_modules.board_id` 구조 매칭, 키워드 추정이던 EPIC-072의 `adminDomainGrouping.ts`보다 정확), `/admin/pages`·`/admin/boards`·`/admin/posts/*` 전부 브랜치 들여쓰기 트리로 재구성(기존 액션 버튼 그대로 유지). **사용자가 로그인 후 세 화면 모두 실제 사이트 내비게이션 순서/깊이와 일치하게 들여쓰기되는지, "기타/미분류" 버킷에 엉뚱한 게 들어가진 않았는지 직접 확인 필요**(에이전트는 로그인 세션을 못 만들어 클릭 테스트 불가 — tsc/build/lint 통과 + PostgREST 쿼리 문법만 curl로 검증함). 매칭 안 되는 페이지/게시판을 발견하면 원인은 대개 `page_builder.slug`가 `hrefToSlug(nav.href)` 규칙과 어긋나거나, 그 게시판을 참조하는 `page_modules` 행이 아예 없는 경우다.
- **EPIC-072B 후속(선택)**: `/admin/posts/*`는 여전히 "도메인 전체 최신순 20건" 서버 페이지네이션이라 한 페이지의 트리가 그 페이지에 걸린 글의 브랜치만 보여준다(완전한 트리가 매 페이지 안 보임) — 브랜치별 페이지네이션으로 바꾸려면 쿼리 전략 자체를 다시 설계해야 해 범위 밖으로 남김.
- **EPIC-074B(완료, 2026-07-30 적용됨 — 문서 전용, 코드 변경 없음)**: `docs/PROJECT_DASHBOARD.md` 최상단에 PROJECT VISION/Roadmap(Stage 1 95%, Stage 2 Ready, Stage 3~8 Waiting)/Current EPIC(EPIC-074B)/Next EPIC(EPIC-075) 블록 추가, 이 블록을 최상단에 항상 유지하는 규칙 반영. 기존 하단부의 모순되던 구식 "Current EPIC"/"Next EPIC" 섹션은 제거. **다음 EPIC 착수 시마다 이 최상단 블록의 Current EPIC/Next EPIC/Roadmap 상태를 갱신할 것** — 나머지 Progress/Recent Completed 등 하단 섹션은 여전히 stale(EPIC-068 시점), 이번 범위 밖.
- **EPIC-074(완료, 2026-07-30 적용됨 — 문서 전용, 코드 변경 없음)**: `docs/VISION.md` 신설(사용자가 전체 내용 직접 작성) — "왜 Silo Platform을 만드는가", 핵심 가치 6개(Archive First/Story First/Community First/Collection First/Long-term Preservation/Timeless Design). `CLAUDE.md` 세션 시작 시 읽기 순서 1번으로 등록, Blueprint 문서 목록에도 추가 — **앞으로 새 기능이 이 Vision과 어긋나 보이면 구현 전에 사용자에게 먼저 확인할 것.**
- **EPIC-073(완료, 2026-07-30 적용됨 — 문서 전용, 코드 변경 없음)**: `docs/STAGES.md`를 사용자가 지정한 CTO Roadmap 구조(Stage 1 Foundation ~ Stage 8 Scale Platform)로 전면 재작성 — 기존 EPIC별 진행률 표는 제거하고 새 구조로 완전 교체. 문서 끝에 CURRENT STAGE/CURRENT EPIC/NEXT EPIC 블록을 항상 유지(현재 Stage 1 — Foundation / EPIC-072 Board Management System, 다음 EPIC-073 Content Platform). **다음 EPIC 착수 시마다 이 블록을 최신 상태로 갱신할 것.**
- **EPIC-072(완료, 2026-07-30 적용됨 — DB 변경 없음, 즉시 반영)**: "페이지/게시판/전체 글 관리가 분류가 안 되어있다, 4대 도메인(사일로상점/살롱데상/스튜디오/마이페이지)으로 나뉘면 좋겠다"는 요청 — `src/lib/adminDomainGrouping.ts`(분류기) + `AdminDomainTabs`(공용 탭 UI) 신설, `/admin/pages`·`/admin/boards`에 도메인 탭 추가, `/admin/posts`는 기존 2탭(사일로상점/살롱데상)에서 5탭(물품/사일로상점 게시글/살롱데상/스튜디오/마이페이지)으로 확장. DB 마이그레이션 불필요(코드만으로 동작) — **사용자가 로그인 후 세 화면 모두 도메인 탭이 예상대로 필터링되는지 직접 확인 필요**(에이전트는 로그인 세션을 못 만들어 클릭 테스트 불가, 미인증 리다이렉트/콘솔 에러 없음까지만 확인함). 상세는 CHANGELOG.md EPIC-072 참고. 분류가 틀린 페이지/게시판을 발견하면 `src/lib/adminDomainGrouping.ts`의 매핑 테이블에 추가할 것.
- **EPIC-071-HOTFIX(완료, 2026-07-30 적용 및 사용자 검증 완료)**: `docs/sql/EPIC-071-HOTFIX-member-admin-rls.sql`(`is_current_user_admin()` SECURITY DEFINER 함수 기반 안전한 admin-bypass 정책) 사용자가 Supabase SQL Editor에서 실행 완료 — 사일로상점/살롱데상 하위 페이지 정상, `/admin/members`에서 전체 회원 목록 조회/수정 정상 동작까지 사용자가 직접 확인함(재귀 재발 없음). 상세는 CHANGELOG.md EPIC-071-HOTFIX 참고.
- **EPIC-071(완료, 2026-07-30 적용됨)**: "ethanki@silostore.net을 관리자로, 회원 정보를 보이고 수정하는 페이지를 만들어달라"는 요청 — `/admin/members` 페이지 + `GET`/`PATCH /api/admin/members[/id]` 신설, 관리자 상단 탭 추가. RLS는 위 EPIC-071-HOTFIX의 안전한 버전으로 최종 확정.
- **EPIC-070(완료, 2026-07-30 적용됨)**: dev.silostore.net에서만 자유게시판 등 게시판 위젯이 안 뜨던 문제 — jsdom(`isomorphic-dompurify`) 제거(`sanitize-html`로 교체), 게시글 API 순차 쿼리 병렬화 + 동일 게시판 중복 fetch 제거, Vercel Function Region을 Supabase와 같은 Seoul(icn1)로 통일, Vercel의 별도 `npm run lint` 배포 게이트 실패 해소(신규 strict 규칙 warning 하향 + 실제 `any` 타입 5건 수정)까지 4단계 원인을 순차로 해결. 응답시간 1.9초 → 0.16~0.25초. 상세는 CHANGELOG.md EPIC-070 참고.
- **EPIC-070 후속(선택, 저위험)**: `react-hooks/set-state-in-effect` 경고 24곳(effect 진입 시 로딩 상태를 동기 setState 후 비동기 fetch 시작하는 흔한 패턴) — 지금은 warning으로만 낮춰뒀고 배포는 막지 않음. 실제로 고치려면 각 파일마다 "loading 초기값을 render-time에 계산" 또는 "fetch 콜백 안에서만 setState"로 재구조화가 필요해 파일 수(24개)에 비례한 신중한 작업 — 필요해지면 진행.
- **EPIC-069 SQL 실행 필요**: `docs/sql/EPIC-069-hub-widget-upgrade.sql`을 Supabase SQL Editor에서 실행해야 Hub 6개(docent/community-topics/heritage/gallery/archive/membership)의 Slide 위젯 고도화와 orphan 페이지 2개(salon-gallery-awards/shop-reviews) 보강이 실제로 반영됨 — 아직 미실행. 실행 후 각 Hub 페이지와 두 orphan 페이지를 브라우저에서 열어 Slide 위젯에 실제 게시글 미리보기가 뜨는지 확인할 것.
- **EPIC-068(완료, 2026-07-29 적용됨)**: "카테고리 만들면 자동으로 페이지+위젯 템플릿 생성" 요청 — `src/app/[...slug]/page.tsx`(catch-all) + `src/lib/pageTemplates.ts`(`ensurePageForSlug`)로 메커니즘 구현, `CategoryTreeManager.tsx`의 href 저장 시점에 연결. 실제 카테고리 생성으로 브라우저에서 직접 검증(새 URL이 catch-all로 렌더링, 5위젯 템플릿 자동 생성, PageEditButton도 뜸). 상세는 CHANGELOG.md 참고.
- **EPIC-068 백필 SQL 실행 완료(2026-07-30)**: `docs/sql/EPIC-068-category-page-templates.sql`을 사용자가 Supabase SQL Editor에서 실행 완료 — 신규 게시판 30개 + 71개 페이지의 위젯 템플릿이 라이브 DB에 반영됨(anon-key REST 조회로 확인). 이제 하위 카테고리에 실제 콘텐츠가 생겼으니, 그 위에 있는 Hub 페이지(예: `/docent`, `/community/topics`)들의 단순 구성을 더 풍부한 Hero+Slide 조합으로 고도화하는 후속 작업이 가능해짐(아래 항목 참고).
- **EPIC-068 진행 중 발견(P0 dual-nav의 구체 사례, 문서화만 하고 nav는 건드리지 않음)**: 아래 항목들은 라이브 `site_navigations`의 실제 href가 이번에 만든 페이지와 다른 경로를 가리키고 있었다 — 이미 문서화된 P0("FALLBACK_NAV_TABS vs 라이브 site_navigations 이중 구조")와 같은 계열의 문제가 여러 곳에서 추가로 확인된 것:
  - "나의 아티스트 소개"/"마음일기"/"나의 보물 이야기"/"한문장 소설 프로젝트"/"비밀의 방 도슨트"(멤버십 카테고리) → 실제 href는 `/membership/*`가 아니라 `/salon/*`. 갤러리 5종(시상식/공연들/파티/운명의 방문자들/패트론들)도 `/gallery/*`가 아니라 `/salon/gallery/*`. **이 10개는 이번 EPIC에서 실제 nav가 가리키는 `/salon/*`·`/salon/gallery/*` 쪽도 동일하게 전환 완료** — `membership-*`/`gallery-*` slug 5+5는 이제 아무도 안 보는 중복이니, dual-nav 통합 논의 시 삭제 후보로 고려.
  - "할머니 Grandmas"/"할아버지 Grandpas" → 실제 href는 `/heritage/grandmas`·`/heritage/grandpas`가 아니라 `/shop/heritage/grandma`·`/shop/heritage/grandpa`(EPIC-054A가 만든 기존 정적 페이지) — 이미 `docs/PROJECT_DASHBOARD.md` P1에 기록된 기존 이슈라 이번엔 손대지 않음. `/heritage/grandmas`·`/heritage/grandpas`(이번에 위젯 적용)는 `/heritage` hub를 거쳐도 직접 링크되지 않는 orphan 상태.
  - "소개지"/"포스터"(아카이브 카테고리) → 실제 href가 둘 다 `/downloads`(자체 테이블을 쓰는 완전히 다른 기능) — `/archive/brochure`·`/archive/posters`(이번에 위젯 적용)는 nav에서 도달 불가능한 orphan 상태. `/downloads`를 게시판 위젯으로 바꿀지, nav href를 고칠지는 사용자 결정 필요(임의로 재배선하지 않음).
  - "경제"/"예술"/... 13개 클럽, "Monday Rebels"/... 7개 모임방 → 실제 href가 `/community/topics/economy`처럼 중첩된 경로가 아니라 `/community-topics-economy`처럼 대시로 이어진 flat 경로 — 이번에 추가한 catch-all(`[...slug]`)이 이 flat 경로를 그대로 slug로 받아 정상 렌더링하므로 우연히 문제없이 동작하지만, `src/app/community/topics/economy/page.tsx`(중첩 파일, 동일 slug) 자체는 이 nav로는 도달되지 않는 죽은 라우트가 됨 — 마찬가지로 dual-nav 통합 논의 대상.
- **EPIC-067-HOTFIX(완료, 2026-07-29 적용됨)**: "Board를 제외한 Gallery/Latest Posts Slider/Timeline/Video 위젯이 전혀 렌더링되지 않는다"는 보고를 조사 — 실제 클릭 테스트 결과 Gallery/Timeline/Slide는 게시판을 선택하면 정상 동작함을 확인(결함 아님, 게시판 미선택 상태의 안내 문구를 오인했을 가능성). 실제 결함은 Video 위젯 하나뿐이었다 — `url` 미설정 시 아무 안내 없이 `return null`(Board/Gallery/Timeline과 달리 위젯 추가 시 설정 화면이 자동으로 안 열려 더 눈에 띄지 않았음). `VideoWidget.tsx`에 placeholder 추가로 수정 완료.
- **EPIC-067-HOTFIX 후속(선택)**: `ImageWidget.tsx`/`AudioWidget.tsx`도 `VideoWidget.tsx`와 동일하게 `url`이 비었을 때 조용히 `null`을 반환하는 동일 패턴일 가능성이 높다(이번 핫픽스 지시 범위(Gallery/Slider/Timeline/Video)에 없어 손대지 않음) — 필요하면 동일한 safe-render placeholder를 적용할 것.
- **EPIC-067-HOTFIX 추가(완료, 2026-07-29 적용됨)**: `/treasures` 실사용 중 "URL을 넣었는데도 영상이 로드 안 된다" 재보고 — 운영자가 유튜브 워치 URL을 넣었는데 `VideoWidget.tsx`가 항상 `<video src>`로만 렌더링해 재생 불가(`MEDIA_ELEMENT_ERROR`)였던 것이 원인. 유튜브/비메오 URL을 감지해 `<iframe>` embed로 변환하는 `toEmbedUrl()`을 추가해 수정, 직접 업로드한 mp4 등은 기존 `<video>` 그대로 유지. `/treasures`에서 실제 유튜브 URL이 정상 재생됨을 확인.
- **EPIC-067-HOTFIX 추가 후속(참고)**: `ImageWidget`/`AudioWidget`은 유튜브/비메오 같은 "페이지 URL vs 파일 URL" 구분이 필요 없는 단순 미디어(이미지/오디오 파일)라 이번 embed 변환 로직 대상이 아니다 — 위 항목(url 미설정 시 safe-render)과는 별개 사안.
- **EPIC-067(완료, 2026-07-29 적용됨)**: EPIC-066이 발견한 "79개 페이지가 `PageEditButton`만 있고 `PageBuilderRenderer`를 호출하지 않는" 결함의 Phase 1 — 핵심 도메인 12개(`/`, `/shop`, `/shop/[id]`, `/docent/[id]`, `/boards/[id]`, `/clubs`, `/clubs/[id]`, `/rental`, `/rental/[rentalTypeId]`, `/downloads`, `/attendance`, `/u/[memberId]`)를 형제 페이지와 동일한 `fetchPublishedPageBySlug`+`<PageBuilderRenderer>` 패턴으로 전환 완료. 기존 핵심 콘텐츠(구매/예약/글 목록 등)는 그대로 두고 그 아래에 위젯 영역을 이어붙이는 방식(콘텐츠 대체가 아님) — 상세는 CHANGELOG.md EPIC-067 항목 참고.
- **EPIC-067 후속(남은 66개, Phase 2 규모)**: 아래는 `grep -rL "PageBuilderRenderer" $(grep -rl "PageEditButton" src/app --include=page.tsx)`로 재현되는 나머지 목록(Phase 1의 12개 제외) — 우선순위와 통합 방식(콘텐츠 대체 vs 병존)을 사용자와 다시 정해야 한다:
  - **정적 PageHeader류 — 남은 8개(EPIC-068에서 24개 전환 완료, 아래만 남음)**: `salon/docent-tour`, `salon/drinks`, `space-inquiry/{item-rental,shoot-rental,styling}`, `studio/{items,rental,styling}` — 나머지(`salon/{artist-intro,mind-diary,one-sentence-novel,my-treasure-story,secret-room,monthly-events}`, `salon/gallery/*5`, `membership/*5`, `gallery/*5`, `archive/*3`)는 EPIC-068에서 순수 위젯 페이지로 전환 완료(위젯 실연결은 `docs/sql/EPIC-068-category-page-templates.sql` 실행 후).
  - **마이페이지 12개 탭(레이아웃이 별도 처리, 병존형)**: `mypage/{badges,bucketlist,collections,collections/[category],comments,docent-certificate,exhibition,follow,salon,space,timeline,visitors,wishlist}` — `mypage/page.tsx`(인덱스)는 이미 완전 대체형으로 전환됨(EPIC-060/061), 나머지 12개 탭은 각자 실데이터 목록이 있어 Phase 1과 동일한 병존형 패턴이 맞을 것.
  - **인증/설정 폼(위젯 조합 부적합, 확인 필요)**: `login`, `signup`, `settings`, `me`, `me/write` — 애초에 위젯 조합형 페이지가 아니라 EPIC-066 당시에도 "전수 변환보다 우선순위 대상 결정"이 필요하다고 기록됨. Page Builder 대상에서 아예 제외할지 사용자와 결정할 것.
  - **동적 이름별/게시판 하위 라우트**: `community/club/[name]`, `heritage/grandma[s]/[name]`, `heritage/grandpa[s]/[name]`, `boards/[id]/write`, `boards/[id]/[postId]`, `boards/page.tsx`(디렉토리 — 이미 자체 `PageModuleRenderer`(EPIC-054C, DB `page_builder`와 무관한 별도 compile-time 모듈 시스템)로 여러 Board를 배치 중이라 두 렌더러를 한 페이지에서 같이 쓰는 방식을 별도로 설계해야 함, Phase 1에서 의도적으로 제외).
  - **기타**: `docent/collections`, `docent/library`, `shop/heritage/{grandma,grandpa}`, `shop/projects`, `shop/projects/[id]`, `polls`.
- **EPIC-066 추가(완료, 2026-07-29 적용됨)**: `boards` 테이블 신규 admin 컬럼(`is_public`/`group_key`/`render_type`/`default_card_type`/`use_search`/`use_like`/`use_comment`/`use_view_count`/`default_page_size`/`default_sort`/`description`/`widget_settings`) + admin 쓰기 RLS 정책(`boards_admin_write`)을 사용자가 Supabase SQL Editor에서 직접 실행 완료 — anon-key REST 조회로 `is_public=true`/`default_page_size=24`/`default_sort='latest'` 기본값이 기존 게시판에 정상 반영됨을 확인함(`group_key`/`render_type`은 기존 게시판에 대해 의도대로 null, 오버라이드 없음).
- **EPIC-066 추가 후속**: Survey/Calendar/Application 3개 Board Type은 사용자 확인 하에 스텁으로만 구현됨(안내 배너 + 기존 글 목록) — 실제 투표 집계(Survey), 날짜별 보기(Calendar), 신청서 제출/심사 흐름(Application)은 각각 새 데이터 모델/스키마 설계가 필요한 별도 EPIC 규모 작업. 필요해지면 요구사항부터 사용자와 다시 정의할 것.
- **EPIC-066 추가 후속**: 새 게시판 관리(`/admin/boards`)로 생성한 게시판은 전부 `boards.board_type='topic'`(가장 보편적인 전체 등급 글쓰기 허용)으로 생성된다 — patron/artist_promo 같은 특수 쓰기 권한이 필요한 게시판은 여전히 DB에서 직접 `board_type`을 지정해야 한다(관리자 UI는 "Board Type"이라는 이름으로 렌더링 축(`render_type`)만 다루고, 권한 축(DB `board_type`, `src/lib/serverAuth.ts`)은 의도적으로 노출하지 않음 — 두 축이 이름은 비슷해도 완전히 다른 개념이라 혼동 주의).
- **EPIC-066 추가 후속**: `/admin/boards`(게시판 생성/수정/복제/삭제/미리보기/검색·필터 UI)는 관리자 로그인 세션이 필요해 에이전트가 자격 증명을 직접 입력하지 않는 정책상 클릭 테스트를 하지 못했음(`npx tsc --noEmit`/`npm run lint`만 통과 확인) — 사용자가 직접 로그인 후 (1) 새 게시판 생성 → Board Type을 바꿔가며 폼 우측 미리보기가 즉시 바뀌는지, (2) 목록에서 검색/Category/Board Type 필터, (3) 복제 버튼으로 전 필드가 복사되고 슬러그만 자동으로 유니크하게 생성되는지, (4) 삭제 버튼 → 확인 모달 → 실제 삭제(글이 있는 게시판은 안내 메시지와 함께 막히는지), (5) 수정 화면의 "미리보기" 버튼이 실제 게시판 화면을 새 탭으로 여는지 확인 필요.
- **EPIC-066 추가 후속**: `/boards`(게시판 디렉토리) 페이지 수준의 Category/Board Type 필터(이전 EPIC-066 항목, 아래 참고)는 여전히 미구현이지만, 이번 추가 작업으로 `boards.group_key`/`render_type` 컬럼이 생겨(DDL 실행 후) 그 필터를 구현할 기반은 마련됐다.
- **EPIC-066 후속(EPIC-067 Phase 1로 부분 해결됨)**: 감사 결과 발견된 "79개 페이지가 `PageEditButton`만 달려 있고 `PageBuilderRenderer`를 호출하지 않는" 결함 — 위 EPIC-067 항목에서 핵심 도메인 12개를 전환 완료, 나머지 66개는 위 "EPIC-067 후속(남은 66개, Phase 2 규모)" 목록 참고.
- **EPIC-066 후속**: Filter 요구사항 중 Tag/Year는 실제로 구현했지만 Category/Board Type 필터는 시간 제약으로 못했다 — 둘 다 게시글이 아니라 게시판 단위 속성이라 `/boards`(게시판 디렉토리) 페이지 수준에서 "어떤 게시판을 보여줄지" 거르는 형태가 맞을 것 같다(개별 Board Widget 안에서 필터링할 대상이 아님).
- **EPIC-066 후속(참고)**: 감사 중 `page_modules`에 board_id가 비어있는 위젯 2개(`salon-gallery-awards`의 Gallery 위젯, `shop-heritage-grandma`의 Board 위젯)를 추가로 발견 — 세션 중 관리자가 직접 "+ 위젯 추가"로 새로 만든 것으로 보여(실사용 확인!) 임의로 게시판을 연결하지 않고 그대로 뒀다. `/admin/pages`에서 게시판을 마저 선택해주면 된다.
- **EPIC-065 후속(완료, EPIC-066에서 검증됨)**: 관리자 세션이 실제로 열려 있어 Visual Widget Builder를 직접 클릭 테스트했다 — Board Widget Inspector 6개 토글, Live Preview 실데이터 반영, BoardEmptyState 문구 전부 실제 화면에서 확인 완료.
- **EPIC-065 후속(알려진 제한)**: Board Widget의 "썸네일" 토글은 story 레이아웃(`StoryThumbnailModule`)에만 적용된다 — gallery/hub 레이아웃은 이미지가 레이아웃 자체의 핵심이라 토글 대상에서 제외했다. 이 두 레이아웃까지 썸네일을 끌 수 있어야 하면 별도 작업 필요.
- **EPIC-065 후속(알려진 제한)**: Survey 위젯은 여전히 순수 표시용 데모다(EPIC-054B 때부터의 제한 계승) — 실제 투표 저장/집계 기능은 없다. 실제 투표가 필요하면 `/polls`처럼 별도 백엔드가 필요하다.
- **EPIC-064A 후속(완료, 2026-07-29 적용됨)**: `docs/sql/EPIC-064A-page-builder-backfill.sql`을 사용자가 제공한 Supabase Management API 토큰으로 직접 실행 완료 — `page_builder`가 9개 → 126개 행으로 늘어나 이제 `PageEditButton`이 걸린 모든 Route에서 관리자에게 버튼이 뜬다. 적용 중 추가로 발견: `treasures`(사일로 보물들)는 애초에 `page_builder` 행 자체가 없었다(당초 이미 있다고 잘못 판단해 backfill SQL에서 빠뜨렸음) — 별도 INSERT로 즉시 수정, 최종 126/126 slug 확인됨. 대부분의 페이지는 여전히 `page_builder`를 직접 읽지 않는 하드코딩 렌더링이라, 버튼을 눌러 `/admin/pages`에서 모듈을 구성해도 실제 화면엔 반영되지 않는다(`PageBuilderRenderer`를 쓰는 페이지만 해당).
- **EPIC-064A 후속(선택)**: `/admin/**` 9개 페이지(navigation, navigation/settings, pages, pages/[id], payments, posts, posts/salon, posts/shop, projects/new)에는 의도적으로 `PageEditButton`을 붙이지 않았다 — 이미 `admin/layout.tsx`가 관리자 가드를 하고 있고 CMS 도구 자체(예: `/admin/pages/[id]`는 Page Builder 편집기 그 자체)라 버튼의 목적과 안 맞는다고 판단했다. 만약 사용자가 이 판단에 동의하지 않으면(예: "모든 Route"를 문자 그대로 admin 포함으로 원한다면) 별도로 알려주면 추가할 것.
- **EPIC-063 (완료, 2026-07-29 적용됨)**: `docs/sql/EPIC-063-nav-audit-fix.sql` 실행 완료 — "입양신청서 라이브러리" href가 `/shop`으로 연결됨(실제 의도된 목적지가 다르면 site_navigations 해당 행의 href만 바꾸면 됨).
- **EPIC-063 후속(P2, 문서 부채)**: `docs/EPIC.md`의 "완료" 목록이 EPIC-056에서 멈춰 있고 EPIC-057~062(Route 51개 신설, Page Builder CMS 신설, Fallback 제거, Page Architecture 전환)의 상세 요약이 누락돼 있었음 — EPIC-063 작업 중 발견, 커밋 로그 기준 한 줄 placeholder만 추가해뒀다. 각 EPIC의 상세 요약을 커밋 diff 기준으로 소급 작성하는 별도 작업 필요.
- **EPIC-063 후속(참고)**: `/community/topics`·`/community/weekday`·`/treasures` 등 여러 Hub Page가 `page_builder`에 published 모듈이 없어(또는 하위 세부 slug만 채워져 있어) "이 페이지에는 아직 배치된 모듈이 없어요" EmptyState로 보인다 — 이번 EPIC은 Navigation/Page 라우팅·UX만 다루고 콘텐츠 시딩은 범위 밖("Board를 수정하지 않는다" 지시). 콘텐츠 채우기는 `/admin/pages`에서 관리자가 직접 모듈을 배치하면 된다.
- **EPIC-056 후속**: `/shop`(사일로상점)·`/docent`(온라인 도슨트)·마이페이지 전체는 이번 EPIC에서 Board Module 조합으로 재구성하지 않았다 — 전부 `posts`/`boards` 테이블이 아닌 별도 데이터 도메인(items/docent_contents/개인 데이터)의 성숙한 기존 화면이라, 강제로 재구성하려면 새로운 어댑터/기능이 필요해 범위 밖이었다. 지시문 예시의 "사일로 보물들"/"보물 목록"/"온라인 도슨트"/"My Page" 조합을 실제로 구현하려면, 이 페이지들을 Board Module 체계로 옮길지 여부를 사용자와 먼저 결정해야 한다(데이터 모델이 근본적으로 다름).
- **EPIC-056 후속**: `/studio`의 Calendar Module은 이번 달력만 보여주고 실제 예약/대관 일정(예: `rental_bookings` 테이블의 예약된 날짜)과는 연동되지 않았다 — 실 데이터 연동은 새 기능이라 범위 밖. 필요해지면 `markedDates` prop에 실제 예약된 날짜를 채우는 조회를 추가할 것.
- **EPIC-056 후속**: `HeroModule`이 자동 생성하는 breadcrumb의 중간 항목(부모 hub 이름)은 링크가 없는 plain text다 — 부모 hub의 실제 board id를 모르기 때문(현재 `resolveBoardDefinition`은 slug만 알고 id는 모름). 클릭 가능한 링크로 만들려면 `/api/boards`를 한 번 더 조회해 부모 id를 찾는 로직이 필요.
- **EPIC-055 후속**: `/community/club/[name]`은 `useBoardIdByName`으로 board 행의 `name` 컬럼과 URL의 이름을 정확히 문자열 일치시켜 찾는다 — 라이브 DB에 클럽/주제 게시판 20개(EPIC-049)가 시딩되면(`SALON_TOPIC_BOARD_NAMES`/`SALON_WEEKDAY_CLUB_NAMES`, `src/lib/navConfig.ts`) 각 board의 `name` 컬럼 값이 이 배열의 문자열과 정확히 같아야 정상 연결된다 — DB 시드 작성 시 철자/공백까지 동일한지 확인할 것.
- **EPIC-055 후속**: `/heritage/grandma|grandpa/[name]`은 이제 전체가 공유하는 `grandmas`/`grandpas` 스토리 게시판에 연결되므로, 실제로 시딩되면 모든 이름별 URL이 "같은 게시판"(개인별로 필터링되지 않은 전체 글 목록)을 보여준다 — 특정 이름의 글만 자동으로 필터링하려면 검색어를 URL에서 자동으로 채워주는 기능이 필요한데, 이는 "새 기능 금지"였던 EPIC-055 범위 밖이라 구현하지 않음(사용자가 직접 검색창에 이름을 입력해야 함). 필요해지면 별도 EPIC에서 검토.
- **EPIC-054F 후속(P2)**: legacy `BOARD_DEFINITIONS.archive`(그룹, "자료게시판")와 신규 `INDIVIDUAL_BOARD_DEFINITIONS.archive`(hub, "Archive")가 동일한 slug `"archive"`를 쓴다 — `resolveBoardDefinition`이 category(개별 정의)를 board_type(그룹)보다 먼저 확인해 지금은 동작이 깨지지 않지만, 두 서로 다른 개념이 같은 slug를 공유하는 건 혼동 여지가 있다. `/archive` 신규 페이지가 실제로는 legacy "자료게시판"을 보여준다(신규 Archive hub 행이 아직 DB에 없어서). Board Definition System 자체를 고치는 일이라 "새 게시판/DB 변경 금지"였던 EPIC-054F 범위 밖 — slug 재명명 또는 병합 여부를 사용자와 논의할 것.
- **EPIC-054F 후속**: Community/Heritage/Studio/Membership/Gallery 5개 신규 페이지는 해당 hub의 board 행이 라이브 DB에 아직 시딩되지 않아 전부 EmptyState("게시글이 없습니다.")로 보인다 — EPIC-048~051의 DB INSERT를 Supabase SQL Editor에서 실행하면(NEXT_TASK.md 기존 항목 참고) 실제 콘텐츠로 채워진다.
- **EPIC-054F 후속**: nested hub(`salon-topics`/`salon-weekday`/`survey`)는 전용 top-level Route를 만들지 않았다 — Community 허브 페이지의 "하위 게시판" 카드를 통해서만 접근 가능. 직접 URL(`/salon-topics` 등)이 필요해지면 별도 페이지 신설을 검토할 것.
- **EPIC-054E 후속(해결됨, 2026-07-30 병합 완료)**: `feature/EPIC-053`(Block Editor 시스템 확장/완성)이 오랫동안 이 브랜치 계보와 완전히 별개인 미병합 브랜치로 남아있던 문제 — `develop`으로 정식 병합 완료(충돌 11개 파일 수동 해소: BoardRenderer는 develop의 EPIC-066 Registry 아키텍처를 유지하고 `featured_image_url` 우선순위만 `StoryThumbnailModule`/`GalleryModule`에 포팅, PostDetailHeader/posts API/write·detail 페이지는 양쪽 신규 기능을 함께 유지). `npm install`/`tsc`/`build`/`lint` 전부 통과(lint는 기존 29-error/2-warning 베이스라인과 동일, 신규 이슈 없음). `docs/STAGES.md` Stage 1 "Block Editor" 항목도 완료로 갱신 완료.
- **EPIC-054E 후속**: `docs/PROJECT_DASHBOARD.md`/`docs/STAGES.md`는 EPIC-054D까지의 상태를 기준으로 작성됐다 — 다음 EPIC이 완료될 때마다 두 문서를 함께 갱신하는 습관이 자리 잡을 때까지(특히 "Recent Completed"/"Progress"/"Current EPIC" 필드) 누락 여부를 한동안 주의 깊게 확인할 것.
- **EPIC-054D 후속 — Architecture Review(P0)**: `navConfig.ts`의 `FALLBACK_NAV_TABS`(EPIC-044 재작성, `/heritage/*/[name]`·`/community/club/[name]` 동적 구조)와 라이브 `site_navigations`(DB 시드, `docs/navigation-blueprint.md` 반영분)가 서로 다른 nav 트리로 공존한다 — 어느 쪽을 "진짜" 구조로 삼아 일원화할지 사용자와 결정 필요. 이는 실질적 nav 재설계라 EPIC-054D(감사 전용) 범위 밖.
- **EPIC-054D 후속(P1)**: 위 이중 구조의 직접적 결과로 중복 콘텐츠 쌍이 존재 — `/shop/heritage/grandma|grandpa`(정적, EPIC-054A) vs `/heritage/grandma|grandpa/[name]`(동적, EPIC-044), `/space-inquiry/styling`(정적) vs `/shop/projects`(완전 구현, EPIC-016), `/salon/{one-sentence-novel,mind-diary,artist-intro,my-treasure-story,secret-room,gallery/*}`(정적 PageHeader) vs 동일 주제의 실제 게시판(Board Definition System, EPIC-050). 위 P0 결정과 함께 통합/역할 분리 여부를 논의할 것.
- **EPIC-054D 후속(P1)**: Breadcrumb가 사이트 전체에서 자동 생성되지 않는다 — 19개 정적 페이지(`PageHeader`)만 손으로 breadcrumb를 입력하고, 나머지 51개 페이지는 breadcrumb 자체가 없다. 손으로 입력한 19개 중 다수도 "Membership"/"Gallery" 같은, 실제 nav 트리에는 없는(Board Definition System의 hub 그룹명일 뿐인) 중간 계층을 표시한다 — 클릭 가능한 잘못된 경로는 아니지만(plain text) nav 구조와 불일치. `getActiveNavTabKey`(활성 탭 판정)를 거꾸로 이용해 pathname → nav 트리 경로를 자동 도출하는 공용 breadcrumb 생성기를 만드는 방향을 검토할 것 — 다만 이는 사이트 전체에 새 기능을 추가하는 작업이라 "Audit만 수행" 범위인 EPIC-054D에서는 만들지 않았다.
- **EPIC-054D 후속(P1)**: `Navbar.tsx`에 반응형 브레이크포인트(`sm:`/`md:`/`lg:`)가 전혀 없다 — 데스크톱/태블릿/모바일에서 동일한 레이아웃(고정 높이 슬라이드 사이드바, hover 기반 드롭다운)이 그대로 쓰인다. 모바일 hover가 불안정한 환경(터치 기기)에서 드롭다운 진입이 어려울 수 있음 — 모바일 전용 UX(예: 햄버거 메뉴)를 만드는 것은 새 디자인 추가라 이번 감사에서는 구현하지 않음, 별도 EPIC 필요.
- **EPIC-054D 후속(P2)**: 페이지별 SEO metadata(title/description/OG/canonical)가 아직 없다 — root `layout.tsx`의 기본값만 전 페이지가 상속한다. 70개 페이지 각각에 의미 있는 title/description을 붙이는 작업이 선행돼야 `alternates.canonical`도 페이지별로 채울 수 있다.
- **EPIC-054D 후속(P2)**: `Navbar.tsx`의 순수 CSS(`group-hover`/`group-focus-within`) 드롭다운은 Escape로 닫을 수 없다(포커스를 다른 곳으로 옮겨야 닫힘) — LeftSidebar/RightSidebar처럼 Escape 핸들러를 붙이려면 JS state가 다시 필요한데, 이는 EPIC-041-042-HOTFIX가 의도적으로 피한 구조라 이번 EPIC에서는 되돌리지 않았다. 필요성이 확인되면 별도로 재검토할 것.
- **EPIC-054D 후속(P3)**: `next.config.ts`에 `experimental.reactCompiler`가 꺼져 있어(eslint-config-next의 React Compiler 대비 lint 규칙만 활성) `Navbar.tsx`를 포함한 모든 Client Component가 수동 메모이제이션 없이 렌더링된다 — 실제로 활성화하면 상당수의 수동 `useMemo`/`useCallback` 필요성이 사라지므로, 켤지 여부와 부작용(레거시 훅 패턴과의 충돌 등)을 별도로 검토할 것.
- **EPIC-054C 후속(중요)**: Board 계열 4종(Story/Gallery/List/Slide Board)만 실제 Board와 연결됐다 — `hero`/`notice`/`form`/`calendar`/`survey`/`ranking`/`profile_card` 등 나머지 모듈은 여전히 어떤 실제 Page에도 연결되지 않았다. 이 모듈들을 실제로 어디에 배치할지(예: 홈 히어로를 `hero` 모듈로 이관, 마이페이지에 `ranking`/`profile_card` 연결 등)는 사용자와 논의 후 별도 EPIC으로 진행할 것.
- **EPIC-054C 후속**: `/boards/page.tsx`의 "게시판 허브" 바로가기 카드 그리드와 레거시 8개 그룹(자유게시판/패트론 라운지 등) 링크 목록은 이번 EPIC에서 Board Module로 바꾸지 않고 그대로 유지했다(순수 내비게이션 링크 목록이지 게시글 목록이 아니므로) — 필요하면 각 그룹도 `list_board`/`story_board` 모듈로 교체해 실제 게시글 미리보기를 보여주는 방향을 검토할 것.
- **EPIC-054C 후속**: `Page 하나 = Board 하나 구조를 만들지 않는다`는 요구는 `BoardModule`이 boardId 하나로 자기완결형이라는 점 + `/boards/page.tsx`가 hub당 하나씩 최대 8개의 `slide_board` 모듈을 배치한다는 점으로 구조적으로 증명했지만, 라이브 DB에 아직 hub 게시판이 시딩되지 않은 상태라(NEXT_TASK.md EPIC-048~051 항목 참고) 실제로 여러 Board가 동시에 데이터를 보여주는 화면은 DB 시드 실행 후에만 눈으로 확인 가능하다.
- **EPIC-054C 후속**: `slide_board` 모듈은 `BoardRenderer`의 `HubView`(정확히는 `boardType==="hub"`)에 위임하는데, `HubView`가 내부적으로 그리는 3개 `FeedSlide`(최신글/인기글/추천글)는 여전히 export되지 않은 `BoardRenderer.tsx` 내부 함수다 — 향후 "Slide Board" 모듈에 개별 슬라이드 단위 커스터마이징(제목 변경 등)이 필요해지면 `FeedSlide`를 export하거나 `HubView`에 옵션을 추가하는 리팩터가 필요.
- **EPIC-054C 후속**: 모듈을 추가/삭제/순서 변경하는 관리자 UI(드래그앤드롭 등)는 만들지 않았다 — `PAGE_MODULE_LABELS`(사람이 읽는 라벨)만 준비해뒀다. 실제 편집 UI가 필요해지면 `src/components/admin/CategoryTreeManager.tsx`(dnd-kit 드래그앤드롭, EPIC-035)의 기존 패턴을 참고할 것.
- **EPIC-054C 후속**: Notice/Form/Calendar/Survey/Ranking/Profile Card 6개 모듈은 여전히 데이터 조회 없는 순수 셸이다 — 실제로 페이지에 쓰려면 caller가 실데이터를 props로 채워야 한다. Ranking은 특히 EPIC-052 후속 항목(아래)과 동일하게 "집계 가능한 공개 뷰가 없다"는 근본 제약이 그대로 남아있다.
- **EPIC-054A 후속**: 이번 EPIC은 "게시판 연결 금지/기능 추가 금지" 범위였으므로, 19개 정적 페이지(`PageHeader` 기반)는 여전히 실제 데이터가 없다 — 특히 `/salon/gallery/*` 5개는 이미 Board Definition System의 `Gallery` hub(EPIC-050, `/boards/[id]`)에 동일 주제의 실제 콘텐츠가 존재하므로, 정적 페이지를 해당 게시판으로 리다이렉트하거나 안내 링크를 붙일지 사용자와 논의 후 진행할 것.
- **EPIC-054A 후속**: `navConfig.ts`의 `FALLBACK_NAV_TABS`(EPIC-044~053에서 갱신된 Board/Heritage/Community 동적 라우트 구조)는 실제 사용자에게 보이는 라이브 nav(`site_navigations` DB 시드, EPIC-023 기준)와 여전히 다르다 — 이번 EPIC은 라이브 nav 기준으로만 placeholder를 정리했고, 이 nav-wiring 불일치 자체는 건드리지 않았다(기존에 기록된 사안, EPIC-044/047 후속 항목과 동일). `/heritage/grandma/[name]`, `/heritage/grandpa/[name]`, `/community/club/[name]`, EPIC-047~053의 개별 `/boards/[id]` 게시판들은 전부 실제 페이지이지만 여전히 라이브 nav에서 진입점이 없다.
- **EPIC-052 (중요)**: `member_bucket_list` 테이블을 Supabase SQL Editor에서 실제로 실행해야 함(에이전트는 Management API 토큰 없이는 직접 적용하지 않음 — CLAUDE.md 규칙). `docs/database-schema.sql`의 "EPIC-052" 주석 아래 DDL 참고. 실행 전까지 `/mypage/bucketlist`에서 항목 추가 시 에러가 남.
- **EPIC-053.1 (가장 중요, 반드시 먼저)**: `docs/sql/epic-053-1.sql`을 Supabase SQL Editor에서 실행해야 실제로 동작한다 — Management API 토큰이 이번 세션엔 없어 에이전트가 직접 적용하지 못했다(CLAUDE.md 규칙). 포함 내용: `member_bucket_list` 테이블+RLS(select/insert/update/delete, own-row), `posts.body_json`/`featured_image_url`/`featured_image_path` 컬럼, `posts.visibility` CHECK에 `'draft'` 추가, `image_cleanup_queue` 테이블+RLS, `post-images`/`gallery`/`attachments` Storage Bucket 생성+Policy(공개 읽기/로그인 사용자 업로드/관리자 또는 업로드 본인 삭제). 실행 전까지: `/mypage/bucketlist` 추가 시 에러, 새 글의 JSON Block이 저장되지 않고 HTML만 저장(POST 라우트가 42703에 자동 폴백), 대표 이미지가 저장되지 않음, 이미지 업로드가 버킷 없음 에러로 실패.
- **EPIC-053.1 후속**: 이미지 Garbage Collection은 큐에 적재하는 것까지만 구현했고, 실제 삭제는 관리자가 `POST /api/admin/storage-cleanup`을 호출해야 한다(한 번에 최대 50건) — 자동으로 도는 스케줄러는 아직 없다. 정기적으로 비우려면 이 라우트를 예약 작업(Scheduled Task)으로 admin 세션을 만들어 주기적으로 호출하는 방식을 검토할 것.
- **EPIC-053.1 후속**: `/api/boards/[id]/drafts`(서버 사이드 임시 저장)는 여전히 어떤 화면에서도 호출하지 않는 죽은 코드다 — localStorage 자동 저장(PostForm.tsx)만 실제로 쓰인다. 필요 없으면 삭제하고, 필요하면 PostForm에 연결할 것.
- **EPIC-053.1 후속**: `GET /api/boards/[id]/posts`는 게시판의 모든 글을 한 번에 가져와 라우트 핸들러에서 검색/정렬/페이지네이션을 처리한다(EPIC-047부터 있던 기존 설계) — 게시판 하나가 1만 건을 넘어가면 이 방식이 실제 병목이 된다. DB 쪽 페이지네이션(`.range()`)+검색 인덱스로 옮기는 리팩터가 필요.
- **EPIC-053.1 후속**: Google Maps 임베드는 사용자가 붙여넣은 URL이 실제 `/maps/embed?...` 형태라고 가정한다 — 일반 `maps.google.com` 공유 링크를 붙여넣으면 Google이 iframe 임베드를 막아 빈 화면이 나올 수 있다. 공유 링크 → 임베드 URL 자동 변환은 구현하지 않음.
- **EPIC-053.1 후속**: Version History(수정 이력)는 이번 범위에 없지만 구조적으로는 쉽다 — `posts.body_json`이 JSONB이므로 `post_revisions(post_id, body_json, author_id, created_at)` 테이블을 만들고 PATCH 라우트에서 업데이트 직전에 스냅샷 한 줄만 insert하면 된다.
- **EPIC-052 후속**: "받은 배지"의 "전체 랭킹"/"다음 배지 진행률"은 구현하지 않음 — 랭킹은 `member_badges`가 본인 행만 읽는 RLS라 다른 회원과 비교 집계하려면 공개 집계 뷰(예: `docent_content_popularity`처럼 `grant select`된 뷰)가 새로 필요하고, 진행률은 배지 획득 조건을 정의하는 규칙 테이블 자체가 없어(스키마 추측 금지) 계산 근거가 없다. 두 기능이 정말 필요하면 먼저 배지 규칙(조건/등급 체계)을 사용자와 정의해야 한다.
- **EPIC-052 후속**: "나의 도슨트 수료증"은 `docent_purchases`(결제 확정)를 "수료"로 재해석했을 뿐, 실제 강의 진행률/모듈 개념은 없다 — 진짜 "진행률"이 필요하면 도슨트 콘텐츠에 모듈/챕터 개념을 추가하는 별도 스키마 설계가 선행돼야 함.
- **EPIC-052 후속**: "나의 공간"의 공간 스타일링 신청 내역은 표시하지 않음(고객 신청을 기록하는 테이블이 없음, `styling_projects`는 관리자 포트폴리오 전용) — Studio 게시판 링크로만 안내. 실제 신청 내역이 필요해지면 고객 신청 테이블 설계가 먼저 필요.
- **EPIC-052 후속**: "타임라인"은 `points_ledger`+`likes`+`member_follows`만 조합 — "도슨트"/"배지" 활동은 `points_ledger`에 대응하는 `reason` 값이 없어 타임라인에 포함되지 않는다(예: 도슨트 구매는 `shop_purchase`와 구분되지 않음, 배지 부여는 포인트 적립을 동반하지 않음). 필요하면 `points_ledger.reason`에 `docent_purchase`/`badge_granted` 같은 값을 추가하는 스키마 확장을 검토할 것.
- **EPIC-052 후속**: Tiptap 에디터가 저장하는 HTML은 `src/lib/sanitize.ts`의 화이트리스트(`p/br/strong/em/s/u/h1-3/ul/ol/li/blockquote/a/code/pre`)만 허용 — 이미지/표/코드 하이라이팅 등 더 풍부한 서식이 필요하면 Tiptap 확장 추가 + 화이트리스트 확장이 함께 필요(허용 태그만 늘리고 확장을 안 붙이면 에디터에서 애초에 만들 수 없음).
- **EPIC-052 후속**: 기존에 plain text로 저장된 글(EPIC-052 이전)은 여전히 `PostBody.tsx`가 태그 유무로 자동 판별해 줄바꿈만 살려 보여준다 — 일괄 HTML 변환 마이그레이션은 하지 않았음(필요성 낮다고 판단, 필요해지면 별도 스크립트로 처리).
- **EPIC-052 후속**: 마이페이지 허브(`/mypage/page.tsx`)는 카드 그리드(대표 카드) 그대로 유지했고, 지시문이 요구한 "최근 활동/인기 콘텐츠/진행 현황/배지"를 탭마다 요약해 보여주는 리치 대시보드까지는 구현하지 않음(12개 탭 각각에 대한 요약 쿼리를 새로 설계해야 하는 큰 작업이라 이번 EPIC 범위에서 제외) — 필요하면 별도 확인 후 진행.
- **EPIC-051 (중요)**: 신규 게시판 5개(`boards` insert)를 Supabase SQL Editor에서 실제로 실행해야 함(에이전트는 Management API 토큰 없이는 직접 적용하지 않음 — CLAUDE.md 규칙). `docs/database-schema.sql`의 "EPIC-051" 주석 아래 INSERT 문 참고. 실행 전까지는 Studio hub와 4개 서비스 게시판이 어디에도 보이지 않는다(DB에 행이 없으니 정상).
- **EPIC-051 후속**: "문의하기"/"예약하기" 버튼이 지금은 4개 게시판 전부 같은 href로 연결된다(별도 "단순 문의" 채널이 없어서) — 실제로 문의와 예약을 구분된 흐름으로 나누고 싶으면, 문의 전용 폼/연락 채널을 먼저 설계해야 한다(스키마 추측 금지 원칙상 이번 EPIC에서 임의로 만들지 않음).
- **EPIC-051 후속**: "물품 대여"(`rental`)와 "공간 스타일링"(`styling`)의 신청 버튼은 여전히 `/space-inquiry/item-rental`·`/space-inquiry/styling` **placeholder(ComingSoon) 페이지**로 연결된다 — 이 두 페이지 자체는 아직 미구현이라(이전부터 있던 상태, 이번 EPIC 범위 아님), 실제로 신청까지 되게 하려면 그 페이지들의 구현이 먼저 필요하다.
- **EPIC-051 후속**: "공간 스타일링" 게시판의 "대표 프로젝트" CTA는 `/shop/projects`로 연결되지만, 그 페이지에서 다시 이 Studio 게시판으로 돌아오는 링크는 없다(단방향) — 양방향으로 연결하고 싶으면 `/shop/projects`(또는 `[id]` 상세)에 "Studio 공간 스타일링 게시판" 링크를 추가하는 별도 작업이 필요(새 페이지 수정이라 이번 EPIC 범위 밖).
- **EPIC-051 후속**: hub 슬라이드 썸네일(`HubFeedItem.photo_url`)은 이번에 추가돼 Silo Store/Online Docent/Heritage/Community/Membership/Gallery/Archive hub에도 소급 적용됨 — 해당 게시판들에 `photo_url` 있는 글이 이미 있다면 이제 카드에 썸네일이 보일 것(의도된 개선, 회귀 아님).
- **EPIC-050 (중요)**: 신규 게시판 17개(`boards` insert)를 Supabase SQL Editor에서 실제로 실행해야 함(에이전트는 Management API 토큰 없이는 직접 적용하지 않음 — CLAUDE.md 규칙). `docs/database-schema.sql`의 "EPIC-050" 주석 아래 INSERT 문 참고. 실행 전까지는 Membership/Gallery/Archive 영역 게시판이 어디에도 보이지 않는다(DB에 행이 없으니 정상).
- **EPIC-050 후속(중요, 보안 관련)**: "패트론 게시판"(`patron-board`)의 실제 잠금은 `src/lib/serverAuth.ts`의 `canReadBoard`/`canWriteToBoard`가 `resolveBoardDefinition(board).accessLevel === "patron"`을 확인하는 방식으로 구현됨 — DB 시드 실행 전에는 이 게시판 자체가 존재하지 않으니 테스트 불가하고, 실행 후에는 반드시 비패트론 계정으로 읽기(403)와 글쓰기(403) 둘 다 직접 확인할 것(로그인 세션 관련 정책상 에이전트가 직접 검증 못함).
- **EPIC-050 후속**: "비밀의 방 도슨트"(`secret-room-docent`)는 `accessLevel:"secret_room"`만 지정돼 있고 실제 인가 로직은 아직 없음 — 지금은 다른 일반 게시판과 동일하게 전체 공개 상태다. 실제 "비밀의 방" 개념(기존 `salon_rooms`/`salon_room_access`, 시험 통과 여부)과 연결할지, 아니면 단순 패트론 등급 게이팅으로 충분한지 사용자와 논의 후 `serverAuth.ts`에 분기를 추가할 것.
- **EPIC-050 후속**: "Gallery" 5개 게시판(시상식/공연들/파티/운명의 방문자들/패트론들)은 기존 `/salon/gallery/{awards,performances,parties,visitors,patrons}` ComingSoon 페이지와 동일한 주제를 다룬다 — 새 게시판이 실제 콘텐츠를 담당하게 됐으니, 그 ComingSoon 페이지들을 이 게시판으로 연결(리다이렉트 또는 안내 링크)할지 사용자와 논의 후 진행할 것(이번 EPIC은 "새 페이지 생성/수정 금지" 범위 안에서 기존 페이지를 건드리지 않음).
- **EPIC-050 후속**: Timeline Engine(`groupPostsByYearMonth`+`TimelineView`, `src/components/boards/BoardRenderer.tsx`)은 지시대로 마이페이지 타임라인 탭(`/mypage/timeline`, 현재 `PlaceholderPanel`)에서도 재사용 가능하도록 범용 필드에만 의존하게 만들어뒀다 — 실제로 그 탭에 연결하려면 두 함수를 export해서 mypage 쪽에서 import하거나, `src/lib/`로 옮겨 공유하는 리팩터링이 필요(이번 EPIC은 게시판 자체 구현까지만, mypage 연동은 하지 않음).
- **EPIC-050 후속**: "나의 보물 이야기"(`my-treasures`)는 마이페이지 "나의 컬렉션 > 나의 보물"과 실제로 연동돼 있지 않음(지시문의 "연동 가능한 구조 유지"를 문자 그대로 "구조만"으로 해석) — 실제 연동(예: 마이페이지에서 이 게시판에 바로 글쓰기 링크 제공 등)이 필요하면 별도 확인 후 진행.
- **EPIC-049 (중요)**: 신규 게시판 10개(`boards` insert)를 Supabase SQL Editor에서 실제로 실행해야 함(에이전트는 Management API 토큰 없이는 직접 적용하지 않음 — CLAUDE.md 규칙). `docs/database-schema.sql`의 "EPIC-049" 주석 아래 INSERT 문 참고. 실행 전까지는 Community/출석체크/자유게시판/주제별 소통 게시판/요일별 클럽/월별 모임/설문/공연·전시회/이벤트 공지/Q&A 고민 게시판이 어디에도 보이지 않는다(DB에 행이 없으니 정상). 반면 경제/예술/... 13개 클럽과 월~일 7개 모임방은 기존 DB 행을 재사용해서 별도 실행 없이도 이미 존재 — Community 하위 hub로의 "재분류"만 반영되면 되므로 코드 배포만으로 충분.
- **EPIC-049 후속**: "출석체크/예술가의 달력"과 "각 모임방 추가 기능"(주간 참석 신청/참석 버튼/참석 인원 표시)은 전용 UI 없이 일반 게시글로 대체 구현됨 — 실제 배지/포인트 연동이나 참석 버튼 같은 상호작용 기능이 필요해지면, Board Definition 스키마 자체를 확장(예: `checkin: boolean` 같은 새 필드)하거나 별도 컴포넌트가 필요한 사용자 확인 후 진행할 것.
- **EPIC-049 후속**: "설문 [우리들 맴]"(survey)은 하위 게시판 없이 생성됨 — 실제 설문 콘텐츠를 채우려면 (1) 개별 설문 주제마다 `INDIVIDUAL_BOARD_DEFINITIONS`에 `parent:"survey"`인 story/community 게시판 추가 + DB 시드, 또는 (2) 기존 `/polls`(poll/poll_options/poll_votes) 시스템과의 통합 여부를 사용자와 논의할 것 — 이번 EPIC은 후자를 임의로 연결하지 않음(스키마 추측 금지 원칙).
- **EPIC-049 후속**: `qna`(신규, Q&A 고민 게시판)와 기존 board_type='qna'(질문과 답변) 게시판이 이름이 비슷해 혼동 가능 — 실사용 중 둘 중 하나로 통합하는 게 낫다고 판단되면 사용자 확인 후 하나를 정리할 것.
- **EPIC-048 (중요)**: 신규 게시판 20개(`boards` insert)를 Supabase SQL Editor에서 실제로 실행해야 함(에이전트는 Management API 토큰 없이는 직접 적용하지 않음 — CLAUDE.md 규칙). `docs/database-schema.sql`의 "EPIC-048" 주석 아래 INSERT 문 참고. 실행 전까지는 Silo Store/Online Docent/Heritage 등 새 게시판이 `/boards` 디렉토리·hub 카드·검색 결과 어디에도 보이지 않는다(DB에 행이 없으니 정상).
- **EPIC-048 후속**: "보물 목록(items)"의 "카테고리 필터" 요구사항은 전용 필터 UI를 새로 만들지 않고 기존 태그+검색 기능으로 대체 구현했음(중복 컴포넌트 생성 금지 원칙) — 실사용 결과 검색창 입력만으로는 부족하다고 판단되면, `items` 게시판 전용 카테고리 드롭다운 컴포넌트 추가를 검토할 것(이 경우 "중복 컴포넌트 금지" 원칙과 충돌하니 사용자 확인 후 진행).
- **EPIC-048 후속**: 시대별 도슨트 게시판 11개(`renaissance`~`digital`)의 slug 표기가 실제 `docent_contents.era` CHECK 제약값(`neoclassic`/`empire`/`victorian` 등)과 철자가 다르다(`neoclassicism`/`regency`/`victoria`) — 이번 EPIC은 지시문에 명시된 slug를 그대로 따랐고, `docent_contents`와 실제 데이터 연동은 하지 않음(순수 커뮤니티 게시판). 향후 실제 도슨트 콘텐츠와 연결하려면 이 철자 불일치를 먼저 정리할 것.
- **EPIC-048 후속**: hub 3개(Silo Store/Online Docent/Heritage)는 `allowPosting:false`라 글쓰기가 막혀 있지만, DB 관점에서는 다른 게시판과 동일한 `boards` 행이라 `/api/boards/[id]/posts`(POST)를 직접 호출하면 서버가 403으로 막아준다(정상) — 다만 이 hub 게시판들은 애초에 존재 목적이 "집계 전용"이니, 실사용 중 어드민이 실수로 여기에 글을 쓰려 시도하는 혼란이 생기면 `/boards/[id]/write` 페이지 자체에서 hub 게시판일 때 더 명확한 안내 문구로 바꿀 것(현재는 공통 "이 게시판에는 글을 쓸 수 없어요." 메시지 그대로 재사용).
- **EPIC-047 (중요)**: `posts.view_count`/`tags`/`updated_at` 컬럼과 `post_bookmarks` 테이블을 Supabase SQL Editor에서 실제로 실행해야 함(에이전트는 Management API 토큰 없이는 직접 적용하지 않음 — CLAUDE.md 규칙). 실행 전까지는 코드의 폴백 로직 덕분에 게시판 읽기/쓰기 자체는 동작하지만, 조회수는 항상 0, 태그는 항상 빈 배열, 북마크는 503 에러로 보인다. `docs/database-schema.sql`의 posts 테이블 정의와 §14(`post_bookmarks`) 참고. `view_count` 갱신을 위해 `grant update (like_count, is_best, view_count) on posts to authenticated;`도 함께 실행해야 함(기존 GRANT 확장, 파일 하단 RLS 섹션 TODO 참고).
- **EPIC-047 후속**: 게시판 레이아웃 매핑(`adoption_story`→story, `archive`→gallery, 나머지→community)은 전용 컬럼 없이 지시문 스펙과 기존 board_type 의미를 근거로 판단한 것 — 실제 화면에서 어울리지 않으면(예: "모임별 게시판"도 사진이 많다면 story가 더 어울릴 수 있음) `src/lib/boardLayout.ts`의 `BOARD_DEFINITIONS`에서 해당 항목의 `boardType`만 수정하면 된다(레이아웃 컴포넌트 자체는 그대로 재사용).
- **EPIC-047 Part 2 후속(중요)**: 다음 EPIC부터 새 게시판을 추가할 때는 (1) `boards` 테이블에 새 board_type 시드 행 추가 (2) `src/lib/boardLayout.ts`의 `BOARD_DEFINITIONS`/`BOARD_GROUP_ORDER`에 정의 한 항목 추가 — 이 두 가지만 하면 되고 페이지/컴포넌트 코드는 건드리지 않는다. `resolveBoardDefinition()`이 board_type을 못 찾으면 조용히 "topic" 정의로 대체하니, 새 board_type을 seed했는데 화면이 이상하게 나오면 정의 추가를 빠뜨린 게 아닌지 먼저 확인할 것.
- **EPIC-047 Part 2 후속**: `BoardDefinition`의 `visibility`/`parent` 필드는 아직 어떤 코드도 참조하지 않는 예약 필드다 — "비공개 게시판" 개념이나 Heritage→Renaissance 같은 실제 계층형 hub가 필요해지면 이 두 필드를 실제로 배선하는 작업이 필요하다(현재는 8개 그룹이 전부 평면 구조).
- **EPIC-047 Part 2 후속**: `membership` 필드는 잠금 안내 문구(`RANK_LABELS[definition.membership]`) 생성에만 쓰이고, 실제 읽기/쓰기 인가는 여전히 `serverAuth.ts`의 `canReadBoard`/`canWriteToBoard`(등급별 boolean 플래그 기반)가 담당한다 — 새 게시판 정의를 추가할 때 `membership` 숫자만 바꿔서는 실제 열람 제한이 바뀌지 않으니, 진짜 등급 제한이 필요하면 `canReadBoard`/`canWriteToBoard`도 함께 확장해야 한다는 점을 놓치지 말 것.
- **EPIC-047 후속**: "추천글"은 별도 플래그 없이 기존 `is_best`(좋아요 10개 이상 승격) 재사용 — 진짜 "추천"(관리자 선정 등) 개념이 필요해지면 별도 플래그/스키마가 필요.
- **EPIC-047 후속**: 게시글 수정(edit) 기능 자체가 없어 `updated_at`은 항상 `created_at`과 동일하게 표시됨 — "수정일"이 의미를 가지려면 게시글 수정 API/UI를 별도로 구현해야 함.
- **EPIC-047 후속**: 검색/정렬/페이지네이션은 게시판 규모가 크지 않다는 전제로 매 요청마다 게시판 전체 글을 가져와 라우트 핸들러에서 처리 — 게시글이 아주 많아지면(수천 건 이상) DB 쪽 페이지네이션(LIMIT/OFFSET + 인덱스 활용 검색)으로 전환 검토.
- **EPIC-046 후속**: "태그 영역"은 게시판 카테고리/도슨트·개념글 여부를 파생 표시하는 방식으로만 구현됨(`posts`에 전용 컬럼 없음) — 사용자가 직접 자유 태그를 입력/편집하길 원하면 `posts.tags`(text[]) 컬럼 추가 + 글쓰기 폼에 태그 입력 필드가 필요한 별도 작업.
- **EPIC-046 후속**: "글 번호(No.)"는 저장 컬럼이 아니라 매 상세 조회마다 `count` 쿼리로 계산하는 파생값 — 게시글이 아주 많아지면(수만 건 이상) 매 조회마다 count 쿼리 비용이 문제될 수 있음, 필요해지면 시퀀스 컬럼(트리거 또는 identity)으로 전환 검토.
- **EPIC-046 후속**: `/boards`(게시판 목록), `/boards/[id]/write`(글쓰기)는 타이포그래피만 가볍게 맞췄고 `BoardHeader` 등 공용 컴포넌트를 적용하지 않음(글쓰기 페이지엔 애초에 헤더용 "글쓰기 버튼"이 필요 없고, 목록 페이지는 특정 게시판이 아니라 전체 디렉토리라 `BoardHeader`의 "게시판명 단수" 개념과 맞지 않음) — 필요하면 별도 컴포넌트로 통일할 것.
- **EPIC-045**: "나의 살롱"/"나의 도슨트 수료증"/"나의 공간"/"나의 전시회"/"타임라인" 5개 라우트는 여전히 `PlaceholderPanel`만 표시(EPIC-022 때부터 이어지는 미해결 항목, 데이터 소스 미지정) — 각 라우트가 독립 페이지가 됐으니 실제 기능을 붙일 때 해당 페이지 파일만 수정하면 된다(레이아웃/네비 변경 불필요).
- **EPIC-045 후속**: `/mypage` 허브 페이지의 카드 이모지(`TAB_ICONS`)는 디자인 시스템에 없는 값이라 임의로 고른 것 — 실제 아이콘/일러스트 디자인이 필요하면 별도 확인 필요.
- **EPIC-044**: "주제별 소통게시판"(경제/예술/... 13개)이 "요일별 클럽"과 동일한 `/community/club/[name]` 라우트를 공유하도록 처리했음 — 지시문에 전용 라우트가 없어 판단한 것이라, 두 카테고리를 실제로 다른 화면/데이터로 분리해야 한다면 별도 라우트(예: `/community/topic/[name]`) 신설이 필요.
- **EPIC-044 후속**: 헤리티지(할머니/할아버지)·클럽 대량 사이드바 항목은 `navConfig.ts`의 `FALLBACK_NAV_TABS`(DB 미연결 시 폴백)에만 반영됨 — `site_navigations`가 실제로 채워져 있으면 이 폴백이 무시되므로, 운영에 반영하려면 같은 항목을 Supabase `site_navigations`에도 시딩해야 함(Management API 토큰 없어 미실행).
- **EPIC-044 후속**: `UniversalBoard`는 검색/정렬 UI와 목록 렌더링 뼈대만 구현 — `posts`를 실제로 채우는 조회 API/테이블이 아직 없어, 헤리티지/클럽 상세 페이지는 항상 "아직 게시글이 없어요" 상태로 보임. 실제 데이터가 필요해지면 카테고리별 게시글 테이블 스키마 설계부터 필요.
- **EPIC-043 후속 (중요)**: `HeroSlideshow.tsx`에 `next/image`(`fill`)를 적용하면 이 Next 16 + Turbopack 개발 환경에서 Navbar를 포함한 클라이언트 전체가 hydrate되지 않는(콘솔/서버 로그에 에러 없이 상단 탭이 완전히 빈 채로 굳는) 회귀가 재현됨을 확인했다 — `priority`/`sizes`/`quality`/`className`을 다 빼고 `<Image fill src=... />` 딱 하나만 남겨도 재현되고, `.next` 캐시 삭제+서버 재시작+새 탭에서도 동일. 근본 원인은 특정하지 못해 일단 원래 `<img>`/CSS `backgroundImage` 구현으로 되돌려뒀다. 실제로 next/image 최적화가 필요하면: (1) `npm ls next`로 정확한 버전을 확인하고 해당 버전의 알려진 이슈를 검색, (2) 프로덕션 빌드(`npm run build && npm run start`)에서도 같은 문제가 재현되는지 확인(dev 전용 버그일 가능성), (3) React DevTools 등으로 hydration 에러를 더 정확히 포착할 별도 세션에서 재도전할 것.
- **EPIC-043 후속**: `LeftSidebar.tsx`의 아코디언 그룹 판별(`isAccordionGroup`)이 그룹 라벨에 "도슨트"가 포함되는지로 판단하도록 완화했다 — 관리자가 완전히 다른 이름으로 바꾸면(예: "도슨트"라는 단어 자체를 빼면) 다시 매칭이 깨진다. `site_navigations`에 그룹의 안정적인 key/slug 필드가 없어 라벨 텍스트로만 판별 가능한 구조적 한계 — 필요해지면 `site_navigations`에 별도 식별자 컬럼을 추가하는 게 근본 해결책.
- **EPIC-043 후속**: 커스텀 폰트 다중 등록(`main_logo.customFonts`)은 실제 폰트 파일 업로드/적용 토글을 브라우저 자동화로 재현할 수 없어, 관리자 폼 렌더링과 `Navbar.tsx`의 `@font-face` 주입 로직만 코드 리뷰 수준으로 확인함 — 사용자가 직접 여러 폰트 URL을 등록하고 "적용"을 여러 개 켰을 때 폴백 체인이 실제로 동작하는지(첫 폰트 로드 실패 시 다음 폰트로 넘어가는지) 확인 필요.
- **EPIC-041-042-HOTFIX 후속**: 이번 세션은 Browser 창이 화면에 표시되지 않는 상태라(`screenshot`/`hover` 등 실제 커서 이동이 필요한 도구가 모두 실패) 재구현한 순수 CSS `group`/`group-hover` 드롭다운의 실제 마우스 동작(hover 열림, 완전히 벗어났을 때 즉시 닫힘, 2차 플라이아웃)을 스크린샷으로 직접 확인하지 못했다 — `npm run lint`/`npx tsc --noEmit`만 통과 확인. 사용자가 직접 상단 탭에 마우스를 올려 1차/2차 드롭다운이 뜨는지, 마우스를 빈 공간으로 치웠을 때 즉시 닫히는지, 특히 화면 오른쪽에 가까운 탭에서 2차 플라이아웃이 뷰포트 밖으로 잘리지 않는지 확인 필요.
- **EPIC-041-042-HOTFIX 후속**: `<nav>`의 가로 스크롤(`overflow-x-auto`)을 `flex-wrap`으로 바꿔, 아주 좁은 화면(탭 4개가 한 줄에 안 들어가는 경우)에서는 스크롤 대신 다음 줄로 넘어간다 — 실사용 중 이 레이아웃이 어색하면 별도 반응형 처리를 검토할 것.
- **EPIC-041-042 후속**: `src/app/page.tsx`의 홈 큐레이션은 `domain==="shop"`만 `items` 테이블을 실제로 조회하고, `salon`/`collection`/`docent` 도메인은 각 화면(예: `/clubs`, `/docent`)의 실제 테이블·컬럼을 확인하지 않은 채 추측으로 조회하지 않기 위해 더미 데이터로 대체해뒀음 — 실사용 데이터가 필요해지면 각 도메인 화면의 조회 로직을 참고해 `fetchCurationItems()`에 추가할 것.
- **EPIC-041-042 후속**: 커스텀 폰트 업로드(`main_logo.fontFileUrl`)는 실제 폰트 파일 업로드를 브라우저 자동화로 재현할 수 없어 코드 리뷰 수준으로만 확인함 — 사용자가 직접 `.woff`/`.ttf` 파일을 업로드해 로고 좌/우 텍스트에 실제로 적용되는지 확인 필요.
- **EPIC-041-042 후속**: 사이드바 아이콘 크기(`iconSizePx`) 필드도 같은 이유로 실제 값 변경 후 아이콘 크기가 바뀌는지 직접 확인 필요.
- **EPIC-040 후속**: 2차 플라이아웃(`group-hover:block absolute left-full`)은 화면 오른쪽 끝에 가까운 탭(예: 맨 오른쪽 탭)에서 열리면 뷰포트 밖으로 잘려 나갈 수 있음 — 현재는 항상 오른쪽으로만 펼쳐짐. 실사용 중 잘림 현상이 발견되면 뷰포트 우측 여백을 계산해 필요 시 왼쪽으로 펼치는 로직을 추가할 것.
- **EPIC-039 후속**: `sidebar_icons`는 새 `site_settings` 키라 라이브 DB에 해당 row가 없으면(최초 1회) 조회는 정상 동작하되(빈 값으로 처리) 관리자 화면에서 저장 시 `site_settings` 테이블 자체는 이미 있으므로(EPIC-026) upsert가 그대로 새 row를 만든다 — 별도 DDL 불필요, 확인만 하면 됨.
- **EPIC-039 후속**: `main_logo`의 `align`/`extraText`/`textPosition` 필드는 이제 UI에서 노출되지 않고 렌더링에도 쓰이지 않는 죽은 필드가 됐다(구버전 데이터 호환을 위한 타입 정의 및 1회 마이그레이션 용도로만 존재). 사용자가 확인 후 완전히 무의미하다고 판단되면 별도 정리(스키마/타입에서 제거) 작업을 검토할 것 — 이번 EPIC 범위 밖.
- **EPIC-039 후속**: `link` 타입 상단 탭(마이페이지)은 하위 그룹/아이템이 데이터 모델에 없어 hover 시 드롭다운을 띄우지 못하고 색상 힌트만 적용했음 — "모든 상위 탭 hover 시 하위 메뉴 노출"을 문자 그대로 마이페이지에도 적용하려면 `site_navigations`에 마이페이지 하위 항목(예: 마이페이지/설정/로그아웃 등)을 추가하는 별도 데이터 작업이 필요함(추측으로 만들지 않음).
- **EPIC-039 후속**: `HeroSlideshow.tsx`의 wallpaper 랜덤 선택(`Math.random()`)을 `useEffect`+`setState`로 구현해 `react-hooks/set-state-in-effect` 위반이 1건 늘었음(26→27, 이미 저장소 전반에 퍼진 동일 클래스 pre-existing 이슈와 같은 성격) — 아래 "보류 중인 P2 이슈"와 함께 전체 훑는 리팩터링 시 같이 처리할 것.
- **EPIC-039 후속**: Wallpaper 다중 업로드/사이드바 아이콘 업로드는 실제 파일 선택 다이얼로그를 자동화로 조작할 수 없어 코드 리뷰 수준으로만 확인함 — 사용자가 직접 `/admin/navigation/settings`에서 배경 이미지 여러 개를 업로드하고 슬라이드쇼가 "contain" 모드로 전환될 때 배경이 무작위로 바뀌는지, 사이드바 아이콘이 실제로 🔑/🚪 이모지를 대체하는지 확인 필요.
- **EPIC-037 후속**: 팝업이 탭 바로 아래(`top: rect.bottom`)에 붙어 있어 마우스가 버튼→팝업으로 자연스럽게 이동하면 계속 열려 있지만, 버튼에서 팝업을 거치지 않고 다른 곳으로 이동하면(예: 옆의 다른 탭으로 곧장 이동하지 않고 화면 아무 곳이나 이동) 팝업이 "hover 상태"로 남아있다가 실제로 팝업 영역을 스치기 전까지 안 닫히는 경우가 이론상 있을 수 있음(기존 dropdown 타입 코드의 동일한 설계를 그대로 계승). 사용자가 실사용 중 불편함을 느끼면 팝업 컨테이너에 약간의 patding/여유 공간을 두거나 hover-intent 딜레이 로직 추가를 검토할 것.
- **EPIC-036 후속**: `package.json`에 `@dnd-kit/core`/`@dnd-kit/sortable`/`@dnd-kit/utilities`(EPIC-035)가 선언돼 있었으나 `node_modules`에 실제로 설치돼 있지 않아 `/admin/navigation`(`CategoryTreeManager`) 전체가 컴파일 에러로 로드되지 않는 상태였음 — 이번 EPIC 작업 중 `npm install`로 해결. 이 저장소를 여러 기기/세션에서 오가며 작업할 때는 `git pull` 후 `npm install`도 함께 실행할 것(그렇지 않으면 lockfile에는 있지만 로컬 `node_modules`에는 없는 패키지가 생길 수 있음).
- **EPIC-036 후속**: `hero_slideshow.wallpaperUrl` 업로드/저장 자체는 로고·슬라이드 이미지와 동일한 `uploadImage()` 경로를 재사용해 구현했지만, 실제 파일 첨부→업로드→`objectFit="contain"` 상태에서 배경으로 렌더링되는지는 브라우저 자동화로 실제 파일 선택 다이얼로그를 조작할 수 없어 코드 리뷰 수준으로만 확인함 — 사용자가 직접 `/admin/navigation/settings`에서 이미지 채움 방식을 "원본 모두 보이게(contain)"로 바꾸고 Wallpaper 이미지를 업로드해 홈 히어로에서 여백이 채워지는지 확인 필요.
- **EPIC-022 후속**: `member_collections`는 anon key REST 조회(`200 []`)로 라이브에 실제로 존재함을 EPIC-029 작업 중 확인함 — `docs/database-schema.sql` 상단 동기화 노트("아직 라이브 미적용")가 이 테이블에 한해 stale함. `member_follows`/`member_badges`/`member_visitors` 3개는 미확인 상태이므로 여전히 Supabase SQL Editor에서 실제 적용 여부 점검 필요(에이전트는 Management API 토큰 없이는 직접 적용하지 않음 — CLAUDE.md 규칙). 문서 상단 노트 자체의 정정은 이번 EPIC 범위(수정 대상 파일 제한) 밖이라 별도 작업 필요.
- **EPIC-022 후속**: "나의 살롱"/"나의 도슨트 수료증"/"나의 공간"/"나의 전시회"/"타임라인" 5개 탭의 데이터 소스 결정 필요(현재 Empty State만 존재).
- **EPIC-022 후속**: `member_follows`(팔로우 버튼)/`member_visitors`(방문 기록 insert) 쓰기 경로 구현 필요 — 아마도 `/u/[memberId]` 페이지에 추가.
- **EPIC-023 후속**: `docs/database-schema.sql` §12의 `site_navigations`/`site_categories` Seed는 라이브 DB에 적용되어 있음(EPIC-027 검증 중 실제 탭 이름 수정→저장→새로고침→Navbar 반영까지 확인 완료).
- **EPIC-023 후속**: `site_categories`가 아직 실제 화면(`/shop` era 필터, `/docent` era 필터, `CollectionsPanel` 카테고리 탭 등)에는 연결되지 않음 — 이번 EPIC은 테이블/시드/관리자 CRUD까지만. 각 화면의 하드코딩된 카테고리 목록을 `site_categories` 조회로 바꾸는 작업은 별도 Epic 필요.
- **EPIC-026 후속**: `docs/database-schema.sql` §13에 정의된 `site_settings` DDL+Seed를 Supabase SQL Editor에서 실제로 실행해야 함 — 실행 전까지 `/admin/navigation/settings`는 조회 시 "Could not find the table" 에러 배너가 뜨지만 폼 자체는 기본값으로 정상 동작함.
- **EPIC-026 후속**: (EPIC-032에서 해결됨 — `main_logo`/`hero_slideshow`는 Navbar/홈페이지에 연결 완료. `home_curation`은 이번 EPIC 범위 밖이라 여전히 미연결.)
- **EPIC-028 후속**: (EPIC-031에서 해결됨 — `is_hidden` 전용 컬럼으로 교체, 아래 EPIC-031 항목 참고)
- **EPIC-028 후속**: (EPIC-031에서 해결됨 — 20건 단위 페이지네이션 구현, 아래 EPIC-031 항목 참고)

- **EPIC-030**: 작업 지시문이 "프로젝트 제목(title)" 필드를 요구했으나 실제 `styling_projects` 스키마/Blueprint에는 `title`이 없고 `client_name`+`industry`가 그 자리를 대신함(EPIC-016에서 의도적으로 설계) — 사용자 확인 결과 기존 필드 유지로 결정. 만약 향후 정말 범용 "제목" 개념이 필요해지면 스키마 변경(별도 Epic)이 필요.
- **EPIC-031 후속**: `docs/database-schema.sql`의 `posts.is_hidden` DDL을 Supabase SQL Editor에서 실제로 실행해야 함(에이전트는 Management API 토큰 없이는 직접 적용하지 않음 — CLAUDE.md 규칙). 실행 전까지 `admin/posts/salon`에서 숨기기 토글 시 `is_hidden` 컬럼이 없다는 에러가 날 수 있음.
- **EPIC-031 후속**: `admin/posts/shop`(물품 게시글 관리)은 이번 EPIC 범위(수정 대상 파일이 `salon/page.tsx`로 한정)에 포함되지 않아 여전히 `.limit(200)`과 무관하게 동작하지만, "숨기기" 관련 로직은 애초에 없어 영향 없음 — 페이지네이션이 필요해지면 별도 확인 필요.
- **EPIC-032 후속**: `site_settings` 테이블이 라이브 DB에 아직 없어(EPIC-026 후속 DDL 미실행) 현재는 Navbar 로고/홈 히어로 모두 항상 fallback UI로 보임 — 위 EPIC-026 DDL+Seed를 Supabase SQL Editor에서 실행하고 `/admin/navigation/settings`에서 실제 값을 저장해야 실제 연동 화면을 확인할 수 있음.
- **EPIC-032 후속**: `home_curation`(도메인/슬러그/정렬 기준) 설정은 이번 EPIC 범위 밖이라 여전히 홈페이지에 연결되지 않음 — 필요해지면 별도 Epic에서 처리.
- **EPIC-033 후속**: Supabase Storage `public-assets` 버킷이 실제로 생성되어 있는지, public read 정책이 설정돼 있는지 확인 필요 — 버킷이 없으면 관리자 화면에서 파일 업로드 시 에러가 남(에이전트는 Storage 버킷 생성 권한/Management API 토큰이 없어 직접 만들지 않음). Supabase Dashboard → Storage에서 `public-assets` 버킷을 생성하고 공개 읽기로 설정할 것.
- **EPIC-033 후속**: `site_settings` 자체가 아직 라이브 DB에 없어(EPIC-026 후속 DDL 미실행), 새로 추가된 `heightPx`/`autoAdvanceSeconds`/`objectFit` 필드도 DDL 실행 전까지는 저장할 곳이 없음 — EPIC-026 DDL을 먼저 실행해야 이번 EPIC의 저장 기능 전체가 동작함.
- **EPIC-034 후속**: 같은 이유로 `align`/`extraText`/`fontFamily`/`bold`/`fontSizePx`도 `site_settings` DDL 실행 전까지는 저장할 곳이 없음.
- **EPIC-034 후속**: 로고 정렬(`align`)은 지시문이 "헤더 컨테이너"에 justify를 걸라고 했지만, 그대로 하면 우측 계정 영역(로그인/마이페이지)까지 로고 옆으로 끌려와 레이아웃이 깨짐 — 로고+추가텍스트만 감싸는 별도 flex-1 컨테이너에 적용하도록 범위를 좁혀 구현. 만약 계정 영역까지 포함해 완전히 헤더 전체를 재배치하고 싶다면 별도 지시 필요.
- **EPIC-035 후속**: `site_navigations.topic`/`thumbnail_url`/`description`/`is_public` 4개 컬럼을 Supabase SQL Editor에서 실제로 실행해야 함(에이전트는 Management API 토큰 없이는 직접 적용하지 않음) — 실행 전까지 `/admin/navigation`의 [관리] 저장 시 컬럼이 없다는 에러가 날 수 있음. 아래 ALTER TABLE 참고.
- **EPIC-035 후속**: (EPIC-035-Fix에서 해결됨 — `top-tabs`/`sidebar-left`/`sidebar-right` 3개 페이지를 삭제하고 `/admin/navigation`(`CategoryTreeManager`) 하나로 일원화. 아래 EPIC-035-Fix 항목 참고.)
- **EPIC-035-Fix 후속**: `admin/navigation/shared.tsx`의 `NavNodeEditor`/`CategoryRowEditor`/`NavRow`/`TargetType`/`TARGET_TYPE_OPTIONS`가 삭제된 3개 페이지에서만 쓰이던 export라 이제 죽은 코드가 됨(`DOMAIN_OPTIONS`/`inputClass`/`primaryButtonClass`/`smallButtonClass`/`CategoryDomain`은 `settings/page.tsx`가 계속 사용해 남겨둠) — 이번 작업 범위(수정 대상 파일 미포함)라 정리하지 않았음, 필요하면 별도 정리 필요.
- **EPIC-035-Fix**: `admin/layout.tsx`의 최상위 관리자 탭에도 "메뉴·카테고리 관리" 링크가 여전히 `/admin/navigation`(구 top-tabs 리다이렉트 대상이 삭제됨에 따라 지금은 통합 관리 화면)로 잘 연결되는지 확인함 — 별도 수정 불필요.
- **EPIC-035 후속**: `CategoryTreeManager`의 드래그앤드롭은 dnd-kit의 다중 컨테이너(부모별 자식 목록) 패턴으로 구현 — 형제 순서 변경과 다른 항목으로의 재부모화는 지원하지만, "루트 레벨로 다시 꺼내기"는 최상위 컨테이너(`list-root`)로 직접 드롭해야 함(빈 공간이 아니라 최상위 항목들 목록 자체로 드롭). 사용성이 불편하면 명시적인 "최상위로 이동" 버튼 추가를 검토할 것.
- **EPIC-035 후속**: `public-assets` Storage 버킷 존재 여부는 EPIC-033 후속 항목과 동일 — 아직 미확인.
- **EPIC-034-Ext 후속**: `textPosition`/`textCustomFont`도 `site_settings` DDL 실행 전까지는 저장할 곳이 없음(EPIC-026 후속과 동일 사유).
- **EPIC-034-Ext 후속**: `Graphire`/`Primor` 실제 폰트 파일이 없어 `globals.css`의 `@font-face`가 주석 처리된 상태 — 폰트 파일을 확보하면 `public/fonts/`에 추가하고 경로를 채운 뒤 주석 해제할 것. 그 전까지는 브라우저가 serif로 자동 대체하므로 화면이 깨지진 않음.
- **환경 메모**: Next.js(Turbopack)는 같은 프로젝트 디렉토리에 대해 dev 서버를 동시에 두 개 띄울 수 없다(`.next/dev/logs`의 락으로 감지, 포트를 바꿔도 무관하게 즉시 종료됨) — 이 저장소를 여러 세션이 동시에 작업할 때, 다른 세션이 이미 `npm run dev`를 띄워둔 상태라면 이번 세션에서는 로컬 브라우저 검증이 불가능하다. 사용자가 직접 다른 세션의 dev 서버를 내리거나, 그 세션에서 검증을 요청해야 함.

## 사용자 확인 필요
- **EPIC-052**: DB 시드(`member_bucket_list`) 미적용 + 다른 세션의 3000번 포트 점유가 겹쳐 있어(환경 메모 참고), Tiptap 에디터 실제 타이핑/서식 적용, 상세 페이지 HTML 렌더링, 버킷리스트 추가/체크/삭제, 나의 살롱/도슨트 수료증/공간/전시회 패널의 실데이터 표시, 원글 이동 링크를 브라우저로 확인하지 못함(type-check/lint만 통과 확인) — 사용자가 직접 로그인 후 (1) 아무 게시판에 글쓰기로 들어가 굵게/목록/링크 서식이 적용되는지, 저장 후 상세 페이지에 서식이 그대로 보이는지, (2) `docs/database-schema.sql`의 EPIC-052 DDL 실행 후 `/mypage/bucketlist`에서 항목 추가·체크·삭제, (3) `/mypage`의 살롱/도슨트 수료증/공간/전시회/타임라인/내가 쓴 댓글(원글 이동) 탭이 실제 데이터를 보여주는지 확인 필요.
- **EPIC-051**: DB 시드 실행 + 다른 세션의 3000번 포트 점유 둘 다 겹쳐 있어(환경 메모 참고) Studio hub와 4개 서비스 게시판, "문의하기"/"예약하기"/"대표 프로젝트 보기" 버튼이 실제로 올바른 페이지로 이동하는지, hub 슬라이드 카드에 대표 이미지가 보이는지 브라우저로 확인하지 못함(type-check/lint만 통과 확인) — 사용자가 직접 `docs/database-schema.sql`의 EPIC-051 INSERT 실행 후 `/boards` → "게시판 허브"에서 Studio 진입 → 1F/2F 대관·물품 대여·공간 스타일링 게시판 각각 열어 버튼 이동과 콘텐츠(공간 소개/이용 안내 등 직접 작성 필요)를 확인 필요.
- **EPIC-050**: DB 시드 실행 + 다른 세션의 3000번 포트 점유 둘 다 겹쳐 있어(환경 메모 참고) Membership/Gallery/Archive 3개 hub와 그 하위 게시판, 그리고 "타임라인" 게시판의 연/월 그룹핑 렌더링을 브라우저로 확인하지 못함(type-check/lint만 통과 확인) — 사용자가 직접 `docs/database-schema.sql`의 EPIC-050 INSERT 실행 후 (1) `/boards` → "게시판 허브"에서 Membership/Gallery/Archive 진입 → 하위 게시판 카드 확인, (2) "패트론 게시판"을 비패트론 계정으로 열람 시도해 실제로 막히는지 확인, (3) "타임라인" 게시판에 글을 몇 개 써서 연/월별로 올바르게 묶여 보이는지 확인 필요.
- **EPIC-049**: DB 시드 실행 + 다른 세션의 3000번 포트 점유 둘 다 겹쳐 있어(환경 메모 참고) Community hub 구조(Community → 출석체크/자유게시판/주제별 소통 게시판(hub)/요일별 클럽(hub)/월별 모임/설문(hub)/공연·전시회/이벤트 공지/Q&A)가 정상 노출되는지, 특히 기존 13개 클럽+7개 모임방이 `/boards` 평면 목록에서는 빠지고 `주제별 소통 게시판`/`요일별 클럽` hub 안에서는 정상적으로 보이는지 브라우저로 확인하지 못함(type-check/lint만 통과 확인) — 사용자가 직접 `docs/database-schema.sql`의 EPIC-049 INSERT를 실행한 뒤, `/boards` → "게시판 허브"에서 Community 진입 → 하위 게시판 9개 카드 → `주제별 소통 게시판`/`요일별 클럽` 재진입 → 각각의 13개/7개 자식 게시판이 예전과 동일한 URL(`/boards/[id]`)로 여전히 동작하는지 확인 필요.
- **EPIC-048**: DB 시드 실행 + 다른 세션의 3000번 포트 점유 둘 다 겹쳐 있어(환경 메모 참고) 신규 게시판 20개가 `/boards` 디렉토리의 "게시판 허브" 섹션과 각 hub의 하위 게시판 카드/피드 슬라이드에 정상 노출되는지 브라우저로 확인하지 못함(type-check/lint만 통과 확인) — 사용자가 직접 `docs/database-schema.sql`의 EPIC-048 INSERT를 실행한 뒤, `/boards`에서 "게시판 허브" 3개 카드 → 각 hub 진입 → 하위 게시판 카드 → 실제 게시판(글쓰기/좋아요/댓글/북마크/태그) 순서로 확인 필요.
- **EPIC-047 Part 2**: 다른 세션이 3000번 포트를 점유 중이라(환경 메모 참고) Board Definition System 도입 후에도 8개 게시판 그룹의 실제 동작(검색/정렬/페이지네이션 노출 여부, 좋아요/북마크/댓글/태그 노출 여부)이 이전과 동일하게 보이는지 브라우저로 직접 재확인하지 못함(type-check/lint만 통과 확인) — 코드상으로는 모든 토글이 기존과 동일한 값으로 설정돼 있어 변화가 없어야 정상. 특히 `archive`(자료게시판)만 `pageSize:20`으로 바뀌어 있으니, 한 페이지에 게시글이 20개까지 보이는지 확인 필요.
- **EPIC-047**: 다른 세션이 3000번 포트를 점유 중이라(환경 메모 참고) 검색/정렬/페이지네이션/북마크/공유/hub 피드 등을 브라우저로 직접 확인하지 못함(type-check/lint만 통과 확인) — 사용자가 직접 게시판에서 검색어 입력, 정렬 옵션 변경, 페이지 이동, 좋아요/북마크/공유 버튼, `/boards`의 최신글/인기글/추천글 섹션이 기대대로 동작하는지 확인 필요. 특히 `posts.view_count`/`tags`/`updated_at`/`post_bookmarks`를 라이브에 아직 안 올렸다면 조회수 0/태그 없음/북마크 503이 정상 동작(마이그레이션 후 다시 확인).
- **EPIC-046**: 다른 세션이 3000번 포트를 점유 중이라(환경 메모 참고) 게시판 3개 화면(목록/상세/게시판 디렉토리)의 실제 렌더링(3열 헤더, `font-serif` 제목, 대표 이미지 풀와이드, hairline 목록/댓글)을 브라우저로 직접 확인하지 못함(type-check/lint만 통과 확인) — 사용자가 직접 아무 게시판이나 열어 새 레이아웃과 좋아요/댓글/글쓰기 기존 기능이 그대로 동작하는지 확인 필요.
- **EPIC-045**: `/mypage`가 로그인 필요 페이지라 실제 로그인 세션으로 라우트 이동(허브 → 각 섹션 → 컬렉션 9개 카테고리)과 데이터 렌더링을 시각적으로 검증하지 못함(type-check/lint만 통과 확인) — 사용자가 직접 로그인 후 `/mypage` 허브 카드 클릭 → 각 섹션 이동 → `MyPageNav`/`CollectionsSubNav`의 활성 탭 표시가 URL과 맞는지, "나의 컬렉션"에서 등록/수정/삭제가 여전히 정상 동작하는지 확인 필요.
- **EPIC-044**: `/heritage/grandma/[name]`, `/heritage/grandpa/[name]`, `/community/club/[name]` 동적 라우트와 사이드바 신규 그룹("사일로 헤리티지"/"주제별 소통게시판"/"요일별 클럽") 렌더링을 브라우저로 직접 확인하지 못함(type-check/lint만 통과 확인) — 사용자가 직접 좌/우 사이드바를 열어 새 그룹이 hover로 펼쳐지는지, 항목 클릭 시 이름이 제목으로 표시되는 동적 페이지로 이동하는지 확인 필요.
- **EPIC-022**: `/mypage`가 로그인 필요 페이지라 에이전트가 실제 로그인 세션으로 탭 전환/데이터 조회를 시각적으로 검증하지 못함(정책상 실제 자격증명 입력 불가) — type-check/lint/컴파일 확인만 완료. 사용자가 직접 로그인 후 11개 탭 전환 확인 필요.
- **EPIC-029**: 같은 이유로 "나의 컬렉션" 8개 서브탭의 실제 등록/수정/삭제 CRUD 동작을 로그인 세션으로 직접 검증하지 못함(type-check/lint만 통과 확인) — 사용자가 직접 로그인 후 "+ 아이템 추가"로 신규 등록, 카드의 ✏️ 수정/🗑️ 삭제 버튼 동작을 확인 필요.
- **EPIC-032**: 로컬 3000번 포트가 이 세션 밖의 다른 프로세스로 이미 점유돼 있어 Navbar 로고/홈 히어로 렌더링을 브라우저로 직접 확인하지 못함(type-check/lint만 통과 확인) — 사용자가 직접 `/`(홈)과 임의 페이지의 Navbar 로고 영역을 확인 필요. `site_settings`가 라이브에 없는 현재 상태에서는 둘 다 fallback UI로 보이는 게 정상.
- **EPIC-033**: 같은 이유(포트 점유 + `site_settings`/`public-assets` 버킷 라이브 미적용)로 관리자 설정 화면의 실제 파일 업로드, 로고 높이/자동 전환 시간/이미지 채움 방식 저장 및 반영을 직접 검증하지 못함(type-check/lint만 통과 확인) — 사용자가 직접 `/admin/navigation/settings`에서 파일 업로드·숫자 필드·Select 동작과 Navbar/홈 반영 여부를 확인 필요.
- **EPIC-034**: 같은 이유로 로고 정렬/추가 텍스트/서체/굵기/크기 설정 저장 및 Navbar 반영을 직접 검증하지 못함(type-check/lint만 통과 확인) — 사용자가 직접 정렬을 좌/중앙/우측으로 바꿔보며 계정 영역(로그인 등)이 밀리지 않고 로고만 이동하는지 확인 필요.
- **EPIC-035**: 같은 이유(포트 점유 + `site_navigations` 신규 컬럼 라이브 미적용)로 `/admin/navigation`의 드래그앤드롭 순서/재부모화, [관리] 모달 저장(공개 설정/주제/이미지/소개), 파일 업로드를 직접 검증하지 못함(type-check/lint만 통과 확인) — 사용자가 직접 항목을 드래그해 순서/하위 이동을 시도하고, [관리] 모달에서 값 저장 후 새로고침해도 유지되는지 확인 필요.
- **EPIC-035-Fix**: 삭제된 3개 페이지 관련 링크/서브 탭이 실제로 다 정리됐는지, `/admin/navigation`이 의도대로 카테고리 통합 관리 화면으로 잘 뜨는지 브라우저로 직접 확인하지 못함(type-check/lint + grep으로만 확인) — 사용자가 직접 `/admin/navigation`과 서브 탭 2개(카테고리 통합 관리/홈페이지 설정 관리) 전환을 확인 필요.
- **EPIC-034-Ext**: 같은 이유(다른 세션의 dev 서버 점유)로 텍스트 위치 반전(`flex-row-reverse`)과 폰트 Select 동작을 직접 검증하지 못함(type-check/lint만 통과 확인) — 사용자가 직접 텍스트 위치를 좌/우로 바꿔보며 로고와의 순서가 바뀌는지, 폰트를 Graphire/Primor로 바꿨을 때 자유 입력 서체 필드가 비활성화되는지 확인 필요.

## 보류 중인 P2 이슈 (Error Triage Policy, CLAUDE.md 참고 — 사용자 지시 전까지 미수정)
- **EPIC-052**: `BucketListPanel.tsx`도 동일 클래스(`set-state-in-effect`) 위반이 1건 추가됨(26→27) — 아래 전체 훑는 리팩터링 시 같이 처리할 것.
- `npm run lint`가 프로젝트 전반(예: `AuthProvider.tsx`, `WishlistButton.tsx`, `attendance`, `clubs/[id]`, `docent/[id]`, `boards/[id]/[postId]`, `Navbar.tsx`(mounted 플래그), `admin/layout.tsx`(authorized 플래그), `admin/navigation/shared.tsx`(draft 재동기화 effect) 등 다수 파일)에서 `react-hooks/set-state-in-effect` 규칙 위반으로 실패 중 — EPIC-018 작업 범위 밖의 기존(pre-existing) 상태이며, 프로젝트 전체를 훑는 별도 작업이 필요.
