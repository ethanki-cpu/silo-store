# EPIC LIST

## 진행중
- EPIC-132 : "홈페이지 설정 관리" 4개 섹션(메인 로고/사이드바 아이콘/상단 탭 디자인/사용자 메뉴 디자인)을 craft.js 기반 클릭-선택 WYSIWYG로 전환 — 사용자가 "요소들을 클릭하면 설정창이 뜨고 자유롭게 드래그로 옮기게 해달라, craft.js 페이지 빌더처럼"이라고 명시적으로 요청(기존 3단 레이아웃/폼 방식이 "번거롭다"는 불만). EPIC-126/129/130이 세 번 명시적으로 미뤄온 "전역 크롬의 Craft 이전"을 실제로 시도 — 단, `Navbar.tsx`(모든 페이지에서 렌더링되는 실제 서비스 경로)는 코드 한 줄도 안 건드리고, 그와 별개인 독립 프레젠테이션 컴포넌트(`src/components/craft/chrome/views.tsx`)로 시각적 복제품을 만들어 그 위에만 Craft를 얹었다(Navbar.tsx 수정 시도는 CSS-only hover 메가메뉴+세션 분기가 깊게 얽혀 있어 사이트 전체 내비게이션이 깨질 위험이 커 계획 단계에서 이미 배제). 저장은 여전히 `site_settings`(craft_state 신설 안 함) — 각 블록의 `related.settings` 패널이 Craft `setProp`(캔버스 즉시 반영)과 기존 shim setter(`setMainLogo` 등, 실제 저장 버튼용) 둘 다 호출하는 이중 배선으로 "타이핑하며 실시간으로 보임"과 "저장 버튼이 여전히 정확한 값을 저장"을 동시에 만족시킨다. 신규 `ChromeCraftEditor.tsx`(전용 경량 Editor+Frame+SettingsSidebar 셸, Toolbox 없음 — 탭 추가/삭제는 여전히 `/admin/site-structure` 담당)와 로고/사이드바아이콘(좌우 고정 2개)/상단탭(site_navigations 수만큼 자동 생성)/계정메뉴 4개 블록. `tsc`/`lint`/`build` 0 errors. Browser pane 실측으로 4개 섹션 전부 클릭→설정 패널 표시→실시간 캔버스 반영→기존 폼 필드와 값 동기화까지 확인 — 도중 실측으로 발견한 실제 버그(중앙 컬럼이 우측 640px 프리뷰에 밀려 300px 미만으로 찌그러져 캔버스 텍스트가 폭 0으로 접히던 레이아웃 버그)를 고쳐 해결(마이그레이션된 4개 섹션에서는 우측 iframe 프리뷰를 숨기는 방식으로). **명시적으로 범위 밖으로 미룸(B6 컷오버)**: 기존 폼 필드 UI를 아직 제거하지 않고 새 Craft 캔버스 위에 나란히 유지 — 파일 업로드 왕복/PC·모바일 토글 전환/실제 저장 버튼 클릭까지의 전체 왕복은 이번 세션에서 실행 안 함, 다음 세션에서 확인 후 컷오버 진행 필요. 슬라이드쇼 섹션은 이번 범위 제외(이미 홈페이지 Craft 캔버스에 `HeroSlideshowWidgetBlock`으로 임베드 가능한 상태라 별도 후속). 상세는 CHANGELOG.md 동명 항목 참고. 브랜치 `feature/EPIC-124`(작업 완료 시마다 자동으로 develop/main에 fast-forward됨) — "진행중"으로 분류한 이유는 병합 여부가 아니라 B6 컷오버(옛 폼 UI 제거)가 아직 남아있기 때문.
- EPIC-115 : 어린 왕자 감성 렌더링 오버홀 + Leva 설정 패널 — EPIC-114 위에 이어서, 절차적 수채화 텍스처(캔버스 블롭+노이즈, CanvasTexture), 궤도 사진 기본 숨김(가벼운 마커로 대체), maxDistance 5000, About Me 위성 5종, 캐릭터 절차적 아이들 모션, 살롱데상 오브제 폴라로이드 통합, `leva` 설정 패널(배경/행성/캐릭터/내핵 오브젝트 실시간 제어). `tsc`/`lint` 0 errors(React Compiler가 잡은 렌더 단계 클로저 재대입 에러를 순수 해시 PRNG로 해결). 상세는 CHANGELOG.md 동명 항목 참고. 브랜치 `feature/EPIC-115`, main/develop 병합은 실사용 확인 후 진행.
- EPIC-114 : 3D 우주 감성 렌더링 강화(유튜브 배경 + 씬 재배치 + 내핵 오브젝트) — EPIC-113 위에 이어서, 몰입형 유튜브 배경(8종 무작위 자동재생), SILO/유저 행성 동시 등장 + CatmullRomCurve3 연결선 + 앉은 실루엣, maxDistance 120, 내핵 4종 형상+3D Text 라벨, `@react-three/postprocessing`(Noise+Vignette). 실측 중 CameraControls 초기화 타이밍 버그 2건 발견·수정, 행성 내부 진입 시 셸이 사라지는 메커니즘이 Three.js 기본 컬링으로 이미 올바르게 동작함을 확인. `tsc`/`lint` 0 errors. 상세는 CHANGELOG.md 동명 항목 참고. 브랜치 `feature/EPIC-114`, main/develop 병합은 실제 마우스 휠 테스트 확인 후 진행.
- EPIC-113 : /about-silo를 "어린 왕자" 감성의 3D 우주(Universe) 프로토타입으로 개편 — `three`/`@react-three/fiber`/`@react-three/drei` 신설, 피보나치 구면 배치 + 툰 셰이딩 행성 + 클릭 fly-to(`CameraControls`) + 거리 기반 LOD(궤도 사진↔사일로상점/살롱데상/온라인도슨트 노드 전환) + 유저 위성. `tsc`/`lint` 0 errors, Browser pane 실측(휠 dolly는 자동화 한계로 코드 리뷰만). 상세는 CHANGELOG.md 동명 항목 참고. 브랜치 `feature/EPIC-113`, main/develop 병합은 실제 마우스 휠 테스트 확인 후 진행.
- EPIC-112 : [Stage 2] Universal JSON Editor 도입(Tiptap + SunEditor Classic UI) — 데이터 파이프라인(JSON 정본 저장, 구형 HTML 폴백)은 EPIC-053.1부터 이미 구축돼 있어 신규 작업 없음. `BlockEditor.tsx` 툴바를 SunEditor `buttonList` 그룹핑 문법(`[...]`+`"|"`)으로 재배치(기능 삭제 없음, lucide-react 아이콘화, undo/redo 버튼 신규). `tsc`/`lint` 0 errors. 상세는 CHANGELOG.md 동명 항목 참고. 브랜치 `feature/EPIC-112`, main/develop 병합은 로그인 세션 실사용 확인 후 진행.

## 완료
- EPIC-109 : 자유 배치 이미지 리사이즈에 비율 유지/직접입력/프리셋 추가 — `ImageBlock`에 `lockAspectRatio` 토글(기존 `aspectRatio` 필드를 잠금 목표비율로 재사용) + 비율 프리셋 드롭다운(`ASPECT_RATIO_PRESETS`: 1:1/4:3/3:4/3:2/2:3/16:9/9:16/21:9/5:4) + 직접입력. `FreePositionHandles`가 컨테이너의 실제 픽셀 너비/높이로 환산해 %가 아니라 진짜 픽셀 비율을 유지하도록 확장(컨테이너가 정사각형이 아니어도 정확). `tsc`/`lint`/`build` 0 errors, 클린 세션에서 1:1 잠금 리사이즈가 픽셀 단위로 정확히 유지됨을 확인. 상세는 CHANGELOG.md 동명 항목 참고. 브랜치 `feature/EPIC-109`.
- EPIC-108 : 자유 배치(콜라주) + 홈페이지 슬라이드쇼 위젯 관리자 연동 — 사용자가 Kinfolk 실제 화면을 보여주며 요청한 "이미지 위에 이미지, 콜라주처럼 자유롭게 드래그해서 겹쳐 놓기"를 컨테이너/텍스트/이미지/버튼/영상/슬라이드쇼/게시판연동 7종 블록에 신설(`FreePosition` %좌표+`FreePositionHandles`(모서리 이동/리사이즈 핸들, 인라인 편집 안 가림)+`FreePositionSettingsSection`). `ContainerBlock`/`RootContainer`는 항상 `position:relative`가 기본이라 그 자식들이 정확히 해당 컨테이너 기준으로 겹친다. `HeroSlideshowWidgetBlock`에는 `syncWithSiteSettings` 토글 신설 — 켜면 관리자 "홈페이지 설정 관리"(`site_settings.hero_slideshow`)를 실시간으로 읽어와 그대로 반영(진짜 연동, 그 화면에서 바꾸면 이 위젯이 박힌 모든 페이지가 함께 바뀜). `tsc`/`lint`/`build` 0 errors, 임시 진단 라우트로 이미지 위 이미지 겹침/드래그 이동(20%→37.96%)/리사이즈(30%→47.96%)/실제 관리자 슬라이드 데이터 로딩까지 전부 실측 확인. 상세는 CHANGELOG.md 동명 항목 참고. 브랜치 `feature/EPIC-108`.
- EPIC-107 : 홈페이지 슬라이드쇼를 Craft 위젯으로 전환(EPIC-101~107 로드맵 완결) — 기존 "홈페이지 설정 관리"의 히어로 슬라이드쇼(`HeroSlideshow.tsx`, 크로스페이드/objectFit=contain 시 랜덤 여백 배경/4방향 여백px/자동전환)를 새로 만들지 않고 그대로 감싼 신규 `HeroSlideshowWidgetBlock`으로 Craft 캔버스 어디든 삽입 가능하게 함(`device="both"`, Native "hero" 위젯과 동일한 단순화). 설정 패널에 슬라이드/여백/objectFit/여백배경 이미지(최대 10개) 등 기존 관리자 폼 옵션을 거의 그대로 이식. `tsc`/`lint`/`build` 0 errors, 임시 진단 라우트로 실제 `HeroSlideshow`와 동일한 DOM 구조 확인. 상세는 CHANGELOG.md 동명 항목 참고. 브랜치 `feature/EPIC-107`. **이것으로 사용자가 요청한 EPIC-101~107 전체 로드맵 완료.**
- EPIC-106 : 게시판 연동 블록 카테고리 필터 — `GET /api/boards/[board_slug]/posts`에 `category`(게시글 하나하나의 자유 텍스트 분류, 게시판 자체의 라우팅 슬러그인 `board.category`와는 다른 개념) 파라미터와 `availableCategories` 응답 필드 신설(기존 `tag`/`year` 필터와 동일 패턴). `useBoardPosts.ts` 4번째 인자로 노출(하위 호환), `BoardEmbedBlock`/`TimelineEmbedBlock` 설정 패널에 게시판별 실제 카테고리 드롭다운 추가. `tsc`/`lint`/`build` 0 errors, 존재하지 않는 카테고리로 필터링 시 0건임을 확인해 파라미터가 실제로 적용됨을 검증(라이브 DB에 아직 카테고리를 채운 글이 없어 양성 매치 케이스는 데이터 생긴 뒤 확인 필요). 상세는 CHANGELOG.md 동명 항목 참고. 브랜치 `feature/EPIC-106`.
- EPIC-105 : 하단 Footer를 Craft.js 비주얼 에디터로 전환 — 기존 폼 기반 `/admin/footer`(`site_settings.footer_config`)를 다른 Craft 페이지들과 동일한 자유 레이아웃 에디터로 교체. 신규 Craft 페밀리 `FooterCompanyInfoBlock`(회사정보, 빈 필드는 공개 화면에서 자동으로 숨김)/`FooterLinksRowBlock`(items+openInNewTab, 법적/FAQ/부가링크 및 SNS 링크 공용). `GlobalFooter.tsx`(신규 Server Component, `fetchPublishedPageBySlug("footer")`)가 기존 `Footer.tsx`(삭제)를 대체해 `layout.tsx`에 마운트. `/admin/footer` 전면 재작성(`page_builder.slug='footer'` 행이 없으면 자동 생성 후 바로 Craft 에디터 오픈). 기존 `footer_config` 실데이터(회사명/대표자/이메일)를 `footerDefaultTree` 초깃값으로 마이그레이션, DB에 `page_builder(slug='footer')` 행 신규 생성. `tsc`/`lint`/`build` 0 errors, 클린 서버 재시작 후 홈/`/shop` 양쪽 실렌더링 확인. 상세는 CHANGELOG.md 동명 항목 참고. 브랜치 `feature/EPIC-105`.
- EPIC-104 : 헤더 스크롤 모션 — Kinfolk.com처럼 스크롤을 내리면 헤더가 숨고 올리면 다시 나타나는 모션. `useHideOnScroll.ts`(신규, 스크롤 방향 감지) + `Navbar.tsx`를 로고·탭 줄만 `position:fixed`+`transform`으로 전환(LeftSidebar/RightSidebar는 이미 자체 fixed라 transform 조상 밖 형제로 분리 — 안 그러면 containing block이 깨짐), `ResizeObserver`로 실측한 헤더 높이만큼 spacer로 문서 흐름 공백을 메움. **개발 중 실제로 겪은 버그**: spacer 높이 변경→스크롤바 유무 변화→헤더 줄바꿈 재계산→ResizeObserver 재발화로 이어지는 되먹임 루프가 리렌더를 멈추지 않게 돌려, 다른 데이터 fetch effect들의 cleanup이 응답 전에 계속 실행되며 메뉴/로그인 영역이 영원히 로딩 상태로 멈추는 것을 로컬에서 재현(`git stash`로 구버전과 대조해 원인 확정) — `requestAnimationFrame` + 반올림 값 비교로 루프 자체를 차단해 수정. `tsc`/`lint`/`build` 0 errors, 로컬 dev에서 scrollTo 시퀀스로 hide/show 로직과 사이드바 오픈 시 z-index stacking(헤더<백드롭<사이드바) 확인. 상세는 CHANGELOG.md 동명 항목 참고. 브랜치 `feature/EPIC-104`.
- EPIC-103 : Kinfolk 16블록 홈 레이아웃 — EPIC-102가 만든 원자 블록을 조합해 사용자가 스크린샷 14장으로 요청한 Kinfolk.com 홈페이지 1~16번 블록 구조를 실제로 재현. `SlideshowBlock`에 `sticky`(1번, 다음 블록이 스크롤로 덮어 올라오는 커튼 효과) 추가, `BoardEmbedBlock`에 `dragRow`(3rd/6th/9th, 드래그로 좌우 넘기는 목록)/`spotlight`(4th, 게시글 1개 강조) 카드 스타일 추가(드래그 로직은 `usePointerDragScroll.ts`로 공유), `ImageBlock`에 오버레이 캡션(5th/8th/10th/13th) 추가, 신규 `ShopItemsGridBlock`(12th, 가격 있는 사일로 상점 items 직접 조회 — posts에는 가격 필드가 없어서). `src/components/craft/home/defaultTree.tsx`를 16블록+뉴스레터+푸터로 재구성. `tsc`/`lint`/`build` 0 errors, 로컬 진단 라우트로 16블록 전체가 실제 DB 데이터(사일로 상점 아이템 등)까지 포함해 정상 렌더링됨을 확인(콘솔 신규 에러 없음). 홈페이지에 이미 저장된 `craft_state`가 있어 이 기본 트리가 즉시 라이브에 반영되지는 않음 — 라이브 전환 여부는 사용자 결정 대기. 상세는 CHANGELOG.md 동명 항목 참고. 브랜치 `feature/EPIC-103`.
- EPIC-102 : Craft.js 프리폼 에디터 코어 — Kinfolk 홈페이지 16블록 재현 요청을 계기로, "정해진 6~7종 섹션 블록만 추가" 방식이던 Craft 에디터를 "컨테이너 안에 원자 블록을 자유롭게 드래그앤드롭 + 개별 설정 패널 + 공통 모션"으로 전환하는 기반 작업(사용자가 5~6개 EPIC 분량 요청 중 이번 실행 범위로 EPIC-101+EPIC-102만 확정). 신규 원자 블록 8종(`src/components/craft/primitives/`): `ContainerBlock`(canvas, 다른 블록을 담는 그릇), `TextBlock`/`ImageBlock`/`ButtonBlock`/`VideoBlock`(YouTube/Vimeo/mp4), `SlideshowBlock`(auto/arrows/drag 3모드 — arrows는 이미지 좌우 클릭 네비게이션, drag는 이 레포 최초의 pointer 기반 드래그-스크롤 구현), `BoardEmbedBlock`/`TimelineEmbedBlock`(기존 `useBoardPosts` 재사용, posts에 가격 필드가 없어 썸네일/카테고리/제목/요약까지만). 이 코드베이스에 Craft.js `related` 설정 패널 패턴을 처음 도입(`SettingsSidebar.tsx`) — 원자 블록마다 `craft.related.settings`로 개별 옵션을, `MotionSettingsSection.tsx`+`useScrollReveal.ts`(IntersectionObserver)로 모든 블록 공통 스크롤 모션(fade/slide-up/slide-left)을 제공. `Toolbox.tsx`는 Craft.js 공식 `connectors.create()` 패턴으로 진짜 드래그 소스를 구현(클릭 시 ROOT 끝에 추가하는 폴백도 유지). `CraftPageEditor.tsx`에 Toolbox(좌)+SettingsSidebar(우)를 통합해 5개 페밀리(shop/docent/salon/studio/mypage)가 자동으로 새 기능을 받도록 하고, 이번에 홈페이지 전용 독자 구현이던 `CraftHomeEditor.tsx`/`CraftHomeRenderer.tsx`도 공용 셸(`CraftPageEditor`/`CraftPageRenderer`)로 마이그레이션(동작 동일, 진입점만 통일) — `homeDefaultTree`/`homeBlockOptions`를 다른 페밀리와 같은 패턴(`defaultTree.tsx`)으로 분리. `tsc`/`lint`/`build` 전부 통과(0 errors), 로컬 dev에서 공개 홈페이지 무회귀(그리드 2/3/4열 유지, 콘솔 에러 없음) 확인. **관리자 로그인 세션이 없어 Toolbox 드래그/설정 패널/모션 저장 왕복은 코드 리뷰로만 검증** — 다음 세션에서 실제 클릭 확인 필요(NEXT_TASK.md 기록). Kinfolk 16블록 조합(EPIC-103)/헤더 스크롤 모션(EPIC-104)/하단메뉴 Craft 전환(EPIC-105)/게시판 연동 다듬기(EPIC-106)/히어로 슬라이드쇼 위젯화(EPIC-107)는 이 기반 위에서 다음 세션에 순차 진행 예정(계획 파일: `C:\Users\nasdo\.claude\plans\fancy-mapping-eich.md`). 상세는 CHANGELOG.md 동명 항목 참고. 브랜치 `feature/EPIC-102`.
- EPIC-101 : 홈페이지 Craft.js 이미지 크기 버그 긴급 수정 — `CraftHomeRenderer.tsx`의 wrapper에 EPIC-100에서 3개 공용 셸에는 추가됐지만 이 렌더러에는 빠졌던 `@container` 클래스 한 줄 추가. 상세는 CHANGELOG.md 동명 항목 참고. main/develop 병합 완료.
- EPIC-099 : EPIC-098에서 미룬 3개 후속(제안서 기준) — **1/3** 뉴스레터 실제 구독 저장(`newsletter_subscribers` 테이블 신설). **2/3** Craft 에디터 Toolbox UI(복제/삭제/드래그 재정렬/"+ 섹션 추가", `query.node().toNodeTree()`가 원본과 같은 id를 반환해 삭제 시 크래시하던 버그를 `query.parseReactElement()`로 우회). **3/3** 다른 허브 페이지로 Craft 확장 — 사용자 확인 후 범위 확대(대상 5개 페이지: `/silo-store`/`/online-docent`/`/salon-des-cent`/`/studio`/`/mypage`, 페이지별 전용 블록 제작 + 사이트 구성 관리 토글 + Craft를 위젯 타입으로도 노출), 3단계로 진행: **Phase 1** 사이트 구성 관리 트리에 `builder_type`(native/craft) 토글 UI, **Phase 2** 공용 Craft 셸(`CraftPageRenderer`/`CraftPageEditor`, `src/components/craft/shared/`) 일반화 + 5개 페이지 전용 블록 각 3종(총 15종, `src/components/craft/{shop,docent,salon,studio,mypage}/`) — `/studio`/`/mypage`는 홈페이지처럼 전용 정적 라우트 파일이 있어 catch-all 등록만으로는 적용되지 않는 라우팅 버그를 발견해 각 파일에 직접 분기 추가, **Phase 3** 공용 Craft 블록 4종(Editorial Hero/Text Directory/Newsletter/Minimal Footer)을 `CraftWidgetShell`(단일 블록용 최소 Editor+Frame 컨텍스트)로 감싸 Native Page Builder 위젯 타입(`craft_hero`/`craft_directory`/`craft_newsletter`/`craft_footer`)으로도 노출(페이지 전용 블록 21종은 범위 밖, 사용자 확인). `tsc`/`lint` 매 단계 0 errors, 5개 허브 페이지 공개 렌더링 + 관리자 Craft 에디터 + Native 페이지에 Craft 위젯 추가까지 로컬 dev 실측 확인. DB 마이그레이션(`page_builder.builder_type` 전환 등)은 사용자 지시로 Management API를 통해 agent가 직접 실행. 상세는 CHANGELOG.md 각 항목 참고. 브랜치 `feature/EPIC-099`, main/develop 병합 완료.
- EPIC-098 : 홈페이지 전용 Craft.js 에디토리얼 빌더(Two-Track) — 기존 Native Page Builder(EPIC-060/065)는 다른 모든 페이지에서 그대로 유지한 채, `page_builder.builder_type`('native'|'craft')로 홈페이지 하나만 별도의 Craft.js 엔진으로 분기. 킨포크류 매거진 레이아웃(풀블리드 히어로/비대칭 스플릿/3열 에디토리얼 그리드/텍스트 디렉토리/뉴스레터/미니멀 푸터)을 저작권 문제 없이 구조·타이포그래피 리듬만 재현한 6종 블록(`src/components/craft/home/`)을 Craft.js Default State로 자동 주입, 관리자는 더블클릭으로 텍스트/이미지만 수정(`CraftHomeEditor.tsx`). `tsc`/`lint` 0 errors, 로컬 dev 관리자 세션으로 편집→저장→공개 반영 왕복 및 기존 Native 페이지 무회귀 실측 확인. 상세는 CHANGELOG.md 동명 항목 참고. 브랜치 `feature/EPIC-098`, main/develop 병합은 사용자 승인 대기 중.
- EPIC-089 : [Stage 2] Floating Action UX, YouTube-style Nested Comments, & Admin/Sidebar Control — 지시문이 P0로 지목한 "게시판 추가 버튼 누락"은 라이브 재현 결과 실제로는 정상 동작함을 확인(재구현 안 함), 대신 진짜 원인(게시글을 다른 게시판으로 옮겨 저장할 때 `posts(board_id, slug)` UNIQUE 충돌로 저장 실패)을 찾아 slug 재발급 로직으로 수정. 게시글 상세 좌측 하단 플로팅 액션 바(목록으로/좋아요/북마크/공유/댓글) 신설, "페이지 수정" 버튼을 본문 상단 좌측으로 이동. `comments.parent_id`/`comment_likes`/`public_profiles.avatar_url` 신규 스키마로 유튜브 스타일 대댓글(1단계 들여쓰기+@멘션)/좋아요/아바타 구현. `/admin/posts/common`(미분류 게시글 관리) 탭 신설, 위젯 편집기에 Board Type 인라인 드롭다운 추가, 사이드바 여닫이 아이콘 Y축 위치를 관리자 설정으로 노출(기본값도 상단으로 상향). `tsc`/`lint` 0 errors, 로컬 dev 로그인 세션으로 실측 검증. 상세는 CHANGELOG.md 동명 항목 참고. 브랜치 `feature/EPIC-089`, main/develop 병합은 사용자 승인 대기 중.
- EPIC-087 : [Stage 2] Admin Dashboard Overhaul, Tier-based Access Control, & Global UX — 9개 요구사항(2.1~2.6)을 PHASE-0~G로 분할 진행. 착수 전 3개 병렬 Explore 조사로 실제 코드 상태를 먼저 확인해, 요구사항 중 일부(게시판 슬러그 잠금·위젯 게시판 드롭다운)는 이미 의도된 설계이거나 해결된 것으로 확인해 재작업하지 않았고, "물품대여 신청 관리"·GNB 팝오버는 데이터 모델부터 새로 만들어야 함을 확인. 4개 아키텍처 결정(티어 게이트 UX 범위/게시판 D&D 범위/매트릭스 의미/스튜디오 메뉴 교체)은 사용자 확인 후 진행. **PHASE-0**: 라이브 DB 스키마 사전 확인 + `site_settings` 키(`membership_guide_redirect`/`footer_config`) 예약. **PHASE-A**: `AdminPostsBoardView.tsx` 체크박스 다중 선택+일괄 숨기기/삭제, `BoardForm.tsx`에 "이 게시판을 사용 중인 페이지" 역방향 조회. **PHASE-B**: `CategoryTreeManager.tsx`에 게시판을 트리 노드로 편입해 분기 간 완전 재배치 D&D(`page_modules` 연결 재배정) + 페이지/게시판 체크박스 일괄 삭제 + 연결된 게시판 종류/공개여부 인라인 표시. **PHASE-C**: `boards.min_rank_to_read`/`page_builder.min_rank_to_read` 신규 — 게시판은 기존 inline-lock 관례 유지, 페이지는 신규 리다이렉트 게이트(`usePageRankGate` 훅)를 catch-all + 손으로 만든 정적 page.tsx 62개(동일 템플릿, 코드모드 스크립트로 기계적 패치)에 적용, 하이브리드 페이지 7개는 의도적으로 제외. **PHASE-D**: 회원관리 컬럼 정렬, `membership_tiers` 플래그 매트릭스(신규 admin-write RLS 정책), 멤버십 안내 페이지 URL 설정. **PHASE-E**: "스튜디오 포트폴리오 등록"을 "스튜디오 대관/물품대여 신청 관리"(`/admin/rentals`)로 완전 교체, `item_rental_requests` 신규 테이블+실제 신청 폼(기존 "준비 중" 정적 안내 대체). **PHASE-F**: GNB를 `[멤버십 신청/등급] | [마이페이지] | [회원 이름] | [로그아웃]`으로 재배치, 클릭 시 멤버십 팝오버(프로필 사진/팔로우·팔로워 수/최근 활동/메시지 준비중 안내/마음일기 링크) 신규 구현(`members.avatar_url`+`avatars` 버킷 신설). **PHASE-G**: `Footer.tsx` from-scratch 구축(이전엔 사이트 전체에 footer가 없었음), `/admin/footer` 편집 화면. `npx tsc --noEmit`/`npm run lint` 매 단계 0 errors 확인, Browser pane으로 다수 단계 실측 검증(리다이렉트 게이트 왕복, footer 렌더링 등). 관리자 로그인 세션이 없어 각 admin 화면의 실제 클릭 상호작용은 다음 세션 확인 필요(NEXT_TASK.md 개별 기록). 상세는 CHANGELOG.md PHASE별 항목 참고. 브랜치 `feature/EPIC-087`, main/develop 병합은 사용자 승인 대기 중.
- EPIC-086 : Slide Module 글쓰기 버튼 — Gallery 위젯(EPIC-084)에만 있던 Contextual Write 버튼을 Slide 위젯에도 동일 패턴(`boardId` opt-in)으로 추가. 다른 컴퓨터에 남아있던 EPIC-084 미커밋 WIP를 검토해 이미 병합된 작업과 중복되지 않는 유일한 항목만 반영(나머지는 이미 EPIC-084/EPIC-084-REVISED/EPIC-085에서 더 개선된 형태로 구현돼 있었음). `tsc`/`lint` 통과, 로컬 dev(비로그인)에서 렌더링 확인. 상세는 CHANGELOG.md 동명 항목 참고. main/develop에 병합 완료.
- EPIC-084-REVISED : [Stage 2] Universal Navigation, Contextual Write Flow, 3-Column Category Selector, & Editor UX Overhaul — EPIC-084 재작업. [CRITICAL] 이미지 업로드 500 에러의 실제 원인이 이 로컬 머신 `.env.local`의 R2 자격증명 누락임을 실측으로 규명(코드 결함 아님, 컴퓨터마다 별도 설정 필요한 gitignore 파일 — NEXT_TASK.md P0 기록), 실패해도 화면에 표시가 없던 UX는 alert로 보완. Tiptap `link`/`underline` 중복 확장 경고 수정. Navbar "글쓰기" 버튼을 마이 페이지 오른쪽으로 정정 + 게시글 상세 하단에도 "목록으로" 추가. `CategoryBoardPicker.tsx`(신설) — UUID가 노출되던 단일 `<select>`를 3열 Miller Columns로 전면 교체(글쓰기/수정/위젯 게시판 선택 3곳), `useBoardOptions.ts`가 branches/boardBranchMap도 노출하도록 확장, 이제 안 쓰는 `BoardPageSelect.tsx` 삭제. 에디터: 이미지 삽입을 커서 위치로 변경(EPIC-079의 NodeSelection-교체 함정을 피해 `insertContentAt`/위치 추적 방식으로), "이미지 주소 복사" 붙여넣기도 R2 재호스팅하도록 통일, 공용 `useDragResizeWidth` 훅으로 갤러리(`width` 속성 신설)/임베드(HTML 코드 임베드 포함)에도 코너 드래그 리사이즈 적용(LinkCard는 시각적 카드가 없어 제외). "미분류 페이지" 삭제는 실측으로 정상 동작 재확인(EPIC-084 수정이 유효함), 그 버킷이 53개까지 부풀었던 근본 원인(`ensureUnassignedPagesInTree()`가 위젯 0개인 PageEditButton 전용 빈 draft까지 무조건 흡수)을 찾아 코드 수정 + 기존 빈 항목 정리(`docs/sql/EPIC-084-REVISED-cleanup-empty-unassigned-pages.sql`, 라이브 반영 완료, 53→23개로 감소, `docs/navigation-blueprint.md` §9 원래 추정치와 일치) — 위젯이 있는 나머지는 실제 콘텐츠라 보존, 개별 검토 항목으로 NEXT_TASK.md에 기록. `tsc`/`lint`/`build` 전부 통과, 실제 admin 세션으로 이미지 업로드 500/3열 선택기 렌더링/미분류 페이지 삭제 API 200을 전부 브라우저 실측 확인. 상세는 CHANGELOG.md 동명 항목 참고.
- EPIC-085 : [Stage 2] Universal Block Renderer, Frictionless Archiving (Scraping System), & Dynamic OG/SEO Engine — `src/components/content/UniversalBlockRenderer.tsx`(신설)로 읽기 전용 렌더링 로직을 `PostBody.tsx`에서 분리해 공용화(에디터 편집 코드와 완전 분리), 점검 결과 정렬/색상/리사이즈/표/출처는 이미 정상 동작해 `globals.css`에 표 반응형 스크롤+임베드 iframe 최대너비만 보강. `user_scraps` 테이블(`docs/sql/EPIC-085-user-scraps.sql`, 라이브 반영 완료, `auth.uid()=user_id` own-row RLS) + `ScrapButton`(자기완결형, Optimistic UI, 비로그인 로그인 유도 모달) + `/api/scraps/[postId]`로 원클릭 스크랩 구현 — 라이브 DB에 한 번도 마이그레이션되지 않아 죽어있던 기존 `post_bookmarks`/북마크 버튼을 대체(라우트 삭제). 게시글 상세 상/하단·갤러리 위젯 카드에 배치, `/mypage/archive`("아카이브 / 내 스크랩" 탭)에서 게시판 카테고리별로 모아보기. `src/lib/utils/extractPostMetadata.ts`(신설, description은 신규 파싱, ogImage는 기존 `findFeaturedImage()`/`findFirstImage()` 재사용) + 게시글 상세 라우트를 Server Component 래퍼(`generateMetadata`)+클라이언트 컴포넌트(`PostDetailClient.tsx`)로 분리해 동적 OG/SEO 메타데이터 생성. `tsc`/`lint`/`build` 전부 통과. 상세는 CHANGELOG.md 동명 항목 참고. main/develop에 병합 완료.
- EPIC-084 : [Stage 2] Universal Routing & Contextual Write Flow, Editor Media Control, & Admin Clean-up — 상단 탭 "마이 페이지" 왼쪽에 전역 "글쓰기" 버튼(`/write?boardId=`, Contextual Write) + `WriteBoardForm` 공용 컴포넌트로 기존 `/boards/[board_slug]/write`와 로직 공유, 갤러리 위젯에 글쓰기 버튼 추가, 게시글 상세에 "목록으로" 링크 추가. 에디터: 외부 사이트에서 여러 이미지를 드래그 선택해 복사→붙여넣기 시 서버(`/api/media/fetch-external`)가 대신 내려받아 R2로 재호스팅(기존엔 hotlink), 갤러리 슬라이더/이미지 리사이즈는 점검 결과 기존 구현이 이미 정상 동작해 추가 구현 불필요. `navConfig.ts`의 `buildNavTree()`가 사일로상점/살롱데상 top-level `href`를 조립 결과에서 누락시키던 버그 수정(상단 탭/사이드바 헤더 클릭 무반응의 실제 원인) + `LeftSidebar`/`RightSidebar` 헤더를 Link로. "미분류 페이지" 삭제가 `ensureUnassignedPagesInTree()`에 의해 즉시 되살아나던 버그 수정(원본 페이지까지 삭제하도록 변경). 위젯 게시판 선택 드롭다운을 `adminTreeGrouping.ts` 재사용해 카테고리별 `<optgroup>`으로 계층화. `tsc`/`lint`/`build` 전부 통과. main/develop에 병합 완료.
- EPIC-083 : [Stage 2] Universal Editor Extension (Media Library UI, Text Color, Table, Custom Font Upload, & Content Attribution) — EPIC-082의 R2 Direct Upload API + `media_library` DB를 Tiptap 에디터 UI에 배선. `@tiptap/extension-table`+`table-row`/`-header`/`-cell`(리사이즈 비활성) + `@tiptap/extension-font-family`(기존 설치돼 있던 Color/TextStyle와 함께) 추가, 툴바에 색상 칩/폰트 드롭다운/표 삽입·수정/출처 입력 버튼 신설. `custom_fonts` 테이블(`docs/sql/EPIC-083-custom-fonts.sql`, 라이브 반영 완료) + `/admin/fonts`(Admin 업로드 UI) + `useCustomFonts` 훅(동적 `@font-face` 주입, 에디터+글 상세 양쪽 공유). `SourceAttributionBlock`(신규 커스텀 노드, 출처 카드 UI). 이미지/갤러리 업로드 경로를 Supabase Storage에서 R2 Direct Upload로 전환(`src/lib/r2Upload.ts`), `POST /api/media`(media-architecture.md가 미구현으로 남겨뒀던 업로드 완료 확인 API) 신설. 인스타그램 스타일 갤러리 슬라이더는 EPIC-079-PHASE-5에서 이미 완성된 `GalleryBlock`을 재사용(신규 노드 아님). `tsc`/`lint` 통과, 로컬 dev에서 표/출처 카드 삽입 및 sanitize 통과 렌더링까지 확인. 상세는 CHANGELOG.md 동명 항목 참고. main/develop에 병합 완료.
- EPIC-082 : [Stage 2] Universal Media Library Schema & Cloudflare R2 Direct Upload Pipeline — Stage 2 "Data and Media Separation" 전략의 첫 구현. `docs/sql/EPIC-082-media-library.sql`에 중앙 집권형 `media_library` 테이블(id/user_id/file_url/file_name/mime_type/size_bytes/width/height/duration/alt_text) + RLS(전체 공개 조회, 본인/관리자만 쓰기) 정의. `src/app/api/media/presigned/route.ts`가 `@aws-sdk/client-s3`+`@aws-sdk/s3-request-presigner`로 Cloudflare R2(S3 호환 API)용 5분짜리 pre-signed PUT URL을 발급(로그인 필요, 100MB 상한, image/video/audio만 허용) — 파일 바이트는 서버를 거치지 않고 브라우저가 R2에 직접 업로드한다. `src/lib/media.ts`에 `MediaAsset`/`PresignedUploadRequest`/`PresignedUploadResponse`/`UniversalMediaBlockAttrs`(다음 EPIC에서 Tiptap 노드가 참조할 목표 스펙, 아직 미연결) 타입 정의, `docs/media-architecture.md`에 전체 파이프라인 데이터 계약 문서화. 지시대로 UI/에디터 컴포넌트(`blockEditorCore.ts` 등)는 전혀 수정하지 않음 — 실제 배선은 EPIC-083. `tsc`/`lint`/`build` 전부 통과. **DB 미반영**(auto mode 분류기가 프로덕션 DB 쓰기 차단, 사용자가 SQL Editor에서 직접 실행 필요). **후속(같은 날, 사용자 로그인 세션으로 실기 검증)**: R2 업로드 왕복 확인 과정에서 `.env.local`의 `R2_ACCOUNT_ID` 오설정(계정 ID 대신 API 토큰 값이 들어가 있어 엔드포인트 호스트명이 무효했음)을 발견/수정 — presigned PUT→R2 저장→public URL GET까지 전부 정상 동작 확인. Vercel 배포 환경엔 R2 환경변수가 아직 미등록(별도 이슈, 사용자 조치 필요). 상세는 CHANGELOG.md EPIC-082/EPIC-082 후속 참고.
- EPIC-001 : 사용자 인증
- EPIC-002 : 멤버십 시스템
- EPIC-003 : 상점
- EPIC-004 : 게시판
- EPIC-005 : 마이페이지
- EPIC-006 : 온라인 도슨트
- EPIC-007 : 대관 시스템
- EPIC-008 : 살롱 체크인
- EPIC-009 : 관리자 결제
- EPIC-010 : 다운로드
- EPIC-011 : 출석체크
- EPIC-012 : 설문
- EPIC-013 : 캐릭터 은행
- EPIC-014 : Time Slip
- EPIC-015 : Your Treasures
- EPIC-016 : 공간 스타일링 포트폴리오
- EPIC-017 : Navigation 재구성 + Wishlist
- EPIC-018 : 상단 탭 재구성 (스튜디오 대관 → 공간 문의 통합, 마이페이지 탭 추가, 중앙 정렬)
- EPIC-019 : Studio Navigation Rename ("공간 문의" → "스튜디오" 라벨 변경)
- EPIC-020 : Google OAuth "No API key found in request" 버그 수정 (`.env.local`의 `NEXT_PUBLIC_SUPABASE_URL` `/rest/v1/` 접미사 제거)
- EPIC-021 : Bug Investigation — Google 로그인 재확인 및 해결 (PC별 `.env.local`/서버 재시작 필요성 확인)
- EPIC-022 : 마이페이지 11개 탭 재구성 + `member_collections`/`member_follows`/`member_badges`/`member_visitors` 스키마 설계
- EPIC-023 : 동적 네비게이션(`site_navigations`) + 카테고리(`site_categories`) 관리자 CMS
- EPIC-024 : 관리자 대시보드 레이아웃 및 인증 가드 수정
- EPIC-025 : 관리자 대시보드 재구성 및 중첩 라우팅
- EPIC-026 : 관리자 홈페이지 설정(`site_settings`) 구현
- EPIC-027 : BugFix — Navigation Editor 저장 기능 추가
- EPIC-028 : 관리자 게시글 관리(상점/살롱) 구현
- EPIC-029 : 나의 컬렉션(Member Collections) CRUD 구현
- EPIC-030 : 스튜디오 포트폴리오 등록 — 제출 UX 개선
- EPIC-031 : 관리자 게시글 관리 — 페이지네이션 및 `is_hidden` 컬럼
- EPIC-032 : 홈페이지 & Navbar `site_settings` 연동
- EPIC-033 : 관리자 CMS — 파일 직접 업로드 및 로고/슬라이드쇼 동적 스타일링
- EPIC-034 : 로고 및 헤더 고급 커스터마이징
- EPIC-034-Ext : 로고 텍스트 고급 스타일링
- EPIC-035 : Tistory 스타일 드래그앤드롭 카테고리 CMS
- EPIC-035-Fix : 카테고리 CMS 일원화 및 구버전 페이지 정리
- EPIC-036 : 로고 텍스트 색상 및 슬라이드 배경 Wallpaper
- EPIC-037 : 내비게이션 hover/click UX 개선
- EPIC-039 : 마스터 UI 개선 — 사이드바/아이콘/로고 텍스트/hover/랜덤 Wallpaper
- EPIC-040 : 마스터 UI — 중첩 드롭다운, 독립 사이드바, 로고 텍스트, 랜덤 Wallpaper
- EPIC-041-042 : UI 통합 개선 — hover UX, 커스텀 폰트, 아이콘, 홈 큐레이션
- EPIC-041-042-HOTFIX : hover 버그 수정 및 직접 URL 입력 방식 전환
- EPIC-043 : 폰트 설정 개편, 사이드바 클릭 전용 + hover 아코디언
- EPIC-044 : 동적 라우팅, 내비게이션 데이터 대량 주입 및 UniversalBoard 컴포넌트
- EPIC-045 : 마이페이지 재구성 — 박물관 컨셉, 라우트 기반 탭 구조
- EPIC-046 : 게시판 디자인 시스템을 Editorial Magazine으로 통일
- EPIC-047 : 게시판 공통 Board Engine 구축(Part 1: BoardRenderer 기반 community/story/gallery/hub) + Board Definition System 전환(Part 2: config만으로 게시판을 정의하는 BOARD_DEFINITIONS 레지스트리, 하드코딩된 그룹 판별 로직 제거)
- EPIC-048 : Board Definition System으로 Silo Store 실제 게시판 20개 생성(hub 3개: Silo Store/Online Docent/Heritage + story 17개) — 새 페이지/컴포넌트 없이 config 추가만으로 생성, BoardRenderer의 hub 레이아웃 실제 구현
- EPIC-049 : Board Definition System으로 Salon des Cent Community 영역 게시판 생성(최상위 hub Community + 하위 9개(중첩 hub 3개 포함) + 기존 DB의 클럽 13개/모임방 7개를 재분류) — 새 DB 행은 10개만 추가, 나머지 20개는 기존 board_type='topic'/'group' 행 재사용, 새 페이지/컴포넌트 없이 config 추가만으로 생성
- EPIC-050 : Board Definition System으로 Salon des Cent Membership/Gallery/Archive 영역 게시판 생성(hub 3개 + 하위 14개, 전부 신규 DB 행) — 5번째 boardType "timeline" 추가(BoardRenderer 내 재사용 가능한 Timeline Engine), BoardDefinition에 accessLevel 필드 추가하고 "패트론 게시판"에 실제 serverAuth.ts 인가 로직 연결
- EPIC-051 : Board Definition System으로 Studio(공간 문의) 영역 게시판 생성(hub 1개 + story 4개, 전부 신규 DB 행) — 새 예약 시스템 없이 BoardDefinition.ctas 필드로 "문의하기"/"예약하기"/"대표 프로젝트 보기" 버튼을 기존 /rental·/space-inquiry/*·/shop/projects(styling_projects) 페이지에 연결, hub 슬라이드에 대표 이미지(photo_url) 표시 추가
- EPIC-052 : 마이페이지를 Personal Hub로 확장 — Tiptap Block Editor 도입(모든 Board Definition 게시판 글쓰기 폼 공용, posts.body에 HTML 저장 + 서버 sanitize), Timeline Engine 추출·재사용(groupByYearMonth+TimelineView), "나의 컬렉션" 비공개 유지하며 StoryCard로 시각 통일, 살롱/도슨트 수료증/공간/전시회 Placeholder를 실데이터로 교체, 버킷리스트 신규 기능(member_bucket_list) 추가
- EPIC-053 : Block Editor 시스템 확장 — RichTextEditor → BlockEditor로 전면 재작성(툴바 확장/체크리스트/밑줄/정렬/형광펜/색상), 이미지 Drag&Drop/붙여넣기/여러장 업로드, Supabase Storage 연동(post-images/gallery/attachments), 자동 저장(localStorage)/임시 저장(서버 API), sanitize 확장(img/iframe/video/audio/table)
- EPIC-053.1 : Block Editor 완성 — 정본을 Tiptap ProseMirror JSON(posts.body_json)으로 전환(HTML은 서버가 항상 재계산하는 파생 캐시), FigureImage/Gallery/Embed(Youtube/Vimeo/Instagram/Spotify/Maps)/LinkCard 커스텀 노드 + Lightbox, 대표 이미지 지정(featured_image_url), 게시글 수정 화면 신설(PATCH + PostForm 공용화) 및 JSON 복원, 실제 게시글과 동일한 Preview, Storage Garbage Collection 큐 + 관리자 Cleanup Job, member_bucket_list/posts 신규 컬럼/Storage Bucket+Policy SQL 작성(docs/sql/epic-053-1.sql, 라이브 DB 미적용) — **EPIC-068 merge 시점(2026-07-30) 기준 develop에 병합 완료, 아래 board post 관련 파일들(BoardRenderer/PostDetailHeader/write·edit 페이지/posts API)은 이 병합 과정에서 EPIC-054~068의 이후 변경(Board Renderer Registry, 위젯 toggle 6종, Filter 등)과 함께 재조정됨(자세한 병합 내역은 CHANGELOG.md "EPIC-053 병합" 참고).**
- EPIC-054A : 모든 상위/하위 메뉴가 실제 Page를 갖도록 정비 — `<ComingSoon>`을 렌더링하던 19개 placeholder 페이지(사일로 Heritage 할머니/할아버지, 살롱 Community/Membership/Gallery 각 항목, 스튜디오 물품 대여/공간 스타일링, nav 미연결 orphan 3개(투어 도슨트/음료 주문/공간 촬영 대관 구 페이지))를 전부 공용 `PageHeader` 컴포넌트(Title/Subtitle/Breadcrumb/Description/Page Container)로 교체하고 `ComingSoon.tsx` 삭제 — 게시판 연결/기능 추가/Block Editor 수정/Board Module 생성은 범위 밖. 라이브 nav(site_navigations DB 시드 기준, docs/navigation-blueprint.md)에 연결된 모든 메뉴가 404 없이 실제 페이지로 연결됨을 확인.
- EPIC-054B : Page(화면)와 Board(게시판) 개념 분리 — `src/lib/pageModules.ts`(16종 `PageModuleKind`: Hero/Story Board/Gallery Board/List Board/Slide Board/Timeline/Comment/Search/Pagination/Notice/CTA/Form/Calendar/Survey/Ranking/Profile Card + 판별 유니온 `PageModuleConfig`/`PageDefinition`) + `src/components/modules/PageModuleRenderer.tsx`(모듈 배열을 순서대로 렌더링하는 조합기) 신설. Board 계열(Story/Gallery/List/Slide Board)·Timeline·Comment·Pagination은 기존 Board Definition System/Timeline Engine 컴포넌트를 그대로 재사용, Search/CTA는 `BoardHeader.tsx`에서 `SearchInput`/`CtaButtons`로 추출해 공유(중복 제거), 재사용할 기존 컴포넌트가 없던 Notice/Form/Calendar/Survey/Ranking/Profile Card 6개만 최소 프레젠테이션 셸 신규 작성. 콘텐츠/실제 Board 행/이 시스템을 사용하는 Page 인스턴스는 전혀 만들지 않음(구조만 준비, 향후 EPIC이 실제 데이터로 채움).
- EPIC-054C : 모든 Page를 Board와 연결 — `src/components/modules/BoardModule.tsx` 신규(boardId 하나로 정의 조회+posts 조회+Search/Sort/Pagination을 전부 스스로 처리하는 자기완결형 모듈, 기존 `/boards/[id]/page.tsx` 로직을 그대로 이전). Story/Gallery/List/Slide Board 4종 모두 이 컴포넌트로 통일(`PageModuleConfig`의 `BoardModuleProps`를 `{boardId, includeChildBoards?}`로 단순화). `/boards/[id]/page.tsx`는 `<BoardModule boardId=.../>` 하나만 렌더링(Board 1개)하도록 축소, `/boards/page.tsx`(디렉토리)는 최상위 hub마다 `slide_board` 모듈을 만들어 `PageModuleRenderer`로 **한 Page에 여러 Board를 나란히 배치**(Page 하나 = Board 하나 구조가 아님을 실제로 증명) — 기존 "게시판 허브" 바로가기 카드/레거시 그룹 링크 목록은 순수 내비게이션이라 그대로 유지. `src/components/modules/EmptyState.tsx` 신규 — `BoardRenderer`의 게시글 0건 분기와 `PageModuleRenderer`의 모듈 0개 분기가 공유(Placeholder Module 대신 Empty State UI). 모든 Story/Community/Timeline 정의는 이미 searchable/sortable/pageable=true(hub만 예외, 설계상 의도).
- EPIC-054D : 사이트 전체 Navigation/Routing/SEO/Breadcrumb/404/URL 구조 전수 감사(Audit) — 새 기능/디자인/게시판/Block 생성 없이 감사 및 리팩토링만 수행. **핵심 발견**: `navConfig.ts`의 `FALLBACK_NAV_TABS`(EPIC-044 재작성분)와 실제 라이브 `site_navigations`(DB 시드, `docs/navigation-blueprint.md` 반영분)가 서로 다른 두 개의 nav 구조로 공존(P0, 별도 EPIC 필요) — 코드 링크 기준으로는 죽은/중복 링크 없음, 실제 문제는 이 이중 구조로 인한 orphan/중복 콘텐츠(`/shop/heritage/*` 정적 페이지 vs `/heritage/*/[name]` 동적 라우트 등). **적용한 수정**: (1) SEO — root `metadata`를 create-next-app 기본값("Create Next App")에서 실제 사이트 정보(title/description/OpenGraph/Twitter Card/`metadataBase`)로 교체, `lang="en"→"ko"`; (2) Sitemap — `src/app/sitemap.ts` 신규, `src/app` 파일시스템을 직접 스캔해 정적 라우트를 자동 포함(새 page.tsx 추가 시 코드 수정 불필요) + 하드코딩 이름 목록(heritage/community) + Supabase 동적 콘텐츠(boards/items/docent_contents/clubs) 조합; (3) robots.txt — `src/app/robots.ts` 신규, 관리자/마이페이지/인증 폼 제외 + sitemap 링크; (4) 접근성 — `LeftSidebar`/`RightSidebar`에 Escape 닫기, 닫힐 때 트리거로 포커스 복귀, `inert`/`aria-hidden`(닫힘 시), `aria-expanded`; `Navbar` 드롭다운에 `group-focus-within`(키보드 Tab으로도 열리도록, JS state 재도입 없이) + `aria-haspopup`; (5) 성능 — `Navbar`의 `leftSidebarTab`/`rightSidebarTab` 파생값 `useMemo`(단, `fontFamilyValue`는 eslint-config-next의 React Compiler 대비 lint 규칙과 충돌해 원래 형태 유지, 컴파일러 자체는 아직 미활성). Breadcrumb 자동 생성·Responsive 재설계는 "새 기능/디자인 추가 금지" 범위를 벗어나 구현하지 않고 P1 아키텍처 발견사항으로만 기록(NEXT_TASK.md).
- EPIC-054E : EPIC 번호와 프로젝트 진행 단계(Stage)를 분리하는 운영 체계 구축(문서/프로세스만, 코드 변경 없음) — `docs/STAGES.md` 신규(Stage 1 Foundation ~ Stage 6 Scale 6단계 정의, Stage 1의 14개 세부 항목을 EPIC-054D까지의 실제 코드 상태로 감사해 완료/부분/미착수 분류, Block Editor 완전판이 별도 미병합 브랜치 `feature/EPIC-053`에만 있음을 명시), `docs/PROJECT_DASHBOARD.md` 신규(Current Stage/Progress/Current·Next EPIC/Recent Completed 10/Current Priority/Known Issues P0-P3/Technical Debt/Next Milestone — 매 세션 가장 먼저 읽는 현황 문서), `PROJECT_BLUEPRINT.md`에 §11(Project Stages/Dashboard/Current Stage/Current Milestone) 추가, `CLAUDE.md`에 "세션 시작 시 읽기 순서"(PROJECT_DASHBOARD → STAGES → PROJECT_BLUEPRINT) + EPIC 완료 시 문서 동기화 규칙(4개 문서) 신설. Stage(진행 단계)와 EPIC(작업 기록)은 절대 혼용하지 않는다는 원칙을 명문화.
- EPIC-054F : 모든 상위/하위 카테고리 메뉴가 실제 Page(Route)를 갖도록 정비 — 새 기능/디자인/DB/게시판/Block/Editor 없이 Page 생성만 수행. Community/Heritage(동적 서브라우트만 있고 인덱스 없음)와 Studio/Membership/Gallery/Archive(디렉토리 자체 없음) 6곳이 404였음을 확인(마이페이지 12개 탭·Online Docent는 이미 완비 확인, 변경 없음). 공용 `PageTemplate`(`src/components/PageTemplate.tsx`) 신설 — 기존 `PageHeaderContent`(EPIC-054A에서 분리)+`BoardModule`(EPIC-054C)만 조합, 새 디자인 없음. `src/lib/useHubBoardId.ts` 신규(slug→board id 조회 훅, 기존 `/boards/page.tsx` 패턴 재사용). `/community`·`/heritage`·`/studio`·`/membership`·`/gallery`·`/archive` 6개 페이지 신설 — Board 미연결 시 EmptyState("게시글이 없습니다.") 정상 노출 확인(브라우저 검증). Route 총 76개(page.tsx 기준, 기존 70 + 신규 6). legacy `board_type='archive'`와 신규 hub `archive`의 slug 충돌 1건 발견·기록(수정 안 함, P2 — Board Definition System 변경은 범위 밖).
- EPIC-055 : Universal Board System 완성 — 모든 페이지를 실제 게시판에 연결(새 페이지/기능/DB/Board 컴포넌트 없이 연결+중복 제거만). `src/components/shared/UniversalBoard.tsx`(EPIC-044, 실데이터 없는 뼈대 stub, 자체 검색/정렬 재구현)가 `/heritage/grandma|grandpa/[name]`·`/community/club/[name]`(최대 69개 이름별 URL)에서 여전히 쓰이며 실제 게시판과 미연결 상태였음을 발견·해결 — 전부 `PageTemplate`+`BoardModule`로 교체(할머니/할아버지는 공유 `grandmas`/`grandpas` 스토리 게시판, 클럽/주제는 `useBoardIdByName` 신규 훅으로 이름이 정확히 일치하는 기존 board에 연결). `UniversalBoard.tsx` 삭제 + `src/components/mypage/EmptyState.tsx`(Board System의 `modules/EmptyState.tsx`와 중복, 12곳 사용)를 통합 삭제. Board Config(`boardLayout.ts` 하나만 수정해 게시판 추가 가능한 구조)는 이미 충족돼 있음을 재확인, 변경 없음. `/shop`·`/docent`·마이페이지 개인 데이터는 게시판이 아니라는 이유로 Board 연결 대상에서 의도적으로 제외.
- EPIC-056 : 모든 페이지를 Board Module 조합으로 구성 — 지시받은 14개 Board Module(Hero/Board Header/Search/Sort/Story Thumbnail/Community List/Gallery/Slide/Timeline/Filter/Calendar/Application/Pagination/Empty State)을 `src/components/modules/`에 독립 재사용 가능한 형태로 정립(대부분 `BoardRenderer.tsx`/`BoardHeader.tsx`에 갇혀 있던 기존 로직 추출, 신규 마크업은 `FilterModule` 하나뿐). `BoardModule`에 Hero Module을 내장해(`showHero` prop) `/boards/[id]`(약 72개 게시판) 전체에 Breadcrumb+제목+설명이 새로 보이도록 함, `/studio`는 PageTemplate 대신 Hero+Application Module(신청/문의 버튼 3개)+Calendar Module+Board Container로 직접 재조립, `/boards` 디렉토리에도 Breadcrumb 추가. 브라우저로 실제 화면 변화 확인(스크린샷). `/shop`·`/docent`·마이페이지는 게시판이 아닌 별도 데이터 도메인이라 이번 EPIC에서 재구성하지 않음(남은 미적용 페이지로 기록).
- ⚠️ EPIC-057~062 : 커밋 로그상 완료되어 이 브랜치 계보에 포함돼 있으나(모든 카테고리 하위 Route 51개 신설, Board Module 14종→Page Builder 전환, Page Builder CMS(Page→Module→Board→Post) 신설, Fallback 제거, Page Architecture로 Navigation 직결) 각 EPIC의 상세 요약이 이 문서에 아직 기록되지 않았다 — EPIC-063 작업 중 발견한 문서 동기화 누락(P2). 커밋 메시지(`git log --oneline`)를 근거로 각 EPIC 요약을 소급 기록하는 별도 작업 필요.
- EPIC-063 : Navigation System Completion(Page-first Architecture) — Board가 아니라 Navigation/Hub Page/Sidebar UX만 수정(BoardRenderer 무변경). **감사 결과 라이브 `site_navigations`는 이미 정확히 연결돼 있었다**(사일로 보물들→`/treasures`, 온라인 도슨트→`/docent`, 사일로 유산→`/heritage`, 커뮤니티→`/community`, 멤버십→`/membership`, 갤러리→`/gallery`, 아카이브→`/archive` — EPIC-062까지 이미 완료돼 있었음), 유일하게 깨져 있던 리프 항목 "입양신청서 라이브러리"(href=null)만 `docs/sql/EPIC-063-nav-audit-fix.sql`로 수정 대기(관리자가 Supabase SQL Editor에서 실행 필요). 실제 버그는 프론트엔드에 있었다: (1) `Navbar.tsx` 상단 탭 hover 플라이아웃에서 그룹 헤더가 `group.href`가 있어도 항상 클릭 불가한 `<button>`으로 렌더링됐음 — `group.href`가 있으면 `<Link>`로 렌더링하도록 수정(LeftSidebar/RightSidebar 본 패널은 EPIC-058부터 이미 Link였음); (2) `LeftSidebar.tsx`의 `defaultExpanded()`가 "도슨트"/"헤리티지" 미포함 그룹(예: "사일로 보물들")을 기본 펼침으로 뒀던 것을 제거, 모든 그룹 기본 접힘으로 통일; (3) `LeftSidebar.tsx`/`RightSidebar.tsx`/`Navbar.tsx`의 sidebar 여닫이를 "클릭으로만 열림 + hover-out 시 닫힘"(EPIC-043)에서 "hover 또는 클릭으로 열림 + Outside Click(기존 backdrop)·✕·Escape로만 닫힘"으로 변경, `onMouseLeave`/`onAmbientLeave` 제거. 상단 Navbar의 작은 hover 플라이아웃 자체(hover-open/hover-out-close)는 요구사항대로 무변경. `/treasures`·`/docent`·`/heritage`·`/community`(+`/topics`·`/weekday`)·`/membership`·`/gallery`·`/archive` 9개 라우트와 각 페이지의 관리자 전용 "페이지 수정" 버튼(`PageEditButton`)은 EPIC-062에서 이미 전부 구현돼 있었음을 브라우저로 재확인(신규 생성 없음).

- EPIC-064A : 모든 Route에 관리자 전용 "페이지 수정" 버튼 부착 — Page Builder/Widget/DB/새 기능 없이 기존 `<PageEditButton>`(EPIC-060)만 재사용. `src/app` 하위 135개 page.tsx 전수 조사 → 126개(관리자 CMS 도구 성격의 `/admin/**` 9개 제외 전부)에 부착 완료. Static/Dynamic Route 모두 포함(예: `/boards/[id]`, `/docent/[id]`, `/heritage/grandma/[name]`, `/u/[memberId]`). 버튼 위치는 컴포넌트 자체가 `fixed top-20 right-4`라 페이지마다 동일하게 고정됨(별도 레이아웃 작업 불필요). slug는 URL 경로를 대시로 이은 문자열(예: `shop-projects-id`)로 통일, 동적 라우트는 인스턴스별이 아닌 라우트 템플릿 기준 slug(향후 필요 시 해당 slug로 page_builder 행을 만들면 그 라우트 전체가 같은 편집 내용을 공유) — 로딩/에러 중간 상태(early return)에는 버튼을 붙이지 않고 최종 콘텐츠 렌더링 지점에만 부착. `/admin/**`는 이미 `admin/layout.tsx`가 관리자 가드를 하고 있고, 그 자체가 CMS 도구(`/admin/pages/[id]`는 Page Builder 편집기 그 자체)라 "페이지 수정" 버튼의 목적(사이트를 보다가 바로 편집 진입)과 맞지 않아 의도적으로 제외. 후속으로 관리자가 실제로 로그인해보니 9개 Hub 외 117개 하위 Route는 `page_builder`에 매칭 행이 없어 버튼이 안 보이는 문제가 발견돼(컴포넌트만 심고 DB는 건드리지 않았던 지시 사항의 당연한 결과), `docs/sql/EPIC-064A-page-builder-backfill.sql`(사용자가 제공한 Management API 토큰으로 직접 실행)로 117개 slug를 draft 행으로 채워 해결 — 이 과정에서 `treasures`도 애초에 행이 없었음을 추가로 발견해 보정, 최종 126/126 slug 확인.
- EPIC-065 : 기존 JSON 기반 Page Builder를 운영자용 No-Code Visual Widget Builder로 전환 — 운영자가 JSON/HTML/board_id를 직접 입력하지 않고 "위젯 추가 → 설정(체크박스/드롭다운/텍스트/목록) → 저장"만으로 페이지를 완성한다. **DB**: `page_modules.module_type` CHECK 제약을 제거(위젯 종류가 계속 늘어날 수 있어 애플리케이션 코드가 유효성 검증을 대신함)하고 `is_hidden boolean default false` 컬럼 추가(둘 다 사용자 제공 Management API 토큰으로 직접 실행, `docs/sql/EPIC-065-widget-builder-schema.sql`에 기록). **위젯 23종**(Hero/Breadcrumb/Board/Latest Posts Slider/Gallery/Timeline/Calendar/Reservation/Survey/Button/CTA/Quote/Image/Video/Audio/FAQ/Search/Filter/Statistics/Badge/Divider/Spacer/HTML)을 `src/lib/widgetSchema.ts`에 라벨/아이콘/카테고리/필드 스키마로 선언 — 기존 14종 중 11종은 기존 컴포넌트를 그대로 재사용(Hero/Board/Slide/Gallery/Timeline/Calendar/Application→"Reservation"/Search/Filter/Spacer/Divider/HTML), Survey/CTA는 EPIC-054B가 이미 만들어뒀던 `SurveyCard`/`CtaButtons`를 재사용, 나머지 9종(Breadcrumb/Button/Quote/Image/Video/Audio/FAQ/Statistics/Badge)만 새 프레젠테이션 컴포넌트 작성(전부 `src/components/modules/`, 기존 카드/버튼 스타일 관례 재사용). **Inspector**: `WidgetInspectorForm`(체크박스/드롭다운/텍스트/숫자/목록 5종 필드 타입만 지원, JSON 없음) 하나로 23종 전부를 설정 — 목록형 필드(FAQ 질문/답변, Statistics 항목 등)는 행 추가/삭제/위아래 이동 UI로 편집. **Board Widget**: 요구된 6개 토글(검색/정렬/Pagination ON-OFF, 페이지당 개수, 썸네일, 글쓰기 버튼)을 전부 실동작으로 구현 — `BoardModule`에 옵션 prop 6개 추가(전부 undefined 기본값이라 기존 호출부는 무영향), 검색/정렬/글쓰기는 핸들러를 안 넘기는 방식으로 `BoardHeader`의 기존 조건부 로직을 그대로 활용, 페이지당 개수는 `/api/boards/[id]/posts`에 `pageSize` 쿼리 파라미터 추가, 썸네일은 `StoryThumbnailModule`(story 레이아웃)에만 적용됨(gallery/hub 레이아웃은 이미지가 레이아웃의 핵심이라 토글 대상이 아님, 알려진 범위 제한). **Live Preview**: 편집 중인 위젯 하나만 draft(로컬 state)로 관리하고, `PageBuilderRenderer`가 modules 배열에서 그 자리만 draft로 치환해 그린다 — 저장 전에도 화면이 즉시 바뀌고 새로고침이 없다. **Drag & Drop**: 기존 `dnd-kit`(EPIC-035 CategoryTreeManager와 동일 패턴) 재사용. **Duplicate**: 원본 바로 다음에 복제본을 끼워 넣고 뒤 항목들의 sort_order를 한 칸씩 미룸. **Hide**: `is_hidden` 즉시 토글 — 공개 페이지(`PageBuilderRenderer` 기본값)는 숨긴 위젯을 건너뛰고, 관리자 Live Preview(`includeHidden`)는 흐리게 계속 보여줌. **개발자 모드**: 상단 체크박스(세션 로컬, 저장 안 됨)를 켜야만 위젯별 원시 JSON 보기/수정 textarea가 나타남 — 기본은 완전히 숨김. `/admin/pages/[id]/page.tsx` 전면 재작성, `admin/pages`(목록)는 무변경. **검증**: `npx tsc --noEmit`/`npm run build`/`npm run lint`(신규 에러 0건) 통과. 관리자 로그인 자격 증명이 없어 Visual Builder UI 자체를 브라우저로 클릭 테스트하지는 못했다 — 대신 Management API로 새 위젯 5종(Quote/FAQ/Statistics/Badge/Button)과 숨김 위젯 1개, Board 위젯 토글 6종을 라이브 게시판 페이지(`/gallery`)에 임시로 심어 공개 렌더링·숨김 필터링·검색/정렬/글쓰기 버튼 미노출을 실제로 확인한 뒤 전부 정리(원상복구, page_modules 행 개수 원래대로 확인)했다.

- EPIC-067 : Page Builder Integration for Remaining Pages (Phase 1) — EPIC-066이 발견한 결함(79개 페이지가 `PageEditButton`만 있고 `PageBuilderRenderer`를 호출하지 않아 위젯을 구성해도 화면에 절대 반영되지 않음) 중 사용자 노출도가 높은 핵심 도메인 12개(`/`, `/shop`, `/shop/[id]`, `/docent/[id]`, `/boards/[id]`, `/clubs`, `/clubs/[id]`, `/rental`, `/rental/[rentalTypeId]`, `/downloads`, `/attendance`, `/u/[memberId]`)를 우선 전환. 기존 형제 페이지(`/salon/event-notices` 등)는 콘텐츠를 위젯으로 완전히 대체하는 패턴이었지만, 이 12개는 전부 실제 핵심 기능(물품 구매/도슨트 구매/게시판 글 목록/클럽 신청/대관 예약/자료 목록/출석 달력/회원 프로필)이 있는 페이지라 기존 콘텐츠는 그대로 두고 그 아래에 `fetchPublishedPageBySlug(slug)`+`<PageBuilderRenderer>`를 이어 붙이는 병존형 패턴을 새로 확립(서버 컴포넌트 4개는 직접 `await`, 클라이언트 컴포넌트 8개는 별도 `useEffect`). `npx tsc --noEmit`/`npm run build`/`npm run lint`(기존 baseline과 동일, 신규 0건, 수정 파일 전부 무관 확인) 통과, 로그인 세션으로 5개 페이지를 브라우저에서 직접 열어 기존 콘텐츠 유지+위젯 영역 정상 표시(콘솔 에러 없음) 확인. 나머지 66개(정적 PageHeader류/마이페이지 12탭/인증 폼/동적 이름별 라우트 등)는 Phase 2 이후로 NEXT_TASK.md에 우선순위와 함께 기록.
- EPIC-069 : Hub 페이지 Hero + 다중 Slide 위젯 고도화 + Orphan 페이지 2개 보강 — EPIC-068 백필 SQL 실행 이후 하위 카테고리에 실제 게시판/글이 생겼는데도 `/docent`·`/community/topics`·`/heritage`·`/gallery`·`/archive`·`/membership` 6개 Hub는 여전히 application(버튼) 위젯만 나열해 미리보기가 안 보이던 문제 — `docs/sql/EPIC-069-hub-widget-upgrade.sql` 작성(사용자가 Supabase SQL Editor에서 직접 실행). 각 Hub의 application 버튼을 실제 게시판에 연결된 `slide`(Latest Posts Slider) 위젯으로 교체(11+2+5+3+4=25건), `community-topics`는 hero 자체가 없던 상태라 hero+13개 클럽 slide로 전면 재구성. `EPIC-068-category-page-templates.sql`에 이미 동일 목적으로 작성돼 있었으나 "모듈 0개" 가드에 막혀 실행되지 못했던 Hub 템플릿 블록을 발견해 동일한 board 매핑을 재사용, 단 기존에 살아있는 hero 문구·전체 목록 `board` 위젯(자료게시판/패트론 라운지/아티스트 홍보)은 완전 교체하지 않고 보존. Orphan 페이지 2개(`salon-gallery-awards`, `shop-reviews`)도 기존 gallery 위젯은 유지한 채 hero/quote/board 보강. 모든 UPDATE는 `module_type='application'` 매칭이라 재실행해도 안전(멱등), INSERT 구간은 hero 존재 여부로 가드.
- EPIC-053(merge) : `feature/EPIC-053`(Block Editor 시스템, EPIC-053/053.1) 병합 — 오랫동안 `main`에서 분기된 채 별도로 남아있던 브랜치를 `develop`에 정식 통합. 충돌 11개 파일 수동 해소(`BoardRenderer.tsx`는 `develop`의 EPIC-066 Registry 아키텍처를 유지하고 EPIC-053이 추가한 `featured_image_url` 썸네일 우선순위만 `StoryThumbnailModule`/`GalleryModule`에 포팅, `PostDetailHeader`/게시글 상세·작성 페이지/posts API는 양쪽이 각자 추가한 신규 기능(좋아요·댓글·조회수 토글 vs 수정 링크·`body_json`)을 모두 유지). `npm install`/`npx tsc --noEmit`/`npm run build`/`npm run lint`(기존 29 errors/2 warnings 베이스라인과 동일, 신규 0건) 전부 통과 후 `develop`에 fast-forward merge, push. `docs/sql/epic-053-1.sql`(Storage Bucket/Policy, 신규 컬럼)은 아직 라이브 DB 미실행 — 상세는 CHANGELOG.md "EPIC-053 병합" 참고.
- EPIC-068 : Auto Page+Widget Generation on Category Creation + Site-wide Template Backfill — "관리자에서 새 카테고리를 만들면 자동으로 페이지+제목/설명/인용/최근글/게시판/갤러리 5개 위젯 템플릿이 생기길 원한다"는 요청. 사전 조사에서 이 앱에 catch-all(동적 DB 기반) 라우트가 전혀 없어(138개 page.tsx 전부 정적 파일) 카테고리 DB 행만 만들어서는 실제 URL이 생기지 않는 아키텍처 공백을 발견 — 사용자와 범위 조율(메커니즘 + 실제 게시판이 있는 7개 그룹(사일로상점/온라인도슨트/헤리티지/커뮤니티/멤버십/갤러리/아카이브)의 상세 위젯 구성 + 필요한 신규 게시판 30개 생성, 마이페이지/스튜디오/랭킹·방문기록·설문·예약연동 등 신규 기능은 제외). **Part A(메커니즘)**: `src/app/[...slug]/page.tsx`(catch-all — 기존 138개 라우트보다 낮은 우선순위라 전혀 영향 없음) + `src/lib/pageTemplates.ts`(`ensurePageForSlug` — page_builder 없으면 생성, page_modules 0개일 때만 5위젯 기본 템플릿 삽입) + `CategoryTreeManager.tsx`의 href 저장 시점에 연결. **Part B(백필)**: `docs/sql/EPIC-068-category-page-templates.sql`(신규 게시판 30개 + 71개 페이지 위젯, 전부 `on conflict`/`not exists`/DO블록 가드로 재실행 안전, board_id는 이름/카테고리 서브쿼리라 게시판이 없어도 안전하게 null). 16개 정적 placeholder 페이지(`heritage/{grandmas,grandpas}`, `salon/monthly-events`, `membership/*5`, `gallery/*5`, `archive/*3`)를 순수 위젯 페이지로 전환. **진행 중 발견**: `membership/*`·`gallery/*`로 만든 5+5개가 실제 라이브 nav에서는 `/salon/*`·`/salon/gallery/*`를 가리키고 있음을 확인(기존 P0 dual-nav 구조의 구체 사례) — 실제 nav가 가리키는 10개 페이지도 동일하게 전환해 같은 보드를 재사용, `membership-*`/`gallery-*`는 중복으로 남겨둠(해가 없음). Heritage(`/shop/heritage/grandma·grandpa`)·Archive(`/downloads`) 쪽 유사 불일치는 기존 이슈이거나 실제 기능 충돌 우려가 있어 NEXT_TASK.md에 기록만 하고 nav는 건드리지 않음. **검증**: `npx tsc --noEmit`/`npm run build`/`npm run lint`(`git stash -u`로 정확한 clean-baseline 대조, 29 errors/2 warnings 동일, 신규 0건 — 이 과정에서 catch-all의 `react-hooks/set-state-in-effect` 신규 위반과, catch-all 추가가 `@next/next/no-html-link-for-pages`의 라우트 스캔에 영향을 줘 드러난 `login`/`signup`의 기존 `<a>` 태그 흠을 각각 발견해 수정) 통과. 관리자 세션으로 실제 카테고리를 만들어(href 저장 → page_builder/page_modules 자동 생성 → catch-all 렌더링 → PageEditButton 노출) 전 과정을 브라우저에서 직접 확인 후 정리.
- EPIC-067-HOTFIX : Fix Non-Rendering Widgets(Gallery, Slider, Timeline, Video) — "Board를 제외한 Gallery/Latest Posts Slider/Timeline/Video 위젯이 전혀 렌더링되지 않는다"는 보고를 관리자 세션으로 직접 위젯을 추가·저장해 Live Preview와 새로고침한 공개 페이지(`/shop`) 양쪽에서 조사. **Gallery/Timeline/Latest Posts Slider(`slide`)는 게시판을 선택하면 정상 동작함을 확인**(`PageBuilderRenderer`의 매핑, `DbGalleryModule`/`DbSlideModule`/`DbTimelineModule`의 `useBoardPosts` 데이터 주입 모두 결함 없음 — 셋 다 `BOARD_LINKED_MODULE_TYPES`라 위젯 추가 시 게시판 선택 화면이 자동으로 열리므로, 미선택 상태의 "연결된 게시판이 없거나 글이 없어요." 안내를 "렌더링 안 됨"으로 오인했을 가능성). **실제 결함은 Video 위젯 하나**: `VideoWidget.tsx`가 `url` 미설정 시 아무 안내 없이 `return null`(Video는 board_id 계열이 아니라 설정 화면이 자동으로 안 열려 더 눈에 띄지 않았음) — 다른 데이터 의존 위젯과 동일하게 "영상 URL이 아직 설정되지 않았어요." placeholder를 렌더링하도록 수정. `npx tsc --noEmit`/`npm run lint`(기존 baseline과 동일, 신규 0건) 통과, 테스트에 쓴 위젯은 전부 삭제해 원상복구.
- EPIC-081 : Community-Led Growth Vision & Architecture Alignment(Stage 2 공식 진입 선언, 문서 전용) — CEO가 구상한 "Community-Led Growth" 5단계 유저 퍼널(흥미 유입→유지/아카이빙→유대감→지식 구매→소속감/멤버십 락인)을 `PROJECT_VISION.md`에 핵심 전략으로 명문화하고, 이를 뒷받침하는 CTO의 기술 전략(Universal Content Editor+R2 Direct Upload, Frictionless Archiving, Event Telemetry, Paywall Routing — 전부 계획 단계, 아직 미구현으로 명시)을 `PROJECT_ARCHITECTURE.md`에 별도 절로 추가. `docs/STAGES.md`/`docs/PROJECT_DASHBOARD.md`의 Current Stage를 Stage 2로 갱신. 상세는 CHANGELOG.md EPIC-081 참고.
- EPIC-080 : Navigation Dual-Structure Unification(기술 부채 청산) — Management API로 라이브 `site_navigations`(153행)를 직접 조사한 결과, 커뮤니티/갤러리/멤버십 dual-nav는 이미 라이브 DB가 올바르게 정리돼 있었고 진짜 문제는 `navConfig.ts`의 `FALLBACK_NAV_TABS`(DB 조회 실패 시에만 쓰이는 코드 폴백)가 EPIC-044 이후 라이브와 다른 이름별 동적 라우트 구조(헤리티지 67개 이름, 커뮤니티 20개 이름)를 쓰고 있던 것과 과거 혼선의 잔재인 그림자 정적 페이지 17개였음을 발견. `FALLBACK_NAV_TABS`를 라이브 트리와 동기화(헤리티지는 단일 링크로 통합, 커뮤니티는 라이브와 동일한 고정 flat slug로 교체 — 사용자 확인 후 진행), 그림자 페이지 17개 삭제+301 리다이렉트(`next.config.ts`), 죽은 동적 라우트/훅 제거, `sitemap.ts`/`docs/database-schema.sql`/`docs/navigation-blueprint.md`/`NAVIGATION_SYSTEM.md` 동기화. 추가로 `mypage` 탭이 라이브에서 13개 하위 항목을 가진 dropdown인 것이 의도된 구조임을 사용자에게 확인받아 `FALLBACK_NAV_TABS`/`CLAUDE.md`도 동일하게 갱신. `tsc`/`lint`/`build` 전부 통과, 로컬 dev 서버에서 리다이렉트 동작/실제 렌더링까지 확인. **라이브 DB 반영은 미완료** — `docs/sql/EPIC-080-nav-unification.sql` 작성 완료, Claude Code auto mode 분류기가 이번 세션 프로덕션 DB 직접 쓰기를 차단해 사용자가 직접 실행하거나 권한 조정 필요. 상세는 CHANGELOG.md EPIC-080 참고.
- EPIC-078 : 사이드바 아이콘 기본/호버 듀얼 미디어 업로드 + 순수 크로스페이드 전환 — EPIC-076의 인위적인 CSS 3D/회전 호버 애니메이션(아르누보 백합 개화/양문형 문 회전)을 전부 제거하고, 관리자가 기본(Default)/호버(Hover) 미디어를 좌/우 사이드바 각각 독립적으로 업로드(이미지 또는 투명 비디오 `.webm`/`.mp4`)할 수 있게 확장. 실제 사이트에서는 두 미디어를 같은 자리에 겹쳐 `opacity` `group-hover` 크로스페이드로 전환. `site_settings.sidebar_icons`에 `leftIconDefaultUrl`/`leftIconHoverUrl`/`rightIconDefaultUrl`/`rightIconHoverUrl` 추가(구버전 단일 URL 필드는 로드 시 자동 폴백). "배경 항상 완전 투명" 요구사항이 EPIC-076의 배경색 커스텀 기능과 충돌해, 설정은 남기고 시각적 적용만 중단하기로 사용자와 합의. 상세는 CHANGELOG.md EPIC-078 참고.
- EPIC-077 : 사이드바 트리거 모드 설정(호버/클릭) + "사이트 구성 관리" 통합 CMS 개편 — EPIC-076 아르누보 호버 애니메이션을 감상할 새 없이 사이드바가 즉시 열리는 문제와, `메뉴/카테고리 관리`·`페이지 관리`·`게시판 관리` 3개 화면이 분리돼 있어 카테고리 계층 수정이 사이트 전반에 통합 적용되지 않던 문제를 함께 해결. `site_settings.sidebar_icons.triggerMode`(`'click'`|`'hover'`, 기본 `click`)를 신설해 `LeftSidebar`/`RightSidebar`의 호버 열림을 조건부로 만들고, 기존 `CategoryTreeManager.tsx`(EPIC-035)를 재사용한 `/admin/site-structure`("사이트 구성 관리")로 3개 화면을 통합 — "관리" 모달에 노출 위치/연결된 페이지/연결된 게시판(신규 `boards.topic`/`thumbnail_url` 컬럼) 편집을 추가하고, 기존 3개 목록 화면은 리다이렉트로 전환(상세 편집기는 유지). 상세는 CHANGELOG.md EPIC-077 참고.
- EPIC-066 : Board Widget을 실데이터 렌더링으로 완성 — "관리자가 만든 게시판이 Page Builder를 통해 즉시 페이지에 렌더링되는 것"이 목표. **사전 감사(사용자 확인 후 범위 확정)**: 지시문의 "Board Type 10종" 중 실제 시스템에 존재하는 건 5종(story/community/gallery/hub/timeline, `boards.board_type`이 매핑되는 값)뿐이고 Survey/Calendar/Application은 게시판 타입이 아니라 위젯 타입, Collection/Forum은 코드베이스에 존재한 적이 없어 사용자에게 확인 후 **실제 5종 기준**으로 진행하기로 함. 또한 "빈 화면"의 대부분이 렌더링 버그가 아니라 실제 게시글 0건(EmptyState가 정상 동작 중)임을 확인, 가짜 콘텐츠는 만들지 않기로 함. **① Renderer Registry**: `src/components/boards/renderers/`(StoryRenderer/CommunityRenderer/GalleryRenderer/TimelineRenderer/HubRenderer + `index.ts`의 `BoardRendererRegistry: Record<BoardLayoutType, Component>`)로 기존 `BoardRenderer.tsx`의 switch/case를 대체 — 새 레이아웃 추가 시 컴포넌트 파일 하나 + registry 한 줄만 추가하면 됨(Record 타입이라 등록 누락은 컴파일 에러로 드러남). **② 필드 보강**: 요구된 8개 필드(게시글/좋아요/댓글/작성자/조회수/태그/카테고리/썸네일)를 5개 레이아웃 전부에서 실제로 출력하도록 `CommunityListModule`/`GalleryModule`/`StoryThumbnailModule`/`TimelineRenderer`/`SlideModule`을 확장(카테고리는 게시글이 아닌 게시판 속성이라 상세 페이지의 기존 `displayTags` 관례처럼 태그 칩으로 병합, `/api/boards/feed`에도 comment_count/view_count/tags 배치 조회 추가). **③ Pagination**: `Pagination.tsx`에 페이지당 개수(12/24/48/100) 선택 드롭다운 추가(`pageSize`/`onPageSizeChange` 없으면 기존 동작 그대로, 하위 호환). **④⑤ Search/Sort**: 이미 구현돼 있었음을 코드 확인(제목/본문/작성자/태그 검색, 최신/조회/좋아요/댓글/오래된순 5종 정렬) — 변경 없음. **⑥ Filter**: Tag/Year를 `/api/boards/[id]/posts`에 실제 필터 파라미터로 추가(선택지는 필터 적용 전 원본 기준으로 계산해 다른 필터가 걸려도 옵션이 줄어들지 않음) + `FilterModule` 재사용으로 UI 연결. Category/Board Type 필터(게시판 단위 속성, `/boards` 디렉토리 수준 관심사)는 시간 제약으로 이번 EPIC에서 구현하지 못함(NEXT_TASK.md 기록). **⑦⑧ Empty/Skeleton**: `BoardEmptyState`("아직 게시글이 없습니다."/"첫 글을 작성해보세요." — 지시문 예시 문구 그대로) + `BoardSkeleton`(카드형 펄스 애니메이션)으로 기존 EmptyState/"불러오는 중..." 텍스트 대체. **⑨ 캐시**: 별도 캐싱 레이어를 두지 않는 기존 구조를 그대로 유지 — boardId/필터가 바뀔 때마다 항상 새로 fetch하므로 "즉시 반영"이 구조적으로 보장됨(React Query/SWR 미도입, 지시문의 "현재 프로젝트 구조 유지" 조건 충족). **⑩ Page Builder 연동**: EPIC-065에서 이미 완성돼 있었음을 실제 관리자 세션으로 직접 확인(아래 참고) — 추가로, 감사 중 **79개 페이지가 `PageEditButton`만 달려 있고 실제로는 `PageBuilderRenderer`를 전혀 호출하지 않아 위젯을 아무리 구성해도 화면에 영구히 반영될 수 없는 구조적 결함**을 발견(EPIC-064A가 컴포넌트만 기계적으로 부착하고 페이지 본문 로직은 안 건드렸던 결과) — 이번 EPIC의 필수 테스트 대상 중 이 결함에 해당하는 건 `/salon/event-notices`(정적 "준비 중" 텍스트) 하나뿐이라 그 파일을 형제 페이지(`community/events`)와 동일한 패턴으로 재작성해 수정, 나머지 78개는 이번 EPIC 범위를 크게 벗어나는 별도 작업 규모라 NEXT_TASK.md에 목록과 함께 기록만 함. **⑪⑫ 테스트**: 아래 "실제 출력 확인" 목록 참고 — 이번에는 관리자 세션이 실제로 열려 있어(사용자가 로그인) Visual Widget Builder 화면 자체를 직접 클릭해 확인(Board Widget Inspector의 6개 토글, Live Preview의 실데이터 렌더링, BoardEmptyState 문구 전부 실제 화면에서 확인). **⑬ 리팩토링**: `BoardService`(`src/lib/useBoardData.ts`)로 조회 로직을 뽑아내 `BoardModule.tsx`를 프레젠테이션 전용으로 축소, 파일당 최대 230줄(500줄 기준 이내 이미 충족 확인). **⑭ 성능**: `/api/boards/[id]/posts`·`/api/boards/feed` 모두 작성자/댓글 수를 배치(IN절) 조회해 N+1 없음을 재확인. **DB**: `treasures` Gallery 위젯을 실제 존재하는 유일한 관련 게시판("After Adoption 분양 후 이야기")에 연결, `gallery` 페이지의 죽은 빈 Gallery 위젯(5개 버튼 위젯과 중복) 삭제 — 둘 다 사용자 제공 Management API 토큰으로 직접 실행. **검증**: `npx tsc --noEmit`/`npm run build`/`npm run lint`(기존 30개 baseline과 동일, 신규 0건) 통과.

## 진행중
(없음)

## 예정
(비워둠)

## 규칙

- 앞으로 새로운 기능을 만들면 반드시 EPIC 번호를 부여합니다.
- 기능 완료 시 "완료"로 이동합니다.
- 진행 중인 기능은 "진행중"에 둡니다.
- 다음 작업 예정은 "예정"에 둡니다.
- CHANGELOG.md와 NEXT_TASK.md와 함께 항상 최신 상태를 유지합니다.
